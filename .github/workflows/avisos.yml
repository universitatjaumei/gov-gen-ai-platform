# Los avisos que emite GitHub, revisados solos
#
# El job `supply-chain` de `ci.yml` ya audita los cinco locks en cada commit y bloquea, con
# `avisos_aceptados.toml` como única puerta de salida. Lo que ese job **no puede** ver es lo que
# no está en el árbol: las PR de actualización que abre Dependabot y los informes de la revisión
# automática de código. Eso se quedaba por revisar cuando a alguien se le pasaba, y esto lo
# mecaniza.
#
# ─── LO QUE ESTE WORKFLOW **NO** HACE, Y POR QUÉ ──────────────────────────────────────────────
#
# Aquí hubo un tercer control: reconciliar las **alertas de la pestaña Dependabot** con las
# decisiones escritas en `avisos_aceptados.toml`, de modo que ninguna alerta se quedara abierta
# sin que alguien la hubiera decidido. Se construyó, se estrenó, y se retiró el 2026-09-23. Las
# tres cosas que lo explican, por orden:
#
#  1. **El `GITHUB_TOKEN` de Actions no alcanza a las alertas de Dependabot.** No es cuestión de
#     permisos del workflow: `security-events: read` cubre el análisis de código y los secretos,
#     no esto. La primera ejecución real sobre `main` respondió «403 Resource not accessible by
#     integration».
#
#  2. **La alternativa era un token propio**, de grano fino y con un solo permiso. Se pidió y
#     quedó pendiente de aprobación de la organización. No se insistió, por dos razones que
#     valen más que el control: no pedir una excepción a una política de credenciales que existe
#     por algo, y **conservar que este repositorio no tenga ni un secreto** — hoy despliega con
#     federación de identidad, sin claves que custodiar, rotar o revocar.
#
#  3. **Lo esencial no se pierde.** Las vulnerabilidades las sigue bloqueando `supply-chain` en
#     cada commit, auditando los cinco locks sin ningún token. Lo que se pierde es la
#     reconciliación con la pestaña: que una alerta pueda quedarse ahí sin decidir. Eso vuelve a
#     depender de que alguien mire, y conviene no disimularlo.
#
# **Lo que hizo bien mientras existió**, y por lo que se cuenta aquí: comprobaba el error de la
# llamada. Sin esa comprobación habría informado de **cero alertas en verde, para siempre**,
# dando tranquilidad falsa sobre vulnerabilidades — el guardarraíl que pasa en el vacío, en su
# peor versión. El 403 se supo porque el paso estaba escrito para no poder callárselo.
#
# Si algún día la organización admite tokens de grano fino, vuelve: está en el historial de git,
# completo y con sus tests.
#
# ─── EL CHECK DE COPILOT ES UNA FOTO, NO UNA VIGILANCIA ───────────────────────────────────────
#
# Copilot comenta cuando termina, que suele ser **después** de que CI haya pasado. Lo suyo sería
# que cada comentario y cada resolución volvieran a calcular el estado. **Se intentó, y las dos
# vías están cerradas.** Medido el 2026-09-23, y escrito aquí para que nadie lo reintente:
#
#  1. **Resolver un hilo no emite ningún evento que Actions admita.** `pull_request_review`
#     (`submitted`) y `pull_request_review_comment` (`created`) no cubren la resolución, y
#     `pull_request_review_thread` —que sí existe como webhook— **no está en la lista de eventos
#     que disparan workflows**. Ponerlo en `on:` no da error de validación: deja el workflow sin
#     arrancar, con una ejecución sin jobs marcada como fallo en cada push. Costó un commit.
#
#  2. **Los runs que dispara Copilot quedan retenidos.** Llegan como `action_required` porque el
#     actor es `Copilot` y el repositorio exige aprobación para contribuyentes externos. El ajuste
#     que lo gobierna **no es accesible mientras el repositorio sea privado**: la API responde
#     «Fork PR approval is not allowed for private repositories». Conviene revisarlo al abrirlo.
#
# Consecuencia, y hay que ser honesto con ella: en la ejecución que abre la PR este check dice la
# verdad del instante en que corre, que suele ser **antes** de que Copilot haya comentado. Se
# vuelve a calcular con el siguiente push, o a mano (`gh run rerun`). **Por eso no debe ser un
# check obligatorio**: una foto vieja no puede gobernar un merge.
#
# ─── LO QUE NO SE PUEDE OLVIDAR AL TOCAR ESTE FICHERO ─────────────────────────────────────────
#
# Que cada paso distinga **«no pude mirar» de «no hay nada»**. Una llamada sin comprobar deja la
# variable vacía y el job informa de cero en verde. Lo vigila
# `server/tests/infra/test_los_avisos_de_github_se_revisan.py`.

name: Avisos

on:
  # El despliegue: el momento en que se mira todo lo demás.
  push:
    branches: [main]
  # La puerta de mezcla, para los informes de Copilot.
  pull_request:
  pull_request_review:
    types: [submitted]
  pull_request_review_comment:
    types: [created]
  # Por si nadie toca el repositorio en dos semanas.
  schedule:
    - cron: "0 7 * * 1"
  workflow_dispatch:

# Sólo lee avisos. No tiene por qué poder cambiar nada, y no usa ningún secreto.
permissions:
  contents: read
  pull-requests: read

jobs:
  dependabot:
    name: Las actualizaciones que esperan, a la vista
    if: github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Las PR de Dependabot pendientes
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail

          # `--limit` explícito: sin él son 30 y la lista se truncaría en silencio, que en un
          # resumen cuya única función es «estén a la vista» sería peor que no mostrarlo.
          #
          # Y el `if !`, porque un fallo tragado aquí se lee exactamente igual que «no hay nada
          # pendiente», que es la única afirmación que este paso hace.
          if ! PRS="$(gh pr list --repo "$REPO" --app dependabot --state open --limit 100 --json number,title,createdAt --jq '.[] | "\(.number)\t\(.title)\t\(.createdAt[:10])"' 2>&1)"; then
            echo "::error::No se han podido leer las PR de Dependabot. Esto NO quiere decir que no haya."
            echo "$PRS"
            exit 1
          fi

          echo "## PR de Dependabot pendientes" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          # El aviso del hueco se escribe **antes** de mirar si hay PR, y por eso va aquí y no al
          # final. En la primera versión colgaba del camino «sí hay PR», de modo que el día que
          # no hubiera ninguna —el caso tranquilo, el que más se repite— el resumen habría dicho
          # «Ninguna.» y nada más: lectura de mes tranquilo para un informe al que justamente le
          # falta media foto. Lo señaló la revisión automática.
          aviso_del_hueco() {
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "> Las **alertas** de vulnerabilidad no se listan aquí: el token de Actions no" >> "$GITHUB_STEP_SUMMARY"
            echo "> alcanza a leerlas. Quien bloquea por vulnerabilidad es \`supply-chain\`, en cada" >> "$GITHUB_STEP_SUMMARY"
            echo "> commit y sobre los cinco locks. La pestaña Dependabot hay que mirarla a mano." >> "$GITHUB_STEP_SUMMARY"
          }

          if [ -z "$PRS" ]; then
            echo "Ninguna." >> "$GITHUB_STEP_SUMMARY"
            aviso_del_hueco
            echo "ninguna PR de Dependabot pendiente"
            exit 0
          fi

          # No bloquea a propósito: una actualización pendiente no es un defecto, y bloquear el
          # despliegue por ella sólo enseñaría a saltarse la puerta. Lo que hace falta es que
          # estén a la vista en el mismo sitio y el mismo momento en que se mira todo lo demás.
          echo "| PR | Desde | Título |" >> "$GITHUB_STEP_SUMMARY"
          echo "|---|---|---|" >> "$GITHUB_STEP_SUMMARY"
          while IFS=$'\t' read -r numero titulo fecha; do
            [ -n "$numero" ] || continue
            echo "| #$numero | $fecha | $titulo |" >> "$GITHUB_STEP_SUMMARY"
          done <<< "$PRS"

          aviso_del_hueco

  copilot:
    name: Los informes de Copilot están atendidos
    if: github.event_name == 'pull_request' || github.event_name == 'pull_request_review' || github.event_name == 'pull_request_review_comment'
    runs-on: ubuntu-latest
    steps:
      - name: Ningún comentario de Copilot queda sin resolver
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          if ! HILOS="$(gh api graphql -F owner='${{ github.repository_owner }}' -F nombre='${{ github.event.repository.name }}' -F numero='${{ github.event.pull_request.number }}' -f query='
            query($owner: String!, $nombre: String!, $numero: Int!) {
              repository(owner: $owner, name: $nombre) {
                pullRequest(number: $numero) {
                  reviewThreads(first: 100) {
                    nodes {
                      isResolved
                      isOutdated
                      comments(first: 1) { nodes { author { login } path } }
                    }
                  }
                }
              }
            }' --jq '.data.repository.pullRequest.reviewThreads.nodes[] | select(.isResolved | not) | select(.isOutdated | not) | select(.comments.nodes[0].author.login | test("copilot"; "i")) | .comments.nodes[0].path' 2>&1)"; then
            echo "::error::No se han podido leer los hilos de revisión de esta PR."
            echo "$HILOS"
            exit 1
          fi

          echo "## Informes de Copilot" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          if [ -z "$HILOS" ]; then
            echo "Ningún comentario sin resolver." >> "$GITHUB_STEP_SUMMARY"
            echo "ningún comentario de Copilot sin resolver"
            exit 0
          fi

          CUANTOS="$(printf '%s\n' "$HILOS" | grep -c . )"
          echo "**$CUANTOS sin resolver:**" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          printf '%s\n' "$HILOS" | sed 's/^/- /' >> "$GITHUB_STEP_SUMMARY"

          # Los hilos obsoletos quedan fuera: si el código que comentaban ya cambió, el
          # comentario dejó de describir la PR. Los vivos, no: resolverlos es el acto explícito
          # que separa «lo he leído y decido que no» de «no lo he visto».
          #
          # Y no todos los hallazgos de Copilot son ciertos —en la PR #110 uno era falso y se
          # rebatió midiéndolo contra producción—. Resolverlo también cierra ése: lo que este
          # paso exige es el juicio, no la obediencia.
          echo "::error::Quedan $CUANTOS comentarios de Copilot sin resolver en esta PR."
          echo ""
          echo "Resuelve cada hilo tras decidir sobre él —arreglándolo o rebatiéndolo—. Un hilo"
          echo "vivo y sin respuesta no distingue un hallazgo pendiente de uno que nadie miró."
          echo ""
          echo "Resolver no vuelve a ejecutar esto: no hay ningún evento de Actions que se emita"
          echo "al resolver un hilo (ver la cabecera del workflow). Después de resolver, este"
          echo "check se pone verde con el siguiente push o con \`gh run rerun --failed\`."
          exit 1

[project]
name = "script-sandbox"
version = "0.1.0"
description = "Microservicio aislado que ejecuta scripts Python aprobados en un subproceso efímero."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "pydantic>=2.13.5",
    "uvicorn[standard]>=0.53.0",
    "pandas>=3.0.5",
    "openpyxl>=3.1",
    "pdfplumber>=0.11.10",
    "matplotlib>=3.11.2",
    "seaborn>=0.13",
    "numpy>=2.5.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sandbox"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

# Historial del desarrollo — Gov Gen AI Platform

Archivo de la tabla que vivía al final de `PROJECT_STATE.md`. Se separó el 2026-08-21 porque eran
535 de sus 626 KB, y `PROJECT_STATE.md` se lee **entero al arrancar cada sesión**: pagar ese peso
para consultar un cursor que ocupa dos pantallas no tenía sentido.

Aquí no se mira el estado actual —eso está en `PROJECT_STATE.md`— sino **por qué** algo se hizo como
se hizo: qué se desvió del plan, qué defecto apareció al verificar, qué decisión se tomó y con qué
razón. Las entradas nuevas se añaden **arriba**, como siempre.

| Fecha | Prompt | Descripción |
|-------|--------|-------------|
| 2026-09-15 | DEP.7 — la puerta se enciende, pero no con el umbral que el prompt proponía | **El prompt proponía «bloquear en `high` y `critical`, informar por debajo», y lo marcaba como propuesta para discutir. Bien marcado: no es implementable.** `pip-audit` **no informa de severidad** — sus entradas traen exactamente `id`, `aliases`, `description` y `fix_versions`, y ni un CVSS ni una etiqueta. Un umbral por severidad habría tenido que inventarse el dato o dejar un **hueco silencioso**: los avisos sin severidad no bloquearían nunca, y eso no se ve en el verde. **El criterio que se aplica, decidido por el usuario sobre tres opciones medidas**: bloquea lo que **tiene corrección publicada y no está aceptado**; lo que no se puede arreglar informa. No es un apaño: «¿tiene corrección?» es un dato que la herramienta sí da, y es **exactamente la pregunta que decide si quien lee el rojo puede hacer algo**, que era la razón escrita cuando el job nació informando. En JavaScript sí hay severidad, así que `npm audit` bloquea en `high` sobre producción — medida en **cero** antes de encenderlo, así que la puerta nace en verde y lo que vigila es la deriva. **Antes de encender hubo que limpiar, y eso es medio prompt**: el árbol del servidor tenía **10 avisos en 5 paquetes, todos con corrección publicada**, aparecidos **después de DEP.2** (cinco días antes) — que es, por sí solo, el argumento de por qué la puerta hacía falta. Se subieron cuatro: `langsmith` 0.6.4→**0.12.4** (salto grande, arrastra `httpx2`, `httpcore2` y `truststore`), `langchain-classic` 1.0.4→1.0.8, `pydantic-settings` 2.14.0→2.15.0, `soupsieve` 2.8.1→2.9.2. **El quinto no se puede subir y ésa es la primera entrada del fichero de aceptados**: `setuptools` PYSEC-2026-3447 se corrige en 83.0.0 y **`torch` 2.11.0 declara `setuptools<82`**, así que subirlo exige subir `torch`. **`avisos_aceptados.toml`** exige cinco campos —`id`, `paquete`, `motivo`, `acepta`, `caduca`— y lo vigila `test_dep7_las_aceptaciones_caducan.py`, que además comprueba que el motivo diga algo (un `motivo = "no aplica"` pone rojo) y que la caducidad no pase de 190 días: **aceptar tiene que costar algo, o la lista degenera en una lista de exclusiones con mejor prosa**. **Verificado, y esto es lo que distingue el guardarraíl de la afirmación**: los dos casos negativos del test se ejecutan de verdad, la puerta se probó contra el informe **anterior** a las subidas (bloquea, 9 avisos, rc=1), contra el posterior (verde, con `setuptools` informando por aceptado) y contra un **informe que falta** (rc=1 — un informe ausente no es «cero avisos», es una auditoría que no corrió, que fue literalmente el agujero de la primera ejecución del job), y las **8 mutaciones** del job se comprobaron una a una sobre una copia en memoria del YAML. **Un detalle que costó un rojo y conviene no repetir**: el guion reventaba con `UnicodeEncodeError` al imprimir una flecha, porque la consola de Windows es cp1252. En CI (Linux, UTF-8) no habría pasado nunca — un guardarraíl que falla según la codificación de quien lo mira da un rojo que no habla de seguridad. Arreglado reconfigurando los flujos. **Suite completa: 4.445 passed, 1 skipped** (10 tests nuevos, cero regresiones pese al salto de `langsmith`). `ruff check app/` limpio. **Lo que queda y no es de agente**: el criterio de done pide una ejecución real en `desarrollo` que demuestre el bloqueo, y eso exige un push |
| 2026-09-15 | DEP.3 — `starlette` a 1.6, y una premisa que no se reproducía | **El prompt se cerró, pero lo que enseña es que su premisa era falsa.** `PROJECT_STATE.md` decía desde el 2026-09-10 que `starlette` 1.6 «deja de aplanar las rutas en `app.routes` y mete un `_IncludedRouter` sin API pública», rompiendo «10 tests en 8 ficheros», y sobre eso se decidió reescribirlos contra el cliente HTTP. **Al medir: no se reproduce.** Con `fastapi` 0.136.0 —que pide `starlette>=0.46.0` **sin techo**, así que sube sola y el diff del lock son 3 líneas— `app.routes` sigue plano: **198 rutas, todas `APIRoute`/`Route`, todas con `path`, ningún `_IncludedRouter`**, y los **14** tests que enumeran rutas pasan. Se verificaron uno a uno con `-v` y no por fichero en verde, precisamente porque la primera ejecución traía 85 *skipped* y un fichero verde no demuestra que sus tests corrieran — el mismo error de método que costó el «4.432 verdes» de DEP.2. **No se reescribió ninguno**: reescribir catorce tests verdes contra un defecto inexistente es churn, y el usuario había aprobado la reescritura sobre un diagnóstico mío que resultó equivocado. La hipótesis de dónde salía la premisa —que el intento revertido se hiciera antes de que DEP.2 subiera `fastapi`— **no está comprobada y se deja anotada como tal**, no como causa. Corrobora el diagnóstico que **`mcp_server` ya corría starlette 1.6.0 desde DEP.5** sin que nadie notara nada. **Lo que la subida sí destapó, y se arregla aquí**: starlette 1.x deprecó dos constantes que el código usaba **15 veces en 7 ficheros** (`HTTP_422_UNPROCESSABLE_ENTITY` → `..._CONTENT`, `HTTP_413_REQUEST_ENTITY_TOO_LARGE` → `HTTP_413_CONTENT_TOO_LARGE`). Se renombran las 15: los valores numéricos son los mismos, el contrato se regeneró dos veces y sale idéntico (163 paths, 274 schemas), y dejarlas era deuda con fecha de caducidad en el próximo mayor. **Medido**: los 5 avisos de `starlette` desaparecen (PYSEC-2026-161, -248, -249, -2280, -2281; 1.6.0 da «No known vulnerabilities found»); `ruff check app/` —el comando exacto de CI, no `ruff check app tests`, que lintea lo que CI no— limpio; **suite completa 4.435 passed, 1 skipped**; `uv lock --check` limpio. **Verificado en navegador**, que es lo que el criterio de done ponía por delante porque middlewares y streaming no los ve un test unitario: panel renderizando y los tres endpoints de login devolviendo 401 **a través de la cadena de middlewares**; widget compilado y servido desde un estático, con **preflight CORS (OPTIONS) en 200** cross-origin 4173→8000, pregunta real respondida con citas y anclas, y consola sin errores. El SSE se comprobó además a pelo: `text/event-stream`, `Transfer-Encoding: chunked`, **22 eventos** (3 `status`, 18 `token`, 1 `done`) y las cuatro cabeceras de seguridad presentes. **Lo que NO se verificó, y se dice**: el panel **autenticado**. No hay credencial de desarrollo documentada —`bootstrap.py` las recibe por argumento o entorno— y crear un usuario en la BD de desarrollo era un cambio que nadie pidió |
| 2026-09-14 | El cursor estaba partido en dos y DEP.4 no constaba en ningún sitio | **No mueve el cursor: lo arregla.** `PROJECT_STATE.md` tenía **dos párrafos de cursor contradiciéndose** —la cabecera decía «Bloque REPO, prompt REPO.1» (2026-09-07) y sesenta líneas más abajo otro decía «Bloque DEP, 4 de 7» (2026-09-10)—, porque el segundo se **añadió al final** en vez de sustituir al primero. Y ninguno de los dos estaba al día: **DEP.4 se cerró el 2026-09-10 en el commit `95ef392`** y no constaba ni en `PROJECT_STATE.md` ni aquí, así que el fichero que se lee entero al arrancar cada sesión decía que había un prompt esperando decisión que en realidad llevaba cuatro días cerrado, **con su medición hecha**. Tampoco existía **fila de DEP en la tabla de bloques**: el bloque nació el 2026-09-08 y vivía sólo en la sección «EMPEZAR AQUÍ», que es prosa. Los tres se arreglan: fila de DEP en la tabla con las cinco mediciones, un solo cursor en la cabecera con la regla escrita de que **se reescribe, no se añade debajo**, y el segundo párrafo reducido a lo que aportaba de historia. **La lección es de método y es la de siempre en este proyecto**: el estado no se anotó porque el prompt se cerró fuera de un bloque en marcha —DEP.4 iba aparte a propósito, por ser un cambio de mayor en un parser—, y el cierre de bloque es justo el momento en que la regla obliga a escribir. Un prompt suelto no tiene ese momento. **Y se registra aquí que el usuario ya tiene rol `admin` sobre `universitatjaumei`**, con lo que **REPO deja de estar bloqueado** |
| 2026-09-10 | DEP.4 — `camelot-py` 2.0, y `pypdf` sale del árbol entero | **Cerrado el mismo día que DEP.1-DEP.6 pero en prompt aparte, y la razón es el método**: un cambio de mayor en un **parser** cambia el texto en silencio, así que la comprobación que vale **no era la suite**. `pypdf` acumulaba **30 avisos, más que ningún otro paquete del árbol**, y parsea PDF que vienen de fuera —la superficie que un atacante elige—; no se podía subir porque `camelot` 1.0.9 lo fija con techo `<6`, y `camelot` 2.0 lo **sustituye por `playa-pdf`**, con lo que desaparece entero. Base del servidor: **199 → 197**. **La medición**: extracción comparada sobre **20 PDF reales del corpus, por la misma ruta de código que usa la aplicación** — 18 idénticos y 2 distintos. Mirados celda a celda, **en los dos la versión vieja era la equivocada**: en `es_planes_en_extincion.pdf` la tabla pasa de 18×11 a 18×13 y no son dos columnas de más, son **dos que faltaban** (cabecera de dos niveles que la 1.0.9 colapsaba, dejando el encabezado duplicado y truncado); y en `val_Normativa_de_beques…pdf` **desaparece** una tabla de 28×2 que no era una tabla, era **la portada y el índice** con la segunda columna entera vacía —falso positivo que la 2.0 rechaza bien—. Sobre 60 PDF, el triple de la muestra: 24 documentos con tabla, 25 tablas, cero errores. Comparación completa en `_local/DEP4_camelot_antes_despues.html`. **Deja un defecto anotado y deliberadamente NO arreglado aquí**, porque es de código y no de dependencias: el filtro del pipeline descarta tablas de menos de 2 columnas, y esa portada pasaba con **exactamente 2, una de ellas vacía del todo**; endurecerlo —una columna entera vacía no es una columna— es otro prompt. Suite completa: **4.435 passed, 1 skipped**; `uv lock --check` limpio |
| 2026-09-10 | Bloque DEP (4 de 7): las dependencias vulnerables, y tres que estaban por accidente | **DEP.1** saca `browser-use`, `ragas` y `datasets` a extras —ya estaban escritos como opcionales, con `try/except`, y declarados como obligatorios— y el árbol del servidor pasa de **408 a 189** dependencias; con ellos se van `authlib`, `mcp`, `langchain`, `diskcache` y **los dos únicos avisos sin versión corregida** del árbol, que no se arreglaban subiendo. **Pero lo que de verdad enseñó el prompt fue lo que destapó**: al quitarlos aparecieron **tres paquetes usados y no declarados** que llegaban de rebote como transitivas suyas —`python-multipart` (22 usos de `UploadFile`/`Form(` en 7 ficheros; FastAPI falla al ATENDER, no al importar, así que el síntoma habría sido un 500 subiendo un PDF con el arranque en verde), `langchain` (lo exige `observability.py` para el `CallbackHandler` de Langfuse) y `beautifulsoup4` (import de nivel superior en dos ficheros)— más `langchain-community`, que sostiene el proveedor Ollama desde un import perezoso sin guarda. Los tres primeros se encontraron **de uno en uno, a golpe de suite roja**, y por eso entra `test_dep1_no_hay_dependencias_de_rebote.py`, que los habría dado los cuatro juntos. **Y un error de método que conviene no repetir**: cerré DEP.2 con «4.432 verdes» y esa suite **no medía el estado commiteado** — el entorno conservaba paquetes que el lock ya no declaraba, restos de antes de DEP.1. Sólo al sincronizar con `uv sync --locked` apareció el `ModuleNotFoundError`. Es *el medidor miente antes que el sistema*, cometido sobre una cifra que parecía inmejorable. **DEP.2** sube 14 paquetes uno a uno, con `cryptography` cruzando cuatro mayores (desviación documentada: la verificación real de SAML sigue pendiente de una persona y del IdP). **DEP.5** deja `mcp` en 1.30 y mete el techo `<2` **en el manifiesto**, porque 2.x renombra `FastMCP` a `MCPServer` y sin techo el próximo `upgrade` rompe el servidor MCP en silencio. **DEP.6** descubre que cuatro herramientas de construcción estaban en `dependencies`: reclasificarlas, **sin subir nada**, quita 16 de los 21 avisos, y `npm audit fix` deja el árbol de producción en **cero**. **DEP.3 revertido**, esperando criterio; **DEP.4 se cerró el mismo día**, en prompt aparte y con su medición sobre 20 PDF reales — fila propia arriba |
| 2026-09-08 | Bloque DEP escrito: las dependencias vulnerables, medidas y no supuestas | **Nace de la primera ejecución del job `supply-chain`** (run `34214582147`), no de una sospecha: 32 avisos en `server`, 4 en `mcp_server` y 21 en el frontend, 13 altos. Lo que reordenó el bloque fue **el mapa de padres**, porque casi todo es transitivo y el arreglo no es editar `pyproject.toml` sino `uv lock --upgrade-package`. Y ahí aparecieron dos cosas que no se veían. **La primera es un defecto latente**: `python-multipart` no está declarado en ningún sitio y llega por `browser-use → mcp`, mientras la aplicación usa `UploadFile`/`Form(` **22 veces en 7 ficheros**; FastAPI no falla al importar por eso, falla al atender la petición, así que el síntoma sería un 500 subiendo un PDF en producción y no un arranque en rojo. **La segunda es que `browser-use` y `ragas` están escritos como opcionales y declarados como obligatorios** —los dos importan dentro de un `try/except ImportError` y degradan a propósito— y entre los dos arrastran ~19 de los 32 avisos, incluidos **los dos únicos sin versión corregida** (`diskcache` y el propio `ragas`): no se arreglan subiendo, sólo dejando de traerlos. De ahí que DEP.1 sea quitar masa y no subir versiones. **Corregido de paso un error dicho ese mismo día**: `authlib` se citó como «la pieza de SAML/OIDC» y no lo es —el SAML es `python3-saml` + `xmlsec` + `lxml`—, `authlib` entra por `browser-use`. Dato que conviene no leer al revés: `mcp_server` va **más adelantado** que `server` (cryptography 48 contra 46, starlette 1.3 contra 0.50). **DEP.7 cierra el ciclo** y sin él el bloque deja el árbol limpio hoy y sucio en tres meses: convierte el job de informar en bloquear, con fichero de aceptaciones que llevan motivo, responsable y **fecha de caducidad**, y un test que se pone rojo cuando una caduca — sin esa fecha, aceptar es gratis y para siempre |
| 2026-09-07 | La hoja de validación RAG/agéntico dice ahora que la comparación es asimétrica | **Hallazgo del triaje del mismo día, arreglado.** `VALIDACION_GERENCIA_RAG_VS_AGENTICO.html` presentaba las respuestas de los dos asistentes para que los informadores dieran su veredicto, y **no decía en ninguna parte que los dos no están configurados de forma comparable** —cero menciones al reordenador, al `score` fijo o a la asimetría—. La asimetría es **de construcción y no del lote**: el reranker actúa sólo en uno, y el otro puntúa cada evidencia con un valor fijo, así que su puerta de calidad **no descarta nada con ningún umbral** y por eso contesta siempre. Sin ese matiz, el resultado se lee al revés: como si dijera que una técnica es mejor que la otra, cuando lo que dice es cuál de **dos configuraciones concretas** responde mejor a siete preguntas. Añadida una salvedad visible en la introducción, con estilo propio (`.salvedad`, sobre las variables `--medio` que el fichero ya traía para advertencias) y tres párrafos: qué no se puede concluir, qué sí sigue valiendo el veredicto, y que **no cambia nada de lo que el informador tiene que hacer** —las respuestas siguen sin identificar y el aviso no delata cuál es cuál, cosa que se comprobó al redactarlo porque habría invalidado la hoja—. **El fichero no se publica** (sale del repositorio en el paso 5.bis de REPO.1, por ser un instrumento y no un informe), así que esto es para uso interno: quien lea esos veredictos dentro de seis meses necesita el matiz tanto o más que quien los emite hoy. Verificado con `node --check` sobre el JavaScript extraído —**sintaxis OK**— en vez de dar por bueno el `html.push`, y comprobado que el guardarraíl de REPO.5 sigue en cero: el texto nuevo no introduce ningún identificador de esta instalación. Contexto de la asimetría en la memoria del proyecto sobre los chatbots de Gerencia |
| 2026-09-07 | `docs/mediciones/`: las instantáneas fechadas dejan de estar sueltas en la raíz | **Propuesta del usuario** al decidir publicar los tableros: una carpeta para mediciones y experimentación, porque «de mediciones haremos más y el proyecto es experimental». Acertada, y la comprobación la reforzó más de lo esperado: **los doce `.html` de `docs/` no estaban en el índice**. O sea que hoy no eran «documentación menos estable», eran **ficheros sueltos** que sólo encontraba quien supiera el nombre. El criterio que la justifica es que `docs/` mezclaba dos cosas que **envejecen distinto**: referencia que se mantiene, e instantáneas que no se tocan nunca más. **Cuatro decisiones de forma, y la segunda es la que de verdad hacía falta.** (1) `docs/mediciones/`, porque los ocho son mediciones —incluido `CONFIGURACION_APERTURA_PILOTO`, que es «con qué abre el piloto y **qué medición decidió cada valor**»—: experimentación y configuración son lo que la medición justifica. (2) **Fecha en el nombre** (`AAAA-MM-DD_NOMBRE.html`): sin ella `MEDICION_RETRIEVAL_TOP_K.html` ocupa el nombre bueno y la segunda ronda no tiene dónde ir sin inventarse un sufijo o pisar la anterior; con ella la carpeta se ordena sola. Las fechas salen del **primer commit** de cada fichero (25–28 de agosto, el periodo del bloque HIB); se descartó la fecha «interna» de `CONFIGURACION_APERTURA_PILOTO` porque era un falso positivo, un dato dentro del fichero. (3) Un **índice propio** que dice lo que más importa al publicarlas: son instantáneas, **no se mantienen**, y dónde está lo vigente — sin esa nota alguien lee una medición de hace seis meses como si describiera el sistema de hoy. (4) Un **guardarraíl**: ningún `.html` suelto en la raíz de `docs/`, con los cuatro que salen en REPO.1 como **excepción fechada que el paso 5.bis vacía**; es lo que hace que la próxima medición caiga en la carpeta sin que nadie se acuerde, que es exactamente como llegaron a ser doce. **El guardarraíl de enlaces se puso rojo y tenía razón**: `docs/README.md` enlazaba el índice nuevo antes de que estuviera en git, y como pregunta a `git ls-files` y no al disco, lo cazó — la misma propiedad que se arregló esta mañana, funcionando. **Y una trampa anotada en el plan**: al verificar el filtro de 5.bis, **tres de las siete rutas no existen en `git ls-files`** y es correcto —el trío de marca salió del árbol pero sigue en el historial, que es sobre lo que actúa `filter-repo`—; quien coteje la lista contra el árbol y «arregle» lo que no resuelve, borra tres entradas buenas y publica el logotipo. Se comprueba con `git log --all -- <ruta>`. `ruff` limpio; **16 passed** en los cuatro guardarraíles de documentación |
| 2026-09-07 | Se publican ocho de los once tableros; los otros tres no son documentos | **Decisión del usuario, condicionada**: «no hay problema en publicar los tableros, si tienen interés». O sea que la pregunta era cuáles lo tienen, y eso hay que **abrirlos** para saberlo — que es justo la lección que este mismo día costó tres ficheros mal clasificados. **Ocho sí, y bastante**: `CALIDAD_RESPUESTA_TOP_K` («redacta mejor con menos documentos»), `DIAGNOSTICO_POR_QUE_NO_CONTESTA` («y por qué no es la anchura»), `GERENCIA_TOP_K_Y_UMBRAL` («lo calla el umbral»), `EXPERIMENTO_GRANULARIDAD_CONTEXTO` («qué puede decidir un lote de 25»), más `MEDICION_RETRIEVAL_TOP_K`, `BLOQUE_HIB_CIERRE`, `GERENCIA_CIERRE_BLOQUE_HIB` y `CONFIGURACION_APERTURA_PILOTO`. Son hallazgos útiles a cualquiera que monte un RAG en administración pública y **la evidencia de la tesis del proyecto**. **Tres no, y el motivo no es sensibilidad: es que no son documentos.** Los `VALIDACION_GERENCIA*.html` renderizan `p.pregunta` de un array: son las **hojas de anotación** que se envían a los informadores, con salida del modelo **sin validar** pendiente de veredicto humano. Publicar salida sin validar como si fuera un documento induce a error, y un instrumento no tiene lector fuera de su proceso; si algún día se publica el **resultado** de esas validaciones, será otro documento. **Y por poco repito el error del día**: iba a mandar fuera `GERENCIA_TOP_K_Y_UMBRAL` y `GERENCIA_CIERRE_BLOQUE_HIB` por llamarse «Gerencia», y al abrirlos resultaron **metodológicos** —el primero lleva la sección «Por qué el agéntico contesta a todo», que **es** la advertencia de que la comparación RAG/agéntico es asimétrica por construcción—. **Un hallazgo de paso**: `VALIDACION_GERENCIA_RAG_VS_AGENTICO.html` documenta esa comparación y **no lleva la advertencia** (cero menciones a la asimetría, al reranker o al `score` fijo); no se publica, así que no urge, pero conviene añadírsela también para uso interno, porque una comparación sin ese matiz se lee al revés. **Y una sospecha mía que era falsa**: creí ver un fallo de renderizado en `VALIDACION_GERENCIA.html` («`" + esc(TITULO) + "`») y es **JavaScript dentro del fichero**, línea 490; se ve bien en el navegador. El filtro de historial de REPO.1 baja de **15 rutas a 7**: los tres instrumentos, el trío de marca y la revisión de literatura. Guardarraíles: **235 passed** |
| 2026-09-07 | REPO.5 — el procedimiento se publica, los valores de esta casa no | **Adelantado a propósito**, y la razón sale de la revisión del reparto: se planificó después de REPO.1 «para no reescribir dos veces los mismos documentos», pero al reclasificarse `RUNBOOK_REINGESTA.md` como público **ninguno de los documentos de este prompt quedó en la lista de rutas filtradas**, así que la dependencia desapareció. Hacerlo ahora evita que la documentación escrita durante la espera de la organización vuelva a meter valores reales — el usuario preguntó justamente si REPO se quedaría obsoleto esperando, y ésta era la parte que sí se degradaba. **Cinco documentos y no cuatro**: `RUNBOOK_REINGESTA.md` entró al reclasificarse. Sustituidos **20 apariciones de seis identificadores** —proyecto, instancia de Cloud SQL, VM, dos *buckets* y el host provisional— por marcadores, más una **tabla nueva de «qué valor va en cada marcador y de dónde sale»**, que es lo que convierte el documento en reutilizable de verdad: sin ella, quien lo siga tiene que adivinar qué sustituir. **La distinción que gobierna la lista de excepciones es la misma que AIS.2 usa entre código y comentario**: un **valor copiable** se sustituye —quien copiara el `data-api-url` de `WIDGET_INCRUSTACION.md` estaría apuntando su widget **a nuestra API**— y una **procedencia de medición** se conserva, porque «verificado el 2026-08-15 contra el proyecto `<PROYECTO_GCP>`» no dice nada y el valor del dato está en poder ir a comprobarlo; por eso `DECISION_MODELOS_EMBEDDING_RERANKER.md` va a `_EXCEPCIONES` con su razón. **No se generalizan** `normativa.uji.es` —dominio público del servicio, que aparece hasta en las citas del corpus— ni `europe-southwest1`, que es un dato de GCP y no una identidad: un documento que diga «Madrid» es más útil que uno que diga `<REGION>`. **Y aparecieron tres datos que habían dejado de ser ciertos**, que es lo que de verdad justifica el prompt: `DESPLIEGUE_PROTOTIPO_GCP.md` decía **«Tres imágenes»** (son cuatro desde REG.4, que añadió `mcp`), **«Diez» variables** (son doce, medidas con `gh variable list`) y documentaba **un solo anclaje de WIF** cuando son **dos** —la condición del proveedor y el `principalSet` del enlace de la cuenta de servicio—. Quien siguiera el documento con uno solo se quedaría a medias, y el síntoma —la autenticación falla— no señala a lo que falta; ahora hay una subsección con los dos y con el procedimiento aditivo para cambiar de dueño sin ventana de rotura. **Y un fallo de mi propio test, cazado antes de darlo por bueno**: el de las variables pasó en el primer RED porque el patrón buscaba «variables» en minúscula y el documento dice «Variables»; con `re.IGNORECASE` los cuatro se pusieron rojos como debían. `ruff` limpio; **`tests/infra` 690 passed, 33 skipped**, con los dos rojos de entorno ya inventariados (demonio de Docker parado) y `test_d0doc_el_despliegue_que_la_documentacion_promete.py` —el que cruza este documento con la realidad— en verde |
| 2026-09-07 | El reparto de `docs/` revisado: no hay repositorio privado, y tres ficheros estaban clasificados por su nombre | **Corrección de la fila anterior, y la pidió el usuario preguntando lo correcto**: si 15 ficheros justifican mantener dos repositorios, y si un repositorio privado de operación es un patrón habitual. **Sí lo es, pero para otra cosa**: código y configuración que el equipo trabaja —Terraform, Ansible, estado de entornos, registro de incidencias—. Quince documentos estáticos, once de ellos instantáneas congeladas, no son un repositorio de operación: son **un archivo disfrazado de repositorio de operación**. **Y el argumento con el que lo defendí no aguanta**: dije que un Drive «pierde el historial» y que los tableros valen por poder compararse con la medición de hace un mes, pero son **instantáneas fechadas e inmutables** —cada fichero **es** el registro, y una medición nueva produce un fichero nuevo—, así que git no aporta casi nada ahí. Lo afirmé con más seguridad de la que tenía. **Había además una incoherencia de fondo**: a la pregunta del *fork* institucional respondí «no lo crees hasta que tenga peso, hoy estaría vacío», y el mismo criterio se aplica aquí y no lo apliqué. **Y el error más instructivo: tres ficheros clasificados por su NOMBRE y no por su contenido, los tres publicables.** `DATOS_DEL_PILOTO.md` no son datos del piloto, es el documento de diseño que distingue catálogo de producto de datos operativos y explica cómo viaja cada uno; `RUNBOOK_REINGESTA.md` es un procedimiento —«los comandos exactos, en orden, con la puerta de comprobación delante»—; y `CASO_CURACION_ESCOLA_DOCTORAT.md` es un caso guía con los umbrales que hubo que corregir, escrito «para repetirlo en otro apartado sin volver a descubrirlo todo». Los tres llevan valores reales, y eso lo arregla **REPO.5** generalizándolos, no sacarlos del repositorio. Clasificar por el nombre del fichero llevó a un reparto **tres veces más grande de lo necesario**. **El reparto que queda**: fuera del repositorio, **15 rutas** a `_local/docs_operacion/` + Drive —los once tableros, el trío de marca y la revisión de literatura—, y **sin repositorio privado**, que se creará cuando tenga peso real, con el mismo test que el *fork*. Los once tableros salen **no porque no puedan publicarse** —comprobado: cero datos personales, cero credenciales, cero valores de configuración, y para un proyecto que se defiende sobre «garantía → mecanismo → precio» publicar cómo se mide es un **activo**— sino porque recogen **juicios de calidad sobre un servicio en uso**, que es decisión de la institución y no de un triaje documental; y porque **publicar es la dirección irreversible**: abrirlos mañana es un commit y cerrarlos cuesta una reescritura de historial. La revisión de literatura no es ni pública ni de operación: material de investigación sin publicar. El paso 3.bis deja de crear un repositorio y pasa a ser «copiar a `_local/` **y al Drive** antes de filtrar», con el aviso de que `_local/` está ignorada y no la respalda nadie. El filtro de 5.bis baja de 18 rutas a **15**, verificado contando. **Y queda anotada la decisión que simplificaría esto del todo**: si la institución publica los tableros, el filtro baja a 4 rutas. Guardarraíles: **253 passed** |
| 2026-09-07 | El reparto de `docs/` pensando en abrir, y la marca fuera de todo el árbol | **Salió de una revisión del usuario**: en `docs/` había ficheros del despliegue de la UJI y tableros de experimentos que no tenía claro que debieran ir a un repositorio público. **La pregunta previa tiene respuesta rotunda: GitHub no permite mezclar visibilidad dentro de un repositorio** —no hay carpetas privadas, las ramas la heredan, un *Project* es un tablero y no almacena ficheros, y un submódulo privado sigue siendo otro repositorio y rompe el clon de quien no tiene acceso—. **Decidido repositorio privado de operación**, no `_local/` + Drive: un Drive pierde el historial —y estos tableros valen precisamente por poder compararse con la medición de hace un mes— y una copia manual deriva sin que nadie sepa cuál es la última. **El criterio no se inventa: es principal contra despliegue, el mismo que ya rige el código.** Al privado, **15 ficheros**: los once tableros de medición y experimento, `DATOS_DEL_PILOTO.md`, `RUNBOOK_REINGESTA.md`, `CASO_CURACION_ESCOLA_DOCTORAT.md` y el trío de marca. **Medido antes de decidir, y el resultado cambia el motivo**: los once `.html` **no llevan datos personales ni credenciales** —cero correos, cero DNI, cero valores de configuración—, así que no es un problema de protección de datos: son historia operativa de una institución, que es otra cosa. `LITERATURA_ASISTENTES_NORMATIVA.html` no es ninguno de los dos: es material de investigación y su sede es donde vivan los papers. Y **`DESPLIEGUE_PROTOTIPO_GCP.md` no se retira, se generaliza** (REPO.5): el procedimiento es de lo más valioso que se puede publicar porque es justo lo que una entidad local no sabe hacer; lo que se va son los valores, que es **lo que el código ya hacía y la documentación nunca recibió**. **La consecuencia que el usuario no había considerado, y que fija el momento**: la visibilidad alcanza al historial, así que borrar un fichero antes de abrir **no lo saca del pasado**. Medido: de **707** commits los tableros tocan 33, `chatbots-publicos/` 5, `DATOS_DEL_PILOTO.md` 2 y `RUNBOOK_REINGESTA.md` 1, así que el filtro es quirúrgico — y la ventana es REPO.1, que ya reescribe y empuja desde cero. Hacerlo después cuesta otra reescritura y otro periodo de objetos huérfanos servidos por SHA. Añadidos a REPO.1 los pasos **3.bis** (crear y sembrar el privado **antes** de filtrar: si se filtra primero y algo falla, esos 15 sólo estarían en el bundle) y **5.bis** (el `filter-repo` con las 18 rutas, **en un clon de trabajo** porque reescribe todos los SHA, con tres comprobaciones: las rutas desaparecidas de todo el historial, la suite verde, y que el recuento de commits no se desplome —si cayeran decenas, un `--path` cazó más de lo previsto—). **Y el guardarraíl de marca extendido a todo el árbol**, que era el encargo explícito: los dos que había miraban **sólo el frontend**, y por ese hueco `marcauji.png` llevaba versionado desde el 21 de agosto. Lo instructivo es **por qué entró**: lo puso el arreglo de `test_los_html_de_docs_no_apuntan_a_imagenes_fantasma.py`, que detectó que `demo-uji.html` apuntaba a un logotipo fuera del repositorio; **un guardarraíl exigió lo que otro habría prohibido**, y la contradicción no se vio porque el segundo no existía. La resolución no es aflojar ninguno: **el trío sale junto** a `_local/docs_operacion/`, y entonces los dos quedan satisfechos. El patrón busca **por subcadena y no con `\b`**, porque `marcauji` pega el nombre a otra palabra —el mismo defecto que dejó pasar `UjiMergeStrategy`—; medido sobre `git ls-files` completo caza **exactamente tres ficheros y ninguno más**, y pregunta a git y no al disco. Segundo test hermano: **ninguna imagen versionada bajo `docs/`**, que hoy pasa porque no queda ninguna. El bloque pasa de 2 a **5 prompts** y el total de 49 a **52**; orden **REPO.1 → REPO.4 → REPO.5 → REPO.2**. Guardarraíles: **255 passed** |
| 2026-09-07 | El guardarraíl de REPO.3 pasaba en local y CI lo tumbó: comprobaba mi disco, no el árbol | **Rojo en CI de mi propio test, y la causa es del género que este proyecto persigue.** `test_repo3_el_indice_de_docs_no_miente.py` usaba `Path.is_file()` para resolver los enlaces de `docs/README.md`; en local pasaba y en CI señaló dos: `EU_GOVERNANCE_CONCEPT_NOTE.md` y `EU_GOVERNANCE_TOPICS.md`. **Existen en el disco del mantenedor y no en el repositorio**, porque están excluidos en **`.git/info/exclude`** —que es por clon, no está versionada y no viaja—. O sea que el test **pasaba por el entorno y no por el árbol**: [[feedback_el_medidor_miente_antes_que_el_sistema]] otra vez, y esta variante es peor que un guardarraíl que no mira, porque este miraba el sitio equivocado y daba verde con autoridad. **Arreglado preguntando a `git ls-files`** en vez de al sistema de archivos, con el porqué escrito en el helper: la propiedad que importa no es «el fichero está en mi disco» sino **«lo tiene quien clona»**. Comprobado que el arreglo funciona de verdad y no sólo que los tests pasan: `EU_GOVERNANCE_TOPICS.md` da `en_disco=True, en_repositorio=False`, que es exactamente la discrepancia que lo hacía mentir. **El defecto que había detrás era real**: el índice versionado de `docs/` anunciaba dos documentos que ningún clon salvo uno tiene, así que en la portada de `docs/` había dos enlaces muertos para todo el mundo —y el repositorio se va a abrir—. Sustituidos por una nota que dice que no están versionados y por qué. Quedan **tres menciones en prosa** a esos ficheros (`MARCO_GOBERNANZA_IA.md` ×2 y `PRESENTACION_PROYECTO.md`) que **no se tocan**: van en comillas invertidas y no son enlaces, así que ningún test de enlaces las ve, y decidir si esos dos documentos se publican o se dejan fuera es del usuario. **Y de aquí sale un riesgo concreto para REPO.1**, anotado como paso 0.bis: `.git/info/exclude` **no viaja al clon nuevo**, y hoy protege tres ficheros —los dos del proyecto europeo y **una convocatoria de plaza**—; en el clon nuevo un `git add -A` los publicaría. Hay que decidir por cada uno `.gitignore` (que sí viaja) o moverlos a `_local/` **antes** de empujar. Verificación: `ruff` limpio en `app/` y en el test; **229 passed** en el guardarraíl y el índice de planificación |
| 2026-09-07 | REPO replanificado a la variante B, y el cursor entra en el bloque | **Decisión del usuario**: el repositorio limpio **nace en `universitatjaumei`** en vez de recrearse en la cuenta personal y transferirse. **Y conviene decirlo con precisión porque el nombre engaña: con B no hay transferencia**, no se usa la operación «Transfer» de GitHub en ningún momento. Eso es exactamente lo que hace que nazca limpio —una transferencia se lleva el almacén de objetos **completo**, así que con la variante A los 46 volcados entrarían en la organización de la universidad y sólo morirían al borrar el original—. Con B no entran nunca. **Tres consecuencias en la planificación.** (1) **Los pasos de REPO.1 se reescriben**: un paso 0 con las tres preguntas a UADTI que no se averiguan desde fuera —rol y quién aprueba la creación, **permisos base de la organización** y plan—, apuntar las 12 variables antes de nada, **no renombrar el viejo** (en B se queda sirviendo hasta el final: es la red), **ampliar WIF a los dos nombres antes de empujar** con los dos comandos escritos —proveedor y `principalSet`—, subir las dos ramas, y **un despliegue real desde el nuevo como comprobación de cierre**, que es lo único que demuestra que WIF acepta el nombre nuevo. El borrado del viejo pasa a ser el paso 7 y se dice explícitamente que **es ahí donde mueren los 46 volcados, y no antes**; estrechar WIF, el 8. (2) **Aparece REPO.4**, que con la variante A no hacía falta porque el nombre acababa siendo el mismo: al cambiar el dueño, el repositorio **se nombra a sí mismo en seis sitios** —`CODEOWNERS` con 14 entradas, los dos `Repository =` de los `pyproject.toml`, `CONTRIBUTING.md`, `DESPLIEGUE_PROTOTIPO_GCP.md` §177 y `PLAN_DESARROLLO.md`—, y `deploy.yml` **no** lleva el nombre, comprobado. Sus dos tests RED son «ningún fichero vivo escribe el dueño anterior» y **«`CODEOWNERS` no asigna a una persona sino a un equipo»**, que es lo que impide que la retirada se haga cambiando un nombre propio por otro: una persona no es un mantenedor sostenible para un repositorio institucional. Y lleva una advertencia de fondo: al reescribir la tabla de `CONTRIBUTING.md` **hay que conservar que no hay fork privilegiado, «incluida la universidad donde nació»** — que el principal viva en la organización de la UJI no le da privilegio, y si el texto deja de decirlo la gobernanza se lee como que la UJI dirige. (3) **El cursor entra en el bloque REPO, prompt REPO.1**, que no es de agente. Orden: **REPO.1 → REPO.4 → REPO.2**. El bloque pasa de 2 a 4 prompts y el total de 49 a **51**. Registrado además en el cursor el **modelo de gobernanza acordado** —un solo repositorio y no un fork institucional, con lo específico de la UJI en variables, Secret Manager y filas— y que **la AGPL ya garantiza el uso** por irrevocabilidad (§2), así que lo que hay que proteger con arquitectura es la **dirección**, no el uso. Guardarraíles de planificación y documentación: **329 passed** |
| 2026-09-07 | REPO.3 — El triaje editorial de `docs/`, y REPO.1 puesto al día | **Dos cosas, y la primera cambia el guion del bloque.** *Plan al día*: el inventario de REPO.1 se escribió el 2026-08-21 y desde entonces pasaron el despliegue (31-08) y la rama `desarrollo` (02-09), así que decía tres cosas que ya no valían. **(a) Las 12 variables de Actions se perderían** —el inventario dice «secretos: ninguno», cierto, pero no menciona variables porque no había; hoy `deploy.yml` lee `vars.GCP_WIF_PROVIDER` y once más, y un repositorio nuevo nace vacío: con eso vacío la autenticación falla de un modo que **no se parece a su causa**—. **(b) Sólo subía `main`**, dejando fuera donde vive el trabajo. **(c) El SHA que mandaba comprobar no demostraba nada**: medido caminando la cadena de huérfanos en GitHub, el árbol de `82f475b` **no tiene `logs/`** —es la punta posterior al borrado y anterior a la reescritura—, así que su `contents/logs` da 404 aunque la exposición siga viva; el primer ancestro que **sí** sirve los volcados es **`dc4904e0c763`** (06:20:35) con **46 ficheros**, y por debajo `152c3d2f3f2e` con otros 46. O sea que la comprobación de cierre medía lo que no era. **Y la exposición sigue viva**: 17 días después de la reescritura GitHub no ha recogido basura. Añadida la variante **B** —crear el limpio **directamente en la organización** en vez de recrear y transferir—, recomendada porque el repositorio de la universidad **nace limpio** y los 46 volcados no entran nunca en su almacén de objetos; su único coste son los dos anclajes de WIF, que se mueven de forma aditiva y sin ventana de rotura. *Triaje*: **cuatro instantáneas retiradas** —`CAMBIOS_ARQUITECTURA.md` y `CAMBIOS_PLANIFICACION.md`, que son **encargos escritos a un agente** y no fotos de estado; `PRUEBAS_PENDIENTES.md`, sustituido por el `.bat` por bloque; y `PLAN_CHATBOTS_E_INGESTA_LOCAL.md`, cuyo corpus ya está ingerido— y **`mcp.md` unificado** en `MCP_SERVER.md`, que gana una §8 con la procedencia y una tabla de dónde se contestó cada una de sus tres incógnitas (PAT revocable, `confirm=true`, y la opción B que implementó REG.4). **Lo que se conserva y por qué**: las tres `COMPARATIVA_*_LEGACY` **porque ese código ya no está en el árbol** y son el único registro dentro del repositorio de lo que el legacy tenía mejor resuelto; `COMPARATIVA_RAG_LAMB` y las dos auditorías, como instantáneas fechadas. Y **`DECISION_OPENWEBUI_CARCASA_CHAT.md` se queda: desviación documentada**, porque desde que se escribió REPO.3 existe `docs/DECISIONES.md` con su guardarraíl y ese fichero es su **decisión nº 1**; retirarlo rompería el registro. **Antes de retirar nada se extrajo lo abierto, y medirlo fue la mitad del trabajo**: de las siete deudas de `VALORACION_PROYECTO.md` §2.3, **seis están cerradas** (`asyncio.to_thread` en `to_pdf`, el `create_all` del arranque por BD.2, el fallback de `DATABASE_URL` que ahora falla duro, los 45 fragmentos en diferido, `client_app/` por NIC.3, y el mapa de anonimización que AIS.5 **retiró por decisión** del 2026-08-24) y **sobrevive una que ha empeorado**: los `print()` de `server/app/` eran 157 y son **186**. De `PRUEBAS_PENDIENTES.md` sólo seguía abierta la verificación manual del Camino 3 —el 500 de `templates`, el 403 del propio workspace y el `data-token` del widget están arreglados, comprobado uno a uno—. Las dos van a `PROJECT_STATE.md`; el informe se queda con una **segunda nota de estado** que dice qué de su §2.3 ya no es cierto, en vez de reescribirlo, que es lo que le quitaría el valor de auditoría. **Tres tests, y uno cazó mi propio despiste**: el índice sin enlaces rotos y «ningún documento activo cita uno inexistente» se escriben como **propiedad permanente y no como lista de retirados**, para que no caduquen en el siguiente triaje — y el segundo se puso rojo señalando que `MCP_SERVER.md` seguía enlazando `mcp.md` en su cabecera. Reapuntadas además cuatro menciones en prosa que no son enlaces y por tanto ningún test ve: en `EVOLUCIO_I_ASPECTES_PENDENTS.md` (dos), `mcp_server/http_server.py` y `app/core/auth/pat/scopes.py`. **`tests/infra` 684 passed, 33 skipped**; los dos rojos son de entorno —**Docker instalado pero con el demonio parado**— y ninguno toca documentación. De paso, un defecto del mismo género: el `skipif` de `test_caddy_valida_la_configuracion_compuesta` comprueba `shutil.which("docker")`, o sea el binario, y no que el demonio responda, así que en vez de saltarse falla. `ruff check app/` limpio |
| 2026-09-07 | AIS.2 — el guardarraíl no veía el CamelCase, y tres clases se llamaban `Uji*` | **No mueve el cursor**: cierra un incumplimiento de AIS.2 que llevaba abierto desde 9B.11 (2026-05-11) con el guardarraíl **en verde**. Salió al analizar, a peticion del usuario, qué parte del reparto principal/despliegue se cumple de verdad. **La causa es el patrón, no el olvido**: `_INSTITUCIONES` es `\b(uji\|jaume\|castell[oó]n?\|innovap\|generalitat)\b` y se aplicaba a la línea cruda, así que en CamelCase **no hay frontera de palabra** y `UjiMergeStrategy` pasaba —comprobado ejecutando el propio patrón contra los tres nombres: PASA, mientras `source_type == "uji"` y `fabra@uji.es` sí CAZAN—. Es la variante de «el medidor miente antes que el sistema» aplicada a un guardarraíl: pasaba **por no mirar**, no por estar limpio. **Se arregla la entrada y no el patrón, a propósito**: quitar el `\b` final haría casar «castellano», que es el nombre de una lengua y sale por todo el código, y a partir de ahí el guardarraíl se desactivaría a base de excepciones. Se separa el CamelCase antes de buscar, con el par estándar de transiciones —minúscula→mayúscula y fin de acrónimo, que sin la segunda dejaría escapar `UJIMerge`— y se **reporta la línea original**, porque quien lea el fallo necesita ver el identificador tal como está escrito. Con eso el barrido general se puso rojo señalando exactamente tres líneas y **ninguna más**, que es el dato tranquilizador: no había otros nombres institucionales escondidos en `app/`. Renombradas siguiendo la convención descriptiva del perfil hermano (`public_kb_rich.py`: `SingleSourceRetrievalStrategy`, `PassthroughMergeStrategy`, `GenericAnswerTemplateStrategy`), no la del perfil: `UjiDualSourceRetrievalStrategy`→**`DualSourceRetrievalStrategy`** (que empareja con `SingleSource*`), `UjiMergeStrategy`→**`PrimaryFirstMergeStrategy`**, `UjiAnswerTemplateStrategy`→**`TwoSectionAnswerTemplateStrategy`**. **Era nombre y no arquitectura**, y conviene dejarlo escrito: la lógica ya era genérica —dos fuentes, fusión con una preferente, plantilla de dos secciones— y los dos chatbots de origen **se inyectan por constructor**, así que de quién son las fuentes ya era configuración. También el fichero de tests (`test_uji_aggregator.py`→`test_public_portal_aggregator.py`, con `git mv`), sus métodos, el `answer_template="uji"` del fixture —campo `str(50)` libre con defecto `generic`, así que el valor era arbitrario— y dos docstrings: el del perfil y el de `normativa_spider.py`, que anunciaba «fuentes BOE, DOGV y UJI». Los registros históricos (`HISTORIAL.md`, `12_FASE_9B.md`, `VALORACION_PROYECTO.md`) **se dejan como están**: documentan lo que pasó. **Dos hallazgos que NO se tocan y quedan anotados.** (1) El perfil está en `PERFILES_SIN_CONFIGURAR` y su factory lanza `NotImplementedError` porque «no hay dónde declarar de qué dos chatbots agrega», y `graph_factory` **ni siquiera importa** las tres clases: son **inalcanzables**, así que por «sin código muerto especulativo» cabría borrar el perfil entero en vez de renombrarlo. Es decisión del usuario y excede lo que pidió. (2) `test_should_not_ship_a_spider_for_one_institution_portal` **promete más de lo que comprueba**: mira nombres de institución en el código del spider, y `procedimientos_spider.py` los tiene sólo en prosa mientras su carga institucional real son los **selectores** (`h1.proc-titulo`, `span.proc-codigo`…) y los campos del `dataclass`. Es el hallazgo #3 de AIS.2, todavía abierto, y su plan es **PRC**, que se replanificó para que «la plataforma no conozca ni un campo» del catálogo. Verificación: `tests/public_graphs` + AIS.2 **205 passed, 6 skipped**; `ruff check app/` limpio **con el comando de CI** —`ruff check .` daba 231 errores de `tests/` y otros directorios que CI no linta, preexistentes y ajenos a esto— |
| 2026-09-07 | (despliegue — LANG + REG + VAS + NIC + BD pasan a `main` y a producción) | **No mueve el cursor**: no implementa nada, despliega lo que ya estaba cerrado. Los **38 commits** que `desarrollo` acumulaba desde el 2026-09-02 pasan a `main` en **avance rápido** —`main` no tenía nada propio, así que el historial sigue lineal— y `deploy.yml` construye y publica las cuatro imágenes con la etiqueta `2518a46`. **Comprobado antes de empujar, y no todo estaba cubierto por CI**: CI verde sobre exactamente ese SHA (sus tres jobs: `test`, `contract`, `accessibility`), cabeza única de Alembic, y los dos ficheros de bloqueo cuadrando con su manifiesto (`uv lock --check` en `server/` y `mcp_server/`, que es lo que dice la verdad y no `uv sync` a secas). **Lo que hubo que mirar a mano es lo que CI no construye**: NIC.4 retiró `pyproject.toml` y `uv.lock` de la raíz, y el `Dockerfile` de la raíz es el que el despliegue construye —resultó que sólo copia `server/pyproject.toml`, `server/uv.lock` y `shared/`, así que la retirada no lo tocaba—; y el `Dockerfile` de `mcp_server` copia una **lista explícita** de módulos, donde cabía que REG.4 o VAS.4 hubieran dejado uno fuera —la lista cubre los seis que existen más `tools/`—. **Las seis migraciones se aplicaron en orden** contra Cloud SQL, de `c8f1b5d92e47` a `8f5a3c2d1e07 (head)`, con la salida en el log. Las tres nuevas son idempotentes a propósito y por eso la misma revisión sirve en los dos entornos: **BD.1 es un no-op en producción** (censada antes: 51 tablas, cero huérfanas) y **BD.2 sí hace allí sus siete cosas** —retira las tres FK cruzadas que violaban la frontera edge/cloud, entre ellas la que **borraba en silencio las interacciones** al borrar un chatbot, crea los tres índices sobre `chatbot_id` y quita el índice redundante del vocabulario—. **Verificado en vivo**: `/health` 200 por TLS en los **dos** nombres (el provisional y `normativa.uji.es`; el workflow sólo comprueba el primero), `/api/v1/docs` y `/api/v1/openapi.json` 404 en los dos, SEC.7 sigue cerrado. Las rutas nuevas responden **401 y no 404**, que es lo que distingue «protegida» de «no registrada»: las cuatro de `/actividad` (REG), las cuatro de `/verificaciones` (VAS) y `/hub/opciones/lengua` (LANG.2). El asistente **contesta de punta a punta** —pregunta en valenciano, respuesta en valenciano citando `Estatuts… art. 43`, y la URL citada devuelve 200 **con su ancla `id="art-43"` dentro del HTML**—, y el panel sirve bajo su prefijo redirigiendo a `/panel/login?from=%2F`. **Y se comprobó que el frontend servido es el nuevo y no una capa de caché**, que es un fallo que no da síntoma: el paquete del panel trae `language_mode` (24 apariciones), `actividad` y `registro`, y los fragmentos cargados en diferido `RegistroActividadPage` (REG.6) y `hub-opciones` (LANG.2) están publicados y contienen sus rutas. Que `verificaciones` no aparezca en ningún paquete es correcto y se verificó por el otro lado: **VAS no tiene UI**, no hay una sola referencia en `frontend/src`. **Lo que NO se pudo comprobar**: `alembic check` contra producción, porque las credenciales locales de `gcloud` piden reautenticación interactiva; queda pendiente de una persona y es la comprobación que cerraría del todo que el esquema vivo coincide con los modelos. Tampoco las vistas autenticadas del panel (el registro de REG.6 y el desplegable de LANG.2), que exigen entrar con credenciales. `docs/ESPECIFICACIONES.md` mueve **tres madureces** a `producción` —§5.2 (LANG), §5.9 (REG) y la de VAS—, que es lo que la regla pide al desplegar y no al cerrar el bloque; las tres decían literalmente «sin desplegar», y la de REG, «pasa a producción cuando el bloque llegue a `main`» |
| 2026-09-04 | (replanificación — Bloque PRC: sólo lo que afecta a ingesta y recuperación entra en la plataforma) | **No mueve el cursor** (PRC sigue pendiente y bloqueado por sus dos prerrequisitos externos). Origen: el usuario señala que **el catálogo de procedimientos es de la UJI y la plataforma es general** —otras universidades y ayuntamientos tienen el suyo, con otros campos y otra lengua—, y pide que los prompts sean genéricos y que la transformación viva en un pipeline aparte, ajustado al contrato de ingesta. **Comprobado: la mitad ya estaba bien y la otra la incumplía yo.** PRC.0 y PRC.1 ya decían «vive en `cataleg_procediments/`, no en `server/`», y la frontera ya está decidida desde el 2026-08-02 en `docs/DECISION_CURACION_SEPARADA.md`, cuyo criterio de éxito es literalmente «que cargar un corpus nuevo **no exija tocar código de la plataforma**». **Tres correcciones al lado plataforma.** (1) **`id_ficha` se retira**: el contrato ya tiene `versio_idiomatica_de` —la referencia a la canónica por `id_publicacio`, el mecanismo con el que ACT emparejó las lenguas de la normativa—, así que una segunda clave con nombre de catálogo era la doble fuente de verdad que el proyecto persigue; los 15 campos del catálogo van a `doc_metadata` tal cual, que es lo que el contrato dice para que el esquema crezca sin tocar código. (2) **`tipus_document` deja de ser un `StrEnum` de valores**, que incumplía el invariante I4: los términos (`norma`, `procediment`, y el `ordenança` de quien venga después) son **dato en `hub_vocabulary_terms`**, y lo estructural es la **clase de autoridad** —normativa contra administrativa—, que sí es estructura porque el código se bifurca de verdad (la vigencia se aplica por clase, no por término). Añadir un término no toca código; añadir una clase sí, y eso es correcto. (3) **Ni un literal institucional en el código**: la marca «segons la fitxa del catàleg (Servei X…)» y el nombre de campo `servei` salen a la plantilla de respuesta y al vocabulario, que ya son por chatbot, con un test que exige que cambiar la plantilla cambie la frase sin tocar código. **Y una duda del usuario que resolvió un precedente**: si PRC.0 y PRC.1 se ejecutan desde aquí, dejan rastro en este historial aunque el código vaya a otro sitio. La convención existe: PUB (2026-08-16) ya se topó con esto y registró aquí tres cambios hechos allí nombrando el otro repositorio. Escrito en el bloque: el código y los commits de PRC.0/PRC.1 van a **`normativa-uji`** (`ModestoFabra/normativa-uji`, que en el disco es `Descarregar_pdf/normativa_propia/` y ya contiene `cataleg_procediments/`), y **aquí queda una línea por prompt** porque `PROJECT_STATE.md` es el cursor del plan completo y dos prompts sin rastro no se distinguen de «bloqueado». **Corrección del mismo día**: la primera versión de esta fila decía que ese repositorio necesitaba su primer commit, y era falso porque medí `Descarregar_pdf` —el contenedor, cero commits— en vez del repositorio anidado que importa; `normativa-uji` tiene **79 commits**, rama `rubriques-bilingues` y está al día con su remoto. Lo único real que conviene antes: `cataleg_procediments/` tiene 76 ficheros modificados sin commitear —la revisión viva de las fichas—, y merece su propio commit para que el diff de PRC.0 no se lea mezclado con ella. Sin cambios de código: sólo planificación |
| 2026-09-04 | BD.2 — Alembic como única fuente del esquema, y CI que lo demuestre | **La decisión del usuario, con la medición delante**: la cadena de Alembic aplicada a una base vacía produce **exactamente las 51 tablas declaradas**, así que el `create_all` del arranque no aportaba ninguna y era la única fuente de las 36 huérfanas de BD.1. **Retirados** `init_server_db()` y `_init_hub_db()` con sus llamadas en el *lifespan* (las semillas se quedan; son llamadas aparte), `db.py` dice ahora que el esquema lo define sólo Alembic y por qué, y **`alembic check` entra en CI después de `alembic upgrade head`** —contra una base vacía propondría crearlo todo y estaría siempre rojo, y un guardarraíl siempre rojo se desactiva—. **Las tres columnas que divergían** (`hub_content_findings.created_at`, `hub_provider_credentials.created_at`/`updated_at`: `nullable=True` en su migración, `NOT NULL` en el modelo por `Mapped[datetime]` sin `nullable=` explícito; anotadas y aplazadas por REG.1 y USR.1) pasan a `NOT NULL` en `7d4e2f1a9b3c`, **con backfill previo** —`detected_at` en hallazgos, `now()` en credenciales— para que un `upgrade` no reviente en una base ajena con nulos; en desarrollo y en producción (comprobada en solo lectura) había **cero nulos en 292 filas**. El ciclo de FK `hub_llm_configs ↔ hub_organizaciones` que SQLAlchemy avisaba, resuelto con `use_alter=True` en la FK anulable. **Y el guardarraíl nuevo encontró en su primera ejecución lo que el prompt no preveía**: sobre una base recién migrada, `alembic check` reportó **siete diferencias** que el censo de tablas no podía ver y que desarrollo no tenía —sus tablas nacieron de `create_all` con la forma del modelo— pero **producción sí, porque nació de Alembic**: tres FK cruzadas `chatbot_id → hub_chatbots` con `CASCADE` en tablas operacionales (la primera migración del Hub las creó y los modelos las perdieron con el split edge/cloud sin que la cadena las quitara), dos índices sobre `chatbot_id` que el modelo pide y la cadena nunca creó, y un índice redundante con la restricción única. **Una hacía daño de verdad**: `corpus_purge.py` decide que las interacciones **no se borran** al borrar un chatbot («no reescribe la historia de lo que dijo», REV.1), y en producción `hub_interactions_chatbot_id_fkey ON DELETE CASCADE` las destruía en silencio. Comprobadas las siete en producción en solo lectura y resueltas en `8f5a3c2d1e07`, idempotente (`IF EXISTS`/`IF NOT EXISTS`: no-op en desarrollo, siete cambios en producción), con `downgrade` que no recrea las FK cruzadas por la frontera y porque fallaría sobre interacciones de chatbots ya borrados, que es lo normal por diseño. **Guardarraíl añadido que `alembic check` no da**: ninguna FK desde tabla operacional hacia tabla de configuración en la base migrada. **Tres falsos positivos de mis propios tests, los tres por prosa**: el orden `upgrade`/`check` en CI medido sobre el texto entero en vez de sobre las órdenes; `create_all` buscado como palabra en vez de como acceso al atributo; y la higiene de la suite cazando `metadata.create_all` junto a `DATABASE_URL` en un comentario mío. Reformulados los tres antes que aflojar nada. **Invariante I14** en `docs/ESPECIFICACIONES.md` §4 y sección nueva en `AGENTS.md` §Migraciones (`nullable=` explícito; modelo y migración en el mismo commit; `alembic check` en local antes del push). Desviación documentada: el prompt preveía tres derivas y salieron diez. `alembic check` limpio en desarrollo. Suite completa: **4400 passed, 1 skipped, cero fallos (8 m 45 s desde Git Bash)** |
| 2026-09-04 | BD.1 — Retirar las tablas que no pertenecen a ningún modelo | **Salió de una pregunta del usuario al cerrar NIC**: si la retirada del NiceGUI dejaba tablas colgando. La respuesta corta era «no, NiceGUI usaba SQLite» y era **media respuesta**: AutomatIA tenía también un lado servidor, el «Brain», con modelos SQLModel que apuntan a PostgreSQL. Censado `govgenai`: **88 tablas, 51 declaradas, 36 sin dueño**, todas con cero filas, con nombres que se leen solos —`mailwatcherstate`, `webwatcherhistory`, `rpaplaybook`, `localautomation`, `wizarddraft`—. **La causa es que el esquema lo gobiernan dos mecanismos a la vez**: Alembic en el despliegue y la creación automática desde el metadato en cada arranque, que hace lo declarado y **nunca borra lo que dejó de estarlo**; ninguna de las 36 aparece en ninguna de las 89 migraciones anteriores. **El primer censo se equivocó y lo dijo él mismo**: importaba unos módulos de modelos a mano, dejaba fuera `redaccion` y sacaba `hub_report_templates` —con 23 filas— como huérfana; corregido importando la aplicación entera. Comprobado además lo que un censo por metadatos no ve: **ninguna se lee por SQL crudo** en los siete árboles, y **cero FK desde una tabla viva hacia una huérfana**. **Producción, censada en solo lectura** por SSH a la VM y `SELECT` sobre `information_schema` a través del contenedor `migrate`: **51 tablas, exactamente las declaradas, cero huérfanas** —cuadra con el mecanismo, porque se desplegó el 2026-08-31, cuando esos modelos ya no existían—. **Así que la migración es un no-op en producción**, y lo que arregla es otra cosa: **desarrollo y producción habían divergido, 88 tablas contra 51, y nada lo decía**. **El `upgrade` falló al primer intento, y falló por lo que debía**: `flowregistry` tiene dos dependientes y sin `CASCADE` PostgreSQL se negó. Con `CASCADE` habría arrastrado los dependientes en silencio, y si alguno hubiera sido una tabla viva no inventariada no se habría enterado nadie; el DDL es transaccional, así que el intento no dejó nada a medias. Se midió el grafo —6 FK entre las 36, cuatro tablas hija— y el orden de borrado sale de la medición. `IF EXISTS` para que la misma revisión valga donde borra 36 y donde no borra ninguna, y **`downgrade()` no las recrea a propósito**: no hay de dónde copiar su esquema, y saldrían treinta y seis tablas con el nombre correcto y las columnas inventadas. Aplicada: **88 → 52 tablas**, datos vivos intactos (`hub_document_chunks` 163.965, `hub_documents` 758), y la única diferencia que queda con producción es **`hub_actividad_ia`**, la de REG, que está construido y sin desplegar. **La higiene de la suite cazó mi propio test, y los dos rojos eran de naturaleza distinta**: el de `metadata.create_all` era falso positivo de prosa —el test lo explica, no lo llama— y se reformuló el texto antes que aflojar el guardarraíl; el de leer `DATABASE_URL` **no era falso**, así que el fichero entra en la lista de excepciones con su justificación, que es para lo que la lista existe. No puede usar la fixture desechable: una base recién creada tiene exactamente las tablas declaradas, así que el censo pasaría siempre. **Queda abierto y es decisión del usuario**: unificar las dos fuentes del esquema. Mientras sigan las dos, cada modelo retirado dejará su tabla; el guardarraíl las hace visibles, no lo impide. `tests/infra` **701 passed**, ruff limpio |
| 2026-09-04 | NIC.4 — El entorno legacy de la raíz también se va | **Dos de las cuatro piezas que el prompt daba por huérfanas no lo eran, y eso lo dijo medir antes de tocar.** `arranque.bat` ya estaba limpio —LEG.5 lo arregló al mover el lanzador: invoca `uv run --project server uvicorn server.app.main:app`—, así que no se toca. Y el árbol `tests/` de la raíz **no era un bloque**: medido fichero a fichero, `tests/redaccion` daba **117 pasando y 12 en rojo**, `tests/public_graphs` **9 y 13**, `tests/modules` **5 y 0**, y `tests/unit` **31 errores**. Retirarlos juntos habría perdido 131 aserciones buenas; migrarlos juntos habría traído seis ficheros que no compilan. **La pregunta que decidió el reparto**: ¿duplican lo que CI ya ejecuta? Medido con AST sobre los nombres de test, **cero de 128 y cero de 22 coinciden con ninguno de los 3.630 de `server/tests`** — no eran duplicados, era cobertura que CI nunca ejecutó, sólo colectaba. **Migrados 16 ficheros** a `server/tests/modules/redaccion`, `server/tests/public_graphs` y `server/tests/modules/agents_hub/unit`, fundiéndolos con las carpetas que ya existían y no en una segunda `redaccion/` al lado. **Retirados** `tests/unit` (el mundo SQLModel *Brain/partner/licencia* de AutomatIA, que importa `server.app.services.ai_brain`, inexistente), `tests/security` y `tests/integration` (vacíos salvo `__init__.py`) y el `conftest.py`. **Los 25 rojos, reparados con una regla escrita, no caso a caso**: rojo por símbolo renombrado o guarda añadida → se repara, porque conserva la intención; rojo por afirmar un valor que un bloque posterior cambió a propósito → se escribe el de hoy **con quién lo eligió y por qué**, porque un default de columna que cambia sin que nadie lo decida es una regresión silenciosa y esto es lo único que la vigila (`quality_threshold` 0,65 por HIB.R, `min_retrieval_score` 0,0 por RAG.5, `reranker_enabled` False por RAG.6/HIB.A). Los renombrados eran `HubClient`→`HubOrganizacion`, `client_id`→`organizacion_id` y `owner_kind` «organization»→«organizacion». **Cuatro rojos eran el doble del propio test, no el producto**: dos endpoints devolvían 409 porque `MagicMock().archived_at` no es `None` y el router había ganado la comprobación de plantilla retirada; uno devolvía **500** —justo lo que ese test existe para prohibir— porque parcheaba `model_validate` para devolver `None` y SEG.5 añadió después `bloques_sin_seccion(spec.sections, ...)`; y el `AsyncMock` pelado del ConfigResolver hacía que `scalars()` devolviera una corrutina. **Y uno pasaba en verde por la razón equivocada**: `test_only_admin_can_create_global_template_contract` esperaba un `ValidationError` y lo obtenía por el literal inválido, no por la regla de `is_global`; ahora comprueba que el rechazo sea el suyo. **Retirado el `pyproject.toml` de la raíz con su `uv.lock` de 1,6 MB**: se llamaba `automatia`, declaraba sesenta dependencias empezando por `nicegui==3.4.1`, y **no lo usaba ningún workflow** —CI y `deploy.yml` corren `uv sync` con `working-directory: server`, el `Dockerfile` copia `server/pyproject.toml` y el propio paso de colección invocaba `--project server`—. **El paso `Collect root tests` de CI se quedó sin sujeto, y borrarlo sin más habría movido el agujero**: `shared/tests` y `mcp_server/tests` seguían igual de fuera. Ahora se **ejecutan** (14 s los dos), y la razón la dio la medición: `shared/tests` escondía **dos rojos** —`FieldDefinition`, retirado de `automatia_shared.dtos` sin sucesor— cuyo import está **dentro** de la función, así que un `--collect-only` los habría dejado pasar; retirados con su razón, y `shared` queda en **142 passed** y `mcp_server` en **89**. Retirados también `translations.json` (158 KB), `automatia_shared.core.i18n` con su reexportación en el `__init__` y sus cuatro tests, y `scripts/test_i18n_nested.py`. `nicegui` **se queda** en las dos listas de imports prohibidos del auditor de scripts, con una línea que lo explica: quitar una entrada de una lista negra nunca la mejora. Actualizados `README.md`, `CONTRIBUTING.md` —donde `uv sync` ya no se puede escribir en la raíz— y `docs/Arquitectura.md`. Los cuatro `uv lock --check`, verdes. Suite completa: **4387 passed, 1 skipped, cero fallos (desde Git Bash; 4291 antes del bloque NIC, y la diferencia son los 16 ficheros migrados desde la raíz menos los retirados)** |
| 2026-09-04 | NIC.3 — `client_app/` y `_legacy_nicegui/` se retiran completos | **El prompt decía «`client_app/` queda reducido a su única razón de existir», el agente de ejecución local; NIC.2 midió que ese agente no arranca** —`workflow_engine.py`, el motor por el que ejecutan sus dos vigilantes, importa en sus líneas 1478 y 1604 dos módulos que ya no existen, y hay 28 imports más así—, y reducir el directorio al agente habría dejado un agente roto con aspecto de funcionar. **Decisión del usuario: retirada completa de los dos directorios**, porque el repositorio se va a abrir y 500 ficheros de una aplicación que no compila no se distinguen de código vivo por quien llegue de fuera. El repositorio pasa de **2105 a 1531 ficheros versionados** (574 menos). La referencia queda en tres sitios y ninguno dentro del árbol: el **historial de git** —que viaja con el repositorio y no se puede perder—, la carpeta `AutomatIA` y el *bundle* de GenGov; y `INVENTARIO_RETIRADA_LEGACY.md` es el mapa que se lee antes de ir a cualquiera de las tres. **Un directorio no se va solo, se va con lo que le apuntaba, y eso hubo que medirlo.** (1) **El paso `Collect root tests` de CI se puso rojo** —existe justo para cazar imports a módulos que ya no existen—: se retiraron siete ficheros más (tres tests de `tests/unit/`, un guion de depuración del ETL y los tres `scripts/` que validaban el entorno del NiceGUI) y las fixtures `test_client_db` y `db_session` del `conftest.py` de la raíz; colección de nuevo verde, 197 tests. (2) **Cuatro guardarraíles afirmaban la cuarentena, y no fallan igual: los que se ponen rojos son los inofensivos.** «`_legacy_nicegui/main.py` existe» y «los diecisiete documentos de AutomatIA siguen en cuarentena» pasaron a falsos y lo dijeron, así que se invirtieron —ahora vigilan que la cuarentena **no vuelva**— y sus dos ficheros se renombraron, porque el nombre `..._esta_en_cuarentena` había pasado a ser mentira. **El peligroso es el que se pone verde**: el de NIC.1 cruzaba `git ls-files client_app/app/ui` contra la tabla del inventario, y con el directorio retirado `git ls-files` devuelve la lista vacía y sus cuatro tests de cruce iteran sobre nada y **pasan sin comprobar nada**; igual `test_should_have_no_legacy_ui_files_in_client_app`, cuya primera línea era un `return` sobre un directorio inexistente. Los dos retirados, y un test de NIC.3 impide que el de NIC.1 vuelva. (3) **La documentación deja de prometer lo que se retira**, que es la mitad que se olvida: `AGENTS.md` (párrafo de contexto, mapa de módulos —donde de paso se corrigen dos rutas que no existían: `frontend/src/automation/` y el tamaño real de `modules/automation/`— y el protocolo de retirada, que pierde el Caso A porque pierde la cuarentena), `CONTRIBUTING.md`, `README.md`, `docs/METODOLOGIA_AGENTICA.md`, `docs/README.md`, `docs/PRUEBAS_PENDIENTES.md` y **`docs/PRUEBAS_MANUALES.md`, que mandaba a una persona instalar el agente local y probarlo**. **§10.1 nueva en `docs/ESPECIFICACIONES.md`**, aparte de los seis límites deliberados y diciendo por qué: aquellos están escritos para que nadie los implemente creyendo que faltan, y este sí falta. **Lo que NIC.3 no toca a propósito**: cinco ficheros de `tests/unit/` que pedían `db_session` siguen en rojo —ya lo estaban, importan `server.app.services.ai_brain`, que tampoco existe— porque son el mundo SQLModel *Brain/partner/licencia* que mide NIC.4; aquí se retira lo que la retirada rompe **en colección**, que es lo que CI comprueba, y no más. Suite completa desde Git Bash, tras la retirada: **4219 passed, 1 skipped, 1 failed** —y el rojo era **prosa mía**: el guardarraíl que impide enlazar los diecisiete documentos de AutomatIA busca nombres de fichero a secas, y mi §6 del inventario nombraba uno; reformulada, y al explicar por qué nombré otro, así que pasó dos veces seguidas. Igual el guardarraíl de los punteros de `AGENTS.md`, que saltó con las tres rutas inexistentes que la advertencia nueva citaba. **Reformulados los tres textos en vez de afinar los tests**; los cuatro guardarraíles afectados, reejecutados solos: **111 passed**. Antes de la retirada la suite medía **4291 passed, 1 skipped**, y la diferencia son los tres ficheros de test retirados con su sujeto** |
| 2026-09-04 | NIC.2 — La medición que descartó mover el legacy por cobertura | **El prompt iba a mover a `_legacy_nicegui/` los 67 ficheros «cubierto» y «no aplica»; la medición previa dijo que no se podía y al final NIC.2 no mueve nada.** 48 de los 67 tenían quien los importara, y no era código muerto: las páginas «no cubierto» que se quedan usan servicios «cubierto», y cinco los usa el agente. **Tres hallazgos.** (1) **Dos etiquetas de NIC.1 eran peligrosas**: «no aplica» significa borrar, y `event_bus_service.py` lo importan los vigilantes —el agente— y `layout_manager.py` lo importa `app/core/state.py`; la etiqueta salió de leer el propósito del fichero, y **quién lo importa no se deduce del propósito**. (2) **El bloqueo es transitivo**: calcular los importadores excluyendo a los candidatos vale si los 67 se mueven juntos, pero un candidato bloqueado que importa a otro lo retiene igual; hubo que cerrar **a punto fijo** (de 41 movibles a 32). (3) **`client_app/` ya no arranca, y no por este prompt**: hay 28 imports hacia diez ficheros que bloques anteriores llevaron a la cuarentena, y **el motor por el que ejecutan los dos vigilantes importa en sus líneas 1478 y 1604 dos módulos que no existen**. Ese hallazgo cambió el bloque: se llegó a mover 22 ficheros y devolverlos, y el resultado se descartó al decidir el usuario la **retirada completa** (NIC.3). Lo que queda de NIC.2 es la medición escrita en las §4 y §5 del inventario —incluida la quinta etiqueta que se consideró y se descartó, porque habría servido para justificar conservar código que no compila— y **el arreglo del único import real desde fuera de `client_app/`**: `shared/tests/test_security_unification.py` importaba una función que nunca llamaba y luego afirmaba que **el texto** de un fichero del legacy contenía tres cadenas —habría pasado igual con las tres en un comentario—; ahora afirma sobre `SAFE_IMPORTS`, la lista que el auditor consulta. Un segundo test del mismo fichero leía ese fuente y también se reapuntó. **Dos errores de método propios, los dos del tipo «el medidor miente»**: un guardarraíl medía **vacío en verde** porque el heredoc convirtió `` en un byte de retroceso —la trampa ya anotada, y por eso los parches van con Write— y otro daba falso positivo porque `git ls-files client_app/**/*.py` **no expande `**`**. Suite completa sobre el estado con los movimientos: **4291 passed, 1 skipped, cero fallos** |
| 2026-09-04 | NIC.1 — El inventario de la retirada: qué está cubierto y qué no | `docs/INVENTARIO_RETIRADA_LEGACY.md` cruza los **187 ficheros** de `client_app/app/ui` (105) y `app/services` (82) contra lo que existe, con una fila y una justificación por fichero. **Nada movido ni borrado**, y hay un test que lo comprueba contando los ficheros que siguen en su sitio: es lo que impide que este prompt se convierta en el siguiente por accidente. **El recuento: 40 cubierto, 23 parcial, 97 no cubierto, 27 no aplica.** La lectura que importa es que sólo 40 de 187 tienen equivalente verificado, así que llegar a lo que `CLAUDE.md` describe no es una limpieza: es trabajo de migración que nadie ha hecho. **Y una desviación del prompt, escrita en el propio documento**: pedía cruzar contra `server/app/modules/automation/` y `frontend/src/automation/`, y medido hoy tienen **5 y 0 ficheros**; lo que existe de verdad es `redaccion/` (96 en el servidor, 53 en el frontend, 10 routers), que es donde la funcionalidad de AutomatIA aterrizó. Mantener la letra del plan habría etiquetado «no cubierto» los 187, que es falso y además inútil. El bulto de lo no cubierto cae en cuatro familias y **tres tienen dueño**: el editor de flujos y los átomos (bloque FUN), la interfaz de los vigilantes —el agente sí existe, su pantalla no— y la de RPA; la cuarta, **conocimiento local y sincronización cliente↔nube**, no tiene dueño y `edge_sync` son dos stubs 501. Varias filas quedan marcadas «**decide NIC.3**» porque hay que resolver si el servicio es parte del agente o era interfaz. **Dos hallazgos de paso**: `design_sandbox_service.py` **no compila** (`SyntaxError`), así que además de estar cubierto es código muerto; y `extraction_service.py` tiene **3.494 líneas** con `ui_translations.json` en **60 KB**, los dos etiquetados «parcial» a propósito porque hay que leerlos antes de decidir. La tabla de etiquetas se genera de una lista explícita con dos comprobaciones que cazaron tres errores míos: un fichero sin regla, una regla que inventé (`autosave_service.py`, que no existe en `client_app`) y dos reglas de familia que tapaban a las específicas por orden. 5 guardarraíles nuevos; el de las filas tuvo que hacerse preciso —`| \`ruta\` |` y no `ruta in linea`— porque la prosa de la cabecera nombra tres ficheros y daba falso positivo |
| 2026-09-04 | VAS.4 — Las tres verificaciones como tools MCP, y la documentación | `verificar_citas`, `consultar_vigencia`, `auditar_codigo` y `reglas_de_auditoria` sobre el transporte remoto de REG.4, con el token de cada cliente propagado por petición. **La lección de REG.7 aplicada de entrada**: las firmas declaran los campos con descripción en vez de un `dict` opaco, y el guardarraíl del lado del servidor se amplía para comparar estas firmas con `PeticionDeCitas` y `PeticionDeAuditoria` — nombres y obligatoriedad, no tipos. `modo` no se expone a propósito: hoy el contrato se comporta igual en los tres modos y un parámetro que no cambia nada sólo invita a rellenarlo mal. **Y el defecto que destapó la sesión MCP real**: el pool de `httpx` se cerraba en el ciclo de vida de **FastMCP**, y con `stateless_http` ese ciclo corre **por petición**, así que el servidor remoto servía **una sola llamada por proceso** —la segunda moría con «Cannot send a request, as the client has been closed»—. Lo que hace instructivo el caso es que **el test de dos clientes concurrentes pasaba**: sus peticiones empiezan antes de que el ciclo de la primera termine, así que las dos encuentran el pool abierto, y lo que fallaba era lo secuencial, que es lo normal. Arreglado envolviendo el ciclo de vida de la **aplicación** —no con `add_event_handler`, que esta versión de Starlette ya no tiene— y con un test de dos llamadas seguidas. Documentado en `GOVERNANCA_PER_API.md` (§4 y las tres subsecciones reescritas como hechas, con el contrato de cada endpoint) y en `MCP_SERVER.md` (mapa de scopes, las cuatro tools y el ejemplo de sesión «auditar un script antes de compartirlo», que es el caso de la Instrucció). **Verificado con una sesión MCP real**: `tools/list` anuncia las siete tools y las cuatro nuevas responden bien de forma secuencial contra el servidor local. 15 tests nuevos en `mcp_server` (**89 en total**) y 4 guardarraíles más en el servidor |
| 2026-09-04 | Bloque VAS completo | Cuatro prompts: el contrato de citas, la vigencia y la auditoría estática como servicio, más las tools MCP y la documentación. Las tres filas de `GOVERNANCA_PER_API.md` pasan de «Candidat» a «Hui», y quedan tres candidatos esperando a la primera aplicación externa real. **Las tres reglas duras del bloque, con test cada una**: sin segunda implementación (paridad contra la función que usa el motor, en los tres servicios), el texto no se guarda, y **sólo la auditoría registra evento**. Un solo scope, `verificaciones:use`. **Y dos defectos que sólo aparecieron al comprobar en vivo, los dos en código de REG**: `PatService._orgs_del_dueno` dejaba a un PAT de administrador sin organización —con lo que `POST /actividad` respondía 403 a todo integrador real— y el MCP remoto **servía una sola llamada por proceso** porque cerraba su pool en el ciclo de vida de FastMCP, que con `stateless_http` corre por petición. Ninguno lo veía la suite; los dos los encontró un `curl` y una sesión MCP de verdad. Suite completa: **4224 passed, 1 skipped, cero fallos**; 89 en `mcp_server`. `docs/ESPECIFICACIONES.md` gana §5.10 |
| 2026-09-04 | VAS.2 — La vigencia de un documento, y **el defecto que destapó** | `GET /api/v1/verificaciones/vigencia` por `document_id` o `url`, acotado por tenencia sobre `hub_chatbots` y por `MetadataFilter()` por defecto; lo que no cumple da **404 y no «no validado»**, porque el propio título de la respuesta confirmaría que el documento existe. El aviso es **byte a byte** el del grafo —sale de `marca_de_vigencia` y `aviso_para` sobre un item sintético—, y el test lo compara con la función: dos superficies de la misma institución que redacten distinto el mismo aviso acaban contradiciéndose. La URL se resuelve con el normalizador del emparejador de citas, que **sube de privado en `evaluation/` a `core/urls.py`** y aquel módulo lo importa de ahí; no es un alias de compatibilidad, es que hay una sola implementación. La pareja bilingüe se resuelve en **los dos sentidos** de `versio_idiomatica_de`, porque cuál de los dos lados lo declara depende de qué se ingirió primero. **Y lo que de verdad importa de este prompt**: el `curl` real con un PAT real daba 404 en un documento de la propia organización del dueño del token, con los veinte tests en verde. La causa era `PatService._orgs_del_dueno`, que resolvía las organizaciones **sólo** por `HubOrganizacion.partner_id == owner_id` —el modelo de partner— cuando desde ROL/IDE la identidad de administración es un `HubUser` que **pertenece** a una organización por su columna: comparaba `60082aa4-…` con `uji` y no encajaba nunca. Para un rol no privilegiado la tupla vacía significa «ninguna», así que **`POST /api/v1/actividad` de REG.2 respondía 403 a todo integrador real y no había funcionado nunca con un PAT de verdad**; sus 17 tests pasan porque sobreescriben `get_current_user`. Es el patrón que el proyecto ya tiene anotado dos veces —un mock sin el camino real esconde el bug— y lo encontró la comprobación en vivo. Arreglado probando primero la identidad de usuario y cayendo al partner, con cuatro tests que recorren `PatService.verify` de punta a punta contra la base y **sin sobreescribir el principal**, incluidos el caso del partner y el comodín del superadministrador. 20 tests del endpoint + 4 del PAT; los **123 de PAT y scopes siguen verdes**. También se retiró la exención de `verificaciones_router` en el inventario de routers, que VAS.1 había puesto diciendo que caducaría aquí |
| 2026-09-04 | VAS.3 — La auditoría estática como servicio, con evento y la caja publicada | `POST /api/v1/verificaciones/codigo` devuelve el `AuditResult` del `ScriptSecurityAuditor` **tal cual** más el `code_sha256`, y `GET .../codigo/reglas` publica la caja de herramientas. Es la revisión posterior del nivel 2 de la Instrucció 02/2026 para código que no corre en la plataforma: la misma vara que el catálogo de funciones aplica dentro, prestada a quien desarrolla fuera. Paridad con el auditor sobre seis scripts —limpio, módulo fuera de lista, `eval`, `import os`, uno que no compila y un `open`—: si la API «suavizara» un CRITICAL, pasar por aquí no probaría nada. **El código no se guarda, sólo su hash**, con centinela en logs y comprobación de que el evento no lo lleva. **La asimetría del registro, ahora con test en los dos lados**: esto sí escribe evento (auditar es un acto de gobernanza y tiene que constar quién auditó qué hash con qué nivel) y la vigencia **no** (comprobación sin estado; un evento por comprobación duplicaría el registro sin decir nada nuevo). **Y una decisión que se aparta del prompt**: éste preveía añadir el nivel de riesgo como clave al contrato de REG.1 si no cabía; no se añade, porque sería un campo que **sólo esta operación** rellenaría y el contrato es el mismo para toda herramienta que registre —un campo por caso de uso lo convierte en un formulario con huecos—. El nivel va delante de la finalidad declarada, que es donde un registro de gobernanza cuenta qué pasó; si algún día hay tres consumidores que necesiten el metadato estructurado, entonces sí es un campo del contrato. **La caja de herramientas se compone leyendo los `frozenset` por `sys.modules`** y no por la referencia de importación, para que el `monkeypatch` del test la vea: cerrada sobre el valor de importación, el test pasaría con una copia y la garantía se perdería. `version_auditor` es un SHA-256 del contenido canónico y **no `hash()`**, que va con sal por proceso y daría un cambio falso en cada reinicio. `REGLAS` vive en el auditor junto a los `frozenset`, y **un guardarraíl que el prompt no pedía** extrae los `rule=` del fuente y comprueba que están todos catalogados: sin él, añadir una regla publicaría una caja incompleta sin que nada fallara. 29 tests nuevos; los **92 del auditor y del catálogo de funciones siguen verdes** |
| 2026-09-04 | VAS.1 — El contrato de citas como servicio, y el scope de verificaciones | `POST /api/v1/verificaciones/citas` presta a una aplicación externa la misma regla que el asistente aplica a sus respuestas: «ninguna afirmación sin fuente resoluble». Scope nuevo `verificaciones:use`, **uno para los tres servicios del bloque y no uno por servicio**: son la misma capacidad —comprobar con la vara de la plataforma algo que se produjo fuera— y partirlo obligaría a pedir tres permisos para un caso de uso. **La regla dura del bloque, y cómo se hace cumplir**: el cuerpo de `enforce_citation_contract` se movió a `aplicar_contrato`, que devuelve lo mismo **más el desglose** de lo que hizo, y la función vieja delega. No hay dos caminos, hay uno con dos vistas — y el test de paridad compara el texto de la API con el de la función sobre seis casos, así que «mejorar» el endpoint por su cuenta pone rojo. El desglose (válidas, inválidas, degradadas, despojadas) sale de la **misma pasada** que produce el texto: calcularlo aparte describiría un texto distinto del que se devuelve, porque los pasos no son idempotentes entre sí y el orden de degradar y despojar *es* el contrato. `citas_de()` expone la expresión regular del validador para que el router no tenga la suya; hay un guardarraíl con `ast` que lo comprueba —y su primera versión daba **falso positivo**, porque `"import re"` es subcadena de `import require_pat_scopes`, y un guardarraíl que salta cuando no debe se acaba silenciando—. **El texto no se guarda**, con la garantía fuerte en vez del cuidado: el módulo **no recibe sesión de base de datos**, así que no hay dónde escribir, y un test lo fija leyendo el fuente; más el centinela con `caplog` a DEBUG. Tope de 64 KB → 413: no hay límite del sandbox del que heredarlo porque FUN.6 no existe, así que se fija aquí y ese bloque lo heredará de este sitio. **Dos desviaciones documentadas**: los tests van a `tests/api/`, donde viven todos los de router, y no a `tests/routers/` como pedía el prompt —dos directorios para lo mismo—; y la cuota **se respeta pero no se consume**, ver la nota de abajo. 26 tests nuevos; **los 183 del motor de citas siguen verdes** tras el refactor, que es la comprobación que importaba. Verificado con `curl` real y un PAT emitido en la BD de desarrollo: la cita válida se conserva, la ajena pierde el enlace y queda como mención, `remisiones_despojadas: 1` |
| 2026-09-04 | REG.8 (corrección) — El panel pedía el catálogo sin decir la organización | **Salió verificando en el navegador**, y es el mismo tipo de defecto que el bloque ya había dado dos veces: la pantalla llamaba a `/actividad/categorias` sin `organizacion_id`, el catálogo es de cada organización y un superadministrador no tiene «la suya», así que el servidor respondía **400**, el mapa de etiquetas quedaba vacío y la tabla caía a los códigos crudos — exactamente lo que REG.8 venía a evitar. **El test de la pantalla no podía verlo porque mockea el hook**: devolvía el catálogo igual con cualquier parámetro. Ahora comprueba con qué parámetros se pide, que es la parte que el mock no puede fingir, y que sin organización elegida no se pide nada. La organización es la del selector del panel (REV.10). Verificado en vivo: `?organizacion_id=...` → 200, la columna enseña «Datos identificativos, Datos de contacto» y el código sin catalogar sigue saliendo crudo. El `.bat` del bloque gana cuatro pasos (el catálogo, que un código inventado no se rechaza, y los dos mensajes distintos del 422). **Y una lección de medición**: la pasada de vitest anterior arrancó antes del arreglo y se editaron esos ficheros mientras corría, así que sus 14 fallos no medían nada — se repitió con el árbol quieto |
| 2026-09-04 | REG.7 — La tool MCP declara el contrato, no un objeto opaco | `registrar_actividad` recibía `evento: dict`, así que el esquema que un cliente MCP obtenía de `tools/list` era `{"type": "object", "additionalProperties": true}`: **ni un nombre de campo**, ni cuáles son obligatorios, ni que `ocurrido_en` exige zona horaria — y encima prometiendo que cualquier extra vale cuando el servidor los rechaza con 422, así que un agente lo descubría a base de 422 en bucle. Ahora la firma lleva los ocho campos con `Annotated[..., Field(description=...)]`, que es donde caben las reglas que el tipo no expresa. Lo que nadie declara no viaja, y menos como `null`: `categorias_datos` no admite nulo en el contrato, así que rellenar el hueco sería provocar un 422 desde el cliente. **El guardarraíl contra la deriva vive en la suite del servidor**, no en `mcp_server/`: comprobar que la firma y `ActividadIAEvent` dicen lo mismo exige tener los dos delante, y ese paquete no puede importar `server.app` —regla de MCP.1, con su propio test— ni tiene sus dependencias en el venv; se lee con `ast` y se comparan **nombres y obligatoriedad, no tipos**, porque los del lado MCP son los que viajan en JSON y el que valida es el servidor. Origen: la pregunta del usuario al cerrar el bloque —si hace falta documentar el contrato o MCP ya lo intercambia—, que al comprobarla destapó esto. 4 guardarraíles + 3 tests de esquema; 74 passed en `mcp_server` |
| 2026-09-04 | REG.8 — El catálogo de categorías de datos, que se anuncia sin imponerse | `categorias_datos` era vocabulario abierto sin catálogo, y eso resuelve un problema y crea otro: quien registra sabe qué trató, pero si cada herramienta inventa sus códigos —`datos_identificativos`, `identificativos`, `PII`— **el registro deja de poder agregarse**, que es para lo que existe en una auditoría; y ningún canal automático lo arregla, porque ni el esquema MCP ni OpenAPI transmiten un vocabulario que no existe. `GET /api/v1/actividad/categorias` sobre `hub_vocabulary_terms` con el eje nuevo `categoria_dades`. **Eje y no tabla propia**: un catálogo de protección de datos necesita justo lo que esa tabla da —`vigent` para retirar sin borrar el histórico, `substituit_per_codi` para renombrar dejando la traza, el par de nombres para las dos lenguas—, y una tabla nueva habría tenido que reinventarlo, probablemente empezando por borrar filas. **La decisión que más importa: se anuncia, no se impone.** El `POST` sigue aceptando cualquier código, porque rechazar uno no catalogado convertiría «esta categoría todavía no está dada de alta» en «este uso de IA no queda registrado»; y los sin catalogar se enseñan en el panel **tal como llegaron**, que es la única señal de que al catálogo le falta una entrada y por tanto lo único que hace que alguien lo cure. Sin exigir rol ni módulo, a diferencia de la lectura: exigir `registro` dejaría fuera al consumidor principal, el token de máquina con sólo `actividad:write`. La semilla se **copia** en la migración a propósito —una migración es un hecho histórico y un `import` la ataría al presente— con un test que comprueba que hoy coinciden. Migración `4b1d8e29c7f3` aplicada; sembradas 8 categorías del art. 30 del RGPD, **pendientes de validación** por quien lleve el registro de actividades. El panel pasa a enseñar la etiqueta del catálogo en vez del código crudo. **Y una corrección del arnés**: `_principal` daba una organización también al superadministrador, así que el test que comprueba que se le pide indicarla pasaba con la implementación equivocada. 16 tests nuevos + 2 de frontend |
| 2026-09-04 | REG.9 — El rechazo del contrato explica la regla y señala la salida | `extra="forbid"` hacía cumplir la regla del bloque y decía «Extra inputs are not permitted»: **qué, no por qué**. Quien integra lee eso y saca la conclusión razonable de que al esquema le falta un campo, así que pide que se añada; la conversación de explicar que el registro no puede ser un segundo sitio donde viven los datos personales acaba teniéndose igual, sólo que a posteriori y por un canal lento. Un `model_validator` en `mode="before"` —antes que el rechazo de Pydantic, y sólo sobre campos no declarados, para que añadir uno al contrato no lo convierta en error por parecerse— distingue **dos malentendidos distintos**: mandar contenido (13 nombres que se mandan de buena fe, con la regla y `payload_hash` como salida) y mandar `organizacion_id` (que no es contenido: es creer que se elige algo que sale del token). Cualquier otro extra recibe el mensaje genérico con el puntero al documento, sin adivinar la intención de un campo que nadie ha visto. Los tres mensajes nombran `REGISTRO_ACTIVIDAD_IA.md`, y el docstring de la tool MCP también: quien llega por MCP no ve nunca la documentación del repositorio. 15 tests nuevos, cuatro de ellos de que **lo que el contrato acepta y rechaza no cambia** —el arreglo es del mensaje— |
| 2026-09-03 | Bloque REG completo | Seis prompts: contrato y tabla del evento, endpoint de escritura con dos scopes nuevos, anonimización como servicio, MCP remoto con token por petición, lectura y exportación, y la vista del panel con su documentación. La plataforma pasa a poder dar cuenta de la IA que ocurre **fuera** de ella. Cuatro reglas duras del bloque, todas con test: **metadatos sí, payloads no** (el contrato no declara contenido y rechaza los cinco nombres con que se intentaría colar); **el servicio de anonimización no registra el texto** (centinela con `caplog` a DEBUG); **el MCP remoto no tiene credencial propia** (test con dos clientes concurrentes, que es el que descarta guardar el token en una variable de módulo); y **escribir es de máquinas y leer es de personas**, cada uno con su credencial y ninguno con la del otro. Dos migraciones aplicadas (`28d7fafbf4af`, `3a9c1e77b402`). Suite completa: **4106 passed, 1 skipped, 0 fallos**; 71 en `mcp_server`; 742 en vitest. `docs/ESPECIFICACIONES.md` gana la capacidad **§5.9** con lo que garantiza y sus cuatro pendientes, y §5.6 el módulo nuevo. **Nada desplegado**: el servicio `mcp` está declarado en la pila de la VM y el paso a `main` es decisión del usuario |
| 2026-09-03 | REG.6 — La vista del registro en el panel y el contrato documentado | Pantalla «Registro de actividad IA» en el panel —tabla paginada, filtros por herramienta y rango de fechas, exportación a CSV— con hooks de Orval y cero cadenas fijas, en es/ca/en. **Una decisión de arquitectura que el prompt no cerraba, y va como desviación documentada**: la pantalla cuelga de un **módulo nuevo, `registro`**, y no de `plataforma`. El criterio está escrito en `PlataformaLayout.tsx` —ahí va la configuración de la plataforma (`Deploy: cloud`) y el registro es dato operacional de la organización (`Deploy: edge`)—, y meterlo ahí obligaría a conceder los modelos de LLM, las organizaciones y los tokens para poder conceder el registro: el problema que PLAT.2 y USR.9 arreglaron, en un caso y en el opuesto. Migración `3a9c1e77b402` con el patrón de USR.9: fila de catálogo y concesión idempotente a quien ya tenía `plataforma`, con el `ON CONFLICT` por nombre de restricción y los cuatro campos de la clave. `require_module("registro")` en los **dos endpoints de lectura y en ninguno más**: el POST lo autentica un PAT de máquina y los módulos son de personas. **Un defecto encontrado verificando en el navegador y arreglado ahí mismo**: con un filtro sin coincidencias la pantalla decía «todavía no hay actividad registrada, aparecerá cuando una herramienta empiece a declararla», que es falso —hay actividad, pero no ésa— y lleva a concluir que el registro no funciona en vez de que el filtro no acierta; ahora son dos mensajes distintos, con test. `docs/REGISTRO_ACTIVIDAD_IA.md` con el contrato campo a campo, **el mapeo a las convenciones OTel GenAI** —incluidos los cuatro campos que no se recogen y los dos que no tienen equivalente a propósito, `finalidad` y `categorias_datos`, que son justo los que convierten una traza en un registro de cumplimiento—, cómo dar de alta un agente externo con curl y con MCP, y la regla «metadatos sí, payloads no» explicada para que un integrador no pida el campo de contenido. Enlazado desde `mcp.md` y `MCP_SERVER.md`. 13 tests de frontend nuevos; **742 passed** en vitest. Verificado en navegador: la entrada del menú, los 5 eventos sembrados, el filtro llegando al servidor (`?herramienta=claude-cowork` → 200), la exportación (`/actividad/export?herramienta=claude-cowork` → 200), el mensaje del filtro sin resultados y la pantalla completa en valenciano; consola limpia |
| 2026-09-03 | REG.5 — Leer y exportar el registro, acotado por organización | `GET /api/v1/actividad` paginado con filtros por herramienta y rango de fechas, y `GET /api/v1/actividad/export` en CSV. **Sesión JWT y no PAT, al revés que la escritura**: la lectura es para personas —el panel—, con rol admin o superadmin; darle a un PAT capacidad de leer el registro sería regalar a cada integración una ventana a la actividad de toda la organización. Acotado con `scope_query_to_orgs`, con los cuatro casos comprobados: cada administrador ve lo suyo, el superadmin lo ve todo y **un administrador sin organizaciones no ve nada** —lista vacía significa «ninguna», que es el hallazgo A2 de SEC.2—. **El desempate, que es el punto técnico del prompt**: `ocurrido_en` lo declara quien registra y los empates son normales —un lote que se manda de golpe trae el mismo instante repetido—, así que sin desempate el planificador elige el orden dentro del empate y paginar puede devolver una fila en dos páginas y otra en ninguna; el `ORDER BY` lleva `id` y el test comprueba **el síntoma sobre tres páginas de una siembra con dos eventos al mismo instante**, no la cláusula, porque el síntoma es lo que le pasa a quien audita. Rango **cerrado por los dos extremos**: quien pide «del 1 al 3» cuenta con el 3, y abierto por arriba se perdería el último día sin decirlo. El SELECT lo comparten listado y exportación en una función: dos copias podrían divergir y la acotación por organización es justo lo que no puede tener dos versiones. Cabeceras del CSV con los **nombres del contrato** y no rótulos traducidos —el fichero se cruza con otras fuentes; los rótulos son del panel, que sabe en qué idioma está su lector—, `categorias_datos` aplanadas con `;` porque la coma partiría la celda en una hoja de cálculo, y `Content-Disposition: attachment` para que el navegador ofrezca guardarlo. Tope de página de 200: sin él, `size=100000` pide la tabla entera por un endpoint paginado. 21 tests nuevos; **76 passed** con los de REG.2, REG.3, higiene y SEC.9.6. `ruff` limpio |
| 2026-09-03 | REG.4 — El MCP remoto: segundo transporte y token por petición | `mcp_server/` gana un transporte **streamable HTTP** (`http_server.py`) y tres tools nuevas —`registrar_actividad`, `detectar_pii`, `anonimizar_texto`— que llaman a los endpoints de REG.2 y REG.3. Es la «opción B» que `docs/mcp.md` dejó anotada el 2026-06-06 sin caso de uso. **La decisión del prompt, y el motivo**: el stdio lee `GOVGENAI_PAT` del entorno porque es mono-usuario —un proceso, una persona, un token—, y en un servicio remoto multi-cliente eso uniformaría a todos: mismos permisos, misma organización y mismo rastro, con lo que el registro de actividad no distinguiría a nadie. Así que el token es el `Authorization: Bearer` de cada petición, leído del contexto en cada llamada y **nunca guardado en una variable de módulo**; hay un test con **dos clientes en vuelo a la vez** contra el mismo servidor que descarta esa implementación —con uno solo, pasaría—. Sin token la tool falla con «Falta el PAT» y **sin llamar a la API**, porque un 401 del servidor haría buscar el problema en el token en vez de en su ausencia; y un `Authorization` que no sea `Bearer` se rechaza aquí por lo mismo. Los tests hablan el transporte de verdad sobre ASGI en vez de invocar la función de la tool: lo que cambia es de dónde sale el token, y eso sólo se ve si la petición entra por donde entra. **Un arreglo de paso en `ApiClient`**: la cabecera se ponía al construir el cliente `httpx`, así que pasar `client=` descartaba el PAT en silencio; ahora se pone por petición, lo que además permite compartir un solo pool entre clientes con tokens distintos. **Transporte sin sesión y con respuestas JSON**, las dos por despliegue: nada que guardar entre peticiones (el proxy reparte sin afinidad y un reinicio no deja sesiones huérfanas) y un flujo SSE tras un proxy inverso funciona sólo si alguien desactivó el buffering. **Y una trampa que costó encontrar**: la protección contra DNS rebinding del SDK valida la cabecera `Host` contra una lista, y sin ella el transporte responde **421 a todo** — un fallo que no se parece a su causa; `GOVGENAI_MCP_ALLOWED_HOSTS` es obligatoria y en el despliegue **se deriva de `GOVGENAI_HOST` y `GOVGENAI_HOST_INSTITUCIONAL`**, las mismas que usa Caddy, para que no se quede atrás cuando cambie un nombre. Despliegue: `mcp_server/Dockerfile` (imagen propia y pequeña, sin el venv del servidor), servicio `mcp` en el compose de la VM, ruta `/mcp` en Caddy sin despojar el prefijo, cuarta imagen en `deploy.yml`. `uvicorn` pasa a dependencia declarada —el arranque del contenedor depende de ese binario y venía del extra `cli` de `mcp`— con `uv lock` en el mismo commit. **Sigue sin importar `server.app`**, con el test corregido para buscar la importación y no la cadena: la versión inicial se marcaba a sí misma y a los tests de MCP.1, que importan legítimamente `server.py`, el módulo stdio del propio paquete. 17 tests nuevos en `mcp_server/tests` (**71 en total, verdes**) y 8 guardarraíles de despliegue en `server/tests/infra`; **684 passed** en todo `tests/infra`. Documentado en `MCP_SERVER.md` (los dos transportes y por qué no son intercambiables), `mcp.md` y `DESPLIEGUE_PROTOTIPO_GCP.md` §3.septies con el `claude mcp add` y la comprobación posterior. **Nada levantado**: el servicio se desplegará con el bloque |
| 2026-09-03 | REG.3 — La anonimización como servicio, y el texto que no se registra | `POST /api/v1/anonimizacion/spans` y `/replace` sobre el motor que ya existía en `modules/redaccion` —regex, NER de spaCy y anclajes de formulario, con los identificadores españoles ya afinados—, que hasta ahora sólo usaba `redaccion`. Scope `anonimizacion:use` con `require_pat_scopes`, el mismo patrón de REG.2. **La regla de oro tiene test propio y es del camino bueno, no una guarda sin vigilar**: aquí llega por definición el material más sensible que pasa por la plataforma —nos lo mandan *porque* lleva PII—, así que un `logger.debug(text)` de una depuración convertiría el log del servidor en el archivo de datos personales que el servicio existe para evitar; el test llama a los dos endpoints con un centinela y comprueba con `caplog` a **DEBUG** que ni el centinela ni el DNI aparecen en ningún registro. **Y una regla del mismo tipo que el prompt no pedía**: el motor guarda el mapa real→ficticio en el `FakerGenerator` de su contexto, así que un detector compartido entre peticiones acumularía en memoria la PII de todos los que llamaron y `deanonymize` de una podría deshacer la de otra; el detector se inyecta por petición y hay un test que lo fija. **`spans_aplicados` sale de la misma detección que el texto devuelto**, no de una segunda pasada: el sustituto lo genera un `Faker`, y dos pasadas darían un informe que no describe el resultado —con test que comprueba que cada sustituto está en el texto—. Sustitución de derecha a izquierda para que los offsets pendientes no se muevan. **Un cambio en el motor**: la política por defecto estaba escrita dentro de `anonymize()` y ahora es `TIPOS_ANONIMIZADOS_POR_DEFECTO`, porque el endpoint necesita la misma lista para informar y una copia habría divergido en silencio; un test prohíbe que el router se quede una copia. Sin parámetro de política, a propósito: se añade cuando un consumidor real diga qué necesita. Nada persiste, y el test lo comprueba contando **todas** las tablas operacionales antes y después, no las que se sospechan. Router `Deploy: edge` en `_register_edge`, `operation_id` explícito en los dos. 21 tests nuevos, 44 con higiene, SEC.9.6 y la frontera de imports; los 104 de anonimización que ya existían siguen verdes tras tocar el motor. `ruff` limpio |
| 2026-09-03 | REG.2 — El endpoint que registra actividad, y por qué es sólo para máquinas | `POST /api/v1/actividad` recibe un `ActividadIAEvent` de una herramienta externa y lo persiste. **Dos decisiones, y las dos con test**: la organización **se deriva del dueño del token** y no viaja en el payload —si viajara, un token de una organización podría escribir en el registro de otra y el registro dejaría de dar cuenta de nada; `organizacion_id` en el cuerpo llega **422** y no se ignora en silencio, porque ignorarlo haría creer a quien integra que elige—; y la escritura **exige un PAT, no una sesión**. Lo segundo obligó a una dependencia nueva: **`require_scopes` deja pasar una sesión JWT a propósito** (`if scopes is None: return user`, porque un principal humano se autoriza por rol y no por scope), y aquí eso permitiría fabricar entradas del registro desde el panel. De ahí `require_pat_scopes` en `api/deps.py`, que exige las dos cosas y distingue los dos 403 —`PAT_REQUIRED` y `PAT_SCOPE_MISSING` con la lista de lo que falta— porque son dos problemas distintos para quien integra. Escrita junto a `require_scopes` y no en el router porque REG.3 la necesita igual. Dos scopes nuevos en el catálogo, `actividad:write` y `anonimizacion:use`, **emisibles por superadmin y admin** a diferencia de `chatbots:write`: aquél muta un chatbot en producción in-place, y esto añade metadatos sin mutar nada. Separados entre sí porque son capacidades distintas —una herramienta puede querer limpiar PII sin registrar, y otra registrar sin pedirnos que limpie—. Router `Deploy: edge` en `_register_edge` con `operation_id="registrarActividad"`. 17 tests nuevos; con higiene, SEC.9.6, los de rol y `core/auth`, **112 passed**; con los guardarraíles de frontera e instalación limpia, 29 más. `ruff` limpio. **Una corrección del RED**: el fichero pedía una fixture `db_session` que no existe —la convención de la suite es `db_url` y montar el motor en el test—, así que se define local a partir de `db_url`; y la tabla no se crea ahí porque la plantilla de `conftest` ya ejecuta `create_all` de las dos bases. `openapi.json` no se commitea: está en `.gitignore` porque se genera |
| 2026-09-03 | REG.1 — El contrato del evento de actividad IA y su tabla | `ActividadIAEvent` y `hub_actividad_ia`, para registrar usos de IA que ocurren **fuera** de la plataforma —agentes de terceros, asistentes de escritorio— al servicio de la conservación de registros del AI Act y del registro de actividades del RGPD. **Es registro de gobernanza, no trazado técnico**: el detalle token a token ya lo cubre la observabilidad interna y no se reconstruye. Tabla nueva y no `hub_interactions` sobrecargada, porque aquélla registra conversaciones internas con otra forma —mensajes, valoraciones, veredictos— y mezclarlas dejaría la mitad de las columnas nulas en la mitad de las filas. **La regla dura, con dos tests que la fijan**: el contrato no declara ningún campo de contenido y `extra="forbid"` rechaza los cinco nombres con los que un integrador intentaría colarlo (`payload`, `content`, `prompt`, `mensaje`, `texto`); si hace falta evidencia va el SHA-256, validado en forma porque un hash mal formado no sirve para cotejar nada. **Dos desviaciones documentadas, y las dos porque el prompt contradecía al código**: (1) pedía `__ambito__="organizacion"` afirmando que «el guardarraíl de MT.1 lo exige igualmente», y MT.1 recorre **solo** `HubConfigBase` —declararlo habría hecho de esta la única de trece tablas operacionales con un ámbito que ningún test lee—; (2) pedía FK a `hub_organizaciones` «con el patrón de las vecinas», y ese patrón es precisamente **no tener FK**: hay cero en los modelos operacionales, a propósito, para que las dos bases declarativas sigan separables al partir cloud y edge. Las dos se sustituyeron por tests que fijan la regla correcta, y el objetivo del prompt se cumple igual. **Y la trampa del autogenerate, otra vez**: propuso borrar **40 tablas del legacy de AutomatIA** y alterar tres columnas ajenas, porque compara los modelos con la base que tiene delante. La migración se reescribió a mano con solo la tabla y sus tres índices; comprobado despues de aplicarla que la base pasa de 87 a 88 tablas y que las del legacy siguen ahí. `registrado_en` con `server_default now()` y no defecto en Python como las vecinas, a propósito: en un registro de gobernanza la marca de cuándo se recibió el evento la pone la base, no el proceso que inserta. Migración `28d7fafbf4af` aplicada. 24 tests nuevos; con MT.1, MT.7 y el guardarraíl de higiene, **50 passed**; `ruff` limpio. El inventario de `MULTITENENCIA.md` lleva la fila nueva aunque su test no la exija: sin ella el documento estaría incompleto |
| 2026-09-03 | Arreglo — el interruptor de actividad decía «Editar» y activaba | **Encontrado verificando LANG.2 en el navegador, y de la peor manera**: buscando el botón de editar de la fila pinché el chip de la columna «Activo», que en un chatbot inactivo se pintaba con `tc('edit')`. No abrió ningún diálogo — **activó el asistente**, y hubo que devolverlo a `is_active: false` por API. No es un caso raro: es lo que le pasa a cualquiera que busque «Editar» en la fila, porque el editar de verdad está en el clic sobre la fila y no en un botón. **La etiqueta pasa a nombrar el estado** —«Activo» / «Inactivo»—, que es lo que la columna dice que muestra (su encabezado es «Activo») y lo que ya hacía la mitad verde: ahora el color y el texto dicen lo mismo, y el texto no promete una acción que no ocurre. Con `aria-pressed`, porque es un interruptor y sin él un lector de pantalla lee «Inactivo» como etiqueta y no dice que se pueda pulsar. **Y sin `aria-label` con el verbo a propósito**: el nombre accesible tiene que contener el texto visible (WCAG 2.5.3), así que poner «Activar» donde se lee «Inactivo» sería el mismo defecto que se está arreglando, un piso más abajo. Los tests van al fichero de `ChatbotsPage` que ya existe y **no a un doble nuevo**: mi primer intento fue un fichero con su propio mock y murió porque le faltaban hooks —es la enésima vez que el doble se queda corto respecto al código, y el fichero viejo ya tiene el doble completo y mantenido—. Y un rojo más que fue mío: escribí `expect.anything()` como segundo argumento de `mutate` y el interruptor sólo pasa uno. 7 tests nuevos; `ChatbotsPage.test.tsx` **28 passed**, frontend completo **729 passed**, `tsc -b` limpio, accesibilidad **10 passed**. Verificado en navegador: los dos inactivos dicen «Inactivo» con `aria-pressed="false"`, alterna a «Activo» sin abrir diálogo, consola limpia, y el chatbot quedó como estaba |
| 2026-09-03 | LANG.2 — El modo de idioma en el panel, ofrecido desde el contrato | **Un endpoint y no un `enum`**, de las dos opciones que daba el plan, y por una razón de fondo: `fixed:<código>` es **paramétrico**, así que el conjunto de valores válidos no es enumerable y un `Literal[...]` no puede expresarlo. Enumerable sí es la lista de *modos* y la de *lenguas*, que es lo que hace falta para pintar el desplegable sin escribir los valores en React. `GET /hub/opciones/lengua` (nuevo router, `Deploy: cloud`) los da, con `requiere_lengua` y `es_por_defecto` dichos por el servidor: el panel no deduce que «fixed» lleva parámetro ni decide cuál es el defecto de la cascada. **Y el panel decía dos cosas que no eran verdad**, las dos encontradas al escribir los tests: `ChatbotsPage` ofrecía **`strict`** —una política que la factoría del grafo **nunca compuso**, o sea una opción que no hacía nada, y que desde LANG.1 además da 422— y `ValoresPorDefectoPage` sugería **`neutral`**, que no es ningún valor: es el nombre de la clase, y el modo se llama `none`. Es «el menú promete lo que la API niega» de PLAT.2, y desaparece por construcción al traer la lista del servidor. **La trampa que el endpoint existe para cerrar**: el corpus llama **`val`** al valenciano, no `ca` —`language_detector.py` traduce `ca`→`val` porque «mientras los dos códigos convivieron nunca coincidían»—, así que un panel que ofreciera «Valencià» y enviara `fixed:ca` pasaría la validación —la **forma** es correcta— y daría una preferencia que no prefiere **ninguna versión de ninguna norma**, en silencio. La validación sigue aceptando cualquier código de 2-3 letras a propósito: el catálogo es lo que el panel **ofrece**, no lo único que la plataforma **admite**, y así otra administración puede desplegar esto en gallego sin tocar el núcleo. De paso se sigue la convención del sitio: las etiquetas dicen «idioma», no «lengua», que es lo que ya decía el panel. **Verificado en navegador y contra el servidor real**: el desplegable trae los tres modos del servidor, elegir «Responder siempre en un idioma» hace aparecer el segundo con `val`/`es`/`en`, consola limpia; y la misma pregunta en catalán se responde **en catalán con `prefer`** («El límit dels contractes menors…») y **en castellano con `fixed:es`** («El límite de los contratos menores…»), las dos con `done`. El chatbot quedó devuelto a `prefer`. **Y una confesión de instrumento**: di por roto el diálogo de edición del panel tras cinco intentos de abrirlo, y no lo estaba —mis clics sintéticos no disparaban el `onClick` del `<tr>`; pinchando la fila por programa abre a la primera—. Antes de acusar al producto lo comprobé apartando mi propio cambio, que era lo correcto, pero la conclusión seguía siendo mía y falsa. 14 tests nuevos de frontend + 11 del endpoint; frontend **722 passed**, `tsc -b` limpio; `ruff` limpio |
| 2026-09-03 | LANG.1 — `language_mode` deja de estar desconectado | El mando existía y no llegaba a ningún sitio: `language_mode` viaja por **toda** la cascada —plataforma (`prefer`) → `HubOrganizacion.default_language_mode` → `HubChatbot.language_mode` → `PublicGraphConfig.language_mode`— y `_make_public_kb_rich` montaba **siempre** `DefaultLanguagePolicy`; el valor `"none"` que documenta `metadata_filter.py` no tenía ni un `if` que lo leyera. Y faltaba el modo que pide una organización monolingüe: «responde siempre en X, pregunten como pregunten» —hoy a quien escribe en catalán a un ayuntamiento castellanohablante se le contesta en catalán, sin que el organismo tenga dónde decidir lo contrario—. **El CoreGraph no se toca, y eso es el diseño, no una economía**: todo lo que hay aguas abajo cuelga de `detect()` —el «Responde en {lang}» del prompt, el `language=` de la recuperación, la segunda búsqueda y el aviso de traducción—, así que una política que devuelve `None` degrada sola y una que devuelve siempre `es` fija la lengua sola. Lo único que cambia es **qué política compone la factoría**: los tres modos son el mismo grafo con otra estrategia, que es literalmente el argumento de «framework, no generador» dado a desarrollo. **`FixedLanguagePolicy` hereda de `PreferLanguagePolicy` y solo cambia `detect`**, con lo que la lengua fijada prefiere esa versión de una norma bilingüe **dentro** de la vigencia (la regla de VIS.4: citar lo derogado en la lengua correcta es peor que citar lo vigente en la otra), y se ahorra `langdetect` en cada consulta. **El valor se valida al escribirse**: era `String(20)` libre, así que `language_mode="castellano"` se guardaba tal cual y caía a `prefer` **sin avisar a nadie**; ahora 422 con los tres modos enumerados en el mensaje, desde `core/language_mode.py` —en `core/` porque lo comparten la factoría (edge), los dos routers (cloud) y el contrato que lee el panel, y en un módulo y no en un `Enum` porque `fixed:<código>` es paramétrico y un `CheckConstraint` no podría expresarlo—. **Desviación documentada**: el plan pedía crear `NoneLanguagePolicy` y ya existía `NeutralLanguagePolicy` con ese comportamiento exacto y sin usar por nadie; se reutiliza en vez de duplicarla. 16 tests nuevos; `tests/public_graphs`, `tests/api/hub`, los valores por defecto de PLAT.3, el export de OpenAPI y el guardarraíl de higiene: **274 passed**; `ruff` limpio |
| 2026-09-03 | USR.11 — El contrato de citas, con contenido en bloques | Pedido por el usuario **antes de pasar nada a `main`**, al preguntar si los arreglos afectaban a producción. `enforce_citation_contract` era el último sitio del camino de la respuesta que seguía leyendo el contenido del modelo como si siempre fuera cadena, y se portaba de **dos maneras distintas**, que es lo peor de los dos mundos: con `sources` reales moría en la primera expresión regular (`TypeError: expected string or bytes-like object, got 'list'`), y con `sources` vacías salía por `if not sources` **devolviendo la lista**, incumpliendo su propia firma `-> str` y dejándola viajar aguas abajo con etiqueta de texto. La segunda mitad es la que no avisa, y es justo la forma del modo agéntico cuando el modelo contesta sin leer nada. **Se normaliza en la entrada con `texto_de`, no se envuelve en un `except`**: un `except` cambiaría un fallo por silencio, que es lo que USR.8 acababa de arreglar en dos sitios; y normalizar sólo hace que la función cumpla lo que su firma ya prometía. **Y hay que decir que mi primera medición fue mala**: reporté al usuario que «con una lista el contrato no se aplica y no avisa», y eso salía de una prueba que pasaba `sources=[]` —o sea que salía antes de mirar nada—; con fuentes reales sí levanta. Es la enésima vez que el instrumento miente antes que el sistema, esta vez en una comprobación de tres líneas escrita para contestar una pregunta. **Verificado en vivo con los dos modos** contra el servidor real: RAG-Normativa 3 fuentes y 6 citas, agéntico 3 fuentes y 3 citas, los dos con `done` e `interaction_id` en la BD (Normativa 33→34, agéntico 1→2). 5 tests nuevos; `tests/modules/agents_hub`, `tests/public_graphs` y el guardarraíl de higiene: **1602 passed**; `ruff` limpio |
| 2026-09-03 | USR.10 — «No tienes ningún módulo» le salía a quien sí los tenía | `RutaDeModulo` mandaba a `/sin-acceso` en **dos** situaciones distintas, y esa pantalla dice literalmente «tu cuenta existe pero **no tiene ningún módulo concedido**. Pide acceso a quien administra indicando que necesitas: informes, chatbots o curación». A quien tiene Informes y pincha un enlace a Chatbots eso le dice **algo que no es verdad**, le propone pedir lo que ya tiene, y encima le esconde que su propia pantalla sigue ahí. Se separan: sin ningún módulo se sigue redirigiendo —ahí hay que decir que no hay a dónde ir—, y con módulos y sin éste **no se cambia de dirección** y aparece `SinEsteModulo`, con la salida a lo primero que sí puede abrir. **Que la URL no cambie es la mitad del arreglo**: es lo único que dice qué parte se estaba pidiendo, y perderla convierte «pide acceso a esto» en «pide acceso a algo». Dos tests que ya existían cambiaron de expectativa y se dice por qué en el propio test: lo que fijaban —que la zona no se abre— sigue valiendo; a dónde va, no. 6 tests nuevos; frontend **708 passed**, `tsc -b` limpio. **Verificado en navegador** con tres cuentas: quien tiene Informes y pide `/personas` se queda **en `/personas`** con el texto nuevo y el enlace a Informes; quien no tiene ningún módulo sigue yendo a `/sin-acceso` con el texto de siempre; consola limpia. De paso, y visto en la misma verificación, el encabezado decía «Personas de la plataforma» a quien administra **una organización** y ve un listado acotado: ahora es «Personas de tu organización», y el criterio sale de la capacidad que concede el servidor, no de un `rol ===` |
| 2026-09-03 | USR.9 — Quien administra una organización llega a sus personas | El hueco lo encontró la verificación de USR.6 y quedó anotado en `docs/DECISION_IDENTIDAD_DE_ADMINISTRACION.md`: USR.1 abrió `PATCH /hub/users/{id}/password` a quien administra la organización de esa persona, con su razón escrita —«quien da de alta a sus probadores es quien administra su organización»—, pero `GET /hub/users` seguía siendo sólo superadministrador. O sea que **la capacidad existía por API y no por pantalla**: para usarla había que saberse el UUID de la persona. **Se parte, no se abre**, que es la forma que el propio documento señalaba: listar y fijar contraseña sí, crear personas, cambiar roles y borrar no —«un admin que pudiera crear personas con rol podría crearse un admin», que ya estaba escrito en el router—. El listado se acota con `scope_query_to_orgs` y no con un `if` en el endpoint, y **las cuentas de arranque de `superadminaccount` no se le enseñan**: no son de ninguna organización, así que colarlas en un listado acotado sería filtrarle las cuentas de la plataforma por la puerta de atrás. El reparto lo dice `GET /hub/users/capacidades` con `acciones_permitidas` y la pantalla itera (regla maestra 2): un `rol === 'superadmin'` en React sería la autorización escrita por segunda vez, que es lo que ya pasó con `puede_fijar_contrasena` antes de USR.3. **Y la pantalla sale del módulo `plataforma` a un módulo propio, `personas`**: tenerla ahí obligaba a dar los modelos de LLM, las organizaciones, los tokens y los módulos para poder dar lo primero — el caso de «Modelos LLM» de PLAT.2 **al revés**. Módulo en el catálogo (que es tabla y no `Enum`, así que entra por migración) y concedido a quien ya tenía `plataforma`, para que mover la pantalla no se la quite a quien la usaba. **Dos trampas medidas**: (1) copiar el `ON CONFLICT (subject_id, module_code)` de la migración de PLAT.1 **falla**, porque MT.5 añadió `subject_type` y `organizacion_id` a esa clave única —y por lo mismo hay que copiar los cuatro campos, o conceder «en todas» a quien tenía `plataforma` en una sola organización le ampliaría el alcance—; (2) `RUTA_DEL_MODULO` necesita su fila o quien **sólo** administre personas aterriza en `/sin-acceso` teniéndolo, que es la nota que PLAT.2 ya había dejado escrita ahí. Cuatro tests preexistentes cambiaron: tres enumeraban los cuatro códigos del catálogo a mano —ahora salen de `MODULOS_INICIALES`, que es la semilla— y el de IDE.3 fijaba «el listado es sólo de superadmin», que es exactamente lo que este prompt cambia. Migración `c8f1b5d92e47` aplicada. 12 tests nuevos de integración + 5 de la pantalla + 3 de la ruta; frontend **707 passed** y `tsc -b` limpio |
| 2026-09-03 | USR.8 — El chat agéntico no terminaba nunca, y nadie se enteraba | **El hallazgo, medido**: el asistente con `retrieval_mode='MD_AGENT_SELECTOR'` tenía **0 interacciones registradas** mientras los tres de `RAG` tenían 33, 9 y 1. No es que se usara poco: es que **todas** sus conversaciones se perdían —sin interacción guardada, sin consumo contabilizado y sin `interaction_id`, o sea sin valoración posible— y el cliente recibía la respuesta y se quedaba esperando para siempre porque nunca llegaba el `done`. La causa, del log: `"".join(collected_tokens)` con `TypeError: sequence item 0: expected str instance, list found`. **Son dos defectos y se arreglan los dos, porque el segundo es el que lo hizo invisible durante días**: (1) el `content` de Gemini viene como **lista de bloques** en cuanto la respuesta tiene más de una parte, y el flujo de tokens lo daba por cadena —el `delta` viajaba al cliente como lista, además—; (2) todo lo que va **después** del bucle del grafo —la interacción, la traza, la cuota, el commit y el propio `done`— estaba **fuera de cualquier `try`**, así que un fallo ahí no producía ni un evento `error`. Un cliente no puede distinguir «sigo pensando» de «me he muerto», y ésa es la diferencia entre un fallo y un fallo invisible: se emite `error` y **no** un `done` con datos inventados, que dejaría al cliente con un `interaction_id` que no existe. **Tercera mordida del mismo problema** —el docstring de `core/llm_text.py` la había anunciado con estas palabras: «falla más tarde y en otro sitio»—, así que en vez de arreglar el sitio se arregla la clase: `texto_de` pasa a **desenvolver el mensaje entero** y no sólo su `content`, porque el desenvoltorio se escribía a mano en **ocho sitios** y siempre con la misma forma, la que protege del caso raro (que no llegue un mensaje) y deja pasar el frecuente (que el `content` sea una lista). Los ocho quedan en `texto_de(respuesta)` y un guardarraíl sobre `server/app` prohíbe que la forma vuelva. **Y el guardarraíl gemelo de USR.5 estaba pasando en vacío**: su `parents[5] / "app"` no existe, o sea que recorría un directorio inexistente y no encontraba nada por construcción — corregido a `parents[4]`, que es el octavo caso del proyecto en que el instrumento miente antes que el sistema. 10 tests nuevos; `tests/modules/redaccion`, `tests/modules/agents_hub`, `tests/api` y el guardarraíl de higiene: **2513 passed, 1 skipped**; `ruff` limpio |
| 2026-09-03 | USR.7 — Cambiar la propia contraseña, que no existía en ningún rol | `POST /auth/me/password` con `{password_actual, password_nueva}`, y el formulario en el **menú de la propia cuenta** —junto al correo y a cerrar sesión, porque es lo que se hace con la cuenta y no una pantalla de administración—. **La actual se exige siempre, también a un superadministrador**, y no es ceremonia: con el token basta para actuar, pero no tiene que bastar para quedarse la cuenta, así que una sesión robada sin la contraseña no puede cerrarle la puerta a su dueño. **Una sola ruta para las tres clases** con login local, resolviendo la tabla por **la forma del `user_id`** y no por el rol: desde USR.6 un administrador o un superadministrador nuevos son filas de `hub_users` con UUID, mientras los de las tablas viejas traen un entero o el `partner_id` de texto — preguntar por el rol primero le daría la tabla vieja a una persona nueva. `limitar_login` **también aquí**, porque si no es un oráculo para adivinar la actual a ritmo de red y encima con el token en la mano; y **el mismo 401** cuando la actual no casa y cuando la cuenta no tiene hash, que distinguirlos diría si esa cuenta usa login local. De paso, `MINIMO_CONTRASENA` deja de estar escrito tres veces: es el momento que USR.1 anotó como bueno para sacarlo, ahora que hay tres contratos que lo piden. **Cuatro dobles de test se quedaron cortos y hay que decirlo**: `AppLayout` monta ahora el componente nuevo, así que los cuatro ficheros que doblan `generated/auth/auth` sin `useCambiarMiPassword` hacían que el layout entero no montara — 19 rojos que parecían del menú, de la marca y de la sección de plataforma, y eran del doble. Es la séptima vez en el proyecto que el instrumento miente antes que el sistema. Verificado en navegador de punta a punta: cambio hecho desde el panel con su confirmación, **la contraseña vieja pasa a 401 y la nueva a 200**, y con la actual equivocada sale «La contraseña actual no es correcta» sin cerrar el formulario y con el campo vacío —es una contraseña, no un borrador—. Frontend: **693 passed** en 91 ficheros, `tsc -b` limpio; 11 tests de integración nuevos en el servidor y 8 de la pantalla; suite completa de backend **3642 passed, 1 skipped, 0 fallos** (13 m 12 s) |
| 2026-09-03 | USR.6 — La identidad de administración: decisión **(b)** y varias personas por organización | **La decisión, escrita antes de tocar código** en `docs/DECISION_IDENTIDAD_DE_ADMINISTRACION.md`: la identidad de administración es **`HubUser` con `role='admin'`**, y `AdminAccount` se queda con el partner y la facturación. **Lo que decidió no fue la elegancia, fue el recuento en producción**: `adminaccount` **0 filas**, `clientaccount` **0**, `hub_users` **0** y `superadminaccount` **7** —la propia y los seis probadores elevados—. Con `adminaccount` vacía, (b) no migra ni una fila; con (a) —la tabla puente— habría dos clases de administrador **para siempre** en la pantalla de personas y encima habría que arreglar aparte la deuda de las seis cuentas. **Y lo más importante del prompt: (b) ya era verdad** después de USR.1–USR.3, así que aquí no se construyó, se **comprobó** — y se dice como es, los seis tests nuevos pasaron a la primera y son guardarraíles, no un RED. Verificado en navegador con dos personas `role='admin'` sobre la Universitat Jaume I: **las dos entran con su contraseña**, las dos ven **los mismos 4 asistentes de la UJI** y ninguna el de Organización Demo, ninguna alcanza `/plataforma` (redirige a `/sin-acceso`), y sus `user_id` son distintos — que es justo la atribución por persona que la credencial compartida perdía. Y **la tercera cosa que no se podía hacer ya se puede sin código nuevo**: un superadministrador nuevo es un alta más en la pantalla de Personas con su contraseña por USR.1, y `superadminaccount` se queda sólo con la cuenta de instalación. **Tres cosas se dejan escritas y NO se hacen aquí, con su motivo**: (1) retirar el login de `AdminAccount` —dos endpoints, dos ramas del ACS, la segunda puerta del `LoginPage` y la cuenta de desarrollo—, que es la consecuencia de (b) pero mezcla «decidir» con «desmontar», y el propio prompt avisaba de que si no cabe se parte **hacia** (b); (2) una persona administrando **varias** organizaciones, que hoy no pide nadie y cuyo camino queda escrito (tabla puente que sustituya a la columna, no volver a `AdminAccount`); y (3) el hueco que apareció verificando — **la pantalla de Personas es sólo de superadministrador, así que un admin no puede llegar a la capacidad que USR.1 le dio**: el endpoint le deja fijar la contraseña de alguien de su organización, pero `GET /hub/users` exige superadmin, o sea que existe por API y no por pantalla; abrirla exige partirla (listar y contraseña sí, crear y cambiar roles no) y eso es una decisión de permisos con peso propio. `tests/modules/agents_hub/integration`: **615 passed**; `ruff` limpio |
| 2026-09-03 | USR.5 — El correo de administrador es único | Índice único sobre `adminaccount.email` (`ix_adminaccount_email`), declarado **en el modelo y no sólo en la migración** —lo que copia `create_all` es el modelo, y así una instalación que no pase por Alembic también lo tiene—, con `downgrade` probado. **El defecto que cierra**: dos sitios consultan ese correo esperando una fila —`login_admin` y el ACS de SAML— y con dos filas del mismo correo cuál contesta depende del orden que devuelva Postgres, que sin `ORDER BY` no está definido; el síntoma sería un **401 intermitente**, la misma contraseña entrando unas veces y otras no. `SuperAdminAccount` ya lo tenía único por esta misma razón. **Ahora era gratis**: medido, `adminaccount` tiene 1 fila en desarrollo y 0 en producción, así que el índice entra sin migración de datos. **La guarda del `upgrade` no es adorno**: la migración corre también en bases ajenas —cada organización despliega desde su fork— y si hay duplicados falla con un mensaje que **nombra los correos repetidos y dice qué hacer**, en vez del error de Postgres que sólo dice qué ha pasado. `login_admin` pasa de `.first()` a `.one_or_none()`, y **el ACS de SAML también** (`identity_service.py`), que el prompt no mencionaba y tiene exactamente el mismo problema. Lo que el prompt mandaba comprobar y no suponer: `SuperAdminAccount.email` y `HubUser.email` **ya** eran únicos (con test que lo fija), y `ClientAccount.email` **no** lo es y se deja así a propósito — no hay ni una consulta que lo lea, así que no hay ambigüedad que cerrar, y el test que se añade no exige unicidad sino **que siga sin consumidor**: el día que alguien escriba esa consulta, el rojo le recuerda que hace falta el índice. **Y otro rojo del andamio**: `test_auth_login.py` doblaba la sesión con `.first()` y sin `.one_or_none()`, así que al cambiar el endpoint devolvía un `MagicMock` y el test fallaba con un 401 que parecía de la lógica. `alembic current`: **`b7e0a4c81259`**. `tests/core` + `tests/api` + instalación limpia: **563 passed**; `ruff` limpio |
| 2026-09-03 | USR.4 — El recorrido de un probador, y **el 500 que sólo se ve verificando** | **El recorrido entero pasa, y encontró un defecto que los tests no podían ver.** Al entrar como la persona, `POST /auth/user/login` devolvía **500**: `sqlalchemy.exc.MissingGreenlet` en `auth_router.py:214`, `str(persona.id)`. La causa exacta: `commit()` **expira los atributos** de la instancia, así que leerlos después dispara una recarga perezosa —IO síncrona— fuera del greenlet de SQLAlchemy. `login_admin` no lo sufre porque no commitea; éste sí, porque anota `last_login_at`. Arreglado moviendo la construcción del `UserInfo` **antes** del commit, con un comentario que dice que no es estilo. **Por qué los tests de USR.2 no lo vieron, que es la lección**: doblan la sesión con un `MagicMock`, y un doble acepta cualquier consulta y devuelve lo que se le dice. De ahí un fichero nuevo, `test_usr4_login_contra_la_base_real.py`, que hace el recorrido con la sesión y la fila de verdad — y que en el camino tuvo **su propio rojo falso**: doblar `get_session` con la sesión de la fixture del hub daba `AttributeError: 'AsyncSession' object has no attribute 'exec'`, porque `deps.get_session` es la de **SQLModel** y la de la fixture la de SQLAlchemy; se arregló montando una sesión de SQLModel sobre la misma conexión. Es la sexta vez en el proyecto que el instrumento miente antes que el sistema. **Los ocho pasos, con evidencia**: alta en el panel con rol `user` y organización UJI; contraseña fijada desde la pantalla con su confirmación y sin volver a mostrar el valor; login 200; conversación con un chatbot `authenticated` de su organización con respuesta fundada y citas; **no ve nada del panel** —ni menú ni sección de plataforma— y `/plataforma/usuarios` redirige a `/sin-acceso`; el chatbot de otra organización responde **403 `ACCESS_MODE_FORBIDDEN`** con «the actor does not belong to this chatbot's organization»; fijar la contraseña de alguien, incluida la suya, **403**; desactivada, el login **401** con el mismo cuerpo. Y el hash **no aparece** en el JSON real del listado (comprobado sobre la respuesta, no sobre el DOM). **Dos hallazgos que se reportan y NO se arreglan aquí**, porque son de otros subsistemas: (1) el chatbot con `retrieval_mode='MD_AGENT_SELECTOR'` termina el flujo SSE **sin evento `done`** —medido: 18 `token` y 0 `done`, mientras el de `RAG` da 34 `token` y 1 `done`—, así que un cliente recibe el texto pero nunca las fuentes ni el `interaction_id` de la valoración; (2) una persona con rol `user` y sin módulos concedidos aterriza en «Todavía no tienes acceso a ningún módulo», así que un probador del piloto necesita que se le conceda uno o entra sólo por API/widget |
| 2026-09-02 | USR.3 — El panel: la tercera puerta del login y fijar la contraseña | `LoginPage` prueba **tres** puertas en orden (superadmin → admin → persona) y el mensaje de error **sólo** aparece si fallan las tres, sin decir cuál conocía el correo: decirlo sería un oráculo para saber en qué tabla está alguien, y enseñarlo antes de tiempo es lo que pasó el 2026-09-01, cuando ninguno de los 401 de la cadena distinguía «contraseña mala» de «esta cuenta no tiene login local». La tercera puede contestar **404** —`LOCAL_USER_LOGIN_ENABLED=false`— y se trata igual que un rechazo, con su test. En `UsuariosPage`, la acción «Fijar contraseña» por fila con `react-hook-form` + `zodResolver`, el valor **nunca se vuelve a mostrar** (queda la confirmación, no el secreto) e i18n en las **tres** lenguas, verificado con un aserto de que los tres ficheros tienen las mismas claves. **Quién puede lo dice el servidor**: `puede_fijar_contrasena` se añade a `UsuarioRead` calculado con `puede_acceder`, **la misma función que autoriza el endpoint**, no una copia de su regla; el DTO lo trae falso para las cuentas de arranque de `superadminaccount`, que viven en otra tabla. Con eso, tres tests de servidor comprueban que el DTO y el endpoint no pueden contradecirse. **Desviación documentada**: el login navegaba a `/hub` fijo y ahora va a `/`. INF.7 centralizó el aterrizaje en `Aterrizaje` —cae en el primer módulo concedido— y el destino escrito en el login era la misma decisión en dos sitios; con la tercera puerta la copia daba la respuesta equivocada, porque una persona `user` sin el módulo de chatbots aterrizaba en `/hub` y rebotaba a «sin acceso». Se actualizó el test que fijaba `/hub`. Contrato regenerado dos veces —la primera exportación fue antes de añadir el campo al DTO y Orval leyó el `openapi.json` viejo, así que el modelo salió sin él—. Frontend completo: **685 passed** en 90 ficheros; `tsc -b` limpio; servidor: 14 passed en el fichero de USR |
| 2026-09-02 | USR.2 — `POST /auth/user/login`, el login de la persona | El tercer login local, escrito **al lado de `login_admin` y no de memoria**, que es la razón de que este prompt exista separado del USR.1: el de Admin nació comprobando sólo que el correo existiera (A1) y el de superadmin aprendió por separado el hash señuelo y el límite de intentos. Sus tres defensas, cada una con test: `limitar_login` **lo primero**, `verify_password` **siempre** —contra el hash o contra el señuelo— y **el mismo 401 en los cuatro casos** (no existe, sin hash, inactiva, contraseña mala), con la inactiva también en 401 y no en 403 porque un 403 diría que el correo existe. El JWT lleva el rol real de la persona y su organización, con los grupos de SAML vacíos: **una persona no es un administrador**, y hay un test por cada guarda —superadmin y admin— porque ése es el fallo que ascendería a un probador del piloto. Sin organización entra pero no abre ningún chatbot de organización, comprobado **llamando a `assert_chatbot_access`** y no razonando sobre el claim. `LOCAL_USER_LOGIN_ENABLED` (defecto `true`) con **404 y no 403** cuando está apagado: un 403 confirmaría que la ruta está ahí. **El rojo que no era del código**: seis tests fallaban al correr el fichero entero y pasaban sueltos — el limitador de intentos es del proceso y son 10 por minuto y por IP, así que a partir del décimo login del fichero llegaba un **429** y el rojo parecía de la lógica. Se arregla con un `autouse` que llama a `limitador.reiniciar()`, que para eso existe. Es la cuarta vez en el proyecto que el instrumento miente antes que el sistema, y la segunda esta misma sesión. Contrato regenerado (151 rutas). `tests/api` completo **428 passed**, `tests/core` + higiene **137 passed**, `ruff` limpio |
| 2026-09-02 | USR.1 — `hashed_password` en `hub_users` y la vía para fijarla | La columna (nullable, `String(255)`) y `PATCH /hub/users/{id}/password` (`setUsuarioPassword`, 204). **La diferencia deliberada con su gemelo `setAdminPassword`**, que es sólo superadmin: aquí **un admin puede fijar la contraseña de una persona de una organización que gestione**, porque quien da de alta a sus probadores es quien administra su organización y obligar a pasar por el superadministrador convierte cada alta en un cuello de botella. La acotación la resuelve `assert_org_access` de `tenancy.py` y no un `if` del endpoint; efecto secundario correcto: una persona **sin** organización (nivel plataforma) sólo la alcanza un superadministrador, que es lo que `puede_acceder` responde ante una organización nula. `SetPasswordRequest` se **importa** de `auth_router` en vez de escribir un segundo modelo con el mismo `min_length=12`: dos validaciones acabarían divergiendo y la que se relajara sería la débil (si USR.7 necesita una tercera, ahí es el momento de sacarla a `core/`). **El autogenerate de Alembic propuso, además de la columna, borrar 40 tablas** —el legado de AutomatIA que sigue vivo en la base de desarrollo y ya no está en el metadata— **y tres `NOT NULL`** de deriva real (`hub_content_findings.created_at`, `hub_provider_credentials.created_at` y `.updated_at`); la migración se reescribió a mano con la única columna del prompt y **eso queda explicado en su docstring**, porque el próximo que ejecute autogenerate se encontrará lo mismo. Borrar esas tablas es del Bloque NIC, con su inventario delante. Antes de migrar hubo que aplicar `e2b3c4d5f6a7`, que estaba pendiente en la base de desarrollo: se comprobó primero que su condición no tocaba nada (dimensión ya en 1024) y después que los **163.965** fragmentos con vector seguían ahí. `alembic current`: **`a6d9893f743a`**, con `downgrade` probado (la columna desaparece y vuelve). Y la respuesta a lo que el prompt mandaba comprobar: **el hash NO cruza la frontera edge** — la sync API es un stub 501 y su instantánea enumera campo a campo clientes, chatbots, configuraciones y plantillas; `HubUser` no está. El test que se añadió no mira eso, mira lo de mañana: que la instantánea no declare nunca un campo de contraseña, porque el día que alguien implemente `get_edge_config` con un `model_dump` del ORM el hash se iría al edge sin que nadie lo decidiera. 11 tests nuevos; `tests/modules/agents_hub/integration` completo **594 passed**; higiene, ámbito, login y instalación limpia **41 passed**; `ruff` limpio |
| 2026-09-02 | Orden de los diez bloques que quedan, y la rama `desarrollo` | **Orden acordado** sobre una propuesta razonada: USR → LANG → REG → VAS → NIC → REPO → PLG → DIN → FUN, con PRC en paralelo y a tandas porque depende de dos prerrequisitos externos. **49 prompts.** El criterio que ordena: primero lo que cierra una exposición viva (USR: siete superadministradores en producción, seis de ellos probadores, con la misma contraseña y sin poder cambiarla), después lo barato que se encarece con el tiempo (LANG: dos prompts hoy, dos prompts más una migración cuando haya datos puestos), después lo que desbloquea a otros (REG desbloquea VAS y FUN.6), y al final lo grande que nadie está esperando. NIC y REPO se quedan en ese orden por decisión previa del usuario, con la advertencia de que **REPO tiene ventana**: antes de que exista el primer *fork*. **Y el trabajo pasa a la rama `desarrollo`**: `deploy.yml` sólo dispara con `push: branches: [main]`, así que empujar a la rama no despliega y el despliegue se hace a propósito, por bloque o conjunto de bloques. Lo motivó un despliegue que no hacía falta —un commit que sólo tocaba `scripts/publica_sitio_corpus.sh`, y `scripts/` no puede ir en `paths-ignore` porque de ahí salen ficheros que sí viajan a la máquina—. **CI y DCO sí se añaden a la rama**: empujar sin desplegar era el objetivo, empujar sin comprobar nada no |
| 2026-09-02 | DOM.5.1 — El favicon del portal, en el bucket | `normativa.uji.es/favicon.ico` daba **404** en cada carga: las páginas del corpus no declaran icono, así que el navegador lo pide **en la raíz del origen** y el proxy lo mandaba al bucket, donde no estaba. Lo detecté verificando DOM.5 y lo dejé anotado como pendiente menor; el usuario pidió cerrarlo y **que el icono saliera del portal**, que es el que tiene color. Es el ICO de 16×16 de `static.uji.es/templates/uji2016_plantillas/static/images/favicon.ico` —el rótulo UJI en verde, amarillo y rojo—; el logo que ya viajaba dentro de `estil_uji.py` como data URI no servía, porque es el rótulo completo de 181×29 y a 16 px no se lee. **Un solo fichero en la raíz vale para las 313 fichas y los cuatro cercadores**, porque la petición automática del navegador va siempre a la raíz del origen aunque la página esté en `/html/`: ninguna página tiene que declararlo. Añadido a la lista enumerada de `scripts/publica_sitio_corpus.sh` para que no se pierda en la republicación siguiente, y commiteado como fichero en el repositorio del corpus (`16b7660`) porque no lo produce ningún programa. Verificado 200 con `Content-Type: image/x-icon` por el dominio y por la URL del bucket |
| 2026-09-02 | DOM.5 — Desplegado, verificado y qué se hace con el provisional | **Despliegue correcto y el reparto medido en vivo**: `/` 200 (la portada, no el login), `/cercador` 301 → `cercador.html`, `/gerencia` 301 → `cercador_gerencia.html`, `/html/<norma>.html` 200 por el proxy al bucket, `/health` 200, `/panel/` 200 y `/api/v1/docs` 404 (SEC.7 sigue cerrado). El panel navega bajo su prefijo **incluso recargando en una ruta profunda** —`/panel/curation/sites` devuelve el SPA y el router redirige a `/panel/login?from=%2Fcuration%2Fsites`—, con los tres `/panel/assets/*` en 200 y **ningún 404**. El nombre provisional sirve exactamente lo mismo, panel incluido. **La verificación destapó un defecto y se arregló ahí mismo**: `https://…/panel` sin barra contestaba `Location: http://normativa.uji.es/panel/`, porque nginx compone la URL absoluta con **su** esquema y el suyo es `http` (el TLS lo termina Caddy). Funciona —Caddy devuelve a https y el HSTS de la aplicación evita el salto—, pero es innecesario: `absolute_redirect off;` y un test que lo fija. **Las citas, abiertas de verdad**: 9 URL citadas en tres consultas reales, todas 200 y **todas con su ancla dentro del HTML**. Y aquí el medidor volvió a mentir antes que el sistema: la primera versión de la comprobación grepeaba el flujo SSE completo y daba 11 problemas de 20 —trozos de URL partidos entre chunks (`/html/20190529_U`) y paréntesis de markdown pegados al ancla (`#faq-5).`)—; leyendo el campo `sources` del evento `done`, 9 de 9 limpias. CORS medido con control: el dominio y el bucket reciben permiso y un origen inventado no. **El provisional NO se retira**: apuntan a él el paso de comprobación de `deploy.yml`, la vigilancia de tiempo de actividad con su alerta y los enlaces ya enviados; recomendado un mes. `docs/DESPLIEGUE_PROTOTIPO_GCP.md` gana la sección **3.sexies** con el reparto completo, y se corrigen dos cosas que ya no eran ciertas en su §4: «sin dominio propio» y «el asistente de Gerencia es interno» |
| 2026-09-02 | DOM.4.1 — Una lengua cada vez en la portada, con el selector del sitio | **Lo vio el usuario probando, y tenía razón**: la portada que DOM.4 acababa de publicar mezclaba valenciano y castellano dentro del mismo párrafo, cuando el resto del sitio tiene una versión por lengua y un selector arriba a la derecha. Yo escribí la presentación bilingüe en línea (`<span lang="es">` detrás del texto en valenciano) sin comprobar antes cómo lo resolvía el sitio: el cercador tiene tabla de textos por lengua, botones Valencià/Castellano y `?lang=` en la URL, y las fichas tienen un `.lang` que lleva a la otra versión del documento. La portada ahora hace lo mismo que el cercador y **comparte su clave de `localStorage` (`uji_lang`)**, así que la lengua elegida en la portada es la que abre el cercador. **Dos diferencias deliberadas**: la portada **recarga** al cambiar de lengua (no tiene estado que perder, al revés que el cercador con sus filtros puestos) y la portada del paquete de revisión de Gerència gana el selector y su tarjeta traducida pero sigue sin presentación ni pie. **Y el arreglo destapó dos restos del mismo defecto**: el widget abría en valenciano sobre la página en castellano —`data-lang` es fijo y se lee al inicializar, así que el ajuste tiene que ir **antes** de que cargue el bundle, y por eso vive en `estil_uji.assistent(segueix_selector=True)` y no en el guion de la página— y la cabecera del chat decía «Assistent normativa» en castellano, de donde sale `ASSISTENT_TITOL_ES`. **El seguimiento se aplica sólo donde el selector cambia la interfaz** (portada y cercador) y **nunca en una ficha**: allí la lengua es la del documento, no una preferencia, y seguir el `localStorage` haría hablar al widget en una lengua y al documento en otra. Verificado en navegador en las dos lenguas, con la consola limpia. Commit en el repositorio del corpus (`bd4a81b`), que incluye además el `data-title` del widget, sin commitear desde una sesión anterior y ya publicado |
| 2026-09-02 | DOM.4 — La portada ampliada y el sitio republicado al dominio | **El plan pedía regenerar las 313 páginas y no hacía falta**: medido, las fichas no llevan widget dentro (cero de 313) y ninguna menciona el nombre provisional, así que no tienen URL de API que actualizar. Sólo cambian los cuatro HTML sueltos —`index.html` y `cercador.html` con el asistente de normativa, `cercador_gerencia.html` e `index_gerencia.html` con el económico-administrativo—, cada uno con SU chatbot, SU credencial (leída del HTML publicado, porque en claro no se recupera de la base) y SU `data-title`. Eso reduce el riesgo del paso a casi nada, y se anota porque la próxima vez ahorra media hora. La portada gana presentación del proyecto (qué es el sitio, qué hace el asistente y que lo que vale es el texto oficial), las **dos** tarjetas de entrada y el enlace al panel al pie. **Dos decisiones de diseño en el generador**: las tarjetas se componen en `main()` y no dentro de la plantilla, porque la portada del paquete de revisión de Gerència usa la misma y **no** debe cambiar —comprobado: sigue con su tarjeta y sin presentación ni pie—; y el enlace al panel es **URL absoluta por entorno** (`URL_PANELL`) y no `/panel/`, porque esa misma portada sigue siendo accesible por la URL del bucket mientras siga pública, y allí una ruta absoluta apuntaría a un `/panel/` que no existe. El asistente de Gerència es público y el usuario confirmó que puede seguir siéndolo, así que su tarjeta se queda en la portada pública. **La publicación se bloqueó a mitad** por credenciales caducadas de `gcloud` («Reauthentication failed. cannot prompt during non-interactive execution»): la reautenticó el usuario y se publicó a continuación. Verificado en el bucket: `data-api-url=https://normativa.uji.es/api/v1` en los cuatro, las dos tarjetas y el pie. CORS medido con control: el origen del bucket recibe permiso, uno inventado no, y el del dominio **todavía no** —lo será tras el despliegue de DOM.5—. Cambio del generador commiteado en el repositorio del corpus (`96b2b3c`) |
| 2026-09-02 | DOM.3 — Las citas y los orígenes apuntan al dominio | **La configuración del despliegue no vive en el repositorio**: la escribe `deploy.yml` desde las variables del repositorio de GitHub, así que esto fue `gh variable set` y no una edición. `CORPUS_SITE_BASE_URL` pasa de `https://govgenai-normativa-uji.storage.googleapis.com` a `https://normativa.uji.es`, `CORS_ALLOWED_ORIGINS` suma el origen del dominio **conservando el del bucket** (aditivo a propósito: mientras la URL del bucket siga pública, sus páginas son de otro origen) y `CORPUS_BUCKET` es nueva, para el proxy de DOM.1. Los valores anteriores quedan escritos aquí porque son el camino de vuelta. **Los tests de este prompt no fueron RED y se dice como es**: la forma de la URL (`{base}/html/{slug}.html#ancla`) y la lectura de la base desde el entorno ya estaban cubiertas por `test_citas_al_sitio_publicado.py` desde PUB.3; lo que se añadió son tres guardarraíles sobre invariantes que ya se cumplían —ningún nombre del despliegue escrito en `server/app/`, la forma de la URL fija, y las tres variables presentes en el heredoc del workflow— más el único que sí era rojo antes de DOM.1, el de `vars.CORPUS_BUCKET`. La tabla de variables de `docs/DESPLIEGUE_PROTOTIPO_GCP.md` §2 y el paso 7 de su orden quedan corregidos, con la nota de que son variables de GitHub y no ficheros. **Las tres consultas reales con `curl` sobre las URL citadas se hacen en DOM.5**, después de desplegar: hoy la variable nueva no está en la máquina |
| 2026-09-02 | DOM.2 — El panel bajo `/panel/`, sin literales en el código | **Tres capas y un solo prefijo.** `base` en Vite (por `VITE_BASE_PATH`, con la raíz por defecto para no invalidar `arranque.bat`, `.env.example` y una docena de guiones de pruebas manuales que dicen `localhost:5173`), `basename={import.meta.env.BASE_URL}` en el `BrowserRouter` —así el prefijo se dice una vez y los tests siguen montando en la raíz— y el `dist` copiado a `html/panel` con su `location /panel/` en nginx. **La decisión de fondo**: el prefijo lo sirve la imagen **de verdad**, no un `handle_path` que lo despoje por delante; con el despojo, el contenedor abierto directo sirve en la raíz mientras su HTML pide `/panel/assets/…`, funciona sólo con el proxy puesto y esa diferencia se descubre depurando. El `try_files` cae en el `index.html` **de dentro**: con el de la raíz, un recurso inexistente devolvería HTML con tipo de contenido de JavaScript y el navegador diría «Unexpected token '<'». Medido en la construcción real: el HTML sale con `/panel/assets/admin-*.js` y `/panel/favicon.svg`, o sea que Vite reescribe también lo que está escrito a mano en `index.html`. **Una trampa del entorno, no del código**: `VITE_BASE_PATH=/panel/ npx vite build` desde Git Bash produce `href="/Users/fabra/AppData/Local/Programs/Git/panel/favicon.svg"` — MSYS convierte el valor con barra inicial en ruta de Windows. Se construye desde PowerShell; dentro de la imagen no pasa porque el `build` corre en `node:24-alpine`. El SSO **no se toca**: `SAML_FRONTEND_RETURN_URL` es de desarrollo en los dos ficheros donde está escrita y en producción no existe (no hay ningún secreto de SAML en el inventario), así que lo que se hizo fue dejar dicho, donde alguien lo lea al configurarlo, que la vuelta del ACS lleva el prefijo dentro. 7 tests nuevos en `test_dom2_el_panel_bajo_su_prefijo.py`, entre ellos el que ata los tres prefijos —Dockerfile, nginx y Caddyfile— porque divergen sin dejar ningún log. Frontend: **662 passed** en 89 ficheros (`--no-file-parallelism`), `tsc -b` limpio; infra tocada: 59 passed |
| 2026-09-02 | DOM.1 — Caddy: un solo reparto para los dos nombres | **El dominio dejó de enseñar el login.** Al resolver el DNS, `normativa.uji.es/` servía el panel: no era un fallo, era lo que decía `(govgenai_rutas)`, que mandaba al frontend todo lo que no fuera `/api/*` ni `/health`, y la plantilla del sitio con certificado propio importa ese fragmento. El reparto nuevo manda `/` y el resto al bucket del corpus, `/panel` y `/panel/*` al frontend, y deja API y salud donde estaban. **Dos hallazgos que cambiaron el código escrito**: (1) la raíz del bucket **no** es su `index.html` —devuelve 200 con `ListBucketResult`, el inventario de todos los objetos, porque el mapeo lo hace la configuración de sitio web de GCS y eso pide el balanceador que el bloque evitó—, así que hay `rewrite / /index.html` con su test; (2) `handle /panel /panel/*` **no es sintaxis válida**: `handle` acepta un solo argumento, y con eso Caddy no arranca — o sea que se habría llevado por delante también el nombre provisional al que apuntan las 313 páginas publicadas. Lo cazó `caddy validate` dentro del propio test, que es la razón de tenerlo: leer el fichero no lo habría visto. Las dos direcciones cortas (`/cercador`, `/gerencia`) van como redirección permanente y no como copia, para que la página canónica siga siendo una y los enlaces relativos de dentro del sitio sigan resolviendo. El nombre del bucket llega por `CORPUS_BUCKET`, exigido en el compose con `:?` porque un valor vacío compone `https://.storage.googleapis.com` y rompe portada, cercadores y fichas a la vez sin decir por qué. 12 tests nuevos en `server/tests/infra/test_dom1_reparto_publico.py`; `server/tests/infra` completo: **345 passed** (13 m 10 s, `-n0`) |
| 2026-09-02 | Planificación: Bloque PLG ampliado — las estrategias también por *entry points* | **El usuario señaló que la carga de estrategias por plugin era lo aceptado con desarrollo y no estaba en el plan**: PLG descubría perfiles y pipelines y había documentado como desviación que «una estrategia suelta no es seleccionable por configuración». El argumento se resuelve al revés: **hacer las estrategias seleccionables**, eje a eje. Nuevo **PLG.2** (Opus): `StrEnum` de cuatro ejes (los protocolos que compone el `CoreGraph`: retrieval, merge, template, language), protocolos `@runtime_checkable`, `StrategyRegistry` con el núcleo registrando por el mismo camino, grupo `govgenai.strategies` con nombre `<eje>.<nombre>` verificado al arrancar contra el protocolo del eje, el perfil operativo compone **por nombre** con test de regresión de que monta exactamente las mismas cuatro clases, columnas JSONB `default_estrategias` (organización) y `estrategias` (chatbot) **heredables clave a clave**, sobreescritura aplicada en `GraphFactory.build` (donde ya se resuelven rewrite_llm y agentic_loop; ni perfiles ni `CoreGraph` la conocen), fallo en alto si el nombre no está registrado, y la traza lo recoge por valor sin cambios. **El bucle agéntico no es eje**: lo monta el modo de recuperación con tres colaboradores que habría que separar antes; queda como candidato. El panel pasa a PLG.3 con un select por eje y «heredar». La desviación anterior se retira del plan, de PROJECT_STATE y de la decisión 9 del informe. |
| 2026-09-02 | Planificación: Bloque VAS — verificaciones como servicio (citas, vigencia, auditoría estática) | **De los seis candidatos de `docs/GOVERNANCA_PER_API.md`, los tres baratos entran al plan** como bloque de 4 prompts después de REG. Criterio de selección: ninguno toca el modelo, ninguno requiere el corpus para funcionar, y los tres son deterministas, así que exponerlos no crea una segunda fuente de verdad. Decisiones: **sin segunda implementación** (cada servicio envuelve la función que ya usa el motor, con test de paridad); **el texto y el código no se guardan**, solo `code_sha256` (misma regla que REG.3); **un scope** `verificaciones:use`; **solo la auditoría registra evento REG** — auditar es un acto de gobernanza (la revisión posterior del nivel 2 de la Instrucció) y verificar citas o consultar vigencia son comprobaciones sin estado cuya actividad ya registra la aplicación por REG.2; **la vigencia respeta tenencia y filtro cerrado** con 404 (no revelar existencia); **la caja de herramientas se publica desde los frozenset del auditor**, con `version_auditor`, para que la UADTI la contraste con sus Guías sin que documento y sistema diverjan. Los otros tres candidatos (parámetros de gobernanza, depósito de manifiestos, asistentes externos con escenarios) siguen sin planificar hasta la primera aplicación externa real. Decisión 11 del informe actualizada. |
| 2026-09-02 | Planificación: Bloque FUN alineado con la Instrucció 02/2026 (desarrollo ciudadano governat) | **El usuario aclaró el propósito del catálogo**: no son los desarrolladores profesionales, es **gobernar el desarrollo ciudadano** que regula la Instrucció 02/2026 (copia en `_local/`), y el **nivel 1** (uso personal: macros, scripts locales, Colab) **vive fuera de la plataforma**; la plataforma entra en el **nivel 2**, así que registrar una función en el catálogo ES el acto de declararla y compartirla «por los canales que indique la UADTI». Al contrastar el diseño con la Instrucció aparecieron una contradicción y tres huecos: (1) **aprobación humana previa bloqueante** para toda función, que la Instrucció prohíbe expresamente en el nivel 2 («declaración responsable y registro, revisión posterior o por muestreo, nunca como condición previa») — corregido: registrar = declaración + AST sin CRITICAL + sandbox automáticos y uso inmediato; la persona entra después; la aprobación previa queda solo para el nivel 3 (promoción con valoración); (2) faltaba la **declaración responsable como dato** (finalidad, categorías de datos, vocabulario compartido con REG); (3) faltaba la **suspensión** por quien revisa, distinta de la retirada por el autor (§9, dos responsabilidades); (4) el registro solo contemplaba código propuesto por la IA, y el caso mayoritario de la Instrucció es **un script que ya funcionaba en el equipo de la persona referente** — ahora se acepta con `autoria=persona`. Además: el auditor AST se reconoce como **la caja de herramientas** (regla 1) y sus reglas deben coincidir con las Guías Operativas Técnicas, o cada subida de nivel sería una reescritura. Tensión dicha sin rodeos: la regla 2 (ejecución local con credenciales propias) no aplica igual porque el código va a los datos en el nodo institucional; es un régimen más controlado pero distinto, que UADTI y OIATI deben dar por bueno. Estados: draft/registrada/suspendida/retirada/no_instalada. FUN.4 reescrito (cola de revisión con muestreo, suspensión, señal de nivel 3) y pasa a Opus. Informe (Qüestió 4, sección nueva con la correspondencia) y correo actualizados. |
| 2026-09-02 | Planificación: Bloque PLG — perfiles y pipelines por *entry points* | **La petición de plugins del correo de desarrollo, convertida en bloque** tras contestarla en el informe (decisión 9: sí al descubrimiento, no a congelar los protocolos). Al medir el punto de partida salieron cuatro cosas que el bloque corrige: `register_profile` **sobrescribe en silencio**, `PublicGraphProfile` es un **enum cerrado** donde un perfil de terceros no cabe por construcción, `retrieval_pipeline_factory` es una cadena de `if` y los routers tipan el modo como `Literal` cerrado **sin validar el perfil contra nada**, y el frontend lleva perfiles y modos **hardcodeados** (viola la regla maestra 1). Y una que condiciona el diseño: **el servidor no está instalado como paquete en el venv** (sólo `automatia_shared`, en editable; sin `[build-system]`), así que unos entry points del propio servidor no serían visibles — PLG.1 mide si hacerlo instalable cuesta sólo `[build-system]`+relock y decide con la regla «un solo camino de registro». Dos grupos: `govgenai.graph_profiles` y `govgenai.retrieval_pipelines`; **desviación documentada** respecto al informe, que decía `govgenai.strategies`: una estrategia suelta no la selecciona la configuración, la compone un perfil; lo seleccionable es el pipeline. 2 prompts; después de LANG porque tocan la misma factoría. |
| 2026-09-02 | Planificación: Bloque FUN revisado — doble origen del catálogo de funciones | **Segunda observación de desarrollo, releída contra el diseño de FUN**: «gestionar ese código dentro de la herramienta es reinventar la rueda; tiene que estar en manos de quien lo define, versionado, testeado y mantenido contra una API declarada, con control semántico de versiones» y «¿qué valor añade hacerlo en la herramienta en vez de en Claude Code?». **Acierta para el autor desarrollador y no ve al autor informador**, que no tiene repositorio ni despliegue y cuyo código corre cerca de datos que no puede sacar. Decisión: el catálogo pasa a ser un **registro de referencias `función@versión` con dos orígenes** — *autoservicio* (código en el catálogo, IA+AST+sandbox+aprobación, anclaje por versión exacta por construcción) y *empaquetado* (código en el repositorio del equipo, descubierto por *entry point* `govgenai.funciones`, semver, anclaje por mayor por contrato, corre in-process con la confianza en quien instala). Lo que **no cambia entre orígenes** es lo que justifica el catálogo: un solo contrato y validador, entrada validada antes de ejecutar, `RunManifest` con versión exacta y hash, rastro en REG, inventario de uso y frontera de organización. **Nuevo FUN.5** (origen paquete, Opus); el consumo externo pasa a FUN.6 y el cierre a FUN.7 (7 prompts). Recogido en `docs/EVOLUCIO_I_ASPECTES_PENDENTS.md` (Qüestió 4, catàleg) con una sección nueva sobre por qué centralizar revisión, manifiesto y trazabilidad aunque el código viva fuera. |
| 2026-09-02 | Planificación: Bloque PRC — el catálogo de procedimientos al asistente de normativa | **Valoración de diseño para el piloto**: los asistentes no contestan procedimiento (servicio que tramita, plazos, silencio) porque eso vive en el catálogo de procedimientos (363 fichas de 15 campos, en revisión por los servicios: 206 con banderas y 28 citando normas derogadas). **Decisión: integrar en el mismo asistente, no dos chatbots con router** — los usuarios preguntan mezclado y no distinguen normativa de trámite, el router clasificaría justo donde el usuario es ambiguo, la pregunta mixta necesita ficha y artículos en el mismo contexto, y los perfiles router/aggregator siguen sin implementar (I5). **El circuito de actualización reutiliza lo construido**: rendido determinista ficha→`.md` (sin LLM, la parte cara la hace el servicio en el formulario), la validación del servicio como puerta (el catálogo actual NO se ingiere tal cual), y el reconciliador existente (hash, poda, salvaguarda de proporción) con pasadas supervisadas antes de programar — la lección de SYNC.1. Bloque PRC (6 prompts, PRC.0–PRC.5), ejecutable por tandas conforme los servicios validan; recalibración obligatoria con el lote dorado antes/después. **Y el contrato del dataset del catálogo, decidido el mismo día**: sincronización por **estado completo, no por deltas de horas** — un filtro por fecha no expresa bajas, exige recordar la última pasada exitosa (ventana perdida = divergencia permanente y silenciosa) y depende de fechas que mienten; a 363 fichas (~2 MB) el estado entero cuesta menos que el mecanismo de deltas, y el incremento lo da el hash en destino. Endpoint pedido al equipo del catálogo: HTTPS con token, solo validadas, `generated_at`+`total` con completitud verificada (la lección de `is_census()`), ID estable y bajas explícitas si puede ser. Recogido como Qüestió 6 (+ petición textual y decisiones 14-16) en `docs/EVOLUCIO_I_ASPECTES_PENDENTS.md`. |
| 2026-09-01 | Planificación: Bloques FUN y LANG, y la Fase 3 corregida antes de detallarse | **De la observación de desarrollo («generador, no framework») salieron tres cosas al medirla contra el código.** (1) Para chatbots y motor la premisa era falsa —crear un chatbot es un `INSERT` que interpreta `GraphFactory`+`GraphProfileRegistry`, y el «código generado» que se vio era el snippet del widget—, pero **releída sobre los scripts deterministas acierta**: el código aprobado se incrusta copiado en el bloque de cada plantilla (`scripts_router.py:555-560`), así que un bug en un script usado por N plantillas se arregla N veces. De ahí el **Bloque FUN** (6 prompts): catálogo `HubFuncion` versionado con contrato E/S declarado, referencia `funcion_id@versión` en vez de copia, promoción a plataforma con superadmin y ejecución externa por API. (2) Al comprobar si un grafo puede ser monolingüe apareció que **`language_mode` viaja por toda la cascada y ningún nodo lo consume** — la factoría monta siempre `DefaultLanguagePolicy` — y que el modo «responde siempre en X» no existe: **Bloque LANG** (2 prompts). (3) La convergencia informes↔expedientes señalada por el usuario destapó que `ejecuciones_accion.codigo_ejecutado` en `Plan_TDD_Fase3.md` habría construido **un segundo motor de ejecución de código** paralelo a sandbox+auditoría+aprobación: corregido el plan antes de detallarse — las acciones de fase referencian `plantilla@versión` (motor de redacción, la ejecución ES un workspace) y `función@versión` (catálogo FUN). Todo recogido también en `docs/EVOLUCIO_I_ASPECTES_PENDENTS.md`. |
| 2026-08-31 | La alerta probada, y la comprobación que nunca existió | **Al ir a probar la alerta se descubrió que la comprobación de salud no existía.** La lista de `uptimeCheckConfigs` estaba vacía: el POST había fallado con «selected_regions must include at least three locations» —puse una sola región— y **el guion lo canalizaba a `/dev/null` e imprimía «[creada]» a continuación, pasara lo que pasara**. Así que la alerta de caída llevaba dos horas **inerte**: existía, parecía correcta, y no podía dispararse porque le faltaba la métrica que la alimenta. Una alerta huérfana es peor que ninguna, porque da impresión de cobertura. Es el cuarto caso del mismo patrón en la sesión —el `tail` comiéndose códigos de salida, `ruff` con 169 errores y exit 0, el `clone` con exit 1 habiendo funcionado—: **no basta con actuar, hay que leer el resultado**. Arreglo estructural en tres capas: una función `crear` que mira la respuesta y falla con el motivo, la comprobación final del guion revisa **también** la comprobación de salud (antes sólo miraba las alertas, que es por lo que se escapó), y `selectedRegions` se omite —la API exige tres y omitirlo comprueba desde todas—. El test nuevo cazó de inmediato **un segundo caso** que quedaba: el POST del canal de avisos también descartaba su respuesta. **La prueba, hecha por CLI**: servicio parado 13 min, la comprobación registró **38 puntos `false` desde seis regiones** (Bélgica, Singapur, Brasil y tres de EE. UU.), y tras arrancar volvió a pasar a las 18:09:44. Lo único que no puede verificar un agente es que el correo llegue al buzón. Los incidentes de Monitoring no se escriben en Cloud Logging, comprobado. |
| 2026-08-31 | **La dimensión del vector estaba mal en toda instalación nueva**, y llevaba meses invisible | **Un defecto de producción, no del volcado, y el hallazgo más consecuente de la sesión.** `a1b2c3d4e5f6_hub_schema.py:133` deja `hub_document_chunks.embedding` en `vector(1536)` y **ninguna migración posterior lo corrige**, mientras el modelo declara `Vector(1024)` y una migración más reciente crea `page_embedding` como 1024 — o sea que la convención es 1024 (BGE-M3) y ese 1536 es un residuo. Consecuencia: **cualquier base creada con `alembic upgrade head` tiene una columna en la que la aplicación no puede escribir**, y la primera ingesta falla con «expected 1536 dimensions, not 1024». Afecta a este despliegue, a cualquier edge node y a cualquier fork. **Por qué no se veía**: (1) la base de desarrollo está en 1024 porque su tabla no nació de la cadena de migraciones —evolucionó con `create_all` y ALTERs—, así que allí nunca falló; (2) `test_migrations_fresh_install.py` comprueba que las columnas **existan** (`"canonical_url" in columnas`) pero **no su tipo**, y un test de esquema que sólo mira nombres deja pasar cualquier divergencia. Apareció al restaurar el corpus porque **producción es la primera base de este proyecto creada de verdad sólo con migraciones**: es literalmente el valor de haber desplegado. Corregido con **`e2b3c4d5f6a7`**, y **condicional a propósito** —sólo altera donde la dimensión no es ya 1024, así que en una base correcta no toca nada y no borra vectores que costarían GPU y horas; donde está en 1536 los datos no pueden ser válidos porque la aplicación nunca pudo escribir ahí—. `downgrade` vacío también a propósito: volver a 1536 sería regresar al defecto. **Verificado en una instalación limpia de verdad**: base nueva, `alembic upgrade head`, `vector(1024)`. Guardarraíl nuevo que cruza las dimensiones del modelo con las de las migraciones sin necesitar base de datos — y que primero pasó **por ausencia de casos**, porque su expresión regular no casaba con `Mapped[list[float] \| None]` (corchetes anidados): la peor forma de pasar, corregida con el porqué escrito. 4 tests. |
| 2026-08-31 | La restauración del piloto: cinco intentos, cinco causas, y una línea que tapaba tres | **Restaurar de verdad encontró lo que ningún test en seco encontró.** (1) `session_replication_role = replica` en la cabecera del volcado: **Cloud SQL lo prohíbe** (exige superusuario) y fue una suerte, porque tapaba **tres errores de mi propia lista** — faltaba `hub_llm_configs` que los cuatro chatbots referencian, `hub_crawled_pages` iba después de `hub_documents` y 3 documentos citan una página, y 138 documentos se referencian entre sí. Desactivar una comprobación es una forma de no enterarse. (2) `hub_document_chunks.tsv` es `GENERATED ALWAYS` y `COPY FROM` no acepta valores para esas: fallaba con «extra data after last expected column», que suena a fichero corrupto y era una columna de más. (3) **Dos bases en la misma revisión de Alembic pueden tener distinto orden físico de columnas** —`document_id` es la tercera en desarrollo y la duodécima en producción, porque una evolucionó y la otra nació de cero—, así que un `COPY` sin lista de columnas desplaza los campos y el síntoma fue un hash aterrizando en la columna del vector. Corolario: **en un volcado entre bases la lista de columnas no es opcional**. (4) La dimensión del vector, en su propia fila. **Y una decisión para no hacer subir dos veces un gigabyte**: en vez de regenerar los 3,4 GB, se partió el volcado —58 MB con todo lo corregido, y los fragmentos extraídos del fichero ya subido a GCS—, con una opción `--excluir` que **avisa por pantalla de que el volcado no está completo**, porque un volcado con una tabla menos y sin decirlo es el que alguien restaura creyendo que lo tiene todo. Las autorreferencias se cargan en dos pasos (a nulo y `UPDATE` al final) en vez de intentar ordenar las filas, que se rompería con una cadena de dos saltos. Cuatro guardarraíles nuevos, uno por cada trampa. Criterio y trampas escritos en `docs/DATOS_DEL_PILOTO.md`. |
| 2026-08-31 | D.7 ✅ — qué llega al piloto, y el filtro que no filtraba | **El hallazgo del prompt, y lo cazó su propia guarda**: la primera versión del volcado usaba `pg_dump --table=…`, y **`pg_dump` no filtra filas** — volcaba cada tabla entera. Lo peor es que imprimía antes un recuento por organización, así que *parecía* filtrado: 3,2 GB con filas de **tres organizaciones ajenas**, y sólo la comprobación sobre el fichero generado lo destapó. Es literalmente el escenario que el prompt describía al exigir «se comprueba sobre el volcado ya generado, no sobre la consulta que lo generó», y sin esa insistencia el fichero habría llegado al piloto. Rehecho con un `\copy` por tabla y **el filtro escrito tabla a tabla**, porque los caminos no son uniformes: unas cuelgan de la organización, otras del chatbot, otras del sitio de curación. **La segunda cara del mismo prompt**: `Escola de Doctorat (RAS.5)` tenía `organizacion_id` a NULO, así que el filtro lo dejaba fuera y con él sus 351 páginas y 292 hallazgos; asignado a la UJI antes del volcado, que es lo que el prompt pedía. **Resultado medido**: 1 organización, 4 chatbots, 751 documentos, 163.950 fragmentos, 69 términos de vocabulario, 1 sitio, 351 páginas, 292 hallazgos, 1 tema, 3 credenciales — **165.423 filas, 3,4 GB que comprimen a 1,0 GB**— y cero informes y cero personas verificados sobre el fichero. **Desviaciones documentadas**: el prompt esperaba 3 chatbots, 548 documentos y 288 hallazgos; hay 4, 751 y 292, porque HIB.T añadió un banco de inyección y ACT reingirió el corpus en los cuatro asistentes después de escribirse D.7. Y **no** se renombra el agéntico para quitarle el «(proves)» como el prompt pedía: ese renombrado se pensó cuando era uno de tres asistentes del piloto, y HIB.U lo dejó inactivo como banco — hoy «(proves)» describe lo que es. El catálogo de plantillas va por otro camino a propósito: exportado a `app/data/plantillas_demo/` con **todas** sus versiones (2 y 1), sembrado por `bootstrap.py --con-demo`, verificado en una base limpia recién migrada y **idempotente** en la segunda pasada. La plantilla vacía de 2 bytes (`{}`) queda declarada para borrar y el exportador se niega a exportarla. `created_by` es NOT NULL y el superadministrador no puede firmar —su clave es un entero y la columna un UUID—, así que firma un UUID5 derivado de un nombre; **no** un `uuid.UUID(int=0)`, que se leería como «falta un dato» y es el defecto que el hallazgo I5 tuvo que quitar de dos perfiles de grafo. Criterio y cifras en `docs/DATOS_DEL_PILOTO.md`. 29 tests. |
| 2026-08-31 | La alerta llegó — y seis veces, que también era un defecto | **El usuario recibió los correos**, así que la cadena de aviso funciona de punta a punta: es la comprobación de cierre de D.6-VM que dije que sólo podía verificar una persona. Pero **seis correos por una sola caída** es configuración mía mal hecha: sin `crossSeriesReducer`, cada región de comprobación es su propia serie y abre su propio incidente —Bélgica, Singapur, Brasil y tres de EE. UU.—, y seis avisos de lo mismo enseñan a ignorar los avisos. Corregido en la política viva y en el guion: se agrupa por `resource.label.host` con `REDUCE_COUNT_FALSE` y el umbral pasa a «**más de una** región fallando», que además evita despertar a nadie por un parpadeo de red entre un comprobador y la máquina. La credibilidad de una alerta se gasta con los falsos positivos, no con los reales. |
| 2026-08-31 | D.6.1 ✅ — el corpus navegable, público y con las anclas funcionando | **El bucket abierto por decisión del usuario** y verificado en vivo: `index.html` y `cercador.html` en 200, `Cache-Control: public, max-age=300` aplicado, y —lo que da sentido a todo— la página de una norma sirve `id="art-1"`, así que una cita del asistente abre el artículo y no la portada. **Un susto propio más, y el patrón se repite**: mi primera comprobación de anclas dio **cero** y estuve a punto de reportarlo como fallo; el patrón del `grep` estaba mal y además muestreé una circular de 2005, que legítimamente no tiene artículos. Medido bien: **2.967 anclas en 107 de las 313 páginas**, que es exactamente lo esperable —sólo las normas articuladas tienen artículos—. Cuarta vez en la sesión que sospechar del instrumento antes que del sistema evita un informe falso. |
| 2026-08-31 | D.6.1 ▶ — el corpus navegable, subido y sin abrir | **Las 313 páginas, los PDF y las imágenes están en `gs://govgenai-normativa-uji`** (~54 MB) con `Cache-Control` explícito —cinco minutos para HTML, un día para estáticos, porque un HTML de norma cambia cuando cambia la norma— y subida idempotente por `rsync`, así que republicar tras corregir una norma cuesta esa norma. `CORPUS_SITE_BASE_URL` no necesitó cambio de código: ya se leía sin defecto de localhost («vacía significa desactivado»). **El bucket NO se ha abierto a lectura pública**: es una acción hacia fuera y difícil de deshacer, y aunque la carpeta es literalmente `publicacio_transparencia_2026-07` y el plan lo prevé, hay 219 defectos del corpus pendientes de Secretaría General — la decisión es del usuario. **Dos sustos del propio guardarraíl, los dos investigados antes de seguir**: (1) la guarda de credenciales se negó a publicar porque detectaba `data-widget-key`, y era un falso positivo mío — miraba también `widget.iife.js`, que menciona el atributo porque es el código que lo lee; ahora busca un **valor** y sólo en HTML, y verificado: cero credenciales, el sitio se publica sin asistente, que es lo correcto sin chatbot en producción; (2) `index_gerencia.html` se titula «Normativa i actes de **règim intern**», que olía a material confidencial subido por error — comprobado, las 448 coincidencias están en **313 de 313 ficheros**, o sea el título del sitio repetido en cada página, y «règim intern» ahí significa normativa propia de la universidad. Queda pendiente incrustar el widget, que necesita un chatbot y una credencial de sitio en producción: eso es **D.7**. |
| 2026-08-31 | **El despliegue funciona**: ocho intentos, ocho causas distintas, ninguna del código de la aplicación | **`https://34-175-38-129.sslip.io/health` responde 200 con certificado válido** (`tls_verify=0`), y la etiqueta desplegada —`605a34f5…`— coincide exactamente con el commit, que es la comprobación de cierre de D.5-VM. **Las migraciones se aplicaron contra Cloud SQL** (cadena completa hasta `d1a2b3c4e5f6`, ACT.3, con la salida en el log del workflow). **El reinicio, comprobado con escepticismo**: 50 s de `reset` a servir, y como la cifra parecía demasiado buena se verificaron los relojes de la máquina —arranque 16:58:18, unidad activa 16:59:02—, así que el sistema **vuelve solo** de verdad. La capa 8 sobrevive al reinicio (`runtime=runsc`, `Starting gVisor...`). Y la **vuelta atrás quedó probada por accidente honesto**: la reversión que disparó mi propia comprobación defectuosa restauró la configuración anterior, reinició y devolvió la pila entera y sana. **Los ocho fallos, que es lo que este prompt de verdad enseña**: (1) el cliente de Orval está en `.gitignore` y `docker build ./frontend` no puede funcionar sin generarlo — el contrato se genera, no se guarda; (2) faltaba `actAs` **sobre la cuenta de la VM**, y el mensaje no dice sobre qué cuenta; (3) `sudo tar` extrae como root y mi `rm` de limpieza no llevaba `sudo`; (4) tener `artifactregistry.reader` no basta, el demonio de Docker necesita el *credential helper* — «Unauthenticated request» suena a permiso y era cliente; (5) la imagen del proxy corre con uid 65532 y un volumen nuevo nace de root, así que no podía crear su socket; (6) **mi guarda de sockets huérfanos borró el socket con el proxy vivo** porque `ExecStop` es el mismo compose y también había fallado — una guarda que no comprueba causa el fallo que venía a evitar; (7) la vuelta atrás mandaba `${ANTERIOR}` escapado, expandido en la máquina remota donde no existe, y dejaba `GOVGENAI_IMAGE=` vacío; (8) **mi comprobación de `/docs` medía el frontend** —SPA con catch-all, 200 en cualquier ruta— y **revirtió un despliegue que funcionaba**; lo autoritativo es dentro del contenedor, donde `/docs`, `/openapi.json` y `/redoc` dan 404 con `ENVIRONMENT=production`. Los ocho tienen guardarraíl. **Y un noveno hallazgo al cerrar, del tipo que no da síntoma**: editar `startup.sh` **no cambia la máquina** —GCE lo lee de los metadatos de la instancia—, así que la VM llevaba horas con la versión original y el agente de operaciones y la rotación de logs **nunca se instalaron**: las dos alertas de D.6-VM eran **inertes** y nada lo decía. Refrescado, verificado (`active`, `max-size 10m`, `max-file 3`) y ahora el despliegue refresca los metadatos en cada pasada. **Lo único que no se pudo comprobar**: que un rastreo termine y deje páginas, porque la base de producción está recién migrada y vacía —`ENVIRONMENT=production` bloquea el sembrado de desarrollo, que es lo correcto— y qué datos pasan al piloto es D.7. Sí queda probado el mecanismo por el que se descartó Cloud Run: «Planificador de calidad de contenido arrancado (cada 24h)». 76 tests de infraestructura verdes. |
| 2026-08-31 | El primer despliegue real murió en el frontend, y el motivo era el contrato | **`docker build ./frontend` no puede funcionar tal cual, y no es un fallo del contenedor.** Veinte errores `TS2307: Cannot find module '@/shared/api/generated/...'`: ese directorio está en `.gitignore` **a propósito** —es código que genera Orval desde `openapi.json`, que a su vez exporta el backend—, así que no viaja en el repositorio. Es la arquitectura contract-first diciendo que el contrato se genera y no se guarda, y el flujo de D.5-VM («construir la imagen en CI») no lo mencionaba. Se añaden al workflow los mismos cinco pasos que el job `contract` de `ci.yml` —`uv sync --locked`, `export_openapi.py`, `npm ci`, `npm run generate:api`— **antes** de construir, y un guardarraíl que fija ese orden. **Y una lección sobre mí, la tercera vez en la sesión**: `gh run watch --exit-status | tail -25` devolvió 0 y di el despliegue por bueno; el 0 era de `tail`. La conclusión real era `failure`. Un código de salida al final de una tubería no es el del comando que importa, y ya me lo había hecho `ruff` (169 errores con exit 0) y `gcloud sql instances create` (tier inválido con exit 0). |
| 2026-08-31 | D.6-VM ▶ — copias, vigilancia y las dos guías que faltaban | **La restauración, probada y no supuesta**: clonado por punto en el tiempo a una instancia nueva, **463 s (7 min 43 s)** cronometrados, comprobando no sólo el estado `RUNNABLE` —una instancia vacía también lo estaría— sino que el clon **trae la base `govgenai` y su usuario**; y el clon borrado después, que una instancia de prueba olvidada factura igual. Se clonó por PITR en vez de restaurar sobre producción porque probar la copia no puede poner en riesgo lo que la copia protege, y el clonado ejercita la cadena entera (copia base más WAL). **Antes de medirlo dije «unos 13 minutos» estimando por cuándo me tocó consultar: el cronómetro dio casi la mitad**, y la cifra que queda escrita es la medida. Tres cosas anotadas para cuando haga falta con prisa: la operación de clonado sigue abierta un rato después de que la instancia ya sirva (arranca su propia copia inicial), el borrado devuelve `409` mientras haya una operación en curso, y **`gcloud sql instances clone` salió con código 1 habiendo funcionado** —creó la instancia y luego reventó con un `404` al volver a leerla—, así que si esto se automatiza el criterio no puede ser el código de salida. **La contrapartida de haber elegido VM, puesta.** Copias automáticas y PITR ya venían de D.3; se añade versionado en los dos buckets, comprobación de salud cada 5 minutos sobre `/health` **con validación de certificado** —sin ella un TLS mal renovado pasaría la comprobación mientras los navegadores rechazan la página—, y tres alertas (caída, memoria > 85 %, disco > 85 %) a un correo **obligatorio**: una alerta que no llega a una persona es un gráfico. **El hallazgo del prompt**: las alertas de memoria y disco **no se podían ni escribir** sin el agente de operaciones, porque Cloud Monitoring sólo ve la máquina desde fuera (CPU y red); el guion de arranque lo instala ahora, y además fija rotación local de los logs de contenedor (`max-size 10m`, `max-file 3`) — el envío remoto no protege el disco: si la red falla, el fichero crece igual, y el disco lleno por logs es la avería más común de una VM. Dos guías nuevas: `docs/RUNBOOK_REINGESTA.md` (la pregunta del usuario sobre curar en local e ingerir en el cloud, con el `--dry-run` como puerta y las dos comprobaciones de cierre: `metadatos=0` y cero documentos sin fragmentos) y `docs/WIDGET_INCRUSTACION.md`, que recoge lo que D.1 dejó pendiente — incluido el orden de la rotación de la credencial («emitir, publicar, revocar»; al revés deja el widget muerto en producción) y que el contenido mixto **no da error visible en la página**. El guion de vigilancia usa la API REST y no `gcloud beta`, para no instalar componentes en la máquina de quien despliega; su primera versión leía el JSON con `python3`, que en Git Bash no existe, y se rompía con un «curl: Failed writing body» que no señalaba a la causa. 11 tests. |
| 2026-08-31 | D.5-VM ▶ — el despliegue continuo, montado y sin ejecutar (falta un push, que no es mío) | **Aprovisionado**: Artifact Registry `govgenai`, **Workload Identity Federation sin ninguna clave** en el repositorio, cuenta `govgenai-deploy` con cuatro roles mínimos, y las **diez variables del repositorio** puestas con `gh variable set` — ninguna es secreta, son identificadores, y lo que autoriza es la federación. `deploy.yml` construye las tres imágenes etiquetadas con el SHA (nunca `latest`), copia los artefactos por IAP —no `git pull`, que haría depender lo que corre del estado del disco—, ejecuta **las migraciones en un paso propio antes de cambiar la imagen** y, si `/health` no responde o `/docs` contesta 200, **vuelve a la etiqueta anterior**. **Lo que el guardarraíl fijó, porque es lo que se rompe sin ruido**: que no vuelva una clave JSON (`credentials_json`), que la etiqueta no sea `latest`, y **el orden** —migraciones antes del despliegue— que es el invariante de verdad: si se colaran después, un fallo de Alembic dejaría la imagen nueva sirviendo contra un esquema viejo. **Tres cosas que aparecieron escribiéndolo**: (1) el servicio de migración lo escribí con `uv run alembic` copiándolo del prompt, y la imagen de runtime **no lleva `uv`** —tiene el venv en el PATH—, así que habría fallado en el despliegue con «executable file not found»; (2) `yaml.safe_load` de un workflow de GitHub parsea `on:` como el **booleano `True`**, no como la cadena, y el primer test cayó justo ahí; (3) la elección del origen del bucket no es cosmética: `https://storage.googleapis.com/BUCKET/…` tiene como origen **el de todos los buckets del mundo**, mientras `https://BUCKET.storage.googleapis.com/…` da un origen acotado a ese bucket — se usa el segundo, para no abrir `CORS_ALLOWED_ORIGINS` a medio internet. Buckets creados: `govgenai-prod-docs` (privado, documentos) y `govgenai-normativa-uji` (el sitio). **Falta ejecutarlo**: el workflow se dispara con un push a `main` o a mano, y la metodología de este proyecto dice que no se hace push — es decisión del usuario. Con él se cierran también los cuatro puntos pendientes de D.4-VM. 14 tests. |
| 2026-08-31 | D.4-VM ▶ — la máquina existe, y el compose que el bloque daba por escrito no servía | **VM `govgenai-vm` creada y verificada** (e2-small, `europe-southwest1-b`, IP estática **34.175.38.129**, Shielded VM, cuenta de servicio propia con cuatro roles mínimos, 80/443 abiertos y **22 sólo desde el rango de IAP**). El guion de arranque dejó instalados Docker 29.7.2, Compose v5.5.0, `runsc` release-20260817.0 y gcloud 582. **El hallazgo del prompt**: el bloque decía que en una VM esto era «prácticamente `docker compose up -d`» sobre `docker-compose.prod.yml`, y no lo era — ese fichero es la opción **autoinstalable** (su propia cabecera lo dice) y trae **su** Postgres, **su** MinIO y un perfil de Ollama, con el `DATABASE_URL` del servicio `app` apuntando al contenedor `postgres`. Levantarlo en GCP habría corrido una base de datos que nadie usa junto a la que sí, con el mismo nombre de variable. De ahí `deploy/vm/docker-compose.vm.yml`, con Cloud SQL Auth Proxy por socket Unix, GCS y sin Langfuse (degrada a no-op sin clave y su v3 exige Clickhouse: no cabe en 2 GB). **La capa 8 dejó de ser una promesa**: `runsc` registrado con `--platform=systrap` y **probado de verdad** — bajo `--runtime=runsc` el contenedor arranca con `[0.000000] Starting gVisor...` y sin él `dmesg` responde `klogctl: Operation not permitted`, así que el comando que D.0.doc escribió en `SANDBOX_SECURITY.md` discrimina. **Dos desviaciones documentadas**: (1) el prompt pedía `Restart=always` en systemd, que no aplica a `Type=oneshot` — lo que mantiene los contenedores en pie es `restart: unless-stopped` en el compose, y la unidad existe para el arranque de la máquina y para `systemctl restart` con recarga de secretos; (2) el sandbox se copió **literalmente** del compose de producción salvo la línea de `runsc`, en vez de «mejorarlo»: mi primera versión le había quitado `pids_limit` y el `mode=0700` del tmpfs sin darse cuenta. Y una tercera cosa que el test cazó: el Caddyfile repetía las cabeceras de seguridad que ya pone `security_headers.py`, **y la copia era la débil** (`SAMEORIGIN` donde la aplicación dice `DENY`); ahora el proxy no las toca y el fichero explica por qué. Bucket privado `govgenai-prod-docs` creado. **Falta lo que depende de la imagen** (D.5-VM): TLS respondiendo, `/docs` cerrado, reinicio, y que el scheduler y un rastreo funcionen. 16 tests. |
| 2026-08-31 | D.2 y D.3 ✅ — los secretos y la base de datos de producción, y dos veces el mismo retorno de carro | **Aprovisionado de verdad en `uji-teclab`**: instancia `govgenai-prod` (POSTGRES_16, `db-g1-small`, `europe-southwest1`, copias a las 03:00 y PITR), BD y usuario `govgenai`, y seis secretos en Secret Manager. **`pgvector` no necesita paso manual**: la primera migración ya ejecuta `CREATE EXTENSION IF NOT EXISTS vector` y el usuario creado por la API de Cloud SQL tiene permiso — el prompt lo pedía como paso aparte. **Dos cosas que el plan daba por hechas y no lo estaban**: (1) D.2 estaba escrito para el despliegue gestionado, donde la plataforma inyecta las variables; en una VM eso hay que escribirlo, y es `vm_fetch_secrets.sh`, que monta `/opt/govgenai/.env.runtime` con permisos 600 y **se niega a escribir un fichero a medias** —arrancar con media configuración es peor que no arrancar—. (2) La edición: el proyecto crea por defecto en `ENTERPRISE_PLUS`, que **rechaza** los tiers de núcleo compartido, así que `db-g1-small` exige `--edition=ENTERPRISE`. **El hallazgo del día, y su moraleja doble**: ejecutando los guiones desde Windows, `openssl` termina en CRLF y `tr -d '\n'` deja el `\r` dentro del secreto —96 caracteres almacenados como 97—, y ese `\r` en la contraseña hacía que `gcloud sql users create` fallara con «batch file arguments are invalid», un mensaje que no señala a ninguna parte. Arreglado en un solo sitio (`limpiar()`) y fijado con dos tests. Pero **el segundo CR que creí ver era mío**: la URL derivada daba positivo en mi comprobación y `od -c` sobre el valor almacenado demostró que estaba limpia, 194 bytes — la propia captura en el intérprete añadía el CR que buscaba. Es la lección de [[feedback_el_medidor_miente_antes_que_el_sistema]] otra vez: para comprobar un secreto, `od` sobre un fichero, no un `case` en el shell. Inventario único en `scripts/lib/secretos.tsv` con lo que deja fuera **y por qué** (Langfuse degrada a no-op y su v3 exige Clickhouse, que no cabe en 2 GB; OpenAI/OpenRouter no los consume nadie; `DEV_ADMIN_*` es el hallazgo CR-1 de la auditoría). Falta el valor de `govgenai-google-api-key`, que lo aporta una persona. 14 tests. |
| 2026-08-31 | D.0 ✅ — los servicios de GCP, con la lista cruzada contra el código | **Ejecutado contra `uji-teclab`: los 14 servicios habilitados y comprobados**, y la idempotencia verificada contra la nube de verdad (segunda ejecución, exit 0) y no sólo en seco. En medio hizo falta un `gcloud auth login` interactivo —el primer intento murió con «Reauthentication failed. cannot prompt during non-interactive execution»—, que es la única parte del prompt que un agente no puede hacer. **La lista del plan estaba incompleta en dos servicios y sobraba en uno.** Faltaban `iamcredentials.googleapis.com` y `sts.googleapis.com`, que es lo que Workload Identity Federation de D.5-VM usa para el intercambio de token —sin ellos el despliegue por CI falla en el primer paso—, y `iap.googleapis.com`, porque D.5-VM entra por SSH sobre IAP. Sobraba `cloudbuild.googleapis.com`: la imagen la construye GitHub Actions, no Cloud Build. `run.googleapis.com` fuera por el cambio de destino, y `compute`+`oslogin` dentro. **El test que vale es el que cruza**: escanea `server/app` buscando hosts `*.googleapis.com` y exige que cada uno esté en la lista, así que una llamada nueva a un servicio que nadie habilitó sale en rojo aquí y no como un 403 en producción. Ese cruce cazó de entrada un falso positivo propio —`www.googleapis.com` en `reranker.py` es el **ámbito de OAuth** (`/auth/cloud-platform`), no una API habilitable—, y se excluye con su razón escrita en vez de relajar la expresión regular. El proyecto es parámetro obligatorio **sin valor por defecto a propósito**: heredar el de `gcloud config` habilita APIs en el proyecto que el desarrollador usara la última vez. El script no sólo habilita, **comprueba** y sale con código distinto de cero si algo falta, avisando de que la propagación tarda antes de concluir que falló. 10 tests. |
| 2026-08-31 | D.0.doc — la documentación prometía una capa de seguridad que la VM no trae | **Nueve documentos mencionaban Cloud Run; cinco estaban mal.** Cuatro razonaban sobre el destino actual y uno afirmaba una protección inexistente. **La capa 8 del sandbox, resuelta en un sentido y no matizada**: gVisor se queda, pero deja de ser algo que regala la plataforma y pasa a ser **configuración nuestra** —`runsc` registrado como runtime de Docker, `runtime: runsc` sólo en el servicio del sandbox, plataforma `systrap` y no `kvm` porque `kvm` exige virtualización anidada que GCE no da en todos los tipos de máquina—, con el comando que comprueba que está (`dmesg | grep -i gvisor`) y la frase que faltaba: si no sale nada, quedan 7 capas. Instalarlo es tarea nueva de D.4-VM. Se decidió quedársela porque es la única capa que mitiga una fuga del kernel y el propio modelo de amenazas supone que la auditoría AST es *bypassable*. **Y al corregir las otras cuatro aparecieron dos hallazgos ya arreglados que el documento seguía dando por abiertos**: ME-1 (`X-Forwarded-For` por la izquierda) se arregló contando desde la derecha con `TRUSTED_PROXY_HOPS`, y B1 (temas en disco local) lo cerró PLAT.7 —`THEMES_DIR` ya no existe—; los dos quedan anotados como resueltos, que es la convención del propio informe. El criterio «externalizar BGE-M3 cuando el cold start supere 15 s» se retira: no hay cold start en una VM, y MOD.2 ya dejó los embeddings por API. Un descuido del mismo tipo **en código**: el docstring de `rate_limit.py` ponía Cloud Run como ejemplo del salto de confianza; ahora dice «el proxy inverso que termina TLS», que es lo que habrá. Guardarraíles: un inventario de menciones legítimas —los tres registros de decisión más el índice, cada uno con su razón— y el cuadre entre las capas que la prosa declara y las cabeceras que existen, que es lo que impide volver a contar una capa que no está. 3 tests. |
| 2026-08-31 | Bloque DIN replanificado — la sección es dato, y el censo tiene que acotarse | **Corrección del usuario sobre la primera versión del bloque: «los apartados se han de poder parametrizar como bloques o secciones dentro de un proceso de curación; no tiene sentido que estén ahora fijados».** Tenía razón y la primera versión estaba mal encuadrada: preguntaba a la reunión *qué* apartados eran (`/jornadas`? `/eventos`?) y los habría fijado en el plan, cuando lo que va a cambiar mientras alguien lo usa **es dato, no estructura** — el mismo razonamiento que CLAUDE.md §5 aplica al vocabulario del corpus. Hoy un apartado se expresa creando un `HubWebSite` entero con su `url_regex_filter` (decisión de RAS.5, con buena razón: cada apartado tiene un responsable distinto), lo que duplica `root_url`, sitemap, cortesía y criterios, y hace que «añadir el apartado de becas» sea trabajo de administración de sitios en vez de trabajo de quien cura. El bloque pasa de 4 a **7 prompts** con `HubWebSection` en el centro: patrón, cadencia, modo, responsable y criterios propios, con **herencia sitio→sección donde nulo hereda** (la semántica que `core/ambito.py` ya da a `heredable`) y pertenencia de página **derivada del patrón, no almacenada** (los patrones se editan; una columna exigiría reescribirla en cada edición). **Y al rediseñarlo apareció la trampa que la primera versión no veía**: `site_crawler.py:232` declara bajas comparando `seen_urls` contra `existing_pages` de TODO el sitio, protegido sólo por `truncated`. Hoy es correcto porque el sitio *es* el apartado; con varias secciones por sitio, una pasada de `/eventos` que termine completa declararía baja el resto del portal — y con la auto-retirada de DIN.4 encima, **vaciaría corpus en silencio**. Es literalmente la lección que `ingestion/corpus/source.py` tiene escrita para el corpus normativo («una carga parcial que se interpretara como censo retiraría cientos de normas»), así que la regla dura nº1 del bloque es que **el censo se acota al ámbito rastreado**, con su test dedicado en DIN.2 y la comprobación de recuento antes/después en DIN.7. Detalle secundario que también cambia: `mode` de una sección nueva nace en `manual`, al contrario que `auto_ingest_new` (que nació con default `True`). |
| 2026-08-31 | Bloque DIN planificado — secciones dinámicas (jornadas/eventos) sin curación manual | **La premisa de la reunión («esto no está previsto») resultó falsa en dos tercios al revisar el código.** Ya existen, con tests y corriendo en el APScheduler que arranca con la app: el rastreo periódico por apartado (RAS.5: un sitio por apartado con `url_regex_filter` y `crawl_interval_hours`), la **auto-ingesta de páginas nuevas** que casan una regla `HubCorpusSelection` con `auto_ingest_new=True` — columna con default `True` desde 9Q.7, consumida en `quality_job.py` paso 4 — y la **reingesta automática de lo que cambió** donde ya estaba ingerido (RAS.5 paso 3.bis, con aviso `content_updated`). Lo que falta es el resto del ciclo de vida, y son tres huecos concretos: (1) **auto-retirada** — `site_crawler` marca las desaparecidas (`mark_gone`) y ahí muere; `retire_page` solo lo llama el endpoint manual, así que un evento borrado del portal sigue en el corpus; (2) **puerta de calidad** — el paso 4 ingiere sin consultar los hallazgos que el mismo job calculó dos pasos antes: una página vacía o `needs_javascript` entraría; (3) **visibilidad** — `documents_auto_ingested` vive en el summary del job y se pierde; el curador no puede auditar lo que la automatización hizo. **4 prompts DIN.1–DIN.4** en `Plan_TDD_Fase1.md`, con dos salvaguardas de diseño: retirada masiva detenida al 30% (el patrón de la salvaguarda de proporción del reconciliador) y el principio «curación una vez, automatización después», que preserva la decisión del 2026-08-02 — no se auto-ingiere nada que ningún humano haya aprobado nunca; se mantiene lo que un humano aprobó una vez. Informe de la reunión actualizado con la valoración 3. |
| 2026-08-31 | Bloque REG planificado — la plataforma como registro de actividad IA | **Origen: reunión del usuario con desarrollo, con dos propuestas valoradas en `docs/EVOLUCIO_I_ASPECTES_PENDENTS.md`.** (1) BD vectoriales embebidas para chatbots de corpus estable: **desaconsejada** — quitar los vectores no quita Postgres (documentos, jobs, vocabulario y tenencia siguen ahí), la recuperación híbrida con umbrales calibrados (HIB) habría que recalibrarla en un segundo camino de código, rompería «reclasificar = un UPDATE» con el vocabulario aún pendiente de Secretaría General, y «corpus estable» es empíricamente falso (ACT reingirió los cuatro asistentes el 28-08). La puerta queda abierta gratis: el retriever ya se inyecta detrás de un protocolo. (2) Registro de usos de IA externos + anonimización como servicio: **aceptada con matiz** — el diagnóstico «no escalará» está sobrevalorado (el monolito ya está partido por dentro: dos bases ORM, `DEPLOY_MODE`, `ConfigProvider`), pero la propuesta encaja con el AI Act (arts. 12 y 26) y gira hacia fuera piezas que ya existen: servidor MCP (16 tools), PAT con scopes y el módulo de anonimización de redacción. **6 prompts REG.1–REG.6** al final de `Plan_TDD_Fase1.md`, posición: tras el Bloque Deploy. Reglas duras: metadatos sí payloads no, el texto a anonimizar no toca logs, MCP remoto con token por petición (no el PAT de entorno del stdio). Es la «opción B» que `docs/mcp.md` dejó anotada el 2026-06-06 esperando caso de uso. |
| 2026-08-29 | ACT.9, ACT.10 y ACT.11 — la caducidad, la ficha de los cinco y el listado de los 35 | **ACT.9**: la fecha de caducidad se propaga entre versiones, como ya hacía la validación; al implementarlo apareció que el defecto de 365 días se ponía DENTRO de `_aplicar`, o sea antes del emparejamiento, así que la hermana ya lo tenía y la propagación la saltaba —habría quedado escrita, probada y muerta—. Se mueve al final. **ACT.10**: los cinco documentos de la UJI sin ficha la reciben, y con ella el puente léxico (eran los **únicos cinco del corpus sin `termes_bilingues`**: quien preguntaba «gastos de viaje» no encontraba «despeses de viatge», y la búsqueda no fallaba, dejaba de encontrar en silencio), el emparejamiento de sus dos parejas y el canal de validación. Dos intentos no salieron y enseñan: llevarlos a `md_contracte` hizo **abortar al conversor por «pérdida de texto»** —está pensado para normativa articulada y una FAQ no sobrevive—, y darles ficha hizo petar `build_pagines`, que construía página para CADA ficha porque `publicar_al_portal` era un campo que **nadie leía**. Tener ficha no es estar publicado. `alta_norma.py` estrena uso real y necesitó dos correcciones, las dos del mismo tipo: una guarda que no preveía un caso legítimo (series nuevas, y URL compartida entre hermanas idiomáticas — la misma distinción que ACT.3 le enseñó a VIS.3). **ACT.11**: los 35 sin vigencia confirmada, listados con título en §12.7, separando las 31 internas sin URL de las 4 publicadas. Y un residuo de idempotencia: `data_revisio_des_de` se conservaba bajo la condición de la *validación*, que no tiene que ver, así que en PLA-003-val se perdía y se reponía en cada pasada; la condición pasa a ser por campo. Cierre: **idempotencia total, `metadatos=0` en los cuatro asistentes**. |
| 2026-08-28 | ACT.8 — el banco agéntico ingería sin trocear, y se habría ido vaciando solo | **Apareció revisando los recuentos del informe, no midiendo nada.** De los 6 documentos que el banco agéntico recibió al actualizar el corpus, los 6 entraron con **cero fragmentos**: la ingesta troceaba sólo en modo `RAG` y ese asistente está en `MD_AGENT_SELECTOR`. El informe decía `ingeridos=6` sin mentir, porque el documento sí entraba. La premisa —«los otros dos modos no embeben nada»— estaba escrita **igual en tres sitios**, así que fue una decisión; pero es anterior a que el agéntico tuviera `search_knowledge`, que consulta justamente el índice vectorial, y HIB reparó sus 14.198 fragmentos dejando escrito que «no son peso muerto». **Y el hueco tapaba algo peor**: la rama que no trocea BORRA, y como al cambiar el cuerpo de una norma la ingesta sustituye el documento, el agéntico habría ido perdiendo su índice norma a norma según se actualizaran — una fuga lenta, sin aviso. La condición deja de preguntar «¿es RAG?» y pregunta «¿este modo busca por fragmentos?», declarada **una vez** e importada por los tres consumidores: que estuviera repetida es por lo que envejeció sin que nadie la revisara. `MD_LONG_CONTEXT` sigue fuera y su borrado sigue siendo correcto. Dos tests fijaban la premisa vieja (`test_upload_in_agentic_mode_skips_chunks` y el de crear un chatbot no-RAG sin embeddings) y se migraron con su porqué. Reindexado: **cero documentos sin fragmentos** en los cuatro asistentes; el agéntico 14.208 → 14.459. 1922 passed. |
| 2026-08-28 | Bloque ACT ejecutado entero — el corpus actualizado y cuatro defectos que nadie veía | **8 prompts, y el corpus del 27-08 ingerido en los cuatro asistentes con la segunda pasada a cero.** ACT.7 y ACT.0 en el repositorio del corpus: dos comandos (`vigencia.py`, `alta_norma.py`) que sustituyen a los doce programas de alta escritos uno por lote, las 22 externas fuera de Normativa escrito en `assistents.json` —donde el reconciliador lo lee— y la vigencia con un solo eje. ACT.1: las 292 «actualizaciones de metadatos» por pasada eran fechas *aware* contra *naive*, y con ese ruido no se veían las 24 reales. ACT.2: `langdetect` decía `ca` y el corpus dice `val`, así que **nada casaba** — y los tests de VIS.5 usaban `ca` en los dos lados, pasaban, y producción fallaba. ACT.3: `canonica` era filtro duro y hacía que preguntar en castellano devolviera el texto valenciano; retirado, y VIS.3 reescrita para decir lo que de verdad detecta (de las 57 parejas, **47 tienen URL distinta**, así que `canonica` no la sostenía). ACT.4: la primera versión exigía `estat_vigencia='vigent'` y **52 tests en rojo** enseñaron que un documento sin estado no es una norma que dejó de regir, es una página rastreada. ACT.5: `chunk_copier` tenía cero llamadores desde HIB.N; enchufado, Gerencia copió sus 6 documentos en vez de embeberlos. ACT.6: el detector de divergencias daba 17 avisos y **cero normas divergían de verdad** —agrupaba por URL y comparaba `FAQ-001` con `FAQ-001-val`—; detrás había algo real que vio el usuario: 14 parejas con la castellana validada y la valenciana no, que desde ACT.3 hacían que la misma norma avisara distinto según la lengua. Corregido de concepto: **lo que se valida es que la norma rige**, y se comparte entre las dos versiones. Quedan 9 parejas sin validar ninguna, y basta validar una de cada. |
| 2026-08-28 | Bloque ACT planificado — auditoría de externas, lengua y vigencia con el corpus del 27-08 | **Empezó como una pregunta operativa —cómo actualizar el corpus— y acabó en siete hallazgos, ninguno de esta actualización.** El `--dry-run` del paquete `normatiu` (317 docs) contra `Normativa UJI` dio `+19 ~0 =292`; los 292 eran ruido (`revisat_el` *aware* vs *naive*, y `vigencia_validada_el` pisado con la fecha del `.md` en 249 documentos) que tapaba 24 diferencias reales. Entre ellas, **22 `EXT-*` que pasaban de `us_assistents=no` a `si`**: el usuario recordaba haberlas excluido de Normativa y no había tocado nada; la exclusión vivía sólo en la columna de la BD, y `assistents.json` declara `normatiu.tot: true` sobre `md_externes`. Del lado de la lengua: `rag_vector_pipeline.py:91` no pasa `language`; `prefer` ordena y no descarta; **`canonica=false` es filtro duro** (33 castellanas hoy, 57 con este corpus: preguntar en castellano por una norma bilingüe devuelve la valenciana, lo contrario de la regla que quiere el usuario); y `langdetect` dice `ca` donde el corpus dice `val`, así que nada casa (`graph_factory.py:203` ya lo documentaba como «no se usa»). Vigencia: DIR-003/004 están `no_vigent` a mano y el corpus las pone `vigent` + `vigencia_curs: substituida`, campo con **cero coincidencias en el repo**; `no_vigent` se recupera con aviso sin que nadie lo decidiera; `estat=suprimit` se escribe y nadie lo lee. Y `chunk_copier.py` (HIB.N) tiene cero llamadores: los 6 nuevos de Gerencia son subconjunto de los 19 de Normativa y se embeberían ×3. El usuario **decidió no ingerir** («prefiero esperar y abrir el piloto en condiciones») y pidió plan + sección en `CONFIGURACION_APERTURA_PILOTO.html` (§12) para discutirlo con Secretaría General. Excel de SG leído: 55 parejas, 180 sin la otra lengua, 14 parejas con una validada y otra no, 7 sin ninguna; PRG-003 pasa de `parcialment_derogat` a `vigent`, contradiciendo `INGESTA.md`. Además: `DEV_ADMIN_EMAIL`/`DEV_ADMIN_PASSWORD` añadidos a `server/.env` para que el seed regenere la cuenta `fabra@uji.es` y no `admin@example.local` (la de AIS.2). |
| 2026-08-28 | El `uv.lock` de la raiz llevaba 17 dias caducado, y CI no podia decirlo | **El diagnostico empezo al reves.** Avise al usuario de que el `M uv.lock` permanente de su arbol se llevaba «la pila de torch» y que no se commiteara. Comprobado, era justo lo contrario: `uv lock --check` sobre la version **commiteada** responde «The lockfile needs to be updated», y la del arbol esta al dia. EXT.3 retiro Docling del `pyproject.toml` de la raiz el 2026-08-11 y no regenero el lock; con lock != manifiesto, cualquier comando de uv que resuelva ese proyecto lo reescribe, asi que reaparecia cada sesion y se dejaba sin commitear por prudencia — 17 dias del mismo diff fantasma. El diff son **0 inserciones y 558 borrados**, ningun cambio de version, y los 31 paquetes que salen son el cierre de Docling; **`torch` y `sentence-transformers` se quedan** porque el manifiesto de la raiz declara `sentence-transformers>=5.4.1` como dependencia dura, y `torchvision` se iba por ser dependencia de Docling, no por el extra `local-models`. Confundir esas dos cosas fue el error. **Se commitea ahora y no en NIC** —que retira el proyecto de la raiz— porque delante hay el bloque Deploy entero, donde `git status` es el instrumento con el que se decide que se sube a una maquina real, y porque **NIC.4 toca ese mismo `pyproject.toml`**: con el lock caducado su diff habria llegado mezclado con estas 558 lineas ajenas. **El guardarrail, y la propuesta que se descarto a si misma**: propuse un paso `uv lock --check` en CI, y al comprobarlo no servia — «The project is re-locked before syncing unless `--locked` or `--frozen` is provided», asi que un paso de comprobacion DESPUES del install no puede fallar nunca. Queda un solo mecanismo: `uv sync --locked` en los dos pasos que instalan (`test` tenia `--all-extras` pelado y **volvia a resolver en silencio**; `contract` tenia `--frozen`, que usa el lock tal cual **sin comprobar que corresponda al manifiesto**). El nombre de los pasos lleva la razon, para que un rojo de lock no se lea como averia de infraestructura. `server/uv.lock` estaba intacto y al dia, asi que nada de esto toco el despliegue. |
| 2026-08-28 | CI en rojo — ocho tests que el cierre del bloque HIB no vio | **Ocho rojos en `Lint & Test` desde el cierre de HIB, y ninguno era del codigo de produccion: los ocho estaban en los tests.** Tres familias. **(1) Cuatro E2E de chat** (`test_chat_flow.py`): HIB.I hizo que el endpoint leyera `core_graph.cfg` para escribir la traza en `interaction_metadata`, que es una columna JSON, y el doble del grafo era un `MagicMock` pelado — asi que `cfg.retrieval_mode` era otro mock y el `INSERT` de la interaccion moria serializandolo. El doble declara ya una `PublicGraphConfig` real, igual que declara `compile()`. **(2) Tres expectativas que el propio bloque HIB dejo viejas por detras**: HIB.Q encendio `query_rewriting_enabled` en la plataforma y el test de RAG.10 seguia exigiendo `False`; HIB.R subio `quality_threshold` a 0,65 y el test de HIB.Q —escrito el dia antes— seguia afirmando 0,6, docstring incluido; HIB.J sustituyo la nota del gate por la similitud coseno y el test de RAG.6a seguia clavando el 0,7 que era justamente **la constante que HIB.J vino a quitar**. **(3) Uno que media el entorno y no el codigo**: `test_should_resolve_the_vertex_reranker_from_the_configuration` heredaba `GOOGLE_CLOUD_PROJECT` del `gcloud` del desarrollador, asi que pasaba en local y fallaba en CI; ahora la pone con `monkeypatch`. **Lo que ensena sobre el cierre de bloque**: la suite completa —el nivel 3 de la tabla escalonada— no se corrio al cerrar HIB, y el precio no fueron solo ocho rojos. Al fallar el paso de la suite, CI **se salta los tres pasos siguientes**, y entre ellos esta el gate de aislamiento entre organizaciones, que es el que dice «no despliegues»: un rojo cualquiera apaga el semaforo que importa. Verificado: 46/46 en los cinco ficheros con `GOOGLE_CLOUD_PROJECT` vacia, la suite entera con `-n0` desde Git Bash, y `pytest tests --collect-only` en el arbol de la raiz (221 tests), que era el unico de los pasos saltados que la suite no cubre. |
| 2026-08-28 | HIB.T y HIB.U — la inyección de norma entera es un mando, y un chatbot estaba citando el corpus de su hermano | **HIB.T**: `VectorRetrievalStrategy` gana `inject_whole_document`. No es una estrategia aparte a propósito — el experimento de granularidad midió que 5 normas enteras por top-k vectorial era la única celda que ganaba a Gemini de forma significativa (7-0, p=0,016) pero se midió con llamada directa al modelo y salió con **0 enlaces de 25**; un mando en la estrategia que ya hay hereda el contrato de citas, el ancla, el aviso de vigencia y la agrupación, que es lo que a las celdas les faltaba. Y el `MD_LONG_CONTEXT` que existía **no sirve**: selecciona por fecha de creación e **ignora la consulta**. Cinco normas y no tres, porque tres midió 50 % —por debajo del artículo ×3— y cinco 68 %. Tercer chatbot de Gerencia creado como banco (decisión del usuario: no reconfigurar el agéntico, que habría cerrado esa comparación justo cuando su nota se volvía real), corpus copiado por `content_hash` con **coste de embeddings cero**. **HIB.U, el hallazgo del día**: al medirlo salió **1 de 13** fuentes requeridas frente a 11 de los otros dos, y comprobarlo antes de reportarlo destapó que el asistente **agéntico tenía 14.198 de sus 14.208 fragmentos con `chunk_metadata['document_id']` apuntando a documentos del chatbot RAG** — fragmentos suyos (`chatbot_id` correcto, búsqueda bien filtrada) pero la agrupación leía el metadato, cargaba las filas del hermano y **la cita salía con su título sin ningún error**. Es palabra por palabra el fallo que el comentario de `chunk_copier.remapea`, escrito el día antes, describía. Se descartaron tres explicaciones mejores: el retriever sí filtra, el índice sólo ofrece propios, y `read()` sin filtro está documentado como deliberado y su premisa se cumple. **Invalida todas las cifras del agéntico del bloque, incluido su primer puesto (76 %) en el experimento de granularidad.** Arreglo en dos capas: `UPDATE` del metadato (sin coste de embeddings, verificado con segunda pasada: los cinco corpus coherentes) y la agrupación pasa a leer la **columna**, con el metadato como repliegue para los legados — la columna la sostiene una clave ajena y el metadato es copia denormalizada. Hubo que llevar la columna por **tres** puntos: las dos búsquedas base y la **fusión RRF**, que reconstruye el resultado desde cero; caerse ahí habría dejado los tests en verde y el arreglo inerte. **Y lo que más importa: el guardarraíl ya existía** (`chunk_copier.incoherentes()`) y sólo se pasaba por los corpus que la copia tocaba — un invariante que sólo se comprueba donde se sospecha no es un invariante. **Segundo caso del mismo patrón el mismo día**: el puntuador del agéntico usaba `PURPOSE_QUERY` sin importarlo, el `except` puesto para que un puntuador roto no tumbara la respuesta se lo tragaba, y el arreglo de HIB.E estuvo **inerte** hasta que `ruff` lo señaló; único síntoma, un `score_sin_medir=True` que nadie miraba. **Resultado final de las tres estrategias** sobre el mismo lote, corpus y código: RAG 11/13 fuentes y 4 de 5 negativos mal contestados; agéntico 11/13 y 5 de 5; inyección 7/13 y 4 de 5. Ancla 0 en las tres, por publicación y no por estrategia. **Gerencia abre con el RAG**; los otros dos a `is_active=False`, que es fuera de la vista de los usuarios y dentro del alcance del banco. 19 tests nuevos (12 + 7). |
| 2026-08-27 | HIB.R (CONFIG + RED/GREEN) — El umbral a 0,65, que es lo único que el umbral compra barato | **Cursor: HIB.R ✅.** La curva de las dos columnas se calculó **sin generar nada**, porque la puerta es `nota >= umbral` y las notas del lote de 48 estaban guardadas: 0,60 declina 0 de 18 y pierde 0 %; **0,65 declina 2 y pierde 0 %**; 0,70 declina 4 y pierde 7 %; 0,72 declina 10 y pierde 17 %; 0,75 declina 16 y pierde 50 %. 0,65 es el único punto que compra algo sin pagar nada. Se movió el defecto de la **columna** además del de plataforma, por la trampa de HIB.Q (`quality_threshold` es NOT NULL, así que un chatbot nuevo nace con el de la columna y ese gana siempre), **sin `server_default` ni migración** para no tocar los cuatro que ya existen. Valor vivo de Normativa 0,60 → 0,65, registrado en `registro_configuracion.json` con la cifra, la autoría y **qué no arregla**. Lo que no arregla es la mayor parte: los cinco casos de contratación que el asistente contesta y no debería puntúan **0,742-0,787**, por encima de casi cualquier respuesta correcta, así que ningún umbral los alcanza. Antes de escribir HIB.S se descartaron por medición los otros tres mecanismos: la nota no separa las dos poblaciones (0,643-0,787 contra 0,692-0,805), la brecha entre el primero y el segundo tampoco **y va al revés** (0,020 en los fallos contra 0,010 en los aciertos), el número de fuentes es idéntico, y la señal «la mejor evidencia es un documento vetado» gana en 2 de 8 negativos y en 1 de 6 contestables. **El hallazgo que decide dónde está el margen**: la puerta de calidad rechazó **cero de 48**; los 7 rechazos correctos los hizo el **contrato de citas**, con precisión perfecta (0 rechazos indebidos sobre 30 contestables). Al contrato le falta alcance, no acierto: hoy pregunta «¿ha citado?» y los 11 fallos citan. De ahí HIB.S. 4 tests. |
| 2026-08-27 | HIB.N (RED/GREEN) — El techo del padre se aplica con un UPDATE, y el permiso de copia se comprueba en los datos | **El techo, medido antes y después**: 5.172 de 60.859 fragmentos (8,5 %) traían un padre por encima de 8.000 tokens; tras el `UPDATE`, **0**, y los tokens de padre almacenados bajan de **179,8 a 54,5 millones** para un corpus de 2,1 millones —la amplificación era que cada hijo guarda su padre entero—. **Y no movió ninguna métrica**: de los 48 escenarios sólo 7 podían verse afectados (los que recuperan uno de los 11 documentos con padre sobredimensionado), se midieron esos 7 y salieron idénticos en fuentes (6/6), ancla (0/2), rendición (5/5) y **nota de calidad a tres decimales**. Medir 48 para ver 7 habría sido pagar 41 generaciones por nada; se pudo acotar porque `parent_content` **no se embebe**, así que el techo no puede mover la recuperación ni la nota. **Dato que reencuadra HIB.L**: 9 de los 11 documentos con padre sobredimensionado son leyes estatales, vetadas en Normativa — el techo muerde donde se decidió, en las normas externas de Gerencia. **El permiso de copia no se puede dar comparando configuraciones**: Normativa *declara* techo de 8.000 y *tenía* padres de 59.172, porque sus fragmentos son anteriores a HIB.L. `chunk_copier.se_puede_copiar` mira los datos. **Las cuentas del prompt salían mejor**: los **124 documentos de Gerencia tienen gemelo exacto por `content_hash`**, ninguno queda fuera, así que el coste de embeddings es **cero** (el prompt preveía reembeber ~11.000). **Tres fallos propios en la copia, y el tercero enseña**: `chunk_index` no existe (listaba columnas a mano → ahora se derivan del modelo); `tsv` es **GENERATED ALWAYS** y no se puede insertar (→ se excluyen las generadas por `c.computed`, no por nombre); y el fallo que esos dos evitaron es el contrario y silencioso — olvidar `embedding_text` o `bilingual_terms` deja el corpus sin búsqueda léxica **sin dar error**. Las dos transacciones fallidas revirtieron limpias: 14.198 fragmentos intactos. 11 tests. |
| 2026-08-27 | HIB.E (2.ª parte) — El arreglo de la nota estaba en el sitio equivocado, y lo dijo la medición | La primera pasada puso la similitud coseno en los documentos que el `AgenticLoop` **lee**. Correcto y útil, pero **no es la nota que la puerta mira**: `core_graph` toma el máximo de `merged_items`, y en `MD_AGENT_SELECTOR` esos items son el **índice** del pipeline — la línea `md_agent_selector_pipeline.py:82` que el propio prompt señalaba. Lo delató el barrido: las tres tandas (0,35, 0,50 y 0,65) dieron `quality_score = 1.000` **exacto** en los 18 escenarios. Un arreglo que no mueve el número que pretendía mover es indistinguible de no haberlo hecho, y sólo se ve midiendo. Ahora cada entrada del índice lleva la mejor similitud coseno de sus fragmentos, en la misma escala que la rama vectorial —si fueran escalas distintas, un 0,65 significaría una cosa en cada asistente de Gerencia y la comparación entre ellos, que es el objeto de la rama, no querría decir nada—, y con `index_scores_medidos` en la traza para distinguir «no había con qué medir» de «se midió y salió plano». **Y una predicción escrita antes de repetir el barrido**: no se espera que el umbral arregle el agéntico, porque su nota será el mejor parecido del corpus con la pregunta y para una pregunta fuera de ámbito eso sigue siendo ~0,7 — el mismo solapamiento del RAG. La nota real no hace útil la puerta: hace **visible** que la puerta no puede separar, que es el argumento de HIB.S. 8 tests. |
| 2026-08-27 | HIB.M (MEDICIÓN) — El reranker también resta en el corpus de Gerencia | Ablación sobre el lote de 18 de Gerencia, con el pool ya desacoplado de HIB.J. **Apagado**: fuente requerida 9/13, contestó sin deber 2. **Encendido**: 8/13 y 3. Empeora en los dos ejes, así que por el criterio escrito antes —el de HIB.A, palabra por palabra— **se queda apagado**, y además por coherencia con Normativa y por coste. Se vuelve a medir con el lote del piloto. **Y la comparación que la rama existía para hacer**: sobre el mismo lote, el agéntico recupera **12-13 de 13** fuentes requeridas contra las 9 del RAG y **nunca calla teniendo respuesta** (0 de 13), pero contesta **4-5 de 6** negativos que debería declinar frente a 2-3 del RAG. Encuentra mucho mejor y declina mucho peor; es la primera vez que se ve sin la asimetría de la nota constante, aunque el barrido de umbral haya que repetirlo ahora que la nota es real. |
| 2026-08-27 | HIB.G (DATOS + RED/GREEN) — Un lote que se puede medir sin un humano delante, y tres hallazgos que no se buscaban | **Cursor: HIB.G ✅ → HIB.N.** Dos lotes validados contra `escenario_contrato.Lote`: **Normativa 48** (36 preguntas reales + 12 negativos sintéticos; 30 con fuente requerida, 9 con ancla verificada) y **Gerencia 18** (12 reales + 6 sintéticos). **Desviación documentada**: el prompt pedía 60-80 y 30-40, y a la vez prohibía redactar preguntas mirando el articulado porque inflan la recuperación léxica (ObliQA contra BSARD); con los informadores de vacaciones las dos cosas no se podían cumplir y se cumplió la segunda — **ninguna pregunta está inventada**. La fuente esperada la anotó el equipo (`equipo_provisional`) localizando el candidato con `resolver_fuentes.py` y **verificándolo leyendo el artículo**; el prompt HIB.G ya preveía esta salida, con su regla de que ninguna cifra que dependa de ella se publica. **Cambio de métrica primaria**: en vez de juzgar la prosa —rúbrica de 2024 y juez que discrepa consigo mismo hasta el 28 %— se miden tres cosas deterministas: fuente requerida recuperada, ancla correcta y rendición correcta. **Los tres hallazgos**: (1) las 24 leyes estatales y autonómicas están en Normativa con `us_assistents='no'` y son el **45 % de su corpus por fragmentos** —el reconciliador las retiró al salir de su manifiesto, correctamente— así que las **seis preguntas reales de contratación que llegaron ahí no se podían contestar**: entran como negativos `fuera_de_alcance` de procedencia real y **las mismas seis entran en Gerencia como contestables**, con `meta.gemelo_negativo`; (2) el mandato del **Síndic de Greuges** se preguntó **siete veces** por el widget, dos con fallback por *quality gate*, teniendo la fuente en el corpus (`Reglament de la Sindicatura de Greuges`, art. 9); (3) el síntoma reportado —«varios artículos de la misma norma»— **no lo reproduce el sistema**: la agrupación por documento da una norma = una fuente (0 de 25 repiten) y **ningún padre del corpus contiene dos artículos**; lo que se vio eran apartados de un mismo artículo, que es correcto, en una interacción **anterior a la reingesta**. Lo que sí salió de ahí es una cita que **nombra una norma y enlaza otra** («artículo 6 del Real Decreto 99/2011» → enlace al Reglamento de la UJI art. 8.1), anotada para HIB.F. **Dos fallos silenciosos propios, corregidos**: `ILIKE` no ignora acentos y daba cero candidatos en 21 de 42 casos sin error alguno; y la primera medición dio **0 de 12 fuentes recuperadas en Gerencia** porque `EvidenceItem` habla `source_id`/`source_url` y yo leía `document_id`/`url` — la norma correcta estaba recuperada con 0,88. **Cinco contestables sin fuente** (ORI-12, ORI-18, SGE-03, SGE-04, REA-06 y REAL-07) quedan fuera del lote en `pendientes_de_anotar.json`: relajar el contrato habría dado un lote más grande y menos medible. 17 tests en `test_escenario_contrato.py`. |
| 2026-08-27 | HIB.O (DATOS) — Las 22 normas externas, firmadas; y la columna de la firma estaba vacía en todo el corpus | **Cursor: HIB.O ✅ → HIB.M** (que sigue esperando el lote de HIB.G). Instrucción del usuario: «las 22 normas externas están vigentes, anótalo y pon mi firma». Escrito `estat_vigencia='vigent'`, `vigencia_validada_el` y `vigencia_validada_per = 'Modesto Fabra <fabra@uji.es>'` en **66 filas = 22 normas × 3 copias**, y verificado: de las 66, **0 disparan el aviso** de vigencia. Lista con URL de consolidado en `_local/golden/vigencia_externas_firmada.json`. **El conjunto se identificó y se comprobó antes de escribir**, porque la firma afirma algo sobre documentos concretos: `source_kind='boe'` **no existe en el corpus** (todo es `crawler` o `publicacio`), y filtrar por URL de diario oficial da **30** normas, no 22 — porque la normativa propia de la UJI también se publica en el DOGV (Estatuts, Reglament de permanència, resoluciones del Rectorado, acuerdos del Consell de Govern). Las 22 externas son las que además tienen título de norma estatal/autonómica, y coinciden exactamente con las que tienen `us_assistents` mixto ('no' en Normativa, 'si' en Gerencia). El script asertaba `== 22` antes de escribir. **Dos hallazgos que la instrucción destapó.** (1) Las 22 **ya no avisaban** antes de la firma: tenían `vigencia_validada_el = 2026-08-10`. Lo que faltaba era **quién lo decía** — un control que parece hacer algo. (2) La causa: `corpus/reconciler.py` manda `vigencia_validada_per` a `_A_METADATA` (va a `doc_metadata`) mientras sí escribe `vigencia_validada_el` en `_COLUMNAS`, así que **la columna estaba vacía en las 549 filas del corpus** y `vigencia_no_validada()` sólo mira la fecha. **No se ha pisado ninguna firma humana**: en `doc_metadata` esas 22 llevan *procedencia* del dato («BOE, text consolidat: la vigència ix de l'atribut `fecha_vigencia`…»), no autoría, y sigue intacta. Pero el mismo campo del manifiesto significa **dos cosas distintas**: firma de persona en la normativa propia (359 filas con «Modesto Fabra») y procedencia en las externas (57+6+3). **Decisión de gobernanza pendiente, no tomada aquí**: si la columna debe recibir la firma del manifiesto y qué se hace con el doble uso del campo. **Estado del aviso tras la firma**: Normativa 50 de 297 documentos avisan, Gerencia econ-adm 38 de 124, Gerencia agéntico 41 de 127; de los 136 que avisan en total, **117 están en estado 'vigent' pero sin fecha** —normativa propia, que es de Secretaría General— y 6 están derogados o parcialmente derogados, donde el aviso es correcto. |
| 2026-08-27 | HIB.L (DECISIÓN + RED/GREEN) — Techo del padre: 8.000 tokens, y el que no cabe se queda sin padre | **Cursor: HIB.L ✅ → HIB.M.** `parent_max_tokens` heredable, defecto **8.000**; la sección que lo supera deja al hijo **sin padre** —responde con su propio texto, como `structural`—, sin subdividir y sin truncar. **La opción que el prompt recomendaba es inaplicable**, y el paso 0 lo demostró: los 24 padres que pasan de 8.000 son **8 preámbulos** (ancla `preambul`; los dos mayores, 59.172 y 45.329 tokens, sin encabezados internos porque los números romanos son texto corrido) y **16 artículos o anexos reales**, casi todos de leyes de acompañamiento cuyos artículos modifican otras leyes enteras (`art-119` de la Ley 6/2024 = 21.484 tokens). Ninguna de las dos clases tiene unidad estructural inferior a la que bajar; truncar estaba descartado porque parte artículos, que es lo que el padre existe para evitar. **Corrección de método, y me corrijo a mí mismo dos veces**: (1) la premisa del prompt —que yo escribí— generalizaba desde el máximo, porque la mediana de los 10.614 padres son **205 tokens** y el p90 **710**; (2) pero mi primer desmentido («0,2 %, es la cola, no la norma») también estaba mal, porque **contar padres distintos engaña**: lo que cuesta son los fragmentos, y cada hijo arrastra su padre, así que un padre gigante va pegado a todos los hijos de su sección. Medido por fragmentos: **5.172 de 60.859 (8,50 %)** arrastraban un padre de más de 8.000, en **11 documentos**; coste medio del padre por fragmento **2.955 → 895** tokens; y el peor caso con `top_k=3` sumaba **177.516 tokens, el 139 % del presupuesto**, así que el packer **descartaba evidencia en silencio** salvo por `dropped_count`. Con el techo baja al 19 %. Conclusión honesta: la premisa del prompt era más certera que mi desmentido; lo que erraba era el mecanismo del arreglo, no el problema. **Lo que este prompt NO decide y va a Secretaría General**: 13 de los padres que pasan de 4.000 tokens son preámbulos, y un preámbulo no es normativo — que el asistente pueda citarlo como fundamento es curación del corpus, no techo del padre. **Efecto pendiente**: el techo actúa al trocear, así que los corpus ya ingeridos conservan sus padres grandes hasta re-trocearse; para Gerencia eso es HIB.N, y por eso va después. Decisión escrita en `docs/DECISION_MODELOS_EMBEDDING_RERANKER.md`. **Fallo propio en el RED**: los tests llamaban a `chunker.chunk()` y el método es `split()`. **Tests: 9 nuevos + 194 passed** en troceado, ingesta y configuración. |
| 2026-08-27 | HIB.D (RED/GREEN) — El agéntico YA cita con ancla: cerrado sin cambio de código | **Cursor: HIB.D ✅ → HIB.L.** **El paso 0 obligatorio salvó el prompt**: su premisa era falsa y arreglar lo que señalaba habría empeorado el producto. Quinta premisa caída del bloque, y la primera que se caza ANTES de tocar nada. Siguiendo el camino real de una cita: (1) la línea que el plan señalaba —`md_agent_selector_pipeline.py:79`, `source_url=d.canonical_url`— construye el **índice**, y el índice **no es citable**: `core_graph` toma los `citables` de `agentic_loop.run()`, que devuelve `leidas`, lo que el bucle abrió de verdad; (2) `search_knowledge` **sí lleva ancla**, y por el mismo camino que el RAG —`_BuscadorVectorial.search` → `VectorRetrievalStrategy.get_context` → `url_de_cita(doc, best.metadata)` (PUB.3/ING.0.4) → `_source_to_evidence` la conserva—; (3) `read_document` **no lleva ancla y es correcto que no la lleve**: inyecta el documento entero, no hay un artículo al que apuntar, y un ancla arbitraria sería PEOR que ninguna porque apuntaría a un artículo que no fundamenta nada y el contrato no lo cazaría —sólo detecta anclas inexistentes, no equivocadas (defecto 3 del inventario del 2026-08-26)—. **Verificado en vivo y en navegador**: consulta real al agéntico sobre el límite del contrato menor de servicios; citó `#art-64`, `#div-3`, `#faq-5` y `#norma-2`, y sirviendo el sitio publicado se comprobó que `#art-64` cae en «Article 64. Contractes menors en l'àmbit de la investigació» a 19.060 px de scroll —el artículo exacto, no la cabecera— y `#div-3` en «3. Tramitació», el apartado citado. **Lo entregado, en vez del cambio**: `test_hibd_ancla_del_agentico.py` (6 tests) fija las tres cosas de arriba, y su función es impedir el arreglo equivocado — que alguien lea «el agéntico no pone ancla» y le ponga una a las lecturas de documento entero. **Dos observaciones del mismo ensayo**: la Ley 9/2017 se citó sin enlace, y es coherente —no tiene página en el sitio publicado, así que `search_knowledge` le da al modelo el nombre del fichero y no una URL: es el defecto 2 del inventario, no una regresión—; y `nota: 1.0`, que confirma el `score` fijo del agéntico y es HIB.E. **Tests: 6 passed.** |
| 2026-08-27 | HIB.G (continuación) — Los dos dashboards de anotación, verificados en navegador | **Cursor: HIB.G ▶ (instrumental completo, falta la anotación) → HIB.D.** Pedirle a un informador que edite un JSON es pedirle un error de sintaxis en la tercera pregunta, así que la hoja se convierte en interfaz. `_local/golden/generar_dashboard.py` produce `dashboard_anotacion_normativa.html` (25 preguntas, catálogo de **241** documentos citables) y `dashboard_anotacion_gerencia.html` (7 preguntas, **116**). Mismo patrón que los de `publicacio_transparencia_2026-07`: HTML autónomo sin recursos remotos, `localStorage` para que cerrar la pestaña no cueste el trabajo, contador, filtros y exportación por Blob. **El invariante que la interfaz protege**: el documento se ELIGE del catálogo, nunca se escribe —una URL a mano no casa con `canonical_url`, la fuente esperada no aparecería nunca «en lo recuperado» y el fallo se leería como fallo de recuperación—; el catálogo se filtra con el mismo `MetadataFilter` que la recuperación, para no ofrecer como fuente algo que el asistente no puede recuperar. El ancla sí se teclea, porque embeber los artículos de 241 documentos haría la página inmanejable. **No propone nada**: sale con los campos vacíos, porque proponer el primer candidato sería enseñarle al lote lo que el sistema ya hace y la métrica mediría cero. **Verificado en navegador** (`file://` está bloqueado para la extensión, así que se sirvió la carpeta por HTTP en el 8765): render de los dos, 3 candidatos por pregunta en Gerencia, filtros pendientes/anotadas (24 y 1 de 25), persistencia tras recargar, cero errores de consola, y **la exportación capturada del `Blob` real**. `test_export_del_dashboard.py` fija esa salida y comprueba que valida contra `Lote`, que el ancla viaja separada de la URL (`a118`, sin `#`), que un negativo sale sin fuentes y con motivo, que `provenance` es `informador`, y que `resume()` calcula las tres métricas sobre ella sin intervención humana. **Error propio**: el primer intento de verificación disparó el `alert()` de la exportación y **congeló el renderizador** —está avisado en las instrucciones y lo hice igual—; se cerró la pestaña y se repitió neutralizando los diálogos antes de tocar nada. Las marcas de prueba se borraron del `localStorage` al terminar. **Tests: 7 passed.** |
| 2026-08-27 | HIB.Q (RED/GREEN) — Un chatbot nuevo nace con lo que se ha medido | **Cursor: HIB.Q ✅ → HIB.D.** Prompt **añadido al bloque** a partir de una pregunta del usuario al cerrar HIB.C: si los cambios valen también para los chatbots que se creen después. Respuesta: el **código** sí (HIB.J, HIB.B, HIB.C, HIB.I), la **configuración por chatbot** no. **La trampa que decidió el arreglo**: `_apply_layer` aplica sólo los valores no nulos de cada capa, así que una columna NOT NULL del chatbot gana SIEMPRE al defecto de plataforma — y `retrieval_top_k` es NOT NULL con defecto 8, de modo que tocar sólo `_PLATFORM_DEFAULTS` no habría movido nada para ningún chatbot. Se mueve el defecto de la **columna** a 3, y el mismo 3 en la plataforma y en el dataclass para que no haya dos números para la misma anchura. `query_rewriting_enabled` sí es nullable —nulo hereda—, así que su sitio es la plataforma: **False → True**, porque desde HIB.C un chatbot nuevo recibiría el historial y no lo reescribiría, sin ningún error que lo delatara. **Sin migración y sin `server_default` a propósito**: un defecto de columna sólo actúa al insertar, así que los chatbots existentes no se tocan. Se quedan igual `min_retrieval_results` (2; el 1 de Normativa fue un apaño de desarrollo con un corpus de un documento), `quality_threshold` (0,6, que coincide con lo que eligió HIB.J por casualidad y no por diseño) y `reranker_enabled` (False, coherente con HIB.A). **Tres valores vivos siguen en la escala vieja y quedan anotados**: Gerencia RAG con umbral 0,35 y reranker True, y el agéntico con 0,5 puesto cuando su puerta no hacía nada — los tocan HIB.M, HIB.P y HIB.E. **Fallo propio en el RED**: el primer test construía `PublicGraphConfig()`, que tiene campos obligatorios; se asienta sobre `_PLATFORM_DEFAULTS`, que además es la capa que de verdad hereda. **Tests: 8 passed** (nuevos) + **176 passed** en `tests/public_graphs` y RAG.15 sin tocar nada. |
| 2026-08-27 | HIB.C (RED/GREEN) — El seguimiento conserva el contexto; y la premisa del prompt era falsa | **Cursor: HIB.C ✅ → HIB.Q.** **Cuarta premisa caída del bloque, y la de mayor efecto visible.** El prompt decía que `necesita_reescritura` exige «dos turnos previos» y que por eso el segundo turno no se reescribe. Falso: el historial se aplana a `'rol: texto'` **por turno**, así que una pregunta y su respuesta ya son DOS entradas y la segunda pregunta sí entraba. Y `query_rewriting_enabled` está en True en Normativa y en los dos de Gerencia. **El defecto real**: el widget no enviaba historial en absoluto —el cuerpo era `{ message, lang }`—, así que `request.history` llegaba siempre vacío, la reescritura NUNCA corría desde el widget y cada pregunta se trataba como la primera. `ChatRequest.history` existía, el grafo lo consumía y RAG.10 lo probó, pero **ningún cliente del frontend lo enviaba** (comprobado en todo `frontend/src`). Eso explica entero el caso del doctorado internacional del 2026-08-26. **Lo hecho**: el widget envía los turnos previos, con tope de **5 intercambios (10 entradas)** — decisión del usuario del 2026-08-27, que juzgó 3 poco para el hábito de un chat y recordó que el modelo de reescritura es pequeño. El historial se toma ANTES de añadir el turno nuevo, o el reescritor tomaría la pregunta actual como su propio antecedente. `necesita_reescritura` **se queda en 2** y el test lo documenta, para que nadie lo baje creyendo que arregla algo. **Y una duda del usuario que destapó un segundo defecto**: al cerrar y reabrir el panel la conversación sobrevive (`useChat` se monta siempre; `open` sólo decide qué se pinta), y desde que el historial viaja eso pasa de resto visual a **contaminación de la recuperación** —una pregunta de otro tema se reescribe contra el anterior— y a problema de privacidad en equipo compartido. Decisión del usuario: **botón «nueva conversación» + caducidad a los 30 min sin actividad**, y cerrar el panel NO vacía, porque cerrar no significa «he terminado». i18n en es/ca/en. **Fallo propio, encontrado antes de que llegara a ningún test**: `messagesRef` se escribía sólo al enviar, así que la respuesta que se rellena token a token nunca entraba y el historial habría viajado con la respuesta anterior VACÍA; ahora el ref sigue al estado con un efecto. **Tests: 40 passed** (widget, 8 nuevos) + **17 passed** (servidor), `tsc` limpio. |
| 2026-08-27 | HIB.B (RED/GREEN) — Al usuario sólo le llegan los tokens de la respuesta | **Cursor: HIB.B ✅ → HIB.C.** Dos defectos encadenados. (1) `on_chat_model_stream` emitía los tokens de **cualquier** llamada al modelo, y en el grafo hay tres nodos que no son la respuesta —`rewrite_query`, `reformular` y, en modo agéntico, el selector dentro de `retrieve`—, así que quien preguntaba veía la consulta reformulada como si fuera la contestación. (2) El mensaje de rendición sólo se emitía `if fallback_answer and not collected_tokens`, así que cuando algo ya había dejado tokens **nunca se mostraba**. **La lista de nodos no se escribe a mano**: se deriva de `_FINAL_NODES`, que ya es la fuente de verdad de quién produce la respuesta y que el manejador del estado final ya usaba. Un test recorre el grafo real y se pone rojo si aparece un nodo que escribe `answer` sin declararse ahí. **Decisión del usuario (2026-08-26), con las dos cifras delante**: el contrato de citas corre DENTRO de `generate_answer`, después de generar, así que sus tokens ya han salido cuando rechaza. Retenerlos hasta validar costaría el streaming en TODAS las respuestas —medido en HIB.I, el primer token pasaría de **780 ms a 2.140 ms**— para arreglar el **~4 %** que el contrato rechaza (1 de 25). Se elige conservar el streaming y añadir un evento SSE `discard`: el widget vacía la burbuja (no la borra, o el mensaje de rendición no tendría dónde aterrizar) y la rendición llega detrás como un `token` normal. `assistant_message` guarda ahora lo que el usuario vio; antes guardaba la reformulación, así que quien revisaba juzgaba una respuesta que nadie recibió. **Fallo propio en el RED**: el primer test cortaba el bloque del stream a 900 caracteres y el comentario que explica la decisión empujó la comprobación fuera de la ventana; ahora se delimita por el `elif` siguiente. **Tests: 6 passed** (servidor) + **32 passed** (widget, 2 nuevos del descarte). Vitest falló una vez por timeout del worker (60 s, infraestructura, sin tests ejecutados) y pasó al reintento. |
| 2026-08-26 | HIB.I (RED/GREEN) — La traza que permite volver atrás | **Cursor: HIB.I ✅ → HIB.B.** `interaction_metadata` pasa de una clave (`usage_source`) a **20**, construidas por `evaluation/traza.construye_traza` y fijadas por snapshot. Lleva la configuración **vigente en esa respuesta** —modo, troceado, `top_k`, `candidate_k`, umbral, reranker— y no un puntero a la del chatbot: la del chatbot cambia, y una respuesta de hace tres semanas leída con el umbral de hoy deja muda la comparación entre tandas sin dar ningún error. Además: `retrieved` con identificador, URL, **ancla ya separada**, nota y título de cada evidencia en su orden; `dropped_count` del packer; `best_score` y `gate_passed`; lenguas y aviso de traducción; `turn_index`, `rewritten_query`, `reformulada`; `last_index_level`; y `latency_ms` + `first_token_ms`, con el reloj arrancado ANTES del primer evento del grafo porque la latencia que importa es la que espera quien pregunta. **Lo que no entra**: el contenido de los fragmentos (ya está en `hub_document_chunks` y duplicarlo multiplica la tabla por el tamaño del contexto) y `fallback_reason` (ya es columna indexada). **Por qué JSONB**: la lista va a crecer con cada prompt de medición y una migración por campo es lo que hace que dejen de añadirse; el precio es que un cambio de nombre rompería el instrumental en silencio, y de ahí el snapshot. **Verificación del cableado**: se comprueba sobre el fuente del endpoint, siguiendo el precedente de `test_should_mark_usage_source_when_estimated` (SEC.4) — el valor se escribe donde se decide y separarlo inventaría una indirección que sólo existiría para el test. **El servidor en el 8000 sigue con el código anterior**: no se reinició a propósito, es el proceso del usuario; la comprobación en vivo de la traza queda para el próximo arranque. **Tests: 41 passed** (13 nuevos). |
| 2026-08-26 | HIB.H (RED/GREEN) — La revisión deja una referencia reutilizable | **Cursor: HIB.H ✅ → HIB.I.** `hub_interactions` gana `review_expected_sources` (JSONB) y `review_reference_answer` (Text); `hub_test_scenarios` gana `expected_sources` con la MISMA forma, validada por el mismo modelo Pydantic (`evaluation/escenario_contrato.ExpectedSources`) que ya valida el lote de HIB.G. Migración `a7t8u9v0w1x2`, aplicada. **Por qué importa**: con el veredicto solo, cada conversación revisada valía una vez y la siguiente ablación exigía volver a pedir juicio humano sobre las mismas preguntas — la razón concreta por la que las ablaciones no se hacían. **NULL es «no anotado», no lista vacía**, y por eso no hay `server_default`: una lista vacía afirmaría que el informador miró y decidió que no hay fuente, que es otra cosa, y la diferencia es el denominador de «cuántas están anotadas». **Y un `None` no borra lo ya anotado** en `record_review`: el veredicto se pide de todas las conversaciones y la fuente esperada sólo de algunas, así que la mayoría de los PATCH llegan sin ella y machacar con nulo perdería la anotación cara cada vez que alguien reconfirma un veredicto. **La URL se valida en el contrato** (`http(s)://`): el título de la norma escrito a mano casa con nada, así que la fuente esperada nunca aparecería «en lo recuperado» y el fallo se leería como fallo de recuperación — se pierde la métrica sin dar ningún error. Exportación CSV con tres columnas nuevas (`review_expected_required`, `..._acceptable`, `review_reference_answer`), con documento y ancla juntos porque una hoja de cálculo no puede con una lista anidada. Contrato regenerado (149 paths, 251 schemas) y `tsc --noEmit` limpio. **Hallazgo de fixture, no de código**: dos tests de REV.1 se pusieron rojos porque su `MagicMock` sin `spec` fabricaba hijos para los campos nuevos y Pydantic recibía mocks donde promete dict y str; se declaran explícitamente en la fixture. Es el mismo patrón que ya está anotado sobre los mocks sin `spec`. **Tests: 41 passed.** |
| 2026-08-26 | HIB.G (DATOS + RED/GREEN) — El contrato del lote y los negativos, hechos; la anotación es de los informadores | **▶ PARCIAL. Cursor: HIB.G ▶ → HIB.H.** Lo hecho: (1) `escenario_contrato.py` —`Escenario`/`Lote` en Pydantic con `expected_sources` (`required`/`acceptable`, documento + ancla del ARTÍCULO), `answerable`+`refusal_reason`, `kind`, `language`, `domain`, `provenance` y `reference_answer`; `uso='afinado'` viaja con los datos para que nadie reporte sobre el mismo lote con el que afinó—; (2) `escenario_metricas.py` con las tres métricas automáticas —fuente exigida en lo recuperado (fracción, no booleano: en `varios_articulos` acertar 1 de 2 no es acertar), ancla correcta y rendición correcta—, que devuelven `None` cuando no aplican, distinto de `False`; (3) `_local/golden/negativos.json`, **12 negativos** escritos a propósito y validados contra el contrato (4 ca / 8 es; motivos: `dato_no_normativo` ×4, `premisa_falsa` ×3, `fuera_de_alcance` ×4, `vigencia_no_validada` ×1; 4 son de Gerencia, y NEG-10 —umbral de contrato menor inventado en la propia pregunta— es el que más importa allí); (4) `_local/golden/hoja_anotacion.py` y las dos hojas generadas, que proponen al informador los candidatos que el sistema recupera hoy para que **revise en vez de buscar**. **Lo que NO se ha hecho y por qué**: el lote no llega a 60-80 (Normativa) ni a 30-40 (Gerencia). Dos razones medidas, no estimadas. **(a) No hay preguntas reales de donde sacarlas**: `hub_interactions` tiene **47 filas y 39 consultas distintas** repartidas entre los cuatro chatbots, y buena parte son las propias tandas de medición. El prompt prohíbe expresamente escribirlas desde el corpus —una pregunta redactada mirando el artículo comparte su vocabulario e infla la recuperación léxica, que es la diferencia entre ObliQA y BSARD—, así que las fuentes legítimas están agotadas: **el lote contestable sólo puede crecer con el piloto, que es su fuente natural**. **(b) La fuente esperada de las 25 existentes no se puede extraer de la prosa**: sólo **4 de 25** notas declaran «FUENTE ESPERADA» y **2** traen número de artículo; el resto es el veredicto del informador sobre por qué falló la respuesta anterior, que es un dato distinto y más valioso. Anotarlas exige decidir qué artículo de 297 documentos contesta cada pregunta: juicio jurídico, y el prompt ya dice que una cifra sobre fuentes anotadas por quien afinó el sistema no se publica. De ahí la hoja de anotación en vez de rellenarlas yo. **Consecuencia para el bloque**: HIB.E, HIB.F, HIB.M y HIB.P miden sobre este lote, así que su parte cuantitativa queda a la espera de la anotación. Los negativos SÍ están listos, y con ellos el eje «contestó y no debía» ya se puede medir por primera vez. **Error propio, corregido**: generé la primera hoja de Gerencia con un UUID inventado a partir de los 8 primeros caracteres del listado; salió «0 candidatos» en las 7 consultas y eso lo destapó. Repetida con `722aaa10-d7eb-4b7d-b623-a53603d3a777`: 3 candidatos en cada una. **Tests: 69 passed** en `tests/modules/agents_hub/evaluation/` (13 del contrato + 20 de las métricas, nuevos). |
| 2026-08-26 | HIB.J (RED/GREEN + MEDICIÓN) — La puerta leía una constante, y `top_k` no contaba documentos | **Cursor: HIB.J ✅ → HIB.G.** Ejecutado antes de HIB.B, según el orden aprobado. **Paso 0, antes de tocar nada**: sobre la tanda sin reranker de HIB.A, `quality_score` valía **exactamente 0,7 en las 25 consultas** —un solo valor distinto— y los documentos por consulta eran {1:6, 2:13, 3:6}. **Mi predicción en el plan era incorrecta en el mecanismo**: dije «casi binaria (≈0,98/≈0,50)» y la realidad es una constante, porque 0,7 ES `vector_weight`: el ganador de la fusión es siempre el rango 1 de la rama vectorial (`vector_weight·1/(k+1)`) y al normalizar por `RRF_MAX_SCORE = 1/(k+1)` queda `vector_weight`; el techo teórico exige que el MISMO fragmento salga por las dos ramas y con troceado fino no ocurre nunca. La dirección sí era la predicha: la puerta había dejado de discriminar. **Desviación documentada, y es la decisión del prompt**: el plan preveía «recalibrar el umbral en la escala RRF», pero en esa escala no hay señal que calibrar. La nota que lee el gate pasa a ser la **similitud coseno** del mejor fragmento (`SearchResult.relevance`, que `hybrid_search` ya no pierde); la ORDENACIÓN sigue siendo RRF. No cambia la intención original, la cumple: `Source.score` se documentaba como «relevancia [0,1]» y el contrato de `hybrid_search` ya pedía esa escala para este consumidor. Colateral: con y sin reranker el umbral significa lo mismo, y desaparece la asimetría que HIB.A declaró como trampa. **Segundo defecto**: `candidatos = pool_size(top_k) if reranker else top_k` encogía el pool de 30 a 3 al apagar el reranker —dos cambios en una línea—; ahora es `candidate_k` (nulo ⇒ `pool_size(top_k)`), independiente del reranker, y `top_k` se aplica TRAS agrupar, en documentos. **Resultado medido con `top_k=3`**: notas distintas **1 → 25**; documentos por consulta **{1:6,2:13,3:6} → {2:1,3:24}**; consultas con un solo documento **6 → 0**. **Curva del umbral** (notas 0,6917–0,7854): hasta 0,68 no calla ninguna, 0,70 calla 3, 0,72 calla 4, 0,74 calla 7, 0,76 calla 17, 0,80 calla 25. **Punto de operación provisional**: `quality_threshold` 0,40 → **0,60** en Normativa UJI, `top_k=3`, `candidate_k` nulo. Elegido así porque las 25 del lote tienen respuesta en el corpus, luego todo silencio es un fallo y la medición NO puede elegir el umbral: 0,60 queda bajo el mínimo observado con margen y deja la puerta capaz de actuar. Sobre el lote no cambia nada; el definitivo sale de HIB.G (con negativos) y del piloto. **Verificado en vivo**: ORI-01, que el informador marcó incorrecta, responde con el art. 4.1 (20 % de grado) en vez del art. 7 (máster, 40 %), con ancla y tres documentos, nota 0,768. **La migración autogenerada se descartó**: quería borrar todas las tablas legacy de AutomatIA; se escribió a mano (`z6s7t8u9v0w1`) y se aplicó. **Test viejo reescrito, no borrado**: `test_normalizes_rrf_score_to_0_1_scale_without_reranker` probaba la fórmula en su techo teórico y por eso pasaba; ahora es `test_does_not_derive_the_gate_score_from_the_rrf_fusion` y es el guardarraíl contra reintroducirla. **Tests: 718 passed, 1 failed** (`test_deploy_mode`, ajeno: `deterministic_chart_service` importa matplotlib, que necesita directorio home en este shell). Instrumental: `_local/golden/rejilla_hibj.py`. |
| 2026-08-26 | HIB (planificación, 2.ª revisión) — Diez prompts nuevos y cinco ajustados; HIB.J antes de HIB.B | **Cursor: HIB.A ✅ → HIB.J.** Redactado con Fable 5 sin ejecutar nada, a petición del usuario. **Por qué otra revisión el mismo día**: (1) apagar el reranker en HIB.A cambió `candidatos = pool_size(top_k) if reranker else top_k` (`vector_strategy.py:69`) de 30 a 3 fragmentos, y como después se agrupa por documento `top_k=3` ya no garantiza tres documentos; además la nota RRF normalizada es casi binaria (≈0,98 si el documento está en ambas ramas, ≈0,50 si en una), así que con umbral 0,40 la puerta probablemente no rechaza nada — de ahí **HIB.J** y su orden; (2) el bloque sólo dejaba listo Normativa y el usuario abre también Gerencia — rama L–P; (3) los dos lotes no se pueden medir sin humano: `expectation_note` es prosa y Gerencia son 7 consultas con `name`+`prompt` — de ahí **HIB.G** (fuente esperada por artículo, clases, negativos, procedencia) y **HIB.H** (el panel captura la solución: `review_expected_sources`, `reference_answer`). **Premisas comprobadas en código antes de escribir**, por la lección de la primera versión: streaming sin filtro y rendición sólo sin tokens (`hub_chat.py:501-554`), `interaction_metadata` sólo con `usage_source`, agéntico con `score=1.0` y `canonical_url` sin ancla, `necesita_reescritura >= 2`. Ajustes: HIB.B sube a Opus (nodos derivados del grafo; retener tokens hasta el contrato), HIB.C (test de repregunta autocontenida; coste 800 ms/400 tokens fijado), HIB.D (anotar por qué no hay ancla), HIB.E (sobre el lote de G, no sobre 7), HIB.F (remisión fuera del denominador; juez validado contra 20-30 a mano). **Desviación documentada**: HIB.J medirá sobre las 25 con la columna «contestó y no debía» vacía, y HIB.G repite la rejilla cuando exista su lote. Valoración del lote en el informe de literatura: no un lote nuevo desde el corpus (inflaría la recuperación léxica, ObliQA vs BSARD) sino el real enriquecido; 60-80 Normativa, 30-40 Gerencia; afinado y piloto separados. |
| 2026-08-26 | HIB.A (MEDICIÓN) — El reranker resta, y llevábamos meses dándolo por bueno | **Cursor: HIB.A ✅ → HIB.B.** Ablación con y sin reranker sobre `Normativa UJI`, corpus con padre, mismas 25 consultas. **Sale de la literatura**: LegalBench-RAG midió que un reranker comercial de uso general **empeora** la recuperación jurídica, y nosotros teníamos el Ranking API de Vertex en el camino crítico **sin haberlo medido nunca aquí**. **Los cuatro ejes apuntan al mismo sitio**: cumple la rúbrica del informador **15 de 25 sin reranker frente a 9 con él** —el salto más grande medido en toda la sesión, mayor que el del padre (8→12)—; el juez a ciegas con doble vuelta **gana SIN por 9 a 5**; trae la fuente esperada en **4 de 4** frente a 2 de 4; y los descartes por citas caen de 5 a 1. **Y no es un matiz de orden**: sólo **5 de 25** consultas recuperan el mismo conjunto de documentos, con solapamiento medio de Jaccard **0,49**. El reranker estaba cambiando la mitad de lo que llegaba al modelo. **La trampa que el prompt declaró ANTES de medir, y que hay que seguir declarando**: el «24 contesta frente a 19» **no es comparable**, porque sin reranker la nota es la fusión RRF normalizada y con él la del reranker en [0,1] — el mismo umbral 0,40 significa cosas distintas. Por eso el criterio escrito de antemano no era el recuento, sino la fuente esperada y el juez, y los dos dan lo mismo. **Decisión aplicada según ese criterio**: `reranker_enabled = false` en Normativa UJI. **Queda pendiente y es obligatorio antes del piloto**: recalibrar el umbral sobre la escala RRF, porque 0,40 se eligió sobre la otra. **Honestidad estadística**: la prueba de signos da **p = 0,424**; nada de esto es significativo con 25 consultas. Lo que lo hace creíble es que cuatro ejes independientes coinciden, no la p. **Señal de mecanismo, no sólo de resultado**: con reranker aparecen repetidamente las «Directrices académicas» del curso entre lo recuperado (ORI-05, 07, 08, 12, 16, 20), que es el documento asociado a `curso_caducado`, el defecto dominante del ensayo anterior con 15 de 25. **Lo que gana el reranker, para no tirarlo del todo**: ORI-12 y ORI-18, donde distingue el curso académico correcto y evita citar el reglamento de permanencia donde no toca. **Esto revierte de facto RAG.6a/6b**, que lo activó sin medirlo contra la alternativa. |
| 2026-08-26 | HIB.0 (RED/GREEN) — El contrato de citas enlaza lo que el modelo nombra bien, y despoja lo que no se le dio | **Cursor: HIB.0 ✅ → HIB.1.** **El prompt se escribió sobre una premisa falsa y la sonda la tumbó antes de implementar nada, que es justo para lo que existe.** HIB.0 decía que con `parent_child` el modelo enlaza las remisiones que trae dentro del artículo y el contrato tira la respuesta entera; de ahí que los descartes por citas fueran 1 → 2 → 5 en las tres tandas. **Nada de eso era cierto.** Primero: hoy basta **una** cita válida para pasar, así que una respuesta con fundamento *y* remisiones ya sobrevivía — cuatro de los cinco tests nuevos pasaban antes de tocar el código. Segundo: al sondear las cinco descartadas, **tres (ORI-02, ORI-11, ORI-14) no se reproducen**: dan `descartada por: None` y citan bien. Era **intermitencia entre tandas**, la misma que este lote ya había mostrado, y leer «1 → 2 → 5» como tendencia causada por el padre fue un error de interpretación mío. **Lo que sí había, y es el defecto de verdad**: en SGE-01 el modelo respondió correcto y con detalle —1,5 créditos ECTS por claustral, 80 % de asistencia— citando `[Reglamento sobre reconocimiento y transferencia de creditos…, ANEXO II, apartado 3]`, **corchetes sin URL**. No es un enlace markdown, no hay cita válida y la respuesta se descarta entera. **RES.5 había previsto exactamente esta forma** y aplazó el enlazado determinista porque midió **0 casos en 85 respuestas**, dejando escrito que cero en 85 es compatible con hasta un 4,4 % y que había que repetirlo con tráfico: **ese cero ha caducado**. **Tres cambios, en un orden que es el contrato**: (1) `degradar_anclas` primero, para que una cita al documento correcto con ancla equivocada baje al documento y sobreviva; (2) **`enlazar_citas_en_prosa`**, que pone la URL cuando el corchete nombra un documento **recuperado** —comparando títulos normalizados sin tildes, porque el corpus escribe «creditos» sin tilde y «máster» con ella en el mismo título, y ganando el título más largo cuando dos empiezan igual—; (3) **`despojar_remisiones`**, que deja en texto plano lo que apunta fuera del conjunto recuperado. La comprobación va **la última**, sobre el texto ya despojado: al revés, una remisión enlazada contaría como cita y el contrato dejaría de proteger de lo que existe para proteger. **La regla la fijó el usuario**: se enlaza la norma que se usa, no las que esa norma menciona. **Lo que NO amplía**: el enlazado busca sólo entre lo recuperado y la URL la escribe el código desde `source_url`, así que no hay puntero que el modelo pueda inventar. **Verificación en vivo, con su límite dicho**: SGE-01 vuelve a responder, pero en esa ejecución el modelo **sí escribió un enlace bueno por su cuenta**, así que el rescate no se le puede atribuir al cambio — la prueba del comportamiento es el test unitario con la cadena real observada. De paso se vio al modelo escribir `http://127.00.1:4174/…`, una URL mal tecleada, que ahora se despoja en vez de contar. **Queda abierto y no lo toca este prompt**: SGE-04 sigue descartándose y **el contrato acierta** —el modelo declinó honestamente—, pero sustituye una rendición específica y útil («los documentos detallan los baremos de PTGAS, pero no los requisitos del título de mestre») por el mensaje genérico. **`tests/modules/agents_hub` + `tests/public_graphs` + higiene: 1.293 passed.** 10 tests nuevos. |
| 2026-08-26 | Planificación — **Bloque HIB**, salido de re-trocear el corpus con padre y medirlo | **Cursor: sin cambio (Deploy/D.0).** Siete prompts, ninguno inventado: los siete salen de tres tandas del lote ujirag-2024 y de las consultas reales que el usuario hizo con Secretaría General. **Lo que la medición dio**: el corpus de Normativa UJI pasa a `parent_child` (23.306 → **60.859** fragmentos, 297 documentos, 0 errores) y el juez a ciegas con doble vuelta da **12 de 25 contra 8** cumpliendo la rúbrica del informador, ganando **11 a 6** con sólo **3 desacuerdos entre vueltas** —frente a 12 en la comparación de `top_k`, o sea que aquí la diferencia es lo bastante grande como para que el orden de presentación deje de decidir—. El caso que trajo el usuario, el quórum del Consell de Govern, pasa de citar «art. 8 · Comissions» a **«art. 5.8»** con su ancla: la causa era que `_bloque_de_fuentes` **no escribe el encabezado del artículo** y el fragmento empezaba por «8. », que es el APARTADO 8 del artículo 5 — el modelo no tenía de dónde sacar el número y cogió el único que veía. **Aviso estadístico, anotado en el propio bloque**: 11-6 da p=0,33 y la mejora de rúbrica p=0,39; la dirección es consistente en dos métricas y los motivos son del tipo esperado («B incluye las cuatro becas», «B distingue grado de máster»), pero **25 consultas no demuestran nada** y no se puede reportar como demostrado. **Lo que el padre rompió, y por eso HIB.0 va primero**: los descartes por el contrato de citas van **1 → 2 → 5** en las tres tandas, y las tres nuevas tienen la nota de recuperación IDÉNTICA a la tanda anterior — no es el corpus, es que el artículo entero trae **remisiones a otras normas**, el modelo las enlaza y `enforce_citation_contract` tira la respuesta completa. Es hoy el modo de fallo dominante, por encima de la puerta de calidad, con el **20 % de las consultas**. **Hallazgo sobre el agéntico**: `agentic_strategy` elige leyendo un catálogo de fichas y **no consulta los embeddings en ningún momento**, así que sus 14.208 fragmentos son peso muerto y su `score = 1.0` fijo deja la puerta inerte; HIB.1 los convierte en la nota que le falta, **añadiendo** la vía vectorial sin sustituir al catálogo, porque «quién firma esto» es una consulta de tabla que el vector falla y la ficha acierta. **Reutilización medida**: las 22 normas externas están en los tres chatbots con `content_hash` **idéntico**, así que pasar Gerencia a `parent_child` cuesta copiar 27.353 fragmentos (0 €) y re-embeber sólo ~11.000: el 71 % del trabajo ya está pagado. **Las leyes estatales quedan apagadas** con `us_assistents='no'` a petición del usuario para que las pruebe él: medido, quitarlas cambia la recuperación en **3 de 25** consultas y sólo una de verdad (SGE-02, de 0,364 a 0,641 y de callar a contestar), y **ninguna respuesta citaba el BOE** con ellas dentro. **Colisión de nombres corregida al escribirlo**: los tres últimos prompts nacieron como COR.1–COR.3 y ya existía un bloque COR —el del corpus por organización—; renombrados a HIB.4–HIB.6. |
| 2026-08-25 | **Bloque RES: RES.1–RES.4** — Que el asistente responda | **De 18 de 25 y 2 de 7 a 22 de 25 y 6 de 7.** Sale de que el usuario rechazara el resultado de RAG.15 con el criterio correcto, y de que afirmara que todas sus preguntas tienen respuesta en el corpus: comprobado por SQL, la tenían. **RES.1 — la puerta puntúa con el mejor fragmento** y no con la media, porque con la media la cola votaba sobre si hay respuesta y `retrieval_top_k` decidía de rebote cuántas preguntas se contestan; SGE-01 tenía un fragmento de 0,637 con umbral 0,50 y se rendía porque los tres de detrás bajaban la media a 0,371. Lo delicado no era el cambio sino que **`quality_threshold` cambia de significado** —de «buenas de media» a «tengo una fuente buena»—, escrito en el modelo, en el contrato del router y en `DEPURAR_CONTEXTO_RAG.md`, porque quien puso 0,35 lo puso con el otro criterio. **RES.2 — segunda búsqueda al fallar**, con guarda contra bucles (la propia `reformulated_query`) y **plantilla nueva**, porque la de `query_rewriter` resuelve pronombres con el turno anterior y en una primera pregunta no hay pronombres; se paga sólo cuando la puerta rechaza, que es el argumento frente a normalizar siempre. **Y dos fallos silenciosos encontrados verificándolo, los dos con el síntoma idéntico a no tener reformulación**: `get_rewrite_model` traía `max_tokens=100` escrito para un modelo que no razona, y `gemini-2.5-flash` sí razona, así que las reformulaciones llegaban como «Cont» y «Contrato menor de» —y no lo cazaba nada, porque media palabra no está vacía ni pasa de 300 caracteres—; al subirlo a 512 el modelo devuelve la correcta pero tarda 2,4-2,6 s contra un plazo de 2,0, así que **se mataban todas**. Ahora hay guarda por `finish_reason` y plazo propio de 8 s, y la guarda vale también para la reescritura de RAG.10, que llevaba el mismo tope y el mismo defecto. Con las dos cosas, REAL-04 pasa de 0,126 a **0,601** y REAL-05 de 0,172 a **0,574**. **RES.3 — expansión léxica** sobre `bilingual_terms`, que ya alimenta el `tsvector` como columna generada: aplicar un par es un `UPDATE` sin re-embeber. Tres invariantes con su motivo —proyección **derivada** y reconstruible, nada se aplica sin que una persona lo apruebe, y el léxico nunca entra en `embedding_text`— y la cosecha que cierra el bucle sin pedirle a nadie que invente: cada respuesta rescatada por RES.2 deja el par ya validado por haber funcionado. **La suite completa destapó que la tabla estaba del lado equivocado** y el guardarraíl de la frontera tenía razón: nació en `HubConfigBase` por analogía con `hub_vocabulary_terms`, pero `termino_de_usuario` es literalmente lo que escribió una persona y la configuración se sincroniza cloud→edge — o sea que obligaba a que el texto de las preguntas del cliente existiera en el cloud. Lo que decide el lado no es «es vocabulario» sino si lleva dentro texto de alguien; movida a `HubOperationalBase`, sin FK y sin `__ambito__`, mismo motivo que `hub_test_scenarios`. **RES.4 — `retrieval_top_k = 3` en los dos**, elegido con cifras: con la puerta desacoplada la columna del mejor es plana en 2, 3, 5 y 8, así que la anchura sólo decide contexto; end-to-end, 3 responde 22 de 25 frente a 20, los rechazos están **anidados** y la calidad es indistinguible (4-2 con **12 de 25 veredictos dados la vuelta**). El coste se miró una por una: en las dos respuestas de fuente única con 3, la versión con 5 cita un segundo documento pertinente. Se acepta porque `respuesta_incompleta` es 4 de 25 en el catálogo frente a los 15 de `curso_caducado`, que es el modo que estrechar reduce. **Y la decisión pendiente del umbral 0,35 de Gerencia se resolvió sola**: con la puerta nueva responde 6 de 7 con ese valor. **Desviación de método**: RES.1 y RES.2 fueron en un commit (`a99e16f`) porque ambos cambian `merge_node` y `quality_gate` en el mismo fichero y separar los *hunks* habría dejado un commit cuyo código no cubren sus tests. **Suite: 3.166 passed, 1 skipped, 1 failed (23:03, `-n0`)** — el fallo era el guardarraíl de la frontera, ya corregido. Migraciones `x4q5r6s7t8u9` y `y5r6s7t8u9v0` aplicadas con `downgrade` comprobado; contrato regenerado a 149 rutas. |
| 2026-08-25 | Planificación — Bloque RES (que el asistente responda), delante de Deploy | **Cuatro prompts que salen del diagnóstico, no de suponer nada.** Se meten **antes de Deploy** por decisión del usuario: desplegar un piloto que calla en la mitad de las consultas es gastar la primera impresión, y ésa no se repite. **RES.1 — la puerta puntúa con el mejor fragmento** (Sonnet): `merge_node` usa la media de todas las puntuaciones, así que la cola vota sobre si hay respuesta; con el mejor son 20/25 y 4/7 **con cualquier anchura**. Lo que el prompt obliga a escribir y es lo delicado: **`quality_threshold` cambia de significado** —de «mis fuentes son buenas de media» a «tengo una fuente buena»—, así que hay que actualizar la ayuda del campo y el docstring, porque un admin que puso 0,35 lo puso con el significado viejo. Y deja registradas las políticas ya descartadas **con cifras**, para que nadie las reproponga: media de 3 y mediana 18/25, suelo relativo 19/25 y reintroduce fuentes únicas, `top_k=2` igual pero con 6/25 de fuente única. **RES.2 — segunda búsqueda al fallar** (Opus): nueva rama del grafo `gate → reformular → retrieve → merge → gate`, con **guarda contra bucles** (`ya_reformulado`) porque sin ella es un bucle infinito con coste por vuelta, y respetando el bypass de RAG.11. El prompt de reformulación **es nuevo**: el de `query_rewriter` está escrito para resolver pronombres con el turno anterior y aquí no hay pronombres, hay que traducir intención a vocabulario normativo. Se paga **sólo al fallar** —28% en Normativa, 71% en Gerencia—, que es el argumento de todo el diseño frente a normalizar siempre. Segunda búsqueda **silenciosa** pero el hecho se expone en el contrato (`reformulada`), porque una respuesta rescatada no se revisa igual que una directa y el texto para el ciudadano es decisión de producto. Se anota explícitamente que **NO** cablea `needs_secondary_search` (es la segunda pasada por *lengua*, otra razón; mezclarlas es como se consigue un nodo que nadie entiende). **RES.3 — expansión léxica** (Opus): tabla nueva en `HubConfigBase` con `__ambito__ = "organizacion"`, con la forma de `hub_vocabulary_terms` (`vigent`, `substituit_per_codi`) y **prohibido expresar los términos como Enum o CheckConstraint**. El invariante que lo hace mantenible: **`bilingual_terms` es DERIVADO** y se regenera entero desde la tabla, nadie lo edita a mano — sin eso, en seis meses nadie sabe de dónde salió un término y «reclasificar» vuelve a ser «reindexar». La cosecha sale de RES.2 (cada rescate deja el par ya validado por haber funcionado) y de `hub_interactions`, cuyas columnas de feedback y revisión **ya existen**. Por qué no repite el fracaso de los lotes en Excel: allí se pedía *inventar* pares en frío; aquí se pide *aprobar o rechazar*, el mismo giro que hizo funcionar la curación. **RES.4 — medición** (Sonnet): la anchura y los umbrales, ahora que `top_k` sólo significa una cosa; la doble vuelta del juez se declara **no opcional** porque 6 de 25 veredictos se dieron la vuelta al invertir el orden; y regenerar la hoja de validación de Gerencia, que el usuario **decidió esperar a enviar** porque hoy 5 de sus 7 preguntas muestran «no contesta» en tres de cuatro variantes y eso no se puede validar. Sección «Lo que este bloque NO hace»: no baja ningún umbral, no arregla el pipeline agéntico (`score = 1.0` fijo), no cablea la segunda pasada por lengua y no reindexa nada. Cursor a **RES.1**. |
| 2026-08-25 | Diagnóstico: por qué no contesta, y por qué no es la anchura | **El usuario rechazó el resultado anterior con el criterio correcto —«no es aceptable un sistema que no contesta a casi la mitad»— y afirmó que todas sus preguntas tienen respuesta en el corpus. Comprobado: tenía razón, y `top_k` es el mando equivocado.** Dos defectos independientes. **(1) La puerta promedia todas las puntuaciones.** Rejilla anchura × política, calculada sobre puntuaciones ya medidas (coste cero): puntuar sobre **el mejor fragmento** da **20 de 25 en Normativa y 4 de 7 en Gerencia con cualquier anchura** —la columna es *plana* en 2, 3, 5 y 8—, frente a 18 y 2 hoy. Casos que lo retratan: SGE-01 tiene un mejor fragmento de 0,637 y se rechaza porque la media es 0,371; REAL-07, 0,626 contra media 0,327. **`top_k=2` NO es la salida**: contesta lo mismo pero con **6 de 25 y 5 de 7 respuestas de fuente única**, o sea el defecto que RAG.15 acababa de corregir. **Idea propia probada y descartada**: un *suelo relativo* (descartar lo que puntúe bajo el 60-75% del mejor) da 19 en vez de 20 y reintroduce fuentes únicas, porque recorta el contexto. **(2) No hay normalización de la consulta en la primera pregunta**, y no es un flag apagado: `query_rewriting_enabled` está en `t` en los tres chatbots, pero `necesita_reescritura` exige **dos turnos previos** —se diseñó para resolver referencias de seguimiento—, así que `rewritten_query` salió `None` en las 14 ejecuciones medidas. Una pregunta redactada como intención («quiero tramitar una compra de 6.500 euros») se busca tal cual contra un corpus redactado como norma («expedients de contractes menors») y no comparten ni una palabra. **Sonda que lo demuestra**: reformulada a mano al vocabulario de la norma, REAL-04 pasa de 0,126 a **0,640** y REAL-05 de 0,172 a **0,643** —por cinco—; 4 de las 5 mudas pasarían. La quinta (portátil / alta gama) no llega al umbral pero **recupera el documento exacto**, la «CIRCULAR DE GERÈNCIA SOBRE COMPRA D'EQUIPAMENT TECNOLÒGIC DE CONSUM D'ALTA GAMA». Verificado además por SQL que los documentos están en el corpus: no falta nada. **Aviso honesto anotado en el informe**: esas reformulaciones las escribió el agente conociendo el corpus, así que los 0,64 son un techo, no una promesa. **Tres hallazgos de arquitectura que abaratan el plan**: `LanguagePolicy` **ya declara `needs_secondary_search(...)`, con tests propios, y `core_graph` no lo llama nunca** —la segunda búsqueda condicional está especificada y sin cablear, así que el escalón «si falla, reformula y reintenta» es completar un mecanismo previsto—; `get_rewrite_model` ya existe con tope de 100 tokens para devolver una línea; y **`hub_document_chunks.bilingual_terms` es el hueco de la expansión léxica, ya cableado**, porque `tsv` es una columna generada sobre `content \|\| ' ' \|\| bilingual_terms` — escribir ahí es un `UPDATE` y el índice se regenera solo, sin re-embeber. **Orden propuesto**: puerta → escalón de reformulación (se paga sólo cuando falla: 28% de las consultas de Normativa, 71% de las de Gerencia) → expansión léxica alimentada por `feedback_score`/`review_verdict`, que ya son columnas de `hub_interactions` → y *entonces* medir la anchura. Por qué la expansión no repetiría el fracaso de los lotes en Excel: allí se pedía **inventar** pares en frío; aquí los candidatos salen de preguntas que fallaron de verdad y a la persona sólo se le pide aprobar o rechazar — el mismo giro que hizo funcionar la curación. **Entregables autónomos** (petición explícita: HTML enviable por correo, sin artifacts): `docs/DIAGNOSTICO_POR_QUE_NO_CONTESTA.html` y `docs/VALIDACION_GERENCIA_AUTONOMA.html`, ambos con **cero recursos remotos** (verificado por regex sobre `link/script/img/iframe/source`), tipografías del sistema, veredictos en `localStorage` y descarga por `Blob` con `<a download>`; probado en navegador el ciclo marcar → recargar → persistir → descargar JSON válido. |
| 2026-08-25 | Calidad de la respuesta con `top_k` 5 y 8, y la batería de Gerencia | **La pregunta era si el modelo redacta mejor con menos documentos, y la respuesta es no.** 50 llamadas al modelo más 50 de un juez (`gemini-2.5-pro`, **distinto** del `gemini-2.5-flash` que redacta, porque un modelo que se juzga a sí mismo se prefiere), a ciegas y con **doble vuelta intercambiando el orden**. Cuando las dos configuraciones contestan: 4-3 con **6 veredictos que se dan la vuelta al invertir el orden** — ruido. Toda la ventaja del 5 es que responde **18 de 25 frente a 15**, y el dato que lo cierra es que **no hay ni un caso en que el 8 responda y el 5 se calle**: los rechazos están anidados, así que ampliar no rescata nada. Donde sí hay señal es en el motivo: el 8 gana cuando completa (distinguir grado/máster/enseñanzas propias) y pierde cuando **contamina con normas de otro curso**; cuatro contra tres empata pero el coste no, porque el lote cataloga `curso_caducado` en **15 de 25** escenarios y lo llama «el defecto más repetido del ensayo» frente a 4 de `respuesta_incompleta`. Se elige 5 por riesgo, no por calidad media. **Dos validaciones de método**: el barrido de recuperación predijo 18 y 15 y el grafo completo dio exactamente 18 y 15 —reescritura incluida—, así que para decidir sobre el filtro el barrido barato sustituye a las llamadas; y juzgar una sola vez habría reportado un ganador inexistente. **El hallazgo grave está en Gerencia**: sus 6 preguntas reales son del 15-16 de agosto, **anteriores a RAG.15**, cuando la anchura efectiva era 1 y el filtro promediaba un solo fragmento (0,427 sobre umbral 0,35) — contestó las seis. Con la anchura arreglada la media baja a 0,282 y **se rinde en 5 de 7, con anchura 5 y con 8 igual**: una corrección introdujo una regresión en otro chatbot, y nadie lo vio porque nadie volvió a preguntarle. Misma raíz que el acoplamiento del informe anterior. `top_k=5` aplicado en los dos chatbots por decisión del usuario; **el umbral no se toca** —bajarlo a 0,30 lleva de 2 a 4 de 7, pero cambia qué evidencia basta para afirmar algo—. Dato para leer la batería: el agéntico responde 7 de 7 porque `md_agent_selector_pipeline` asigna `score=1.0` fijo y **su filtro no puede rechazar nada**; no es que recupere mejor, es uno con filtro contra otro sin filtro. Entregado a Gerencia `docs/VALIDACION_GERENCIA.html`: 7 preguntas × 4 configuraciones **a ciegas y con el orden permutado**, publicada con las capacidades `artifact` (guarda versiones de sí misma, así que los veredictos vuelven) y `downloads` (respaldo si entran en sólo lectura); probado el ida y vuelta del guardado. **Sin juez, a propósito**: en Gerencia no hay respuestas validadas, y de lo que marquen saldrán los tests. Informes en `docs/CALIDAD_RESPUESTA_TOP_K.html` y `docs/GERENCIA_TOP_K_Y_UMBRAL.html`. |
| 2026-08-25 | Medición de `retrieval_top_k` (cierra lo que RAG.15 dejó abierto) | **La medición que frenaba el prompt costaba 100 llamadas al modelo, y resultó que no hacían falta ninguna.** `top_k` decide qué entra en el contexto, o sea que su efecto se agota en la capa de recuperación; el barrido nuevo (`_local/golden/barrido_topk.py`, en `_local/` porque opera contra el corpus y las credenciales reales) aplica cada valor con `dataclasses.replace` sobre la configuración efectiva —sin tocar la base, sin escribir `HubTestRun`— y sólo paga los embeddings de las 25 consultas. **Recomendado 5**, sobre corpus real (`Normativa UJI`, 23.306 fragmentos): es el valor más bajo que deja en cero las respuestas de fuente única —con 3 quedan 3 de 25, que es justo el defecto que RAG.15 acababa de corregir— y el que menos respuestas pierde. Pasado ahí no aparece nada nuevo: **la puntuación del mejor documento es 0,640 en las cuatro configuraciones**, y en el lote dorado recall@5, recall@10 y MRR son **planos** (0,96 / 0,96 / 0,94 de k=3 a k=20); la precisión cae porque hay un relevante por consulta, que es aritmética y no hallazgo. **El hallazgo de verdad fue otro y no estaba previsto**: el `quality_gate` compara `quality_threshold` contra la **media** de las puntuaciones (`core_graph.merge_node:173`), así que ensanchar la búsqueda **baja mecánicamente la nota que decide si se responde** — con el `8` de hoy **10 de 25 consultas caen a la respuesta de cortesía**, con 5 caen 7. La nota de RAG.15 decía «los dos números se mueven juntos» pensando en recalibrar; es peor que eso, **un mando mueve al otro por construcción**. Salida propuesta —puntuar sobre el mejor fragmento o la media de los tres primeros— **no hecha, y a propósito**: es un cambio de diseño del gate y merece su propio prompt, no colarse en una medición. Segundo detalle que la medición aclara: `min_retrieval_score` (0,3) actúa sobre la similitud coseno en el SQL, **no** sobre la puntuación del reranker que el gate promedia — son dos escalas y el umbral bajo no acota la de arriba. **El valor sigue en 8 en la base**: es configuración de un chatbot del usuario. Informe en `docs/MEDICION_RETRIEVAL_TOP_K.html`, verificado en navegador en los dos temas. |
| 2026-08-25 | **Bloque AIS completo** (AIS.1–AIS.8) — Aislamiento del núcleo y saneamiento previo al piloto | **Cursor: SEC.9.7 → AIS.1 → RAG.15.** Sale del §5 de la auditoría del 2026-08-24, y **la lección del bloque es que la auditoría se equivocó en la dirección menos esperada: varias cosas ya estaban hechas y las que quedaban eran peores**. En AIS.1 decía que la cascada de temas no alcanza el cromo del panel; era cierto **hasta REV.9**, un día antes de auditar —`aplicarColoresDelPanel` escribe los `--*` de shadcn como estilo **en línea**, que gana a cualquier hoja incluido `.dark`, y se comprobó en el navegador leyendo el atributo `style` de `<html>`—. Lo que sí quedaba era peor que una etiqueta: el bloque `.dark` estaba rotulado `UJI brand` con turquesa (hue 211) y azul marino (244) mientras UX.5 había pasado el claro al azul del proyecto (hue ~255), así que **cambiar de modo no oscurecía la identidad, la sustituía por otra**. Ahora la marca se declara una vez y el oscuro deriva con `color-mix`; **medido en navegador píxel a píxel**, los contrastes pasan AA en los dos modos (claro 7.84/6.88/6.81/5.15; oscuro 6.71/12.84/9.95/10.89), y por el camino se descartaron dos falsos negativos míos —el canvas no resuelve `oklch` y devolvía un `#eb0000` imposible—. En AIS.2 el barrido dio **tres** sitios en código, no la docena de la lista: el resto eran **comentarios de procedencia**, que son documentación verificable, y el test lo distingue a propósito para no obligar a borrar la explicación de un dato real. Lo sustantivo de ese prompt fue otro: los dos prompts de redacción de SEG.2 eran los únicos del módulo que no se podían afinar sin desplegar —`RedactorDeBloques` resolvía la instrucción con un `dict` del módulo, así que el mecanismo de MT.6 existía y a ellos no llegaba—. AIS.3 puso la regla que faltaba: `CONTRIBUTING.md` prohíbe que un módulo importe de otro y **la dirección inversa no estaba regulada ni testada**; `core/` importaba de `routers/` en dos sitios, o sea la autenticación dependiendo de la capa HTTP de un módulo. El test tiene **tres severidades distintas a propósito** —`core→routers` prohibido y en cero, `core→modules` congelado en una lista que sólo encoge porque lo que queda son modelos ORM, y módulo→módulo con `agents_hub` **declarado como la capa base que de facto es**, que es más honesto que repetir la regla mientras 24 imports dicen lo contrario—. `modules/automation` **no se borra, se deja probado**: 1.401 líneas sin importador vivo, y su retirada va al Bloque NIC, cuyo primer prompt no borra nada sino que cruza qué está cubierto. AIS.4 destapó que **el panel de anonimización no ha funcionado nunca desde el navegador** —módulo de API a mano sin credencial, 401 los tres endpoints—, y lo que importa es por qué siguió invisible: el cliente correcto ya estaba generado, el test del panel mockeaba el módulo manual, y el guardarraíl de CAL.2 buscaba ficheros que *mencionaran* la cabecera, así que éste pasaba **por no construirla**. Ahora el guardarraíl **descubre** en vez de enumerar, igual que hizo SEC.9.5 con los routers. **Dos errores propios que cazaron tests existentes, y los dos del mismo tipo — un valor por omisión convertido en obligación**: en AIS.5, `MODO_POR_DEFECTO` como suelo habría hecho la anonimización obligatoria con otro nombre (nadie podría elegir `off` en una instalación nueva), así que suelo lo es sólo la política fijada a propósito; y en AIS.8, `basicConfig(force=True)` **elimina** los manejadores existentes —incluido el de pytest— y puso en rojo el test que afirma que un arranque fallido deja su causa en el log, que es justo lo que el prompt venía a mejorar. Un mecanismo de observabilidad que apaga a otro no es observabilidad. AIS.6 cierra el §13 de la AGPL con sus tres condiciones, **incluido el widget público** —«el caso que se olvida» según el propio README—, y con un test que prohíbe URLs escritas a mano porque una fija al principal haría incumplir a todo fork. AIS.7 convierte la gobernanza en control: la primera pregunta de la plantilla de PR es «¿por qué esto es generalizable?», que hasta ahora dependía de que el contribuyente hubiera leído el manual. **Suite backend: 3107 passed, 1 skipped, 0 fallos (25:03); frontend 653 passed sobre 250 ficheros; `tsc` limpio; contrato 149 rutas; migración `v2o3p4q5r6s7` aplicada con `downgrade` comprobado.** 60 tests nuevos |
| 2026-08-24 | Planificación — RAG.15, VIS.4, VIS.5 y D.6.1 salidos de medir el asistente | **Cuatro prompts nuevos, ninguno inventado: los cuatro salen de ejecutar el lote ujirag contra `Normativa UJI`.** **RAG.15 — el asistente contesta con un solo fragmento.** `retrieval_top_k` **no lo lee ningún pipeline**: `rag_vector_pipeline.py:69` construye la estrategia con `top_k=cfg.min_retrieval_results`, que valía 1. Medido: 25 de 25 respuestas con una sola fuente sobre un corpus de 23.306 fragmentos. Y el campo es editable desde el panel (`config_models.py:386`, `hub_chatbots_router.py:66,126,177,324`), así que un admin puede ajustar un número que no hace nada. Efecto de segundo orden que confundió el diagnóstico del umbral: como `top_k == min_retrieval_results`, la comprobación `len(items) >= min_retrieval_results` sólo se cumple cuando se recupera exactamente el máximo, y si falta uno el score se multiplica por 0,5 — subir el mínimo no endurecía un mínimo, ampliaba la recuperación y a la vez hacía más probable el castigo. **Va delante de VIS.4/VIS.5** porque cambia la anchura del contexto y con ella los scores y la calibración del umbral. El prompt avisa de no dar por bueno el `8` por defecto: se escribió cuando nadie lo leía. **VIS.4 — primero vigente, después lengua** (decisión del usuario): `PreferLanguagePolicy` no distingue «la única versión está en otra lengua» de «la única versión es de otro curso», y en el corpus real las directrices de 2026/2027 sólo existen en castellano y las de 2025/2026 sólo en valencià, las cuatro marcadas vigentes y validadas. **VIS.5 — el aviso de traducción avisa de lo que no es**: `_build_translation_warning` (`hub_chat.py:168`) informa del idioma de la PREGUNTA en vez de del de la FUENTE, se calla si la pregunta va en castellano —el caso más frecuente, porque 195 de 290 normas sólo existen en valencià— y está redactado en castellano sin acentos para quien acaba de escribir en valencià. Los dos VIS **reabren un bloque cerrado el 2026-07-31**, que es lo correcto: continúan la canónica bilingüe de VIS.3. **D.6.1 — publicar el corpus navegable y llevar el widget hasta él** (decisión del usuario): el buscador y los HTML por norma se sirven desde la misma VM con los enlaces al bucket, y el widget se incrusta en el buscador y en la plantilla de cada norma para que distintas personas prueben sobre la norma que están leyendo. No estaba anotado en ningún prompt de Deploy —revisados D.0 a D.7, sólo D.6-VM menciona el widget y sólo para documentar su incrustación—. Sin esto `CORPUS_SITE_BASE_URL` sigue en `127.0.0.1`: medido, 16 de 25 respuestas llevaban enlaces a localhost. **Orden vigente: AIS → RAG.15 → VIS.4 → VIS.5 → Deploy/D.0.** **Configuración de los dos chatbots de Gerencia, aplicada el mismo día** (no es código, no entra en este commit): al `economicoadministratiu` (RAG) reranker, reescritura y umbral 0,35 —más permisivo que el 0,50 de Normativa, porque sus usuarios son funcionarios y es preferible mostrarles una norma para que la valoren que no mostrarles ninguna—; al `agèntic` sólo reescritura y mensaje propio, porque **su pipeline no lee `reranker_enabled`** y **asigna `score=1.0` fijo** (`md_agent_selector_pipeline.py:82`), de modo que su quality gate no puede rechazar nada con ningún umbral. La comparación RAG contra agéntico es asimétrica en dos ejes, no en uno. |
| 2026-08-24 | Contrato de citas — el guion bajo que tiraba respuestas correctas | **Encontrado midiendo, no leyendo.** Al ejecutar el lote ujirag contra `Normativa UJI` con el reranker ya activo, **nueve de veinticinco respuestas se descartaban por citas y eran las de mejor recuperación de la tanda** (la primera con calidad 0,909). La hipótesis inicial —que el modelo componía un ancla de subapartado distinta de la del fragmento— resultó **minoritaria**: se capturó la respuesta ANTES de la sustitución, con las dos URLs delante, y **seis de ocho arrastraban un guion bajo de más**. El bloque de fuentes escribía la dirección en cursiva markdown (`_URL: {url}_`) y el guion bajo de cierre, pegado al final, se colaba en la cita: `...#art-1_`. La comparación exacta fallaba y se descartaba la respuesta ENTERA. Y **de forma intermitente**, porque depende de si el modelo arrastra el carácter en esa generación: la misma consulta pasaba en una tanda y fallaba en la siguiente — el peor modo de fallo posible, el que no se reproduce cuando alguien va a mirarlo. **Tres cambios**: (1) la URL sale de la cursiva y va sola en su línea; (2) `enforce_citation_contract` **degrada** al documento una cita cuyo ancla no reconoce, en vez de tirar la respuesta —la URL de la norma pasa a ser citable por derecho propio, que es el «el artículo si se puede, la norma si no» que pidió el usuario— y se añade al prompt la regla de nombrar el artículo en el TEXTO del enlace; (3) el `no_answer_message` del chatbot se respeta también en esta rama, que devolvía un texto fijo en castellano a preguntas hechas en valenciano mientras el `fallback_node` sí lo respetaba. Lo que el contrato sigue atrapando intacto: citar un documento que nunca se recuperó, y no citar nada. **Defecto introducido y corregido dentro del mismo trabajo**: `incumplio_citas` se calculaba como `validated != answer`, equivalencia cierta mientras el contrato solo sustituía y falsa en cuanto también ajusta; contaba las respuestas degradadas como fallback y —peor— **se saltaba el aviso de vigencia**, que solo se añade cuando el contrato no sustituyó. Se compara ahora contra el mensaje de rendición. **Medido sobre las 25**: de 12 respuestas a **20**, descartes por citas de 9 a **0**, ocho recuperadas y ninguna caída nueva; los cinco rechazos restantes son del quality gate y cuatro de ellos son huecos del corpus. 146 tests verdes. |
| 2026-08-24 | RAG.6b — Adaptador del Ranking API de Vertex (adelantado) | **Adelantado al bloque Deploy a petición del usuario**: el plan lo situaba tras D.0 sólo porque D.0 habilitaba Discovery Engine, y `discoveryengine.googleapis.com` **ya estaba habilitado** en `uji-teclab`. Se necesitaba para medir el asistente normativo en local contra un lote de 25 consultas reales de informadores de la UJI. **Los dos riesgos abiertos del prompt, cerrados contra el servicio real antes de escribir el adaptador**: (1) **el valenciano funciona** — el plan avisaba de que Google declara 25 idiomas sin publicar cuáles y el catalán no figura; medido, el documento correcto puntúa 0,7944 y el señuelo (el Reglament del Consell de l'Estudiantat, justo donde el asistente se equivocaba) 0,0504; ante una ambigüedad léxica real el margen se estrecha a 0,8508 frente a 0,8242. (2) **Los scores ya vienen en [0,1], no son logits**: `normalize_score` es correcta para `LocalReranker` y destructiva aquí — convertiría un 0,01 en 0,502 y colaría candidatos irrelevantes con aspecto de medianos en el packer de RAG.5 y en el quality gate. Hay test dedicado a impedir que alguien «unifique» las dos normalizaciones. **Decisión de implementación: sin dependencia nueva.** REST con `httpx.AsyncClient` + token ADC de `google.auth` en vez de `google-cloud-discoveryengine`, porque el cliente de Google es síncrono y envolverlo en un hilo para cumplir la asincronía total de AGENTS.md añadía ~100 MB para acabar haciendo un POST; además evitaba tocar `uv.lock` con otra sesión trabajando en paralelo. `provider_type='discovery_engine'`, `location=global` y `default_ranking_config` (el Ranking API no tiene réplica regional: pedirle una región da 404), modelo `semantic-ranker-default-004` (las variantes -003 y -002 son de 512 tokens y truncarían medio chunk). **17 tests verdes** (7 nuevos + los 10 de RAG.6a sin regresión). **Lo que destapó al activarlo**, y que motiva trabajo aparte: el `quality_score` valía **0,700 constante en las 25 consultas** con umbral 0,6, o sea que el quality gate no había rechazado nunca nada; sólo discrimina cuando el reranker sustituye el score de fusión. |
| 2026-08-24 | **Bloque SEC.9 completo** (SEC.9.1–SEC.9.7) — Endurecimiento pre-deploy, tercera auditoría | **Cursor: MT.7 → SEC.9.1 → AIS.1.** Nace de reescribir `docs/VALORACION_PROYECTO.md` como auditoría previa al piloto. **El resultado que ordena el bloque**: los diez hallazgos de julio están resueltos y con guardarraíles que impiden la recaída, y lo que aparece es **nuevo** — regresiones de los bloques REV y MT—, con la particularidad de que **tres de las cuatro auditorías paralelas encontraron los mismos tres por separado**, que es la señal que convirtió «revisar» en «bloquear el despliegue». **Y todos estaban donde el gate no miraba**, lo que hace de SEC.9.5 el prompt que de verdad cierra la clase de fallo: el paso «Access control gate» de `ci.yml` ejecutaba una lista de cuatro ficheros sin ninguno de los routers de REV ni MT; el guardarraíl de `test_tenant_isolation.py` recorría una tupla de nueve nombres con un `continue` que **saltaba en silencio** los que no encontraba —y había uno mal escrito desde SEC.2, `hub_feedback_router.py`, que vive en `api/v1/`—; y el inventario de módulos de PLAT.5 se validaba sobre **docstrings**, así que `library_router` decía «Módulo: plataforma» y pasaba en verde estando completamente abierto. Ahora el inventario **se recorre** (`rglob`), con el patrón de `tablas_sin_ambito()` de MT.1, y la diferencia de fondo es que olvidarse de añadir a una lista de **exenciones** falla del lado seguro y olvidarse de añadir a una de **vigilados** no. Lo cerrado, por gravedad: `library_router` sin **ninguna** dependencia de identidad, con la tenencia decidida por cabeceras que pone el llamante, escritura anónima que **el servidor firmaba** con la clave de la plataforma, y un oráculo de firma RSA abierto; la `api_key` del proveedor devuelta en claro por `GET /providers` a cualquier administrador de cualquier organización; MT.2 y MT.6 con el **lector** correcto y el **escritor** sin dimensión de organización —el de prompts de actividad permitía sobrescribir el de plataforma que heredan todas, o sea inyección de prompt en las llamadas de las demás—; y cinco controles que estaban **declarados y no llegaban a ejecutarse** (una cuota por IP cuyo parámetro nadie rellenaba, un cubo de peticiones compartido por todos los visitantes anónimos de un widget, un gate de producción que se saltaba con `TESTING=1`, un `workspace_id` que el endpoint ignoraba, y secretos con valor por omisión en el compose de producción). **Dos decisiones del usuario quedan escritas con su alternativa descartada**: la credencial de proveedor se resuelve **restringiendo el método** y no cifrando —cifrar mueve el secreto al entorno del mismo proceso que lo descifra y añade gestión de clave para siempre—, con el autoservicio del panel recuperándose en MT.10 vía `SecretProvider`; y la anonimización **es configurable y no obligatoria**, así que no hay bóveda cifrada antes del piloto (y al planificarlo resultó que el modo **ya era política**: cuatro valores y columna por informe, de modo que AIS.5 pasa a ser heredar el modo desde la organización y acotar la promesa de `REPLACE`). **Tres lecciones de método**, que es lo que este bloque deja para el siguiente. (1) Lanzar la pasada amplia de cierre en segundo plano **mientras se edita el prompt siguiente** dio 7 rojos que no existían: leyó los ficheros a medio cambiar. No se solapa una suite larga con ediciones. (2) `git add -A` barrió tres ficheros de trabajo ajeno y `uv.lock` dentro de un commit; se arregló con `reset --soft` y stage selectivo, y la regla es nombrar los ficheros. (3) **La suite completa encontró lo que ningún test por fichero podía ver**: la restricción de `create_organizacion` a superadministrador contradecía una decisión deliberada de PLAT.5, y al investigarlo apareció algo mejor que las dos posturas — `partner_id` **sí** tiene efecto de autorización (`_orgs_del_admin` resuelve el claim con `WHERE partner_id == <su partner>`, así que aceptarlo del cuerpo dejaba plantar una organización en el ámbito de otro administrador) **y no hacía falta inventar ningún claim**, porque el partner de un administrador *es* su `user_id` tal y como lo emite `login_admin`. MT.16 se **adelanta** de la fase 2 de MT porque el piloto arranca con una sola organización y sin él el aislamiento no se ejercita en producción; montarlo enseñó que `TestClient` es síncrono y su portal de anyio choca con el bucle de la sesión async (`got Future attached to a different loop`), de ahí `httpx.ASGITransport`. El gate pasa de 4 ficheros y ~40 tests a **9 ficheros y 133 tests que bloquean el despliegue**. Una migración (`u1n2o3p4q5r6`) que **no borra a ciegas**: cuenta las filas con el método retirado y se detiene con instrucciones, porque no guarda copia del valor en ningún sitio. **Suite backend completa: 3056 passed, 1 skipped, 0 fallos** (24:34, `-n0` desde Git Bash); **frontend: 626 passed**; `tsc` limpio; contrato regenerado (148 rutas). 62 tests nuevos |
| 2026-08-24 | MT.7 — El inventario del ámbito, escrito donde se lee · **cierra la fase 1 del Bloque MT** | **Cursor: MT.7 → Deploy/D.0.** La auditoría que originó este bloque consistió en reconstruir a mano, leyendo 31 tablas y 32 routers, qué está acotado por organización y qué no. `docs/MULTITENENCIA.md` es el resultado de esa lectura: tabla por tabla, ámbito, camino hasta la organización y qué es de plataforma **a propósito** —con la distinción entre las dos capas que se confunden, `core/auth/tenancy.py` (quién puede ver qué) y `core/ambito.py` (qué fila gana)—. Enlazado desde `AGENTS.md`, junto a la frontera edge/cloud, porque un documento que no se encuentra no sirve. **Lo que lo hace distinto de documentación normal es el test**: si una tabla de `HubConfigBase` falta, o si la fila del inventario no dice el ámbito que declara el código, se pone rojo — así se puede leer como verdad y no como una foto vieja. **El test se estrenó dando un falso rojo suyo propio**: buscaba la primera línea que mencionara la tabla, y varias se citan antes en la prosa que las explica; ahora mira la fila del inventario. Y de paso destapó un fallo real: `hub_workspaces.organizacion_id` estaba en la migración de MT.4 **y no en el modelo**, o sea columna en la base que el ORM no conocía. 4 tests nuevos |
| 2026-08-24 | MT.6 (RED/GREEN) — Un municipio puede escribir su propio prompt | **Cursor: MT.6 → MT.7.** `HubActivityPrompt.activity` era único **global**: defendible para una actividad de plataforma, y deja de serlo en cuanto un municipio quiere su propia redacción — que es exactamente lo que esta tabla existe para permitir, sólo un escalón más arriba de lo que hacía falta. **Los dos campos se heredan por separado**, y no era un detalle: un municipio puede querer el texto de la plataforma con un modelo más caro, y obligarle a copiar el texto para cambiar el nivel lo congelaría igual que copiarlo del código —a partir de ahí, mejorar el de plataforma no le llegaría—, así que se resuelve campo a campo con la cascada de MT.1 y no eligiendo una fila entera. La cadena entera son tres niveles y el tercero es el que PRO.2.1 dejó atado: **el texto del código no se copia a ninguna fila**. La unicidad pasa a `(activity, organizacion_id)` con `NULLS NOT DISTINCT`, que es la misma trampa que el docstring del modelo advertía sobre hacer `chatbot_id` nullable —dos overrides del mismo ámbito conviviendo—, evitada con la cláusula en vez de con otra tabla. El `upgrade` **busca en el catálogo** el nombre de la unicidad vieja en vez de escribirlo: SQLAlchemy lo generó con `unique=True`, y un nombre adivinado que no exista deja la restricción puesta y la segunda organización sigue chocando. **Y una corrección de método sobre la marcha**: la primera versión envolvía la llamada al proveedor en un `try/except TypeError` para tolerar dobles antiguos, lo que se habría tragado un `TypeError` real de dentro de la corrutina; había sólo dos dobles, así que se actualizaron. Migración `t0m1n2o3p4q5` aplicada, `downgrade` comprobado. 8 tests nuevos; `tests/modules/agents_hub` + `tests/api`: 1409 passed |
| 2026-08-24 | MT.5 (RED/GREEN) — El permiso dice dónde | **Cursor: MT.5 → MT.6.** Una concesión decía «informes», no «informes en el ayuntamiento de X». Con una organización da igual; con veinte, conceder un módulo a un grupo del IdP se lo concede **en todas**, o sea acceso a los informes de veinte ayuntamientos a la vez. **La mitad del prompt es la regla de compatibilidad: nulo sigue significando «en todas»**, que es lo que significan las concesiones de hoy — reinterpretarlas en silencio como «en ninguna» le quitaría los permisos a todo el mundo en una migración, y como «en la primera» sería inventárselo. El eje es **perpendicular** al de `subject_type` de IDE.5, que no se toca: aquél dice a quién y éste dónde. La unicidad pasa de tres columnas a cuatro con `NULLS NOT DISTINCT` —la misma persona con el mismo módulo en dos organizaciones son dos concesiones legítimas, pero dos «en todas» idénticas siguen siendo un duplicado, y en Postgres `NULL != NULL` dejaría pasar justo el nivel donde están todas las filas de hoy—. En el token, `organizacion_id` **acota y nunca amplía**: SEC.2 decidió que un PAT hereda las organizaciones de su dueño y las resuelve en cada validación —guardarlas las congelaría, y a un administrador al que se le retira una organización le seguiría valiendo un token viejo, que es una revocación que no revoca—, así que MT.5 intersecta en vez de sustituir. El caso a no equivocar es el superadministrador: su lista vacía significa «todas», así que intersecarla como conjunto vacío dejaría el token sin acceso a nada; acotado a una organización vale exactamente en ésa. El `downgrade` **borra las concesiones acotadas** y lo dice: conservarlas exigiría elegir cuál sobrevive, y eso un `downgrade` no lo puede decidir en silencio. Migración `s9l0m1n2o3p4` aplicada. 11 tests nuevos; el origen de los módulos gana un campo y los dos tests de IDE.5 que fijaban su forma se actualizaron |
| 2026-08-24 | MT.4 (RED/GREEN) — Informes gana la dimensión que no tiene, y la escalera completa de elevación | **Cursor: MT.4 → MT.5.** `owner_kind` admitía `user`, `platform` y `superadmin`: **no existía `organizacion`**. Una plantilla era de una persona o de todo el mundo, sin nada en medio, y eso no es un filtro que falte sino una dimensión que no está. La escalera que pidió el usuario es usuario → organización → plataforma, y los dos extremos ya estaban: 20 plantillas de plataforma, 2 personales de un superadministrador y 1 de un usuario. **La mitad del trabajo fue no romper lo que ya funcionaba**: el nivel de plataforma tiene que seguir viéndose desde todas las organizaciones —son 20 de 23 filas y es lo que permite que la Diputación comparta una plantilla—, y hay un test para eso. **Tres cosas más allá del esquema.** (1) `CheckConstraint` de coherencia en la base y no sólo en el modelo: una plantilla «de organización» que no dice de cuál no la encuentra ninguna consulta **ni la excluye ninguna**, así que se cuela en los dos lados. (2) **`is_global` deja de ser columna**: se calculaba en el router como `owner_kind == "platform"` **y** se guardaba, o sea el mismo hecho en dos sitios, que terminan discrepando; pasa a `hybrid_property` —que sirve igual en Python y en SQL, lo que hacía falta porque el listado filtra por él— y el `upgrade` **comprueba antes de retirar la columna** que los datos coincidan, para parar en vez de perder la distinción. De paso salió una combinación que la redundancia permitía: `approve-as-template` guardaba `owner_kind=user.role` **y** `is_global=True`, o sea dos niveles distintos en la misma fila. (3) **`POST /templates/{id}/fork`**: sin él, el arreglo del 2026-08-24 —plantillas de plataforma sólo a mano del superadministrador— dejaba a un municipio sin forma de adaptar la de su Diputación, y «heredado» pasaba a significar «mírala y no la toques». Bifurcar **copia y no mueve**, se lleva la versión vigente —una bifurcación sin contenido es una plantilla vacía con el nombre de otra, y el fallo aparecería en el piloto— y anota la procedencia. **Desviación documentada: la procedencia (MT.4.2 en el plan) se adelanta aquí**, porque la bifurcación es su primera consumidora y meter las columnas en un prompt anterior habría sido dejarlas sin nadie que las lea, que es el código especulativo que las reglas prohíben. `derivado_de` va con **`ON DELETE SET NULL` y no CASCADE**: con CASCADE, retirar la plantilla de la Diputación borraría las de los municipios que la adaptaron — la procedencia es una anotación histórica, no una dependencia. Migración `r8k9l0m1n2o3` aplicada y `downgrade` comprobado, con los datos intactos tras el ciclo (20+2+1 plantillas, 29 informes). 13 tests nuevos; `tests/modules/redaccion` + `tests/api` + integración: 1446 passed |
| 2026-08-24 | MT.3 (RED/GREEN) — Quien pide un modelo dice para quién | **Cursor: MT.3 → MT.4.** MT.2 puso la cascada en la base y por sí sola no servía de nada: `get_model_for_tier` **no recibía la organización**, así que nadie le decía contra qué resolver y el nivel de organización no se habría consultado nunca. **El parámetro es obligatorio y sin valor por omisión, y ésa es la decisión del prompt**: un opcional se olvida y el fallo es silencioso —resuelve el modelo de la plataforma cuando debía resolver el del municipio, la respuesta llega igual de bien escrita y el consumo se carga al contrato equivocado—, así que no lo caza ningún test; obligatorio, el sitio que no sabe la respuesta **tiene que escribir que no la sabe**, y eso se ve en una revisión. Los siete llamadores dicen de dónde la sacan: el rastreo **del sitio** (`HubWebSite.organizacion_id`, el único que no esperaba a nadie), la ingesta y los tres de Informes **de quien pide** vía `organizacion_unica_de` —sólo cuando pertenece a una sola, mismo criterio que REV.12 aplicó a la marca—, y los dos que corren en tarea de fondo con `None` **escrito**, porque `hub_workspaces` no tenía organización hasta MT.4. El test del `grep` a cero se reescribió con **`ast`**: el primero iba por líneas y las llamadas quedaron multilínea, así que daba por incumplido lo que sí cumplía —y, peor, habría dado por cumplido lo que no en cuanto alguien reformateara—; además comprueba que el argumento va **nombrado**, porque en una llamada de tres el tercero posicional no dice nada en un diff. **Dos lecciones del entorno.** Comprobar que los módulos importan **no basta**: el nombre se usa dentro de la función, así que el `NameError` por el import que faltaba sólo saltó al llamarla, con siete tests en rojo. Y las dependencias de FastAPI llamadas a mano desde un test necesitan que se les pase lo que les daría la inyección: sin `current_user`, la organización se resolvía sobre el objeto `Depends`. 9 tests nuevos |
| 2026-08-24 | Diseño de la reutilización de configuración entre organizaciones (planificación) + arreglo del agujero de las plantillas de plataforma | **Cursor: sin cambios.** El usuario preguntó si un superadministrador podría elevar un chatbot (sin corpus) o una plantilla (sin informes) a nivel de plataforma para que la consuman todas las organizaciones, o si conviene exportar/importar; y añadió que las plantillas las diseña un usuario **o** un administrador —de ahí una doble elevación— y que hay dos supuestos de reparto: para todas, o para un grupo (las que comparten una herramienta de gestión). Con automatizaciones y expedientes por venir, esto deja de ser una función y es un **mecanismo**. **Lo que ya existía y nadie había dicho**: las plantillas de informe **ya tienen nivel de plataforma** (`owner_kind='platform'` + `is_global`), así que la elevación no hay que inventarla — falta el escalón de en medio, que es lo que MT.4 añade, y la escalera completa es usuario → organización → plataforma. **La distinción que decide el diseño** no es «compartir o no» sino compartir **por referencia** frente a **copiar al instanciar**: sirve la primera cuando el objeto es sólo configuración (plantilla de informe, script, plantilla de procedimiento) y la segunda cuando acumula datos propios. Los chatbots son el caso que no hay que equivocar: elevar un chatbot no comparte su configuración, comparte **el chatbot** —un corpus y un registro de conversaciones para todos los municipios—, que es justo el «nunca datos» y contradice la decisión ya tomada de que el corpus no se comparte. **Los dos supuestos de reparto se resuelven con un solo eje**: la organización declara qué tiene (`capacidades`) y el artefacto qué necesita (`requiere`), porque «las que comparten G400» no es un club sino compatibilidad — y así el sistema deduce quién puede consumirlo en vez de que alguien mantenga la pertenencia a mano. **Exportar/importar no es la alternativa sino el otro caso**: dentro de una instalación gana el nivel de plataforma, porque conserva procedencia y propaga arreglos; **entre** instalaciones (una empresa con un despliegue `edge` por municipio y sin base común) es la única opción. De ahí la decisión que abarata el futuro: la unidad de reutilización es un **`spec` serializable y versionado**, no una fila a la que se apunta. **Del reparto ahora/después, aplicando el criterio de coste de migración de la fase 1, sólo una pieza entra antes del piloto**: la **procedencia** (`derivado_de` + versión de origen, prompt MT.4.2 nuevo), porque es la única irrecuperable — cuando haya copias repartidas sin constancia de su origen, «la Diputación corrigió el prompt, propágalo» es imposible para siempre. El catálogo, la instanciación, las pantallas y exportar/importar empiezan vacíos, así que esperar no cuesta nada (MT.17–MT.21). **Y un agujero de hoy, no de mañana, comprobado con tres tests antes de afirmarlo y arreglado en el momento**: `_exigir_admin` miraba **sólo el rol** y `_plantilla_o_404` traía la plantilla por id **sin comprobar de quién es**, así que un administrador de cualquier organización podía renombrar la plantilla de plataforma, **archivarla** —lo que la saca de la lista de todos y bloquea crear informes nuevos con ella— o renombrar la plantilla personal de otra persona. Con una sola organización no se nota. Cerrado con `_exigir_poder_sobre`: la de plataforma sólo el superadministrador, la de una persona su dueño, y un administrador sin relación con ella 403; el arreglo **completo** espera a MT.4, porque hasta que las plantillas tengan `organizacion_id` «administrador» sólo significa «administrador de algún sitio». Ningún test existente se rompió: los que había usaban `superadmin`. 3 tests nuevos |
| 2026-08-24 | MT.2 (RED/GREEN) — Proveedores y modelos dejan de ser globales | **Cursor: MT.2 → MT.3.** Una sola credencial para todos significa, en el modelo Diputación→municipios, que el consumo de un ayuntamiento se factura al contrato de otro y que sus prompts viajan por ese contrato. **Desviación documentada y razonada**: el plan decía «`organizacion_id` nullable en las dos tablas», y en `hub_providers` no se puede — su clave primaria es el texto `google`/`vertex`, así que una columna no permitiría dos Googles y permitirlo exigía cambiarle la clave primaria con una clave ajena por medio y romper `/providers/{provider_id}`. Al mirar las cuatro filas reales resulta que **no hace falta**: esa tabla es un catálogo de **tipos** y ninguna fila tiene `api_key`. Lo que se separa por organización es la credencial, y se va a `hub_provider_credentials`, heredable por la cascada de MT.1. **Los tres métodos son los que ya estaban en uso**, sólo que uno no estaba dicho en ninguna parte: clave literal, **nombre** de variable de entorno —el que sirve al despliegue en GCP, y el único que da credenciales por organización sin meter secretos en la base— y credenciales del entorno de ejecución (ADC de Vertex, que es lo que Vertex aconseja). Hasta ahora «sin clave» era indistinguible de «mal configurado». **El límite de ADC queda dicho en vez de disimulado**: un proceso tiene una sola identidad, así que ADC por organización sólo funciona en modo `edge` —un despliegue por municipio—, y declararlo en cloud se resuelve con el motivo dicho; la salida si algún día hace falta (suplantación de cuenta de servicio) queda anotada y no escrita. **Dos fallos preexistentes.** `_relevar_default` degradaba por `tier` **ignorando `purpose`**, y hoy conviven un chat nivel 1 y un embedding nivel 1 marcados por defecto: promover uno de chat desde la pantalla dejaba la plataforma **sin modelo de embeddings**, y eso no se nota hasta la siguiente ingesta. Y el doble de `test_chatbot_llm_config` era un `SimpleNamespace` sin la columna nueva, así que el endpoint devolvía 500 — lección con matiz sobre `project_external_client_mock_spec`: el `spec` de un `MagicMock` protege de nombrar un atributo que no existe, **no** de olvidarse de darle valor a uno que sí, y sin valor devuelve un `MagicMock` que revienta la validación de la respuesta. **Dos trampas de SQL que cazaron los tests**: `col IN (org, NULL)` nunca trae la fila de plataforma porque `NULL IN (...)` es nulo, y la restricción única necesita `NULLS NOT DISTINCT` para valer en el nivel de plataforma — el único que existe hoy—, porque en Postgres `NULL != NULL`. **Y una trampa del entorno**: la tabla nueva ya existía en la BD de desarrollo antes de la migración, creada por el `create_all` del arranque de `main.py`, que crea tablas nuevas pero **nunca altera las existentes** — de ahí un estado a medias (tabla sí, columna no) que hizo fallar el `upgrade`; se retiró la tabla vacía para que la cadena de Alembic sea la dueña. Migración `q7j8k9l0m1n2` aplicada y `downgrade` comprobado. 17 tests nuevos |
| 2026-08-23 | MT.1 (RED/GREEN) — Que el ámbito de una tabla sea una decisión y no un olvido | **Cursor: MT.1 → MT.2.** Arranca la fase 1 del Bloque MT, que el usuario puso **antes del piloto**. El prompt tiene dos mitades y la que importa es la segunda: `hub_llm_configs` no es global porque alguien lo decidiera, sino porque **no había dónde decir lo contrario** — y averiguarlo costó leer 31 tablas y 32 routers. Ahora las 13 tablas de `HubConfigBase` declaran su ámbito en `__ambito__` y la que no lo haga pone el test rojo. La otra mitad es la cascada «nulo = plataforma, se hereda» escrita una vez en `core/ambito.py`, antes de copiarla a cinco tablas en MT.2…MT.6; resuelve **campo a campo** —con reemplazo de fila entera, una organización que quiere cambiar un color tendría que repetir la configuración completa— y distingue `None` («heredar») de `""` («lo quiero vacío»), sin lo cual no se puede vaciar un valor heredado desde la pantalla. El test que importa es que **no funde entre organizaciones**: una resolución que ordene mal el `ORDER BY` devuelve la configuración del municipio de al lado sin que nada falle. **Tres desviaciones documentadas.** (1) El plan pedía tres valores de ámbito y hacen falta **cuatro**: `hub_prompt_templates` y `hub_widget_keys` llegan a la organización **por su chatbot**, y meterlas en «de organización» diría que tienen una columna que no tienen — `DERIVADA` obliga a decir por qué columna, que es lo que MT.7 necesita escribir. (2) El plan hablaba de **tres** cascadas existentes y contaba el vocabulario: no lo es, `hub_vocabulary_terms.organizacion_id` es NOT NULL desde ING.0.1. (3) El módulo va en `core/ambito.py` y no en `core/tenancy/ambito.py` como decía el plan: ya existe `core/auth/tenancy.py` y dos cosas llamadas «tenancy» en sitios distintos es la manera de que alguien importe la que no era — además son concerns distintos, aquél decide quién puede ver qué y éste qué fila gana. **Un hueco cerrado sobre la marcha**: `getattr` recorre la MRO, así que un modelo que heredara de otro se habría quedado con el ámbito del padre en silencio; se mira la clase y no su ascendencia. Sin migración y sin cambio de comportamiento: se declara lo que hay hoy, incluido lo global que MT.2 y MT.6 van a cambiar — declarar ya `HEREDABLE` sería una etiqueta que la base de datos no sostiene y el diff de esos prompts dejaría de enseñar la decisión. 14 tests nuevos; `tests/core` + higiene + modelos: 138 passed |
| 2026-08-23 | REV.13 (RED/GREEN) — Los prompts en un sitio, sin fingir que son la misma cosa · **cierra el Bloque REV (continuación)** | **Cursor: sin cambios.** La pregunta del usuario tenía dos partes y sólo una tenía la respuesta obvia. Unificar las tablas era la respuesta equivocada: `HubPromptTemplate` cuelga de un chatbot (`chatbot_id` NOT NULL), va por idioma y se versiona al cambiar el texto; `HubActivityPrompt` tiene `activity` **único global** y su catálogo lo declara el código, no la tabla —que sólo guarda la excepción—. Meterlas en una sería perder una de las dos. Lo que faltaba era **verlas juntas**: `/hub/prompts-catalog` las agrega en **sólo lectura** con el `ambito` explícito, y cada edición sigue yendo al router de cada una, que es donde vive su regla. **El hallazgo lo dio abrir la pantalla de al lado, no leer el modelo**: lo que de verdad se ve hoy en «Prompts del sistema» de Chatbots no son plantillas —hay **cero** filas en `hub_prompt_templates` en esta base— sino el **prompt base de cada asistente**, que es una columna de `hub_chatbots` y no está en ninguna tabla de prompts. Un catálogo que lo dejara fuera habría enseñado lo que casi nadie tiene y escondido lo que todos tienen, así que entra como tercer ámbito (`chatbot_base`) y se guarda con un `PATCH` del chatbot, no del router de plantillas —mandarlo allí daría un 404 con un identificador que no existe, y hay un test que lo fija—. **El catálogo acota por organización pasando por el chatbot**, que es quien la tiene: la plantilla no, y filtrar por lo que no está es no filtrar; es el agujero de SEC.2 servido desde una pantalla nueva si se olvida. El filtro de módulo se esconde con el ámbito de asistente porque ahí no aplica, y dejarlo puesto invita a usarlo y a no entender por qué no hace nada. La pantalla pasa a llamarse «Prompts» a secas. Verificado en navegador contra el servidor real: los cuatro asistentes con su prompt base, **guardado de uno comprobado en la base de datos y el valor restaurado después**, consola limpia y las tres peticiones al catálogo en 200. 8 tests de integración + 6 de la pantalla |
| 2026-08-23 | REV.12 (RED/GREEN) — La marca que se ve es la de la organización que se está mirando | **Cursor: sin cambios.** El usuario puso logotipo y color a la UJI y el lateral seguía igual, y la sospecha razonable era «hay que reiniciar». No: `/hub/themes/resolved` fundía el nivel de organización **sólo para quien pertenece a una sola**, y un superadministrador no pertenece a ninguna — así que configuraba la identidad de la UJI y veía siempre la de plataforma. Hasta REV.10 el servidor tampoco tenía forma de saber cuál mirar; con el selector ya en la cabecera, el endpoint gana `organizacion` y `useMarca`/`useColoresDelPanel` le pasan la elegida. **Va con `assert_org_access`**: un parámetro que elige de qué organización se resuelve el tema es, sin comprobación, una forma de leer la identidad de otra. Verificado en navegador: sin el parámetro no hay logotipo, con `?organizacion=<uji>` llega el de la UJI, y el lateral lo pinta |
| 2026-08-23 | REV.11 (RED/GREEN) — Organizaciones no es del módulo Chatbots | **Cursor: sin cambios.** No era colocación: el router de organizaciones **ya** exigía el módulo `plataforma` para crear y borrar, así que quien tenía `plataforma` y no `chatbots` no llegaba a la pantalla que su propio módulo protege, y quien tenía `chatbots` la veía en su menú para que el servidor le dijera que no. Es el caso de «Modelos LLM» de PLAT.2 otra vez, y por eso la ruta se mueve a `/plataforma/organizaciones` en vez de aflojar la guarda. La organización no es un concepto del módulo de chatbots: la usan informes, curación, personas y la identidad visual |
| 2026-08-23 | Auditoría de multitenencia + planificación de REV.11–REV.13 y del Bloque MT | **Cursor: sin cambios.** El usuario preguntó lo que había que preguntar: «cambio de organización y sigo viendo los mismos proveedores y modelos, las mismas personas, los mismos chatbots». Auditoría con cifras, no impresiones. **Lo que sí está bien**: el núcleo operativo está acotado —chatbots, corpus, ingesta, vigencia, curación, temas, vocabulario, interacciones, feedback, escenarios, chat y cuotas—, unas veces por `organizacion_id` y otras por el camino `chatbot_id`/`site_id`; 12 de los 32 routers pasan por la capa de tenencia y `test_tenant_isolation.py` lo vigila con ~28 tests. Eso es lo que filtraría contenido entre municipios, y no lo hace: de las 31 tablas `hub_*`, las que no llevan la columna la alcanzan por FK, salvo las de abajo. **Lo que no**: `hub_providers` —cuatro filas, y una de sus columnas es `api_key`— y `hub_llm_configs` —siete, en tres niveles— son **globales**, sin organización ni camino hacia una, y `get_model_for_tier(tier, provider)` ni la recibe (8 ficheros la llaman); el **módulo Informes no tiene dimensión de organización en absoluto**, porque `owner_kind` admite `user`, `platform` y `superadmin` pero **no `organizacion`** (51 endpoints, 7 modelos); en Personas el dato está y el filtro no, y encima el listado es sólo de superadministrador, así que un administrador no puede gestionar a la gente de su propia organización; las concesiones de módulo y los tokens van por sujeto, sin decir en qué organización; y `scope_query_to_orgs` **no acota a un superadministrador, que es correcto como permiso** — lo que falta es la vista, y el selector que REV.10 acaba de poner no filtra nada todavía. **El corte propuesto no es por importancia sino por migración**: añadir una columna a una tabla vacía es gratis y añadirla cuando el piloto lleva meses de informes firmados es una migración con riesgo, así que la fase 1 (6-8 prompts) mete el esquema **antes** del piloto sin cambiar comportamiento —`organizacion_id` nulo = plataforma y se hereda, como la cascada de temas— y la fase 2 (8-10) deja la vista y los permisos para después. Dicho también lo que el bloque **no** hace: sigue siendo una base con organizaciones dentro, y la separación física por municipio es el modo `edge` que ya describe `AGENTS.md`. De paso quedan escritos REV.11 (Organizaciones a Plataforma, cuyo router ya exige `plataforma`), REV.12 (el tema de la organización elegida, que un superadministrador no ve hoy) y REV.13 (los prompts en una pantalla, sin fingir que los dos modelos son el mismo) |
| 2026-08-23 | REV.10 (RED/GREEN) — La organización se elige una vez, en la cabecera | **Cursor: sin cambios.** Cada pantalla lo resolvía a su manera: «Valores por defecto» con su propio selector y su propio estado, «Identidad visual» con otro, Vigencia por **chatbot**, y Personas y Prompts de actividad sin ninguno —una porque le faltaba, la otra porque no le hace falta—. Cambiar de organización obligaba a repetir la elección pantalla por pantalla, y no había forma de saber si una pantalla sin selector era de plataforma o estaba incompleta. Ahora hay una elección compartida en la cabecera que **se recuerda entre recargas**: es una preferencia de quien usa el panel, no un dato del servidor. Si la guardada ya no existe —organización borrada, o alguien que cambió de permisos— se cae a la primera disponible, porque si no el panel se queda apuntando a un id fantasma del que no se sale desde la interfaz; con una sola organización no se ofrece, mismo criterio que el buscador de REV.7. **Personas gana la columna y el selector que le faltaban**: `organizacion_id` existe con su clave ajena desde AUTH.2 y la pantalla ni lo enseñaba ni lo pedía, así que se podía dar de alta a gente sin organización sin enterarse — «Sin organización» y no una celda vacía, y si el id apunta a algo que ya no está se enseña en bruto, que es la pista de que hay una fila huérfana. Y la pantalla que **no** lleva selector dice por qué: los prompts de actividad son configuración de plataforma (`activity` es único global), y callarlo dejaba justo la duda que planteó el usuario. Tres ficheros de test que renderizan `AppLayout` necesitaron el doble de la lista de organizaciones. **Frontend completo: 630 passed**, `tsc` limpio. Verificado en navegador: elegir «Organización Demo» en la cabecera y encontrarla ya elegida en «Valores por defecto». 13 tests nuevos |
| 2026-08-23 | REV.9 (RED/GREEN) — La cascada de color llega al panel, que hasta ahora no la consumía nadie | **Cursor: sin cambios.** El hallazgo no era «falta un color»: eran **dos fuentes de verdad que no se hablaban**. `ThemeColors` servía al widget; el panel se pintaba con las variables Tailwind de `index.css`, escritas a mano; y el único consumidor de la cascada del servidor era el logotipo (`useMarca`). O sea que se podía cambiar cualquier color en «Identidad visual» y **no cambiaba nada**. El usuario lo notó por donde se nota —«el color de fondo de la barra lateral no aparece en la selección»— y no aparecía porque no existía: la paleta configurable tenía otros tokens, con otros valores, y su `primary` (`#0066cc`) ni siquiera era el azul de la aplicación (`#0b5394`). Tres piezas: la paleta gana los tokens del panel **en la misma paleta y no en un grupo aparte** —la identidad de una institución es una, y partirla obligaría a elegir dos veces el mismo azul—; sus valores por omisión pasan a ser los de `index.css`, porque con los viejos aplicar la cascada habría cambiado la identidad sin que nadie lo pidiera; y un aplicador que escribe en `:root` **sólo lo que el tema define**, para que lo que no define lo siga mandando `index.css` — la diferencia entre «este nivel no lo configura» y «este nivel lo pone vacío». **Dos guardarraíles, porque el problema era la divergencia y no el color**: un test del servidor compara los valores por omisión del contrato con `index.css` campo por campo, y uno del frontend comprueba que el mapa de variables sólo nombra campos que el contrato declara. El primero falló al escribirlo por una razón que merece anotarse: partir el CSS por la cadena «.dark» cortaba en el `@custom-variant dark` de la línea 5 y se llevaba el bloque `:root` entero, así que el test decía «la variable ya no está» cuando estaba. **Y un defecto que destapó el navegador**: se guardaba el color, la fila quedaba bien y el panel seguía igual — la pantalla invalidaba la lista de temas y no `/themes/resolved`, que es otra consulta, así que había que recargar a mano y eso se lee como «no se ha guardado». Verificado en navegador: el lateral pasa a verde y luego a violeta **sin recargar**, y el tema de verificación se retiró. 21 tests nuevos |
| 2026-08-23 | REV.8 (RED/GREEN) — Borrar a quien nunca entró, ver al superadministrador, y no quedarse sin ninguno | **Cursor: sin cambios.** Tres carencias que son la misma conversación. **Borrar**: IDE.4 decidió «desactivar, nunca borrar» con buen motivo —quien ya entró tiene rastro en interacciones, informes y concesiones— y era absoluto de más, porque una fila creada a mano que nadie ha usado no tiene rastro de nada y un correo mal escrito se quedaba en el listado para siempre. La regla es `last_login_at IS NULL`, que es exactamente «se creó a mano y no se ha usado», y las concesiones de esa persona se van con ella en la misma transacción: una concesión a alguien que ya no existe es una fila que ninguna pantalla enseña. **Quién puede borrarse lo dice el servidor** (`puede_borrarse`, `motivo_no_borrable`), no la pantalla — es la regla de `AGENTS.md`, y si el React decidiera por `last_login_at` esa regla viviría en dos sitios—; la pantalla enseña el motivo y no sólo esconde el botón, porque una fila sin acciones y sin explicación se lee como «aquí no se puede hacer nada». **El superadministrador**: no aparecía en ninguna parte, porque el listado leía sólo `hub_users` y esa cuenta vive en `superadminaccount` — la única cuenta real de una instalación nueva era invisible. Se muestra en solo lectura, con su propio origen y sin acciones; no se unifican las cuatro tablas de identidad, que es una migración de otro tamaño. **El último**: 409 en las dos puertas —`PATCH` y `DELETE`— y también al bajarle el rol, que llega al mismo sitio; el recuento mira las dos tablas, porque contando sólo `hub_users` la cuenta de arranque no valdría y la regla bloquearía una desactivación segura. Sin migración. Verificado en navegador: el superadministrador se ve marcado como cuenta de instalación y sin botones, y el ciclo alta → cancelar (no borra) → confirmar (borra) deja la BD como estaba. 13 tests de backend + 7 de pantalla |
| 2026-08-23 | REV.7 (RED/GREEN) — Buscar y filtrar las actividades por módulo | **Cursor: sin cambios.** Hoy son cuatro y la pantalla las apilaba sin más; el catálogo crece cuando se cablea un consumidor y una lista plana de cuarenta tarjetas, cada una con su área de texto, no se recorre. **El trabajo de verdad no era el buscador**: el catálogo decía qué actividades existen, con qué nivel corren y para qué sirven, pero **no de quién son**. `MODULO_POR_ACTIVIDAD` lo declara con los códigos que ya usa la plataforma (`MODULOS_INICIALES`) y no con un vocabulario nuevo —si aquí pusiera «redaccion» y el menú dijera «informes», el filtro y las concesiones hablarían de cosas distintas—, y lo fija un test. El módulo viaja en el contrato porque la pantalla agrupa y filtra por él: deducirlo del nombre de la actividad en el React sería inventarlo. La búsqueda mira la clave **y** el «para qué sirve» —quien busca no recuerda `configuracion_de_grafico`, recuerda que había algo de gráficos— y es insensible a tildes. Cuando nada coincide lo dice, porque una pantalla en blanco tras escribir se lee como «se ha roto»; con una sola actividad no ofrece filtros. Verificado en navegador: «grafico» sin tilde encuentra `configuracion_de_grafico` por su descripción. 7 tests nuevos |
| 2026-08-23 | REV.6 (RED/GREEN) — La cola de vigencia se puede tachar | **Cursor: sin cambios.** Se veía qué documentos estaban pendientes y no había forma de validar ninguno: `vigencia_validada_el` y `revisat_per` **sólo se leían** —no había en todo el servidor un sitio que los escribiera—, así que el aviso que el asistente emite al citar un documento sin validar se repetía indefinidamente y el número no bajaba nunca. Un `POST` por documento y no un guardado de la pantalla: cada fila es una decisión sobre una norma concreta, y agruparlas haría que un fallo en la quinta dejara en duda las cuatro anteriores. **No se valida lo que el estado dice que no está vigente**: un documento derogado sigue en la cola por su `estat_vigencia`, así que sellarlo no lo sacaría de ella y el botón parecería roto — 409 en el servidor, y la pantalla ni lo ofrece: dice que procede retirarlo. **Dos defectos que destapó el navegador y no los tests.** El primero: la firma iba a `revisat_per`, que parecía el sitio evidente y es el equivocado — ese campo viene del frontmatter del corpus y es la revisión humana del **contenido**, obligatoria para `content_class: regulation`, así que escribir ahí al que valida la vigencia destruye un dato del contrato y la columna «Revisado por» pasaría a enseñar a quien pulsó el botón; columna propia `vigencia_validada_per` con su migración y un test que fija que `revisat_per` no se toca. El segundo: `require_role` es coincidencia exacta y no «este rol o superior», así que `require_role("admin")` devolvía **403 a un superadministrador** y la pantalla se quedaba igual, con las 41 filas intactas y sin decir nada — el síntoma que en FIX.1 tapaba cuatro fallos; se arreglan los dos, y la mutación gana `onError`. Migración aplicada: `p6i7j8k9l0m1 (head)`. Verificado en navegador: la cola baja de 41 a 40, el resumen se actualiza, la firma queda en su columna y `revisat_per` intacto; la validación de verificación se revirtió, porque nadie comprobó esa norma de verdad. 15 tests de backend + 6 de pantalla |
| 2026-08-23 | D.7 (planificación) — Qué datos pasan al piloto | **Cursor: sin cambios.** Prompt nuevo en el bloque Deploy, con las decisiones del usuario ya cerradas: pasa **sólo la UJI** —fuera las otras cuatro organizaciones de desarrollo y con ellas el `Chatbot Demo` y sus 7 documentos—, pasan **los tres** asistentes de la UJI (el agèntic «(proves)» renombrado, porque es el único sitio donde se ejercita `MD_AGENT_SELECTOR`), el corpus de 548 documentos, la vigencia y el resultado de curación; **no pasa ningún informe de prueba** (29 `hub_workspaces`) **ni ninguna persona**. Separa dos cosas que viajan distinto: el **catálogo del producto** —las plantillas demo, exportadas a ficheros versionados y sembradas por `bootstrap.py --con-demo`, nunca copiadas de la BD de nadie— y los **datos operativos**, un `pg_dump` con lista explícita de tablas y filtro por organización. **Dos hallazgos del inventario que cambian la decisión que se había tomado.** El sitio de curación `Escola de Doctorat` —el único que hay, con 351 páginas y 288 hallazgos— tiene **`organizacion_id` a NULO**, así que el filtro por organización lo dejaría fuera: se asigna a la UJI antes del volcado, y el prompt lleva los dos tests que hacen de eso un paso consciente. Y la plantilla de presupuesto elegida por tener el nombre más limpio, `Ejecucion presupuestaria trimestral`, **está vacía**: su `spec_json` son 2 bytes. La que tiene el informe real es `GUI3 ejecucion presupuestaria` (5.242 B), que se exporta renombrada —el prefijo es el nombre de un prompt de verificación— y la vacía se borra; de ahí el test que **rechaza exportar un spec vacío**, porque ese `{}` sembrado se abre sin nada dentro y el fallo aparecería en el piloto. Entregado además el juego de datos ficticio que el usuario no tenía —era de mis pruebas, no de las suyas—: `pruebas_manuales/datos/ejecucion_presupuestaria_demo.xlsx` con su generador, tres columnas derivadas del contrato de la plantilla e importes **como texto en formato español con `€`**, a propósito, porque generarlos numéricos no ejercitaría la conversión. **Verificado pasándolo por el `DeterministicETLService` real con las operaciones de la plantilla: 12 filas, 0 nulos, orden descendente correcto** |
| 2026-08-23 | REV.5 (RED/GREEN) — El enlace de la cola de vigencia llevaba a Informes · **cierra el Bloque REV** | **Cursor: sin cambios (Deploy/D.0).** Un síntoma, dos fallos encadenados. `canonical_url` no siempre es una URL: para los documentos del corpus normativo cargados desde carpeta es el **nombre del fichero** (`20260203_UJI_REC_Resolucio_assimilacio_carrecs.md`), y en la BD de desarrollo hay decenas así. Al ser relativo, el navegador lo resolvía contra la ruta actual y abría pestaña en `/hub/<fichero>.md`; ninguna ruta casaba, caía en el comodín `*` y **`Aterrizaje` redirigía al primer módulo concedido**, que en `RUTA_DEL_MODULO` es `informes`. Parecía un fallo del módulo de Informes. Se arreglan por separado porque son dos: la pantalla sólo enlaza `http(s)` —un `javascript:` en ese campo sería un enlace ejecutable servido **desde nuestro propio origen**— y en los demás casos enseña el identificador o el nombre del fichero, que es lo único con lo que ir a buscarlo; y el comodín pasa a `NoEncontrado`, que **dice** que la dirección no existe en vez de disfrazar un 404 de redirección. La raíz sigue aterrizando, que es lo correcto donde no se ha pedido nada concreto. **Dos cosas que salieron del camino**: `CAL.5` cazó `NoEncontrado` como pantalla no partida —entra en la lista de estáticos por la misma razón que `SinAcceso`, diez líneas en el mismo módulo, y de paso se corrige el comentario que decía que `Aterrizaje` estaba «en la ruta índice **y** en el comodín», que ya no es cierto—; y el navegador destapó que el comodín vive **fuera del layout**, así que el texto remitía a un menú lateral que no está: ahora lleva un enlace de vuelta al inicio. **Verificado en navegador sobre datos reales**: 41 documentos en la cola, los del corpus ya sin enlace y el de DOGV conservando su `href`; la URL exacta del fallo enseña «Esta dirección no existe». 5 tests nuevos |
| 2026-08-23 | REV.4 (RED/GREEN) — El selector del logotipo se podía ver y no pulsar | **Cursor: sin cambios.** `disabled={!propio}` en el `<input type="file">`. En una instalación recién levantada el nivel de plataforma no tiene tema —en la BD de desarrollo sólo había dos, los dos de organización—, así que el control salía en gris e inerte con la explicación en un párrafo de 12 px debajo. Para subir el logotipo había que guardar antes unos colores que quizá nadie quería tocar: un callejón. Ahora el tema **se crea al vuelo** y el fichero se sube sobre el que salga, y nace con lo editado sin guardar y no con la paleta del padre copiada, que es la regla que PLAT.6 estableció. Y se ve que es accionable: el control nativo no se puede estilar **ni traducir** —«Tria un fitxer» lo pone el navegador, en su idioma, y comparte renglón con «no s'ha triat cap fitxer»—, así que se oculta con `sr-only`, que lo esconde de la vista y no de los lectores de pantalla, y se pulsa sobre una etiqueta con aspecto de botón; el nombre del fichero elegido va en su propia línea. Fuera `guarda_antes_de_subir` en los tres idiomas: decía algo que ya no es cierto. **Verificado en navegador contra el servidor real**: en el nivel de plataforma sin tema, elegir un PNG crea la fila (`es_plataforma = t`) y sube el logotipo sobre ella; **el tema de verificación se borró después**, para no dejar el panel con un PNG de 1×1 por marca. 5 tests nuevos |
| 2026-08-23 | REV.3 (RED/GREEN) — La opción activa del menú, con negrita y barra en vez de recuadro | **Cursor: sin cambios.** Los tres menús —el lateral y las dos barras de pestañas— pintaban la sección activa con un relleno (`bg-sidebar-accent`, `bg-accent`). En el lateral eso mete un segundo azul dentro del azul de la marca. Ahora negrita más una barra **del color del propio texto**: a la izquierda en el lateral, debajo en las pestañas, donde el `-mb-px` sube el subrayado sobre la línea del `<nav>` para que sean la misma línea. **La barra se reserva también en los inactivos** con `border-transparent`: si sólo la llevara el activo, cambiar de sección desplazaría el menú entero dos píxeles. Se conserva el `aria-current="page"` de `NavLink`, que al quitar el fondo pasa a ser la única señal que ve un lector de pantalla. Verificado en navegador. 4 tests nuevos |
| 2026-08-23 | REV.2 (RED/GREEN) — Los valores por defecto de recuperación se pueden cambiar | **Cursor: sin cambios.** La pantalla los pintaba con `String(valor)` y el único botón era «volver a heredar»: se veía la configuración de una organización y no se podía tocar, así que cambiar el tamaño de fragmento exigía ir por API. **El `PATCH` ya lo aceptaba todo desde PLAT.3**, incluido el `null` explícito; lo que faltaba era el control. El tipo no se puede deducir del dato —la mitad de estos campos valen `null` cuando heredan, y `null` no dice si debajo hay un número o una casilla—, de ahí `CONTROLES`, la única lista de campos que queda escrita en el React; para que no mienta la vigila un test que la compara con **la interfaz que genera Orval**, así que un campo nuevo del servidor sin control pone el test rojo. Los cuatro campos enumerados van con `<datalist>` y no con `<select>` porque el contrato los declara `string` libre y un desplegable perdería en silencio un valor desconocido —mismo criterio que las tipografías de PLAT.6—; cuando el servidor los declare como enumeración cerrada, esto sale de aquí. Dos decisiones más: se guarda **al salir del campo y sólo si cambió** —tabular por encima no es editar, y guardar ahí convertiría en propio un campo que sólo estaba heredando—, y un campo heredado ofrece «establecer valor propio», que antes era un callejón: se podía volver a heredar pero no salir. **Verificado en navegador contra el servidor real**: `default_min_retrieval_results` de la UJI de 2 a 3, comprobado en Postgres, y devuelto a 2. 8 tests nuevos |
| 2026-08-23 | REV.1 — El guardarraíl de la BD del desarrollador no veía el segundo camino · **abre el Bloque REV** | **Cursor: sin cambios.** `test_should_have_no_test_opening_a_session_on_the_developer_database` vigila `server_engine`, y por ahí no pasó el infractor: `test_acs_issues_jwt_and_redirects` **leía `DATABASE_URL` del entorno y se construía su propio motor**. Resultado, **97 filas de usuario reales** acumuladas en la BD de desarrollo, una por ejecución — todas `ada-*@uji.es`, o sea que la pantalla de Personas enseñaba 97 filas de test y ninguna persona real. Borradas. El patrón nuevo exige la comilla de cierre para no cazar `DATABASE_URL_SYNC`, que dos tests de migraciones leen con motivo, y sólo mira **lecturas**: el `setdefault` de `test_openapi_export.py` no conecta a ninguna parte. Las cuatro exenciones van en **lista explícita y no en heurística** —distinguir «lo lee para escribir ahí» de «lo lee para otra cosa» exige seguir la variable, y una heurística que lo intente da los falsos positivos que acaban desactivando el guardarraíl—; una de ellas es `test_db_isolation.py`, que lee el DSN para afirmar que el motor e2e **no** es ese: prohibírselo dejaría sin guardián al guardián. Verificado plantando un infractor sintético, que detecta con fichero y línea |
| 2026-08-23 | CI: el test de SAML escribía en la BD del desarrollador y el contrato no se generaba (fuera de plan) | **Cursor: sin cambios.** Dos rojos con causas distintas, ninguna del código de producción. El de SAML es el que abre REV.1. El otro: los dos `openapi.json` están en `.gitignore` a propósito —artefacto generado— y sólo los produce el job `contract`, que es **otro runner y otro checkout**; mientras CI corría el 42% de la suite el fichero de test no se colectaba, y al pasar a `tests` entero (`e0b2eb3`) los dos tests de presencia quedaron al descubierto. Se genera en el job de tests en vez de relajar los tests, porque el exportador es offline —no levanta Uvicorn ni toca la BD—, cuesta segundos y además **convierte en aserción real** el `test_server_openapi_json_is_valid`, que hasta ahora se saltaba en CI por falta del fichero. **Hallazgo anotado y no arreglado**: `test_openapi_export.py` hace `os.environ.setdefault("DATABASE_URL", <dsn falso>)` en tiempo de import, así que ejecutar la suite en local sin `DATABASE_URL` exportada deja el DSN falso puesto para toda la sesión y el `conftest` salta los tests de BD con «BD Postgres no disponible» — medido: los dos ficheros juntos dan 29 passed/12 skipped sin la variable y 41 passed con ella. En CI no pasa porque es variable del job |
| 2026-08-23 | PLAT.7 | **Fuera la columna que nadie leía y la ruta mal llamada.** `hub_organizaciones.theme_config` era JSONB con un `<textarea>` a la vista —«Configuración de tema (JSON)»— y **ningún lector**: la cascada resuelve desde `hub_themes`. Dos sitios para lo mismo, y el visible era el que no hacía nada; se retira ahora porque PLAT.6 acaba de darle sustituto. Comprobado antes de borrar, como exigía el prompt: **las cinco organizaciones tenían `{}`**, ninguna llevaba configuración escrita creyendo que servía. Migración `o5h6i7j8k9l0`, aplicada y **probada en los dos sentidos** (`downgrade` recrea la columna vacía; el contenido no se recupera y el docstring lo dice para que nadie revierta creyendo que sí). La de `HubChatbot` **se queda**: ahí sí se lee, guarda `{"theme_id": ...}` y es de donde el widget saca su tema. Y `/hub/reports` → `/hub/revision`, `ReportsPage` → `RevisionInteraccionesPage`: la etiqueta ya decía «Revisión» y el nombre chocaba de frente con el módulo Informes, que es donde alguien iría a buscarlo. Sin redirección desde la ruta vieja. El guardarraíl de PLAT.3 que sostenía la columna («no la retires todavía») **se da la vuelta** y ahora vigila que no vuelva, en `tests/infra/test_plat7_retirada_del_tema_en_json.py`; `parseJson` se quedó huérfana al quitar el `<textarea>` y se borra. Actualizadas las referencias vivas que mandaban a la ruta muerta: dos `.bat` de pruebas manuales —releídos y reescritos en cp1252, que es lo que exige CMD— y `docs/PRUEBAS_PENDIENTES.md`. Verificado en navegador: `/hub/revision` carga con sus filtros, `/hub/reports` ya no resuelve, el diálogo de organización tiene tres campos y ninguno es JSON, y el alta sigue funcionando (creada y retirada una organización de prueba) |
| 2026-08-22 | PLAT.5 | **Tres routers de plataforma no declaraban su módulo, y la guarda no podía ir a nivel de router.** `hub_organizaciones_router`, `hub_themes_router` y `pat_router` son `Deploy: cloud` y solo los protegía el rol: un admin con `informes` concedido y nada más los llamaba por API sin tener pantalla. `require_module` depende de `get_current_user`, así que sin sesión responde 401, y hay **tres casos donde eso rompería algo que funciona**: el logotipo y el tema del **widget público** (un `<img>` no manda `Authorization`), `/hub/themes/resolved` —que consume el panel entero, incluida la persona que solo tiene `informes`, porque de ahí sale la marca de la cabecera— y **leer** la lista de organizaciones, que la usan el selector de `ChatbotsPage` y el de valores por defecto. De ahí guardas por endpoint. Consecuencia de PLAT.3 fijada: los valores por defecto de RAG exigen `chatbots`, no `plataforma`. El inventario vive en el test y se comprueba sobre los docstrings; tenía mal `hub_content_quality_router`, que es de **curación**. Verificado en vivo con el servidor arrancado: logotipo 200 sin sesión, lista 401 sin sesión y **403 con sesión sin el módulo**, `/resolved` 200 con solo `informes`. 12 tests propios + **1564 en las suites afectadas** |
| 2026-08-22 | PLAT.6 | **La identidad visual se configura, y la cascada resultó estar de adorno.** Pantalla nueva en `/plataforma/identidad-visual` con los tres niveles, selector de color de verdad en vez del `<textarea>` de JSON, `<datalist>` de familias tipográficas (sugerir sin encerrar: un tema con una familia fuera del catálogo conserva la suya), subida del logotipo con sus límites dichos —PNG/JPEG, 1 MB, y **por qué no SVG**— cortados en el cliente antes de subir un mega para nada, vista previa **sobre `bg-sidebar`**, que es lo que hacía imprescindible a una persona, y enlace desde la pantalla de organizaciones que trae la organización elegida. **Tres defectos los destapó el navegador, no los tests.** (1) En el nivel de plataforma de una instalación recién levantada no hay tema propio ni padre, así que la pantalla iteraba un conjunto vacío y **no ofrecía ni un campo** — justo el estado de quien entra por primera vez; se resuelve con `GET /hub/themes/defaults`, que publica `ThemeConfig` serializado: la lista **es** el modelo, y añadir un color aparece en la pantalla sin tocar TypeScript. (2) El servidor materializaba los valores por omisión al guardar (`model_dump()` sin más), así que el primer guardado de una organización se llevaba los dieciséis colores de la plataforma como **propios** y un cambio posterior arriba ya no le llegaba: la cascada quedaba decorativa y nadie se enteraría. `exclude_unset` en el alta y en la actualización, mismo criterio que PLAT.3. (3) Segundo camino al mismo sitio: `get_theme_for_chatbot` servía el tema del asistente **crudo**, sin resolver la cascada — invisible mientras todo se almacenaba completo, norma en cuanto es disperso; ahora fusiona plataforma → organización → asistente como ya hacía `/resolved`. Y la etiqueta decía «Heredado de **Tema**», el nombre de la fila: ahora dice el **nivel**. Verificado en navegador la secuencia entera —plataforma dispersa, organización que hereda, cambio arriba que llega abajo, consola limpia— y **retirados los temas que creé al verificar**, para no dejar el entorno de desarrollo tocado. 20 tests de la pantalla + 7 backend |
| 2026-08-22 | PLAT.4 (RED/GREEN) — Retirada de «Cerebro IA», que solapaba con las dos pantallas de al lado | **Cursor: PLAT.4 ✅ → PLAT.5 (Sonnet).** El prompt exigía leerla entera antes de decidir, y leída no aporta **nada** que las otras dos no den: `PromptsPage` tiene 9 referencias a la edición de plantillas y `LLMConfigsPage` 8 a `is_default` y 2 al test de conexión. **Y además engañaba**: su selector de chatbot estaba junto al botón «poner por defecto», lo que sugiere que se elige el modelo *de ese chatbot* — y no: `is_default` es global y la mutación no manda el `chatbot_id` a ninguna parte. Una pantalla que insinúa una relación que no existe es peor que no tenerla, porque alguien la usa creyendo que configuró un chatbot. Caso B: borrado directo, con la pantalla, su test, su ruta, su tab y **14 claves i18n** por idioma; corregido de paso un comentario de `ActivityPromptsPage` que apuntaba a `/hub/brain`. **Lo otro que pedía el prompt**: `/plataforma/modelos` dice ahora **quién consume los niveles** — Informes (copiloto, propuesta y auditoría de scripts, borradores) y Curación (rastreo, reconocimiento), no solo Chatbots—, porque vivir bajo aquel módulo hacía creer que los niveles eran suyos. **Tercera vez con la misma trampa**: el guardarraíl de `grep` a cero se cazaba a sí mismo, y esta vez no en un comentario sino en su propio código — un test que busca una cadena la contiene por necesidad; se excluye a sí mismo con la razón escrita, y no deja hueco porque una reintroducción sería en otro fichero. **Verificado en navegador**: `/hub/brain` ya no resuelve, la nota de los niveles se ve en `/plataforma/modelos` y el test de conexión sigue ahí. **Frontend completo: 550 passed.** Anotado: una ejecución intermedia dio un rojo de `SitesPage (curation)` que **pasa aislado y pasa al repetir la suite** — un flake, el primero que se ve desde que se cerró CUR; no perseguido aquí. 4 tests nuevos |
| 2026-08-22 | PLAT.3 (RED/GREEN) — Organizaciones deja de ser tres pantallas en una | **Cursor: PLAT.3 ✅ → PLAT.4 (Opus).** `OrganizacionRead` devolvía identidad del inquilino, tema y **catorce `default_*` de RAG** en el mismo sitio; por eso la pantalla acabó bajo Chatbots —la mayoría de sus campos sí lo eran— y por eso moverla sin partirla se habría llevado los defaults fuera de su módulo. Ahora la identidad va en `/hub/organizaciones` y los defaults en `/{id}/valores-por-defecto`, **endpoints separados** y no un objeto con dos mitades, y los dos con `extra=forbid`: mandar un campo de la otra mitad da 422 en vez de ignorarse, que haría creer que se guardó. **Y al partirla salió un defecto real, distinto del que suponía el prompt**: el plan temía que omitir un campo lo pisara con su defecto, y es al revés. El `PATCH` usaba `model_dump(exclude_none=True)`, que descarta también los **null explícitos**, así que omitir estaba a salvo y lo imposible era **volver a heredar**: seis campos llevan escrito en el modelo que `None` significa «heredar el defecto de plataforma» —presupuesto de contexto, los tres de troceado, reescritura de consulta y el modelo que la hace— y una vez fijado un valor propio no había vuelta atrás por API. `exclude_unset` distingue «no lo mandé» de «mándalo a null», con test parametrizado por los seis. El alta pierde los `default_*` **sin perder nada**: las columnas del modelo declaran exactamente los mismos valores, comprobado antes de tocar. `theme_config` **no se retira aquí**, con test que lo exige: lo hace PLAT.7, cuando exista su sustituto. La pantalla nueva itera lo que devuelve el servidor, dice «Heredado de la plataforma» en vez de dejar la celda vacía —`null` es una decisión, no un dato que falte— y **reutiliza las nueve etiquetas i18n** que quedaron huérfanas al vaciar el formulario viejo en vez de borrarlas e inventar otras, resueltas por plantilla con el nombre técnico como reserva. **Verificado en navegador contra el servidor real**: fijado `default_chunk_size=500` por API, el botón aparece, se pulsa y queda «Heredado de la plataforma» con el botón ya sin sentido — el caso que antes era imposible. La pantalla de identidad se queda con cuatro campos y ninguno de RAG. Consola limpia. Contrato regenerado (144 rutas). **Frontend completo: 548 passed.** 17 tests de backend + 8 de la pantalla |
| 2026-08-22 | CI ejecuta la suite entera, no el 42% de ella (fuera de plan) | **Cursor: sin cambios.** Salió del cierre del Bloque IDE, cuando la suite completa dio 2 rojos que CI no habría visto nunca. Medido: la lista de rutas del paso de tests cubría **1.187 de 2.811**. Fuera quedaban `tests/modules/redaccion` (**647**) y `tests/modules/curation` (**413**) —los dos módulos con más tests del proyecto—, `tests/public_graphs` entero y todo `tests/api` salvo cuatro ficheros: un rojo ahí dejaba el check en verde, el mismo agujero que SEC.8.7 cerró para el árbol de la raíz. **Y mi primera afirmación era demasiado indulgente**: dije que «esos tests necesitan un servicio que CI no levanta», y es cierto de **2** tests, no de 1.624 — comprobado además que en `redaccion`, `curation` y `public_graphs` no hay un solo test que use una clave de API real. Se pone `tests` y no una lista más larga a propósito: una lista se desalinea en cuanto alguien añade un directorio y el síntoma es un check verde. `TESTING=1` al nivel del job. Los dos gates siguen aparte duplicando unos pocos tests: cuesta segundos y compra que un fallo de aislamiento se lea como «no despliegues». Añadido `concurrency` con `cancel-in-progress`, que no existía: se pagaban todos los runs de una tanda de commits aunque solo importara el último. **Verificado con el comando exacto de CI: 2810 passed, 1 skipped, 0 failed en 18:11, cobertura 81%**; los directorios que faltaban tardan 2:33 en serie. **Datos de consumo del usuario** (plan Pro): 346 min de 3.000 incluidos en un mes intenso y 0 € facturados — el límite nunca fue el problema, y al abrir el repositorio los minutos pasan a ser ilimitados |
| 2026-08-22 | IDE.5 (RED/GREEN) — Conceder un módulo a una persona o a un grupo del IdP · **cierra el Bloque IDE** | **Cursor: Bloque IDE completo → PLAT.3 (Opus).** `hub_module_grants.subject_id` era siempre una persona, así que conceder módulos era insertar una fila por cabeza. Migración `n4g5h6i7j8k9` con **tres cambios que hacen falta los tres**: `subject_type` (`usuario` | `grupo`) con `CheckConstraint` y las filas existentes como `usuario`; la restricción única pasa a `(subject_type, subject_id, module_code)` —sin el tipo, un grupo cuyo nombre coincidiera con el UUID de una persona no podría convivir con su concesión—; y **`subject_id` de 36 a 255**, porque estaba dimensionado para un UUID y el primer grupo con nombre largo habría reventado el `INSERT` en producción, no aquí. `modulos_del_usuario` une las dos vías con la semántica de `assert_chatbot_access` —rol o grupo, lo que case— y sigue siendo **fallo cerrado**. **Los grupos se comparan plegando la caja, y es una decisión razonada**: los que va a mandar la UJI son acrónimos (`PDI`, `PTGAS`) que cualquiera teclea en minúscula; se guarda lo escrito para que la pantalla lo muestre tal cual y se compara plegado, con el precio dicho —dos filas que solo difieran en la caja pueden coexistir, y como se devuelve un conjunto no conceden dos veces—. `modulos_con_origen` dice **de dónde le viene** cada módulo, porque un permiso cuyo origen no se ve es un permiso que nadie se atreve a retirar; en un superadmin el origen es el **rol** y se dice así en vez de inventar una fila. `hub_modulos_router` con catálogo —leído de la tabla, nunca de una lista en el frontend—, concesión, retirada, ficha por persona y **alcance de grupo**, que devuelve «personas conocidas» y no «afectadas»: los grupos llegan en la aserción y no se guardan, así que decir un número exacto sería precisión falsa. Rechaza conceder un módulo **retirado del catálogo**, que sería una fila decorativa. Y al conceder a una persona guarda la **clave derivada** (`user_to_uuid`), sin lo cual la pantalla escribiría filas que la resolución no encuentra. **Verificado en navegador contra el servidor real, la cadena entera**: concesión al grupo `PDI` desde la pantalla, y una persona **sin concesión propia** cuyo token declara `pdi` **en minúscula** obtiene `informes` en `/auth/me`; retirada con el texto diferenciado «Retirar a todo el grupo» y la concesión desaparece. Consola limpia. **Dos fricciones del entorno**: el doble autouse de `modulos_del_usuario` volvió a dar verde falso —marcador `sin_guarda_de_modulos` en los tests que prueban la resolución de verdad—, y `@testing-library/user-event` no está instalado, así que los tests de pantalla usan `fireEvent`. Migración aplicada: `n4g5h6i7j8k9 (head)`. Contrato regenerado (143 rutas). **Suites completas al cerrar el bloque: backend 2810 passed, 1 skipped, 0 failed; frontend 540 passed, 0 failed.** La primera pasada del backend dio **2 rojos en `tests/modules/redaccion/`** con `422 TEST_FAILED: Timeout tras 30s`, y **no eran del bloque**: los dos tests hablan con el microservicio del sandbox, el contenedor que estaba arriba era el de **producción** —que no publica el 5000— y `SANDBOX_BASE_URL` seguía en el nombre interno de Docker. Con `TESTING=1`, que `.env.example` documenta como «activa `sandbox_mode=local`», los 7 tests de esos ficheros pasan. **Hallazgo que sale de ahí y no es del bloque: CI no ejecuta `tests/modules/redaccion/`** —ni `curation/`, ni la mayoría de `tests/api/`—, así que una dependencia de entorno ahí no pone ningún check en rojo; es el mismo argumento con el que SEC.8.7 añadió el paso de colectar los tests de la raíz, y queda anotado sin tocar CI porque esos tests necesitan un servicio que CI no levanta. 35 tests nuevos |
| 2026-08-22 | IDE.4 (RED/GREEN) — La pantalla de personas | **Cursor: IDE.4 ✅ → IDE.5 (Opus).** `/plataforma/usuarios`: quién tiene cuenta, con qué rol, de dónde vino y si ha entrado. Antes no había pantalla ninguna y averiguarlo exigía acceso a Postgres. **La pantalla dice dos cosas, no solo las hace**: avisa cuando la autoridad del rol es el IdP —editar un rol a mano ahí es tirar el trabajo, lo pisa el siguiente inicio de sesión— y dice que **el listado no son todas las cuentas**, porque las tres tablas de identidad restantes siguen sin unificar y un listado que se lee como completo miente por omisión. «Nunca ha entrado» en vez de celda vacía: en una fila creada a mano es lo normal, no un dato que falte. Y **no hay botón de borrar**, solo desactivar. `/auth/me` expone `identity_role_authority` — va ahí, en el sitio que ya responde «qué necesita saber esta sesión» junto a `modulos`, y no en la pantalla: adivinarlo en el cliente sería inventarlo. **Tres correcciones que impusieron guardarraíles y dependencias del proyecto**: `@testing-library/user-event` no está instalado, así que el test pasa a `fireEvent`, que es lo que usa el resto; **CAL.5** cazó que mi envoltorio `PantallaDePersonas` en `App.tsx` —que resolvía la autoridad y la pasaba por prop— rompía la carga por ruta, así que la pantalla resuelve la autoridad **ella misma** con un hook, que además es como ya funcionan `useModulos` y `useMarca`; y **CAL.4** cazó seis claves i18n «sin consumidor» que en realidad se resuelven por plantilla (`roles.${role}`, `origenes.${origen}`) — declaradas como prefijos dinámicos, con la razón escrita: si estuvieran literales, la pantalla dejaría de pintar lo que el contrato mande. **Verificado en navegador contra el servidor real**: listado de 89 personas, alta desde el formulario con `Verif-IDE4@UJI.es` que queda como `verif-ide4@uji.es` —normalizado—, rol Administrador, origen «Alta manual», «Nunca ha entrado»; desactivada desde la fila **sigue en el listado** con «Reactivar», sin botón de borrar; y todo persiste tras recargar. Consola sin un error. **Frontend completo: 531 passed**; `tsc` limpio. Contrato regenerado. **Nota de proceso**: la pantalla y su test se colaron en el commit de IDE.3 por un `git add -A`; se dice aquí en vez de reescribir historia. 12 tests nuevos |
| 2026-08-22 | IDE.3 (RED/GREEN) — Dar de alta a una persona, y que el SSO la reconozca | **Cursor: IDE.3 ✅ → IDE.4 (Sonnet).** `hub_users_router` con listado, alta y edición, y la migración `m3f4g5h6i7j8` que añade `created_by` —la tabla sabía **cuándo** se creó una fila y no **quién** la creó; mientras el único escritor era el ACS la respuesta era obvia, con el alta manual deja de serlo, y el argumento ya estaba escrito en el `granted_by` de `HubModuleGrant`—. **El punto del prompt, y su test**: si el alta crea la fila **antes**, el ACS la **encuentra** por correo en vez de crear una nueva con el rol por defecto; alta con `X@UJI.es`, entrada con `  x@uji.ES `, y **una sola fila** con el rol que le pusieron. Para eso la normalización del correo —sin espacios, en minúsculas— **se importa del router en el ACS en vez de reescribirla**: si las dos se separan, el alta manual se queda muerta sin que nadie lo note. **Y un agujero real que el prompt mandaba verificar y estaba abierto**: `is_active` existía desde AUTH.2 y **nadie la miraba**, así que desactivar a alguien lo quitaba del listado y lo dejaba entrando igual; ahora el ACS lanza `SamlUserInactiveError` y el router lo traduce a **403 y no 401**, porque el IdP la autenticó bien y es esta plataforma la que no la deja pasar — con un 401 se busca el fallo donde no está. **Sin contraseña, y el contrato lo rechaza en vez de ignorarlo** (`extra="forbid"`): un campo aceptado en silencio hace creer que se guardó una credencial que no existe. **El correo no se puede editar**: es la clave del reencuentro con el IdP y cambiarlo desengancha a la persona de su identidad sin que se note hasta que intente entrar. **No hay borrado**, solo desactivar: quien ya entró tiene rastro en interacciones, informes y concesiones. Reservado a superadministrador — un admin que pudiera crear personas con rol podría crearse un admin. **Una fricción del entorno**: `db_session` trae las tablas del hub y no las de SQLModel, y `resolve_session` consulta `superadminaccount` antes de mirar `hub_users`, así que el fichero que cruza esa frontera se las trae él —arreglado en el test y no en la fixture compartida, al revés que en IDE.1, porque ahí sí era un defecto de la fixture—. Contrato regenerado (138 rutas). Migración aplicada: `m3f4g5h6i7j8 (head)`. **`tests/core` + `tests/infra` + `tests/api` + integración de IDE.3 + unit: 1138 passed.** 16 tests nuevos |
| 2026-08-22 | IDE.2 (RED/GREEN) — `HubSsoUser` no es de SSO: es la tabla de usuarios | **Cursor: IDE.2 ✅ → IDE.3 (Opus).** La tabla tenía correo único, nombre, rol, organización, activo y último acceso —un registro de personas completo— y se llamaba «sso» porque su **único escritor** era el ACS de SAML. Con ese nombre nadie iba a escribir ahí una persona dada de alta a mano, y el siguiente que lo necesitara habría creado una quinta tabla de identidad. `hub_sso_users` → `hub_users`, `HubSsoUser` → `HubUser`, con `origen` (`sso` | `manual`) y `CheckConstraint`, no `Enum` — dos valores estables con consumidor en el código, mismo criterio que `purpose` en `HubLLMConfig`. **`ALTER TABLE ... RENAME`, no crear-y-copiar**: renombrar no mueve datos ni pierde las claves ajenas. Se renombran también **los índices y la restricción**, que Postgres deja funcionando con el nombre viejo — no hacerlo no rompe nada, pero deja un `ix_hub_sso_users_email` colgando de `hub_users` y el siguiente que lo lea tiene que averiguar si es un resto. **Las migraciones antiguas no se tocan**, con test que lo exige: describen lo que hicieron el día que se aplicaron. Migración aplicada: `l2e3f4g5h6i7 (head)`, **85 filas preservadas**, todas `origen='sso'` (por construcción: no había otro escritor). **Dos trampas del camino, las dos de tests que se disparan con su propio texto**: el guardarraíl de `grep -r` a cero cazaba el docstring que explica el renombrado, así que se reescribió con **`ast`**, que distingue un docstring de una cadena real como `__tablename__ = "hub_sso_users"` —verificado que caza: con un fichero que asigna esa cadena, falla—; y un test de SEC.2.1 seleccionaba «la migración más reciente que mencione `hub_sso_users` y `organizacion_id`», criterio que eligió la mía —que nombra `ix_hub_sso_users_organizacion_id` al renombrar— y la puso roja sin que nada de lo que vigila hubiera cambiado; ahora busca la que **declara la columna**. **Lo que NO hace, dicho a propósito**: unificar las cuatro tablas de identidad. `tests/core` + `tests/infra` + `tests/modules/agents_hub/unit`: **805 passed**. 11 tests nuevos |
| 2026-08-22 | IDE.1 (RED/GREEN) — Quién manda sobre el rol: la aplicación o el IdP | **Cursor: IDE.1 ✅ → IDE.2 (Opus).** `_provision_sso_user` hacía `sso.role = resolve_role(attributes)` **sin condición** en la rama del usuario existente, así que un rol puesto a mano duraba hasta el siguiente inicio de sesión: no faltaba la pantalla, es que la pantalla no habría servido. `IDENTITY_ROLE_AUTHORITY=app|idp`, defecto `app`. **Se paró a preguntar y el prompt del plan estaba mal**: decía «al crear la fila se usa `resolve_role` (no hay nada mejor que usar)», y sí lo hay — `resolve_role` acepta **`superadmin`** desde el atributo de la aserción, así que confiar en ella al crear deja que un IdP mal configurado acuñe un superadministrador **con la autoridad puesta en la aplicación**, y el interruptor solo protegería lo ya creado. **Decisión del usuario**: la opción segura, y con ella una precisión que afina el bloque entero — «el superadmin, e incluso el admin, siempre se ha de hacer a mano; serán pocos», y **los módulos sí vendrán del grupo del IdP** (en la UJI, los grandes colectivos: estudiante, PDI, PTGAS, y quizá el puesto). Eso parte el problema en dos que no se mezclan: **rol → siempre la aplicación; módulos → el IdP**, que es IDE.5. Así que con `app` se ignoran **las dos** vías del IdP —el atributo y `SAML_GROUP_ROLE_MAP`— y quien llega nuevo entra con `SAML_DEFAULT_ROLE`. **Añadido fuera del prompt y por una necesidad real**: como nadie sabe todavía qué emite el IdP de la UJI —lo configura la Unidad de Desarrollo—, cuando la aserción trae un rol que se está ignorando queda un aviso en el log; en INFO y solo si viene algo, porque un aviso en cada entrada es ruido. El ajuste **falla al arrancar** con un valor desconocido, y falla **siempre** y no solo en producción: un entorno de desarrollo con la autoridad equivocada enseña un comportamiento que no es el que va a haber. `organizacion_id` **no entra en el ajuste**, y se documenta por qué: sale de `SAML_ORGANIZACION_ID`, o sea de la configuración del despliegue, así que nunca vino de la aserción y no hay autoridad que disputar — de ahí que el ajuste se llame `..._ROLE_...`. **Dos cosas que salieron del camino**: un test de AUTH.2 fijaba que el mapeo grupo→rol asignara `admin` al aprovisionar, comportamiento que ahora vive tras el interruptor, así que se actualiza al modo `idp` en vez de borrarlo; y un **defecto latente en la fixture `db` compartida** de los tests de auth — `SQLModel.metadata` solo conoce los módulos importados, así que su `create_all` no creaba nada cuando el fichero era el primero de la sesión y el test moría con `UndefinedTableError: relation "superadminaccount" does not exist`, pareciendo un fallo de la lógica probada; funcionaba por casualidad porque otro módulo importaba los modelos antes. Arreglado en la fixture, no en mi test. `tests/core` + `tests/api` + `tests/infra`: **588 passed**. 14 tests nuevos |
| 2026-08-22 | PLAT.2 (RED/GREEN) — La sección Plataforma, con lo que nunca fue de Chatbots | **Cursor: PLAT.2 ✅ → IDE.1 (Opus).** La cuarta sección del menú, que existía en el catálogo desde INF.7 y **no abría nada** —se retiró porque llevaba a un `PlaceholderPage`—. Se mueven tal cual «Modelos LLM» (`/hub/llm-configs` → `/plataforma/modelos`), «Prompts de actividad» y «Tokens de acceso», más `PlataformaLayout`, la entrada en `NAV_SECTIONS` y `plataforma` en `RUTA_DEL_MODULO` **al final**, para que quien solo administre no aterrice en `/sin-acceso` teniendo acceso pero quien tenga informes entre a trabajar. **Sin redirecciones** desde las rutas viejas, con test que lo fija: `AGENTS.md` prohíbe los shims y el panel es interno. **Verificado en navegador contra el servidor real, los dos casos que importaban**: con **solo `plataforma`** el aterrizaje cae en `/plataforma/modelos` y `GET /api/v1/hub/llm-configs` responde **200** —el caso que era imposible, porque la pantalla vivía bajo `/hub` y su router ya exigía `require_module("plataforma")`—; con **solo `chatbots`** `RutaDeModulo` corta la sección, el menú enseña solo Chatbots, su subnavegación ya no lleva los tres tabs, y `/hub/llm-configs` no resuelve (cae al aterrizaje, sin pantalla rota). Consola sin un error. **Tres cosas que salieron del camino y que no se ven leyendo el prompt**: el guardarraíl `adminI18nCoverage` cazó dos claves i18n que quedaron sin consumidor (`hub.activity_prompts.nav` y `auth:nav_access_tokens`) y se borran en los tres idiomas, que es la regla «borra, no comentes»; el test viejo de `AppLayout` **fijaba que «Plataforma» no existiera**, que es justo lo que este prompt cambia, así que se actualiza dejando escrito que «Automatización» sigue fuera porque ese módulo no existe; y un test propio comprobaba el tab activo sobre todos los enlaces cuando el de la sección **también** lleva `aria-current` —un `NavLink` sin `end` marca la ruta padre, igual que en Chatbots y Curación—, así que se acota a la subnavegación. **Frontend completo: 519 passed, 0 failed** (`--no-file-parallelism`); `tsc` limpio. 15 tests nuevos |
| 2026-08-22 | PLAT.1 (RED/GREEN) — Nadie pierde acceso al partir el módulo | **Cursor: PLAT.1 ✅ → PLAT.2 (Sonnet).** **La premisa del prompt estaba medida a medias y se corrige en el propio código**: decía que «nadie tiene `plataforma` salvo los superadmin», y es falso para las cuentas que existían al aplicar INF.7 — su migración `j0c1d2e3f4g5` ya siembra **los cuatro módulos** a cada fila de `superadminaccount` y `adminaccount`. Comprobado en la BD de desarrollo: los dos sujetos que hay tenían los cuatro. Lo que sí quedaba descubierto, y es el motivo real de la migración: los sujetos con `chatbots` concedido **después** de INF.7, y **los usuarios de SSO**, que INF.7 no podía ver porque solo leyó las dos tablas de cuenta. Así que `k1d2e3f4g5h6` es un **relleno idempotente** («a quien tenga `chatbots` y no `plataforma`, dale `plataforma`») y no una siembra: en muchas instalaciones no hará nada, y eso es la propiedad que se busca. Un solo `INSERT ... SELECT` sobre la propia tabla, sin leer sujetos a Python ni depender de qué tablas de identidad tenga el despliegue. **Las filas van firmadas** (`granted_by = 'migracion:plat1'`) y eso no es cosmético: sin firma, `downgrade` no distingue lo suyo de una concesión que puso una persona y revertir se llevaría un permiso legítimo — hay test de las dos mitades. Y el `ON CONFLICT DO NOTHING` **no pisa `granted_by`** de una fila existente, porque es la auditoría del permiso y sobreescribirla la borra. **El catálogo se siembra antes que la concesión**: `modulos_del_usuario` cruza con los módulos `vigente`, así que sin la fila del catálogo el relleno habría sido decorativo — con su test, borrando la fila antes de migrar. Del RED salieron 5 rojos de 8 (tres pasaban ya porque «no tocar a quien no toca» se cumplía solo). **Y una trampa del entorno**: los tres tests del resolutor pasaban en verde falso porque el conftest raíz sustituye `modulos_del_usuario` por un doble que concede todo; el marcador `sin_guarda_de_modulos` devuelve la función real. `tests/infra` del fichero: **8 passed**; resolutor: **3 passed**. Migración aplicada, `alembic current` = `k1d2e3f4g5h6 (head)`. 11 tests nuevos |
| 2026-08-22 | La documentación de AutomatIA sale de `docs/` (fuera de plan) | **Cursor: sigue en PLAT.1.** El usuario preguntó si limpiar `docs/` ahora o en REPO, y al medirlo resultaron **tres problemas distintos**, no uno. **(A) Una isla cerrada de 17 ficheros que describen otro producto**: «Guía de Inicio Rápido - AutomatIA», «Manual de Usuario: Client Node», «DOCUMENTACIÓN API INTERNA (BRAIN)», un `docs/technical/database_schema.md` que documenta un `brain_server.db` de SQLite, y «AutomatIA V4.0». Son los 17 que **no se tocan desde el commit inicial**, y solo se referencian entre ellos y desde `docs/README.md`, que era el índice de AutomatIA: ni el plan, ni el código, ni CI, ni el `README` apuntaban ahí. El daño no era de cara a la publicación sino **del día a día**: quien abría `docs/technical/architecture_detailed.md` se llevaba una arquitectura que no es la de este proyecto, teniendo `docs/Arquitectura.md` al lado. **Cuarentena y no borrado, Caso A de `AGENTS.md`**: NIC.1 tiene que inventariar qué de `client_app/` está cubierto y estos documentos son la descripción de lo que había, así que el borrado es de NIC.5 y lo hace una persona. `docs/README.md` reescrito como índice real de los 41 que quedan, **clasificados por cómo envejecen** —referencia viva / decisión / instantánea—, que era lo que faltaba para poder leerlos sin equivocarse. **Guardarraíl con 36 tests**, y verificado que caza: añadido un fichero con un enlace a `quick_start.md`, el test falla señalando fichero y línea. **(B) Premisa caducada**: ocho documentos mencionan Cloud Run y el destino pasó a ser una VM. La mayoría son inocuos, pero `SANDBOX_SECURITY.md:114` declara una **«Capa 8 — gVisor»** que la daba la plataforma y **en una VM con Docker Compose no existe**: un documento de seguridad que cuenta una capa inexistente sobre el sandbox que ejecuta código generado por un LLM. Va antes de desplegar, como **prompt D.0.doc** nuevo en el bloque Deploy. **(C) Ciertos pero históricos** —los `COMPARATIVA_*`, `CAMBIOS_*`, `VALORACION_PROYECTO`, `AUDITORIA_PRE_DEPLOY`, `PRUEBAS_PENDIENTES`, la decisión descartada de OpenWebUI— a **REPO.3** nuevo, y a propósito: es una elección editorial que se toma mejor mirando al público, y algunos llevan **asuntos abiertos** dentro, así que ahí toca triar y trasladar lo pendiente, no borrar a bulto. De paso quedó anotado en el índice que `MCP_SERVER.md` y `mcp.md` se solapan. `tests/infra`: **152 passed** (eran 113). |
| 2026-08-22 | **PLAT e IDE se mueven delante del despliegue** (decisión del usuario) | **Cursor: pasa de Deploy/D.0 a PLAT.1.** Del usuario: «no tiene sentido hacer el deploy de una aplicación con una estructura y unos mecanismos de identificación que no son los que se van a utilizar. Primero IDE y después PLAT». El argumento se sostiene solo al inventariar qué cambian los dos bloques: **dos migraciones de esquema** (IDE.2 renombra `hub_sso_users`, IDE.5 cambia `hub_module_grants` y ensancha `subject_id`), **dos cambios de autorización** (IDE.1 la autoridad del rol, PLAT.5 los `require_module`) y **el cambio de todas las URL del panel** (PLAT.2). Las tres clases son baratas antes de que haya personas reales dentro y caras después: una migración sobre una base vacía no tiene riesgo, y cambiar la autorización cuando nadie depende de ella no echa a nadie fuera. **Una salvedad, que es dependencia y no preferencia**: el orden literal «primero IDE, después PLAT» no se puede cumplir, porque **las pantallas de IDE.4 e IDE.5 cuelgan de `/plataforma`, la sección que crea PLAT.2**, y porque mover «Modelos LLM» a una sección en la que un admin no puede entrar le quita un acceso que hoy tiene —lo reparte PLAT.1—. Así que se adelantan **solo esos dos prompts**, que ya estaban marcados como adelantables por otro motivo (arreglan solos el 403 del tab). **Orden vigente: PLAT.1 → PLAT.2 → IDE.1…IDE.5 → PLAT.3…PLAT.7 → Deploy → pruebas humanas y RAG.6b → REV → NIC → NIC.5 → REPO.** El argumento de por qué **Deploy va antes de NIC** queda intacto: lo que cambió es lo que va antes del despliegue, no lo que va después. **Tarea heredada por el bloque Deploy**: IDE.1 introduce el ajuste de la autoridad del rol, y el inventario de variables de entorno del despliegue tiene que incluirlo — omitirlo es desplegar con el valor por defecto sin que nadie lo haya decidido; anotado en la fila de Deploy. **Coste asumido y dicho**: 12 prompts entre el cursor y D.0, con el piloto retrasado lo que tarden. Los bloques se dejan escritos seguidos en el plan, no partidos: es más fácil de leer, y el orden de ejecución está en las notas de dependencia de los dos. Prompt de planificación: no hay código ni tests. |
| 2026-08-22 | Planificado el **Bloque IDE** — identidad y permisos, con la puerta al ERP abierta desde el principio | **Cursor: sigue en Deploy → D.0.** Nació de una pregunta del usuario: «la gestión de usuarios y permisos a módulos dónde está prevista?». **En ningún sitio**: `grep` sobre `planificacion/` no encuentra nada en Fase 1, 2 ni 3. Y la pregunta destapó un hueco en el Bloque PLAT escrito una hora antes: su PLAT.8 daba por hecha una lista de personas que **no existe** —`HubModuleGrant` dice en su docstring que «no hay una tabla de usuarios única» y su `subject_id` es un `uuid5` derivado del claim del token—, así que **PLAT.8 se retiró** y PLAT queda en 7 prompts. **El usuario precisó el requisito**: dar usuarios con roles a mano y que el SSO solo identifique, y en el futuro que los permisos vengan del ERP por un atributo del IdP. Al medirlo, **medio mecanismo ya estaba construido**: `role_mapping.resolve_role` resuelve el rol desde la aserción en tres niveles (atributo explícito → mapeo grupo→rol con precedencia por privilegio → defecto), los grupos viajan `_grupos(attributes)` → `UserInfo.saml_groups` → claim `groups` del JWT → `EffectiveActor`, y **ya tienen un consumidor en producción**: el modo `restricted` de un chatbot cruza `allowed_saml_groups`. **Los dos huecos exactos**: `_provision_sso_user` hace `sso.role = resolve_role(attributes)` **sin condición** en la rama del usuario existente —por eso hoy no se pueden dar roles a mano: no falta la pantalla, es que la pantalla no serviría—, y las concesiones solo apuntan a personas. **La decisión de diseño, del usuario**: no son dos sistemas, es declarar **quién es la autoridad** del rol (aplicación o IdP) y que el sujeto de una concesión pueda ser **persona o grupo**, con la semántica que ya usa `assert_chatbot_access` —rol o grupo, y el vacío es «nadie», no «todos»—; así pasar al modelo del ERP no reescribe nada. **Y el grupo entra desde el principio, también en la interfaz**, en contra de la lectura estricta del «sin código muerto especulativo»: la alternativa convertía el futuro en una migración en vez de un cambio de pantalla, y el usuario eligió pagarlo ahora. Detalles que salieron al escribirlo y que no se ven leyendo: `subject_id` es `String(36)` —dimensionado para un UUID, **no le cabe un nombre de grupo**—, `HubSsoUser` **ya es** una tabla de usuarios completa a la que solo le falta un escritor que no sea SAML, y si el alta manual crea la fila antes, el login por SSO **la encuentra** en vez de crearla, lo que arregla de paso que todo el mundo aterrice como `user` sin forma de promocionarlo. **Dos cosas fuera del bloque a propósito**: unificar las cuatro tablas de identidad (`user_to_uuid` está en la propiedad de los workspaces, en los PAT y en las concesiones, y `es_propietario` acepta las dos formas desde SEC.8.1) y repartir un IdP entre varias organizaciones (`SAML_ORGANIZACION_ID` lo asocia a **una**, decisión de seguridad correcta que se vuelve limitación con dos inquilinos). Prompt de planificación: no hay código ni tests. |
| 2026-08-22 | Planificado el **Bloque PLAT** — la administración de la plataforma, separada de la de Chatbots | **Cursor: sigue en Deploy → D.0.** Del usuario: «el apartado organizaciones aparece en el módulo Chatbots, pero en realidad se refiere a todos los módulos, y puede ocurrir lo mismo con los proveedores, los tiers por modelo». Medido contra el código, **no es una cuestión de menús: el backend y el frontend ya se contradicen**. `hub_llm_configs_router.py:32` exige `require_module("plataforma")` y `App.tsx` monta esa pantalla bajo `<RutaDeModulo modulo="chatbots">`, así que **hoy quien tiene `chatbots` y no `plataforma` ve el tab «Modelos LLM» y recibe un 403**, y quien tiene `plataforma` no llega a la única pantalla que ese módulo protege. El módulo `plataforma` **ya está en `MODULOS_INICIALES`**, se puede conceder y no abre nada: se retiró del menú porque era un `PlaceholderPage` y nunca se le dio contenido. Ocho hallazgos con su evidencia: `tier` lo consumen Informes (copiloto, scripts, borradores, workspaces), Curación (spider, crawler, reconocimiento) y Chatbots, y `HubLLMConfig` **no tiene `organizacion_id`** —es global, igual que `hub_providers`—; `OrganizacionRead` mezcla **tres ámbitos** (identidad del inquilino, tema, y una docena de `default_*` de RAG), que es exactamente por lo que la pantalla acabó bajo Chatbots y por lo que **no basta con moverla de tab**; el `<textarea>` «Configuración de tema (JSON)» escribe `HubOrganizacion.theme_config` y **ningún código lo lee** —la cascada resuelve desde `hub_themes`—, así que lo que parecía la pantalla de tema es un callejón sin salida; `AIBrainPage` importa plantillas de prompt **y** configuraciones LLM sin añadir recurso propio (tres pantallas sobre dos recursos); y `/hub/reports` es la revisión de interacciones de chatbots y choca de nombre con el módulo Informes. **El criterio no hubo que inventarlo**: la frontera edge/cloud de `AGENTS.md` ya lo da —`Deploy: cloud` es configuración de plataforma, `Deploy: edge` es operación de un módulo—, y `hub_llm_configs`, `hub_organizaciones` y `hub_themes` son los tres `cloud`. **7 prompts** (eran 8: la pantalla de concesión de módulos salió al Bloque IDE al comprobar que no se puede construir sobre lo que hay), con dos restricciones de orden que no son opcionales: **PLAT.1 (migración de concesiones) va primero**, porque PLAT.5 declara la frontera en el backend y sin la migración deja fuera a todo admin no-superadmin de un día para otro; y **PLAT.7 va después de PLAT.6**, porque retirar el `theme_config` antes de que exista la pantalla que lo sustituye deja a la organización sin forma de configurar su tema. **PLAT.1+PLAT.2 son adelantables**: dos prompts, ningún contrato tocado, y arreglan por sí solos el 403. **Sin decidir: dónde entran PLAT e IDE en la secuencia** (propuesta: entre NIC y REPO, IDE antes que PLAT). Prompt de planificación: no hay código ni tests. |
| 2026-08-22 | La marca institucional sale del código y entra en la cascada visual (fuera de plan) | **Cursor: sigue en Deploy → D.0.** Salió revisando qué hacía la carpeta `Diseño/` de la raíz. Esa era inocua —`*.png` la ignoraba y no la referenciaba nadie; movida a `_local/`—, pero al mirar dónde vivían de verdad los logotipos apareció el problema: **`AppLayout.tsx` importaba `@/assets/logo-uji.png`** y lo pintaba con `alt="Universitat Jaume I"` en la barra lateral de todo despliegue, así que cualquier ayuntamiento que clonara el principal arrancaba con la marca de una universidad ajena. Y no estaba ahí por descuido: la excepción `!frontend/src/assets/*.png` de `.gitignore` **más** el guardarraíl `assetsVersionados.test.ts` **garantizaban** que ese fichero viajara al repositorio. **La cascada ya existía** —`hub_themes` con `organizacion_id` nulo para plataforma— y el docstring de `get_theme` ya decía que un tema «lleva los colores, **el logotipo** y el nombre de la organización»: el logotipo era lo único que no llevaba, así que esto no añade mecanismo, mete la marca en el que había. **Sin migración, y es un dato del plan que resultó falso**: `hub_themes.config` ya es JSONB, así que `ThemeBranding` (logoUrl / logoAlt / logoContentType) entra sin tocar el esquema — desviación documentada frente a la «migración Alembic» que la decisión daba por hecha. `GET /hub/themes/resolved` fusiona **en profundidad** plataforma → organización (con reemplazo, poner un logotipo costaría volver a declarar la paleta entera) y devuelve **solo `config`**, con la disciplina de SEC.5; va registrado **antes** de `/{theme_id}`, porque `_assert_id_de_tema` rechaza lo que no es UUID y al revés contestaría «identificador inválido» haciendo pasar el fallo por culpa del cliente. La subida reutiliza `validate_upload` de SEC.6 con un `UploadKind.IMAGE` nuevo en vez de un validador paralelo: manda la **firma real** y no el `content_type` del multipart, **sin SVG** —lleva guion dentro y se serviría desde el origen de la API— y **sin WebP**, porque su firma está partida (`RIFF` en el byte 0, `WEBP` en el 8) y la comprobación es un `startswith`: aceptar `RIFF` a secas dejaría pasar cualquier contenedor RIFF. El fichero va por **`StorageService`** y el logotipo se sirve **público a propósito**: un `<img>` no manda `Authorization` y en el widget público no hay sesión que exigir; devuelve bytes y nada más. **Y un defecto en un guardarraíl que ya existía**: `assetsVersionados.test.ts` escanea el texto del fichero con comentarios incluidos, así que el comentario que explicaba por qué se retiraba el asset lo ponía en rojo — la misma trampa que `test_theme_persistence.py` ya había documentado («buscar la cadena obligaría a no poder contarla»); los dos guardarraíles quitan comentarios antes de buscar, con `test-utils/codigoSinComentarios.ts`. Fuera `logo-uji.png` y además `hero.png`, `react.svg` y `vite.svg`, que no los referenciaba nadie (Caso B). **Suites completas: backend 2696 passed, 1 skipped; frontend 504 passed** (`--no-file-parallelism`). **Verificado en navegador contra el servidor real**, las dos ramas: con marca, el logotipo carga desde `/api/v1/hub/themes/{id}/logo` a 181×29 px y `complete=true`; sin marca, sale «Gov Gen AI Platform» como texto y ningún `<img>`; `/resolved` 200, logotipo 200 sin token, consola sin un solo error. **Queda sin interfaz de subida**: hoy la marca se configura por API, y eso es lo que recoge `pruebas_manuales_promptMARCA.bat`. 21 tests nuevos de backend + 4 de marca en el panel + 2 de guardarraíl. |
| 2026-08-22 | Ficheros generales del repositorio: módulos reales, principio rector, licencia en español y `AGENTS.md` (fuera de plan) | **Cursor: sigue en Deploy → D.0.** Cuatro correcciones del material que verá quien llegue al repositorio, las cuatro por observación del usuario. **(a) El README anunciaba cuatro módulos y uno no existe.** `server/app/modules/automation/` son tres ficheros de infraestructura que consume Informes —`cortex.py`, `extraction_strategies.py`, `llm_gateway.py`—, no hay `frontend/src/automation/` y `main.py` dice literalmente que sus routers «quedan pendientes de registrar»; además el README callaba **Expedientes**, que está en el mismo estado. La tabla pasa a **tres módulos construidos** (Chatbots, Informes, Curación) y los dos previstos se nombran los dos, aparte y dichos como lo que son. Se movió a la fila de Informes lo que sí existe y estaba atribuido a Automatización (extracción determinista, transformación declarativa, gráficos, sandbox sin red), que es donde vive. **(b) Faltaba el principio rector.** Se añade **conformidad por diseño** (*compliance by design*), materializado como *compliance as code*, con las consecuencias que explican decisiones que sin él parecen caprichos técnicos —determinismo primero, ninguna afirmación sin fuente, la indisponibilidad como función y no como fallo, la frontera edge/cloud vigilada por tests en vez de prometida en un contrato— y el encaje normativo (RIA, RGPD, 39/2015 y 40/2015, ENS), remitiendo a `docs/PRESENTACION_PROYECTO.md` y `docs/MARCO_GOBERNANZA_IA.md`. **(c) Licencia en español: `docs/LICENCIA_ES.md`, explicación y no traducción.** La FSF no aprueba traducciones de sus licencias y una traducción propia sin revisión jurídica crea un segundo texto que acaba diciendo otra cosa; el documento explica qué permite, qué obliga (§4, §5, §6, §11, §13), **qué no obliga** —la confusión que paraliza decisiones: ningún copyleft obliga a contribuir aguas arriba— y qué significa para un pliego (art. 157 Ley 40/2015, compatibilidad con EUPL v1.2), con descargo arriba de que el único texto vinculante es `LICENSE`. **(d) `CLAUDE.md` → `AGENTS.md`.** Las reglas valen para cualquier agente de programación, no solo para Claude Code, y el nombre del fichero decía lo contrario. El contenido pasa a `AGENTS.md`; `CLAUDE.md` queda como stub de tres líneas con `@AGENTS.md`, porque Claude Code busca ese nombre por convención y así no hay dos versiones que puedan divergir. Actualizadas las referencias de `README.md`, `CONTRIBUTING.md` y `docs/*.md`; **`planificacion/*` y los docstrings de tests se dejan intactos** —son registro de lo que se escribió entonces, y el stub hace que sigan resolviendo—. Ningún workflow de CI, hook ni test referencia la ruta: las 16 menciones que quedan en `server/tests/` son docstrings. Cambio solo de documentación, sin código. |
| 2026-08-21 | Alineación de la versión de Python antes de D.0 (fuera de plan) | **Cursor: sigue en Deploy → D.0.** Al decidir el orden del bloque Deploy aparecieron **tres** versiones de Python en juego: el `Dockerfile` construía builder y runtime sobre **3.11**, mientras CI, `server/.python-version` y el desarrollo van en **3.13**, y el sandbox de scripts en 3.12. Se probaba en un intérprete y se habría enviado otro, con un `uv.lock` resuelto para 3.13. **El sandbox no se toca**: declara `requires-python = ">=3.12"` y tiene su propio `uv.lock`, así que ahí 3.12 es coherente y no deriva. **Verificado construyendo la imagen, no leyendo el fichero**, porque el riesgo concreto era `python3-saml` → `xmlsec`, que necesita `libxmlsec1-dev` en Debian y el builder no lo instala: sin rueda `manylinux` para cp313 el build reventaba. Dentro de la imagen: **Python 3.13.15**, `xmlsec 1.3.17` y `onelogin.saml2` importan, las diez extensiones nativas importan, y `server.app.main` se importa con **163 rutas registradas** (imagen de 503 MB, retirada al terminar). **Tres guardarraíles nuevos** en `test_compose_production.py`, con RED previo: la versión del `Dockerfile` coincide con el pin, todas sus etapas usan la misma —un builder y un runtime distintos copian un `.venv` compilado para otro intérprete, y eso no falla al construir sino al importar— y CI prueba en la versión del pin. **`requires-python` NO se toca**: sigue en `>=3.11` en los cinco pyproject, y estrecharlo obliga a re-resolver cinco locks, que no debe viajar en un commit de preparación del despliegue. Queda como paso propio. 22 passed en el fichero. |
| 2026-08-21 | Preparación del repositorio para abrirlo (fuera de plan) + planificación de los **Bloques NIC y REPO** | **Cursor: Bloque NIC, prompt NIC.1 (Opus).** No es un prompt del plan: es el trabajo de dejar el repositorio publicable, y deja dos bloques nuevos al final de la Fase 1 con un orden que el usuario fija expresamente — **NIC (retirada del legacy NiceGUI) antes de REPO (sustituir el repositorio de GitHub)**, porque no tiene sentido montar el repositorio definitivo y acto seguido meterle la retirada de 500 ficheros. **La raíz pasó de 89 entradas a 15 ficheros y 16 directorios**: documentación a `docs/`, planes y cursor a `planificacion/`, los 15 `.bat` a `pruebas_manuales/` (cada uno con `cd /d "%~dp0.."`), datos de trabajo a `_local/` (ignorada), y borrado el código muerto (`desarrollo/` con sus 28 scripts de indentación de NiceGUI, siete scripts de un solo uso, `MAP.md`, `utils/`, `styles/`, `tmp/`, `copia/`, `data_test/`, `.benchmarks/`, y `Instrucciones_agente_programacion.md`, que era una versión temprana de `CLAUDE.md` y la contradecía). **`HISTORIAL.md` nace aquí**: era el 85% de los 626 KB de `PROJECT_STATE.md`, que se lee entero al arrancar cada sesión; queda en 104 KB y la regla de cierre de prompt en `CLAUDE.md` apunta ya a este fichero. **Datos personales fuera del historial de git**: `logs/` tenía 45 volcados `llm_anonymized_input_*.txt` cuya anonimización sólo sustituía las entidades detectadas —el cuerpo de las solicitudes de subvención era real, y `FACULTAD DE CIENCIAS JURIDICAS Y ECONOMDuilio` delata un reemplazo pisando una palabra a medias—; retirados de los 430 commits con `git filter-repo` y push forzado, verificado que **0 de los 4.168 blobs alcanzables** contienen su cabecera. **AGPL-3.0-or-later** con el texto íntegro de la FSF (no transcrito: descargado y verificado), titularidad UJI, procedencia **INNOVAP (Derecho Público e Innovación)**, autoría de Modesto Fabra; el `LICENSE` no se modifica porque su propia cabecera lo prohíbe, así que procedencia y propósito van en el README. **Gobernanza en genérico** —un principal y el fork de cada organización que despliega, sin fork privilegiado— y la distinción que el texto anterior borraba: **la AGPL no obliga a contribuir aguas arriba**; obliga a dar el fuente de tu versión a los usuarios de tu despliegue (§13). **DCO obligatorio, también en el principal**, con `.github/workflows/dco.yml` que revisa PRs y push a `main` y **no usa `grep -q`** a propósito (cerraba la tubería y abortaba, rechazando commits que sí iban firmados). **Acciones de CI a su major actual**; `setup-uv` fijada a `v10.0.1` porque dejó de publicar mayor flotante tras v7 —lo descubrí rompiendo CI con `@v10`—. **Un rojo de CI arreglado que venía de INF.4**: el mock de `redaccion-llm-draft-preview.a11y.test.tsx` no tenía `useDescribeSampleFile`, y `npm test` **no incluye** la suite de accesibilidad, así que se cerró INF reportando verde sin haberla corrido. **Y un defecto de la página de demostración**: `docs/chatbots-publicos/demo-uji.html` apuntaba a un PNG que la regla `*.png` mantenía fuera del repositorio, fallando en silencio por su `onerror`; el logotipo pasa a vivir junto al HTML, con guardarraíl nuevo verificado en los dos sentidos. `tests/infra`: **113 passed** (eran 111). |
| 2026-08-20 | INF.10 (RED/GREEN) — El copiloto sabe en qué informe está · **cierra el Bloque INF** | **Cursor: Bloque INF completo.** El usuario le preguntó «no sé dónde aprobar los bloques» y no obtuvo respuesta: el copiloto responde con RAG **sobre la documentación del proyecto** y sabía en qué **tipo** de contexto estaba («informe» o «flujo») pero no **cuál**, así que no podía hablar del informe que había delante. El legacy sí lo tenía —`client_app/app/core/state.py:79` lo dejaba anotado: los datos cargados se publican al estado «para que el Copiloto conozca las columnas disponibles»—. `services/contexto_del_informe.py` compone el estado del informe abierto —estado general, datos de partida que faltan, cada apartado con su situación y **sus `acciones_permitidas` de INF.2**, qué está pendiente de una persona y dónde se hace, y los avisos abiertos—, y el prompt le dice que si eso responde a la pregunta contteste con la acción concreta de **este** informe y que avise de lo pendiente aunque no se lo pregunten. **Dos reglas de forma, las dos con su test**: se manda el **estado y no el contenido** —el texto que escribió la IA no hace falta para «dónde apruebo esto», y no mandarlo es la diferencia entre un contexto de veinte palabras y uno de veinte mil—, y los avisos **se anonimizan**, porque los escribe el grafo sobre datos del cliente y el modelo puede estar en la nube. El contexto de un informe ajeno no se filtra por una pregunta al copiloto: misma comprobación de propiedad que el resto del módulo desde SEC.8.1. **Dos guardarraíles del proyecto cazaron cambios míos, y los dos tenían razón**: `test_edge_boundary` exigía declarar que las tablas de INF.7 son configuración cloud→edge (lo son: el edge necesita saber si quien pide un informe puede pedirlo), y CAL.5 exigía carga por ruta para `Aterrizaje` y `SinAcceso` — quedan en la lista de excepciones con su motivo, porque partir el componente que decide a dónde vas añade un viaje al servidor antes de poder ir a ningún sitio. **Suites completas del cierre de bloque: backend 2670 passed, 1 skipped, 0 failed; frontend 74 ficheros, 497 passed, 0 failed.** 9 tests nuevos |
| 2026-08-20 | INF.9 (RED/GREEN) — Plantilla o informe suelto, explicado al elegir | **Cursor: INF.9 ✅ → INF.10 (Opus).** Del usuario: «no sabe la diferencia entre workspace y plantilla; por el principio de determinista first debería sugerirse plantilla por defecto si se va a repetir el informe». La pantalla ofrecía «Crear workspace» y «Crear plantilla» como equivalentes, con vocabulario interno y sin decir que la elección importa: una plantilla se reusa y su parte determinista se ejecuta igual cada vez, un informe suelto se tira — la diferencia entre pagar el LLM una vez y pagarlo cada mes. Ahora se llaman «Plantilla reutilizable» e «Informe de una sola vez», la plantilla va **marcada como recomendada y es el defecto**, y debajo se dice **por qué** («se ejecuta sin volver a consultar a la IA»). La palabra «workspace» desaparece de los textos visibles, incluidos los dos de `autosave` que hablaban de «este workspace». **Dos correcciones que trajeron los tests, y las dos eran de comportamiento**: marcar la plantilla como recomendada mientras el modo por defecto seguía siendo el informe suelto es recomendar de boquilla; y poner plantilla por defecto sin más habría hecho que **un trabajador no admin creara plantillas sin saberlo**, porque el selector solo se ofrece a admin y superadmin — el defecto pasa a depender de quién pregunta. Frontend `src/redaccion` + `src/shared`: **208 passed**. 4 tests nuevos |
| 2026-08-20 | INF.8 (RED/GREEN) — Que los controles parezcan controles | **Cursor: INF.8 ✅ → INF.9 (Sonnet).** Los tres defectos que el usuario no reconoció como interfaz. (1) **«No se sabe que *Tria un fitxer* es un botón»**: era un `<input type="file">` desnudo, así que el navegador pintaba su propio control **en su idioma** —de ahí el catalán en una pantalla en castellano—. Ahora hay zona de arrastre con su texto, los formatos aceptados, el nombre del fichero elegido y la marca de obligatorio; el `input` sigue siendo el que recibe el fichero y se oculta con `sr-only` y **no** con `display:none`, para no sacarlo del orden de tabulación ni del lector de pantalla. (2) El submit del contrato **iba sin una sola clase**, así que el único disparador del informe parecía texto. (3) **El cajón**: se declaraban cuatro pestañas y solo `copilot` tenía panel, así que tres salían en blanco; se retiran —una pestaña vacía no es una promesa, es un fallo aparente— y volverán con su contenido. Y su aspa era peor que estética: el `<aside>` es `role="dialog" aria-modal` dentro de un `FocusLock`, o sea que **el foco quedaba atrapado sin control de cierre y sin Escape**; ahora hay aspa, Escape cierra y el foco vuelve. **Dos tests codificaban el defecto**: `should_render_informe_tabs_...` y `should_render_flujo_tabs_...` exigían que existieran las cinco pestañas sin panel, y se han reescrito como la regla que faltaba —no se declara una pestaña sin panel—. Fuera los strings incrustados de `DrawerHub` y `GenericReportWizard`. **Y una cosa que no es de interfaz**: `arranque.bat` levantaba el backend con `--host 0.0.0.0`, publicándolo en toda la red — en el log de las pruebas se veían decenas de `GET /?${jndi:dns://MDEDiscovery...}`, el escáner de Microsoft Defender sondeando el puerto con payloads de log4shell. Ahora `127.0.0.1`. **El hallazgo dudoso queda descartado con evidencia**: el estado de cada bloque se ve **una sola vez** en pantalla (comprobado en navegador al cerrar INF.1); lo que se copiaba dos veces era el `<span className="sr-only">` de `WorkspaceEditor.tsx:74-76`. Frontend `src/shared` + `src/redaccion` + `src/admin`: **340 passed**, `tsc` limpio. 5 tests nuevos |
| 2026-08-20 | INF.7 (RED/GREEN + migración) — Acceso por módulos concedidos | **Cursor: INF.7 ✅ → INF.8 (Sonnet).** Hasta aquí **no había control de acceso por función**: `App.tsx` metía todas las rutas bajo un `PrivateRoute` que solo comprobaba que hubiera sesión, cero `role ===` en la navegación, aterrizaje fijo en `/hub/chatbots` y routers de `redaccion` con `get_current_user` a secas — cualquier trabajador con cuenta podía listar y editar los chatbots institucionales y la configuración de LLM. Resuelto **por módulos concedidos, no por rol nuevo** (decisión del usuario). El catálogo (`chatbots`, `curacion`, `informes`, `plataforma`) vive **en tabla con `vigente`**, no como `Enum` ni `CheckConstraint`: misma regla que el vocabulario del corpus, porque si los módulos fueran código añadir uno exigiría migración y despliegue. Las dos tablas van en **`HubConfigBase`** —no en la metadata de SQLModel, donde las puse primero y el fixture de tests no las creaba—: son configuración administrativa que el cloud decide y el **edge necesita** para saber si quien pide un informe puede pedirlo, o sea exactamente lo que esa base sincroniza. El sujeto de la concesión es el id normalizado con `_actor.user_to_uuid`, porque no hay tabla de usuarios única: hay `superadminaccount.admin_id` (entero) y `adminaccount.partner_id` (texto). **El superadmin entra en todo por su rol, sin fila**: hacerlo depender de datos deja una instalación recién creada con un superadmin encerrado fuera de todo, porque `bootstrap` crea la cuenta y no sus permisos. `/auth/me` devuelve `modulos` y el frontend **genera menú, rutas y aterrizaje iterando esa lista**; mientras la lista no ha llegado no se echa a nadie —tratar «no lo sé» como «no» expulsaría a la gente en cada recarga—, y sin ningún módulo hay una pantalla que lo explica en vez de un rebote. Migración **aplicada**: `j0c1d2e3f4g5 (head)`, 4 módulos y 8 concesiones sembradas (los 2 administradores existentes × 4). La semilla se calcula **en Python** dentro de la migración: en SQL haría falta la extensión `uuid-ossp` y las tablas de cuenta no tienen columna `id`. **Dos correcciones por el camino**: las columnas de fecha salieron *naive* con defaults *aware* (reaplicada la migración con `timezone=True`, como el resto del esquema); y la guarda a nivel de router dejó **73 rojos**, de los cuales varios eran **500 y no 403** porque los tests que mockean la sesión con `AsyncMock` no podían responder a la consulta de concesiones — un fallo de infraestructura disfrazado de decisión de autorización. Arreglado extrayendo `modulos_concedidos` como dependencia propia y parcheando **la función del servicio** en un fixture autouse (no `dependency_overrides`, porque varios tests montan su propia `FastAPI`), con el marcador `sin_guarda_de_modulos` para los que sí prueban la guarda. Backend `tests/api` + `tests/modules/redaccion` + higiene: **961 passed, 1 skipped**, y los 2 restantes son los flakes del sandbox ya conocidos (18/18 aislados). Frontend `src/admin` + `src/shared` + `src/redaccion`: **335 passed**, `tsc` limpio. 21 tests nuevos |
| 2026-08-20 | INF.6 (RED/GREEN) — Corregir la propuesta, no rehacerla | **Cursor: INF.6 ✅ → INF.7 (Opus).** Con la propuesta rechazada, «Aprobar y crear» quedaba deshabilitado y la única salida era **reescribir el prompt entero**: el usuario se quedó ahí el 2026-08-20 con dos líneas rojas que decían `blocks[b6].data_block_refs` y ningún sitio donde tocar. Tres cambios. (1) **La propuesta pasa a ser editable en la pantalla**: antes se usaba directamente lo que devolvió el modelo, así que una propuesta inválida solo se podía tirar; con una copia local, corregir y revalidar cuesta una llamada a `/validate` y **ninguna al modelo** (test explícito de que `propose` no se vuelve a llamar). (2) **Los errores se pintan junto a su bloque** —la pantalla no mostraba los bloques, solo perfil, secciones y modelo— y en lenguaje de quien pide un informe: «esta valoración se apoya en una tabla o un gráfico, que solo dibujan datos», en vez de `blocks[b6].data_block_refs`. Los errores que no hablan de un bloque concreto conservan su sitio. (3) **El arreglo mecánico se ofrece como acción**: `utils/erroresDeLaPropuesta.ts` deduce el destino correcto de la propia propuesta —`b4` es un `TABLE` cuyo `data_block_ref` es `b3`, así que la valoración va a `b3`—, deduplica cuando dos referencias dibujan el mismo dato, y **devuelve `null` cuando hay algo que decidir** (una referencia a un bloque inexistente no se arregla sola). Y el progreso: una espera de treinta segundos con el botón deshabilitado y nada más se lee como una pantalla colgada, así que se dice que hay un modelo trabajando. Retirados los seis strings incrustados que quedaban en la pantalla (`'Generar propuesta'`, `'Aprobar y crear'`, los dos modos, los dos placeholders). Frontend `src/redaccion` + `src/shared`: **192 passed**, `tsc` limpio. 13 tests nuevos. **Nota de método**: en `erroresDeLaPropuesta.ts` escribí test e implementación en la misma pasada, así que **el RED no se observó**; los tests de la pantalla sí fueron RED primero |
| 2026-08-20 | INF.5 (RED/GREEN) — El prompt deja de inducir el error que el validador castiga | **Cursor: INF.5 ✅ → INF.6 (Opus).** Los dos errores rojos que dejaron al usuario sin poder aprobar (`AI_SUMMARY 'b6' valora 'b4', que no produce datos ('TABLE')`) **los causaba el texto del prompt**, no el modelo: `llm_spec_service.py` describía `data_block_refs` como «the table or tables this passage is about», y para el modelo la «tabla» del informe es el bloque `TABLE`, mientras el validador exige el bloque que **produce** los datos. Reescrito: los ids de bloques `DETERMINISTIC_DATA` o `DATA_TRANSFORM`, **nunca** un `TABLE` ni un `CHART` —«son presentaciones de datos que ya existen en otro sitio, no tienen datos propios»— con un ejemplo mínimo de la forma correcta y de la incorrecta. Mismo repaso a las otras dos referencias que el validador comprueba con la misma regla: `data_block_ref` de TABLE/CHART y `config.source_block_ref` de DATA_TRANSFORM. **Y el test que faltaba**: la propuesta completa del caso real de tesorería —datos, `to_number`, tabla, gráfico, valoración anclada y puerta de revisión— **valida sin errores**; su versión con la valoración anclada al TABLE **sigue siendo rechazada** (guardarraíl: si dejara de fallar, se habría aflojado el validador en vez de arreglar el prompt); y con un doble del modelo, el servicio la parsea de punta a punta sin estropearla. De paso, tres detalles del contrato que mi propio fixture tenía mal y que ahora quedan fijados en un test: `to_number` toma `columns` en plural, y `ReportTemplateDraft` exige `model_used` y `prompt_version`. Backend `tests/modules/redaccion` + higiene: **646 passed, 1 skipped**. 6 tests nuevos |
| 2026-08-20 | INF.4 (RED/GREEN + recuperación de legacy) — La IA propone sobre datos que ha visto | **Cursor: INF.4 ✅ → INF.5 (Sonnet).** `propose` recibía **solo** `prompt_nl`: ninguna muestra de datos, así que pedirle la estructura de un informe sobre un CSV cuyas columnas no ha visto era pedirle que adivinara —y adivinó mal—. El legacy sí lo mandaba: `client_app/app/modules/factory/etl_factory.py:146-200` componía `columns`, `dtypes`, `row_count` y `df.head(3)`, **anonimizado antes de enviarlo** (línea 152). Lo señaló el usuario; no se deducía del código. `services/muestra_de_datos.py` nuevo recupera ese diseño: lee `.csv`/`.xlsx`, resume columnas, tipos inferidos y filas totales, y **anonimiza los valores** de las tres primeras filas con `AnonymizationContext` —en el servicio edge, no en `model_factory`, por la frontera de `CLAUDE.md`—. El CSV se lee con `dtype=str` a propósito: convertir «1.234,56» aquí escondería justo lo que el modelo tiene que ver para poner el `to_number` antes de sumar. **Desviación documentada**: el plan decía «`propose` acepta un fichero opcional» y se ha resuelto con un endpoint aparte, `POST /llm-drafts/sample` (multipart) que devuelve la muestra ya anonimizada, más `muestra` en `ProposeRequest`. Dos razones: `/propose` sigue siendo un contrato JSON para quien ya lo usa, y la pantalla puede **enseñar lo que se va a enviar antes de enviarlo**, que en una herramienta cuyos usuarios desconfían de mandar datos a un LLM no es un detalle. El fichero no se guarda: se lee, se resume y se descarta. El contexto que llega al modelo declara además que **esas son las únicas columnas que existen** y que no invente nombres, porque sin decirlo completa con campos plausibles y la extracción falla. Sin fichero, nada cambia (test explícito). Backend `tests/modules/redaccion` + higiene: **640 passed, 1 skipped**. Frontend `src/redaccion` + `src/shared`: **179 passed**, `tsc` limpio. 12 tests nuevos. Contrato regenerado (134 paths) |
| 2026-08-20 | INF.3 (RED/GREEN + navegador) — El bloqueo se explica y tiene salida | **Cursor: INF.3 ✅ → INF.4 (Opus).** «Exportar a Word no hace nada» hacía: pedía el DOCX, recibía 409 y se callaba. Dos causas encadenadas. (1) El mensaje se guardaba en un estado que se pintaba **dentro de la sección del contrato**, así que en un informe ya generado no se pintaba en ningún sitio; ahora hay **un solo aviso, fuera de esa sección**, con el motivo y `pending_block_ids` convertidos en **enlaces al bloque** —`WorkspaceEditor` gana el ancla `id="bloque-<id>"`—. (2) Y el motivo era **ilegible**: con `responseType: 'blob'` axios entrega el cuerpo de **error** también como Blob, así que `error.response.data.detail` era `undefined` y no había forma de distinguir «hay que aprobar dos apartados» de un fallo cualquiera; `download.ts` reconstruye el JSON. La vista previa gana vuelta explícita al informe —se monta fuera del layout a propósito, pero sin salida no es una vista de impresión, es una trampa— y pierde su último string incrustado (`'Imprimir / Exportar PDF'`). **Y un hallazgo que solo salió al verificar en navegador**: la explicación del 409 en la vista previa **no aparecía**, y no era la forma del error —el servidor manda exactamente `{"detail":{"pending_block_ids":[...]}}`— sino que **`retry: 2` estaba puesto para todas las consultas**: un 409 es una decisión deliberada, reintentarla no la cambia, y la consulta se quedaba en `fetchStatus: "paused"` con `status: "pending"` —ni cargando ni en error—, así que la pantalla no podía pintar ni «cargando» ni el motivo. `shared/api/reintentos.ts` nuevo: los 4xx no se reintentan, los 5xx y las caídas de red sí. Además, el aviso de INF.2 que quedó anotado: **regenerar no reescribe el texto en el momento** —saca el bloque de `failed` y el modelo lo redacta en la siguiente generación—, y ahora la pantalla lo dice. **Verificado en navegador**: con `v_tesis` pendiente, exportar muestra «Antes de esto hay que aprobar los apartados escritos por la IA» + enlace `#bloque-v_tesis` (ancla presente), y la vista previa explica el bloqueo con el bloque enlazado y «Volver al informe». Frontend `src/redaccion` + `src/shared`: **174 passed**, `tsc` limpio. 12 tests nuevos |
| 2026-08-20 | INF.2 (RED/GREEN + navegador) — El servidor dice qué se puede hacer con cada bloque | **Cursor: INF.2 ✅ → INF.3 (Sonnet).** El módulo tenía **cuatro** reglas distintas para la misma pregunta, no dos: `preview_builder` consideraba pendiente todo bloque de IA fuera de `{approved, locked}`; `AIBlockReviewPanel` solo el que estaba en `needs_review`; `FinalAssemblerNode` se bloqueaba **solo si un bloque `required` estaba `failed`** —y `required` es `False` por defecto en todo bloque, así que en una plantilla propuesta por un modelo la guarda no se disparaba jamás, de ahí el `assembled` con dos bloques fallidos y la insignia «Listo para exportar» sobre algo que devolvía 409—; y `patch_workspace_block`, **el endpoint que usa el panel**, escribía el estado a pelo desde su propio `_ACTION_TO_STATUS`, sin pasar por la máquina de estados y con `regenerate → extracted` donde el endpoint validado dice `ai_generated`. `services/block_actions.py` nuevo: `acciones_permitidas` **se deriva de `_VALID_TRANSITIONS`**, no de una lista a mano, con un test parametrizado sobre los 9 estados que falla si se ofrece algo que la máquina rechazaría. Consecuencia inmediata: **el panel pintaba «Regenerar» sobre bloques `needs_review` y esa transición no existe** —un test lo codificaba como comportamiento correcto y se ha corregido—. `BlockStateOut` gana `acciones_permitidas`; el panel deja de filtrar por `status` y itera la lista, con el orden que manda el servidor; el endpoint ciego ahora **hace cumplir lo que anuncia** (422 con la lista permitida); y el ensamblador usa `bloques_pendientes`, la misma función que la exportación, así que `assembled` significa «se puede exportar» y si queda IA sin aprobar el informe queda `in_review`. De paso, tres duplicados retirados: `_TIPOS_DE_IA` estaba definido en tres módulos, los estados aprobados en dos, y `BlockStateOut` se construía campo a campo en dos endpoints. **Verificado en navegador reproduciendo el estado exacto del usuario** (los dos bloques de IA en `failed` y el workspace en `assembled`): el panel deja de decir «todos aprobados», los pinta con Editar/Rechazar/**Regenerar** y sin Aprobar; regenerar saca el bloque de `failed`; al relanzar quedan en `needs_review` con Aprobar y el workspace en `in_review` con la vista previa en 409 —coherentes por primera vez—; y aprobando los dos desde el panel, **`preview = 200` y `export = 200`**. Backend `tests/modules/redaccion` + `tests/api` + higiene: **943 passed, 1 skipped**. Frontend `src/redaccion` + `src/shared`: **162 passed**, `tsc` limpio. 22 tests nuevos de backend y 6 de frontend. **Pendiente anotado para INF.3**: «Regenerar» cambia el estado pero no reescribe el texto —eso pasa en la siguiente generación— y la pantalla no lo dice |
| 2026-08-20 | INF.1 (RED/GREEN + navegador) — Un solo camino para generar el informe | **Cursor: INF.1 ✅ → INF.2 (Opus).** La pantalla tenía **dos disparadores de la generación y el destacado se saltaba la subida**: el usuario pulsó ese, el informe se ejecutó sin datos y nada lo dijo. Retirado; el submit del contrato es el único camino, y un `grep` en test falla si alguien reintroduce otro. La guarda del servidor **ya existía y estaba apagada**: `start_run` se llamaba con `validate=False` y `validate_inputs` era un stub que solo detectaba «no se ha subido nada» reportando el literal `<all_required>` —su propio docstring dejaba anotado el cruce contra `required_slots` como pendiente—. Ahora cruza de verdad y el 422 llega **estructurado** (`detail.missing_slots`), que es lo que permite señalar el campo. **Dos huecos del contrato cerrados**: `UIDropzoneDescriptor` no tenía `required` mientras `UIFieldDescriptor` sí, así que la pantalla sabía qué texto era obligatorio y no qué fichero (se **deriva al servir** desde `input_contract`, porque las versiones son inmutables y las ya guardadas no llevan el campo); y `WorkspaceOut` gana `uploaded_slots`, sin el cual el camino único obligaría a resubir el mismo fichero en cada reejecución. **Un bug adyacente encontrado por el RED**: `TIPOS_DE_FICHERO` era una lista escrita a mano que **se quedó corta cuando SEG.3 añadió `markdown`**, así que un slot de markdown salía como campo de texto y un fichero no se puede teclear; ahora se deriva de `_ACEPTA`. **Verificado en navegador contra el workspace real del usuario**: sin fichero, «Informe resumen: falta el fichero» con `aria-invalid` y **cero peticiones al servidor**; con fichero, `POST inputs/datos 201` → `POST run 202` en ese orden; tras ejecutar, `t_matricula`/`t_tesis` pasan de «Datos pendientes» a `extracted` y los dos bloques de IA a `needs_review`, con lo que **el panel de revisión ofrece Aprobar/Editar/Rechazar/Regenerar** donde antes anunciaba «todos aprobados» —el callejón sin salida del bloqueo B desaparece por este camino; el caso general sigue siendo INF.2—. **Y queda descartado el único hallazgo dudoso del análisis**: el estado por bloque se ve **una sola vez** en pantalla; lo que el usuario copió dos veces era el `<span className="sr-only">` de `WorkspaceEditor.tsx:74-76`. Backend `tests/modules/redaccion` + `tests/api` + higiene: **923 passed, 1 skipped**. Frontend `src/redaccion` + `src/shared/i18n`: **126 passed**, `tsc --noEmit` limpio. 6 tests nuevos de backend y 7 de frontend |
| 2026-08-20 | Bloque INF (planificación) — El módulo de informes, utilizable de punta a punta | **Cursor: → INF.1 (Opus).** Planificado desde las pruebas humanas del usuario: los dos caminos del módulo —generar con IA desde cero, y ejecutar una plantilla de seguimiento— **no llegan al final**. Trece observaciones, dos de ellas bloqueantes y ninguna de estilo. **(A)** `WorkspacePage.tsx` ofrece dos disparadores de la generación y el destacado **se salta la subida**: en el log no hay una sola petición de subida, y de ahí toda la cascada hasta los bloques de IA en error; la plantilla, verificada en BD, está bien formada. **(B)** `preview_builder.py:37-49` y `AIBlockReviewPanel.tsx` usan **definiciones distintas de «pendiente»**, así que un bloque de IA que falla bloquea el informe (409 en vista previa y exportación) mientras el panel anuncia «todos aprobados» y no ofrece ninguna acción — regla maestra nº2 incumplida. Y dos causas del camino de la IA: `propose` no recibe **ninguna muestra de datos** (el legacy sí: `etl_factory.py:146-200`, con anonimización) y el prompt llama «table» a lo que el validador exige que sea un `DETERMINISTIC_DATA`. **Decisión del usuario: acceso por módulos concedidos, no rol nuevo** (INF.7) — hoy no hay control de acceso por función, cualquier cuenta puede editar los chatbots institucionales. 10 prompts; INF.1-INF.3 desbloquean el flujo. Sin código tocado: esto fue análisis |
| 2026-08-20 | Entorno + CI (3 commits) — El 8000, chardet, Python 3.13 y el guardarraíl de i18n | **Cursor: sin cambio.** Tres arreglos fuera de plan. `fbe5e74`: el «huérfano del 8000 que no responde a taskkill» **era falso** — uvicorn `--reload` que falla al arrancar deja el árbol vivo con el socket en estado `Bound` (invisible a `netstat \| findstr LISTENING`), y se libera matando el árbol entero; la deriva a 8001 y luego a 8002 nunca hizo falta, y con `VITE_API_TARGET` en el 8002 y el backend en el 8000 toda llamada del frontend en dev daba 502. De paso, tres errores del `.bat` de CUR (el servicio de BD se llama `postgres`, no `db`). `7b39863`: techo a `chardet<6` (camelot no lo ponía y `requests` avisaba en cada arranque) y **`server/.python-version` = 3.13**, que faltaba — sin él uv recrea el venv con el intérprete más nuevo que encuentre (tenía 3.14.2) y divergía de CI en silencio; al reconstruirlo apareció un tercer fallo latente: `opencv-python-headless` **instalado con su `cv2/` inexistente en disco**, que rompía `import camelot` —dependencia obligatoria, no extra— y dejaba dos tests saltando en silencio por su `importorskip`. `9a6e949`: el rojo de CI en `curationI18nCoverage` tenía **dos causas opuestas** — las cuatro `reason_error_*` sí se consumen (`razonDelHallazgo.ts:72` las construye con plantilla, como `type_`/`severity_`/`status_`) y `findings_retired` no se consumía ni podía (es una cifra de `SiteQualitySummary`, dataclass interno que solo se loguea, ausente de routers, schemas y `openapi.json`). Suite backend **2618 passed, 1 skipped, 0 failed**; frontend 66 ficheros, 435 tests, 0 fallos |
| 2026-08-19 | CUR.9 (RED/GREEN + medición real) — Los hallazgos que ya no son ciertos se retiran | **Cursor: sin cambio (Bloque CUR completo).** Del usuario, tras CUR.8: «debería añadirse la pasada de reconciliación al analizar. No tiene sentido que haya filas que ya no sean ciertas». El detector sólo sabía **añadir** (`upsert`): afirmaba y nunca dejaba de afirmar, así que cada mejora dejaba un sedimento de acusaciones caducadas —241 `stale` guardados donde el detector de hoy emite 207—. `reconciliacion.py` nuevo, ejecutado por el job **al terminar la detección y antes de calcular `quality_score`** (la nota de una página no puede seguir castigada por hallazgos retirados). Tres reglas, y las tres están para no retirar de más: (1) **sólo se retiran los tipos de los detectores que han corrido** —un detector saltado, o que falló, no autoriza a retirar nada suyo: reconciliar lo que no se ha mirado sería retirar por no haber buscado—; (2) **`duplicate` lo emiten los dos detectores** (el determinista por `content_hash`, el semántico por significado), así que se reconcilia sólo cuando los dos han pasado — con el semántico apagado, un pase determinista habría resuelto los 13 duplicados semánticos; (3) **lo que una persona decidió no se toca**: `dismissed` es un juicio humano y `resolved` está cerrado, sólo se retiran `new` y `confirmed`. La retirada no pasa por `transition()` —que valida las transiciones humanas— y deja nota, porque «el sistema retira una afirmación que ya no sostiene» no es lo mismo que «una persona lo resolvió». La identidad del hallazgo es **la misma clave que usa el `upsert`**: si divergieran, el mismo hallazgo parpadearía en cada pase. **Y la cola arranca en «Pendientes»**: retirar no sirve si la fila sigue listada como «Resuelto», así que `list_by_site` entiende `status=open` como los dos estados abiertos y el filtro conserva «Todos los estados» para auditar el histórico. **Medido contra el apartado real**: 207 emitidos, tipos reconciliados sin `duplicate` (el semántico no pasó), **111 retirados** —109 `stale` + 2 `thin`—, y los 13 duplicados semánticos intactos; la cola pasa de 288 filas a **177**, que es exactamente 132+29+13+2+1, y los 111 retirados siguen consultables con su razón. Los 132 `stale` que quedan son justo los que llevan el umbral en la señal (CUR.8): cuadre exacto entre las dos mitades. **De paso, el flake de frontend cazado en la raíz**: `cleanup()` explícito en `test-setup.ts` —la librería lo registra sola sólo si detecta los globales y con esta configuración no siempre lo hacía, de ahí los «Found multiple elements» que aparecían al correr varios ficheros juntos y no al depurar uno—. Cifras: curación+higiene+sites **537 passed**; frontend curación **78 passed**, y 190 con api+redacción, estable en tres pasadas; `tsc` y `ruff` limpios |
| 2026-08-19 | CUR.8 (RED/GREEN + navegador) — Las cuatro cosas de las pruebas manuales del Bloque CUR | **Cursor: sin cambio (Bloque CUR completo).** Cuatro observaciones del usuario probando la curación, y ninguna es de estilo. (1) **«Se muestran una serie de páginas duplicadas pero solo se menciona una»**: la fila pintaba `source_url` y la segunda página vivía en la señal sin que nadie la mirara; ahora salen las dos, las dos con su «ver texto guardado» —para eso el contrato sirve `related_page_id`—, que es lo que permite juzgar si el duplicado es cierto. (2) **«Cuando se dice que están desactualizadas se debería decir la razón»**: columna «Por qué» nueva, construida en `razonDelHallazgo` a partir de la señal, con un caso por tipo —fecha + antigüedad + **el criterio con el que se juzgó**, tokens contra el mínimo, parecido y explicación del modelo, causa del fallo de rastreo e intentos—. El umbral pasa a viajar **dentro del hallazgo** y no se lee de la configuración al pintar: el criterio es de cada sitio desde CUR.2.1 y se puede cambiar, así que leerlo después daría el criterio de hoy en vez del que produjo el aviso. (3) **«He seleccionado 4 páginas... pero no sé si ha funcionado»**: había funcionado —cinco páginas con sus fragmentos en la base—, y la pantalla no decía nada porque la mutación se lanzaba **sin `onSuccess`**, así que la tabla no se refrescaba; ahora hay aviso con el número enviado, se dice que la ingesta va en segundo plano (el servidor responde 202) y hay botón de actualizar, además de la invalidación automática. (4) **«Al darle a descargar al pdf descarga un word... o se quita el botón o se permite que la descarga sea en pdf»**: el botón se ofrece **sólo si el servidor puede cumplirlo**; `se_puede_convertir_a_pdf()` mira si hay LibreOffice —`soffice` **o** `libreoffice`, que en Windows solo existe el primero— y el informe lo declara en `pdf_available`, con un aviso que explica qué falta en vez de que el botón desaparezca sin más. Que el fichero no mintiera (VER.7) era lo mínimo, no la solución. **Verificado en vivo**: los 13 duplicados con sus dos URLs (`internalitzacio/…` contra `base/internacionalitzacio/…`) y «Parecido del 100 % · Ambos fragmentos son idénticos palabra por palabra»; «Actualizada el 20/11/2020 · hace 2098 días · el criterio del sitio son 365 días» en 132 hallazgos —los 109 restantes son filas de antes del cambio, que degradan sin mentir—; dos páginas ingeridas con su aviso y el resumen pasando de «5 ya en el corpus» a 7; y en auditoría el botón de PDF ausente con su explicación. **Pendiente anotado, no arreglado**: los hallazgos **no se retiran** cuando dejan de ser ciertos —hay 241 `stale` guardados donde el detector de hoy emite 207, porque las páginas agrupadas como serie ya no producen aviso—; hace falta una pasada de reconciliación, que es más grande que estos cuatro arreglos |
| 2026-08-19 | CUR.7 (RED/GREEN + ejecución real) — Cablear el detector semántico · **cierra el Bloque CUR** | **Cursor: CUR.7 ✅. BLOQUE CUR COMPLETO.** El detector semántico existía desde 9Q.4 con sus tests y **no lo ejecutaba nadie**: el job se construía con `detectors=[determinista]`, así que el sitio guardado con `audit_semantic_scope='full'` no cambiaba nada. Tercera vez que aparece este patrón —el watcher de RAS.5, el bloque `TABLE` de SEG.5—. Cablearlo destapó **cuatro defectos que ningún test cubría**: (1) el job **no persiste** los hallazgos —los cuenta, marca supersesiones y calcula `quality_score`, pero guardarlos lo hace cada detector con su repo, y el semántico dice en su docstring que «eso es del job»—, así que el detector habría llamado al LLM y sus hallazgos morirían con la sesión; (2) `run_semantic` **nunca se aplicaba**, porque el job lo mira con `getattr(detector, "_is_semantic", False)` y ningún detector declaraba el atributo; (3) casi se cablea el adaptador equivocado —`RedactorDeBloques` cumple el mismo protocolo pero su primer argumento es el **identificador** de una plantilla de prompt, así que el juez habría recibido «esa plantilla no está en el catálogo» y devuelto `unrelated` a todo sin que nada fallara: de ahí `JuezDeContenidoWeb`—; (4) el detector **funcionaba exactamente una vez por sitio**, porque en la segunda pasada los vectores vienen de pgvector como `float32` de numpy y la similitud acaba en un JSONB (`Object of type float32 is not JSON serializable`). **Ejecutado cuatro veces contra el apartado real** —349 páginas embebidas con Vertex, tope de 25 pares para acotar el gasto—, y cada pasada corrigió a la anterior. Primera: 10 hallazgos y **los 10 el mismo falso positivo que el usuario corrigió en CUR.2**, con otra sintaxis: el archivo de formación transversal publica una página por curso académico y la identidad de serie sólo reconocía años de cuatro cifras, así que `/23-24/` contra `/24-25/` **tampoco se agrupaba en el determinista**; arreglado en `_process_key`+`_strip_year_segments` (con la comprobación de años consecutivos, para que `/12-34/` no agrupe nada) y el semántico deja de gastar llamadas en pares de la misma serie. Tercera: quedaban 11, de los que 9 eran ediciones del mismo curso **con la ruta reorganizada** (`recerca/programacio-r` → `tecniques/programacio-r`), que ninguna regla de URL caza; se resolvió **diciéndole al juez el dato del dominio** —un portal que archiva ediciones publica una por año y dos ediciones no se contradicen aunque cambien fechas y horas—. Cuarta: **13 hallazgos, 0 contradicciones, 13 duplicados**, y los cuatro primeros son justo lo que el usuario sospechaba: la misma página bajo dos rutas (`/base/escola/normativa/acordacded/` vs `/escola-doctorat/normativa/acordacded/`; `/base/info-academica/supervisio-seguiment/` vs `/info-academica/…`; `internalitzacio/doctorat-internacional/` vs `base/internacionalitzacio/…`, con la errata del portal incluida) y **`/base/calendari` contra `/base/calendari/`** con similitud 1,000 — que no es duplicación del portal sino **nuestra**: la misma página rastreada y guardada dos veces por la barra final. Arreglado con `clave_de_pagina`, que compara sin la barra pero **no cambia la URL que se pide**: añadirla sería peor, porque hay servidores donde `/x/doc` existe y `/x/doc/` da 404 (el primer intento, que sí la añadía, rompió 6 tests con razón). El alcance semántico se puede cambiar desde la fila del sitio, `off` incluido, y `semantic_similarity_threshold`/`semantic_max_pairs` pasan a ser configuración del sitio por la regla de CUR.2.1. Cifras: curación+higiene+sites **518 passed**; frontend curación **62 passed**; `tsc` limpio; `ruff` limpio |
| 2026-08-19 | CUR.6 (RED/GREEN + navegador) — Cuántas páginas tiene esto, antes de comprometerse | **Cursor: CUR.6 ✅ → CUR.7.** Del usuario: «convendría que al dar de alta un nuevo sitio se pudiera generar y descargar un sitemap. De ese modo se podría valorar la extensión del sitio y si conviene hacerlo todo de golpe o por subapartados». El portal **no publica sitemap** —404, medido en RAS.4—, así que se construye recorriendo: `POST /hub/site-reconnaissance` sondea sin guardar nada y sin que el sitio exista, que es justo el momento en que la pregunta se hace. Devuelve las URLs **agrupadas por primer nivel de ruta** con recuento y ejemplos, el total, el CSV descargable y la duración estimada. Tres decisiones que el prompt no cerraba: (1) se cuenta **lo descubierto**, no lo visitado —contar sólo lo visitado convertiría el tope en la respuesta: «tiene 60 páginas» porque paramos en 60—; (2) la estimación **se mide** (`segundos_por_pagina` del propio sondeo) y se le suma **la pausa del rastreo real**, no la del sondeo, porque el sondeo va con prisa y el rastreo va cortés: con dos segundos por página, mil URLs son media hora y con la latencia del sondeo saldrían tres minutos; (3) el CSV se escribe con `csv.writer` y no con `join`, porque una URL con coma —`.../acta,2025/`— desplazaría la columna y el fichero mentiría sin avisar. **Verificado contra el portal, y la primera pasada estaba mal**: el sondeo tomaba la profundidad del formulario (1) y devolvía **11 URLs de un apartado que tiene 349**, o sea contestaba a otra pregunta; el prompt decía «profundidad y tope propios» y no lo había leído bien. Con profundidad 3: **219 URLs desde 60 páginas pedidas**, aviso de truncado por `max_pages`, «unos 11 min, a 3,1 s por página», y el CSV bajando bien formado. **Hallazgo del portal, de propina**: el subapartado mayor sale como `estudis` con 188 URLs, porque la sección es alcanzable **bajo dos prefijos** —`/estudis/centres/escola-doctorat/` y `/centres/escola-doctorat/`—, que es parte de la duplicación que el usuario veía en el informe. Cifras: curación+higiene+sites **497 passed**; frontend curación **59 passed**; `tsc` limpio |
| 2026-08-19 | CUR.5 (RED/GREEN + navegador) — La descarga con el token y la selección de páginas | **Cursor: CUR.5 ✅ → CUR.6.** Dos botones que no funcionaban, y el primero no era un fallo del servidor: descargar el informe era un `<a href download>` apuntando a la API, o sea **una navegación del navegador, que no lleva cabecera de autorización**. El endpoint respondía 401 y Chrome enseñaba su propio error de descarga —«el fitxer no es troba disponible»—, que no menciona el 401 y parece que el informe no exista. Ahora el fichero se pide con el cliente autenticado (`shared/api/download.ts`, con sus cinco tests) y se entrega al navegador desde memoria; si falla, se dice en la pantalla. **Encontrado de rebote: el mismo defecto en Informes** —`<a href>` a `/redaccion/workspaces/{id}/export`, endpoint con `Depends(get_current_user)`—, así que PRO.5 dio el informe por «descargable» y la descarga no podía funcionar; arreglado también, con su test, aunque quede fuera de la letra de CUR.5. Lo segundo, del usuario: «no pueden desmarcarse algunas. Convendría que se pudiera elegir». Casilla por página, «marcar todo» e ingerir sólo lo marcado, con el número marcado en el propio botón. **No es «ingerir todo el sitio»**, que `docs/DECISION_CURACION_SEPARADA.md` retira a propósito: cada página entra al corpus porque alguien la marcó; lo que desaparece es tener que pulsar 349 veces para publicar un apartado. Y «que se vea lo que ya está ingerido» obligó a una **decisión de contrato**: una página ya ingerida deja de descartarse de la lista y se devuelve con `is_ingested` —ocultar la fila cumplía la mitad del objetivo (no ofrecerla dos veces) y rompía la otra: no distinguía «ya está en el corpus» de «nunca fue candidata», y tras pulsar «Ingerir» la fila se esfumaba sin confirmar nada—. **Verificado en vivo contra el apartado real**: el DOCX baja de verdad (39.746 bytes en Descargas, los mismos que sirve el endpoint), «Descargar PDF» guarda un `.docx` porque el servidor declara el fallback sin LibreOffice y el nombre del servidor manda —la honestidad que VER.7 puso en el endpoint llega hasta el fichero guardado—, tres páginas marcadas e ingeridas de una sola vez (202 × 3, tres documentos en la base) y el resumen pasando a «346 por publicar · 3 ya en el corpus» con sus tres casillas bloqueadas; y la exportación de Informes devolviendo un DOCX de 36.648 bytes. Consola sin errores de la aplicación. Cifras: curación+higiene+sites **480 passed**; frontend curación+api+redacción **161 passed** en tres pasadas seguidas; `tsc` limpio |
| 2026-08-19 | CUR.4 (RED/GREEN + navegador) — Poder mirar lo que el informe dice | **Cursor: CUR.4 ✅ → CUR.5.** (CUR.1–CUR.3 quedan detallados en la fila del bloque.) Lo pedido: las URLs de los hallazgos son enlaces con `target="_blank"` y `rel="noopener"` —«ahora hay que copiar y pegar»—, el «+4 más» del informe de auditoría se despliega, y un hallazgo de grupo enseña sus versiones **con sus fechas**, que es lo que se compara. Lo que hacía más falta y no estaba pedido así: `GET /hub/pages/{page_id}/content` y un visor del **texto guardado** de una página —lo que iría al asistente, no lo que se ve en el portal—, con guarda de organización, aviso de qué se está leyendo y enlace al original para comparar. **Y verificarlo en el navegador destapó el defecto de siempre**: la pantalla estaba entera y no tenía con qué. Con los 275 hallazgos reales del apartado delante salían las 275 URLs clickables y **cero** botones de desplegar y de ver texto, porque `_FindingOut` no servía `page_id` ni la señal; los tests de frontend pasaban porque su mock sí los traía. Arreglado en el contrato, y el doble del test de backend convertido en clase real: un `MagicMock` inventa un `.signal` que la fila de la base de datos no tiene, y con eso el test habría dado por bueno un atributo inexistente en producción (es la trampa del `.score()` de Langfuse otra vez). Verificado en vivo tras el arreglo: «Ver las 9 versiones» con sus nueve fechas, 274 botones de ver texto, el visor mostrando `Acords Comité de Direcció … · Escola de Doctorat · 18/11/2021 · 263 tokens` con 1.052 caracteres limpios, y en Publicación las 349 candidatas con enlace y visor. De paso queda comprobado que el recorte de CUR.3 **no se comió contenido**. Consola sin errores de la aplicación |
| 2026-08-19 | Bloque LEG (LEG.1–LEG.5) — Retirada del legacy de prompts de AutomatIA | **Cursor: LEG.5 ✅. BLOQUE LEG COMPLETO. Suite backend completa: 2488 passed, 3 skipped, 0 fallos** (8 m 14 s), **y sin el flake** de `tests/test_prompts.py`, que llevaba semanas «inventariado» —pasaba aislado y fallaba en la suite paralela—. Migración `h8a9d0e1f2g3` aplicada; `ruff` limpio. **No era limpieza estética**: esa vía escribía en la base de datos en **cada arranque del servidor**, y ahí estaba la causa común de los dos síntomas que el usuario había traído en unos logs —arranques fallidos en desarrollo y el flake—, porque el servidor sembraba mientras la suite reinicializaba. **LEG.1 es el prompt que justifica el bloque**: leer 580 líneas antes de borrarlas, como en PRO.1 y PRO.4. Resultado en `docs/COMPARATIVA_PROMPTS_LEGACY.md`: el prompt vivo de scripts **gana con razones concretas** (genera las listas prohibidas del auditor en vez de escribirlas a mano, prohíbe rutas absolutas explícitamente —el legacy las detectaba y no se lo decía al modelo— y declara el contrato de `result`), y una regla del legacy **se descarta a propósito** porque contradice el diseño: «maneja excepciones con try/except» convierte un fallo visible en una tabla vacía que nadie revisa, y el prompt de auditoría vivo dice que eso es peor que fallar. **Lo único que aporta** es `sys_semantic_naming`: el prompt del ETL hace que el modelo cree columnas con `compute_column` y no decía cómo nombrarlas, y ese nombre acaba impreso en la tabla de un informe institucional; portado con su test. Lo que no tiene equivalente vivo se **cita** para el bloque de automatizaciones —prioridad LOCAL/ORGANIZATION/PARTNER/GLOBAL con etiqueta de origen en la respuesta, no inventar átomos fuera del inventario, disparadores como entidades independientes a las que los flujos se suscriben con activación perezosa, pasos `PLACEHOLDER` para la lógica que falta, máximo tres preguntas y ninguna que no cambie la estructura del código— en vez de conservarse como código muerto. **LEG.2** las tres llamadas de siembra y `seeds_prompts.py` (580 líneas) fuera, con guardarraíl que salta si vuelven; comprobado que la aplicación arranca: ocho líneas de seeds útiles y ninguna de prompts legacy. **LEG.3** el orquestador huérfano fuera (0 % de cobertura, 220 líneas que no se ejecutaron nunca) y el guardarraíl que de verdad impide el flake: prohíbe **abrir sesión o conexión** sobre `server_engine`, no importarlo, porque hay un test-smoke legítimo que sólo comprueba que el motor existe —y el mismo cuidado en el `grep -r`, que busca imports y no menciones, para que nombrar el módulo retirado en un docstring no dé un falso positivo—. **LEG.4** dos clases de diecinueve fuera de `models.py` (el fichero se queda, las otras diecisiete están vivas) más dos imports que sólo ellas usaban, y `tests/unit/test_tier_override_model.py` se va con la clase que probaba, que era el `tier_override` del legacy y no el de `hub_llm_configs`. La migración enseñó dos cosas al escribirla: las tablas se llaman **`extractionserviceconfig` y `systemprompt`, sin guiones bajos** —el nombre que SQLModel deriva de la clase, y por escribirlos «bonitos» falló el primer intento con `UndefinedTable`— y va con `IF EXISTS` porque en esta base **no estaban**: el esquema se había creado con `create_all` y no con Alembic, y una migración que revienta por eso bloquea el arranque de ese entorno. **LEG.5** el `main.py` de la raíz (705 líneas que importan `nicegui`, `client_app`, `ExtractionService` y `RPAExecutor`) a `_legacy_nicegui/`, `arranque.bat` sin la opción 3 que lo lanzaba —y comprobado que su comando levanta el servidor de verdad—; el `grep -r` a cero destapó **dos consumidores que el plan no conocía**: `client_app/main.py`, otro lanzador NiceGUI que importaba el de la raíz, y `client_app/tests/unit/test_watcher_migration.py`, que prueba las rutas del fichero movido. Los dos a cuarentena con su ruta relativa, porque dejar un lanzador que importa algo que ya no está es la retirada a medias que CLAUDE.md prohíbe. |
| 2026-08-18 | Bloque RAS (RAS.1–RAS.5) — El rastreo contra un portal de verdad | **Cursor: RAS.5 ✅. BLOQUE RAS COMPLETO. Suite backend completa: 2470 passed, 3 skipped, 0 fallos** (7 m 55 s); **frontend: 376 passed, 0 fallos**; `tsc` limpio; `ruff` limpio; contrato OpenAPI regenerado. Dos migraciones aplicadas (`f6y7b8c9d0e1` señales de renderizado, `g7z8c9d0e1f2` cola reanudable + clasificación del fallo). **El resumen del bloque en una línea: contra un sitio de pruebas el módulo funcionaba; contra el portal de la propia casa no rastreaba, y cuando rastreaba, mentía.** **RAS.1** cortesía —pausa por host con el `Crawl-delay` del sitio ganando si es mayor, `robots.txt` una vez por host, User-Agent con contacto, presupuesto de tiempo, concurrencia acotada— y de paso el defecto que el presupuesto deja a la vista: las bajas se calculaban como «activas que no aparecen en este rastreo», así que **un rastreo truncado declaraba desaparecido lo que no había visitado**, y una baja alimenta decisiones de retirada del corpus. **RAS.2** `needs_javascript` en vez de `empty` crítico, con las heurísticas **calibradas contra el portal**: una página perfectamente estática de `www.uji.es` lleva `<noscript>` y **diez `<script>`**, así que una regla ingenua habría declarado dinámico el portal completo; y el portal **no publica `sitemap.xml`**, así que la señal más fiable del sondeo no existe ahí. En el mismo prompt, lo que el sondeo obligó a mirar: de una página se guardaba el **HTML crudo** en `markdown_content` —51.098 bytes de marcado para 4.249 caracteres de texto—, con lo que `token_count` medía plantillas (el umbral de `thin` era inalcanzable), el detector semántico comparaba menús idénticos y a la ingesta del asistente llegaban `<div>`. **RAS.3** reanudación por cola guardada en el sitio, reintentos con espera creciente, 404 ≠ timeout en severidad, y **no pedir dos veces la misma página** (el recorrido descargaba para extraer enlaces, tiraba el cuerpo, y el guardado volvía a pedir: 25 páginas, 50 peticiones). **RAS.4** decisión escrita **sin navegador**, con la medición delante: 0 de 25 páginas lo necesitan, y las condiciones para reabrirlo quedan en `docs/DECISION_RENDERIZADO_RASTREO.md`. **RAS.5 — el apartado de la Escuela de Doctorado de punta a punta, y siete defectos más.** Tres impedían rastrear: **un enlace roto tumbaba el rastreo completo** (el primer intento real devolvió **cero páginas** por un 404 en `/estudiantat/`, y todo portal real tiene enlaces rotos —son lo que el módulo busca—), **un PDF enlazado se guardaba como texto** y el byte `0x00` lo tumbaba otra vez (antes de reventar ya había calculado 26.872 «tokens» de cabecera binaria e idioma «bengalí»), y **`config_json` no se podía fijar** ni al crear ni al editar un sitio, así que `url_regex_filter` —la decisión operativa del bloque, un sitio por apartado— era inalcanzable desde la interfaz. Cuatro hacían mentir al informe: **`http` y `https` de la misma página** se rastreaban como dos (191 supersesiones falsas y la mitad del presupuesto), **una trampa de rastreador** en el conmutador de idioma del portal (`?urlRedirect=…&url=…` acumulándose: 526 URLs pendientes que no acababan y 107 supersesiones falsas más), los 404 salían **además** como `empty` crítico, y **una página ilegible se declaraba «la versión vigente»** porque sin fecha el orden cae en `first_seen_at`. Más el umbral de `stale`, que es la lección de calibración del bloque: el portal **no declara ninguna fecha** —sin `Last-Modified`, sin `ETag`, sin sitemap—, así que la «fecha del contenido» salía del año más reciente **citado en el texto** (una página menciona 1925, 2010, 2016, 2021, 2024 y 2071) y eso marcaba **303 de 400 páginas**; ahora `stale` exige fecha con procedencia y el hallazgo dice cuál. **Resultado medido: 500 hallazgos → 20**, sobre un rastreo **completo** de **351 páginas** en ~12 minutos, 0 pendientes, 4 excluidas por `robots.txt` (`/seu/`), 0 que necesiten JavaScript. Los 20 revisados uno a uno son ciertos: 9 acuerdos del consejo por año superados por el de 2025, los mismos 9 antiguos con `source: url_year`, y 2 enlaces a contenido que ya no está. **Y el camino al asistente, que estaba cortado en tres sitios**: el botón de ingerir respondía **202 sin ingerir nada** (servicio sin watcher, `AttributeError` en background), **`changed_page_ids` no lo consumía nadie** —el portal cambiaba y el asistente seguía citando el texto viejo—, y **el job del arranque se construía con `watcher=None`**, así que nada de eso podía ocurrir en la aplicación real. Decisión del usuario: una página del corpus que cambia **se actualiza sola y queda avisada** (`content_updated`), y reingerir **no publica** donde nadie aprobó. Caso replicable en `docs/CASO_CURACION_ESCOLA_DOCTORAT.md`. Cifras de coste, para el que venga: 2 s de cortesía + **1,4 s de latencia del propio portal** por página; nuestro proceso, 4 ms, salvo `detect_language` con 153 ms. |
| 2026-08-18 | Bloque SEG (SEG.1–SEG.5) — La valoración anclada a su tabla | **Cursor: SEG.5 ✅. BLOQUE SEG COMPLETO. Suite backend completa desde Git Bash: 2360 passed, 3 skipped, 0 fallos** (8 m 38 s); **frontend: 358 passed, 0 fallos**; `tsc` limpio; contrato OpenAPI sin cambios. **SEG.1** — `data_block_refs` en el bloque de IA y contexto **sólo** con esos bloques, en el orden declarado; un apartado sin anclaje consta en el manifiesto con `context_scope: full`, y de paso `ai_blocks` **estaba declarado en el manifiesto y nadie lo rellenaba**. **SEG.2** — las dos instrucciones (`valoracion_de_tendencia_v1`, `resumen_de_resultados_v1`) con las reglas duras portadas del prompt real de la gema: no calcular, «No hay valor» es una ausencia y se dice, sugerencias sin imperativos, relaciones como hipótesis, y el resumen **menciona todas** las tablas o dice en una frase por qué una no aporta. **SEG.3** — pipeline de tablas Markdown que **reproduce y no interpreta**, con el código de la tabla tomado del pie; el selector `options.table` distingue `Tabla 1.4` de `Tabla 1.4.2` por lo que sigue al punto, y las quince tablas del plan de mejoras llevan su código en un encabezado con negritas (`### **Tabla 8.4.1. Acción…**`), que hay que desnudar o se quedan sin código. Sobre el fichero real: **42 tablas, todas con código, sin duplicados** —y una cifra que di antes mal por un acento en mi propia comprobación (`Tabla sin codigo` ≠ `Tabla sin código`), corregida—. **SEG.4** — la edición cableada: el `editBlock` que el backend ya sabía hacer y **cuyo hook generado no llamaba ninguna pantalla**, ahora con el texto completo, de qué tabla se apoya, quién editó y el original de la IA consultable; la edición **conserva la procedencia** (modelo, versión del prompt, anclaje) en vez de sustituirla. **SEG.5 — el informe de doctorado de punta a punta con datos reales, y aquí salieron cuatro hallazgos más.** (1) **Los bloques `TABLE` no los pintaba nadie**: el tipo está en el contrato, el validador le exige `data_block_ref`, el prompt lo ofrece al modelo… y ningún nodo del grafo lo rellenaba, así que el informe salía con **las nueve tablas vacías**, sin error y sin aviso; las cifras que se veían venían de la prosa del modelo. Es literalmente el agujero que PRO.5 tapó para los gráficos, un tipo de bloque más tarde: `TableRenderNode`, hermano de `ChartRenderNode`, copia al bloque las tablas del bloque que referencia —«No hay valor» incluido— y de ahí las cogen igual la pantalla y el DOCX. (2) **Un bloque que no está en ninguna sección desaparece del informe**: la vista previa y el ensamblado recorren `sections[].block_ids`, así que lo que ninguna enumera se ejecuta, se revisa y no se imprime — el informe salía con las secciones vacías y **aspecto de estar bien**, que es la peor forma de fallar en un informe institucional. Se rechaza ahora al publicar la versión y al validar el borrador del modelo (es el único momento en que avisar sirve), con la regla también en el prompt; `_embed_script_block` ya la respetaba desde PRO.3, pero vivía en ese único sitio sin estar comprobada. Los bloques que sólo producen datos y la puerta de revisión están exentos: no son contenido, y meter el `DETERMINISTIC_DATA` en una sección **duplicaría** la tabla. Al activarla, cuatro fixtures propios describían plantillas de informe vacío: corregidos. (3) **Las tablas del cuerpo se pintaban sin bordes ni alineación**, porque sólo estaba vestido el anexo de auditoría — hasta hoy ningún bloque del cuerpo había llegado a producir una tabla. (4) **La valoración llegaba con markdown crudo y longitud de ensayo**: el informe imprime el texto tal cual, así que `**Producción vegetal:**` salía impreso con los asteriscos; las dos instrucciones piden ahora prosa corrida y un par de párrafos, verificado en la última pasada (0 asteriscos en los nueve apartados). **Verificado en vivo con el informe del Programa de Doctorado en Ciencias (70.181 bytes, 42 tablas)**: los nueve bloques de datos traen **exactamente una** tabla y es la suya, ninguna valoración usa cifras ausentes del origen ni de otra tabla, las tres tablas con «No hay valor» lo dicen —y la valoración de la 1.5.2 escribe «para el curso 2024 y el curso 2022 no se dispone de medición», que es justo lo que la gema no hacía—, la vista previa monta 3 secciones con 9 tablas y 9 valoraciones, y el DOCX sale con 41.193 bytes y las tablas como tablas de Word. Caso documentado en `docs/CASO_INFORME_SEGUIMIENTO.md`; `.bat` del bloque con **una sola prueba humana**: si la valoración le sirve a quien firma. |
| 2026-08-17 | PRO.8 — Gráficos deterministas: lo usual sin programarlo | **Cursor: PRO.8 ✅.** Once tipos (`bar`, `barh`, `bar_stacked`, `line`, `pie`, `donut`, `scatter`, `bubble`, `histogram`, `boxplot`, `heatmap`) frente a cinco, y **seis de los seis nuevos-de-verdad eran un parámetro de lo que ya se hacía** —`barh` son los ejes cruzados, `bar_stacked` un pivot y una llamada, `donut` un `wedgeprops`, `bubble` un `size=`; `bar_grouped` y `line_multi` ya existían como `color_by`—, que es lo que hacía absurdo pagar un script generado por ellos. `violin` y `pairplot` **se quedan fuera**, razonado: exploración estadística, y `pairplot` devuelve un grid que no cabe en `fig, ax`. Pero el hueco mayor no eran los tipos: **una plantilla no podía pedir ni un título** —`ChartConfiguration` los tenía y `ChartBlockConfig` no los exponía— y **`show_values` estaba declarado en el contrato y el renderizador lo ignoraba**, que es peor que no tenerlo porque quien lo pone cree que ha pedido las cifras. Ahora la presentación entera es declarativa (título, etiquetas, cifras, orden, leyenda, rejilla, tamaño, `bins`, estilo) y `sort` ordena. **Y el ahorro:** `translate_nl_to_config("chart_config")` **no devolvía ninguna configuración** —llamaba a `chart_factory.generate_script()`—, así que «barras horizontales» costaba modelo + auditoría + sandbox para lo que es un campo; ahora devuelve `ChartPlan` declarativo y baja al script sólo cuando la petición no cabe, con el catálogo del prompt **generado del contrato** (`catalogo_de_configuracion()`, extraído a `services/contrato_legible.py` junto con el de PRO.4 para que no haya dos copias). Nueva actividad `configuracion_de_grafico` (nivel 2) editable en `/hub/activity-prompts`. **Dos hallazgos que no venían en el plan.** (1) Una `configuration` vacía es la señal **deliberada** del modelo de que la petición no encaja, y se estaba reintentando dos veces más: tres llamadas para volver a oír lo mismo. Ahora se reintenta lo que el modelo puede corregir, no lo que ha decidido. (2) **Verificado mirando el PNG, no con un test**: las cifras salían en formato inglés —`128,340.55`— en un informe de una institución española; los tests comprobaban que la cifra estaba, y estaba. `number_format` por defecto `es`, y el eje también separa millares. Un test del copiloto fijaba la conducta vieja con un `MagicMock` sin `spec` —justo lo que permite que nadie lo note—: actualizado y con `spec=ChartFactory`. 30 tests nuevos en `test_grafico_determinista_completo.py` + 6 en `test_grafico_sin_generar_codigo.py`. El contrato OpenAPI **no cambia** (`ChartBlockConfig` no está expuesto en ningún endpoint); `tsc` limpio. |
| 2026-08-17 | PRO.9 — El ETL cubre lo que una hoja de cálculo necesita | **Cursor: PRO.9 ✅. BLOQUE PRO COMPLETO (9 prompts + PRO.2.1). Suite backend completa desde Git Bash: 2256 passed, 3 skipped, 0 fallos** (6 m 08 s). Comprobado **antes** de escribir nada, y salió lo contrario de lo esperado: **lo determinista del legacy está portado al 100%** en PRO.4 —`transform_operations.py` son exactamente las once que ya estaban, y `etl_page.py` y `data_contract_service.py` no añaden ninguna—, así que de migración no quedaba nada. Lo que faltaba es **cobertura** de cuatro operaciones que allí tampoco existían: `to_number`, `compute_column`, `sort_rows` y `unpivot`. **`to_number` es la importante y es la que menos se ve**: cualquier aplicación de gestión de aquí exporta `"1.234,56 €"`, y medido en la verificación en vivo, `sum()` sobre esa columna devuelve la **cadena** `'128.340,55 €45.120,00 €12.890,75 €(1.500,00)'` —sin error, sin aviso, y el informe con la cifra mal—; tras convertir, 184.851,30. Lee el paréntesis contable como negativo, el `%` y el `€`, y falla en los **dos** casos que son de configuración y no de datos: cuando no se convierte ni un valor, y cuando los separadores están declarados al revés. El segundo **apareció escribiendo el test**: se esperaba que `"1,234.56"` en configuración española no convirtiera, y sí convierte, **a 1,23456**; una cifra distinta es peor que una columna vacía, porque la vacía se ve. Lo delata la posición —el separador de millares nunca va después del decimal—. `compute_column` **sin campo de fórmula y sin `eval`**, decidido así: el auditor de PRO.1 prohíbe exactamente eso en un script y admitirlo aquí sería la misma capacidad por otra puerta, esta vez sin auditor delante; lo compuesto se **encadena** (`t = a + b`, `pct = t / c`, `drop t`), que es más verboso, auditable y pintable en una pantalla. Un operando de texto es **siempre** una columna: si no existe, falla. Dividir por cero deja un hueco, no un `inf`. Y de paso, el mismo derroche que en gráficos: una lista `operations: []` es la respuesta deliberada que el propio prompt pide y se reintentaba dos veces más; arreglado, más el `_SYSTEM_PROMPT` muerto que PRO.4 dejó en `etl_factory.py`. El catálogo del prompt se genera del contrato, así que el modelo ve las cuatro sin tocar el prompt —comprobado con un test—, y las reglas del prompt le dicen que mire el tipo de las columnas de importes **antes** de sumar. 24 tests nuevos + 2 en `test_etl_factory.py`. Verificado en vivo con una hoja de las de aquí: `to_number` + `normalize_text` + `compute_column` + `sort_rows` da la tabla del informe con `pct_ejecucion` bien. Comparativas actualizadas: `COMPARATIVA_ETL_LEGACY.md` y `COMPARATIVA_LEGACY_INFORMES.md`. |
| 2026-08-18 | (planificación Bloque RAS) — el rastreo contra un portal de verdad | **Segunda consulta del usuario antes de probar**: si la curación sirve para obtener contenido para el RAG a partir del rastreo de una web institucional y para depurarla —páginas vacías, versiones nuevas publicadas sin retirar las viejas—, y si `www.uji.es` se puede rastrear de golpe o hay que fraccionar. **Respuesta medida contra el código: la finalidad ya está construida.** Los detectores emiten `empty`, `thin`, `stale`, `orphan_page`, `crawl_error` y `superseded` —este agrupa versiones del mismo recurso quitando los segmentos de año de la URL, que es exactamente el caso que planteaba—, y `CorpusSelectionService` ya cierra el circuito hasta el corpus del asistente. **Fraccionar, y el módulo ya lo permite** con `url_regex_filter`: un sitio de alta por apartado. **Lo que falta son cuatro cosas para apuntarlo a un portal real**, y la segunda la levantó el propio usuario al preguntar si el contenido dinámico «se podría analizar antes o que sí que avisara»: hoy una página pintada con JavaScript vuelve sin texto y el detector emite **`empty` crítico**, un falso positivo que afirma lo contrario de la verdad. Se planifica con las dos cosas que pedía —sondeo previo **sin navegador**, cuya señal más fuerte sale gratis (el hueco entre el sitemap y lo que alcanza el recorrido por enlaces), y un tipo nuevo `needs_javascript` en lugar de la acusación falsa—. Las otras tres: cortesía (pausa, `robots.txt`, User-Agent identificable, presupuesto de tiempo) como **requisito** antes de tocar el portal de la propia institución; rastreo reanudable con reintentos, porque la cola vive en memoria y un `timeout` se convierte hoy en hallazgo crítico; y renderizado con navegador **sólo si el sondeo lo justifica**, como extra opcional —misma decisión que con `torch`—. Caso guía: **el apartado de la Escuela de Doctorado**, que además es la misma unidad del Bloque SEG. 5 prompts, **no ejecutado**. |
| 2026-08-18 | (planificación Bloque SEG) — el supuesto de uso que faltaba | **El usuario describe, antes de probar el módulo, el caso para el que lo necesita, y no estaba escrito en ningún plan.** Informes institucionales donde las tablas y los gráficos se pintan **de forma determinista** en la estructura que define la plantilla, y la literatura —valoración de tendencias, resumen de resultados— la **propone la IA sujeta a aprobación o edición humana**. Caso guía: el informe anual de seguimiento de un programa de doctorado, con dos documentos reales aportados (resumen de datos con 30 tablas y el prompt de la gema de Gemini usada antes). El intento con la gema falló por dos cosas —**inventaba datos al pintar tablas** y **resumía omitiendo lo relevante**— y su prompt lo combate en mayúsculas, que es la señal de que se le está pidiendo al modelo que no haga algo que sí puede hacer. **Diagnóstico del módulo frente a ese caso**: la estructura por plantilla, la extracción y transformación deterministas, los once tipos de gráfico, la puerta de revisión bloqueante y el manifiesto **ya encajan**; y una premisa del usuario resultó incorrecta a favor nuestro —**la edición existe en el backend**: `editBlock` sobreescribe el contenido y guarda el original en un evento de auditoría con su actor; lo que falta es la pantalla, porque el hook generado **no lo llama nadie** (cero referencias)—. Pero hay tres huecos reales, y el primero es el que decide todo: **`_build_context` entrega todos los bloques extraídos a cada apartado de IA** y el contrato del bloque de IA **no puede referenciar una tabla concreta**, así que con 30 tablas se reproduce por arquitectura el fallo de la gema; no hay pipeline que lea tablas de un **Markdown**, que es el formato en que ya existen los datos de todos los programas; y una plantilla de ~60 bloques no la puede proponer un modelo, se redacta una vez por tipo de informe y se facilita (la vía existe: el MCP tiene `create_template` y `publish_template_version`). Planificado como **Bloque SEG**, 5 prompts, **no ejecutado**. |
| 2026-08-18 | (planificación Bloque LEG) + arranque legible | **Dos cosas, y la segunda salió de la primera.** El usuario aportó logs de desarrollo del 2026-08-17 en los que la aplicación no arrancaba, y **el log no permitía saber por qué**: FastAPI fusiona un `lifespan` por cada router incluido, así que con ~40 superficies HTTP el traceback llega con cuarenta marcos idénticos de `merged_lifespan` **antes** de la excepción real y cualquier captura lo corta por el medio —el log terminaba literalmente a media palabra, con `as may` seguido de «Application startup failed»—. El anidamiento no es un defecto, es el precio de tener muchos routers; el defecto es que la causa no se pueda leer, y en el despliegue previsto esa traza será lo único disponible. **Arreglado** (`f2333a4`): el arranque se envuelve, registra la causa en pocas líneas sin marcos de librería y conservando la cadena de causas, y **vuelve a lanzar** —tragarse el error dejaría la aplicación en pie y a medio construir—. Medido: tres líneas donde había cuarenta marcos. Descartado por el camino que la causa fueran las llamadas de red del arranque: refresco de modelos y de precios están dentro de `try/except`. **La causa del log original no se puede recuperar** —el propio log la truncó— y hoy arranca bien, así que se dice como lo que es. Pero al medir apareció la pista: los reinicios del log los disparaban cambios en ficheros de **test**, o sea servidor con `--reload` mientras la suite corría, y la suite completa da hoy un único rojo, `tests/test_prompts.py`, que **pasa aislado**. Mismo mecanismo: la BD de desarrollo mutando mientras otra cosa la usa. De ahí el **Bloque LEG**, planificado y **no ejecutado** a petición del usuario. Al planificarlo se resolvió también su pregunta —si esos prompts eran de las pruebas o de AutomatIA—: son de AutomatIA, y el consumidor que los leía está huérfano con 0 % de cobertura. |
| 2026-08-18 | Bloque GUI (GUI.1–GUI.6) — Lo que la interfaz no dejaba hacer | **Cursor: GUI.6 ✅. BLOQUE GUI COMPLETO. Suite backend: 2284 passed, 3 skipped, 0 fallos** (9 m 35 s); **frontend: 329 passed, 0 fallos**; `tsc` limpio. Nace de una observación del usuario al ir a probar**: «se han creado muchas plantillas, se ven en dos pantallas y no hay ninguna opción para borrarlas o editarlas». Al mirarlo no faltaba el botón: **faltaban los endpoints**, y detrás aparecieron cuatro huecos más del mismo tipo —puerta abierta, camino cortado— que sumados hacían que **la PRUEBA H del `.bat` no se pudiera hacer**. Decisiones del usuario: arreglar los cinco antes de escribir la guía, y **archivar en vez de borrar**. **GUI.1+GUI.2**: `archived_at` + `DELETE` (idempotente), `POST /restore`, `PATCH` para renombrar, `?include_archived`, y **409 `TEMPLATE_ARCHIVED`** al crear informes con una retirada —sin eso, archivar solo la escondería y cualquier enlace guardado seguiría usándola—; la pantalla gana renombrar en línea, retirar con confirmación que dice qué **no** se pierde, y ver/recuperar las retiradas. **Y se retira el alta que había ahí**, porque mandaba `spec_json: {}`: una plantilla sin bloques da un informe vacío y no hay editor de bloques con el que arreglarla, así que la mitad de lo que ensuciaba la lista lo producía ese botón. **GUI.3**: `_BLOCK_KINDS` no incluía `DATA_TRANSFORM` —con un «Do NOT use any block kind not in this list» detrás—, así que **todo el ETL de PRO.4 y PRO.9 era inalcanzable para una persona**: no hay ninguna pantalla que configure bloques, solo entran por el borrador. Y la lista de tipos de gráfico del prompt estaba **escrita a mano con cinco**, así que los seis de PRO.8 no se podían pedir. Ahora los tipos y el catálogo de operaciones **salen del contrato**; el validador acepta que un CHART/TABLE cuelgue de una transformación (la cadena real es extraer→limpiar→dibujar, y exigir `DETERMINISTIC_DATA` la tumbaba justo al meter la limpieza) y comprueba el `source_block_ref` de la transformación. **GUI.4 sale de verificar GUI.3 con el modelo real**: pidió un `barh` y salió **vertical, con 0 1 2 3 en el eje y las etiquetas cruzadas**. Dos defectos, y el primero es de diseño mío: **PRO.8 hizo que `x_column` significara «la categoría»** y cruzaba los ejes por detrás, mientras cualquiera que lea los nombres pone el valor en `x_axis` porque su `x_label` habla del eje X — los dos cruces se anulaban; y el formateador de cifras decidía el eje numérico **por el tipo de gráfico**, así que escribía el índice encima de los nombres. Ahora «x» significa el eje X y el eje numérico se decide **por los datos**. **GUI.5**: el «Aplicar» del copiloto escribía en `pendingAction` y **nadie lo lee** (`useCopilotAction` no lo usa ninguna pantalla) — pasa a copiar, que es algo que de verdad ocurre; y la vista previa mostraba la palabra «Error» a secas donde el servidor decía en un 409 qué bloque falta aprobar. **GUI.6**: la guía con capturas, el `.bat` con la PRUEBA H rehecha (ANSI sin BOM, 0 bytes fuera de ASCII) y el `rationale` del borrador en el idioma de la petición. **Verificado de punta a punta con `gemini-2.5-flash` y una hoja con importes en texto**: propone extraer → `to_number` → `compute_column` → `sort_rows` → tabla → `barh`, las cuatro cifras cuadran a mano (128.340,55 / 150.000 = 85,56 %) y el gráfico sale publicable. Migración `e5x6a7b8c9d0` aplicada. |
| 2026-08-17 | (planificación PRO.8 + PRO.9) | **Bloque PRO reabierto con dos prompts, antes de las pruebas manuales**, a petición del usuario, con el mismo criterio para los dos: «los más usuales se elegían de forma determinista sin tener que programarlos». Al comprobarlo, en los dos casos apareció algo que no esperaba. **Gráficos (PRO.8)**: mi razón en PRO.5 para dejar fuera los diez tipos del legacy —«código especulativo»— estaba incompleta, porque juzgué los tipos sin comprobar **cuál es la alternativa cuando faltan**, y la alternativa es generar código: `translate_nl_to_config(target_kind="chart_config")` **no devuelve una configuración**, llama a `chart_factory.generate_script()`, así que «barras horizontales» cuesta modelo + auditoría + sandbox para lo que es `orient="h"`. Y hay un hueco peor que los tipos: **una plantilla no puede pedir un título ni una etiqueta de eje** —`ChartConfiguration` los tiene, `ChartBlockConfig` no los expone— y **`show_values` está declarado y el renderizador lo ignora**. De los diez tipos, seis son parámetros de lo que ya hacemos (`barh`, `bar_grouped`≡`color_by`, `line_multi`≡`color_by`, `bar_stacked`, `donut`, `bubble`) y dos son familia nueva y estándar (`boxplot`, `heatmap`); `violin` y `pairplot` se quedan fuera (exploración estadística, y `pairplot` devuelve un grid que no cabe en `fig, ax`). **ETL (PRO.9)**: lo contrario de lo esperado —**lo determinista del legacy está portado al 100%** en PRO.4; su catálogo son exactamente las once operaciones que ya están, así que no hay migración pendiente—. Lo que falta es **cobertura** de cuatro operaciones que **ni el legacy ni nosotros** teníamos y que una hoja de cálculo real necesita para llegar a un informe: **columna calculada** (`pct = obligaciones / credito * 100`, que es *la* transformación de un informe presupuestario), **números en formato español** llegados como texto —la peor de las cuatro porque es **silenciosa**: `groupby.sum()` sobre texto concatena o revienta y el informe sale con una cifra mal **y sin error**—, **ordenar** y **unpivot** (las hojas institucionales son anchas y un informe necesita largo; tenemos `pivot`, no el inverso). Hoy cada una cuesta **tres reintentos del modelo** —el prompt le dice que devuelva lista vacía si no cabe, y `_parse_operations_payload` trata la lista vacía como error— **más un script generado, auditado y ejecutado**. Decisión de diseño tomada en la planificación: `compute_column` **sin campo `formula` y sin `eval`** —sería una puerta trasera a exactamente lo que el auditor de PRO.1 prohíbe en un script—; forma declarativa (columna o constante, operador de conjunto cerrado, columna o constante, factor opcional) y lo compuesto se consigue **encadenando** operaciones, que es más verboso, es auditable y se puede pintar en una pantalla. |
| 2026-08-17 | PRO.7 (cierre) — Lo irreducible, el `.bat` y las comparativas | **Cursor: PRO.7 ✅. BLOQUE PRO COMPLETO.** `docs/PRUEBAS_MANUALES.md` recoge los nueve caminos que el agente recorrió con modelos reales para que nadie los repita, y la matriz de lo irreducible gana **las dos filas que de verdad lo son**: la **calidad del script sobre un fichero real de la UJI** —el agente prueba con un Excel sintético cuyas columnas inventó él mismo, así que no puede saber si la tabla es la que se pedía, ni si el script se ha comido una fila de totales— y **si la respuesta del copiloto es útil o sólo correcta**, que lo dice quien preguntaba. La fila de la exportación cambia de sentido: desde PRO.5 **se descarga de verdad**, así que lo humano es abrirla en Word y juzgar la maquetación, no comprobar que existe. **`pruebas_manuales_bloqueVER.bat` ampliado en vez de crear otro guion** —es el mismo módulo—: pasa a llamarse «BLOQUES VER y PRO», la prueba B lleva ya la URL real de descarga y entran las pruebas **F** (script sobre fichero real) y **G** (utilidad del copiloto). ANSI sin BOM verificado (`40 65 63 68` = `@ech`) y **0 bytes fuera de ASCII**: la codificación no puede fallar. De paso, la trampa del `.ps1` en UTF-8 mordió como avisa CLAUDE.md —PowerShell 5.1 lo leyó como ANSI y dejó mojibake donde había una `ñ`—, corregido. **Dos comparativas escritas**, que es lo que evita volver a leer el legacy: `docs/COMPARATIVA_LEGACY_INFORMES.md` (qué se portó de `AutomatIA`, qué se dejó y por qué, y **lo que había aquí y el legacy no tenía**: sandbox real, cola con re-test y hash, anonimización de los datos de prueba, contrato de UI dirigido por el servidor y manifiesto de ejecución) y `docs/COMPARATIVA_ETL_LEGACY.md` (las diez operaciones y las tres divergencias). **Suite backend completa desde Git Bash: 2197 passed, 3 skipped, 0 fallos** (11 m 42 s). |
| 2026-08-17 | PRO.6 (RED/GREEN + navegador) — El copiloto, cableado y alcanzable | **Cursor: PRO.6 ✅ → PRO.7.** El copiloto estaba inalcanzable **por los dos lados**: `get_copilot_service` era un stub que devolvía 503 siempre, y `CopilotPanel` → `DrawerHub` → `FocusLayout` existían con sus tests sin que **ninguna ruta montara el layout**. Cableado con el modelo del nivel 1 y el servicio de embeddings de la cascada, y el **503 dice cuál de los dos falta**: son dos configuraciones distintas y «copilot service not configured» obligaba a ir al log del servidor. **El índice se construye al primer uso, no al arrancar**, con una bandera propia y no «¿hay fragmentos?» —un `docs/` vacío haría que cada pregunta lo recorriera otra vez—, y **en lote si el servicio de embeddings lo admite**: de uno en uno son ~387 peticiones seguidas, y la primera pregunta es la que las paga. Se conserva **el retriever** entre peticiones y no el servicio entero, así que cambiar el modelo en el panel tiene efecto sin reiniciar y el índice —lo que cuesta— sobrevive. **Montado como en el legacy**: `focus_manager.py` entra en modo foco y `drawer_hub.py` pinta un **cajón lateral** con tres pestañas más el copiloto, así que aquí va en la pantalla del informe con un botón que lo abre directamente en su pestaña, y **arranca cerrado** —un panel que nadie ha pedido tapa media pantalla—. **Un hallazgo del navegador, y de los buenos**: preguntado desde un informe, el copiloto contestaba «no tengo esa información» a una pregunta cuya respuesta está en `docs/`, mientras que por API —sin módulo— la contestaba y citaba el fichero. La causa: el panel manda `module: 'redaccion'` y `_infer_module` clasifica como `redaccion` sólo los ficheros cuya **ruta** contiene «redaccion», que en `docs/` es exactamente uno; el filtro era estricto. Ahora el módulo **prefiere y no excluye**: la documentación general vale para cualquier módulo y la de otro módulo se queda fuera, que es lo que el filtro pretendía. **Verificado en vivo**: primera pregunta **56 s** (construye el índice sobre los `docs/` reales), segunda **3,3 s**; las respuestas son correctas y citan ficheros reales —«el destino es una VM, no Cloud Run» citando `docs\DECISION_EXTRACCION_Y_DESPLIEGUE.md`, y el formato del corpus citando `docs\CONTRATO_MD_CORPUS.md`—; y el cajón se abre desde el botón de la pantalla del informe con su pestaña activa, sus cuatro sugerencias y sus cuatro modos. **Nota de método**: los clics sintéticos de la extensión no llegaban al botón (el mismo problema que con el textarea del asistente de scripts), así que se comprobó con `element.click()` real desde la consola; el `aria-expanded` y el cajón responden. 16 tests nuevos de backend + 4 de frontend. **`tests/modules/redaccion` + `api`: 747 passed, 3 skipped, 0 fallos**; frontend **337 passed, 0 failed**; `tsc` limpio. |
| 2026-08-17 | PRO.5 (RED/GREEN + verificación) — Gráficos dentro del informe, y el informe descargable | **Cursor: PRO.5 ✅ → PRO.6.** El prompt lo llamaba «la pieza más avanzada» porque `DeterministicChartService`, `ChartFactory`, `render_chart_from_script` y `ChartHandler` existen con sus tests desde 9R.5.7. Y al recorrerlo: **nadie los llamaba**. `blocks/handlers.py` **sólo lo importan los tests** —cero referencias en `app/`— y el grafo no tenía nodo de gráficos, así que un bloque CHART en una plantilla **no dibujaba nada**: ni imagen, ni error, ni aviso. Nuevo `ChartRenderNode` entre la transformación y la comprobación de calidad (dibuja los datos ya limpios), reutilizando el handler en vez de reimplementarlo. **La imagen no va en la base de datos**: un PNG de 30-80 KB por gráfico dentro de `content_json` hincharía la tabla de bloques y viajaría en cada lectura del workspace, así que va a `StorageService` —igual en local, MinIO y GCS— y el contenido guarda su clave. **Y el mismo fallo que PRO.4 arregló en la transformación**: `ChartHandler._resolve_data` buscaba sólo `content["rows"]`, así que un gráfico colgado de un bloque de extracción se quedaba sin datos y dibujaba un lienzo vacío. **Dos hallazgos mayores del último tramo, los dos del producto y no de los scripts**: **(1)** la exportación a DOCX hacía `add_paragraph(block.html)`, o sea que metía el HTML **como texto** — desde PRO.3, cuando la vista previa empezó a pintar tablas, el documento llevaba dentro `<table><thead><tr><th>capitulo…` escrito a mano; ahora una tabla es una tabla del documento, un gráfico una imagen incrustada y las métricas viñetas, con el contenido estructurado viajando en `PreviewBlock` **excluido de la serialización** (`bytes` no es JSON: el primer intento devolvió un 500 al pedir la vista previa, y el `html` ya lleva la imagen incrustada, así que el contrato del frontend no cambia y el payload no lleva la imagen dos veces). **(2) El informe no se podía exportar**: `ExportService` de redacción también estaba escrito, probado y **sin ninguna ruta que lo sirviera**, así que la prueba manual del bloque VER —«abrir la exportación en Word y en Adobe»— no tenía de dónde descargar nada. Nuevo `GET /redaccion/workspaces/{id}/export`, que sirve **DOCX y no PDF a propósito**: convertir a PDF necesita LibreOffice en la máquina y VER.7 ya enseñó lo que pasa cuando se promete un PDF y se entrega un DOCX disfrazado. **Verificado en vivo**: informe `assembled` con extracción + transformación + gráfico, la vista previa con el PNG de matplotlib como data URI (35 KB) y el DOCX descargado por HTTP con **2 tablas reales, 1 imagen y 0 párrafos con HTML** (56 KB). **Leído el legacy** (`chart_configuration.py`, 198 l.): su configuración es más rica —15 tipos de gráfico frente a 5, título y etiquetas de eje, estilo, tamaño, leyenda, valores encima, orientación, `bins`, `explode`, `donut_ratio`, `sort_values`—. **No se porta aquí**: el criterio de PRO.5 es que el gráfico llegue al informe, y añadir diez tipos que nadie ha pedido sería especulativo; queda anotado para cuando alguien pida un `boxplot`. 25 tests nuevos. **`tests/modules/redaccion` + `api`: 737 passed, 3 skipped, 0 fallos**; frontend **346 passed, 0 failed**; `tsc` limpio; contrato regenerado (129 rutas). |
| 2026-08-17 | PRO.4 (RED/GREEN + verificación) — ETL: el nodo tiene modelo, y el catálogo del legacy | **Cursor: PRO.4 ✅ → PRO.5.** **Lo que descubrió leer el legacy** (`deterministic_etl_service.py` 350 l., `etl_service.py` 424 l., `etl_factory.py` 369 l.): los dos catálogos de operaciones **casi no se solapaban**. El nuestro se orientó a **análisis** —y su propio docstring lo decía: «en lugar de limpieza»— y el del legacy es de **limpieza**. Y limpiar es lo que hace falta primero: un Excel real llega con columnas que no se usan, fechas en tres formatos, filas duplicadas y celdas vacías, así que «transformar» se quedaba en agrupar lo que ya estuviera limpio. **Portadas las diez que faltaban** (`drop_columns`, `rename_columns`, `merge_columns` —conservando la posición de la primera, como allí—, `reorder_columns`, `format_dates`, `replace_values`, `normalize_text` con sus cinco modos, `fill_nulls`, `remove_duplicates`, `drop_null_rows`), con sus tests. **Tres divergencias deliberadas, escritas en `docs/COMPARATIVA_ETL_LEGACY.md`**: (a) una columna que no existe **falla** en vez de ignorarse —el legacy hacía `return df`, que en una interfaz es cómodo y en un informe de fondo es un renombrado que se pierde y una tabla sin tocar sin decirlo—; (b) las fechas ambiguas se leen **con el día primero**, porque `01/03/2026` es el 1 de marzo y pandas por defecto lo lee como 3 de enero, y equivocarse ahí no da error: da un informe con las fechas cambiadas; (c) lo que no se entiende como fecha queda vacío y **se ve**, en vez de un `print` que nadie lee. **Cuatro costuras cortadas, arregladas**: **(1)** `_resolve_df` buscaba `content["rows"]` y el contenido de una extracción es `{tables, metrics, free_text}`, así que un bloque de transformación colgado de uno de extracción —el caso normal— recibía un DataFrame **vacío** y transformaba la nada; **(2)** el nodo se construía con `llm_service=None` porque VER.1 no le pasó `etl_llm`, así que el modo IA fallaba siempre; ahora sale de la actividad **`transformacion_etl` (nivel 2**, porque traducir a operaciones también es programar), resuelta por el catálogo de PRO.2.1 y sobreescribible desde la pantalla; **(3)** lo transformado no se veía en el informe —el contenido era `{rows, ...}` y el renderizador de PRO.3 pinta `tables`—; **(4)** el prompt del planificador enumeraba las **seis** operaciones a mano, y con dieciséis una lista a mano se queda corta en el primer cambio: `catalogo_de_operaciones()` lo genera de los propios modelos Pydantic, el mismo arreglo que VER.3 hizo con el borrador de plantillas. **Si no hay modelo del nivel 2, el modo determinista sigue funcionando** y sólo el modo IA falla con su motivo: un informe que sólo limpia y agrupa no tiene por qué depender de que haya un LLM configurado. **Verificado en vivo, los dos modos**: informe `assembled` con el bloque `DATA_TRANSFORM` en `extracted`, cabeceras `['seccion','credito_inicial']` tras renombrar y quitar columna, y las **dos tablas** en la vista previa; y en modo IA, `gemini-2.5-flash` tradujo «renombra capitulo a seccion y quita obligaciones_reconocidas» a las dos operaciones portadas (`model_used: gemini-2.5-flash`, `mode: ai`). 27 tests nuevos. **`tests/modules/redaccion` + `api` + `agents_hub/unit`: 1251 passed, 3 skipped, 0 fallos**; frontend sin fallos; `tsc` limpio; contrato regenerado (223 esquemas, con las diez operaciones nuevas). |
| 2026-08-17 | PRO.3 (verificación) — El script del modelo llega a un informe | **Cursor: PRO.3 ✅ → PRO.4.** El camino entero recorrido contra la API real —propuesta → auditoría → sandbox → validación → cola → re-test → aprobación → plantilla global → workspace → informe— y **siete hallazgos, todos arreglados con TDD**. El resumen es que **un script aprobado no había extraído nunca nada dentro de un informe**: cada pieza estaba probada por su lado y la costura no se había recorrido. **(1) El bloque que escribía `approve` no tenía la forma que lee el contrato**: `label` donde `BlockContract` espera `title` y `source_kind` donde espera `source_pipeline`, más un `options` que el contrato no declaraba —mismo patrón que el hallazgo #6 de VER.3—; ahora `DeterministicDataBlock` tiene `options` y el bloque valida. **(2) Sobre una plantilla con `spec_json: {}`** —lo que guarda el constructor, anotado en VER.3— el resultado era un spec sin secciones, sin contrato de entrada, sin contrato de UI y sin políticas: el fallo salía lejos, con un **500 al pedir el contrato de UI**, y con la propuesta ya marcada como aprobada. Ahora se completa lo que falte y **la versión resultante se valida antes de guardarla** (422 `TEMPLATE_SPEC_INVALID` en vez de una plantilla inservible). **(3) Sin contrato de entrada el script no tenía fichero que leer**, porque el artefacto se busca por los slots que declara la plantilla: el slot se crea **con el tipo del fichero con el que se probó el script** (`xlsx`→excel, `csv`→csv, `pdf_text`→pdf), que es el dato honesto que ya estaba guardado. **(4) `blocks` se escribía como diccionario** cuando la plantilla no traía ninguno, y el contrato lee una lista. **(5) El nodo de extracción no pasaba las opciones del bloque** (`options={}`), así que el código del script no llegaba y el pipeline respondía `SCRIPT_NOT_APPROVED`: el síntoma acusaba a la aprobación, que estaba bien. **(6) Llamaba `pipeline.extract`**, que `AdminScriptExtractionPipeline` no tiene —sólo `extract_async`—, y el bloque acababa en `failed` con un `AttributeError` por mensaje; la comprobación es `inspect.iscoroutinefunction`, no `hasattr`, porque un `MagicMock` tiene todos los atributos y mandaría a la vía asíncrona a cualquier doble. **(7) El más gordo, y no es sólo de scripts: los datos extraídos no se veían en el informe.** `_block_content_to_html` sólo miraba `content['text']`, y el contenido de **cualquier** extracción es `{tables, metrics, free_text}` — ninguna tabla extraída se había visto nunca en la vista previa, ni por script ni por Excel ni por PDF; y el ensamblado hacía `str(content)`, o sea que volcaba el diccionario de Python en el documento final. Ahora la vista previa pinta `<table>` con `<caption>` y las métricas con su unidad —todo escapado, que los datos vienen de un fichero que sube cualquiera— y el documento final lleva tabla de Markdown y viñetas. **Verificado de punta a punta**: el mapa determinista aprende que `admin_script` sirve a cualquier tipo de slot (un script lee lo que se le dé), y con una plantilla global nueva el recorrido acaba en workspace **`assembled`**, bloque `extracted`, y la vista previa devuelve la tabla real del Excel con sus cuatro capítulos y `porcentaje_ejecucion` a la vista. 20 tests nuevos. **`tests/modules/redaccion` + `api` + `infra`: 789 passed, 3 skipped, 0 fallos**; frontend **346 passed, 0 failed**; `tsc` limpio. **Nota de medición**: rtk reescribe la salida de vitest —incluso redirigida a fichero—, y el JSON del propio reporter dio 333, 341 y 346 en pasadas consecutivas; la cifra que cuadra con la aritmética (331 al cerrar PRO.1 + 8 + 7) es 346, y lo sólido es que **ninguna pasada tuvo fallos**. |
| 2026-08-17 | PRO.2.1 (RED/GREEN + navegador) — La biblioteca de prompts llega a las actividades | **Cursor: PRO.2.1 ✅ → PRO.3.** Lo que el usuario pidió: **el modelo por tier se asigna desde el frontend** (ya se podía, en `/hub/llm-configs`) y **el tier de cada actividad, preasignado en código, se puede sobreescribir desde una biblioteca de prompts**, como en NiceGUI. Nueva pantalla **Prompts de actividad** (`/hub/activity-prompts`) con el nivel y el texto de cada actividad, y `hub_activity_prompts` (migración `d4w5z6a7b8c9` aplicada) guardando **sólo la excepción**. **La decisión de diseño**: no se reutilizó `hub_prompt_templates` haciendo `chatbot_id` nullable, porque en Postgres una restricción unique con NULL **no colisiona** —`(chatbot_id, slug, language)` dejaría de ser única justo para las filas nuevas, dos overrides de la misma actividad conviviendo— y la pantalla que filtra por chatbot perdería el sentido. **Dos reglas que evitan configuración muerta y un prompt congelado**: una actividad **no se puede inventar** desde la pantalla (422 `UNKNOWN_ACTIVITY`: si no está en el catálogo de código, nada la consumiría) y **el texto por defecto no se copia** a la base de datos —se muestra como marcador—, porque copiarlo haría que mejorar el prompt en el código ya no llegara a quien lo abrió; un texto en blanco significa «usa el del código», que es lo que permite volver atrás sin borrar la fila por SQL. Un `{variable}` que la actividad no declara se rechaza **al guardar** (422 `UNKNOWN_VARIABLE`) en vez de aparecer como un hueco dentro del prompt de la petición siguiente. La frontera edge/cloud queda intacta: el override viaja como `ActivityPromptOverride` del `ConfigProvider`, así que `modules/redaccion` no importa ningún modelo de configuración; el router es cloud y lee el catálogo del módulo edge, que es la dirección permitida. El selector de nivel tiene **cuatro opciones y la primera dice cuál es el defecto** («Por defecto: nivel 2»), copiado del editor del legacy: un hueco vacío no se puede interpretar. **Verificado de punta a punta**: puesto el nivel 3 desde la pantalla, la propuesta siguiente la escribe **`gemini-2.5-pro`** en vez de `gemini-2.5-flash`; borrado el override, vuelve a `flash`. **Un hallazgo propio, arreglado con su guardarraíl**: el primer guardado dio **500 con `MissingGreenlet`** —leer `fila.template_text` después del `commit()`, con los atributos expirados: el mismo patrón que VER.4 encontró cinco veces—; el doble de sesión de los tests no lo reproduce, así que hay ahora un fichero de tests **contra base de datos real** para este router. De paso, quitado un `_pro21_clave` que había colado en el código de producción para que el doble supiera qué clave se pedía: el test lo lee de los parámetros compilados. **Alcance declarado**: el catálogo arranca con las dos actividades de PRO.2 y crece cuando se cablea cada consumidor —ETL en PRO.4, gráficos en PRO.5, copiloto en PRO.6—; una actividad en el catálogo que nadie consume sería configuración que parece funcionar. **Suite backend completa: 2133 passed, 3 skipped**, con el guardarraíl de la frontera actualizado a conciencia (`hub_activity_prompts` con su razón escrita). Frontend **346 passed, 0 failed** —medido con el reporter JSON a fichero, porque los `.log` de rtk salen truncados y las cifras que imprime no cuadran—; `tsc` limpio; contrato regenerado. **Hallazgo preexistente, NO arreglado y documentado**: `tests/test_prompts.py` es inestable con `-n auto` (pasó en una pasada y falló en otra) porque **siembra la base de datos real del desarrollador** (`seed_system_prompts()` sobre `server_engine`) y apunta a `ExtractionServiceConfig`, la biblioteca de prompts **del legacy de este repositorio** —cuya interfaz ya está en `_legacy_nicegui/admin_prompts.py`, pero cuyo modelo, semillas y scripts siguen en el árbol activo junto al `main.py` de la raíz—. Retirarlo entero es un prompt propio: toca la raíz y `app/database/`. |
| 2026-08-17 | PRO.2 (RED/GREEN + navegador) — Tier 2 escribe el script, Tier 3 lo audita | **Cursor: PRO.2 ✅ → PRO.2.1.** `POST /scripts/propose` deja de ser un stub 503: **el nivel 2 escribe y el nivel 3, superior, audita**. El mapa actividad→nivel es un **catálogo en código** (`actividades_llm.py`), no literales en la raíz de composición, que es lo que PRO.2.1 podrá sobreescribir. El 503 dice **qué nivel falta y para qué servía** —con dos en juego, «falta un modelo» obliga a adivinar cuál—. La auditoría con modelo va **encima** de la determinista y no puede levantar ni un hallazgo: su veredicto (`acepta`/`duda`/`rechaza`) viaja con la propuesta en columna propia (`model_review_json`, migración `c3v4y5z6a7b8` aplicada) y llega a la cola de revisión; **sobre un crítico no se pide** —no se va a ejecutar nunca, así que es pagar una llamada por una opinión sobre código muerto—, y un auditor que responde en prosa queda en `duda`, nunca en un «acepta» silencioso. El prompt del sistema nombra ahora los prohibidos por forma (`getattr`, `globals`, `__class__`…), los módulos de denegación explícita de PRO.1 y las rutas absolutas, **leídos del auditor** en vez de reescritos a mano. `/redaccion/scripts/wizard` entra en el menú de Informes. **Verificado en vivo con modelos reales**: `gemini-2.5-flash` (nivel 2) escribe el script, la determinista lo deja en SAFE y `gemini-2.5-pro` (nivel 3) lo devuelve como `duda` con dos objeciones que un AST no puede ver («asume que las columnas se llaman exactamente así», «asume que están en la primera hoja»); las dos configuraciones se crearon **desde la pantalla** `/hub/llm-configs`, que ya asignaba tier y por defecto. **Tres hallazgos del recorrido, todos arreglados con TDD.** **(1) El fichero de prueba no llegaba nunca al sandbox, y ningún script podía leerlo**: `AdminScriptExtractionPipeline` mandaba `Path(file_ref.bucket)/file_ref.key` como `file_path`, una ruta del host de la API —y con `StorageService` sobre GCS no es ni una ruta—, así que dentro del contenedor no existía nada ahí y `pd.read_excel(file_path)` fallaba **siempre**. Ahora el **contenido** viaja al sandbox (`file_bytes_b64` + `file_name`), leído por `StorageService`, y el sandbox lo materializa en su propio temporal: es lo que `/execute-chart` y `/execute-etl` ya hacían con `data_csv`, así que la forma estaba inventada en el propio sandbox. Un fichero que no está en el almacén da `TEST_DATA_NOT_FOUND` en vez de un traceback de pandas. **(2) El asistente era un esqueleto de la fase 3 en adelante**: la subida mandaba `new Blob()` en vez del fichero elegido, la anonimización `file_ref: {bucket:'', key:''}` con `kind:'csv'` fijo, y la prueba en sandbox lo mismo — cada paso avanzaba y ninguno decía nada. Ahora se guarda la referencia subida, `kind` sale de la extensión, se usa el sintético si se anonimizó (y `use_real_data` se declara, que es lo que permite al servidor prohibirlo para plantilla global), y **sin fichero el botón de probar está deshabilitado con su motivo**. El paso del PDF sólo aparece con un PDF delante. **(3) El marcador de la caja de descripción estaba en inglés** dentro de una interfaz en valenciano — el primer texto que lee quien entra por el menú nuevo. **Verificado de punta a punta con datos reales**: Excel de ejecución presupuestaria → sandbox Docker → tabla con `capítulo`/`credito_inicial`/`obligaciones_reconocidas` y métrica `porcentaje_ejecucion: 68.52 %` (166.500/243.000, comprobado a mano) → validación 200. **Fuera del alcance, para PRO.3**: `save-to-private-template` da `NO_TARGET_TEMPLATE` porque el asistente no envía `target_template_id`; incrustar el script en una plantilla es justo el objeto de PRO.3. 22 tests nuevos de backend + 8 de frontend. **`tests/modules/redaccion` + `core` + `infra`: 564 passed, 3 skipped, 0 fallos**; sandbox 13 passed; frontend **326 passed, 0 failed** (contados por el reporter JSON); `tsc` limpio; contrato regenerado. Documentado en `docs/NIVELES_DE_MODELO.md`: qué significa cada nivel, quién lo pide y qué pasa si falta. |
| 2026-08-17 | Bloque PRO ampliado a mitad de camino — PRO.2.1 nuevo: la biblioteca de prompts llega a las actividades | **Cursor: sin cambio (→ PRO.2).** El usuario pidió, tras cerrar PRO.1, que **la asignación de modelo por tier se pueda hacer desde el frontend** y que el tier de cada actividad, aunque venga preasignado en código, **se pueda sobreescribir desde una biblioteca de prompts** como la que tenía la aplicación NiceGUI. Leído el legacy (`server/app/database/seeds_prompts.py`, 579 l., y `server/app/ui/admin_prompts.py`, 295 l.): `SystemPrompt` con `name`/`version`/`content`/`context_type`/**`tier`** y un editor con buscador, filtros por tarea y fase, **radio de tier de cuatro opciones** —«por defecto (usa Tier N)», 1, 2, 3— y variables `{...}` detectadas en vivo; el defecto por tarea (`DEFAULT_TIER_MAPPING`) vivía **en código** y la base sólo guardaba el override. **Comprobado contra este código, y las dos mitades ya están hechas aquí**: `/hub/llm-configs` asigna `tier` y `is_default` por modelo desde el formulario (con el relevo de FIX.1), y `/hub/prompts` + `/hub/brain` son ese editor ya portado —texto con `{variables}` resaltadas, chips de tier y **`default_tier` + `override_tier` editables**—. **Lo que falta es exactamente la frontera**: `hub_prompt_templates.chatbot_id` es NOT NULL, así que la biblioteca sólo ve prompts **de chatbot**, y los de las actividades del módulo de Informes —generar script, auditarlo con modelo, ETL, gráficos, proponer plantilla, redactar bloque, copiloto— están escritos en Python y no aparecen en ninguna pantalla: no se puede afinar su texto sin desplegar ni decidir con qué nivel de modelo corre cada una. **Dos cambios en el plan**: PRO.2 escribe el mapa actividad→tier como **catálogo en código** en un solo sitio (no un `2` y un `3` sueltos en la raíz de composición), que es lo que PRO.2.1 podrá sobreescribir; y **PRO.2.1 nuevo (Opus)** añade el modelo de configuración propio de la actividad —**no** reutilizando `hub_prompt_templates` con `chatbot_id` nullable, porque en Postgres una restricción unique con NULL no colisiona y la clave dejaría de ser única justo para las filas nuevas—, su resolución (código por defecto, base de datos como override, texto vacío = el del código), los consumidores pidiéndolo en vez de llevarlo dentro, y la vista de actividades en la biblioteca que ya existe. Dos restricciones que evitan configuración muerta: **una actividad no se puede inventar desde la pantalla** (si no está en el catálogo, nada la consume) y **el texto por defecto no se copia a la base al abrir la pantalla** (copiarlo congela el prompt y mejorarlo en el código ya no llegaría). |
| 2026-08-17 | PRO.1 (RED/GREEN) — La auditoría recupera sus niveles y sus rutas | **Cursor: PRO.1 ✅ → PRO.2.** `ScriptSecurityAuditor` pasa de binario a **`SAFE`/`WARNING`/`CRITICAL`** con `puede_revisarse` y **número de línea en cada hallazgo**, y recupera las **rutas absolutas** que la aplicación nueva había perdido: un `pd.read_excel('C:\\Users\\...')` no usa ninguna llamada prohibida y hasta hoy pasaba la auditoría. **La decisión de fondo, y no estaba en el prompt: el legacy tenía DOS listas, no una.** `audit_code()` las colapsaba en CRITICAL, pero la distinción vivía en los datos —`FORBIDDEN_IMPORTS` frente a «no está en la lista blanca»— y es justo lo que hace segura la graduación: sin ella, «fuera de la lista blanca = warning revisable» convertía `import os`, `socket`, `requests` o `pickle` en algo que un administrador podría aceptar mirándolo. Portada como `_MODULOS_PROHIBIDOS` (33 módulos): `csv` es un hueco en una lista, `os` es una capacidad. **Corregido un fallo del legacy al portarlo**: su regex `[a-zA-Z]:/` marca como ruta de Windows cualquier `https://` —dentro hay un `s:/`— y daba un motivo falso; ahora la letra de unidad exige un carácter no alfanumérico delante, con su test. **Llamadores adaptados sin relajar ninguna puerta de ejecución**: `approved` sigue significando «sin hallazgos», así que `AdminScriptExtractionPipeline`, `chart_renderer` y el sandbox se comportan **exactamente** igual que ayer; lo que cambia es quién puede llegar a una revisión humana — `submit-for-review` y `approve` miran `puede_revisarse` (nuevo 422 `AUDIT_CRITICAL`), y `save-to-private-template` **sigue exigiendo `approved`** porque ahí no hay revisor: la puerta laxa es la que tiene una persona detrás, no la que no la tiene. **Cerrado el hueco que eso abría**: `/test` guarda el `ExtractionResult` pase lo que pase, y un script rechazado por el sandbox deja un resultado con aviso `error`, cero datos y **hash perfectamente estable**, así que el re-test del admin coincidía y la propuesta llegaba a aprobarse sin haber extraído nada nunca (nuevo 422 `TEST_FAILED`). **Dos hallazgos vivos y preexistentes, arreglados con su guardarraíl**: (1) el auditor del sandbox se quedaba **más laxo que el de la API** en cuanto ésta se endurecía, y el contrato de SBX.4 («si divergen, el sandbox es más estricto») vivía sólo en un docstring — nuevo `test_auditores_no_divergen.py`, que no compara listas sino veredictos y carga la copia del sandbox por ruta; (2) **import circular real** en `redaccion/pipelines/__init__.py`: reexportaba `admin_script_pipeline`, que importa `sandbox_client`, que importa `pipelines.contracts` — importar `sandbox_client` con nada cargado antes moría con `partially initialized module`, y en la suite completa no se veía porque otro módulo llegaba primero. Nadie usaba las reexportaciones. (3) Y con el contrato regenerado, `tsc` daba **TS2322 en `WorkspacePage`**: `WorkspaceEditor` declaraba su propio bloque con `[key: string]: unknown`, y una `interface` generada no es asignable a un tipo con firma de índice — ahora usa `BlockStateOut`, que es lo que manda la regla de contrato. **Contrato regenerado** (`AuditFinding`, `RiskLevel` nuevos) y la pantalla del asistente muestra tres estados en vez de dos, con el ámbar de «puede aceptarlo una revisión» y la línea de cada hallazgo; i18n en es/ca/en. 45 tests nuevos. **`tests/modules/redaccion` + `core` + `infra` + `api`: 751 passed, 75 skipped, 0 fallos**; sandbox 13 passed; frontend **331 passed**; `tsc` limpio. |
| 2026-08-17 | Planificado el Bloque PRO — y reescrito el mismo día con el legacy delante | **Cursor: → PRO.1.** Planifiqué primero un bloque de 3 prompts para cablear `scripts/propose` y `copilot/ask`, y el usuario avisó de que **todo eso ya estaba programado, probado y funcionando en la aplicación NiceGUI**, que vive completa en `C:\Users\fabra\Documents\AutomatIA`. Reescrito a **7 prompts**, y el aviso cambió cuatro cosas. **(1) La auditoría del legacy es mejor que la nueva**: `automatia_shared/core/security.py:audit_code()` da `SAFE`/`WARNING`/`CRITICAL` con motivos y, **antes del AST**, caza por regex rutas absolutas de Windows y de sistema Linux — comprobación que la nueva **no tiene**, así que hoy un script que abra `C:\Users\...` pasa su auditoría. La nueva, en cambio, es binaria. Y hay un **segundo** auditor legacy, `ExternalScriptAuditService`, con cuatro severidades, número de línea y la distinción `is_safe` vs `can_proceed_with_review`. **(2) Dos niveles de modelo**, como pidió el usuario: Tier 2 escribe, Tier 3 audita; hoy **todas las configuraciones de LLM son de nivel 1**, así que el nivel existe en el código y no en los datos. **(3) ETL y gráficos son lo más usado en informes, y el ETL está a medias por mi culpa**: `DataTransformationNode` construye `ETLService(llm=None)` porque la raíz de composición de VER.1 no le pasó `etl_llm`, y **no hay ningún endpoint de ETL**; el legacy trae `deterministic_etl_service` (350 l.), `etl_service` (424 l., con auditoría antes del sandbox) y `etl_factory` (369 l.). **(4) El copiloto y el modo foco estaban montados en NiceGUI** —`focus_manager.py`, `drawer_hub.py`, `copilot_context_service.py` (42 KB)—, así que no hay que decidir dónde van: hay que mirar cómo estaban. **Decisión razonada sobre los scripts libres**: van al bloque de automatizaciones, no aquí, y el propio legacy da el argumento —tenía **dos** auditorías porque el modelo de confianza es distinto: la del código que genera la plataforma y la del que trae una persona de fuera—. En un informe el contrato es estrecho y comprobable por máquina, y cualquier código que lo cumpla ya entra por la cola de VER.5; lo que aporta `script_adaptation_service` sólo se paga cuando lo que llega es cualquier cosa: RPA, carpetas vigiladas, ERP. Nota: los fuentes de ETL **están borrados de `client_app/`** en este repo (solo quedan los `.pyc`) y sí están en `AutomatIA`. |
| 2026-08-17 | ~~Planificado el Bloque PRO — las dos puertas al LLM que VER dejó fuera~~ (sustituido por la entrada de arriba) | **Cursor: → PRO.1.** A petición del usuario tras explicarle qué significaba «`scripts/propose` y `copilot/ask` siguen en 503»: «es lo que acaba de dar sentido al bloque de los informes». Y tiene razón en el orden: la cola de aprobación de scripts **ya llega hasta aprobado** contra el sandbox real (VER.5), pero la propuesta hay que sembrarla en base de datos a mano. 3 prompts. **Lo medido antes de planificar, que es lo que cambia el plan**: cablear los dos servicios son veinte líneas cada uno —están escritos y probados desde 9R—, pero `ScriptSecurityAuditor` pone `approved = not findings` y **una advertencia basta**: un `import csv` deja la propuesta sin aprobar exactamente igual que un `eval()`, y la lista blanca son 14 módulos que no incluyen `csv`, `os` ni `pathlib`. Encima, el prompt del sistema lista la lista blanca y las llamadas prohibidas pero **no los nombres que el auditor bloquea por su forma** (`getattr`, `globals`, `locals`, `vars`, `__class__`, `__dict__`…), que marca como CRÍTICO. De ahí la decisión del bloque: **reintentar con los hallazgos del auditor como respuesta** —el patrón de `etl_factory` que VER.3 ya aplicó al borrador de plantillas— y **no tocar la lista blanca para desatascar una prueba**; si aparece un módulo que de verdad hace falta, se añade con su razón escrita. Del copiloto: `DocsRetriever` indexa `docs/**/*.md` **en memoria y sin persistir** —medido: 46 ficheros, 465 KB, ~387 fragmentos, o sea 387 llamadas de embedding por índice—, así que se construye **al primer uso y no al arrancar**; pagar eso en cada arranque es un coste que casi nunca se aprovecha, y en modo edge con embeddings locales retrasa el arranque. Y hay que hacerlo **alcanzable**: `CopilotPanel` cuelga de `DrawerHub` → `FocusLayout`, **que no monta ninguna ruta**, y `/redaccion/scripts/wizard` tampoco está en el menú: son la tercera y la cuarta pantalla del módulo con la misma enfermedad que VER.3 y VER.4 destaparon. |
| 2026-08-17 | VER.9 (cierre) — Lo irreducible, el `.bat` y el informe | **Cursor: VER.9 ✅. BLOQUE VER COMPLETO.** `docs/PRUEBAS_MANUALES.md` recoge lo recorrido por el agente el 2026-08-17 —los seis caminos con su evidencia— para que nadie lo repita, y la matriz de lo irreducible queda con **cinco filas** y dos nuevas: el **PDF real** del informe de calidad (esta máquina no tiene LibreOffice, así que la exportación cae a DOCX y desde VER.7 lo dice en vez de mentir) y el **rastreo de una web con enlaces en HTML** (el sitio local del corpus pinta sus fichas con JavaScript, así que la profundidad no se puede medir contra él). La fila del borrador LLM cambia de sentido: **ahora sí hay algo que juzgar**, porque el informe se genera de verdad. `pruebas_manuales_bloqueVER.bat` en la raíz, **ANSI sin BOM** (primeros bytes `40 65 63 68` = `@ech`) y **enteramente ASCII**, así que la codificación no puede fallar; probado en seco. Cinco pruebas humanas: calidad editorial del informe, exportación abierta en Word y Adobe, anonimización sobre datos reales, rastreo de `www.uji.es` y comparación en vivo del asistente de Gerencia contra su gemelo agéntico. **Suite completa desde Git Bash: 2038 passed, 3 skipped, 0 fallos** (14 min); frontend **330 passed**; `tsc` limpio; contrato regenerado. |
| 2026-08-17 | VER.8 (verificación en navegador) — Curación C: huecos, caducidades y publicación | **Cursor: VER.8 ✅ → VER.9.** Los tres caminos responden: huecos **200** (`gaps_found: 0`), caducidades **200** (`stale_found: 0`) y publicación con selección creada, candidatas marcadas y **la selección sólo en el chatbot destino** —los otros tres siguen a cero, que es lo que había que comprobar—. **La relación entre caducidad y vigencia, escrita**: no se contradicen porque **miden cosas distintas**. La pantalla de Vigencia (A7) cuenta documentos cuya vigencia **nadie ha validado** —50 en Normativa UJI—; la caducidad cuenta documentos cuya **fecha de revisión prevista ya pasó**, y las del corpus están en 2027, así que cero es correcto. **Un hallazgo**: el endpoint aceptaba con **201** un `rule_type` inventado. `SelectionRepo.matches` devuelve `False` para cualquier tipo desconocido, así que quedaba guardada una regla que **no puede casar nunca**: en pantalla, una selección activa y ninguna página seleccionada, sin nada que lo explicara. `rule_type` pasa a ser `Literal` de los tres tipos que el repositorio sabe evaluar —mismo criterio que el spider desconocido del dispatcher: fallar donde se ve—. 5 tests nuevos; `tests/modules/curation` **175 passed**, `tests/api` + higiene **287 passed**, frontend **330 passed**, contrato regenerado y `tsc` limpio. |
| 2026-08-17 | VER.7 (verificación en navegador) — Curación B: análisis, hallazgos e informe | **Cursor: VER.7 ✅ → VER.8.** El análisis se lanza (**202**) y el informe de calidad se genera (**200**, con totales por tipo y severidad). La cola de revisión funciona: sembrado un hallazgo, **confirmar → 200**, la transición inválida `confirmed → new` **→ 422** con el motivo, y **resolver → 200**. **Un hallazgo, el que el plan anticipaba**: el «PDF» exportado **era un DOCX**. `to_pdf` cae a DOCX cuando no hay LibreOffice —degradación razonable—, pero la respuesta seguía diciendo `content-type: application/pdf` y `filename=...pdf`; medidos los bytes, empezaban por `PK`, la firma de un ZIP. Quien lo descargaba se llevaba un fichero que Adobe no abre y que no explica nada. Ahora el tipo y la extensión son los del fichero que de verdad se sirve, así que la degradación **se ve**. 2 tests nuevos; `tests/modules/curation` + `tests/api` **452 passed**. |
| 2026-08-17 | VER.6 (verificación en navegador) — Curación A: sitio, crawl y candidatos | **Cursor: VER.6 ✅ → VER.7.** Un hallazgo, y grande: **el rastreo nunca había funcionado**. Al lanzarlo, el sitio quedaba en `status='error'` con `'HubWebSite' object has no attribute 'url'`: `GenericSpider.crawl` lee `source.url` y el modelo se llama `root_url` desde que `HubWebSource` pasó a ser `HubWebSite`. **No lo cazó ningún test porque todos doblan el spider** —`test_site_crawler.py` usa un `_FakeSpider` y su `_FakeSite` ya declaraba `root_url`—, así que el desajuste vivía justo en la única costura que nadie cruzaba. Corregido el protocolo y sus tres usos, y actualizado el doble de `test_spider_generic.py`, que declaraba `url` y por eso pasaba en verde sobre un contrato falso. Verificado en vivo contra el sitio del corpus en `:4174`: alta del sitio desde la pantalla, rastreo **202**, sitio `active` con `last_crawled_at`, **2 páginas** guardadas y las mismas dos como candidatas para el chatbot Normativa UJI. **Observación sobre el objetivo, no sobre el código**: sólo salen dos páginas porque las fichas de cada norma las pinta el JavaScript del buscador; un rastreador estático llega al índice y al buscador, no a `html/<slug>.html`. Para probar profundidad real hace falta un sitio con enlaces servidos en HTML. 3 tests nuevos; `tests/modules/curation` **169 passed**. |
| 2026-08-17 | VER.5 (verificación en navegador) — Informes C: la cola de aprobación de scripts | **Cursor: VER.5 ✅ → VER.6.** El ciclo funciona: propuesta sembrada → `test` en el sandbox → validación del proponente → `pending_review` → **re-test del administrador con `hash_matches=True`** → aprobado, recorrido en la pantalla y confirmado en la base. Los arreglos del 2026-08-14 y del 15 siguen en pie. **Un hallazgo de uso**: la plantilla de destino era un **campo de texto libre con el marcador «UUID de la plantilla»**. Ningún administrador sabe de memoria el UUID de una plantilla, y escribir uno mal da un 404 **después** de pulsar aprobar; la lista ya la sirve la API, así que ahora es un desplegable con las plantillas **globales** —que son las únicas donde se puede incrustar un script— y el texto del marcador dice «Elige una plantilla». **Prerrequisito de entorno resuelto sin tocar el stack del usuario**: el sandbox del stack de producción no publica su puerto y sólo es alcanzable dentro de la red de Docker, así que desde un backend en el host no resolvía `script-sandbox:5000`; se levantó una **instancia aparte** de la misma imagen en el 5099 y se apuntó `SANDBOX_BASE_URL` a ella. `POST /scripts/propose` sigue en 503 a propósito, fuera del alcance del bloque. 3 tests nuevos; frontend de redacción **66 verdes**, `tsc` limpio. |
| 2026-08-17 | VER.4 (verificación en navegador) — Informes B: del documento subido al informe exportado | **Cursor: VER.4 ✅ → VER.5.** El camino largo, y el que más rompió: **ocho hallazgos, todos bloqueantes**. **(1) No existía la pantalla donde se trabaja un informe.** `WorkspaceEditor`, `DynamicUploadSlots`, `AIBlockReviewPanel`, `DataQualityPanel` y `ReportUIContractRenderer` estaban escritos desde 9R y **ninguna ruta los montaba**: `App.tsx` sólo enrutaba la vista de impresión. Nueva `WorkspacePage` en `/redaccion/workspaces/:id`, que no inventa interfaz —compone lo que ya estaba— y construye el formulario desde el `ui_contract`, no desde campos fijos. **(2) El asistente creaba el informe y no llevaba a ninguna parte**: la mutación se lanzaba y la pantalla no cambiaba, indistinguible de un botón roto. **(3) `POST /run` daba 500** con `MissingGreenlet` —leía `workspace.status` después del `commit()` de `start_run`—, así que **la tarea de fondo no llegaba a encolarse nunca** y el workspace se quedaba en `drafting`. **(4) La subida de un fichero no se persistía**: `inputs_json` se mutaba en sitio y se reasignaba el mismo objeto, que SQLAlchemy no marca como sucio, así que el endpoint devolvía 201 con la ruta y la base se quedaba vacía. **(5) El ejecutor no pasaba los ficheros al grafo** (`inputs={}`), y **(6) la extracción los buscaba en el disco**: los pipelines hacen `Path(bucket)/key`, y con `StorageService` escribiendo bajo `STORAGE_BUCKET` —o en GCS— no hay tal fichero. `FileNormalizationNode` **materializa** ahora el artefacto por `StorageService` en un temporal de la ejecución, que el runner limpia; es lo que su propio docstring anticipaba y deja la regla de portabilidad en pie sin reescribir los cinco pipelines. **(7) Las cuatro transiciones de bloque daban 500** por el mismo patrón post-commit: la revisión entera era inutilizable. **(8) `POST /resume` no reanudaba nada**, igual que `/run` antes de VER.1. **Desviación documentada, y es de diseño**: el ensamblado exigía `approved` a **todos** los bloques, y como nada transiciona un `STATIC_TEXT` a `approved`, un informe habría salido con la mitad de IA y **sin la mitad determinista**, y la vista previa era inalcanzable por construcción. Interpretación aplicada: la puerta de revisión es sobre el **texto de IA** —que es lo que `UserReviewGateNode` implementa—; un bloque determinista no lo aprueba nadie porque no lo escribió nadie. Reanudar conserva ahora lo ya aprobado en vez de regenerarlo. **Verificado en vivo de punta a punta**: plantilla → workspace → Excel sintético de ejecución presupuestaria → generación → `b6 DETERMINISTIC_DATA extracted` con la tabla real → el bloque de IA redacta **citando las cifras extraídas** (120.000, 96.000, 80 %) → aprobar → reanudar → `assembled` → vista previa **200**. 20 tests nuevos; `tests/modules/redaccion` **318 passed, 3 skipped**; frontend **327 verdes** y `tsc` limpio. |
| 2026-08-17 | VER.3 (verificación en navegador) — Informes A: plantilla y contrato SDUI | **Cursor: VER.3 ✅ → VER.4.** Recorrido en navegador con seis hallazgos, todos arreglados con TDD. **(1) La pantalla que propone la plantilla con el modelo estaba fuera del menú**: `LLMDraftPreviewPage` hace el camino entero y su ruta `/redaccion/draft` sólo se alcanzaba escribiendo la URL. Importa más de lo que parece, porque **el constructor de plantillas guarda `spec_json: {}`** —una plantilla sin secciones ni bloques, que no genera nada—, así que proponerla con el modelo es hoy la única vía para obtener una plantilla utilizable desde la interfaz. **(2), (3) y (4): el prompt describía a mano un contrato que pydantic ya conoce**, y fue un juego del topo con tres rondas de peticiones reales —faltaba `data_block_ref` en el bloque `TABLE` (500), luego el `REVIEW_GATE` sin el cual `DraftValidator` devuelve `ok=False` y **ninguna propuesta con IA era aprobable jamás**, luego la forma de `InputSlot`, luego el título de sección llegó como diccionario i18n—. Resuelto de raíz: el prompt lleva el **JSON Schema real** de las tres piezas, más un reintento con el error como respuesta (el patrón que `etl_factory` ya usaba). **(5) Una propuesta que no valida era un 500** con traza de pydantic; ahora es 422 con el motivo, que es lo que permite a quien pide el informe saber que puede reformular. **(6) El hallazgo mayor, y el que nadie había visto porque nadie leyó nunca lo guardado**: `approve-as-template` escribía en `spec_json` el **borrador** —`proposed_sections`, `proposed_blocks`, `rationale`— y una `ReportTemplateSpec` es otra forma, así que toda plantilla aprobada desde una propuesta de IA quedaba inservible: el contrato de UI daba 500 y la generación de VER.1 tampoco podía leerla. Nuevo `spec_builder.py` con la conversión, incluida la **derivación del contrato de UI** que el borrador no trae: slot de fichero → zona de arrastre, slot de dato → campo, y las políticas deducidas de lo que la plantilla contiene. De paso, `nombre_del_modelo` en `_actor.py`: salía «Modelo: desconocido» porque `ChatGoogleGenerativeAI` expone `model` y no `model_name`. Verificado en vivo contra Gemini: propuesta con 4 secciones y bloques `DETERMINISTIC_DATA` + `TABLE` + `AI_ASSISTED_TEXT` + `REVIEW_GATE`, validación `ok=true`, aprobación 200 y contrato de UI **200** con una zona de arrastre y dos campos derivados. 26 tests nuevos; `tests/modules/redaccion` + `tests/api`: **585 passed, 3 skipped**; frontend 184 verdes y `tsc` limpio. |
| 2026-08-17 | VER.2 (RED/GREEN) — Proponer una plantilla con el modelo | **Cursor: VER.2 ✅ → VER.3.** `LLMSpecService` llevaba escrito y probado desde 9R; lo que devolvía **503** era la dependencia que lo construye, un stub con «connect to model_factory». Es la puerta de entrada natural del módulo: describir el informe y que la plantilla se proponga sola. Cableado a `model_factory`, con el 503 conservado para el caso que de verdad lo merece —no hay modelo configurado— y **con el motivo dentro**: es la diferencia entre «esto no está montado» y «esto está roto». |
| 2026-08-17 | VER.1 (RED/GREEN) — La generación de informes se ejecuta de verdad | **Cursor: VER.1 ✅ → VER.2.** `POST /workspaces/{id}/run` cambiaba el estado a `drafting`, devolvía un `run_id` sintético y ahí acababa: el grafo no se ejecutaba **nunca** y nadie había escrito jamás una fila en `hub_workspace_blocks`. Nuevo `drafting_runner.py` con la raíz de composición —repositorio de versiones, almacenamiento, factoría de extracción y modelo— más la conversión workspace↔`WorkspaceState` y la persistencia de bloques, estado y manifiesto; el endpoint lo dispara con `BackgroundTasks` y su propia sesión, como la ingesta. **El estado de error es la mitad del prompt**: un workspace atascado en `drafting` para siempre se lee como «va lento» y nadie lo mira, así que cualquier fallo —incluido el anterior a la ejecución— deja `status='error'` con el motivo en el propio workspace. **Segundo hueco, sin el cual «ejecutar de verdad» era mentira**: el nodo de IA recibe `ai_prompt_template_id`, que es un **identificador** de plantilla de prompt, y en redacción no había nada que lo resolviera —`hub_prompt_templates` cuelga de un chatbot y es otra cosa—; mandarlo tal cual es pedirle al modelo que redacte con la instrucción «generic_report_v1». Nuevo `redactor_de_bloques.py` con catálogo como dato del módulo, y el identificador desconocido viaja **dentro** de la instrucción en vez de desaparecer. **Tercer hallazgo, este vivo y ajeno al módulo**: `get_model_for_tier(1)` moría con `ValueError: Provider type desconocido: google_vertexai` porque `get_llm_config_for_tier` no filtraba por `purpose`, y la configuración marcada por defecto en el nivel 1 era la de **embeddings** creada al montar Vertex en PIL. Afectaba también al analizador de HTML de la ingesta. Filtrado por `purpose='chat'` y corregido el dato de la base de desarrollo. De paso, `texto_de` sube a `core/llm_text.py`: el contenido en lista de bloques de Gemini ya había mordido en el chatbot y volvía a morder aquí. 13 tests nuevos; `tests/modules/redaccion` + `agents_hub/unit` + higiene: **808 passed, 3 skipped**. |
| 2026-08-17 | Planificado el Bloque VER — verificación de Informes y Curación antes del despliegue | **Cursor: → VER.1.** El usuario preguntó si valía la pena probar en local los módulos de Informes y Curación antes del despliegue, «con Chrome in Claude primero y con intervención humana después». Sí, y la propia planificación lo demuestra: al inventariar los **62 endpoints** de los dos módulos apareció que **Informes no genera nada**. `POST /redaccion/workspaces/{id}/run` transiciona el estado a `drafting` y devuelve un `run_id` sintético **sin ejecutar el grafo** —lo dice el propio código: «Para el MVP no dispara la ejecución del grafo en background»—, y tres puertas al LLM son stubs 503 (`llm-drafts/propose`, `scripts/propose`, `copilot/ask`). El `DraftingCoreGraph` **está construido entero**, con sus 14 nodos y sus tests: lo que falta es la raíz de composición que le inyecte repositorio, almacenamiento, factoría de extracción y modelo. Curación, en cambio, está cableada de punta a punta. **Decisión del usuario**: cablear la generación y luego probar; copiloto y propuesta de scripts por LLM quedan fuera, documentados. 9 prompts: VER.1 (ejecución real del grafo) y VER.2 (`llm-drafts`) son RED/GREEN; VER.3–VER.8 recorren los seis caminos en navegador arreglando con TDD lo que aparezca; VER.9 cierra con la matriz de lo irreducible y el `.bat` humano. El crawl de curación apunta al **sitio local del corpus en `:4174`** y no a `www.uji.es`: es HTML institucional real, no limita peticiones y no cambia entre dos ejecuciones, así que un fallo es del código. Contexto que sostiene la prioridad: los Caminos 3 y 4 del 2026-08-14 rozaron estos módulos y sacaron **cinco fallos de cinco intentos**. |
| 2026-08-17 | A8 — El gemelo agéntico de Gerencia, y los tres fallos que impedían que el modo agente funcionara | **Cursor: sin cambio.** El usuario pidió que el asistente alternativo de Gerencia combine **agente + largo contexto para la normativa propia y RAG para las normas externas**, «dado que la Ley de contratos y otras similares son muy largas para inyectarlas en el contexto». Medido sobre el corpus real, tiene toda la razón: las **22 normas externas suman 1.745.337 tokens** y la LCSP sola son **279.425**, mientras que la normativa **propia** más larga son **51.032**, que sí cabe entera. Al ir a construirlo apareció que **`MD_AGENT_SELECTOR` nunca había funcionado end-to-end**, por tres fallos encadenados que ningún test veía porque todos construían el bucle con `tools=[]`. **(1) `bind_tools` moría con `SchemaError`**: los tools declaraban sus dependencias inyectadas (`index`, `reader`, `retriever`) como parámetros con anotación `Protocol`, y pydantic no puede construir un validador para eso; ahora van con `InjectedToolArg` y `get_agent_tools()` devuelve declaraciones de LangChain, así que el modelo solo ve `submateries`, `document_id` y `query`. **(2) El turno del asistente se apilaba sin sus `tool_calls`** —un dict con solo el texto—, así que el `ToolMessage` quedaba huérfano, el modelo no veía el resultado y **repetía la misma llamada hasta agotar las diez iteraciones**, devolviendo una respuesta en blanco. **(3) Gemini devuelve `content` como lista de bloques** y el validador de citas moría con `TypeError: expected string or bytes-like object, got 'list'`. Los tres se vieron ejecutando el grafo real contra el corpus real, no razonando sobre el código. Encima de eso, **`search_knowledge` entra en la caja de herramientas del agente** —cableado a la búsqueda vectorial, y **sin filtrar por idioma**, que era un cuarto vaciado silencioso: los chunks llevan `val`/`es` y el grafo resuelve `ca`— y **`read_document` deja de volcar lo que no cabe**: por encima de `LIMITE_LECTURA_TOKENS = 60.000` devuelve la cabecera y remite a la búsqueda, con `lectura_truncada` en la evidencia. El límite no es un número redondo: deja entera toda la normativa propia y manda a buscar solo las externas. **Gemelo creado**: `Gerència — assistent agèntic (proves)`, 124 documentos y 14.198 chunks **copiados** del original en vez de reingeridos —mismos embeddings, ni coste de Vertex ni una variable más en una comparación que quiere aislar el modo—. Verificado en vivo: «requisits d'un contracte menor» → dos `search_knowledge` y respuesta citada con ancla; «import màxim de despeses protocol·làries» → `list_documents` por submateria + `search_knowledge` y la cifra exacta (30 €) con ancla. 21 tests nuevos. |
| 2026-08-16 | A7 — Pantalla de validación de vigencia en el panel | **Cursor: sin cambio.** El asistente **ya advertía** de cada documento cuya vigencia nadie ha comprobado (VIS.3), pero advertir sin una lista de *cuáles* son no cierra nada: el aviso se repite y no hay forma de ver si el corpus mejora. Nuevo endpoint `GET /hub/ingestion/{chatbot_id}/vigencia` (`VigenciaPendentOut`: `total`, `pendents`, `documents`) y pantalla `/hub/vigencia` junto a Documentos. **El criterio es el mismo que dispara el aviso** —`vigencia_validada_el IS NULL OR estat_vigencia IS DISTINCT FROM 'vigent'`, la traducción a SQL de `vigencia_no_validada`—, a propósito: con un criterio propio, el número de la pantalla y el comportamiento del asistente divergirían sin que se note. El **motivo** (`sense_validar` / `estat_no_vigent`) lo calcula el servidor, y **el estado manda sobre la falta de validación**: un documento derogado que además nadie validó pide retirarlo, no revisarlo, y en el montón de «sólo hay que mirarlo» es donde se queda sin hacer. Cifras reales medidas: **Normativa UJI 50 de 297**, **Gerència 38 de 124** (el «43» que se citaba salía del recuento del corpus en disco, no de la base). 7 tests de API + 7 de la pantalla; `tests/api` + higiene **286 verdes**; `src/admin` + i18n **146 verdes**; `tsc` limpio. Verificado en navegador: lista, contadores, filtro por motivo, enlaces al documento y las tres traducciones. **El estado bruto (`parcialment_derogat`) se pinta sin traducir a propósito**: es vocabulario del corpus, que es dato y no puede acabar como lista literal en el código. |
| 2026-08-16 | A6 — Aviso inmediato al preguntar y pie de aviso de IA en el widget | **Cursor: sin cambio.** El widget tardaba varios segundos en dar señal de vida: el estado del servidor («Buscant a la base documental») no llega hasta que arranca el grafo, así que quien preguntaba veía su pregunta y nada más. Marcador **«Preparant la consulta…»** con puntos parpadeantes desde el envío hasta el primer estado del servidor, y **pie permanente** con el aviso de que el asistente está creado con IA, el modelo entre paréntesis y que las respuestas pueden contener errores; el modelo viaja como `data-model` desde el script de incrustación y lo emite `estil_uji.py`. La verificación en navegador salía en falso porque Chrome servía el bundle cacheado: **cache-busting por `mtime` del bundle** en la generación del sitio. 311 tests de frontend verdes y `tsc` limpio; comprobado en vivo sobre el sitio regenerado. |
| 2026-08-16 | Revisión de los logs del servidor: un fallo vivo, uno ya arreglado y dos de test | **Cursor: sin cambio.** El usuario aportó logs para valorar qué convenía corregir. **(1) FALLO VIVO, arreglado**: subir el mismo `.md` dos veces **a la vez** dejaba el segundo trabajo en «falló» con `PendingRollbackError` y una traza de SQLAlchemy en el log. El bloque que lo cubre existía —savepoint + reutilizar el documento existente— pero **`session.add(doc)` estaba FUERA del `begin_nested()`**, así que el objeto entraba en la transacción exterior y el fallo del flush envenenaba la sesión; el `SELECT` inmediatamente posterior, el que busca el documento que ya existe, moría. Movido dentro. **Sólo se reproduce con dos trabajos concurrentes** —que es justo lo que el log enseña, dos `POST /upload` con 202 seguidos—: en secuencial el savepoint ya absorbía el choque, y por eso mis dos primeros intentos de reproducirlo dieron verde y hubo que llegar hasta `asyncio.gather` con dos sesiones. 5 tests nuevos. **(2) YA ARREGLADO**: el 500 de `POST /hub/redaccion/templates` con `MissingGreenlet` es del hallazgo del 2026-08-14; comprobado contra el servidor en marcha, hoy devuelve **201**. Ese trozo de log es viejo. **(3) Adyacente, arreglado de paso**: la tarea de fondo de la subida resolvía el servicio de embeddings con **la sesión de la petición**, que ya está cerrada cuando `BackgroundTasks` corre; ahora usa la suya. **(4) Dos rojos de test que el arreglo destapó, ninguno del código de producción**: `test_vector_strategy` dependía de que `CORPUS_SITE_BASE_URL` no estuviera puesta, y desde PUB.3 sí lo está en `server/.env` —o sea que el test dependía de un fichero que no está en el repositorio—; y `test_llm_configs_router` conducía un bucle de eventos a mano con `get_event_loop().run_until_complete()`, así que pasaba o fallaba según lo que se hubiera ejecutado antes en el proceso. Los dos corregidos. **`agents_hub` + `api`: 1136 passed, 0 failed.** Lo que NO era problema: la carga de pesos de BGE-M3 en el log es de cuando no había configuración de embeddings —ambas vías de subida resuelven por `resolve_embedding_service`, verificado— y los avisos de `urllib3`/OpenRouter son ruido |
| 2026-08-16 | Cierre de los dos pendientes del piloto y preparación del prototipo en GCP | **Cursor: sin cambio.** **(1) Las cuotas de SEC.4 ya tienen superficie.** Los tres techos —`user_daily_token_quota`, `chatbot_daily_token_quota`, `anon_ip_daily_token_quota`— existían en la base y los aplicaba `core/quotas.py` desde SEC.4, pero **no los exponía ni la API ni el panel**: solo se podían fijar por SQL y en la práctica quedaban en NULL, o sea un chatbot `public_anon` **sin límite por IP**, que es justo el sujeto para el que se construyó la contabilidad. Añadidos a `ChatbotRead/Create/Update`, al contrato regenerado con Orval y al formulario. **Van en la lista de campos explícitos del PATCH** porque `null` significa «volver a heredar» y sin eso un techo puesto una vez no se podría quitar — el mismo agujero que tuvo `valid_until` antes de SEC.4.1. En el formulario son **cadenas y no números**: hay tres estados —heredar, sin límite y un techo— y un `number` solo expresa dos, así que el campo vacío tenía que poder significar «heredar». **El guardarraíl de i18n de CAL.4 cazó un atajo**: había escrito los tres campos con un bucle y `t(\`hub.${campo}\`)`, y una clave construida con plantilla es invisible para él; escritos literales, vuelve a poder avisar cuando sobren. 6 tests nuevos. **(2) Gerencia revertido a `authenticated`** y su credencial de sitio revocada, como se acordó al abrirlo; `widget-gerencia.html` borrado y el aviso retirado del `.bat`. **(3) Preparado el despliegue**: `docs/DESPLIEGUE_PROTOTIPO_GCP.md` separa las dos piezas —el sitio es estático y va a un bucket; el asistente es la VM del bloque Deploy— y fija el orden, las variables y los riesgos. Sin dominio propio, por decisión del usuario: es un prototipo. Dos avisos que valen el documento: **emitir una credencial de sitio nueva para el bucket**, porque la de local acaba dentro del HTML generado; y **`CORS_ALLOWED_ORIGINS` con el origen del bucket**, porque en producción la política es cerrada y un origen que falta no da error visible, da un preflight rechazado. `.env.example` con `CORPUS_SITE_BASE_URL`. **Frontend 302 passed, `tsc` limpio; `tests/api` 29 passed en lo tocado.** **Los tres cambios del proyecto del corpus quedaron commiteados allí** (`97b16a7`), solo los míos: los otros doce ficheros modificados son trabajo del usuario y no se han tocado |
| 2026-08-16 | PUB — el sitio de publicación como anfitrión del asistente, y la cita que abre el artículo | **Cursor: sin cambio.** El sitio de publicación del corpus **ya existe entero** y nadie lo estaba usando: portada, buscador y **271 páginas de norma con sus anclas**, con la misma identidad azul `#0b5394` que el panel. Es el prototipo que el piloto necesitaba, y es además el anfitrión natural del widget. **Tres cambios en el proyecto del corpus** (`Descarregar_pdf/normativa_propia`, repositorio distinto — **quedan sin commitear ahí a propósito**, ver aviso abajo): el **logo institucional** entra por `header.top::before` en `estil_uji.py`, así que aparece en las cuatro cabeceras —portada, buscador, ficha y tablero— sin tocar ningún generador, que es exactamente lo que ese módulo dice existir para evitar; va **incrustado como data URI** porque el buscador vive en la raíz y las fichas en `html/`, y un `src` relativo sería distinto en cada sitio. Y una función `assistent()` que emite el widget, **configurada por entorno** (`ASSISTENT_API`, `ASSISTENT_CHATBOT`, `ASSISTENT_CLAU`) para que el mismo generador sirva a la prueba local y al despliegue; sin chatbot configurado no escribe nada, que es lo correcto: un sitio sin asistente es un sitio normal, no uno roto. **Un fallo de idioma cazado al verlo**: el corpus dice `val` y la interfaz habla `ca`, así que el widget caía al castellano dentro de una ficha en valenciano —la misma mezcla de idiomas que se acababa de corregir en UX.3—; resuelto con una traducción de códigos en `assistent()`. **PUB.3 en este repositorio: la cita apunta al sitio publicado.** El PDF oficial no tiene anclas y se abre por la primera página; `html/<slug>.html#art-9` abre el artículo. `url_de_cita` construye la URL cuando `CORPUS_SITE_BASE_URL` está definida y cae al PDF cuando no —vacía significa desactivado, así que no hay nada que decidir hasta que el sitio exista—. El slug viaja en `doc_metadata['relative_path']` desde la ingesta: **no se puede derivar de `canonical_url`**, que para los 234 publicados es la URL del PDF del portal. Recargar los 422 documentos para añadirlo costó **297 + 124 `UPDATE` de metadatos y CERO re-embebidos**, que es la disciplina del corpus rindiendo. **Verificado de punta a punta en navegador**: el asistente responde «sis anys», cita «art. 9», el enlace abre la página en el **artículo 9** con su marca «desplaçat pels Estatuts» y su nota de vigencia a la vista. 7 tests nuevos; `agents_hub`+`public_graphs`: **967 passed** |
| 2026-08-16 | Bloque UX — el widget se comporta como un widget, y el panel tiene identidad | **Cursor: sin cambio (bloque PIL cerrado; esto es corrección de producto sobre lo que el piloto destapó).** Seis cosas, todas pedidas por el usuario tras usar el asistente de verdad. **UX.1 comportamiento**: arranca **cerrado** con su botón azul —el `useState(true)` obligaba a que el visitante cerrara un panel que no había pedido—; trae **su propia caja** (posición, tamaño, sombra y borde superior azul) en vez de heredar la de la página, que era el defecto que hacía que el fragmento de instalación produjera un chat estirado al final del documento; la caja de texto es un **textarea que crece** en vez de un `<input>` con desplazamiento lateral, con Enter para enviar y Mayús+Enter para salto de línea; **scroll automático** al final del hilo, que antes hacía el usuario a mano en cada respuesta; y letra a 0,875 rem, fijada por la caja en vez de heredada de la página. **UX.2**: la pregunta va enmarcada en azul claro con separación entre turnos; antes pregunta y respuesta se pintaban igual y el hilo era un muro. **UX.3**: los mensajes de progreso los escribía el **servidor en castellano** (`hub_chat.py`), así que salía «Buscando en la base de conocimiento...» dentro de un widget en valenciano; ahora el servidor manda el nodo —que ya mandaba— y el widget traduce, con el texto del servidor como red de seguridad para un nodo sin traducir. **UX.4**: nueva columna `no_answer_message` (migración `b2u3x4y5z6a7`, aplicada) para decir a quién remitir cuando no hay fundamento; el público va a **Infocampus**. Columna propia y no reutilizar `unavailable_message`, que es el mensaje de chatbot cerrado: un campo con dos significados acaba enseñando el texto equivocado en uno de los dos casos. **UX.5**: el panel tenía la paleta **declarada y sin aplicar** —`AppLayout` pintaba el lateral con `bg-card`, blanco, en vez de `bg-sidebar`—; ahora lateral azul `#0b5394` con el logo, tomando la identidad del proyecto de Superset. En hexadecimal y no en oklch a propósito: estos valores se comparan a ojo contra su origen. **UX.6**: el ancla viajaba en la URL pero **no se veía** —el texto del enlace solo decía el documento y la píldora solo el título—, así que el esfuerzo de generar anclas se perdía en el último centímetro; el prompt exige ahora `[Títol, art. 9](URL#art-9)` y la píldora muestra el ancla. De paso, el prompt manda **abrir por lo que se aplica** cuando un artículo está desplazado, que corrige la observación del día anterior. **Verificado en navegador de punta a punta**: la respuesta abre con «sis anys», cita «art. 9», remite a Infocampus lo que no sabe y el hilo baja solo. **Frontend 302 passed, `tsc` limpio; backend `public_graphs`+`api`+`infra` 455 passed.** Queda pendiente lo que no se puede hacer aún: citar la **versión HTML con ancla**, que necesita una URL base publicada que el corpus todavía no emite |
| 2026-08-15 | Hallazgo — el widget no trae su propia caja, y el fragmento de instalación no lo dice | **Cursor: sin cambio (bloque PIL cerrado).** El usuario reportó que «el botón del chat no se muestra en ninguno de los dos». Diagnosticado, y **no era de las páginas de prueba**: (1) `ChatWidget` arranca con `useState(true)`, o sea **abierto**, así que el botón 💬 sólo aparece cuando alguien lo cierra con la ✕ — al cargar no hay botón, hay panel; (2) el panel abierto se renderiza con `height: 100%` **dentro** del contenedor `#govgenai-widget` y **no lleva ni posición ni tamaño propios**: hereda lo que le dé la página anfitriona. Como el fragmento de instalación que muestra el panel de administración es un `<div>` pelado, quien lo copie en la web institucional obtendrá exactamente lo que se veía aquí: los campos del chat estirados a ancho completo al final del documento. El mismo defecto lo tenía `frontend/widget.html`, el laboratorio que ya existía, así que llevaba tiempo ahí sin que nadie lo mirara con ojos de usuario. **Resuelto en las dos páginas de prueba** dándole caja al contenedor con `:has([role="dialog"])`, para que la caja exista sólo con el chat abierto y el botón flote solo cuando está cerrado. Verificado en navegador: panel flotante abajo a la derecha con su ✕, y al cerrarlo aparece el 💬. **Lo que queda abierto y es del producto, no de las pruebas**: decidir si el widget debe traer su propia caja (lo esperable de algo embebible) o si el fragmento de instalación debe incluir el CSS mínimo; y si debe arrancar cerrado, que es la convención de un widget de chat. Ninguna de las dos se toca aquí: son decisiones de producto |
| 2026-08-15 | ⚠ Bancos de prueba del widget, y un cambio temporal que HAY QUE REVERTIR | **Cursor: sin cambio (bloque PIL cerrado).** A petición del usuario, dos páginas de prueba del widget: `frontend/widget-normativa.html` y `frontend/widget-gerencia.html`, con su chatbot y su credencial ya puestos. **Las dos están en `.gitignore`**: llevan credenciales de este entorno dentro. **⚠ CAMBIO TEMPORAL PENDIENTE DE REVERTIR**: la credencial de sitio **sólo abre chatbots `public_anon`** (SEC.8.5 + SEC.2.1), así que para poder probar Gerencia desde una página se cambió su `access_mode` de `authenticated` a **`public_anon`**. Mientras siga así, cualquiera con esa página consulta el corpus de Gerencia **sin identificarse**. Decisión explícita del usuario, con el compromiso de revertir antes de desplegar: `PATCH /hub/chatbots/722aaa10-d7eb-4b7d-b623-a53603d3a777 {"access_mode":"authenticated"}` y revocar su credencial. Avisado en tres sitios: banner rojo en la propia página, sección 6 nueva del `.bat` y esta línea. **Dos cosas que salieron al montarlo**: (1) **el bundle del widget no se puede servir por Vite** — el servidor de desarrollo le inyecta su instrumentación de *fast refresh* a un fichero ya compilado y revienta con `$RefreshSig$ is not defined`; hay que servirlo desde un estático aparte (`python -m http.server 4173` desde `frontend/`), que además es el escenario real de incrustación y ejercita el CORS de `X-Widget-Key` que arregló MAN.2; (2) `dist/widget/widget.iife.js` estaba desactualizado y se regeneró con `npm run build:widget`. **Verificado en navegador**: la página del público responde de forma anónima con el aviso de vigencia desplazada renderizado; la de Gerencia, comprobada su credencial contra la API, cita la instrucción de contratos menores con su apartado. **Observación de calidad para el piloto**: en la respuesta del navegador el modelo **empezó por «cinc anys»** y puso el aviso después, mientras que por la API había empezado por «sis». Las dos son correctas y llevan el aviso, pero abrir con la cifra desplazada es peor para quien lee rápido: es ajustable desde el prompt del sistema y conviene mirarlo en las pruebas manuales |
| 2026-08-15 | PIL.7 — calibración medida, y por qué el umbral casi no discrimina | **Cursor: PIL.7 ✅ — BLOQUE PIL COMPLETO.** **Dorado del propio corpus**: `preguntes_tipus` del front matter (lo llevan los 422 documentos) da pares pregunta→documento esperado. **Desviación documentada**: el plan decía usar `PREGUNTES_GERENCIA.md` y **no sirve** — son preguntas *para* el personal de Gerencia sobre vigencia práctica, con la respuesta vacía a propósito, o sea justo lo que el corpus no puede contestar; medirlo con ellas habría dado 0 % y no habría significado nada. **La medida, con 24 preguntas y `debug_bypass` (informa del gate sin desviar ni gastar LLM)**: el público recuperaba **el documento correcto en 11 de 12** y aun así **sólo 3 pasaban la puerta**; Gerencia, 9/12 correctos y 12/12 pasaban. La puntuación toma tres valores y nada más: **0,350, ~0,694 y 0,700**. **La causa, medida y no supuesta**: la rama léxica devuelve **0 resultados** para una pregunta natural —`websearch_to_tsquery` exige que **todos** los términos aparezcan, y «Quants anys dura el mandat…» no cabe entero en ningún fragmento—, así que el híbrido es vectorial puro y la puntuación normalizada **queda topada en `vector_weight` = 0,700**. Con eso, `quality_threshold: 0.7` era **inalcanzable por construcción**, y el 0,350 es ese 0,700 con la penalización `*0.5` de `min_retrieval_results=2`, que exigía **dos documentos distintos** cuando una pregunta bien dirigida a una norma devuelve legítimamente uno. **Recalibrado el público a 0,6 y `min_retrieval_results` a 1**, con la medida delante. **Consecuencia que hay que saber: hoy la puerta de calidad no gradúa, decide «¿ha vuelto algo?»** — mover el umbral entre 0,4 y 0,7 no cambia nada mientras la rama léxica no funcione. Arreglarla es candidato a prompt propio, y de paso reabre `query_rewriting`. **Verificación final, la que cierra el bloque**: por la credencial de widget (anónimo), «Quants anys dura el mandat del Síndic de Greuges?» responde **sis anys** —no els cinc que diu literalmente el Reglament—, explica que el artículo 9 está desplazado por los Estatutos (art. 128.4, de 2025-12-18) y cita con ancla y URL oficial. Eso es **PIL.3 funcionando sobre datos reales**, y sin él la respuesta habría sido «cinc anys» con toda la seguridad del mundo. **Hallazgo nuevo, con su frontera medida**: la página de documentos del panel **bloquea el navegador ~30-60 s con 297 documentos** y va fina con 124; la API responde en 0,55 s y 136 KB, así que es renderizado, no datos. `pruebas_manuales_bloquePIL.bat` generado en ANSI sin BOM |
| 2026-08-15 | PIL.6 — el corpus real cargado en los dos asistentes con embeddings de Vertex | **Cursor: PIL.6 ✅ → PIL.7.** **Cargados los dos corpus**: `Normativa UJI` **297 documentos / 23.306 fragmentos** (1 omitido, el que declara `us_assistents: no`) y `Gerència` **124 / 14.198**. **37.504 fragmentos en total, los 37.504 con `gemini-embedding-001 / 1024 / RETRIEVAL_DOCUMENT` y ninguno sin vector**: la procedencia de PIL.1 verificada sobre datos reales, no sobre un doble. Con el lote de 250 eso son **~150 peticiones a Vertex en vez de 37.504**. Dry-run limpio antes de cada carga. **Mi estimación de fragmentos era mala y conviene decirlo: dije ~21.400 y son 37.504**, porque apliqué a Gerencia la densidad de fragmentos por documento de `normatiu` cuando sus documentos pesan el triple (presupuesto, bases de ejecución, plan antifraude). El tamaño de fragmento sí cuadra con lo medido por la curación: media 712 y mediana 779 caracteres. **Un fallo de la CLI arreglado por el camino**: `corpus.load` no cargaba `server/.env` —sólo lo hace `core.config`, que la CLI no importaba—, así que abortaba diciendo que faltaba `GOOGLE_CLOUD_PROJECT` **cuando estaba puesta en el fichero que la aplicación sí lee**: un error a la vez cierto y engañoso. Corregido con dos tests de guarda. **Verificado de punta a punta**: consulta real a Gerencia sobre justificación de gastos protocolarios → responde en valenciano, **cita el documento con su ancla** (`…#div-3`) y arrastra el aviso `[VIGENCIA NO VALIDADA]`. **Dos observaciones para el corpus**: (1) los avisos de deriva de DER.2 —4 en Gerencia y 9 en el público— son **falsos positivos**: son parejas bilingües (`GES-001`/`GES-001-val`, `FAQ-001`/`FAQ-001-val`…) que comparten URL oficial legítimamente y difieren en contenido porque están en idiomas distintos; el detector agrupa por `canonical_url` y no sabe de `canonica: false`/`versio_idiomatica_de`, que el corpus declara justo para esto. No afecta a las respuestas, pero un aviso que sale siempre deja de avisar: candidato a arreglo. (2) los 37 documentos sin `url_oficial` se citan con su ruta relativa, así que su cita no es un enlace navegable — es lo que `INGESTA.md` ya anuncia, no una sorpresa |
| 2026-08-15 | PIL.5 — los dos asistentes del piloto creados, y los de prueba retirados | **Cursor: PIL.5 ✅ → PIL.6.** **Los 6 chatbots de prueba borrados por la API** (204 los seis), que es lo que ejercita la purga de PIL.2 sobre datos reales: la base queda en **0 chatbots, 0 documentos, 0 fragmentos y 0 huérfanos**, con las **15 interacciones intactas** — exactamente el comportamiento diseñado. Creados **Normativa UJI** (`d18ffad7…`, `public_anon`, 0,7 / 0,3 / 2) y **Gerència — assistent economicoadministratiu** (`722aaa10…`, `authenticated`, 0,5 / 0,25 / 1), los dos con `RAG` + `structural` + `PUBLIC_KB_RICH` + `prefer`, reranker y reescritura apagados, sobre `gemini-2.5-flash`. Configuración de embeddings de Vertex creada y marcada por defecto (`gemini-embedding-001`, 1024). Credencial de widget emitida para el público. **Verificado en navegador**: los dos aparecen en `/hub/chatbots` y el diálogo de edición muestra los valores guardados —la asimetría 0,7/0,3/2 frente a 0,5/0,25/1 es visible—, no los defaults; consola sin errores. **Tres cosas que salieron al hacerlo**: (1) el guardarraíl de creación en modo RAG **funcionó y ahorró el problema**: devolvió 503 con el mensaje de PIL.1 diciendo que faltaba `GOOGLE_CLOUD_PROJECT`, porque el backend lee **`server/.env`** y no el `.env` de la raíz —dos ficheros con el mismo nombre y distinto alcance, que conviene unificar—; (2) **`Chatbot Demo` reaparece en cada arranque** en desarrollo (lo resiembra `_seed_chatbot`, con el gate de SEC.8.0), así que borrarlo no es duradero y no es residuo sino fixture; (3) **hallazgo real, anotado y no arreglado aquí**: las cuotas de SEC.4 (`user_daily_token_quota`, `chatbot_daily_token_quota`, `anon_ip_daily_token_quota`) **no tienen ninguna superficie** — cero referencias en routers y cero en el frontend—, así que solo se pueden fijar por SQL y en la práctica quedan NULL. Para un chatbot `public_anon` eso significa **sin límite por IP**, que es justo el sujeto para el que SEC.4 construyó la cuenta. Fijada por SQL a 50.000 para el piloto; darle superficie es candidato a prompt propio y va antes de exponer el widget. **Desviación documentada**: el plan pedía `answer_template` «el que exija cita» y solo existe `generic`, sin más consumidores, así que la exigencia de cita vive en el prompt del sistema, que es lo que llega al modelo |
| 2026-08-15 | PIL.4 — vocabulario cargado y los dos paquetes contra el validador real | **Cursor: PIL.4 ✅ → PIL.5.** Organización del piloto creada por ORM (**Universitat Jaume I**, `735a5f55-7020-4c88-a374-c2b641c5b00b`) en vez de reutilizar una de prueba, que PIL.5 va a borrar. Vocabulario cargado en ella con el orden que exige el loader —**ámbitos primero**, submaterias después, porque un padre que falte rechaza el CSV entero—: **5 ámbitos + 58 submaterias**, dry-run limpio antes de cada pasada. **Puerta de validación pasada, que es lo que este prompt aporta de verdad**: en la máquina del corpus `valida_contra_el_pipeline.py` no puede correr (allí no están `pydantic` ni este repositorio), así que nadie había confirmado que los paquetes entran. Ejecutado aquí el mismo camino que ejecuta la carga antes de escribir nada —`LocalDirectorySource` con las guardas de VIS.3 más `assert_vocabulary` contra la BD—: **`normatiu` 298/298 y `gerencia` 124/124, vocabulario OK en los dos**, 2 documentos `content_class: faq` en cada paquete. Sin `default_source_url`, igual que la orden real. **Un falso hallazgo mío, corregido antes de reportarlo**: la primera sonda dijo «0 documentos con URL oficial», y era que el contrato expone el campo como `source_url` y no como `url_oficial`; medido bien son **261 con URL y 37 que caen a su ruta**, que es exactamente lo que declara `INGESTA.md`. Las claves de `source_url` no colapsan (293 distintas sobre 298), que es la condición que VIS.3 vigila. **`docs/CARGA_VOCABULARIO.md` corregido**: prometía que los documentos «aún no emiten esos campos» y que la validación «pasa trivialmente»; ya los emiten los 422, así que la consecuencia real es la contraria —sin vocabulario cargado la ingesta aborta entera— y quien siguiera el documento se habría estrellado |
| 2026-08-15 | PIL.3 — el aviso de vigencia desplazada llega a todos los fragmentos | **Cursor: PIL.3 ✅ → PIL.4.** 45 documentos del corpus tienen artículos **vigentes cuyo contenido está desplazado** por los Estatutos de 2025; el corpus lo marca con `::: nota-vigencia` dentro del artículo, pero un artículo se parte en 3-6 fragmentos y la nota cae físicamente en uno: **83 de 86 unidades desplazadas tenían trozos sin el aviso**, y quien recuperase uno contestaba un texto que no se aplica sin ninguna señal. `hidratar_desplazamiento` en `retrieval/vigencia.py` reconstruye el aviso desde `desplacat_per` —que el front matter declara **por ancla**, así que no depende de dónde cayó el texto— y lo antepone al `excerpt` en `vector_strategy`. **Descartadas las tres alternativas, con su razón en el código**: propagar la nota al trocear metería 400-700 caracteres en cada trozo **y esos caracteres entrarían al embedding**; `parent_child` sólo ayuda si la respuesta se hace con el padre y cambiaría el troceado de todo el corpus por 45 documentos; y añadir `desplacat` a `ESTADOS_CONSOLIDACION` no muestra nada porque nadie consume `estat`. **Desviación documentada respecto al prompt**: éste pedía aplicarlo «en las rutas RAG y agent-selector», y resultó que **la segunda no lo necesita** — `MD_AGENT_SELECTOR` responde con `read_document`, que devuelve el markdown completo con su bloque `::: nota-vigencia` dentro, así que una rama allí sería código muerto. En vez de escribirla, hay un test que comprueba que la nota ya viaja en el documento entero. El formato del aviso se copió del front matter real (`es_REGLAMENTO_DE_LA_SINDICATURA_DE_AGRAVIOS...`), y `motiu` es opcional: no todas las entradas lo traen. **Un guardarraíl propio**: un test recorre `ingestion/` y falla si algún fichero de la ingesta usa la hidratación — el aviso es de lectura, y si entrase al texto embebido reescribir la nota obligaría a re-embeber los 290 fragmentos afectados, que es justo lo que esta solución evita. 11 tests nuevos. **`agents_hub` + `public_graphs` + `infra`: 1033 passed, 0 failed** |
| 2026-08-15 | PIL.2 — borrar un chatbot se lleva su corpus | **Cursor: PIL.2 ✅ → PIL.3.** `DELETE /hub/chatbots/{id}` borraba la fila de configuración y dejaba documentos y embeddings **sin dueño** en la base: `hub_documents.chatbot_id` no tiene FK —la cayó el split `HubConfigBase`/`HubOperationalBase` y la frontera edge-cloud pide que no vuelva—, así que Postgres no puede cascadear ahí. Nuevo `services/corpus_purge.py` con `purgar_corpus_del_chatbot`, que el endpoint llama **antes** del borrado y **en la misma transacción**: al revés, un fallo a mitad dejaría exactamente el corpus huérfano que esto viene a impedir. **Dos decisiones de alcance escritas en el código**: (1) los fragmentos se borran explícitamente **además** de por la cascada `fk_chunk_document_id`, porque los adjuntos temporales de una consulta (`is_temporary`) cuelgan del chatbot **sin documento** y la cascada sola los dejaría atrás — no estaba en el prompt y se vio al mirar el modelo; (2) **las interacciones NO se borran**: son registro de lo que el asistente contestó y desde REV.1 material de revisión, así que borrar el chatbot no reescribe la historia de lo que dijo. **Probado contra BD real y no con la sesión mockeada**, que es la lección de los hallazgos del 12 y el 14 de agosto: un `AsyncMock` acepta cualquier `delete` y no sabe de cascadas, así que el test habría pasado en verde con el bug dentro. 6 tests de integración nuevos —incluido el que comprueba que no quedan documentos huérfanos, que es la misma consulta del criterio de cierre de PIL.5— y 1 de router que fija que la purga se llama **primero**. `tests/api` + `tests/infra`: **345 passed, 0 failed** |
| 2026-08-15 | PIL.1 — adaptador de Vertex, lote de 250 y el propósito del embedding en la procedencia | **Cursor: PIL.1 ✅ → PIL.2.** `VertexEmbeddingService` sobre `langchain-google-vertexai` 3.2.4 (dependencia nueva), elegido por `provider_type='google_vertexai'` en el resolver de MOD.2 y sembrado como `HubProvider` "vertex". **Autentica por ADC, sin clave de API**: es la diferencia que motivó el adaptador —una clave repartida no es lo que una administración pone en producción—. **Tres cosas salieron de medir en vez de leer**: el tope de lote es 250 (los ~21.400 fragmentos del piloto pasan de 21.400 llamadas a ~86); la API **no normaliza** a 1024 dimensiones —norma medida 0,6225—, lo que convierte la normalización del adaptador en requisito comprobado y no en precaución; y `europe-southwest1` sí sirve el modelo (el «no disponible» de la primera pasada era el `Expect: 100-continue` de PowerShell dando 417). **Los dos adaptadores por API comparten `_AdaptadorPorAPI`**: normalizar, trocear y elegir el propósito se escriben una vez. **El propósito del embedding entra en la procedencia del vector**, que es el alcance que se añadió al planificar: `hub_document_chunks.embedding_task_type` (migración `a1p2i3l4m5n6`, **aplicada**), la guarda `assert_embedding_space_matches` compara ahora la **tripleta** (modelo, dimensión, tipo de tarea) y el índice `ix_hub_document_chunks_embedding_space` la cubre entera para que seguir siendo estricta no la haga cara. `NULL` es legítimo y significa «este espacio no distingue propósito» —lo que declara BGE-M3—, así que se compara como un valor más: un corpus anterior a PIL.1 queda señalado como espacio distinto, que es lo correcto. **La decisión de indexado vive en un solo sitio** (`embed_para_indexar`, que comparten el watcher y el re-embebido): dos copias es como se acaba con medio corpus embebido de una forma y medio de otra. En `reembed`, el predicado usa `IS DISTINCT FROM` y no `!=` porque con NULL un `!=` da NULL —falso en un WHERE— y habría dejado fuera del re-embebido justo a los chunks que hay que rehacer. **19 tests nuevos + 116 verdes en lo que toca el prompt** (`test_reembed`, `test_embedding_provenance`, `tests/infra`); una aserción de `test_reembed` se actualizó al contrato nuevo (pares → tripletas), no era un fallo. Suite amplia de `agents_hub`+`infra`+`public_graphs`: 794 passed con los 2 únicos rojos en `test_database_connection`, ambos por la BD apagada en ese momento y ya verdes después. `.env.example` con GOOGLE_CLOUD_PROJECT/LOCATION y la nota de que Vertex **no** lleva clave; `docs/DECISION_MODELOS_EMBEDDING_RERANKER.md` ampliado con lo medido |
| 2026-08-15 | Vertex verificado contra la API real: región, lote y dos decisiones que fijan PIL.1 | **Cursor: sin cambio (PIL.1).** Prerrequisito externo del bloque PIL **resuelto**: proyecto `uji-teclab` (618806480921) con facturación activa, ADC escrito con `uji-teclab` como proyecto de cuota. **`aiplatform.googleapis.com` ya estaba habilitada**, así que no hubo nada que habilitar. Cuatro medidas contra la API real, no de memoria: (1) `gemini-embedding-001` sirve a 1024 dimensiones en `europe-southwest1`, `europe-west1`, `europe-west4` y `europe-west9`; (2) **acepta lotes de 250 instancias**, probado 1/2/16/64/250 — con `embed_batch` los ~21.400 fragmentos del piloto pasan de 21.400 llamadas a ~86; (3) **la API NO normaliza a 1024 dimensiones: norma L2 medida 0,6225**, lo que convierte la normalización del adaptador de precaución en requisito, y su ausencia no daría error sino un umbral de calidad comparando números incomparables —el mismo fallo silencioso que el quality gate del 2026-08-12—; (4) `task_type` aceptado en las dos variantes. **Un falso negativo que casi cuesta una decisión**: la primera pasada dio `europe-southwest1` como no disponible, y era el `Expect: 100-continue` de PowerShell 5.1 —el endpoint contesta **HTTP 417 con una página anti-bot de Google** que se lee como «la región no existe»—; con `Expect100Continue = $false` Madrid responde igual que las demás. Anotado en el prompt para quien vuelva a probar a mano. **Dos decisiones del usuario que fijan PIL.1**: región **`europe-southwest1` (Madrid)**, para que el texto normativo no salga de España —la región del embedding no ata a la del chat ni a la del Ranking API de RAG.6b—; y **`task_type` asimétrico** (`RETRIEVAL_DOCUMENT` al ingerir, `RETRIEVAL_QUERY` al preguntar), que añade un alcance nuevo al prompt: es lo único que toca el protocolo compartido de `EmbeddingService`, `LocalEmbeddingService` lo acepta e ignora, **el propósito viaja con la procedencia del vector** —si no, re-ingerir con otro tipo corrompe el índice en silencio porque modelo y dimensiones seguirían coincidiendo— y se adopta en los dos lados o en ninguno. `discoveryengine.googleapis.com` sigue deshabilitada a propósito: es de RAG.6b, que va detrás del Deploy |
| 2026-08-15 | Bloque PIL planificado — los dos asistentes del piloto, Vertex y el corpus real | **Cursor: → PIL.1.** Análisis del estado real del corpus y del código antes de crear los dos chatbots del piloto. `docs/PLAN_CHATBOTS_E_INGESTA_LOCAL.md` **reescrito**: la versión del 2026-08-11 daba por pendiente lo que ya está hecho (el corpus ya viene desdoblado en `generat/ingesta/normatiu` 298 `.md` y `gerencia` 124, la FAQ ya convertida con `content_class: faq`, la clasificación ya emitida) y por resuelto lo único que no lo está. **Cuatro hallazgos verificados contra el código, no contra la documentación**: (1) los 422 ficheros llevan `ambit_principal` y `submateries` en **campos reales**, así que `assert_vocabulary` valida de verdad y **aborta la ingesta entera** sin vocabulario cargado — `docs/CARGA_VOCABULARIO.md:81` promete lo contrario y hay que corregirlo; (2) **no existe adaptador de Vertex**: `GoogleEmbeddingService` construye `GoogleGenerativeAIEmbeddings` con `GOOGLE_API_KEY` (API de AI Studio) y `embedding_resolver` solo conoce `google_genai` y `local`, con `langchain-google-vertexai` ausente de `pyproject.toml` — configurar Vertex es desarrollo, no configuración; (3) **ningún adaptador por API expone `embed_batch`**, y `watcher.py:270` cae por `getattr` a una llamada por fragmento: ~15.100 + ~6.300 llamadas secuenciales para este corpus; (4) **`hub_documents.chatbot_id` no tiene FK** (se cayó al separar `HubConfigBase` de `HubOperationalBase`), así que `DELETE /hub/chatbots/{id}` deja documentos y embeddings huérfanos — los fragmentos sí cascadean desde el documento. Más un defecto medido por la curación que se resuelve de nuestro lado: de 86 unidades desplazadas por los Estatutos de 2025, **83 tienen fragmentos sin la `::: nota-vigencia`**, y quien recupere uno contesta como vigente un texto que no se aplica. **Cuatro decisiones del usuario**: Vertex con adaptador propio, Gerencia `authenticated` en local y `restricted`+SAML al desplegar, el borrado se arregla en código, y el aviso de vigencia entra antes de las pruebas —si no, un fallo durante el piloto no se distingue de uno nuevo—. Bloque PIL escrito en `Plan_TDD_Fase1.md` con 7 prompts: PIL.1 Vertex+lote (Opus), PIL.2 borrado con corpus (Sonnet), PIL.3 hidratación del aviso (Opus), PIL.4 vocabulario+validación (Sonnet), PIL.5 alta de los dos asistentes (Sonnet), PIL.6 ingesta medida (Sonnet), PIL.7 pruebas y ajuste (Opus). **Estrategia de recuperación razonada**: `RAG`+`structural` en los dos para que las dos pruebas sean comparables, con umbrales asimétricos (público 0,7/0,3; Gerencia 0,5/0,25) porque ante el ciudadano callar cuesta menos que citar mal; `MD_AGENT_SELECTOR` queda como segunda configuración **solo de Gerencia** —124 documentos, índice de submaterias en ~2.300 tokens, `resum_router` y `preguntes_tipus` escritos justo para eso— y se decide con el dorado delante, que es barato porque **cambiar de modo es una columna, no una reingesta**. Sin código todavía |
| 2026-08-14 | Camino 4 cerrado: el paso (d) depende del Prompt 9.16, no construido, y no bloquea nada de lo verificado hoy | **Cursor: sin cambio.** Cierre del Camino 4 tras verificar (a)-(c) contra `docker-compose.prod.yml` real. El paso (d) —arrancar `client_app` y comprobar que recibe la orden de ejecución por WebSocket— **no se puede recorrer porque el canal no existe**, y es importante no confundirlo con el Edge Node: son dos cosas distintas. `server/app/api/v1/edge_sync.py` (`GET /config`, `POST /telemetry`) es el stub 501 del **Edge Node** (separación de datos cloud/cliente por gobernanza EU, `docs/AUDITORIA_PRE_DEPLOY.md`), un tema de despliegue regulatorio sin relación con `client_app`. El canal que de verdad falta es **`client_app` ↔ cloud**, y **ya está diseñado, no es un hueco improvisado**: es el **Prompt 9.16 "Agente de ejecución local: scaffolding"** dentro del **Bloque 9D / Subfase 2.A (Thin Client)** de la Fase 2 (`Plan_TDD_Fase2.md`), con protocolo especificado (`ws://.../agent/ws`, token Bearer, mensajes `{job_id, type, payload}` / `{job_id, status, output, error}`, dispatch a `handlers/script.py|rpa.py|watcher.py`) más un prompt de emparejamiento seguro después. **Aparcado a propósito, no olvidado**: Fase 2 está "no iniciada" y la replanificación del 2026-07-11 ya decidió no recomendar la migración en bloque de `client_app` — `PLAN_DESARROLLO.md` lo llama "legacy, pendiente de migración". **Tampoco hay sondeo (polling) de reserva funcionando**: `SyncManager` sabe tirar del manifiesto de automatizaciones aprobadas, pero no está conectado a ningún planificador (`scheduler_service.py`) — solo lo ejercitan los tests. Hoy no hay ninguna vía, push ni pull, para que una automatización aprobada llegue sola al escritorio de un usuario. **Confirmado que esto no afecta a lo verificado hoy**: `admin_script_pipeline.py` solo depende de `SandboxClient`/`script-sandbox` (Bloque SBX, ✅ completo) — el ciclo propuesta→sandbox→aprobación es autocontenido en el servidor y no toca `client_app` en ningún punto. **Decisión del usuario**: documentar y dejar aparcado, sin adelantar el Prompt 9.16 en el orden de ejecución. Si se decide construirlo en el futuro, el punto de partida es ese prompt, ya especificado en `Plan_TDD_Fase2.md` |
| 2026-08-14 | Camino 4 de plataforma sobre `docker-compose.prod.yml` real: tres hallazgos más (9, 10, 11) | **Cursor: sin cambio.** A petición expresa del usuario: parar el entorno de desarrollo y levantar el stack "one-click" (`docker-compose.prod.yml`) para recorrer Camino 4 (script → sandbox → aprobación → agente local) contra infraestructura real, no contra mocks. **Paso previo, no una migración de código**: `docker compose` v5.1.2 rechazaba el fichero por `pids_limit` y `deploy.resources.limits` en conflicto en `script-sandbox`; se declaró `pids: 128` también en `deploy.resources.limits` para que ambos coincidan, sin tocar el límite de seguridad de SEC.8.3. **Aviso de entorno, no de código**: el volumen `govgenai-prod_postgres_data_prod` no era nuevo — venía de una prueba de FASE11 del 2026-07-15 con un SuperAdmin cuya contraseña nadie recordaba; `bootstrap.py` no resetea la contraseña de una cuenta que ya existe. Se borró el volumen (con permiso explícito del usuario) y se recreó limpio. **El paso (a) del camino no se puede recorrer**: `POST /redaccion/scripts/propose` depende de `get_script_proposal_service()`, un stub que siempre da 503 ("Conectar a model_factory cuando se enchufe el motor LLM real", ya anotado en el propio código) — no es un bug, está documentado como pendiente. Se sembró una `HubScriptProposal` directamente en BD para poder ejercitar (b) y (c) de verdad. **Noveno hallazgo, sistémico**: `POST .../test` daba 500 — mismo patrón que el hallazgo 7 del mismo día (`create_template`), pero repetido en **todos** los endpoints de `scripts_router.py` que hacen `commit()` y luego leen `proposal.id`/`template.id`/`new_version.id` del objeto ORM ya expirado (`test`, `validate-test-result`, `save-to-private-template`, `submit-for-review`, `admin-retest`, `approve`, `reject`, y el `propose` aún sin activar). Nadie lo había cazado porque los tests existentes (`test_script_proposal_workflow.py`) mockean la sesión con `AsyncMock`, que no reproduce `expire_on_commit`. Arreglados los ocho capturando cada valor antes del commit, o usando el parámetro de ruta cuando ya es el id. **Décimo hallazgo, más grave — bloqueaba la aprobación de cualquier script para siempre**: `admin-retest` compara el hash de su propia ejecución contra el de `test` para confirmar reproducibilidad, pero ambos se calculaban sobre el `ExtractionResult` completo, **incluida `provenance.extracted_at`** — la hora real de cada ejecución, distinta siempre aunque el código y los datos sean idénticos. `hash_matches` era `False` en el 100% de las ejecuciones reales, y `/approve` exige `hash_matches=True` con un retest reciente: **ningún script podía aprobarse nunca** por esta vía. Arreglado con `_hash_extraction_result()`, que excluye `provenance` del hash — el contenido de la extracción es lo que hay que verificar, no cuándo se ejecutó. **Undécimo hallazgo, en el frontend**: el botón «Aprobar» del panel real (`AdminScriptReviewQueuePage.tsx`) mandaba siempre `target_global_template_id: ''` — cadena vacía cableada, sin ningún campo en la interfaz para elegirla. Confirmado en vivo: 422 antes del fix. Añadido un campo de texto para el UUID de la plantilla destino (i18n en es/ca/en), con «Aprobar» deshabilitado hasta rellenarlo. **Verificado end-to-end contra el stack de producción real**: propuesta sembrada → `test` ejecuta de verdad contra `script-sandbox` (confirmado en sus logs: `POST /execute-extraction HTTP/1.1 200 OK`) → validar → enviar a revisión → panel real (`/redaccion/scripts/review`) → «Volver a probar» con hash verificado (✓) → «Aprobar» con la plantilla destino → **200, primera vez que este flujo se completa contra Postgres e infraestructura reales**. 272 tests de `tests/modules/redaccion` + higiene (270→272 con los dos tests nuevos de BD real), sin regresiones; 9 vitest de `ScriptWizard.test.tsx` sin regresiones. **El paso (d) —arrancar `client_app` en local y comprobar que ejecuta la orden por WebSocket— sigue pendiente y es del usuario**, contra este mismo backend en `http://localhost:8000`. **Estado del entorno al cerrar**: `docker-compose.prod.yml` completo arriba (`postgres`, `minio`, `script-sandbox`, `app`, `frontend`; `ollama` y `langfuse` no se levantaron, no hacían falta) **más** el Postgres de desarrollo (`docker-compose.yml`, servicio `postgres`) arrancado en paralelo sin conflicto de puertos (el de prod no publica 5432 al host) para poder correr la suite de tests. El backend/frontend de desarrollo (`uvicorn`/`vite` locales) siguen parados desde el arranque del stack de producción. Sigue sin resolverse el "uvicorn zombi" de `0.0.0.0:8000` con PID no localizable (mismo síntoma que el 2026-08-02 y el de hoy más temprano) — no bloqueó nada porque Docker publica el puerto igualmente, pero conviene tenerlo presente si reaparecen respuestas inconsistentes en el puerto 8000 nativo (fuera de Docker) |
| 2026-08-14 | DER.2 verificado en UI + Camino 3 de plataforma: dos hallazgos más (500 al crear plantilla, 403 al leer el propio workspace) | **Cursor: sin cambio.** Continuación de la verificación manual de `docs/PRUEBAS_PENDIENTES.md`, retomada a petición del usuario. **DER.2 verificado en la interfaz** (antes solo probado por SQL directo): dos chatbots con el mismo `canonical_url` → borrar desde uno avisa "Aquesta norma també és a 1 chatbot més de l'organització" con checkbox de cascada desmarcado por defecto → sin marcarlo, borra solo esa copia y la hermana sobrevive; sin copias, el aviso no aparece. Sin hallazgos. **Séptimo hallazgo**: `POST /hub/redaccion/templates` daba 500 siempre — `create_template` construye `TemplateOut(current_version_id=version.id, ...)` **después** de `await session.commit()`, que expira los atributos de todos los objetos de la sesión (`expire_on_commit=True` por defecto); `session.refresh(template)` solo refresca `template`, así que leer `version.id` dispara una recarga perezosa síncrona sobre una `AsyncSession` de asyncpg y revienta con `MissingGreenlet` en vez de con un 500 con traza. **La plantilla sí quedaba grabada** (el commit va antes del crash) — mismo patrón traicionero que los hallazgos anteriores; el panel `/redaccion/builder` acumuló varias filas de prueba con nombres tipo "repro" mientras se acotaba la causa. Root-caused reproduciendo primero con `TestClient` (dio un `MissingGreenlet` distinto por las mismas prisas del arnés de pruebas) y luego limpio con `asyncio.run()` directo sobre `create_template`, que confirmó la traza real. Arreglado capturando `version_id = version.id` **antes** del commit y usándolo en `update_status` y en la respuesta; los otros tres `commit()` del fichero ya evitaban el patrón (uno refresca antes de leer, otro genera el UUID en Python de antemano). **Octavo hallazgo, más grave — bloqueaba el Camino 3 entero para cualquier SuperAdmin o Admin**: los seis endpoints de `workspaces_router.py` que dependen de `_get_workspace` (transición de bloques, subida de inputs, ejecución, preview...) comparan `str(workspace.owner_id) != user.user_id` literal; `owner_id` se escribe con `user_to_uuid(user.user_id)` (uuid5 determinista para cuentas sin UUID nativo, `_actor.py`), así que **el propio dueño del workspace recibía 403 siempre** que su `user_id` no fuera ya un UUID — exactamente el caso de SuperAdmin (`"1"`) y Admin (`"admin_dev"`) de desarrollo. `es_propietario()` ya existe en `_actor.py` para este caso exacto y ya la usa `hub_redaccion_router.py` para la revisión de bloques; `workspaces_router.py` nunca la adoptó. Arreglado sustituyendo la comparación literal por `es_propietario(user.user_id, workspace.owner_id)`. **Ambos arreglados con TDD contra BD real** (`db_url`/`AsyncSession` de SQLModel, sin mockear la sesión — los tests existentes de este router sí la mockean con `AsyncMock`, que no reproduce `expire_on_commit` ni compara UUIDs reales, y por eso ninguno de los dos hallazgos había saltado antes). **Verificado en vivo tras ambos fixes**: crear plantilla → 201 con `current_version_id` real → "Crear informe" en `/redaccion/wizard` → workspace 201 → `/redaccion/workspaces/{id}/preview` renderiza (Índice, Anexo de auditoría, botón «Imprimir / Exportar PDF») en vez de la pantalla en blanco «Error» sin ningún mensaje al usuario — mismo patrón de error silencioso que el resto de hallazgos de esta campaña. **270 tests de `tests/modules/redaccion` + higiene, 0 rojos** (268→270 con los dos tests nuevos). **Camino 3 queda verificado hasta donde da una plantilla vacía** (sin secciones/bloques): falta ejercitar un borrador real vía `/redaccion/llm-drafts/approve-as-workspace` con bloques `AI_ASSISTED_TEXT`, la anonimización con datos sintéticos y abrir el export en Word/Adobe real — fuera de alcance de hoy. **Camino 4 (script → sandbox → aprobación → agente local) queda sin empezar**: exige `docker-compose.prod.yml`, el stack "one-click" completo (build de imagen, MinIO...), que **choca con la sesión de desarrollo actual** y que `docs/PRUEBAS_PENDIENTES.md` ya advertía de no lanzar a medio probar los demás caminos — mejor en una sesión dedicada. Detectado y corregido de paso un problema de entorno (no de código): un "uvicorn zombi" en `0.0.0.0:8000` con PID no resoluble por `Get-Process`/`tasklist` (mismo síntoma que el del 2026-08-02) coexistía con el backend real en `127.0.0.1:8000`; sirvió peticiones con el código viejo durante parte de la depuración hasta reiniciar explícitamente en `127.0.0.1`. Quedan en el corpus del panel varias plantillas de prueba con nombres "repro"/"verificacion final" (siete en total): no hay endpoint de borrado de plantillas todavía, mismo criterio que los documentos de prueba de sesiones anteriores |
| 2026-08-12 | widget.html arreglado + quinto y sexto hallazgo (CORS del widget y front-matter sin separar en la subida) | **Cursor: sin cambio.** Cierre de la verificación manual de hoy, a petición expresa del usuario tras el resumen de Caminos 1 y 2: arreglar `widget.html` y las tres observaciones menores. **Quinto hallazgo, el más grave de los seis de hoy**: al servir el widget compilado desde un origen realmente distinto del backend (no `localhost:5173`, que lo esconde por ir todo por el proxy de Vite), `X-Widget-Key` no estaba en la lista de cabeceras CORS permitidas — el preflight daba 400 "Disallowed CORS headers" antes de que la petición real saliera siquiera. **El widget público (SEC.8.5) no podía hablar con la API desde ninguna página que lo incrustara de verdad, que es su único caso de uso.** Arreglado añadiendo `X-Widget-Key` a `CABECERAS` en `core/cors.py`, con test que fija el comportamiento. **`widget.html` reescrito**: tenía el `chatbot_id` de una fixture ya borrada y un `data-token` muerto desde SEC.8.5 (la cabecera real es `data-widget-key`); ahora son placeholders explícitos con instrucciones, sin ningún secreto. Verificado sirviendo el bundle compilado (`npm run build:widget`) desde un servidor estático en un puerto distinto: antes del fix, `--color-primary` nunca llegaba al DOM; después, `#c026d3` confirmado. **Sexto hallazgo**: la subida del panel (`upload_document` → `IngestionWatcher.run_job`) leía el `.md` del storage y lo pasaba **entero** —front-matter YAML incluido— a `process_source`. Dos consecuencias: (1) el bloque YAML se guardaba y se troceaba/embebía como si fuera contenido real, violando CLAUDE.md §5 (nada de metadatos en el texto que se embebe) — el CLI de curación (`corpus/source.py`) ya separaba esto con `parse_frontmatter`, la subida del panel era la única vía que no lo hacía; (2) el `language:` del front-matter nunca se leía: sin selección explícita en el desplegable (que es "automático" por defecto), el idioma salía de `detect_language()` sobre el texto crudo. Verificado con un `.md` real: `language: es` declarado se guardaba como `'ca'` (detectado sobre encabezados en catalán del esqueleto de ejemplo del contrato mezclados con prosa en castellano — error mío al redactar el fichero de prueba, pero el síntoma destapó que el front-matter nunca se leía). Arreglado separando front-matter y cuerpo en `run_job` antes de llamar a `process_source`, con cascada selección-UI → front-matter → auto-detección; `parse_frontmatter` es no-op sin front-matter, así que no afecta a las demás vías (crawler) que comparten `run_job`. Re-subido el mismo fichero tras el fix: `language='es'` y `markdown_content` ya no lleva el bloque YAML. **De paso, arreglada la tercera observación menor**: la columna "Font" de `/hub/documents` etiquetaba cualquier subida como "PDF" (relicto de antes de EXT.1); ahora dice "Markdown" en los tres idiomas. **Un cuarto punto que el usuario señaló y queda sin hacer, fuera de alcance de hoy**: no hay manera de corregir el idioma de un documento ya subido si la detección se equivoca — no existe `PATCH`/`PUT` en `hub_ingestion_router.py` para editar un documento, así que sería una función nueva (endpoint + UI), no una de las tres observaciones originales. **1264 tests backend + 290 vitest**, sin regresiones. **Total de la sesión: seis hallazgos reales, los seis arreglados con TDD y verificados en vivo contra el servidor real**: (1) quality gate con escala RRF sin normalizar, (2) `run_id` UUID servido donde el contrato promete `str`, (3) `user.user_id` con `user=None` en el widget anónimo, (4) método de Langfuse renombrado en la v4 del SDK, (5) `X-Widget-Key` fuera de la política CORS, (6) front-matter sin separar en la subida del panel |
| 2026-08-12 | Caminos 1 y 2 de plataforma verificados + tercer y cuarto hallazgo (500 en widget anónimo y en feedback) | **Cursor: sin cambio.** Continuación de la verificación manual de `docs/PRUEBAS_PENDIENTES.md`. **Camino 1** (organización → dos chatbots → tema → widget), de principio a fin vía UI + API: `Organizacion Camino 1` con 0 chatbots al crearla, `Chatbot Camino1 A/B` bajo ella, tema creado (201) y aplicado (200) con `primary: #c026d3`, credencial de widget emitida, `GET .../themes/for-chatbot/{id}` con la credencial devuelve el color correcto. **Tercer hallazgo, root-caused y arreglado con TDD**: conversar con la credencial de widget (sin sesión) daba 500 siempre — `chat_stream` construye el handler de Langfuse con `user.user_id`, y `user` es `None` en el widget público (SEC.8.5). `HubInteraction.user_id` ya usaba `actor.subject_id` para el mismo caso unas líneas más abajo; el handler de Langfuse ahora usa lo mismo. **Cero tests en toda la suite enviaban `X-Widget-Key` contra el endpoint real**, así que SEC.8.5 nunca se ejercitó end-to-end hasta ahora. **Camino 2** (corpus → consulta → cita → hueco → feedback): subido un `.md` de prueba conforme al contrato (front-matter + ancla `{#art-1}`) — la subida por `file_input` normal no disparaba el `onChange` de `react-dropzone` y hubo que despachar el evento `change` a mano, un detalle de automatización y no un defecto de la app —; conversación real cita el artículo con su ancla (`...md#art-1`) y el aviso de vigencia; feedback bajo (score 1) visible en Informes con su comentario. **Cuarto hallazgo, root-caused y arreglado con TDD**: puntuar una respuesta (`POST /hub/feedback/{id}`) daba 500 siempre que hay `run_id` — `FeedbackService.submit_feedback` llama a `client.score(...)`, API de una versión antigua de Langfuse; el SDK instalado (4.5.0) renombró el método a `create_score` con los mismos argumentos. La puntuación **sí quedaba grabada** (el `UPDATE`+commit va antes de tocar Langfuse): quien puntuaba veía un error por una acción ya guardada — el mismo patrón traicionero que los hallazgos anteriores. El test existente mockeaba el cliente con `MagicMock()` a pelo, que inventa cualquier atributo pedido y no podía detectar nunca un método renombrado; ahora usa `MagicMock(spec=Langfuse)`, que solo permite atributos reales del SDK instalado — **este patrón (`spec=<clase real>` en vez de `MagicMock()` a pelo) es la lección transversal de los cuatro hallazgos de hoy** y merece aplicarse a otros mocks de clientes externos (revisión pendiente, no hecha hoy). **971→1261 tests backend** tras los cuatro arreglos combinados, sin regresiones. **Dos observaciones menores, no arregladas hoy**: (1) la columna "Font" de `/hub/documents` etiqueta cualquier subida como "PDF" (`kind_upload: "PDF"` en los tres idiomas), relicto de antes de EXT.1 igual que el `max_size_10mb` ya corregido; (2) el selector de idioma de la subida no respeta el `language:` del front-matter del `.md` (mi `es` declarado quedó guardado como `ca`, el idioma de la interfaz) — no bloqueó la cita porque el filtro de idioma de la recuperación no fue estricto en este caso, pero es una desviación real del contrato; (3) el detector de huecos de corpus exige `fallback_reason` no nulo, y una negativa del modelo que cumple bien el formato de citas queda con `fallback_reason=None` aunque diga "no tengo información" — de mis 4 preguntas repetidas sobre "días de vacaciones" solo 2 contaron, así que no se disparó ningún hueco; no es un fallo, es un matiz de la clasificación que vale la pena conocer. **Pendiente de decisión del usuario**: además de `Chatbot MAN Uno`, ahora `Chatbot Camino1 A` también tiene `min_retrieval_results=1` (bajado para poder generar una respuesta real con su corpus de un solo documento); la verificación visual de `widget.html` (con el tema pintado de verdad en un navegador) sigue sin hacerse porque el fichero tiene el `chatbot_id`/`data-token` muertos de antes de SEC.8.5 — el mecanismo de backend completo ya está verificado sin él |
| 2026-08-12 | Bloque REV.1 verificado en vivo + segundo hallazgo (500 en toda revisión real) + SEC 5/5 + RAG interfaz | **Cursor: sin cambio.** Continuación de la verificación manual de `docs/PRUEBAS_PENDIENTES.md` tras el fix del quality gate. **Segundo hallazgo, root-caused y arreglado con TDD**: `PATCH /hub/feedback/interactions/{id}/review` devolvía 500 en **toda** conversación real (nunca en los tests, que dejan `run_id=None`). Causa: `HubInteraction.run_id` es UUID en BD, `InteractionReviewOut.run_id` es `str`, y el endpoint devolvía la fila ORM tal cual — FastAPI no convierte el tipo solo y la serialización de la respuesta revienta con `ResponseValidationError`. **El veredicto SÍ quedaba grabado antes de reventar** (confirmado contra Postgres): quien revisaba veía un error por una anotación que ya se había guardado, el fallo más traicionero de los dos. Arreglado construyendo la respuesta a mano con `str(run_id) si no es None`, igual que ya hacía el GET de la misma cola; test nuevo que fija un `run_id` real para que el hueco no vuelva a colarse. **REV.1 verificado end-to-end con datos reales** tras los dos arreglos: conversación real con cita → cola marca pendientes → 422 sin nota en veredicto malo → 200 con nota → filtro "Revisades" muestra veredicto + `Revisada per fabra@uji.es` + hora al desplegar la fila → columnas del CSV confirmadas por lectura de código (client-side, sin necesidad de descargar el fichero). **SEC 5/5 verificado en la interfaz**: fecha de ayer → chatbot pasa a "Caducat" (rojo) → chat da 403 con el mensaje propio → vaciar la fecha lo devuelve a "Disponible" (confirma FIX.3) y el chat vuelve a 200. **RAG comprobación 3 verificada**: escenario nuevo con "Captura el context" → cita correcta con aviso de vigencia → contexto capturado muestra `quality_gate.passed: true` y la config resuelta → "Jutjat per: fabra@uji.es" al marcar Correcta; consola sin errores propios de la app en toda la sesión (99 excepciones vistas, las 99 el mismo ruido de una extensión de Chrome); la interfaz ha estado en catalán/valenciano (`ca`) desde el principio de la sesión sin ninguna clave sin traducir. **Comprobación 4 (huecos de corpus)**: sin huecos detectados para Chatbot MAN Uno — con las preguntas de prueba repetidas pero con fraseo distinto entre sí, es compatible con el criterio documentado ("no hay al menos 3 preguntas parecidas"), no se investigó más a fondo. **Camino 3 de plataforma, dado por bloqueado en el traspaso de sesión, ya NO lo está**: `GET /hub/redaccion/templates` responde 200 (antes 500 para `user_id` no-UUID); no se siguió explorando el resto del camino (plantilla → borrador → anonimización → export) por alcance. **Cambio de datos pendiente de decisión del usuario**: `Chatbot MAN Uno.min_retrieval_results` bajado de 2 a 1 (vía SQL directo) para poder generar una respuesta real con un corpus de un solo documento; reversible, no revertido todavía. 1019 tests backend (971 + 48 del segundo fix) sin regresiones |
| 2026-08-12 | Hallazgo — el quality gate del chat nunca dejaba pasar una respuesta real | **Cursor: sin cambio.** Verificando a mano el bloque REV (pendiente en `docs/PRUEBAS_PENDIENTES.md`): generando una conversación real contra `Chatbot MAN Uno` (documento real ingerido, Gemini 2.5 real), **las 4 preguntas cayeron al fallback** de "No tengo información suficiente", incluso preguntando casi literalmente una frase del propio documento. `debug_bypass` mostró que el contexto correcto SÍ llegaba al prompt; el bloqueo estaba en `quality_gate` (`core_graph.py:187`), que compara `quality_score` contra `quality_threshold` (0.6 por defecto, pensado para escala [0,1]). **La causa**: sin reranker (`reranker_enabled=false`, el valor por defecto), ese score sale de `HybridRetriever.hybrid_search` en escala RRF pura (k=60 → techo teórico 1/61 ≈ 0,0164) — el propio código ya avisaba de esto en `vector_strategy.py` (`_aplicar_reranker`: *"si conviviesen la escala del RRF (~1/60) y la del reranker ([0,1]), esos dos controles decidirían sobre números incomparables"*), pero solo lo resolvía para cuando el reranker está activo. **Con la configuración por defecto, ningún chatbot con retrieval vectorial podía pasar nunca el quality gate**, sin importar lo buena que fuera la recuperación real (medido: similitud coseno 0,71 en SQL directo, score de fusión 0,0057-0,0115 en el gate). El propio historial de RAG.2 (2026-07-30) ya admitía que "un chat en vivo... está pendiente" — nadie lo había ejercitado de punta a punta con LLM real + corpus real hasta ahora. **Arreglado con TDD** (decisión del usuario: proponer el fix, no solo documentarlo): `RRF_K` se exporta desde `retriever.py` como constante única; `vector_strategy.py` normaliza `best.score` dividiendo por `RRF_MAX_SCORE = 1/(RRF_K+1)` **solo cuando no hay reranker** (con reranker el score ya está en [0,1] y no se toca). Dos tests nuevos en `test_vector_strategy.py` (match perfecto → normaliza a 1.0; con reranker no se re-normaliza) y `test_reranker.py::test_should_replace_fusion_score_with_rerank_score` corregido — su aserción `< 0.05` codificaba el propio bug como comportamiento esperado. **Verificado en vivo tras el fix**: la misma pregunta contra `Chatbot MAN Uno` ahora da `score: 0.7` en vez de `0.0057` (confirma el fix), pero **sigue sin pasar** (`passed: false`) porque ese chatbot solo tiene un documento y `merge_node` aplica una penalización `*0.5` cuando hay menos evidencias que `min_retrieval_results` (2 por defecto) — un mecanismo distinto, probablemente intencional (menos fuentes independientes, menos confianza), que **no se ha tocado** porque no era el bug reportado y cambiarlo es una decisión de configuración, no de escala. **971 passed, 0 failed** en `agents_hub` (unit+integration+evaluation) + `public_graphs` + `infra`, sin regresiones. **Pendiente**: para completar la verificación manual de REV.1/RAG con una respuesta real hace falta un chatbot con ≥2 documentos reales o bajar `min_retrieval_results` a 1 para la prueba — decisión del usuario, no tomada todavía. De paso, corregido `max_size_10mb` (es/ca/en): decía "Solo formato PDF" en `/hub/documents` cuando desde EXT.1 solo entra Markdown |
| 2026-08-12 | Arreglo de los 3 rojos de CI (no era «cosa de Windows») | **Cursor: sin cambio.** CI llevaba **cinco ejecuciones seguidas en rojo** con 3 fallos que el `.bat` y la nota de PowerShell hacían parecer ruido de entorno; son dos defectos reales de los tests, ambos en Linux. (1) `test_setup_script.py`: los dos tests que simulan «docker no instalado» pasan `PATH` vacío al hijo, y en POSIX es **el PATH del hijo** —no el del padre— el que `exec` usa para resolver el ejecutable, así que `["bash", ...]` dejaba de encontrar el propio bash y moría con `FileNotFoundError` antes de ejercer una sola línea de `setup.sh`. Se resuelve la ruta absoluta con `shutil.which("bash")` en el proceso de tests; el PATH vacío sigue llegando al script, que es lo que el test quiere probar. (2) `test_db_isolation.py`: CI levanta el Postgres de `DATABASE_URL` **vacío y sin migrar**, así que `select count(*) from hub_chatbots` sobre la BD «de desarrollo» daba `UndefinedTableError`; ahora se pregunta antes por `to_regclass` y, sin esquema del hub, el criterio se cumple por construcción. Los otros dos jobs (contrato y a11y) estaban en verde: con esto CI vuelve entero |
| 2026-08-11 | DER.3a — qué necesitamos de la publicación (entregable para la unidad de desarrollo) | **Cursor: DER.3a ✅; el bloque DER queda cerrado en lo que no depende de terceros.** `docs/REQUISITOS_PUBLICACION_PLATAFORMA.md`: qué información necesita el asistente de cada norma, y cuatro vías de notificación con su coste **para ellos**. **Dos mejoras sobre el prompt, salidas de releer el contrato**: (1) los campos se parten en dos tablas siguiendo los dos hitos que `CONTRATO_MD_CORPUS.md` ya distingue —lo que no depende de la validación de Secretaría General se puede acordar ya; lo que sí, después—, lo que convierte el documento en algo accionable en vez de una lista plana; (2) se añade la **señal de cambio del contenido** como requisito propio, con la regla de que debe cambiar con el texto y **no** con las etiquetas, porque sin decirlo cada revisión de vocabulario obligaría a reprocesar el corpus entero. **Recomendación explícita y no un menú neutro**: sondeo de índice o depósito de ficheros, porque son las únicas dos que constituyen **censo** —y sin censo no se puede distinguir «norma retirada» de «aviso perdido», así que el corpus se llena de normas derogadas—; webhook y cola valen como complemento para bajar la latencia, nunca como única vía. **Las tres reglas duras que rechazan el paquete se verificaron contra el código antes de prometerlas a otra unidad** (`manifest.py:136-141`, `metadata_filter.py:73`), no contra el contrato solo. La parte 3 explica por qué **no** les pedimos qué asistentes consumen cada norma, para que la pregunta no vuelva en la reunión siguiente |
| 2026-08-11 | DER.2 — deriva entre copias de la misma norma | **Cursor: DER.2 ✅ → DER.3a.** Detector `copia_divergent` que agrupa por **`canonical_url` y no por `content_hash`** —hash igual significa contenido idéntico, así que agrupar por hash es justo lo que NO encuentra la deriva—, `GET .../documents/{id}/copias` para avisar **antes** de confirmar el borrado, borrado en cascada opcional (`en_todos_los_chatbots`, por defecto **falso**: una cascada implícita sobre corpus normativo es cómo se pierde una norma sin que nadie lo pida), y aviso al terminar cada carga sobre qué asistentes de la organización quedan con la versión anterior, **sin propagar nada solo**. **Tres decisiones tomadas al implementarlo**: (1) el detector vive en `agents_hub` y no en `curation` —escribirlo ahí violaba la frontera de CUR.1 y lo cazó `test_frontera_curacion.py`; el criterio correcto ya estaba escrito en esa guarda para `gap_detector`, que su sujeto es un chatbot y su señal la **emite** el asistente hacia la curación—; (2) `severity` **crítica** cuando las copias no coinciden en `estat_vigencia`, que no estaba en el prompt y es el peor caso real: dos asistentes de la misma casa contestando lo contrario sobre si una norma sigue en vigor; (3) cuando no se puede determinar cuál es la vigente —mismas fechas, distinto contenido— se avisa a todos con `indeterminat: true` en vez de elegir una al azar, porque el aviso llevaría implícita una afirmación falsa. **Dos guardarraíles ajenos saltaron y se atendieron en vez de rodearse**: CAL.3 (la página de documentos pasó de 300 líneas → salen `DeleteDocumentDialog` y `useBorradoDeDocumento`) y CAL.4 (las formas plurales de i18next no aparecen literales en el código → la guarda de claves muertas aprende a buscar la clave base, y de paso se añaden las traducciones de `content_gap` y `revisio_vencuda`, que se renderizaban crudas). **Verificado contra Postgres real**: sembradas dos copias de la misma norma con `vigent`/`derogat`, el detector emite 1 hallazgo **crítico** sobre el chatbot atrasado señalando el hash vigente; `GET /copias` devuelve 1 y nombra al otro chatbot; el borrado sin cascada elimina 1 y avisa de que sigue en otro; la copia hermana sobrevive. 19 tests backend nuevos + 4 vitest; **290 vitest en serie, 0 rojos** (los 14 del paralelo eran contención, verificados aislados) |
| 2026-08-11 | DER.3 planificado y **bloqueado** — dónde vive el mapa documento→chatbots | **Cursor: sin cambio (DER.2 sigue siendo lo siguiente).** El usuario pregunta si, de cara al pipeline de publicación automatizado, la publicación debería indicar cuántos chatbots consumen cada norma para sincronizar la ingesta. **La necesidad es real; el sitio propuesto, no**, por tres motivos: el servicio de publicación no conoce los chatbots ni debe (sus UUID son internos); no es una propiedad de la norma sino de la configuración del asistente, y cambia mucho más a menudo; y **viola CLAUDE.md §5** —si el mapa vive en el documento publicado, añadir un tercer asistente obliga a volver a publicar documentos, cuando reclasificar debe costar un `UPDATE`—. **El reparto correcto ya está en el contrato**: la publicación aporta propiedades de la norma, y `us_assistents` (`si|restringit|no`) ya es la señal de publicación sobre este eje, bien acotada a «¿es apropiada para asistentes?» y no «¿para cuáles?». **Y el patrón de este lado ya existe**: `HubCorpusSelection` (`operational_models.py:131`) es una selección N:M chatbot→sitio con `rule_type`/`rule_value`/`auto_ingest_new`, o sea que el rastreo web ya resuelve «cuando aparezca contenido nuevo, quién lo consume» declarativamente y del lado de la plataforma; DER.3 es lo mismo con otra fuente y reinventarlo sería un error. **Queda BLOQUEADO, no pendiente**: el pipeline de publicación no está definido y escribir suscripciones contra un servicio cuya forma se desconoce es especular. Se comprobó que **nada de DER.1 lo cierra** —`--chatbot-id` imperativo y suscripciones declarativas conviven, igual que hoy conviven subida manual y rastreo automático—, y ahí está el motivo de que DER.1 dejara `corpus/sync.py` fuera de alcance: ahí los destinos saldrán de las suscripciones, no de argumentos. Disparador anotado: que se sepa qué metadatos emite el servicio y cómo notifica |
| 2026-08-11 | DER.1 — una carga, varios chatbots | **Cursor: DER.1 ✅ → DER.2.** `--chatbot-id` pasa a ser repetible en `corpus/load.py`. El corpus se lee, parsea y valida **una sola vez** —lo que garantiza que todos los asistentes reciben exactamente lo mismo, cosa que dos ejecuciones seguidas no garantizan si alguien toca un fichero entremedias—; lo que se repite es la reconciliación, que es lo único que depende del destino. Cada asistente resuelve **su** servicio de embedding y la CLI dice cuál usa cada uno: es la libertad por la que se descartó COR. **Dos decisiones que el prompt no cerraba**: (1) *todas* las comprobaciones —existencia, misma organización, espacio vectorial de FIX.4— van antes de la primera carga y no una por una, porque descubrir con el primer asistente ya cargado que el segundo tiene el corpus en otro espacio deja una pasada que no se puede repetir limpia; (2) se **confirma por asistente y no al final**, porque la reconciliación es incremental y conservar lo que sí funcionó hace que repetir solo rehaga lo que falta —tirarlo todo por un fallo en el último obligaría a re-embeber corpus ya embebido, que es la parte cara—; el código de salida es no-cero igualmente. **Verificado ejecutando la CLI contra Postgres real**: dos documentos reales del corpus curado en dos chatbots de la misma organización salen con 0 y reportan `ingeridos=2` por cada uno, con su línea de modelo; y con chatbots de organizaciones distintas aborta con 1 **antes de leer siquiera la carpeta**. **Desviación documentada**: `corpus/sync.py` sigue con un solo chatbot —su fuente es el servicio MCP de publicación y su caso de uso es otro; hacerlo repetible ahí sería una funcionalidad no pedida—. 8 tests nuevos, **795 en `tests/modules/agents_hub` + higiene**, cero rojos |
| 2026-08-11 | FIX.4 — la carga del corpus embebe con el modelo configurado (y vuelve a arrancar) | **Cursor: FIX.4 ✅ → bloque DER.** `corpus/load.py` y `corpus/sync.py` construían `LocalEmbeddingService()` a pelo, contradiciendo MOD.2. El efecto real **no era el que decía el hallazgo**: no es que fallara sin el extra, es que **la carga ignoraba la configuración** — con el extra instalado embebía con BGE-M3 aunque el despliegue estuviera configurado con Google, y la guarda de RAG.9 lo descubría en la primera consulta de chat, con la ingesta entera ya pagada. Ahora el servicio sale de `resolve_embedding_service` y la CLI imprime con qué modelo va a embeber. **Segundo arreglo, no previsto: las dos CLIs morían con SIGSEGV en Windows, y la causa era D.4.0.** El comentario de cabecera decía que importar `MarkdownChunker` arriba forzaba la carga de torch antes de que asyncpg abriera conexión; D.4.0 movió el import de `langchain_text_splitters` dentro del constructor del chunker y **dejó esa precarga sin efecto sin que el comentario dejara de afirmar que funcionaba**. Verificado que el fallo es previo a este prompt haciendo `git stash` y reejecutando sobre HEAD: 139 igual. La carga del corpus llevaba rota desde D.4.0 y no lo sabía nadie, **porque ningún test ejecuta la CLI** — ahora dos tests sobre el AST fijan la precarga y su **orden respecto al import de la conexión de BD**, que es lo que un comentario no puede garantizar. **Tercer añadido**: la guarda de espacio vectorial pasa a correr **en la carga**, donde no estaba —solo estaba en el chat y en `recalculate-corpus`—, así que una carga ya no puede meter un segundo espacio vectorial en un corpus existente; aborta con código 3 antes de escribir. **Verificado ejecutando la CLI de verdad** contra Postgres real: dry-run con 2 documentos reales del corpus curado sale con 0 y reporta `ingeridos=2`. 10 tests nuevos; **787 en `tests/modules/agents_hub` + higiene**, cero rojos |
| 2026-08-11 | REV.1 — el veredicto de quien revisa, sobre conversaciones reales | **Cursor: REV.1 ✅ → FIX.4, y detrás el bloque DER.** `HubInteraction` gana `review_verdict`/`review_note`/`review_by`/`review_at` con el mismo vocabulario y el mismo CHECK que `HubTestRun` (RAG.13), migración **`v9e0f1g2h3i4` aplicada**; `PATCH /hub/feedback/interactions/{id}/review` con la guarda de tenencia de SEC.8.1; el GET gana `review_status` (por defecto **pending**: la cola es lo que falta por mirar) y `verdict`, **con los filtros en el SQL y no en Python** —filtrar tras el `LIMIT` daría lo mismo con 50 filas y nada con 50.000—; `ReportsPage` con botones de veredicto, nota por fila, contador de pendientes, los dos filtros y las cuatro columnas nuevas en el CSV; i18n es/ca/en. **Nota obligatoria cuando el veredicto es `bad`**, validado en el contrato (422) y también antes de pedirlo desde la pantalla: un «mal» sin motivo no reformula nada. **Verificado end-to-end contra Postgres real** (no solo con mocks): 422 sin nota, 200 con nota firmando `review_by`/`review_at`, la cola baja de 2 a 1, los filtros `reviewed`/`verdict` devuelven lo esperado y una organización ajena recibe 403. **Recorrido en navegador** con dos interacciones sembradas y borradas después. **Dos hallazgos de la sesión**: (1) el backend del puerto 8000 servía código anterior —solo Postgres está en Docker—, así que el clic desde la pantalla no llegaba a la ruta nueva; se aisló levantando la app en proceso contra la misma BD, y **hace falta reiniciar el backend para probarlo a mano**; (2) `tsc` estaba **en rojo desde SEC.8.5** (`87d4542`) por `WIDGET_RAW_FETCH_EXEMPT`, una constante que quedó muerta al dejar el widget de ser excepción — borrada, y `tsc` vuelve a estar limpio. **1043 tests backend** en las zonas tocadas + higiene, **118 vitest** en `src/admin` y `src/shared/api`, `tsc` sin errores |
| 2026-08-11 | Bloque COR **descartado** el mismo día que se planificó | **Cursor: sin cambio.** El usuario contraargumentó la planificación de más abajo y **tenía razón**. Cuatro motivos: (1) la deduplicación de fragmentos solo se dispara si coinciden `embedding_model` y `chunking_strategy`, y **el piloto varía la estrategia a propósito** para comparar, así que se pagaría la migración de la tabla más caliente del sistema por un ahorro que en el caso real no llega; (2) el problema de mantenimiento se resuelve fuera del esquema —un solo `.md` en disco y dos cargas incrementales por hash, o sea editar una vez y ejecutar un comando dos veces—; (3) las filas separadas conservan que **cada asistente elija su modelo de embedding**, que es el conmutador que la guarda de RAG.9 existe para hacer seguro, y admiten `nivell_acces`/`us_assistents` distintos por asistente sin mover la política a una tabla de asociación; (4) el coste de duplicar son decenas de MB y céntimos de embeddings. **Dos argumentos míos eran incorrectos y quedan anotados como tales en el plan**: que el censo y la poda sean de la organización y que evaluarlos por chatbot debilite la salvaguarda del 10 % —falso, cada corpus se evalúa contra su propio tamaño, que es lo que la salvaguarda quiere decir—, y que una derogación haya que «aplicarla N veces», que son N ejecuciones de un comando incremental y no N ediciones. **Matiz factual corregido al usuario**: el embedding por defecto ya no será BGE-M3 local (D.4.0 lo sacó a extra opcional y el despliegue va con Vertex), así que duplicar sí cuesta dinero — céntimos, así que no cambia la decisión. **Queda un candidato sin planificar**: la duplicación deja abierto el riesgo de **deriva** —recargar un asistente y no el otro—, detectable agrupando por `canonical_url` dentro de la organización y contando `content_hash` distintos, dentro de la maquinaria de hallazgos de curación que ya existe |
| 2026-08-11 | Bloque COR planificado — un documento, muchos chatbots | **Cursor: sin cambio; COR queda antes de la primera ingesta real.** El usuario objeta que la misma norma usada en dos chatbots se ingiera dos veces, por espacio y sobre todo por mantenimiento cuando lleguen derogaciones. **Comprobado en el código**: `HubDocument` lleva `chatbot_id` con unicidad `(chatbot_id, content_hash)` y `HubDocumentChunk` también, así que son dos filas, dos juegos de fragmentos y dos de embeddings; 24 y 8 referencias respectivamente en 12 ficheros de `app/`. **La objeción de mantenimiento es la correcta y la de espacio es menor** (~60-70 MB de fragmentos por copia de un corpus de 262 documentos). Argumento estructural que aparece al mirarlo: el reconciliador de ING.0.5 exige `is_census()` para podar porque «la fuente es el corpus COMPLETO», y **eso es una afirmación sobre la organización, no sobre un chatbot** — hoy la operación peligrosa se repite N veces y cada pasada ve menos documentos, lo que además debilita la salvaguarda de proporción. **Reparto propuesto**: el documento se comparte siempre; los fragmentos solo si coincide la receta `(embedding_model, embedding_dim, chunking_strategy)`, porque un vector de Vertex y uno de BGE-M3 no son comparables aunque midan 1024 y la estrategia decide qué trozos existen. Se apoya en que **los fragmentos son dato derivado**: compartirlos es optimización, compartir el documento es corrección. **El riesgo real está en la ruta ANN** —hoy `chunk.chatbot_id = X` es igualdad sobre columna indexada y el HNSW de RAG.3 se usa; el subconjunto por asociación mete un filtro más, y DET.1 ya midió que tocar esa consulta la degradaba a `Seq Scan + Sort`—, así que **COR.0 es una medición y no cambia el esquema**. 7 prompts (COR.0-COR.6) en `Plan_TDD_Fase1.md`, con el atajo `usa_todo_el_corpus` para que solo pague el filtro quien de verdad usa un subconjunto |
| 2026-08-11 | Hallazgo I5 — los perfiles de grafo sin configurar dejan de mentir | **Cursor: sin cambio (REV.1 es lo siguiente).** El hallazgo describía **un** perfil, `PUBLIC_PORTAL_AGGREGATOR` con `uuid.UUID(int=0)`; al arreglarlo apareció que **`PUBLIC_PORTAL_ROUTER` tenía el mismo defecto** con `child_chatbot_ids=[]`. Ninguno de los dos falla: devuelven recuperación vacía y el asistente contesta que no encuentra información, indistinguible de un corpus mal cargado y sin traza ni log. Las dos factorías lanzan ahora `NotImplementedError` diciendo **qué falta** y **qué usar en su lugar** (`PUBLIC_KB_RICH`). **Lo más revelador**: `tests/public_graphs/test_profile_contract.py` parametrizaba sobre *todos* los perfiles registrados y **certificaba que los dos stubs compilaban y ejecutaban** — el contrato daba por bueno un perfil que no servía para nada, que es lo que mantuvo el defecto vivo. El contrato se aplica ahora a los perfiles fuera de `PERFILES_SIN_CONFIGURAR` (constante en `graph_factory.py`, fuente única de la distinción), con un test que impide que esa exclusión deje el contrato vacío y pase en verde por ausencia de casos. Retirados además de los dos selectores del panel (`ChatbotsPage`, `OrganizacionesPage`) y su clave i18n en es/ca/en, para que el fallo no se traslade al chat en vez de al momento de elegir. 5 tests nuevos; **113 en `tests/public_graphs` + el nuevo, 874 en las zonas tocadas + higiene**, con el flaky ya inventariado (`test_llm_configs_router::test_should_get_model_for_tier_returns_default`) verde en aislamiento; 25 vitest de las dos páginas |
| 2026-08-11 | D.4.0 — la pila de modelos locales pasa a extra de instalación | **Cursor: sin cambio.** `torch`, `torchvision` y `sentence-transformers` salen de las dependencias base al extra `[local-models]`; `embedding_service` y `reranker` explican al fallar qué extra instalar. **Causa raíz que no estaba en el plan**: `langchain_text_splitters/__init__.py` importa de forma ansiosa un splitter basado en `sentence_transformers` que este proyecto no usa, y no hay forma de cargar un submódulo sin ejecutar el `__init__` del paquete — el chunker lo importa ahora dentro del constructor. **Medido**: la app pasa de **627 MB a 345 MB** de RSS y arranca en **9,4 s** sin el extra (la pila no se carga en absoluto); con el extra instalado, como corre CI, son 559 MB y siguen cargándose `torch` y `transformers`, pero por `langchain_core`, que hace `try: from transformers import GPT2TokenizerFast` para decidir si sabe contar tokens. **Para la VM: `e2-small` (2 GB) es holgado.** Dos aserciones propias corregidas por falsas: un test que exigía que la app no importara `transformers` (imposible con el extra instalado, por ese `try`) y otro que comprobaba `"transformers" in extra`, que pasaba por ser subcadena de `sentence-transformers`. Commit `c92dc47` |
| 2026-08-11 | Bloque REV planificado (Gerencia) + confirmación del descarte de OWUI | **Cursor: sin cambio.** Gerencia quiere un asistente para funcionarios con identificación y poder **revisar las respuestas** para reformular las FAQ. Se reconsideró OWUI con ese caso y **se confirmó el descarte**: OWUI es una interfaz de chat y no aporta la revisión —que es lo que se pide—, duplicaría el registro de conversaciones (guarda las suyas mientras el backend sigue escribiendo `HubInteraction`) y debilitaría la cadena de identidad justo donde se audita quién preguntó qué. **Comprobado en el código que casi todo existe**: el asistente restringido es configuración (SEC.2.1 + SSO de AUTH), `HubInteraction` registra cada turno con `fallback_reason`, `ReportsPage` ya tabula y exporta a CSV, y RAG.14 cierra el bucle hacia el contenido. Falta solo el **veredicto del revisor sobre conversaciones reales** (hoy la valoración es del usuario final) → **REV.1**, que replica el patrón `verdict`/`verdict_note`/`verdict_by` de RAG.13. Los **hilos persistentes** —lo único que OWUI daba hecho, porque hoy el historial lo manda el cliente y no hay `conversation_id`— quedan como **REV.2 condicional**, con disparador explícito |
| 2026-08-11 | Bloque EXT completo + tres decisiones de alcance | **Cursor: EXT ✅ → Bloque FAQ, y detrás Deploy (D.0).** EXT.1 (`b9c2a94`), EXT.2 (`66acd4a`), EXT.3 (`d75a6f3`). **Suite backend 1796 passed, 3 skipped, 0 failed**; frontend 281 passed; `tsc` limpio. Docling retirado del árbol y de los dos `pyproject.toml` (nadie lo usaba en el legacy). **Medición de EXT.3, que da la vuelta a un supuesto**: la app en reposo son 627 MB y quien manda ya no es la extracción de documentos —`pdfplumber` cuesta 5 MB— sino la pila de modelos locales (`sentence-transformers` 216 MB, `torch` 172 MB, más `transformers`), que está por `LocalEmbeddingService` y `LocalReranker`. **Tres decisiones tomadas en conversación**: (1) **bloque OWUI descartado** —se pierde la identidad institucional que SEC.8.6 acaba de construir, actualizarlo cuesta, consume la VM única, y su público era el interno; el adaptador compatible-OpenAI se aparca también por no ser especulativo—; (2) **bloque FAQ nuevo**: las preguntas frecuentes se ingieren como documentos `content_class: faq`, con un encabezado por pregunta para que pregunta y respuesta caigan en el mismo fragmento, y citadas como orientativas y no como norma; (3) **D.4.0 nuevo**: la pila de modelos locales pasa a extra `[local-models]` antes de dimensionar la VM. **Aclaración de modelo registrada**: el corpus ingerido **no** es compartido entre chatbots —`HubDocument` tiene unicidad `(chatbot_id, content_hash)`—, así que varios chatbots sobre el mismo `.md` significan varias ingestas y varios juegos de embeddings; lo compartido es la curación (organización) y lo que varía por chatbot es la estrategia (10 campos en cascada) |
| 2026-08-10 | Bloque SEC.8 completo + decisión de extracción y despliegue | **Cursor: SEC.8 ✅ → Bloque EXT, y detrás el Deploy reescrito.** Los 8 prompts de SEC.8 cerrados (detalle en la fila del bloque). **Suite backend 1782 passed, 1 skipped, 0 failed; frontend en serie 276 passed; `tsc` limpio.** Dos migraciones aplicadas. **Dos decisiones de arquitectura tomadas en conversación** y escritas en `docs/DECISION_EXTRACCION_Y_DESPLIEGUE.md`: (1) **la extracción se parte en dos** — al corpus solo entra `.md` conforme al contrato, porque una extracción mala ahí es una cita errónea que nadie detecta y que sale con autoridad de norma, mientras que en el contexto temporal la persona tiene el documento delante y ve el fallo; la conversión vive fuera y previsiblemente pasará a plantilla + pandoc, que es determinista y mueve el problema al origen. (2) **el despliegue es una VM, no Cloud Run**, porque el scheduler de calidad y el rastreo necesitan un proceso vivo y el ahorro de escalar a cero no era cobrable de todos modos; Cloud SQL y GCS se conservan porque lo que hay en la base es el corpus curado y conversaciones de ciudadanos. **Hallazgo de método registrado**: cerrar SEC.8.1 verificando solo `tests/api` dejó 30 rojos que destapó la suite completa — los directorios que toca un prompt incluyen los tests unitarios de los routers, no solo los de su superficie |
| 2026-08-10 | Auditoría pre-deploy + SEC.8.0 (CR-1) + planificación de SEC.8 | **Cursor: sin cambio (MAN.2 sigue abierto); añadido el bloque SEC.8 como bloqueante pre-deploy antes de D.0.** Auditoría de solo lectura en cuatro frentes (seguridad, integridad funcional, corpus, arquitectura LLM/retrieval) volcada a `docs/AUDITORIA_PRE_DEPLOY.md` (commit `ba9f3dc`). **SEC.8.0 arreglado con TDD** (commit `56d5ef4`): `seed_multitenancy_defaults()` corría en el `lifespan` en cualquier entorno y sembraba el SuperAdmin `fabra@uji.es`/`admin1234` (credencial pública en el repo, acceso a todos los tenants); ahora retorna sin sembrar fuera de `development`, y producción usa `server.app.scripts.bootstrap` (SUPERADMIN_EMAIL/PASSWORD). Tests `tests/unit/test_seed_environment_gate.py` (2) + higiene verde + 34 tests de BD (bootstrap + aislamiento) sin regresión. **Bloque SEC.8 planificado** (SEC.8.1–SEC.8.8 en `Plan_TDD_Fase1.md`): IDOR horizontal en los routers post-SEC.2 (Opus), subidas, sandbox, cabeceras/SAML, widget sin Bearer privilegiado, temas persistentes en BD, limpieza de 14 tests rotos de la raíz `tests/`, decisión del crawler. **Corpus**: el bloqueante de ingesta (`us_assistents` booleano vs `Literal si/no/restringit`) se corrigió en el generador del proyecto del corpus y `md_contracte/` regenerado — 262/262 pasan la validación real del pipeline (cuerpos intactos). El resto de depuración del corpus lo lleva el usuario en el proyecto paralelo |
| 2026-08-09 | MAN.2 (parcial) — `.bat` maestro y recorrido en vivo de los caminos 1 y 2 | **Cursor: sin cambio (MAN.2 no cerrado, falta ejecución humana + decisión sobre 2 hallazgos).** `pruebas_manuales_plataforma.bat` nuevo: menú (o parámetro `0`-`4`) para los cuatro caminos que cruzan módulos que pide el prompt. Probado en seco contra el backend/frontend reales de la sesión; **el propio guion tenía un bug** (faltaba el salto para el parámetro `0`, y unos `^` sobrantes en las cadenas de `curl` sacaban `-^>` en vez de `->`), corregido antes de dar el `.bat` por bueno. **Recorrido en vivo con datos reales** de los caminos 1 y 2: organización nueva + 2 chatbots, un PDF real ingerido (Docling + RapidOCR en los logs), 3 consultas reales contra Gemini (clave real configurada en este entorno) y `POST /hub/quality/gaps/analyze` ejecutado sobre el `GapFinding` que CUR.1 corrigió el día anterior — responde 200 con 0 huecos, que es el resultado correcto dado que las consultas se lanzaron desde Escenarios de prueba y no desde una conversación real. **Dos hallazgos reales nuevos, documentados en `docs/PRUEBAS_MANUALES.md` y sin arreglar a la espera de decisión del usuario**: (1) `GET /hub/redaccion/templates` da 500 para cualquier sesión de desarrollo porque `hub_redaccion_router.py` hace `uuid.UUID(user.user_id)` en 6 sitios sin comprobar que el `user_id` sea UUID — el SuperAdmin tiene `admin_id` entero y el Admin `partner_id` de texto libre, ninguno lo es; bloquea entero el camino 3 (plantilla de redacción) y el frontend no muestra ningún error, sólo una pantalla en blanco; (2) el widget público no consume ningún tema de organización/chatbot — `ThemeProvider` sólo está cableado en `frontend/src/App.tsx` (panel admin), nunca en `frontend/src/widget/`, así que "temas distintos → widget de cada uno" del camino 1 no se puede verificar porque el código no existe todavía. **Hallazgo adicional, no un bug**: "Ejecutar" en `/hub/test-scenarios` fija `fallback_reason=None` a propósito, así que sus ejecuciones nunca alimentan la detección de huecos de RAG.14 — hace falta una conversación real (widget o `POST /hub/chat/{id}`) para ese camino, documentado para que nadie repita la confusión. La organización, los 2 chatbots y el documento ingerido **se dejan sembrados** en la BD de desarrollo. Automatización del navegador con fallos intermitentes de clic sintético durante la sesión (confirmado con `element.click()` vía JS que el código respondía bien; era la capa de automatización, no la app) — se resolvió despachando clics vía `javascript_tool` el resto de la sesión. Commit `aaf3b86` |
| 2026-08-09 | Fix suelto — `SitesPage` no refrescaba la lista tras eliminar (hallazgo de MAN.1) | **Cursor: sin cambio (MAN.1 sigue completo).** A petición del usuario, arreglado el bug anotado en el historial de MAN.1: `useDeleteSite()` en `curation/SitesPage.tsx` no llevaba `onSuccess`, así que el `DELETE` funcionaba en el servidor (204) pero la fila se quedaba en la tabla hasta recargar a mano. RED primero — test que captura el `onSuccess` pasado al hook mockeado y comprueba que invalida `getListSitesQueryKey()`, fallando con "expected undefined to be type of 'function'" contra el código sin arreglar. GREEN — mismo patrón que ya usa `useCreateSite` en el mismo fichero: `onSuccess: () => qc.invalidateQueries({ queryKey: getListSitesQueryKey() })`. **21/21 vitest de `src/curation`.** La reverificación manual en el navegador real se topó con el propio Vite dev server reconectando en bucle (varios "[vite] connecting/connected" seguidos sin que mediara ninguna acción), así que el diálogo "Nuevo sitio" dejó de responder a los clics del agente a media verificación — no se persiguió más al no ser un síntoma del código, y la cobertura del test unitario ya prueba la causa exacta del hallazgo. Commit `6cba8fc` |
| 2026-08-09 | MAN.1 — Inventario, poda y matriz de lo irreducible | **Cursor: MAN.1 ✅ → MAN.2.** Revisados los 11 `.bat` de la raíz contra el código de hoy (el plan hablaba de 10; el conteo real ya incluía `bloqueRAG`). **Borrados 3 caducados**: `9CBis.8` y `9CBis.9` describían subir un PDF desde una pestaña dentro de la edición del chatbot y la tab "Fuentes web" de la vieja `DocumentsPage` — ambas cosas las sustituyó CAL.1-CAL.4 por la página `/hub/documents` independiente; `9Q.9` apuntaba a `/hub/sites` y `/hub/content-quality`, movidas por CUR.2 a `/curation/*`, con un "panel de mapeo" anidado que ya no existe (ahora es la página `Publicación`). **Corregidas 2 rutas muertas** sin borrar el resto del fichero: `bloqueRAG` apuntaba a `/hub/content-quality` (→ `/curation/findings`) y `bloqueSEC` a `/admin/chatbots`, una ruta que **nunca existió** (siempre fue `/hub/chatbots` — bug de copiar/pegar anterior a cualquier bloque, no caducidad). Los 6 restantes, vigentes sin tocar. `docs/PRUEBAS_MANUALES.md` nuevo: matriz por módulo (agents_hub, automation, redaccion, widget, themes, MCP, client_app, `DEPLOY_MODE`) de lo irreducible, con el motivo concreto de cada fila. **Añadido de más, a petición expresa del usuario en la propia sesión**: recorrido real en navegador con Docker levantado y sesión de `fabra@uji.es` — Hub completo (9 pantallas) con datos reales sembrados por `bootstrap.py`, y Curación completa creando un sitio de verdad (`POST /hub/sites` → 201), disparando su rastreo (202), generando su informe de auditoría y ejecutando el análisis de huecos de corpus (RAG.14) contra el `GapFinding` corregido en CUR.1 — las 4 páginas de curación funcionan de punta a punta contra Postgres real. MCP: su suite propia (`mcp_server/tests`, fuera de `server/tests`) da **54 passed**. Widget: `widget.html` no renderizaba por falta de `frontend/dist/widget/`; construido con `npm run build:widget` y probado — el input y el envío funcionan, pero el chatbot de fixture (`E2E Bot`, un UUID que no está en el seed de desarrollo) devuelve 401 sin mensaje de error visible en la interfaz, anotado en la matriz como algo que un humano debe configurar con un chatbot real, no una regresión. **Bug real destapado, sin arreglar a la espera de decisión del usuario**: en `curation/SitesPage.tsx`, `useDeleteSite()` no lleva `onSuccess`/`invalidateQueries` — el `DELETE` funciona en el servidor (204, confirmado recargando la página) pero la fila del sitio borrado se queda en la tabla hasta que alguien refresca a mano. Es anterior a CUR.2 (el código se movió tal cual desde `SiteMappingPanel`/`SitesPage`, sin tocar esa lógica) |
| 2026-08-09 | Fix — `GapFinding` sin `site_id`/`page_id` (regresión real de CUR.1, destapada al levantar Docker) | **Cursor: sin cambio (Bloque CUR completo).** El cierre de CUR.1 se hizo con Docker/Postgres apagados: las 3 suites de integración movidas (`test_site_model.py`, `test_staleness_detector.py`, `test_gap_detector.py`) **saltaron (`SKIPPED`) en vez de ejecutarse**, así que el informe de cierre reportó "1414 passed, 280 skipped" como si fuera verde completo. Al levantar Docker y repetirlas, `test_gap_detector.py::test_should_create_content_gap_finding_with_representative_queries` **falló de verdad**: el `GapFinding` local que sustituyó a `ContentFinding` en `d1b9453` no llevaba `site_id` ni `page_id`, y ese test de 9Q —que CUR.1 prometía no tocar— los sigue comprobando contra el propio hallazgo (`hueco.site_id is None`, `hueco.page_id is None`). Arreglado añadiendo los dos campos a `GapFinding` con default `None`, sin tocar la aserción del test. **Lección operativa**: un `SKIPPED` por falta de BD no es lo mismo que un verde, y un informe de cierre de bloque que se apoya en una suite con saltos por prerrequisito ausente puede estar dando por bueno código que no se llegó a ejecutar. Suite completa reejecutada contra Postgres real: **1697 passed, 1 skipped, 0 failed**. Commit `d9ed15f` |
| 2026-08-09 | CUR.2 (RED/GREEN) — Superficie propia en el frontend y publicación explícita · **cierra el Bloque CUR** | **Cursor: CUR.2 ✅ → Bloque MAN (MAN.1).** `frontend/src/curation/` deja de ser tres pestañas dentro del panel de documentos y pasa a ser una **cuarta sección de nivel superior** en `AppLayout` (Hub / Curación / Automatización / Plataforma), con navegación propia en `CurationLayout`: Sitios, Auditoría, Hallazgos, Publicación. **`SitesPage` pierde el panel de mapeo embebido** que antes aparecía al pulsar una fila: gestionar sitios ya no implica decidir qué se publica. **`PublicationPage`** (antes `SiteMappingPanel`, colgado de una prop `siteId` que sólo llegaba al pulsar una fila) sube a página propia con su propio selector de sitio — es la lectura literal de `docs/DECISION_CURACION_SEPARADA.md`: *"`CorpusSelectionService` es el punto exacto donde la materia prima se convierte en corpus... debe ser explícito y visible"*. Cada página candidata se publica con su **propio botón**; no hay acción masiva de "ingerir todo el sitio", que es justo la automatización que la decisión retira. **`ContentQualityPage` se parte en dos**: `AuditPage` (el informe de calidad vale por sí solo, sin chatbot ni pasos intermedios — se muestra en cuanto se elige un sitio) y `FindingsPage` (hallazgos de sitio + `ContentGapsPanel` de RAG.14 montado siempre, como entrada de trabajo del curador y no como panel aparte del chatbot). **El namespace i18n `contentQuality` se renombra a `curation`** en `es/ca/en` y en `i18n/index.ts`; de paso salen **4 claves que ya estaban muertas antes de este prompt** (`crawl_queued`, `ingest_queued`, `analyze_queued`, `candidate_title` — reservadas para mensajes de estado que nunca se llegaron a renderizar). **Guardarraíl nuevo `curationI18nCoverage.test.ts`**: paridad es/ca/en, sin claves muertas, sin `t('clave','default')` inline (mismo patrón que CAL.4.1) y sin literales castellanos incrustados — los 4 tests que pedía el prompt (`should_render_curation_nav_without_chatbot_context`, `should_require_explicit_publish_action_per_page`, `should_list_corpus_gaps_from_rag14_as_curation_input`, `should_have_no_hardcoded_strings`) viven repartidos entre este fichero y `curation/__tests__/*`, uno por componente. **274/274 vitest** (en serie), `tsc --noEmit` limpio; el guardarraíl `routeSplitting.test.ts` de CAL.5 amplía su lista de envoltorios estáticos con `CurationLayout` (mismo motivo que `HubLayout`: está en todas las rutas de la sección). Verificado en navegador: las 4 pantallas cargan sin errores de consola, "Curación" aparece en la barra lateral entre Hub y Automatización, y las pestañas "Llocs web"/"Qualitat de contingut" desaparecen de la subnav de Hub. Dos commits: `d1b9453`+`fd39ee9` (CUR.1) y `ca53cd5` (CUR.2) |
| 2026-08-09 | CUR.1 (RED/GREEN) — Módulo propio para la curación | **Cursor: CUR.1 ✅ → CUR.2.** `ingestion/quality/` (18 ficheros) y los spiders se mueven a `modules/curation/`, nuevo módulo con su propia frontera: no importa de `agents_hub/agent/` ni de routers cloud, y `agents_hub` no importa de él — la única comunicación es la tabla `hub_content_findings`. **`gap_detector.py` se queda en `agents_hub/ingestion/`** (aplanado, ya no en `quality/`): nace de `HubInteraction`, no de auditar páginas, y es la señal que el asistente emite hacia la curación, no al revés. **La frontera nueva chocó con dos dependencias reales que el prompt no anticipaba explícitamente**: (1) `gap_detector.py` importaba `ContentFinding` de `contracts.py` y `_cosine` de `semantic_detector.py`, ambos ahora en curación — resuelto con un `GapFinding` local (sin el validador de "un solo sujeto", innecesario aquí porque el sujeto siempre es un chatbot) y una copia local de `_cosine` (5 líneas de aritmética no justifican una dependencia cruzada); (2) `site_crawler.py` (que sí se muda) importaba `detect_language` de `agents_hub/agent/language_detector.py` — se resuelve moviendo `language_detector.py` a `agents_hub/services/`, sitio ya establecido para lo compartido (`embedding_resolver` vive ahí y la propia regla de CUR.1 lo permite explícitamente). **`detect_gaps.py`** (CLI de RAG.14) se queda junto a `gap_detector.py`; **`detect_stale.py`** (CLI de SYNC.2) se muda con `staleness_detector.py`. Guardarraíl nuevo `tests/modules/curation/test_frontera_curacion.py`: dos tests de grep por dirección de la frontera, uno que `gap_detector.py` sigue en el lado del asistente, y uno que `ingestion/quality/` y `ingestion/spiders/` no dejan restos. Los 9 tests de 9Q que se movían de directorio se mueven a `tests/modules/curation/{unit,integration}/` **sin tocar una sola aserción**, sólo la ruta de import. `grep -r "ingestion.quality"` = 0 fuera del propio guardarraíl (que lo nombra para no cazarse a sí mismo). **Suite backend completa: 1414 passed, 280 skipped, 4 failed — los 4 ya conocidos y achacables a que Docker/Postgres no estaban levantados en esta máquina** (`test_database_connection.py` ×2, `test_sys_flow_orchestrator_registration`, y el falso rojo de contención de `test_setup_script.py` ya documentado); los tests de integración movidos de curación (`test_site_model.py`, `test_staleness_detector.py`, `test_gap_detector.py`) **saltan (`SKIPPED`) en vez de fallar**, que es el comportamiento correcto de la fixture `db_url` sin servidor. **Cometido en dos partes**: el primer commit (`d1b9453`) dejó `gap_detector.py`/`detect_gaps.py` con el contenido previo al `git mv` porque las ediciones posteriores no se habían vuelto a `git add`; corregido en `fd39ee9` sin usar `--amend`, según la política del proyecto. Sin migración |
| 2026-08-09 | CAL.4.1 (RED/GREEN) — Las 41 claves de la pantalla de documentos · **cierra el Bloque CAL** | **Cursor: CAL.4.1 ✅ → Bloque CUR (CUR.1).** Las 41 claves medidas en CAL.4 que sólo vivían como *default* dentro de `t('hub.x', 'texto')` suben a `es/ca/en admin.json` conservando el texto castellano actual, repartidas exactamente como se había medido: `DocumentsTable` 12, `IngestionJobsPanel` 7, `RechunkControls` 7, `DocumentsPage` 5, `RetrievalBanner` 4, `UploadDropzone` 4, `DocumentBadges` 2. **De paso, dos claves que ya existían en el diccionario pero seguían llevando un default inline** (`hub.delete_doc_confirm` en `DocumentsPage`, `hub.language_label` en `UploadDropzone`) pasan también a `t()` de un solo argumento — un default que nunca se usa es texto muerto que se desincroniza sin que nadie lo note. **Las cuatro cadenas fijas de `JobStatusBadge`** (`'Completado'`, `'Procesando'`, `'Error'`, `'En cola'`), que no pasaban por `t()` en absoluto, se suben también como `hub.job_status_*` — el componente no tenía `useTranslation`, así que se añade. **Guardarraíl nuevo `should_not_keep_admin_keys_only_as_inline_default`** en `adminI18nCoverage.test.ts`: barre el código con el patrón exacto `t('hub.x', '...')` y falla si esa clave no está en `es/admin.json` — el test de paridad existente no lo caza por construcción, porque lo que no está en el diccionario no se compara con nada. RED confirmado con las 41 claves exactas antes del GREEN. **Test nuevo `should_render_documents_screen_in_catalan`** (`DocumentsPageCatalan.test.tsx`, doble de `react-i18next` resolviendo contra `ca/admin.json` real, no contra `es`): confirma que el cuerpo entero de la pantalla —título, tabla, dropzone, panel de jobs, controles de recálculo— sale en catalán. **264/264 vitest en serie, `tsc --noEmit` limpio.** Verificado en navegador con `i18nextLng=ca` en `/hub/documents`: cabecera («Documents i Ingestió», «Gestiona la base de coneixement del chatbot.») y estado vacío («Crea o selecciona un chatbot primer») en catalán, consola sin errores; **el cuerpo con datos reales (tabla, jobs, recálculo) no se pudo recorrer en vivo porque Docker/la BD no estaban levantados** —cubierto igualmente por los dos tests de integración, que renderizan los mismos componentes contra el mismo diccionario real con datos de fixture— |
| 2026-08-03 | Planificación — **Bloque MAN** (validación manual de la plataforma) y **CAL.4.1** | **Cursor: sin cambio de ejecución; se replanifica lo que queda.** A petición del usuario, las pruebas manuales dejan de hacerse **por bloque** y pasan a ser una **campaña de plataforma**. El motivo no es de forma: el modelo por bloque ha dejado **10 `.bat` sueltos en la raíz** y **varios han caducado sin que nadie lo note** —ROL renombró Partner→Admin y Client→Organización, CAL.2 retiró el panel de fuentes web, CAL.3/CAL.4 rehicieron la pantalla de documentos—, así que un guion que dice «pulsa *Fuentes web*» hoy hace concluir que la aplicación está rota. Y **nadie ha probado nunca la plataforma de una pieza**: los caminos que cruzan Hub → Redacción → Automatización no los ha recorrido nadie enteros. Nace el **Bloque MAN** con 4 prompts: MAN.1 (inventario, **poda de los `.bat` muertos** y matriz de lo irreducible en `docs/PRUEBAS_MANUALES.md`), MAN.2 (campaña funcional en local con `.bat` maestro y los caminos entre módulos), MAN.3 (lector de pantalla real e identidad visual — lo que axe no puede oír ni juzgar) y MAN.4 (post-deploy: SSO SAML real, sistemas externos, Cloud Run/SQL/GCS, edge vs cloud). **Se colocan después de CUR** —que mueve módulos, y documentar antes una estructura a punto de cambiar es trabajo tirado— y **antes del Deploy**, salvo MAN.4. **Las pruebas manuales pendientes del Bloque SEC quedan absorbidas por MAN.2** en vez de reejecutarse sueltas. Se añade también **CAL.4.1** como arranque de la próxima sesión. La lista de orden vigente del fichero, que era del 2026-08-01 y daba por pendientes RAG/SYNC/SEC/CAL, queda reescrita: **17 prompts para cerrar la Fase 1**. Sin cambios de código |
| 2026-08-03 | Cierre del Bloque CAL — borrado de los 3 scripts huérfanos + suite completa | **Cursor: sin cambio (Bloque CAL completo).** Borrados `scripts/verify_fix.py`, `scripts/diagnose_startup.py` y `scripts/init_rpa_prompts.py` a petición del usuario: eran Caso B de manual (importaban `app.database.*`, `app.services.*` y `app.core.*`, rutas anteriores al monorepo que ya no existen) y **no podían ejecutarse ni entre ellos** —`diagnose_startup` llamaba a un `init_rpa_prompts` de `app.database.seeds` que `main.py:91` ya documentaba como inexistente—. Nadie los invocaba desde fuera. **Suite backend completa desde Git Bash: 1691 passed, 1 skipped, 1 failed en 17 min.** El único rojo es `test_setup_script.py::test_should_check_docker_and_required_ports`, y **no es una aserción sino un `TimeoutExpired`**: invoca `bash setup.sh --dry-run` con 60 s de margen y con `-n auto` los 16 workers no le dejan acabar. En serie ese fichero da **12 passed en 10,41 s**, así que es contención, no regresión — el gemelo backend del falso rojo de vitest. Anotado como tal para que el próximo que lo vea no lo investigue dos veces |
| 2026-08-03 | CAL.5 (RED/GREEN) — Carga por ruta y retirada de shims · **cierra el Bloque CAL** | **Cursor: CAL.5 ✅ → Bloque CUR.** **Frontend**: las 20 páginas de `App.tsx` pasan de import estático a `React.lazy` + `import()`. Medido con `vite build` antes y después: el arranque baja de **un único bundle de 1.183 kB (gzip 341) a 386 kB (gzip 120) repartido en 29 chunks** — un **67 % menos** de descarga inicial. El más grande que sale del camino crítico es `ReportsPage` con **345 kB**, que es `recharts`: quien entra a ver chatbots ya no se lo descarga. `AppLayout`, `HubLayout` y `PrivateRoute` **siguen estáticos** a propósito: están en todas las rutas, partirlos añade una espera y no ahorra nada. **El `fallback` del `<Suspense>` deja de ser `null`**: con carga por ruta eso era una pantalla en blanco durante la descarga, indistinguible de una aplicación colgada. **Backend**: fuera el shim `init_db = init_server_db` que prohíbe CLAUDE.md, y fuera las tres entradas «Legacy alias» de `seeds.py` (`sys_script_gen`, `factory_gen`, `anchor_based`), que asignaban tier a `service_id` que no declara nadie —el bucle hacía `session.get` y seguía, así que no fallaban: simplemente no hacían nada y sugerían que existían—. **Hallazgo**: los tres únicos llamantes del shim (`scripts/verify_fix.py`, `diagnose_startup.py`, `init_rpa_prompts.py`) importan además rutas pre-monorepo inexistentes y **no pueden ejecutarse**; se les actualizó el nombre real pero **no se han borrado**, por quedar fuera del alcance del prompt — detalle en la nota del cursor. El guardarraíl `test_no_legacy_shims.py` **se excluye a sí mismo** del barrido, porque nombra el alias en sus docstrings y si no nunca podría estar en verde. El test de troceado se apoya en el **fuente de `App.tsx` y no en `dist/`**, que está en `.gitignore`: un guardarraíl que pasa o falla según si alguien compiló antes no sirve de guardarraíl. **262/262 vitest** (en serie), `tsc --noEmit` limpio, backend de las áreas tocadas en verde. Verificado en navegador: `/hub/chatbots` y `/hub/reports` cargan sus chunks por separado, consola sin errores |
| 2026-08-03 | CAL.4 (RED/GREEN) — Cobertura i18n del panel admin | **Cursor: CAL.4 ✅ → CAL.5.** `ca/admin.json` pasa de **75 a 218 claves** y los tres idiomas quedan en **paridad exacta**. Extraídos a claves los literales castellanos de `LLMConfigsPage` (~30, incluida la sección de proveedores entera) y de `ChatbotsPage` (11), y convertidas a **claves de traducción** las constantes de módulo con etiqueta: `LANGUAGE_OPTIONS` y `RETRIEVAL_LABELS`→`RETRIEVAL_LABEL_KEYS` en `admin/documents/constants.ts`, y `RETRIEVAL_MODES` en `ChatbotsPage`. **El motivo de guardar clave y no texto no es de estilo**: una constante de módulo se evalúa una sola vez, así que la etiqueta ya traducida se congelaría en el idioma activo al cargar y cambiar de idioma no la actualizaría. **Accesibilidad**: 29 `<label>` sin `htmlFor` asociados a su campo — LLMConfigsPage 1/14→14/14, ChatbotsPage 13/22→22/22, OrganizacionesPage 5/12→12/12; un label sin `htmlFor` no nombra a nada y el lector de pantalla lee «cuadro de edición». **Borradas 8 claves muertas** (5 del panel de fuentes retirado en CAL.2, más `chatbot_client`, `select_prompt_template` y `tab_assistant`), con un guardarraíl nuevo que las detecta y que **comprueba por prefijo las dinámicas** (`nav.*`, `hub.availability_*`, `verdict_*`), porque un `grep` literal las daba por muertas siendo falso. **Fallo real destapado**: `hub.delete_confirm` estaba usada para dos cosas —la *pregunta* «¿Eliminar este chatbot?» en ChatbotsPage y la *etiqueta del botón* en DocumentsPage—, así que el botón de borrar un documento decía «¿Eliminar este chatbot?»; separada en `hub.delete_doc_confirm`. Lo destapó endurecer el doble de `react-i18next` en los tests para que resuelva contra el diccionario real en vez de devolver la clave. **260/260 vitest** (en serie), `tsc --noEmit` limpio. Verificado en navegador **con `i18nextLng=ca`**: `/hub/llm-configs` sale íntegramente en catalán, consola sin errores. **Deuda medida que queda abierta**: 41 claves de la pantalla de documentos viven sólo como *default* en el código y no están en ningún diccionario, así que con el idioma en catalán el cuerpo de esa pantalla sigue en castellano — detalle y reparto por fichero en la nota del cursor |
| 2026-08-03 | CAL.3 (RED/GREEN) — Descomponer DocumentsPage | **Cursor: CAL.3 ✅ → CAL.4.** `DocumentsPage` queda en **291 líneas** (venía de 696 tras CAL.2, y de 1.019 antes) como orquestador puro: estado, hooks del contrato y composición. Nueve módulos nuevos en `admin/documents/` —`UploadDropzone`, `DocumentsTable`, `IngestionJobsPanel`, `RechunkControls` (+`RechunkStatus`, +`RechunkConfirmDialog`), `DocumentPreviewModal`, `RetrievalBanner`, `ConfirmDialog`, `DocumentBadges`, más `constants.ts` y `format.ts`—, siguiendo la convención de carpeta de feature de `admin/chatbots/`. **Lo que se queda en la página es lo que sólo se puede decidir con el corpus entero delante**: los idiomas del filtro, el total de tokens del banner y el texto de la advertencia al recalcular; por eso la tabla recibe los documentos ya filtrados y `headerActions` como hueco. **`RechunkControls` exporta dos piezas y no una** porque los botones viven en la cabecera de la tabla y el estado encima de ella: unirlas obligaría a mover una de sitio, y el prompt pide *sin cambio de comportamiento*. **La prueba de que el comportamiento se conserva es que `DocumentsPage.test.tsx` no cambia ni un assert** y sigue en 11/11 — un refactor de composición que necesita reescribir sus propios tests de comportamiento no ha demostrado nada. 10 tests nuevos de los subcomponentes por separado, incluido un guardarraíl de tamaño (<300 líneas). **Desviación**: `should_render_sources_panel_component` del plan **no aplica** —CAL.2 retiró el panel de fuentes con su endpoint— y se sustituye por `should_not_reintroduce_sources_panel`, que falla si alguien crea `SourcesPanel.tsx`. **255/255 vitest** (en serie; ver la nota de falsos rojos en paralelo), `tsc --noEmit` limpio. Verificado en navegador: la pantalla queda **idéntica**, los 9 módulos cargan 200, cambiar de chatbot vuelve a pedir sus documentos y el panel de jobs sigue consultando **sólo al desplegarse**; consola sin errores. Los dos chatbots de desarrollo tienen el corpus vacío, así que la tabla **con filas** y los botones de recalcular/limpiar están cubiertos por los tests de componente, no por vista |
| 2026-08-03 | CAL.2 (RED/GREEN) — La capa API del frontend sale del contrato (cierra CF.4) | **Cursor: CAL.2 ✅ → CAL.3.** El prompt pedía «usar los tipos de `generated/model`», y esos tipos **no existían**: los endpoints de ingesta, el de revisión de feedback y dos de `llm-configs` **no declaraban `response_model`**, así que FastAPI los documentaba como objeto vacío y Orval los generaba `Promise<unknown>`. Migrar sin arreglar eso habría *movido* la interfaz escrita a mano de `shared/api/*.ts` a la página, no eliminado — que es justo lo que prohíbe la regla maestra 4. **RED medido sobre el contrato commiteado**: los 6 schemas, ausentes. **GREEN**: 13 modelos de respuesta nuevos en `hub_ingestion_router.py`, `hub_feedback.py` y `hub_llm_configs_router.py`; contrato y cliente Orval regenerados (200 schemas). Retirados **6 módulos manuales** (los 5 del prompt + `copilotApi.ts`, que su criterio de cierre alcanza y cuyo `// TODO CF.4` ya estaba desbloqueado), `AdminIngestionAssistant` y el panel de fuentes web —mando a distancia de un endpoint retirado en `0196ff5`—: `DocumentsPage` pasa de **1.019 a ~610 líneas** y de 3 pestañas a ninguna. Repuntadas 6 pantallas + 8 ficheros de test. **Tres hallazgos que no estaban en el plan**: (1) un `response_model` sobre un objeto ORM **recién construido** revienta, porque `chunks_processed`/`created_at`/`processing_stats` los pone el *flush*, no el constructor —3 tests montaban la sesión con `AsyncMock` y su job nunca lo pasaba; arreglados los dobles, no el modelo—; (2) el `response_model` **filtra en silencio**, y dejarse `processing_stats` fuera tumbó el test de progreso de RAG.12; (3) al pasar de `fetch` a axios, `err.message` habría degradado a «Request failed with status code 409» en todo el panel, así que `client.ts` traduce ahora el `detail` de FastAPI a `message`. Guardarraíl nuevo `contractFirstApi.test.ts` (lee el árbol; falla si vuelve un módulo a mano, un `fetch` en `DocumentsPage`, un `localhost:8000` o un `Authorization` fuera de `client.ts`; exento el SSE del widget). **245/245 vitest, `tsc --noEmit` limpio, 91 tests backend de las áreas tocadas + higiene en verde.** Verificado en navegador: 24 llamadas a `/api/v1/hub/*` en las 6 pantallas, **todas 200**, consola sin errores; de paso se paró el **uvicorn zombi PID 7576** que llevaba desde el 2026-08-02 quedándose `127.0.0.1:8000` y falseaba las pruebas manuales |
| 2026-08-03 | CAL.1 (RED/GREEN) — Retirada de NiceGUI del árbol activo del servidor + Caso B en client_app | **Cursor: CAL.1 ✅ → CAL.2.** `server/app/ui/` eran **21 módulos NiceGUI** —panel de administración y portal de partner— que nadie importaba y que `main.py` no monta, o sea **inalcanzables por HTTP**. Lo único que los mantenía vivos eran tres tests que los importaban para probarlos. **Se mueven enteros a `_legacy_nicegui/server/app/ui/` (Caso A) y no repartidos entre borrado y cuarentena**, que es lo que el prompt dejaba a decisión por fichero: **9 de los 21 se importan entre sí** (comparten `admin_layout`, `partner_layout` y el JSON de traducciones), así que separar los que ya cubre el panel React de los que no habría dejado referencias rotas dentro de la propia cuarentena — y quien migre el portal de partner en Fase 2 los quiere completos o no los quiere. El borrado definitivo sigue siendo del usuario al cerrar la Fase 1, como manda CLAUDE.md. **Borrado directo (Caso B)**: los 3 tests del UI muerto, los 5 UI legacy de `client_app` (ninguno con importadores, comprobado) y **24 extractores generados que estaban trackeados** en las dos copias de `modules/extraccion` —los escribe el runtime, no son código fuente—, con reglas nuevas en `.gitignore` para que el siguiente arranque no los vuelva a colar, que es como llegaron la primera vez. 6 guardarraíles en `tests/infra/test_nicegui_retirado.py`, incluido uno que exige que la **cuarentena esté completa**: incompleta no sirve de referencia. **Desviación documentada**: el prompt mandaba borrar `tests/unit/test_admin_*_ui.py` (cuatro ficheros por nombre) y **no existen**; los que de verdad mantenían vivo el UI eran `test_partner_billing_ui.py`, `test_partner_layout.py` y `test_partner_scripts_ui.py`. Mismo propósito, otros nombres. Los `test_partner_*` de servicio (billing, clients, licenses, scripts) **se quedan**: prueban lógica viva, no interfaz. Fuera de alcance, como el prompt indica: los imports rotos de `workflow_engine`/`clarification_service`/`graphics_wizard` y el `report_factory` duplicado, que dependen de la decisión go/no-go de Categoría C. Suite completa **1682 passed, 1 skipped, 0 failed** |
| 2026-08-03 | FIX.3 — «Pongo la fecha y no la guarda»: dos fallos y un servidor caducado | **Cursor: sin cambio (bloque SEC completo).** El síntoma que reportó el usuario tenía **tres causas distintas**, y solo una era suya. **(1) El servidor seguía siendo el anterior al bloque**, comprobado sin ambigüedad: `/hub/usage/me` respondía 404 y un `PATCH` con `valid_until` devolvía **200 con la respuesta sin ninguno de los campos nuevos**, y en la BD el valor seguía en `None`. Un `ChatbotUpdate` que no conoce el campo lo descarta en silencio, así que «guardado» y «no se guardó nada» son la misma respuesta. **(2) Ese silencio es un defecto en sí mismo**, y se cierra: `ChatbotUpdate` pasa a `extra="forbid"`, así que el mismo caso responde **422 diciendo qué campo sobra** en vez de mentir con un 200. Es la diferencia entre un desajuste de versiones que se ve en el primer intento y uno que se persigue durante dos rondas de pruebas manuales. **(3) Y había un fallo real de SEC.4.1 que aún no había salido**: `model_dump(exclude_none=True)` descartaba `valid_until: null`, así que **vaciar la fecha para reabrir un chatbot caducado no funcionaba** —el paso (g) de las pruebas manuales habría fallado a continuación—. Se arregla mirando `model_fields_set`, que distingue «lo envió como null» de «no lo envió», distinción que `exclude_none` no puede ver; el criterio del resto de campos —donde `None` sí significa «heredar»— no se toca. **Verificado contra un servidor real con el código nuevo** (puerto 8013, apagado después): poner la fecha → 200 y `availability.state=expired`; conversar → **403 con el mensaje del admin**; vaciar la fecha → `available` otra vez; campo desconocido → **422** nombrándolo. La BD de desarrollo queda como estaba y el token de depuración, borrado. 3 tests nuevos. **Hallazgo colateral, ajeno al bloque**: al reabrir el chatbot, el chat da 500 porque su proveedor (`openai_compatible`) no tiene API key en el `.env` de desarrollo — anotado en el `.bat` para que no vuelva a parecer un fallo de SEC.4.1 |
| 2026-08-03 | FIX.2 — Los campos de vigencia faltaban en el formulario (cierre real de SEC.4.1) | **Cursor: sin cambio (bloque SEC completo).** Sale de las pruebas manuales del bloque, y **es un fallo mío de SEC.4.1**: implementé el badge de la lista y el contrato, pero **no los campos del formulario**, que el prompt pedía explícitamente. Resultado: la columna decía «Disponible», no había ningún sitio donde poner la fecha y la función era **inalcanzable desde la interfaz**. Peor, el `.bat` mandaba al usuario a editar un campo que no existía. Añadidos los cuatro campos al formulario (`valid_from`, `valid_until`, `total_token_budget`, `unavailable_message`) con i18n es/ca/en, más el estado derivado en el propio diálogo. **Dos detalles que se ven al usarlo y no al escribirlo**: un `<input type="datetime-local">` no entiende zona, así que la ISO del contrato se recorta a `YYYY-MM-DDTHH:mm` —sin recortar, el campo aparece vacío y **guardar borraría la fecha en silencio**—; y el campo vacío viaja como `null`, no como `''`, porque el backend distingue «sin ventana» de «con fecha» y `''` no es ninguna de las dos. 5 tests nuevos, uno por cada cosa que podía romperse en silencio; frontend completo **244 passed**. **El `.bat` se reescribió** por la otra mitad del problema: los pasos 2, 3 y 4 fallaron en las pruebas manuales porque **el servidor del usuario era anterior al bloque** —el proceso no tenía `/hub/usage/me`, que añadió SEC.4—, y el guion no lo detectaba, así que tres resultados engañosos parecían tres fallos. Ahora hay un **paso 0** que pide una ruta que solo existe desde SEC.4 y manda parar si responde 404. También se corrigió el paso 4, que se contradecía —pedía «400 o 404» y a la vez decía «debe ser 401»—: sin sesión todo responde 401 antes de llegar a la validación de ruta, así que la travesía se comprueba con los 15 tests automáticos y no a mano. **Verificado levantando dos servidores de verdad** (8011 en desarrollo, 8012 en producción, los dos apagados después): 10×401 y luego 429 en el login, las cuatro cabeceras presentes sin HSTS en desarrollo, 401 al leer un tema sin sesión, y en producción `/docs` y `/openapi.json` a 404 con HSTS presente. **Nota para quien lea el historial**: los 500 que aparecieron al probar el login en proceso con `TestClient` **no son un defecto**; son el motor asyncpg reutilizando conexiones de un event loop ya cerrado entre peticiones del TestClient. Contra un servidor real no ocurren |
| 2026-08-03 | SEC.7 (RED/GREEN) — Docs off en producción + cabeceras de seguridad [M5] | **Cursor: SEC.7 ✅ → BLOQUE SEC COMPLETO.** Dos cosas pequeñas que se notan el día de una auditoría y el día de un incidente. **La documentación interactiva se apaga en producción** (`docs_url`/`redoc_url`/`openapi_url` a `None`): no oculta ninguna vulnerabilidad, es que no hay razón para publicar el mapa entero de la API —rutas de administración incluidas— a quien pase por ahí. En desarrollo sigue siendo la herramienta más útil que hay, y ahí no se toca. **Cuatro cabeceras en un middleware**, no en cada endpoint, para que el endpoint que se escriba mañana las lleve sin que nadie se acuerde: `nosniff` (que el navegador no adivine el tipo de un fichero subido y acabe ejecutando como script algo guardado como texto), `X-Frame-Options: DENY` + `frame-ancestors 'none'` (clickjacking sobre el panel), `Referrer-Policy: strict-origin-when-cross-origin` (un enlace saliente desde un panel de administración se lleva identificadores en la ruta) y **HSTS solo en producción** —mandarlo en desarrollo obliga al navegador a exigir HTTPS en `localhost` durante un año y quien se lo coma no tiene forma evidente de deshacerlo—. El middleware usa `setdefault`: **si un endpoint ya puso su cabecera, gana el endpoint**, que es lo que permitirá al widget de D.1 declarar su propia `frame-ancestors` por organización sin tocar este módulo. La CSP es la mínima de un panel que sirve su propio JS; **no** se afina `script-src` con nonce, porque hacerlo sin poder probarlo en navegador sería romper la SPA a ciegas. 11 tests nuevos |
| 2026-08-03 | SEC.5 (RED/GREEN) — Temas: auth en la lectura y rutas que no salen del directorio [M2] | **Cursor: SEC.5 ✅ → SEC.7.** Dos agujeros en `hub_themes_router`. El primero es de manual: `_theme_path` concatenaba lo que llegara en la URL con el directorio de temas, así que con `../` se leía cualquier `.json` del servidor —y como `DELETE` usa la misma función, también se borraba—. **La validación es doble a propósito**: la forma del identificador (`^[a-z0-9-]{1,64}$`, que a los UUID reales no les aprieta) **y** el destino resuelto bajo `THEMES_DIR`. Comprobar solo la cadena es apostar a haber pensado en todas las codificaciones —`..%2f`, `....//`, rutas absolutas de Windows—; comprobar dónde acaba la ruta es no tener que acertar. El segundo es menos aparatoso y peor de explicar: `GET /hub/themes/{id}` era **público**, y un tema lleva los colores, el logotipo y el nombre de la organización a la que pertenece, o sea que servía un censo de clientes sin pedir nada. Ahora exige admin y pasa por `assert_org_access` cuando el tema es de una organización; los de plataforma los ve cualquier administrador porque son la base de la cascada y no son de nadie. **`/presets` sigue público**: son cuatro nombres de tema predefinido, no configuración de ningún cliente. 15 tests nuevos, ocho de ellos parametrizando formas de travesía |
| 2026-08-02 | SEC.4.1 (RED/GREEN) — Ventana de vigencia y presupuesto acumulado por chatbot | **Cursor: SEC.4.1 ✅ → SEC.5.** Un chatbot de campaña —plazo de matrícula, convocatoria, alegaciones— tiene que **caducar solo**; hasta aquí la única palanca era que alguien se acordara de apagarlo el día que tocaba. Cuatro campos de configuración en `HubChatbot` (`valid_from`, `valid_until`, `total_token_budget`, `unavailable_message`) y **ninguno de estado**: `expired` / `not_yet_open` / `budget_exhausted` / `available` se **calculan** en `core/chatbot_availability.py`. Las tres razones, y las tres se sostienen solas: un flag persistido se queda obsoleto en cuanto pasa la fecha y obligaría a inventar un job que lo refresque; el consumo acumulado es dato **operacional** y `HubChatbot` es configuración que se sincroniza al cloud, así que guardarlo ahí rompería la frontera; e `is_active` seguiría significando dos cosas en un booleano —«el admin lo apagó» y «se le pasó el plazo»—, que es justo lo que quien mira el panel necesita distinguir. **El 403 lleva motivo y el mensaje que escribió el admin**: «el plazo de matrícula terminó el 30 de septiembre» es una respuesta, «Forbidden» es un callejón; vacío viaja como `null` para que el frontend ponga su texto i18n. **Orden de las guardas, con test que lo fija**: acceso → disponibilidad → cuota. No es estético — contarle a alguien que el plazo se cerró es contarle que el trámite existe, y eso solo se le dice a quien podría usarlo. El presupuesto **lee el contador `window_key='total'`** que SEC.4 ya escribe (un techo acumulado que se reiniciara cada noche no sería un techo) y trata el `0` como «sin techo», mismo criterio que las cuotas. **`availability` viaja en `ChatbotRead` como objeto derivado y de solo lectura** —no está en `ChatbotCreate`— y el badge del panel solo lo pinta: comparar fechas en el cliente daría dos máquinas de estado, la del panel y la del chat, que se contradicen en cuanto una cambie. Badge + i18n es/ca/en en la lista de chatbots. **Migración `s6b7c8d9e0f1` aplicada**; los chatbots existentes quedan con los cuatro campos nulos/vacíos, o sea exactamente como estaban. 15 tests nuevos. **Deuda de mantenimiento saldada de paso**: los dobles `MagicMock(spec=HubChatbot)` de los tests de chat inventan un valor por cada columna nueva, y eso ya había roto los mismos cinco ficheros **tres veces seguidas** (SEC.2.1, SEC.4 y este). Ahora hay `server/tests/dobles.py::completar_chatbot`, que rellena el doble **leyendo los defaults del ORM** y respeta lo que el test haya declarado: la próxima columna no rompe nada. ⚠️ **Verificación en navegador NO realizada**: la extensión de Chrome pidió permiso para `localhost:5173` y **se denegó**, así que el badge queda para las pruebas manuales del cierre del bloque. El resto —contrato, i18n, `tsc` y los 24 tests de las dos páginas de admin— sí está comprobado |
| 2026-08-02 | SEC.4 (RED/GREEN) — Rate limiting + contabilidad de tokens + cuotas multi-sujeto [A5] | **Cursor: SEC.4 ✅ → SEC.4.1.** Tres piezas que se confunden con facilidad y son distintas: el **limitador cuenta peticiones**, la **contabilidad cuenta tokens** y la **cuota compara lo segundo con un límite**. Ninguna sustituye a las otras: una petición puede costar mil veces más que otra, y mil peticiones baratas tampoco son gratis. **Parte 1 — limitador** (`core/rate_limit.py`): ventana deslizante **en memoria del proceso y no Redis**, con el límite escrito en el módulo: con varias instancias el límite efectivo se multiplica por el número de instancias, y aun así son tres órdenes de magnitud menos que sin límite para lo que defiende —fuerza bruta contra un login que ya exige contraseña desde SEC.1—; además no arrastra una dependencia de infraestructura al edge, donde suele haber una instancia y ningún Redis. Se aplica al login **por IP** (allí todavía no se sabe quién llama, que es la gracia del ataque) y al chat **por actor efectivo**: contarlo por credencial haría que un cliente de confianza que atiende a cien personas se llevara el cupo de una sola. Respeta `X-Forwarded-For`, o detrás de un balanceador el primero que gaste su cupo deja fuera a todos. **Parte 2 — contabilidad**: `prompt_tokens`, `completion_tokens` y `cost_estimated` en `HubInteraction`, **nullable** porque lo histórico no tiene el dato y un cero diría «no gastó nada». El uso sale del `usage_metadata` que declara el proveedor (con respaldo en `response_metadata`, porque el proveedor se elige por configuración y no se sabe cuál responderá) y, si calla, **se estima y se marca** `usage_source='estimated'`: una cuota apoyada en una estimación silenciosa no se puede defender ante quien la sufre. Se escribe en el **mismo commit** que la interacción. **Parte 3 — cuotas** (`core/quotas.py`): cinco sujetos en cascada —usuario/día → usuario/mes → chatbot/día → organización/mes → IP/día—, `NULL` hereda y **`0` es sin límite** (si `0` fuera «bloqueado», quitar una restricción dejaría al chatbot mudo; hay test), y el 429 dice **qué** cuota se agotó con `Retry-After`. Contadores en `HubUsageCounter` (**operacional**, nunca en `HubChatbot`: un contador en una tabla de configuración se sincronizaría al cloud) con **UPSERT atómico** —probado con diez sumas concurrentes reales sobre Postgres, porque read-modify-write es el hueco por donde se salta una cuota—. Se comprueba **antes** de invocar y se contabiliza **después**: la petición que cruza el límite se desborda, y es decisión escrita —reservar exigiría estimar el coste de una respuesta que aún no existe—. Se cuenta **aunque no haya límite configurado**: sin histórico no hay forma de elegir el límite el día que haga falta. **`GET /hub/usage/me`** para que un 429 no sea indistinguible de una avería; pide `chatbot_id` porque no existe «la cuota del usuario» a secas, y aplica la misma frontera de acceso. **Desviación documentada**: `cost_estimated` se queda en NULL. La tabla de precios (`ModelPricing`, que alimenta OpenRouter) vive en la BD de plataforma y el chat es **edge**; convertir tokens en euros es asunto de facturación —cloud—, que ya tiene `calculate_cost`. La columna queda para cuando lo haga. **Migración `r5a6b7c8d9e0` aplicada** (`alembic current` = head): ninguna cuota se activa por migrar, todas las columnas entran nullable y sin default. 33 tests nuevos (`test_rate_limit.py` 9, `test_quotas.py` 24). **Nota de mantenimiento**: los dobles `MagicMock(spec=HubChatbot)` de los tests de chat inventan un valor por cada columna nueva, así que las tres cuotas hay que declararlas a `None` igual que el `access_mode` de SEC.2.1 — está comentado en los cinco ficheros |
| 2026-08-02 | SEC.3 (RED/GREEN) — CORS por entorno [A4] | **Cursor: SEC.3 ✅ → SEC.4.** `allow_origins=["*"]` estaba escrito a mano en `main.py` desde el primer día. Con `allow_credentials=False` no entrega cookies, así que no es el agujero de manual: lo que hace es **no decidir nada**, y en una API de administración pública eso se lee igual de mal en una auditoría que el día que aparezca un XSS en el panel, que guarda el JWT en `localStorage`. La política se muda a `core/cors.py` y `main.py` la consume. **Dos comportamientos, uno por entorno**: en producción **no hay comodín ni por omisión ni escrito** —un `*` en `CORS_ALLOWED_ORIGINS` se **descarta**, porque el fallo que esto evita es copiar el `.env` de desarrollo a producción, no que alguien lo teclee a conciencia—; fuera de producción el comodín se conserva, porque pelearse con CORS en local no protege de nada. **Decisión con su porqué: sin lista en producción la política queda vacía y NO se levanta una excepción.** CORS es una protección del navegador; dejar el panel con un error de CORS —visible y con nombre propio en la consola— es proporcionado, tumbar la API entera y con ella a los clientes que no son navegadores, no. Se avisa por log. **Métodos y cabeceras acotados** a los que se usan, con `X-GovGenAI-Actor` incluida: la introdujo SEC.2.1 y sin declararla el preflight tumbaría la identidad delegada en cuanto un cliente delegante hablara desde un navegador. **Desviación documentada**: el plan pedía `CORS_ALLOWED_ORIGINS` como campo de `Settings`; se queda como argumento de `politica_cors` + variable documentada en `.env.example`, porque `main.py` construye la app en el import y `get_settings()` revienta ahí si falta `JWT_SECRET_KEY` —y un campo en `Settings` que no lee nadie es código muerto—. 12 tests nuevos (`tests/api/test_cors.py`), uno de ellos de extremo a extremo: el navegador no recibe `access-control-allow-origin` para un origen no listado y sí para el listado. `generate_env.sh` emite la variable **vacía y visible** en producción, que es mejor que un comodín puesto por un script |
| 2026-08-02 | SEC.2.1 (RED/GREEN) — Modo de acceso por chatbot + identidad delegada | **Cursor: SEC.2.1 ✅ → SEC.3.** SEC.2 cerró el paso entre organizaciones; dentro de una, `is_active` decía si un chatbot funciona pero **nada decía para quién**, así que un chatbot de gestión interna era tan alcanzable como el público. **Parte 1**: `access_mode` (`public_anon`/`authenticated`/`restricted`) + `allowed_roles` + `allowed_saml_groups` en `HubChatbot`, y `assert_chatbot_access` como **único** sitio donde se decide. Dos reglas que no se leen solas y tienen test cada una: **`via` manda sobre la identidad** —una API key de widget identifica un *sitio*, no a una persona, así que no abre un chatbot `authenticated` ni acompañada de un superadmin, porque el comodín es de identidad y por ese canal no llega ninguna—, y **`restricted` con las dos listas vacías es «nadie», no «todos»**, que es la lectura que convierte un descuido de configuración en un chatbot abierto. Un modo desconocido tampoco se interpreta: se cierra. **Sin tabla de grants**, dos ARRAY: el atributo de grupo ya llega en el ACS y una tabla hoy sería infraestructura sin usuario. **Parte 2**: cabecera `X-GovGenAI-Actor` (JWT HS256, `aud=govgenai-backend`, ventana ≤300 s) y `EffectiveActor`, porque el Pipe de OWUI usa **un PAT de servicio** y sin esto todas las conversaciones quedarían atribuidas a una cuenta y la cuota por usuario de SEC.4 no podría existir. Tres reglas: **sin el scope `chat:onbehalf` la cabecera se ignora en silencio** —un PAT robado que no lo lleve no suplanta a nadie—, **las organizaciones y el rol salen del PAT y jamás de la cabecera** (los grupos sí, que es lo que el IdP del cliente sabe y el backend no), y **ventana corta**, porque una cabecera de horas es un PAT encubierto que nadie puede revocar. HS256 y no RS256 con la razón escrita: hoy hay un solo cliente delegante. **Encargo heredado de SEC.2, cerrado**: `HubSsoUser.organizacion_id` sale de `SAML_ORGANIZACION_ID` y **nunca de la aserción** —si viniera de fuera, quien controla el IdP decidiría a qué organización, y por tanto a qué corpus, llega cada persona—; el login SAML de un Admin recupera además las organizaciones de su cuenta, porque entrar por el IdP no puede dar menos permisos que entrar por el formulario. **Desviación documentada**: un `SAML_ORGANIZACION_ID` que no exista **no tira el login** contra la clave ajena; se avisa por log y la persona entra sin organización —caerse al entrar es peor que no dar acceso—. **Migración `q4z5a6b7c8d9` aplicada** (`alembic current` = `q4z5a6b7c8d9 (head)`), fail-closed por partida doble: todos los chatbots existentes a `authenticated` —nunca `public_anon`, que publicaría el corpus sin que nadie lo decidiera— y los usuarios SSO sin organización hasta que alguien la asigne. **Verificada ejecutándola**, no leyéndola: dos tests nuevos en `tests/infra/test_migrations_fresh_install.py` levantan una BD con `alembic upgrade head`, insertan un chatbot sin declarar modo (sale `authenticated`) y comprueban que la tabla rechaza uno desconocido; los tests que sí leen el fichero cubren lo único que allí no se ve —que no hay `UPDATE` de backfill—, porque una instalación limpia no tiene filas previas. **31 tests nuevos** de autorización + 4 de SAML + 2 de migración; el paso de CI pasa a llamarse *Access control gate* y suma los dos ficheros nuevos (70 tests, verde en local como unidad). Suite completa **1583 passed, 1 skipped, 0 failed**. Contrato OpenAPI reexportado y Orval regenerado —ambos artefactos están en `.gitignore`, así que no aparecen en el commit—; `tsc` limpio salvo dos errores preexistentes ajenos (`rol2_rename.test.tsx`, tipado i18n; `TestScenariosPage.tsx`, variable sin usar) |
| 2026-08-02 | SEC.2 (RED/GREEN) — Aislamiento multi-tenant: claim de organización + filtrado + gate de CI [A2] | **Cursor: SEC.2 ✅ → SEC.2.1.** Cierra el trabajo que quedó a medias en el traspaso anterior. **Los 6 rojos pendientes eran dobles sin organización, ninguno un defecto de producción**, y los tres arreglos fueron los que el traspaso predecía: la fixture `setup_chatbot` de los E2E creaba la organización con un uuid aleatorio mientras `auth_headers` emitía el token sin claim (ahora ambas comparten `ORG_PRUEBA`, fijada en el `conftest` de e2e); el doble de sesión de `test_job_progress` necesitaba `AsyncMock` en `session.get`, que es lo que la guarda de ingesta espera; y `test_reembed` pedía crear chatbots en organizaciones aleatorias. Suite completa **1536 passed, 1 skipped, 0 failed** (7:59, Git Bash). **Dos cosas más, que el traspaso dejaba abiertas.** (1) **El gate no probaba ningún endpoint**: sus 16 tests medían la capa de tenencia y dos comprobaciones estructurales, así que un router que dejara de llamar a `assert_org_access` seguía en verde —y el hallazgo A2 nunca fue que la regla estuviera mal escrita, sino que no estaba en el camino—. Añadidos **10 tests de comportamiento HTTP** (listado acotado en SQL, 403 en get/update/delete, feedback, chat, ingesta, temas con organización ajena, tema de plataforma sin superadmin, y el comodín del superadmin visto en la respuesta): **26/26**, y **verificado por mutación** que se ponen en rojo al quitar la guarda —comprobado sobre `hub_feedback`, que además es el único router de la lista que el guardarraíl estructural no cubre, porque vive en `api/v1/` y no en `routers/`—. (2) **La duda del frontend está resuelta y no había problema**: ninguna pantalla consume `/api/v1/hub/themes`; el cliente generado por Orval existe y **no lo importa nadie**, y el `ThemeEditor` trabaja contra `/api/themes/*`, que es otra superficie. Ningún admin va a empezar a recibir 403 al crear un tema. **Dos desviaciones documentadas.** (a) En temas, el plan pedía *derivar* la organización del principal e ignorar la del cuerpo; se **valida** la del cuerpo contra el token. Un admin puede gestionar varias organizaciones, así que derivarla obliga a elegir una por él en silencio, y a elegir mal en cuanto haya dos: mismo cierre, sin adivinar la intención. (b) En `create_chatbot`, `assert_org_access` **se adelanta** a la comprobación de embeddings: quien no gestiona la organización recibía antes un 503 que le contaba el estado del servicio de embeddings de la instalación. Fuera del alcance literal, pero es el mismo defecto de orden que el prompt persigue. Nota de entorno, no del código: importar `hub_chatbots_router` **a mitad** de una sesión de pytest mata el proceso en Windows (`access violation` al cargar pyarrow por la cadena chunker→sentence_transformers); importarlo en la recolección lo evita, y así está en el gate con su comentario |
| 2026-08-02 | SEC.1 (RED/GREEN) — Contraseña obligatoria en el login de Admin [A1] | **Cursor: SEC.1 ✅ → SEC.2.** `/auth/admin/login` emitía un JWT comprobando solo que el email existiera y la cuenta estuviera activa. **No era un descuido de una rama: `adminaccount` no tenía columna de hash**, así que no había contra qué comparar — cualquiera que conociera un correo de administrador entraba con la contraseña que quisiera. Se detectó de paso al arreglar el login el mismo día, y el propio código lo documentaba («la verificación la añade SEC.1»). **Tres decisiones, y las dos últimas son las que evitan reabrir el agujero por otro sitio**: (1) verificación con `verify_password`, mismo patrón que superadmin; (2) **NULL es «login local deshabilitado»**, no «pasa sin comprobar» — el backfill deja NULL en las cuentas existentes y hay test de que no entran; (3) **el error es idéntico en los tres casos** —cuenta inexistente, sin hash, contraseña mala— y **siempre se paga un bcrypt**, contra un `_HASH_SENUELO` calculado al importar, porque responder antes cuando el correo no existe convierte el login en un oráculo de enumeración medido con cronómetro. **Contrapartida obligatoria del backfill**: `PATCH /auth/admins/{partner_id}/password`, solo superadmin (403 para admin, incluso sobre su propia cuenta: cambiar la propia exige conocer la anterior y ese flujo no es este prompt). Sin ese endpoint, arreglar A1 habría dejado las cuentas existentes sin forma de entrar. **Migración `p3y4z5a6b7c8`** aplicada, `alembic current` en head: columna **nullable y sin default**, deliberadamente. No se inventa una contraseña de oficio ni se deriva del email —sería cambiar un agujero conocido por otro más difícil de ver— y no se pone NOT NULL, que obligaría a rellenar algo para cada fila y es lo mismo con otro nombre. **Efecto en el entorno de desarrollo, a tener en cuenta**: `dev@automatia.local` se queda con hash NULL, o sea que **ya no entra por el login local** — que es exactamente el arreglo. **Un fallo mío en el RED**: los tests sobreescribían `get_async_session` y este router usa `get_session`; el síntoma era un 500 que no decía nada del comportamiento probado. **Tests**: 13 nuevos (RED primero: 10 fallos). `tests/api` + `tests/core` + higiene: **137 passed**. Contrato (119 paths) y Orval regenerados, `tsc` limpio. |
| 2026-08-02 | FIX.1 (RED/GREEN) — El modelo de un chatbot se puede cambiar, y los fallos se ven | **Cursor: FIX.1 ✅ → SEC.1.** Sale de las pruebas manuales del Bloque RAG, que el usuario no pudo completar. **Un síntoma —«el botón de ejecutar no hace nada»— tapaba cuatro fallos, y el que el usuario sospechaba no era el que impedía verlo.** (1) `gemini-2.0-flash` **está retirado**: Google responde `404 NOT_FOUND: "This model models/gemini-2.0-flash is no longer available"`. Confirmado con el endpoint de test. (2) **El fallo era invisible**: `POST …/run` devuelve 500 cuando el modelo no responde y la mutación no tenía `onError`. Y en el chatbot demo ni siquiera llegaba el 500, porque **sin corpus el grafo corta antes de llamar al LLM** y devuelve el fallback: el modelo roto quedaba tapado por un corpus vacío. Se reprodujo el 500 con el chatbot de Ollama, que no está levantado. (3) **No había forma de cambiar el modelo**: el formulario hardcodeaba `llm_config_id: DEV_LLM_ID` y **`ChatbotUpdate` ni declaraba el campo**, así que hubo que repuntar el chatbot por SQL. (4) **Marcar por defecto respondía 409** en vez de relevar a la anterior, y en la BD habían quedado **dos configuraciones tier 1 por defecto** conviviendo. **El riesgo latente era el peor de los cuatro y no lo había visto nadie**: mientras el formulario mandara la constante, **editar cualquier chatbot le reasignaba el modelo en silencio** —el «Chatbot de Ejemplo» usa Ollama a propósito—. Tiene test propio (`should_not_reassign_the_model_when_editing_without_touching_it`). **Dato de producto que el diagnóstico destapó**: el modelo que el usuario había dado de alta, `gemini-2.5-flash-preview-tts`, es de **texto a voz**; habría cambiado un modelo roto por otro que tampoco contesta texto. Por eso el desplegable enseña **siempre el `model_name` exacto** aunque haya etiqueta: es lo que distingue `2.0` de `2.5` y lo que delata un `-tts`. **Cambios**: `llm_config_id` en `ChatbotUpdate` con validación 404 —mejor eso que un IntegrityError a mitad de petición—; `_relevar_default()` demota en la misma transacción, en POST y en PATCH; selector de modelo y de organización en el formulario, con la del chatbot preseleccionada al editar; `onError` visible en ejecutar, crear y borrar escenarios; botón de **editar escenario**, que usa el `PATCH` que existía desde RAG.13 y no llamaba nadie. **Un test ajeno estaba mal y se arregló en vez de acomodarlo**: `ChatbotsPage.test.tsx` cogía los desplegables por índice (`getAllByRole('combobox')[0]`), así que añadir un campo lo rompía; ahora van por etiqueta, y de paso los `<label>` del formulario tienen `htmlFor`/`id` —que es accesibilidad, no solo comodidad del test—. **Ejecución real**: promover `gemini-2.5-flash` por API devuelve **200** y deja **una sola** configuración por defecto (antes, 409). El chatbot demo ya responde 201 al ejecutar un escenario. **Tests**: 6 backend + 4 frontend nuevos, 2 añadidos al fichero de escenarios (RED primero: 5 y 4 fallos). Backend `tests/api` + higiene **67 passed**; frontend **239 passed** en la segunda vuelta. **La primera vuelta dio 2 rojos que NO son míos**: `ChatbotForm` y `AccessTokensPage` fallan solo en suite completa, pasan en aislamiento y no reaparecen al repetir — es la contaminación entre ficheros ya inventariada al arreglar el login, y el frontend sigue sin los guardarraíles que TST.1 puso en el backend. `tsc` limpio, contrato y Orval regenerados. **Sin verificación en navegador**: la extensión de Chrome pidió permiso y se denegó; queda `pruebas_manuales_promptFIX1.bat`. **Sin migración.** |
| 2026-08-02 | SYNC.2 (RED/GREEN) — Caducidad activa del corpus (**cierra el Bloque SYNC**) | **Cursor: SYNC.2 ✅. BLOQUE SYNC COMPLETO → SEC.1.** El sync solo detecta lo que **cambia en origen**, y una norma que nadie toca en tres años no emite ninguna señal — siendo la vigencia el riesgo nº1 del informe (312 de 314 fichas dicen «vigent?»). Este detector convierte el paso del tiempo en señal: lee `data_revisio_prevista` (columna de ING.0.2) y emite un `HubContentFinding` de tipo **`revisio_vencuda`** colgado del chatbot, con la fecha prevista, la última actualización y los días de retraso. **Es el más simple de los tres detectores y a propósito**: compara fechas en SQL, sin embeddings ni LLM, y por eso su `confidence` es 1,0 —no estima nada—. Lo que aporta no es inteligencia, es que la pregunta se haga alguna vez. **La decisión que no era obvia: dónde vive el default de un año.** Va en `_aplicar` del reconciliador, pero **solo si el documento no tiene ya fecha**; y `_difiere` ignora el campo cuando la entrada no lo declara. Sin las dos mitades, cada pasada del sync habría visto una diferencia, la habría «corregido» renovando la fecha y **nada habría vencido jamás** — un fallo perfectamente silencioso: el detector seguiría corriendo y no encontraría nunca nada, que se parece muchísimo a tener el corpus al día. Hay test específico (`should_not_push_the_default_forward_on_every_run`). **Dos exclusiones deliberadas**: los documentos con `us_assistents='no'` no caducan (avisar de algo que el asistente no va a leer es llenar la cola de trabajo inútil), y un hallazgo **cerrado no bloquea uno nuevo** —mismo criterio que RAG.14: si sigue vencida hay que volver a decirlo, o `resolved` se convierte en un silenciador permanente—. **Severidad graduada**: `warning` hasta 180 días de retraso, `critical` a partir de ahí; una semana y tres años no son el mismo problema. **Superficie**: CLI `quality.detect_stale --chatbot-id [--dry-run]` + `POST /hub/quality/stale/analyze` y `GET /hub/quality/stale`, síncronos porque es una comparación de fechas y devolver un 202 obligaría a consultar algo que ya se sabe al terminar. **Sin scheduler**, igual que RAG.14 y SYNC.1: cada cuánto se revisa llena una cola que alguien tiene que atender, y eso es decisión operativa. **Ejecución real** contra el chatbot demo con datos sembrados: `REG-901` (prevista 2019-06-30) sale `[critical] 2590 dias de retard` y `REG-902` (2027-12-31) no sale; tres pasadas seguidas dejan **una sola fila**. Endpoints verificados con el backend recargado: **401 sin sesión** los dos. Datos de prueba borrados de la BD de desarrollo. **Tests**: 12 nuevos (RED primero: 11 fallos). **Suite completa desde Git Bash: 1500 passed, 1 skipped, 0 failed.** Contrato OpenAPI (118 paths) y Orval regenerados, `tsc` limpio. **Sin migración**: la columna es de ING.0.2 y el tipo de finding es un `Literal` del contrato Pydantic, no un CHECK de Postgres. |
| 2026-08-02 | SYNC.1 (RED/GREEN) — Fuente MCP de publicación sobre el reconciliador de ING.0.5 | **Cursor: SYNC.1 ✅ → SYNC.2.** Segunda implementación de `CorpusSource`: `PublicationMcpClient` (JSON-RPC sobre `execute_dataset`) + `PublicationMcpSource` + CLI `corpus.sync`. **Esto es ETL y no una operación agéntica**, y la distinción tiene consecuencia de seguridad: el token del dataset viaja como **argumento de la tool**, así que una llamada desde un agente con trazas activas lo dejaría escrito en el almacén de trazas. Hay dos tests que lo vigilan —`caplog` sin el token, y un espía sobre `model_factory` que revienta si alguien construye un modelo—, y `_redactado()` sustituye el token antes de cualquier volcado de depuración. **La decisión de diseño del prompt, y no estaba resuelta en el plan**: el criterio pedía «pedir el contenido solo para las normas cuyo hash cambió», pero el reconciliador de ING.0.5 **calcula el hash a partir del cuerpo**, o sea que para saber si algo cambió tenía que descargarlo todo. Se resuelve con `CorpusDocumentEntry.content_hash`, **transporte y no metadato**: la fuente lo declara si puede hacerlo sin entregar el cuerpo —el índice del servicio lo trae— y el reconciliador salta la descarga cuando coincide con el hash guardado. Es un solo punto tocado del reconciliador; emparejamiento, deltas, poda y salvaguarda siguen intactos. **El atajo es seguro por construcción**: compara el hash declarado contra el NUESTRO, calculado con `hash_markdown_body`, así que un publicador que hashee de otra forma simplemente no acierta nunca y se descarga el cuerpo. Se pierde el ahorro, no la corrección, y hay test. **La pieza que convierte «no se reimplementa la reconciliación» de promesa en hecho**: la suite entera de ING.0.5 se parametriza por fuente con una fixture autouse, así que sus 21 tests corren **dos veces**, contra la carpeta local y contra un servicio MCP falso que sirve los mismos `.md` por JSON-RPC — **42 passed**. Y como el fake declara el hash, el atajo queda cubierto por toda la suite y no solo por sus tests propios. **Guardarraíl de texto** (estilo `tests/infra`): falla si `publication_source.py` o `sync.py` llegan a nombrar el modelo ORM de documentos o a escribir el motivo de retirada; duplicar el emparejamiento no lo detectaría ningún test funcional, porque los dos caminos darían el mismo resultado hasta el día en que dejaran de darlo. Cazó mi propio docstring en la primera pasada, y la primera versión era **demasiado gruesa**: prohibía `prune_threshold`, que la CLI se limita a pasar al reconciliador. **Ejecución real, con servicio MCP falso pero fiel** (JSON-RPC de verdad por HTTP, hash calculado con la misma función del hub) contra el chatbot demo: pasada 1 `ingeridos=3`; pasada 2 sin cambios en origen `omitidos=3` y **cero llamadas al dataset de contenido** en el log del servicio. Las 3 normas de prueba se han borrado de la BD de desarrollo después. **Observación anotada, no corregida**: la CLI pide el índice dos veces por pasada —una para el recuento y la validación de vocabulario, otra dentro de `reconcile`—; el índice es el dataset pequeño y `load.py` hace lo mismo contra disco, así que no se optimiza sin medir. **`--delta`** declara índice parcial ⇒ `is_census()` False ⇒ el reconciliador no poda. **Config**: cuatro variables en `.env.example` y `scripts/generate_env.sh`, vacías a propósito; el token va por Secret Manager en D.2. **Tests**: 16 nuevos + 21 reparametrizados. `tests/modules/agents_hub/ingestion` + reconciliador + higiene: **64 passed**. **`ruff check app` limpio del todo**: de paso se retiró el `F841` preexistente (`inicio` sin usar en `reconciler.py`), que este proyecto arrastraba como excepción conocida. **Sin migración**: `content_hash` es campo del contrato en memoria, no columna. |
| 2026-08-02 | Fix — El login apuntaba a una ruta inexistente + ojo para ver la contraseña | **No es un prompt del plan: es un fallo encontrado durante las pruebas manuales del bloque RAG.** El usuario no podía entrar y el síntoma acusaba a la contraseña. **No era la contraseña, ni la BD, ni faltaba migración**: `LoginPage.tsx` llamaba a `POST /api/v1/auth/token/admin`, que **no existe**. Las rutas reales son `/api/v1/auth/superadmin/login` y `/api/v1/auth/admin/login`. El 404 caía en el `catch` genérico y pintaba «Credenciales incorrectas», o sea que **el login estaba roto para cualquier contraseña** y el mensaje de error señalaba al usuario en vez de al código. **Lleva así desde el 2026-04-24** (`59f97d6`, el commit que creó la página) y no lo cazó nadie porque **el envío del formulario no tenía ni un test**: los dos ficheros que tocaban `LoginPage` cubrían el botón SSO y accesibilidad, ninguno el submit. **La causa de fondo es la deuda de CAL.2**: la página usa un `fetch` a mano en vez de la capa generada por Orval, y una URL escrita a mano puede divergir del contrato sin que nada falle. **El arreglo cierra esa vía**: las rutas se toman de `getLoginSuperadminApiV1AuthSuperadminLoginPostUrl()` y `getLoginAdminApiV1AuthAdminLoginPostUrl()`, generadas desde `openapi.json`. No se migró el transporte a Orval —usa axios y habría arrastrado el cambio a los tests—; eso sigue siendo CAL.2. **Decisión de comportamiento**: un solo formulario sirve a dos tablas de cuentas (`superadminaccount`, `adminaccount`), así que se prueban las dos puertas en orden; un 401 del SuperAdmin no significa «contraseña mala» sino «no es de los tuyos». Unificarlo en un endpoint que resuelva el tipo de cuenta es trabajo de **SEC.1**, que además añadirá la verificación de contraseña que hoy le falta a `/auth/admin/login`. **Segundo encargo, el ojo de la contraseña**: botón de mostrar/ocultar con `aria-pressed`, `aria-controls` y `aria-label` que cambia con el estado, iconos SVG en línea (no había librería de iconos en esta pantalla y traerla por dos glifos no sale a cuenta) e i18n en es/ca/en. **`type="button"` no es cosmético y tiene test**: dentro de un `<form>`, un `<button>` sin `type` es `submit`, así que el ojo habría enviado el formulario con la contraseña a medio escribir. **`ca/admin.json` no tenía bloque `login`** —deuda conocida de CAL.4—; se añaden solo las dos claves nuevas sin invadir su alcance. **Tests**: 9 nuevos (RED primero: 6 fallos). Frontend completo **232/233**, `tsc` limpio. **El único rojo es preexistente y está verificado como tal**: `ContentQuality.test.tsx::SitesPage muestra la lista de sitios` falla solo en suite completa —en aislamiento pasa— y falla igual con el árbol sin mis cambios (223/224). Es contaminación entre ficheros, la misma familia que TST.1 cazó en el backend; **el frontend no tiene esos guardarraíles**. Anotado como deuda. **Sin verificación en navegador**: la extensión de Chrome pidió permiso de sitio y se denegó, así que la revisión visual queda para el usuario. **Sin migración.** |
| 2026-08-02 | Decisión — La curación del corpus es previa al asistente (Bloque CUR planificado, alcance de 9Q reescrito) | **No es un prompt: es una decisión de producto y su planificación.** `docs/DECISION_CURACION_SEPARADA.md`. **La hipótesis que se cae**: el diseño asumía ingesta más o menos automatizada de información pública —apuntar el spider, ingerir, arreglar la calidad después con detectores—. Preparar el corpus normativo la ha falsado. Ese corpus era **el mejor caso posible** (PDFs de normas con filtro previo de Secretaría General, catálogo, numeración estable) y aun así exigió front-matter derivado del catálogo, decisión humana de consolidación norma a norma, un panel de revisión construido para eso y un parche al generador de HTML para tener anclas por artículo. Si el mejor caso exige eso, un conjunto de páginas rastreadas —sin versión canónica, sin distinción vigente/derogado— **no es un corpus: es materia prima**. **La decisión**: la curación es actividad **previa, humana y con producto propio**; el asistente consume el resultado y no lo produce. El crawler y el detector de incoherencias son herramientas **de curación**, no de ingesta. La frontera entre las dos mitades es **`docs/CONTRATO_MD_CORPUS.md`**, no una interfaz de código. **No hay ingesta automática de web a corpus**: una página nueva es señal para el curador. **Lo que se descartó, y por qué**: partir en dos repos/despliegues duplicaría AUTH (SAML+PAT), tenancy, `StorageService`, CI y Orval, obligaría a publicar librería interna con su versionado, y —lo decisivo— **rompería el bucle de RAG.14**, que es la única fuente sostenible de qué le falta al corpus; ese bucle ya está modelado en el `CHECK` de sujeto único de `HubContentFinding`. Si algún día la curación la opera otra unidad, es un perfil más de `DEPLOY_MODE`, no un fork. **El código ya daba la razón a la tesis**: `HubWebSite` cuelga de `organizacion_id` y **no tiene `chatbot_id`** («propiedad de la organización, no del chatbot», dice su docstring), y las herramientas del corpus ya viven fuera del repo hablando por contrato. **Alcance de 9Q reescrito** en el plan y en su fila: el código no cambia, cambia de qué es parte —motor de la herramienta de curación, con el informe de auditoría como salida principal (vale sin chatbot: auditar coherencia de lo publicado es transparencia) y la higiene del retriever como efecto secundario—. **Bloque CUR planificado**: CUR.1 mueve `ingestion/quality/` + spiders a `modules/curation/` con guardarraíles de import en las dos direcciones —el `gap_detector` **se queda** en agents_hub, porque nace de conversaciones y es la señal que el asistente *emite*—; CUR.2 le da superficie propia en el frontend y saca `CorpusSelectionService` a primer plano como **paso de publicación**. Ninguno reescribe comportamiento ni toca el esquema. **Corrección de mi propia recomendación de orden**: había propuesto hacerlo post-Deploy y el usuario objetó, con razón. Los artefactos de despliegue **codifican la estructura**: D.4 fija imágenes y servicios de Cloud Run y D.5 el pipeline, así que decidir la clasificación de módulos después obliga a rehacerlos y a repetir la verificación contra producción. Un refactor pre-deploy solo arriesga en local y lo cubren los tests. **Orden vigente: SYNC → SEC → CAL → CUR → Deploy**, con CUR detrás de CAL.1 (que retira NiceGUI y deja menos superficie que mover). Sin cambios de código en esta entrada. |
| 2026-08-02 | DET.1 (RED/GREEN) — Desempate determinista en las dos ramas del híbrido (**Bloque DET completo**) | **Cursor: DET.1 ✅ → SYNC.1.** Sale de la deuda cuadrada horas antes: el gate se movió de 0,920 a 0,940 sin que nadie tocara el ranking, y la causa era un **empate exacto de coseno** entre dos fragmentos de documentos distintos con un `ORDER BY` que ordena **solo por puntuación**. **El motivo no es el gate, es producción**: cuando dos fragmentos empatan, decidía el planificador **qué norma se cita**, y en un asistente normativo la evidencia mostrada no es presentación, es la respuesta. **La restricción que manda sobre el diseño, medida antes de escribir el prompt**: un desempate en el `ORDER BY` de la rama vectorial **inutiliza el índice HNSW** —pgvector solo lo sirve para `ORDER BY <distancia>` a secas—; con `enable_seqscan=off` el plan cae a `Seq Scan + Sort` porque no tiene alternativa, o sea que la solución evidente deshace RAG.3 y devuelve cada consulta a recorrer todos los chunks del chatbot. De ahí el reparto: **rama léxica en SQL** (`order_by(rank.desc(), content_hash)`, donde el GIN sirve el WHERE y no el ORDER BY, así que ese Sort ya se pagaba y la segunda clave es gratis) y **rama vectorial en Python, después del LIMIT** (unas 20-30 filas: coste nulo, SQL intacto). **La clave es `content_hash` y no `id`**, y esto es la decisión de diseño del prompt: `id` es un uuid4, determinista dentro de una ingesta y **arbitrario entre reingestas**, así que habría rebarajado los empates en cada recarga del corpus y tendríamos el mismo problema con otra cara; `content_hash` se deriva del contenido del fragmento y es estable entre reingestas y entre máquinas. **Residuo aceptado y documentado en el docstring**: si un empate cae justo en el borde del LIMIT, *qué filas vuelven* sigue dependiendo del plan; lo que ya no depende es el orden de las que vuelven. Con BGE-M3 es teórico —empates exactos entre floats de 1024 dimensiones no ocurren—; aparece con embeddings deterministas. No se arregla porque arreglarlo cuesta el índice. **Un test es un guardarraíl y no una comprobación de comportamiento**: `should_keep_hnsw_usable_by_the_vector_branch` hace `EXPLAIN` y falla si alguien mueve el desempate al SQL vectorial, que es el error que nadie detectaría de otro modo —ningún test funcional se entera de que el plan dejó de usar el índice—. **Bug en mi propio fixture, encontrado por el RED**: los vectores «lejanos» eran `[0.9]*1024` frente a `[0.1]*1024`, que **son el mismo vector** para el coseno —ignora la magnitud—, así que los tres fragmentos empataban a 1,0; sustituidos por ortogonales de verdad. **Baseline regenerada después del cambio**: sigue en **0,940**, pero ahora por desempate estable y no por suerte, y la `nota` del fichero deja de advertir de la oscilación. **Tests**: 5 nuevos (RED primero: 2 fallos, los dos de empate; los otros 3 son guardarraíles que ya pasaban). **`tests/modules/agents_hub` + `tests/public_graphs` + guardarraíles: 935 passed, 0 failed**; `ruff` limpio. **Sin migración**: no hay cambio de esquema. |
| 2026-08-02 | Deuda del Bloque RAG — Baseline del gate regenerada y los 0,02 cuadrados | **No es un prompt del plan: es la deuda anotada al cerrar RAG.14.** **(1) Baseline regenerada** a MRR **0,940** · recall@5 **0,960** · recall@10 **0,960**, sustituyendo al 0,8613 de RAG.1. Con la baseline vieja, un cambio futuro podía devolver entera la mejora de RAG.7 (+0,059) y pasar dentro de la tolerancia de 0,02; ahora no. **El `--update-baseline` del CLI no servía para este fichero y por eso no se usó**: `run_golden.py` mide BGE-M3 contra el corpus real, y escribir esas cifras en la baseline del gate —que mide el mecanismo con embedding determinista sobre el corpus de fixture— dejaría el gate comparando dos regímenes distintos. La regeneración deliberada se hace ahora **en el régimen del gate**, con `GOLDEN_UPDATE_BASELINE=1`, documentado en el docstring del módulo y con el mismo criterio de siempre: nunca en CI, porque una baseline que se regenera sola no detecta nada. **(2) Los 0,02, cuadrados por medición.** Son **una consulta**: «qui tutoritza les pràctiques externes?» pasa de rango 2 a rango 1, y 0,5/25 = 0,02 exacto. La aritmética completa: 0,920 = 22 aciertos en rango 1 + 2 en rango 2 + 1 fallo; 0,940 = 23 + 1 + 1. Medido ejecutando el mismo dorado en un worktree sobre el commit de RAG.8 (`b25680e`) y comparándolo consulta a consulta con HEAD: **52 chunks en los dos, mismas similitudes, y la única diferencia es esa consulta**. **La hipótesis viva de HNSW queda refutada**: `EXPLAIN ANALYZE` enseña `Limit → Sort (top-N heapsort) → Index Scan` por `chatbot_id`; con 52 filas el planificador no toca el índice vectorial, así que la aproximación de HNSW no interviene y el orden es exacto. **La causa real es un empate**: los dos primeros candidatos de esa consulta tienen el **mismo coseno hasta el último decimal** (0,316227773316) y son de documentos distintos, y `vector_search`/`keyword_search` ordenan solo por puntuación —`order_by(similarity.desc())`, sin desempate—, con un top-N heapsort que además no es estable. Quién queda primero lo decide el plan, no el retriever. **Controles ejecutados, y los tres descartan lo que parecía obvio**: quitar el índice `ix_hub_document_chunks_embedding_space` de RAG.9 → sigue 0,940; forzar `seqscan` → sigue 0,940; reescribir todas las filas para deshacer el ensanchamiento por `embedding_text` → sigue 0,940. O sea que el índice de RAG.9 no es la causa (lo que confirma lo que ya se anotó en RAG.10) y la anchura de fila tampoco. **Consecuencia que hay que tener presente**: si el empate se rompe al revés en otra máquina o con otro plan, el gate medirá 0,920, que es **exactamente** el borde de la tolerancia (0,940 − 0,02) y por tanto pasa, pero con margen cero. Queda escrito en la `nota` del propio fichero de baseline. **La solución de fondo —un desempate determinista en el `ORDER BY` de las dos ramas— NO se ha aplicado**: es un cambio en el retriever de producción, no en la baseline, y excede lo pedido. Propuesto para decidir. **Tests**: `tests/modules/agents_hub/evaluation` + `tests/infra/test_suite_hygiene.py` → **39 passed**, con el gate en verde contra la baseline nueva. Sin migración. |
| 2026-08-02 | RAG.14 (RED/GREEN) — Huecos de corpus desde señales de fallo (**cierra el Bloque RAG**) | **Cursor: RAG.14 ✅. BLOQUE RAG COMPLETO.** La señal ya existía y no costaba nada recogerla: RAG.2 persiste `fallback_reason` cada vez que el chatbot no encuentra con qué responder, y el feedback bajo lleva ahí desde 9Q; **faltaba leerlas juntas**. Una pregunta sin respuesta es ruido; veinte parecidas son *un documento que falta*, y eso sí es accionable. **La extensión del contrato 9Q es la parte con riesgo**: hasta ahora todo hallazgo colgaba de un sitio auditado, y un hueco nace de conversaciones —forzarle un sitio sería inventarle un sitio web a una pregunta—. `site_id` pasa a nullable **y entra a la vez un CHECK que exige exactamente uno de los dos sujetos**: sin él, «nullable» se leería como «opcional» y habría hallazgos sin sujeto, que no se pueden revisar ni atribuir; el mismo invariante se duplica en el validador Pydantic porque el contrato lo cruzan las dos capas. **`chatbot_id` va sin FK a propósito**: `hub_chatbots` es configuración (cloud) y esta tabla es operacional (edge). **Clustering aglomerativo por umbral, no k-means**: no se sabe cuántos huecos hay —es lo que se busca— y k-means exige decidirlo de antemano; se compara contra el **centroide** del grupo, no contra su primer miembro. **La deduplicación también es por centroide y no por texto**: el detector corre sobre ventana móvil, así que sin ella cada ejecución añadiría una fila del mismo problema; un hallazgo ya `resolved`/`dismissed` **no** bloquea uno nuevo, porque un hueco que reaparece es información. **Lo que se añadió sobre el plan, y sin ello el prompt no vale nada: la COLA.** El criterio decía «los findings aparecen en la cola de revisión 9Q existente», pero ese endpoint **filtra por sitio**, así que un hueco (`site_id` NULL) no salía en ninguna pantalla; se detectó comparando el criterio con el código real. Endpoint de listado por chatbot + panel en la página de calidad con su propio selector. **El scheduler periódico de 9Q no se toca**, como pide el prompt: cada cuánto se revisan las conversaciones tiene coste y llena una cola que alguien atiende, así que es decisión operativa. **Salida real del comando** (9 interacciones sembradas, ventana 30 días): 2 huecos —«dieta/estranger» con 4 consultas y «teletreball» con 3—, y **segunda pasada: 2 huecos, 2 filas en total (idempotente)**; las dos interacciones que debían quedar fuera (una a 200 días, otra sin señal) quedaron fuera. **Migración `o2x3y4z5a6b7`** aplicada; **la suite 9Q sigue verde** tras la extensión. **Tests**: 13 de backend (RED primero: 12 fallos) + 4 de frontend; **224/224 en vitest**, `tsc` limpio. Un mock ajeno reapuntado: `ContentQuality.test.tsx` mockea el módulo entero, así que montar el panel exigía declarar los tres hooks nuevos. **Suite completa: 1446 passed, 1 skipped, 0 failed**; `ruff check app` limpio salvo el `F841` preexistente de ING.0.5. Contrato OpenAPI y Orval regenerados. |
| 2026-08-01 | RAG.13 (RED/GREEN) — Escenarios de prueba por chatbot con veredicto humano (backend + UI admin) | **Cursor: RAG.13 ✅ → RAG.14.** Que exista esto y el dorado de RAG.1 **no es duplicidad**: el dorado mide *recuperación* con métricas y bloquea el build; esto mide la *respuesta* con juicio humano, que es lo que ninguna métrica sustituye en un asistente normativo. Frente a probar a mano en el widget, lo que aporta es que **queda registro**: la misma consulta antes y después de un cambio, con quién la juzgó y por qué. **`expectation_note` es prosa y no se comprueba automáticamente, a propósito**: convertirla en aserción exigiría un criterio de igualdad entre respuestas de un LLM, y ese criterio *es* el problema. **El test de `capture_context` destapó un olor de diseño y lo cambió**: el router llamaba a un método **privado** del grafo y **recomponía** el contexto tras ejecutar, lo que habría guardado un contexto *parecido* al enviado, no el mismo. Ahora el estado lleva `capture_context` y el propio nodo compone la instantánea mientras responde —sale de donde salió el prompt—, y de paso reutiliza de verdad la maquinaria de RAG.11 en vez de imitarla. **El aislamiento por `chatbot_id` está en toda consulta**, incluida la que más se olvida: pedir el escenario de otro chatbot por su id devuelve 404 y no el recurso; hay test de ese camino porque es la fuga más fácil de escribir y la más difícil de ver leyendo. El CHECK de `verdict` **admite NULL** («sin juzgar todavía» es el estado en que nace toda ejecución). Ambas tablas son **operacionales**: son las pruebas del cliente sobre su corpus, así que el router va `Deploy: edge` y **hay test de que está en `_register_edge` y no en `_register_cloud`** —olvidarlo deja al despliegue edge sin la funcionalidad y no se nota hasta el cliente—. **Frontend contract-first**: la página se construye desde los tipos de Orval y los dobles del test son `ScenarioRead`/`RunRead` **tipados**, así que un cambio de contrato deja de compilar en vez de fallar en producción; i18n es/ca/en con un test que falla si alguna cadena se renderiza como su clave. **Dos tropiezos de infraestructura de test, ajenos al código de producción**: `TestClient` síncrono contra la fixture async de BD da «Future attached to a different loop» —resuelto con `AsyncClient`+`ASGITransport`, lo que ya hacían los E2E de chat desde 6.1— y el helper compartido de chatbots siempre los llama «Bot», que choca con el unique cuando un test de aislamiento necesita dos. **Tests**: 11 de backend (RED primero: 11 fallos) + 6 de frontend (RED primero: no compilaba). **Frontend 220/220, `tsc` limpio**; migración `n1w2x3y4z5a6` aplicada. **Verificado contra el servidor en marcha**: los 6 endpoints publicados en el OpenAPI y `401` sin sesión. **Suite completa: 1433 passed, 1 skipped, 0 failed** — el guardarraíl de frontera edge/cloud saltó al añadir las dos tablas nuevas, que es exactamente su trabajo: obliga a declararlas conscientemente en vez de dejarlas colarse. `ruff check app` limpio salvo el `F841` preexistente. **Lo que NO se pudo verificar: la pantalla en el navegador** — los tools de la extensión de Chrome se denegaron por permiso, así que la revisión visual pasa al `.bat` de pruebas manuales del bloque en vez de darse por hecha. |
| 2026-08-01 | RAG.12 (RED/GREEN) — Progreso granular y estadísticas por etapa de los jobs de ingesta | **Cursor: RAG.12 ✅ → RAG.13.** `status` solo distingue pending/running/completed/failed, y una conversión de Docling sobre un PDF largo tarda minutos: desde fuera, un job trabajando y un job colgado son indistinguibles. Lo que faltaba no era más log, era **estado consultable**. **La decisión de diseño está en dónde vive el acumulador: en memoria.** El manejador de errores hace `rollback`, así que unas estadísticas escritas en la fila conforme se generan se perderían justo en el caso en que más falta hacen; al vivir en un objeto de Python sobreviven al rollback y se escriben después, junto al estado `failed` y con `failed_stage` diciendo cuál reventó. Hay test. **El throttle es por etapa o por lote, nunca por fragmento**: un corpus de 3.000 documentos con un `UPDATE` por chunk convierte la barra de progreso en la parte cara de la ingesta, y el test lo fija comparando avisos contra fragmentos en vez de contar avisos a mano. **Bug encontrado mirando la salida real, no en un test**: el contador daba `4/3` —numerador en etapas, denominador en fragmentos—. Ahora `actual` se cuenta en fragmentos y la etapa va en el mensaje, que es lo que quiere una barra de progreso: `[0/?] convert: docling` → `[0/?] chunk` → `[0/2] embed: 2 fragmentos en 1 lote` → `[2/2] persist`. El `?` no es cosmético: antes de trocear no se sabe cuántos fragmentos hay y un 0 ahí se leería como «no hay nada que hacer», que es por lo que `progress_total` es la única nullable del grupo. **Límite conocido y anotado en el docstring**: la fila se actualiza en la **misma transacción** que la ingesta, así que el progreso intermedio no es visible desde fuera hasta que el job cierra. Hacerlo visible en vivo exige una transacción autónoma —una segunda sesión— y comprometerse a eso por una barra de progreso no sale a cuenta hoy; el consumidor en vivo es `progress_callback`, que es por donde informa la carga masiva por consola. Commitear por etapa **no** es alternativa: expondría un documento sin sus fragmentos a cualquier lector concurrente. El CLI de corpus y el watcher comparten el mismo callback a propósito: una sola forma de contar el progreso, no dos que se contradigan. **El endpoint de jobs no necesitó cambios** —devuelve el ORM entero— pero se verifica con un test en vez de darlo por hecho, mismo criterio que con `corpus_recalculator` en RAG.8. **Un test ajeno se aflojó**: `test_run_job_propagates_catalan_language` comparaba la firma **entera** de `process_source` con `assert_called_once_with`, así que lo rompía cualquier prompt que instrumente la ingesta; ahora comprueba lo que protege —que el idioma se propaga— y no la firma. **Migración `m0v1w2x3y4z5`** aplicada. **Tests**: 9 nuevos (RED primero: 8 fallos; el noveno ya pasaba porque el callback es opcional) + 1 reapuntado. **Suite completa: 1422 passed, 1 skipped, 0 failed**; `ruff check app` limpio salvo el `F841` preexistente. Contrato OpenAPI y Orval regenerados, `tsc` limpio. |
| 2026-08-01 | RAG.11 (RED/GREEN) — Modo bypass: el prompt final construido sin llamar al LLM | **Cursor: RAG.11 ✅ → RAG.12.** Cuando una respuesta sale mal la pregunta es siempre la misma —*¿qué contexto recibió el modelo?*— y hasta ahora se contestaba leyendo trazas de Langfuse o reproduciendo la consulta a mano, las dos cosas más caras que el problema. El bypass ejecuta el pipeline entero (idioma → reescritura → recuperación → gate → empaquetado) y se detiene **justo antes de invocar**. **Tres decisiones, con test cada una**: (1) **cero invocaciones al LLM**, comprobado con un espía que cuenta —un bypass que llama al modelo «solo para una cosa» deja de ser gratis y deja de ser inspección—; (2) **el gate informa pero no desvía**, porque desviar dejaría sin prompt que enseñar justo en el caso que más interesa depurar, y se ve en la salida real (contra el fixture el gate da `passed=false`, o sea que con el comportamiento normal no se habría visto ningún prompt); (3) **no persiste `HubInteraction`** —es inspección, no conversación— y responde **JSON, no SSE**, porque no se genera texto y no hay nada que transmitir en trozos. **El gate de acceso no es un `require_scopes` a secas, y no puede serlo**: `require_scopes` solo filtra principales PAT, así que deja pasar **cualquier** sesión JWT, incluida la del widget público. `puede_depurar` distingue las dos clases: un PAT lleva `chat:debug` explícito o no lo lleva —no lo hereda de quien lo emitió, que es el sentido de acotar un token de máquina— y una sesión humana lo tiene por rol. **Scope nuevo y separado de `chat:test`** porque el bypass enseña el system prompt entero y la configuración resuelta, que es más de lo que concede poder charlar con el bot. La instantánea se serializa **en el grafo y no en el endpoint**, que solo ve el estado final y no la evidencia ni el contexto construido. **Salida real del criterio de done** (fixture, «quant cobro de dieta per anar a Madrid?»): evidencias `REG-020#art-14` y `REG-003#art-8`, `dropped_count 0`, `total_tokens 72` sobre presupuesto 128.000, `messages [system, user]`, `quality_gate {score 0,011, passed false}` —esperado con el embedding determinista, cuyas similitudes son estructuralmente bajas— y `resolved_config` con la cascada entera. **Doc**: `docs/DEPURAR_CONTEXTO_RAG.md`, con el orden de lectura (gate y `packed_context` primero, que entre los dos explican la mayoría de las respuestas malas). **Tests**: 12 nuevos (RED primero: 12 fallos). **Suite completa: 1413 passed, 1 skipped, 0 failed**; `ruff check app` limpio salvo el `F841` preexistente. Contrato OpenAPI y Orval regenerados, `tsc` limpio. **Sin migración.** |
| 2026-08-01 | RAG.10 (RED/GREEN) — Reescritura de la consulta con el historial antes del retrieve | **Cursor: RAG.10 ✅ → RAG.11.** El problema: «i si és a l'estranger?» no contiene ninguna de las palabras por las que se encontraría la norma; su vector apunta a cualquier sitio y la rama léxica no tiene de dónde agarrarse. **Cifras del criterio de done, y son las mayores del bloque**: sobre el subconjunto conversacional, **recall@5 0,400 → 1,000 · recall@10 0,500 → 1,000 · MRR 0,218 → 0,950**. **Pero el criterio no se podía cumplir tal como estaba escrito**: el dorado de RAG.1 declaraba el campo `history` y **no tenía ni una consulta que lo usara**, así que el «subconjunto conversacional» que este prompt manda medir no existía. Se construye: 10 consultas anafóricas de verdad —ninguna llega a 45 caracteres ni contiene el término por el que se encontraría su norma—, con un test que lo vigila para que nadie relaje el dorado y crea que ha mejorado. **Vive en `golden_conversacional.json` y no dentro de `golden_dietes.json`, a propósito**: mezclarlo movería la media contra la que se compararon RAG.3–RAG.9 y dejaría la línea base del gate midiendo otra cosa. **El reescritor con el que se mide es determinista** —concatena el último turno del usuario con el seguimiento—, no un LLM: misma postura que RAG.1 con el embedding y RAG.6a con el reranker, se verifica que *resolver la anáfora ayuda a recuperar*, no la calidad de un modelo reescribiendo. Un LLM daría una consulta más limpia, así que **0,950 es un suelo, no un techo**. **Segunda desviación documentada, y sin ella el prompt no se sostiene: el historial no existía en ninguna parte** —ni en `ChatRequest`, que solo llevaba `message`, ni en `CoreGraphState`—. Lo manda el cliente, porque la API es sin estado y no hay entidad conversación de la que recuperarlo; se acota a 50 turnos en la petición y el reescritor solo mira los últimos 10. **Las dos reglas que lo hacen seguro de encender**: (1) *el chat nunca falla por la reescritura* —excepción, timeout de 2 s, respuesta anómala (vacía o de más de 300 caracteres, que es un modelo pequeño poniéndose a explicar) o modelo que no se puede construir ⇒ se busca con el mensaje del usuario—; **no es el «fallback silencioso» que el proyecto prohíbe**, porque no se degrada a otro modelo sino que se apaga el paso entero y se vuelve al comportamiento por defecto; (2) *la reescritura no llega a la generación* —se recupera con ella y se responde a lo que el usuario escribió—, con test de las dos mitades. Queda en el estado como `rewritten_query`, y en **None cuando el fallback devolvió la original**: distinguir «no se reescribió» de «se reescribió igual» es lo que hace legible la traza y el bypass de RAG.11. **Flag apagado de plataforma**, tercera vez en el bloque y por el mismo motivo que `reranker_enabled` y `min_retrieval_score`. **Las dos columnas booleanas son NULLABLE, al revés que sus hermanas más antiguas**: con un `False` no nulo el nivel de abajo pisaría siempre al de arriba y encender la reescritura en la organización no llegaría nunca a sus chatbots. El LLM de reescritura se configura en la organización (`rewrite_llm_config_id`) porque es otro modelo, más pequeño y rápido; sin él se usa el del chatbot con **el tope de salida bajado a 100**, que es lo que cuesta una línea. La `GraphFactory` lo asigna tras construir el grafo en vez de pasarlo por las tres factorías de perfil: es una decisión de composición que depende de la cascada, igual que el `AgenticLoop`. **Nota que no es de este prompt**: el dorado principal marca ahora **MRR 0,940** frente al 0,920 anotado al cerrar RAG.8. RAG.10 no toca el ranking y **se ha comprobado midiendo en el commit de RAG.9 con este trabajo apartado**: el 0,940 ya estaba ahí. Es una consulta que sube un puesto; descartado el índice nuevo de RAG.9 (medido también sin él). **Queda anotado para cuadrarlo al regenerar la baseline al cierre del bloque.** **Migración `l9u0v1w2x3y4`** aplicada. **Tests**: 17 nuevos (RED primero: 16 fallos) + 2 dobles de la API reapuntados. **Suite completa: 1401 passed, 1 skipped, 0 failed**; `ruff check app` limpio salvo el `F841` preexistente. Contrato OpenAPI y Orval regenerados, `tsc` limpio. **Sin cambios de UI**: la cascada de troceado de RAG.8 tampoco entró en el formulario del admin y el prompt solo pide migración, cascada y routers. |
| 2026-08-01 | RAG.9 (RED/GREEN) — Procedencia obligatoria del vector + CLI de re-embedding masivo | **Cursor: RAG.9 ✅ → RAG.10.** El prompt llegaba **muy solapado con MOD.1**, que ya había añadido `embedding_model`/`embedding_dim` y la guarda de espacio vectorial; lo que quedaba de verdad era hacerlos **obligatorios**, poner la guarda **en el camino de la consulta** y escribir la CLI. **El cambio de fondo: «desconocido» deja de ser un estado legítimo.** MOD.1 tuvo que tratar como desconocidos los chunks sin procedencia porque existían; ahora que se pueden rellenar, ese hueco es por donde se cuela justo la avería que la columna vino a impedir. **El backfill no es una suposición**: hasta MOD.2, `get_embedding_service()` devolvía el local incondicionalmente, así que todo chunk anterior salió de BGE-M3 a 1024 — se rellena un dato conocido, y por eso se puede exigir NOT NULL después. **Desviación documentada, y es la decisión de diseño del prompt: se persiste `embedding_text`**, que el plan no pedía. Desde RAG.7 el texto embebido no es `content`, así que un re-embed que reconstruyera el embebible desde el contenido produciría vectores **que la ingesta nunca generó**: no sería un re-embed, sería un segundo corpus incompatible con el primero, y como el coseno no da error nadie se enteraría. Los chunks anteriores no lo tienen y **no se inventa**: la CLI los cuenta aparte y remite a `recalculate-corpus`, que sí re-trocea desde el documento. **Dos agujeros reales que el NOT NULL sacó a la luz**, invisibles mientras la columna fue opcional: (1) `process_user_upload` escribía en la MISMA tabla **sin procedencia y embebiendo `content` en vez de `embedding_text`** — o sea, el vector de un adjunto salía de un texto distinto del que habría salido por la ingesta normal, y se buscaba junto al corpus; (2) el watcher hacía `getattr(servicio, "model_name", None)` y **escribía NULL en silencio** si el servicio no lo declaraba. Ahora `_procedencia()` falla con un mensaje que dice qué falta y de quién, en vez de un `NotNullViolation` de Postgres a mitad de ingesta. **La guarda entra en `/hub/chat` (409)**, no solo en `recalculate-corpus`: sin ella, preguntar a un corpus embebido con otro modelo devuelve las fuentes más parecidas *en un espacio que no es el suyo* — respuestas plausibles apoyadas en normas que no vienen a cuento, que en un asistente normativo es peor que un error. **Se paga con un índice, no con una excepción a la regla**: `ix_hub_document_chunks_embedding_space` (chatbot_id, embedding_model, embedding_dim) convierte el DISTINCT por consulta en un recorrido solo-índice; sin él, el caso bueno leería el heap entero para responder «no pasa nada». **Validación al crear chatbot** solo en modo RAG: fallar al crear es barato, fallar a mitad de una ingesta de miles de documentos no; a `MD_LONG_CONTEXT`/`MD_AGENT_SELECTOR` no se les inventa un requisito que no tienen. **CLI** `python -m ...ingestion.reembed` con `--chatbot-id/--all/--dry-run/--force/--batch-size`; pagina **por clave primaria y no por OFFSET**, porque el UPDATE de cada lote mueve las filas bajo el cursor y un OFFSET creciente se saltaría chunks en silencio. **Un test de MOD.1 se borra** (`should_accept_a_corpus_without_provenance_yet`): protegía un estado que el esquema ya no permite. **Defecto encontrado al ejercer la CLI de verdad**: la flecha `→` de su salida reventaba con `UnicodeEncodeError` en la consola cp1252 de Windows — pasada a ASCII. **Migración `k8t9u0v1w2x3`** aplicada (`alembic current` = head). **Salida real del dry-run** contra el corpus de fixture (25 normas, 52 fragmentos): mismo modelo activo → `52 en total / 0 a re-embeber / 52 ya al día`; tras cambiar a Google → `52 a re-embeber`; ejecución real → `52 re-embebidos` y el plan vuelve a `0 pendientes`, o sea **idempotente**. **Tests**: 12 nuevos en `integration/test_reembed.py` (RED primero: 10 fallos) + 13 reapuntados —los dobles de embeddings que no declaraban procedencia y los que construían chunks a mano—. **Suite completa: 1383 passed, 1 skipped, 0 failed**; `ruff check app` limpio salvo el `F841` preexistente de ING.0.5. Backend-only: sin cambios de contrato ni de frontend, y por tanto sin verificación en navegador. |
| 2026-08-01 | RAG.8 (RED/GREEN) — Parent-child (small-to-big) + parámetros de troceado en la cascada | **Cursor: RAG.8 ✅ → RAG.9.** La tensión que resuelve: un fragmento pequeño **se encuentra** mejor —el vector es más específico— pero **responde** peor, porque le falta el contexto de alrededor. Small-to-big busca con el hijo y responde con el padre. **La decisión que se señaló al abrirlo, cerrada: la agrupación sigue siendo por documento.** El plan pedía «deduplicar hijos del mismo padre» y agrupar por documento ya lo consigue —es un superconjunto— **sin** emitir varias entradas por documento en el `sources` del evento SSE, contrato que RAG.2 fijó por snapshot. Corolario: la fusión de fragmentos adyacentes que RAG.5 descartó por código muerto **sigue sin tener productor**, así que su desviación se mantiene en pie y ya no hay un prompt pendiente que la haga viva. **Migración `j7s8t9u0v1w2`** (aplicada): `chunk_size`, `chunk_overlap` y `chunking_strategy` en `hub_chatbots`, sus tres `default_*` en `hub_organizaciones` —todas nullable porque NULL es «heredar»— y `parent_content` en `hub_document_chunks`. El CHECK de la estrategia **admite NULL a propósito**: NULL es heredar, no valor inválido. `parent_content` es **columna directa y no JOIN**: el padre ya existe como texto en el documento y duplicarlo cuesta menos que mantener una tabla de secciones sincronizada con cada re-troceado — misma clase de decisión que el puente bilingüe de RAG.4 y con la misma condición, que refrescarlo sea re-trocear. **El watcher deja de hardcodear los defaults** y lee la cascada (`_chunker_para`), que es lo que hace que la configuración por chatbot signifique algo: hasta ahora el admin podía cambiar `chunk_size` en la API y no pasaba nada. Si la cascada no responde, se usa el chunker de plataforma en vez de tumbar una ingesta por un dato de tuning. **`corpus_recalculator` no necesitó cambios**: ya regeneraba llamando al watcher, y el watcher ya lee la cascada — pero **se verifica con un test en vez de darlo por hecho**, porque «funciona de forma transitiva» es justo la clase de afirmación que este proyecto ha visto fallar en silencio. **Tests**: 9 en `integration/test_parent_child.py` (RED primero: 8 fallos) + 10 reapuntados —los mocks de ingesta, que ahora tienen que modelar la consulta de configuración del watcher, y los dobles de la API con los tres campos nuevos—. **912 passed, 0 failed**; contrato OpenAPI y Orval regenerados, `tsc` limpio. **Cifras del criterio de done, y no son las esperadas**: el dorado con `parent_child` ON da **recall@5 0,960 · recall@10 0,960 · MRR 0,920**, **idéntico** a OFF. El corpus de fixture son 25 normas breves, así que sus secciones no llegan a producir varios hijos y small-to-big no tiene nada que reorganizar: **el mecanismo está verificado por tests unitarios, su beneficio en recuperación exige el corpus real**. Es la tercera vez en el bloque —RAG.4, RAG.6a y ahora RAG.8— que el fixture verifica la mecánica pero no puede medir la ganancia; conviene tenerlo presente al decidir cuánto más invertir antes de cargar el corpus. |
| 2026-08-01 | RAG.7 (RED/GREEN) — Contextual retrieval estructural: jerarquía en el texto embebido | **Cursor: RAG.7 ✅ → RAG.8.** `Chunk` gana `embedding_text`: lo que se **embebe** deja de ser lo que se **almacena**. Un fragmento que dice «L'import es de 53,34 euros» no dice de qué importe habla ni de qué norma sale, y así compite contra cualquier otro importe del corpus; con su jerarquía delante queda anclado sin cambiar una letra de la evidencia que ve el usuario. No se persiste: se calcula al trocear y se consume en la ingesta. **La regla dura se respeta y tiene test**: en `embedding_text` solo entra contexto estructural —título del documento y encabezados—, **nunca** `ambit_principal`, `submateries` ni el ancla, porque taxonomía embebida convierte cada revisión del vocabulario en un re-embedding del corpus (CLAUDE.md §5) y el ancla es ruido para el vector. **Lo que el RED enseñó y cambió el diseño**: el contenido **ya conservaba sus propios encabezados** desde ING.0.4 (`strip_headers=False`), así que prefijar la jerarquía sin más la duplicaba entera —se vio en el fallo: `'Norma > Norma > Article 3\n\n# Norma\n## Article 3\n…'`—. La regla final es que **solo se prefijan los niveles que el fragmento no lleva ya dentro**, y los que ganan contexto son los **fragmentos siguientes** de una sección larga, que sí se quedaron sin encabezado al trocear. **Dos tests míos estaban mal y los corrigió el código, no al revés**: uno asumía que el contenido no llevaba encabezados, y otro montaba un `AsyncMock` pelado —que fabrica `embed_batch` sola, así que el watcher tomaba la vía del lote y el test pasaba en verde sin comprobar nada—; sustituido por un doble explícito sin lote. **Embebido por lotes** (`embed_batch` en `LocalEmbeddingService`, una llamada por documento en vez de una por fragmento): con un proveedor por API eso era una ida y vuelta de red por chunk. Se conserva `embed` suelto para servicios que no expongan lote. **Hook de nivel 2 definido y no implementado**: protocolo `ContextEnricher` + `NoopEnricher` por defecto, con test de que el chunker consume su salida —si no, sería decorado—. El enricher con LLM queda para cuando el corpus definitivo esté cargado: cuesta una llamada por chunk y no tiene sentido pagarla contra un corpus que aún va a cambiar. **Cifras**: **MRR 0,861 → 0,920** en el dorado (+0,059), con recall@5 y recall@10 iguales en 0,960 — firma exacta de contextual retrieval: encuentra los mismos documentos y coloca el correcto más arriba. 10 tests nuevos (RED primero: 9 fallos) + 3 reapuntados; `agents_hub`+`public_graphs`+`api`+guardarraíles **903 passed, 0 failed**; `ruff` limpio. **Pendiente señalado**: la baseline del gate sigue en el MRR de 0,861, así que **la mejora no está protegida** — un cambio futuro podría devolverla entera y pasar dentro de tolerancia. Regenerarla es un acto deliberado que conviene hacer al cerrar el Bloque RAG, con todas las mejoras dentro. |
| 2026-08-01 | RAG.6a (RED/GREEN) — Reranker detrás de protocolo: el mecanismo | **Cursor: RAG.6a ✅ → RAG.7.** Mitad de RAG.6 que **no depende de ningún servicio externo**; el adaptador de Vertex y la medición del valenciano son RAG.6b, detrás del Deploy. Nuevo `services/reranker.py`: protocolo `Reranker`, `RerankResult {index, score}`, `normalize_score` (sigmoide) y `pool_size`. **Se prueba con un reranker determinista** —solapamiento de palabras— por la misma razón que RAG.1 mide el mecanismo de recuperación con un embedding determinista: lo que se verifica es la mecánica, no la calidad de un modelo. **Pool ampliado a `max(30, top_k*3)`**: pedirle `top_k` candidatos a un reranker es pedirle que reordene lo que ya está elegido, o sea no puede mejorar nada; hay test de que se le pasa el pool entero. **El score del reranker SUSTITUYE al de fusión**, y eso no es cosmético: el packer de RAG.5 corta por score y el quality gate promedia, así que con dos escalas conviviendo —RRF da ~1/60, el reranker [0,1]— esos dos controles decidirían sobre números incomparables. El test lo fija midiendo las dos escalas. **Sin fallback silencioso, por partida doble**: `resolve_reranker` lanza `RerankerNotConfigured` si el flag está encendido y no hay configuración, y `RerankerProviderNotSupported` si el `provider_type` no tiene adaptador. Devolver `None` habría obligado a cada llamante a decidir, y la decisión cómoda es seguir sin reranker — el fallback silencioso por la puerta de atrás. **`LocalReranker` queda como opción de edge** con import perezoso de `sentence_transformers`, para que el contenedor de cloud pueda dejar de empaquetarlo cuando los embeddings sean por API. **Latencia registrada** en cada rerank: es el dato con el que se decidirá si sale a cuenta, y hoy nadie lo ha medido. **Cableado en `rag_vector_pipeline`** por el punto de extensión `_construir_estrategia` que RAG.5 dejó puesto —ahora `async`, porque resolver el reranker consulta la BD—; solo se resuelve si `cfg.reranker_enabled`, que viene en `False` desde MOD.2, así que nada se activa por sorpresa. **Tests**: 9 en `integration/test_reranker.py` (RED primero: 8 fallos; el que ya pasaba es el del flag apagado, que hoy no llama a nadie) + 1 reapuntado en `test_context_packer.py` por el cambio a `async`. **Cifras**: `agents_hub`+`public_graphs`+`api`+guardarraíles **893 passed, 0 failed**; `ruff` limpio. **Sin migración**: la de `reranker_enabled=False` la hizo MOD.2. |
| 2026-08-01 | MOD.2 (RED/GREEN) — Selección del servicio de embeddings por configuración (**cierra el Bloque MOD**) | **Cursor: MOD.2 ✅ → RAG.6 reordenado. Bloque MOD COMPLETO (2 prompts).** Nuevo `services/embedding_resolver.py`: el servicio se resuelve desde la configuración y **`GoogleEmbeddingService` deja de ser código inalcanzable** tras meses. **El adaptador se elige por `HubProvider.provider_type`**, el mismo mecanismo que `model_factory` usa para el chat: añadir un modelo o cambiar de proveedor dentro de un tipo soportado es un `UPDATE`; solo un **tipo** nuevo exige adaptador, y esa frontera es irreducible. **Sin fallback silencioso**: un `provider_type` desconocido lanza `EmbeddingProviderNotSupported`, porque degradar a local sin avisar dejaría medio corpus en un espacio vectorial y medio en otro. **Sin configuración, local**, a propósito: así el desarrollo y los tests no salen a la red por accidente y una clave que falta no se convierte en una factura ni en una ingesta a medias. **Cableado en los cinco puntos que lo usan** (`hub_chat`, `ingestion`, `hub_ingestion_router` y dos en `hub_chatbots_router`), que es lo que evita repetir el pecado que MOD.1 documentó. **Los 30 rojos que salieron al cablear fueron la señal de que el cambio llegó a producción**: los tests parcheaban `get_embedding_service` en cinco módulos, y que se rompieran demuestra que esos puntos ya resuelven por configuración; si no se hubiera roto ninguno, el resolutor sería otra vez código muerto. Reapuntados a `resolve_embedding_service` con `AsyncMock`, más dos tests de `recalculate-corpus` que ejercen el endpoint y ahora tienen que controlar la nueva dependencia. **Decisión del usuario aplicada: `reranker_enabled` pasa a `False` por defecto** (plataforma, ORM, routers y formularios del admin) con la migración `i6r7s8t9u0v1`, que actualiza **solo** las filas que valían `True`. El motivo, en corto: con el default en `True`, en cuanto RAG.6 tenga implementación el reranker se activa de golpe para todos los chatbots, y eso acopla la disponibilidad del chat a un segundo servicio —el prompt prohíbe el fallback silencioso—, añade latencia y coste por consulta, y no se ha medido que mejore. Es literalmente lo que pasó con `min_retrieval_score`. **`.env.example`** documenta la elección por despliegue: principal (GCP) → Google por API con DPA; edge → sin configurar, BGE-M3 local. **Cifras**: `agents_hub`+`public_graphs`+`api`+guardarraíles **884 passed, 0 failed**; 6 tests nuevos (RED primero) + 19 reapuntados; `ruff` y `tsc` limpios. **Pendiente medible, ya anotado**: cuando el corpus v1 esté cargado, comparar BGE-M3 contra Google a 1024 en valenciano con `run_golden.py`. La documentación de Google declara 100+ idiomas y lidera MMTEB pero **no publica lista por idioma y el catalán no aparece**: la calidad en valenciano se mide, no se supone. |
| 2026-08-01 | MOD.1 (RED/GREEN) — Propósito del modelo en la configuración + procedencia en el vector | **Cursor: MOD.1 ✅ → MOD.2.** Prompt y bloque **añadidos a petición del usuario** al preguntar si en GCP conviene usar embeddings por API en vez de modelos locales. Análisis y verificación en `docs/DECISION_MODELOS_EMBEDDING_RERANKER.md`. **Lo que se encontró mirando el código**: el panel de proveedores y modelos ya existía y es bueno (`HubProvider` con `base_url` y clave, `available-models`, test de conexión, `LLMConfigsPage.tsx`), pero **solo cubría el chat**; `GoogleEmbeddingService` estaba escrito desde hacía meses y **no lo usaba nadie** porque `get_embedding_service()` devuelve el local a pelo; y había una **guarda de dimensión en `recalculate-corpus` que no podía saltar nunca** — comparaba `getattr(servicio, "dimensions", 1024)` con `getattr(llm_config, "embedding_dimensions", 1024)` y ninguno de los dos atributos existía, así que era `1024 != 1024`. **Peor**: el test que la cubría montaba un `SimpleNamespace(embedding_dimensions=1536)`, un campo inexistente en el modelo real, así que verificaba una ficción — el test en verde es justo la razón de que nadie notara que la guarda estaba muerta. **Verificación de la API de Google (2026-08-01)**: `gemini-embedding-001` admite dimensión **flexible 128-3072**, así que **1024 es válido** y la columna `Vector(1024)`, el índice HNSW y el corpus sobreviven a un cambio de proveedor; pero **las dimensiones distintas de 3072 no vienen normalizadas** («you must manually normalize non-3072 dimensions»). **Decisiones**: (1) la plataforma trabaja a **1024**, único valor que sirve a la vez a BGE-M3 en edge y a Google en cloud; (2) **normalizamos nosotros siempre**, porque depender de que lo haga el proveedor es depender de una nota al pie que cambia entre versiones y el síntoma de equivocarse son scores que parecen válidos; (3) `HubLLMConfig` gana `purpose` (`chat`\|`embedding`\|`rerank`, **con CheckConstraint** — tres valores estables con consumidor, mismo criterio que `nivell_acces`) y `output_dimensionality`, reutilizando el panel existente en vez de construir uno paralelo; (4) cada chunk graba `embedding_model` y `embedding_dim`, porque **misma dimensión no es el mismo espacio vectorial** y el coseno entre dos espacios no da error, da resultados malos. Nuevo `services/embedding_space.py` con `assert_embedding_space_matches`, que **sí puede saltar** y nombra los dos espacios para que quien lea el error sepa qué re-embeber; los chunks anteriores a MOD.1 no declaran modelo y se tratan como desconocidos, porque exigir un dato que no existía convertiría la mejora en una migración forzosa. **Migración `h5q6r7s8t9u0`** aplicada y probada en los dos sentidos. Contrato OpenAPI + Orval regenerados, `tsc` limpio. **Tests**: 10 en `integration/test_embedding_provenance.py` (RED primero, 10 fallos) + 2 reapuntados (el de la ficción, ahora prueba la traducción a 409 con la detección probada aparte contra BD real; y el doble de `LLMConfigRead`). **Cifras**: `agents_hub`+`public_graphs`+`api/hub`+guardarraíles **851 passed** con los 2 rojos propios ya corregidos y reverificados (37 passed en los dos ficheros). **Hallazgo ajeno inventariado**: la suite de frontend es **flaky bajo carga** — tres pasadas dieron 1, 5 y 4 fallos en ficheros **distintos cada vez**, y los cuatro implicados pasan **en aislamiento** (32 tests verdes). Vitest no limita workers y usa los 22 núcleos: el tiempo agregado de `environment` fue de 865-1128 s en pasadas de ~85 s. Es el equivalente frontend de lo que TST.4 arregló en backend y merece su propia decisión, no absorberlo aquí. |
| 2026-08-01 | RAG.5 (RED/GREEN) — Umbral de similitud aplicado + empaquetador de contexto con presupuesto | **Cursor: RAG.5 ✅ → RAG.6.** **Umbral**: `min_retrieval_score` se aplica sobre la similitud coseno de la rama vectorial **en el WHERE**, antes del ORDER BY / LIMIT —filtrar después dejaría que los candidatos por debajo del umbral ocuparan plazas del top_k, el mismo error que VIS.1 corrigió—. Viaja **solo a la rama vectorial**: la señal de la léxica es de ranking, no de similitud, y un `ts_rank_cd` de 0,3 no significa lo mismo que un coseno de 0,3. Documentada también su relación con `quality_threshold`: son dos controles distintos, por-chunk y por-respuesta. **Y aquí el criterio de done hizo su trabajo**: pedía comprobar si el default 0,25 recorta aciertos del dorado, y **los recorta** — recall@5 **0,960 → 0,720** y MRR **0,861 → 0,693**. Pero ese 0,72 **no prueba que 0,25 sea mal umbral en producción**: el corpus de fixture usa un embedding determinista (bolsa de palabras por hash) cuyas similitudes coseno son estructuralmente bajas —una consulta de 6 palabras contra un fragmento de 100 rara vez pasa de 0,25 aunque sea el acierto correcto— y BGE-M3 tiene otra distribución. Un umbral **absoluto** es justo lo que ese corpus no puede calibrar. **Decisión del usuario (2026-08-01): mecanismo sí, activación no.** El default efectivo pasa a **0,0** en la cascada, en el ORM, en los routers y en los formularios del admin; migración `g4p5q6r7s8t9` que cambia el default de columna y actualiza **solo** las filas que valían exactamente 0,25, porque un valor distinto es una decisión de alguien y no se pisa. Queda `test_should_not_lose_golden_hits_with_the_default_threshold`, que lee el default **de la cascada** y no un número escrito en el test: si alguien lo sube sin datos que lo respalden, falla. **Empaquetador** (`services/retrieval/context_packer.py`): ordena por score, acumula hasta el presupuesto de la cascada (el de VIS.2, 128.000 con carácter general) y reporta `dropped_count`. Antes, en RAG, quien recortaba el contexto era el proveedor del modelo —y cuando corta el proveedor no queda traza de qué se perdió—. Si ni la mejor evidencia cabe, entra igualmente y el recorte queda anotado: mismo criterio que la degradación de VIS.2, porque un contexto vacío no produce ninguna respuesta y uno recortado sí. Cableado en `rag_vector_pipeline`, que gana además `_construir_estrategia` como punto de extensión para el reranker de RAG.6. **Desviación documentada**: **no se implementa la fusión de chunks adyacentes** que pedía el prompt. Hoy ningún productor emite evidencia a nivel de chunk —`VectorRetrievalStrategy` agrupa por documento y se queda con el mejor fragmento—, así que sería código muerto, y CLAUDE.md lo prohíbe explícitamente; alimentarla exigiría emitir varios fragmentos por documento, lo que **duplicaría entradas en el `sources` del evento SSE `done`**, contrato que RAG.2 fijó por snapshot. RAG.7 (parent-child) es el prompt que la hace viva, y ahí toca añadirla con su test. Está escrito en el docstring del packer. **Contrato regenerado** (los defaults de la API cambian): `openapi.json` + Orval, `tsc` limpio, **214 tests de frontend verdes**. **Tests**: 7 en `public_graphs/test_context_packer.py` + 3 de umbral en `integration/test_retriever.py` + 1 de umbral contra el dorado (RED primero: 10 fallos). **Cifras**: `agents_hub`+`public_graphs`+`api/hub`+guardarraíles **843 passed, 0 failed**; gate de RAG.1 verde con la baseline intacta (**recall@5 0,960 · recall@10 0,960 · MRR 0,861**), que con el default a 0,0 es lo que tenía que salir. Verificación del prompt en **3:09**, con la política escalonada de TST.4. |
| 2026-08-01 | RAG.4 (RED/GREEN) — Pata léxica real: `tsvector` + GIN sustituye al `ILIKE` | **Cursor: RAG.4 ✅ → RAG.5.** **Lo que había no era búsqueda léxica, era subcadena**: sin stemming (`becas` no encontraba `beca`), sin orden por relevancia (todos los resultados valían 1.0, que no es una puntuación sino un relleno) y con un AND por palabra, así que una palabra de más vaciaba el resultado. Ahora `websearch_to_tsquery` sobre una columna generada `tsv` con índice GIN, y score `ts_rank_cd` **normalizado dividiendo por el máximo del lote**: normalización relativa a la consulta, no absoluta, y está escrito en el docstring para que nadie compare scores entre búsquedas distintas. `websearch_to_tsquery` y no `plainto_tsquery` porque acepta la sintaxis que el usuario ya conoce de un buscador y no revienta con entradas raras, que es lo que llega de un chat. **Config por idioma**: `spanish` para castellano, `simple` para el resto —PostgreSQL core no trae stemmer catalán—, con test de la consecuencia: en catalán `beca` encuentra y `beques` no. Un diccionario Snowball catalán sería mejora de despliegue, no de código. **Migración `f3o4p5q6r7s8`** (aplicada, `alembic current` = head): columnas `bilingual_terms` y `tsv` (generada `STORED`) + índice GIN, y un UPDATE que rellena el puente de lo ya ingerido para no dejar el corpus existente sin él hasta la siguiente reingesta. **La decisión que el prompt dejaba abierta**, resuelta con el modelo delante: los `termes_bilingues` se **denormalizan al chunk** porque una columna generada solo puede referirse a su propia fila y los términos viven en `hub_documents.doc_metadata`. Esa denormalización es segura **mientras refrescarla cueste un UPDATE**, que es lo que hace el nuevo `ingestion/bilingual_bridge.py`; hay test de que reetiquetar deja el embedding intacto. La taxonomía (ámbito/submaterias) no entra en el tsvector en ningún caso. **Hueco real encontrado escribiendo el test, y esta es la parte que importa**: el reconciliador aplica el front-matter **después** de llamar al watcher —deliberado desde ING.0.5, porque el watcher es la tubería compartida con el crawler—, así que un documento recién ingerido troceaba **sin** los pares y nacía sin puente. No habría fallado: habría dejado de encontrar `despesa` en silencio. Por eso el puente se refresca en el reconciliador en sus **dos** caminos (ingesta nueva y actualización de metadatos), y por eso `bilingual_bridge` es un módulo propio y no una función del watcher. **Tests**: 9 nuevos en `integration/test_retriever.py` (stemming, config catalana, ranking por terminología exacta, scores normalizados, puente bilingüe, refresco sin re-embedding, contrato RRF intacto, plan de consulta con GIN, y escaneo de que no queda `ILIKE`) + 3 en `test_ingestion_creates_documents.py` (derivación de los pares y cableado). RED primero: 5 fallos. **Cifras**: `agents_hub`+`public_graphs`+guardarraíles **801 passed** tras arreglar un rojo propio; gate de RAG.1 **31 passed** con **recall@5 0,960 · recall@10 0,960 · MRR 0,861**, *idénticas* a la baseline. **Igualdad, no mejora, y conviene decir por qué**: el corpus de fixture tiene 25 normas y embedding determinista, así que la rama vectorial ya acierta por solapamiento de vocabulario y la léxica no tiene margen que aportar; las ganancias que el prompt esperaba en consultas con sigla exigen el corpus real y BGE-M3. **Rojo propio arreglado**: `test_should_apply_and_rollback_migration_cleanly` (RAG.3) usaba `downgrade -1` dando por hecho que la migración HNSW era la cabeza; con la de RAG.4 encima bajaba una revisión que no tenía que ver con esos índices. Ahora baja a la revisión anterior **por su id**, así que sigue probando lo suyo cuando se apilen migraciones. `ruff` limpio en los ficheros nuevos (quedan 5 preexistentes ajenos). |
| 2026-08-01 | TST.4 (GREEN) — Coste de la verificación: la suite deja de frenar el desarrollo | **No mueve el cursor de RAG** (sigue en RAG.4). Prompt añadido a petición del usuario, que preguntó si se podían reducir los tests o ejecutarlos solo al cerrar el bloque. **La medición dice que el problema no era el número de tests sino cómo se ejecutaban**: cobertura forzada en `addopts` (~40 %: 21,3 s → 12,8 s sobre `public_graphs`), **1,3 s de setup por test** de BD (los 139 de `integration` en 212 s eran casi todo setup) y tres atípicos de 39 s, 21 s y 13 s. **Tres cambios**: (1) `--cov` fuera de `addopts` —CI ya lo pasa explícito en sus dos invocaciones, así que la cobertura vigilada no baja—; (2) `pytest-xdist` con `-n auto`, seguro porque cada test de BD crea la suya con nombre único; (3) **BD desechable por `TEMPLATE`**: una plantilla por sesión con el esquema creado y `CREATE DATABASE ... TEMPLATE` por test, que es copia de ficheros. **Setup por test medido: 1,3 s → 0,40 s.** **La contrapartida, atajada en el mismo prompt**: `-n auto` reparte los tests entre 16 procesos y **esconde el estado filtrado entre tests**, que es justo lo que TST.1 unificó CI para cazar (el `AsyncMock` sobre una clase que rompió 10 tests durante meses). **CI se queda en `-n0`**, con el porqué escrito en el workflow y en CLAUDE.md: en local manda la velocidad, en CI la detección. **Política escalonada** añadida a CLAUDE.md: durante el prompt solo el fichero que se escribe; al cerrar el prompt los directorios tocados + `test_suite_hygiene.py` (medio segundo y caza justo lo que se escapa de un subconjunto); la suite entera solo al cerrar el bloque, y desde Git Bash. **Cifras medidas**: suite completa **1.306 passed, 1 skipped, 0 failed en 242,78 s (4:02)** con `-n auto` —y esa misma pasada confirma como medida la cifra que el cierre de RAG.3 había dejado marcada como aritmética—; en `-n0`, el modo de CI, `unit`+`integration` dan **639 passed en 134,77 s**. **Lo que NO mejora tanto como parece**: el paralelismo solo baja la suite de ~5 min a 4, porque el cuello no es CPU sino E/S —`CREATE DATABASE` concurrente y los tests que levantan subprocess con docling, camelot o matplotlib—. La ganancia real para el día a día no es esa: es que la suite completa pasa de una vez por prompt a una vez por bloque, y la verificación por prompt baja a segundos. |
| 2026-07-31 | RAG.3 (RED/GREEN) — Índice HNSW en pgvector | **Cursor: RAG.3 ✅ → RAG.4.** Migración `e2n3o4p5q6r7`: HNSW con `vector_cosine_ops` sobre `hub_document_chunks.embedding` (recuperación del chatbot) y `hub_crawled_pages.page_embedding` (detector semántico de 9Q). Sin índice ANN, cada pregunta recorre todos los vectores del chatbot calculando la distancia coseno de uno en uno. **`vector_cosine_ops` y no otro opclass**: `retriever.py` ordena por `cosine_distance`, y un índice con otra métrica existiría sin usarse — que es peor que no tenerlo, porque parece resuelto. Defaults de pgvector (`m=16`, `ef_construction=64`), que son los recomendados hasta tener medición propia; RAG.1 ya da la vía para medir si conviene tocarlos. **Los índices se declaran en el ORM Y en la migración.** Solo en la migración no habría valido: las BD de test se construyen con `create_all` desde el metadata, así que el índice no existiría **donde se prueba** — es la misma desalineación ORM/migración que dejó `POST /hub/ingestion/upload` roto en instalación limpia hasta ING.0.2. **Prerrequisito verificado y cerrado**: el prompt dejaba abierto «actualizar docker-compose si hace falta»; pgvector es **0.8.2** (se pedía ≥0.5) en `pgvector/pgvector:pg16`, la imagen de dev **y** de producción, así que no hay nada que tocar. **Sin cambios de código**, como decía el prompt: la consulta del retriever ya era indexable. **Tests**: 5 en `integration/test_hnsw_index.py` (RED primero: 4 fallos; el de equivalencia ANN↔exacto ya pasaba sin índice y queda como guardarraíl de que el ANN no cambie la respuesta). El test del plan de consulta desactiva `enable_seqscan` **a propósito y está escrito por qué**: sobre tres filas el planner prefiere legítimamente el escaneo secuencial, y lo que se comprueba es que el índice es *usable* por la consulta real, no qué decide el planner con un corpus de juguete. Migración aplicada (`alembic current` = `e2n3o4p5q6r7`), índices verificados en la BD de desarrollo y **downgrade + re-upgrade probados** en test. **Refactor colateral, con su motivo**: la fixture `fresh_database` sube al **conftest raíz** junto a `db_url` —la usan `tests/infra` y `tests/modules/agents_hub/integration`, y `_sync_url` estaba triplicado en tres ficheros—; importar una fixture de módulo a módulo hace que el parámetro del test redefina el nombre importado (F811). **Guardarraíl de higiene afinado**: `test_should_have_no_test_creating_tables_on_the_dev_database` buscaba `create_all` por substring y marcaba un fichero que solo lo menciona en su docstring; pasa a `metadata.create_all`. **Y no a `create_all(`**, que era la primera opción y habría **desarmado** el guardarraíl: el patrón de los infractores originales es `run_sync(Base.metadata.create_all)`, sin paréntesis. Verificado con un infractor sintético que reproduce ese patrón exacto: el guardarraíl lo caza y luego se retira. **Cifras**: **1.306 passed, 1 skipped, 0 failed** — medido en la pasada completa de TST.4, no deducido. (Durante el prompt la confirmación se abortó dos veces por carga de la máquina y el cierre se escribió diciendo que 1.306 era aritmética; TST.4 lo midió al día siguiente.) **Hallazgo ajeno arreglado**: `redaccion/charts/test_chart_services.py::test_valid_script_returns_bytes` falla de forma intermitente por `timeout` de 30 s del sandbox —levanta un subprocess con matplotlib y bajo la carga de la suite completa en Windows no llega; en aislamiento tarda ~10 s—. Misma familia que el `test_env_generator` que arregló RAG.2. **El default de producción NO se toca** por un problema del entorno de test: el `timeout` generoso va explícito en el test, que comprueba que un script válido devuelve un PNG y no cuánto tarda. |
| 2026-07-31 | VIS.3 (RED/GREEN) — Versión canónica bilingüe + advertencia de vigencia (**cierra el Bloque VIS**) | **Cursor: VIS.3 ✅ → RAG.3. Bloque VIS COMPLETO (3 prompts).** **Canónica**: la exclusión de no canónicas ya la aplicaba VIS.1; lo que añade este prompt es la puerta de vuelta. `AgenticRetrievalStrategy.read()` devuelve `variant_id` —el id de la versión en la otra lengua, resuelto en los dos sentidos por `versio_idiomatica_de`— porque sin eso pedir la cita literal en castellano exigiría buscar por URL, que es adivinar. `read()` **no filtra a propósito**, y eso es la decisión: es la única puerta por la que se leen las dos cosas que la recuperación excluye (la variante idiomática y una norma derogada), ambas consultas legítimas y ambas exigiendo un id explícito que solo sale de un índice ya filtrado. **Derogados**: `estat_vigencia='derogat'` sale de la recuperación **incondicionalmente**, ni con el filtro más abierto —no es una preferencia de recuperación sino un hecho sobre la norma— y hay test de que sigue siendo legible por id. Se excluye **solo `derogat` exacto**: el catálogo real trae `vigent?` y otros estados dudosos, que se advierten en la respuesta y no se ocultan, porque ocultar 312 de 314 fichas dejaría el asistente sin corpus. **Advertencia de vigencia**: nuevo `services/retrieval/vigencia.py` con la regla (`vigencia_validada_el IS NULL` o `estat_vigencia != 'vigent'` ⇒ advertir), el flag que las tres estrategias incrustan en la evidencia y el texto del aviso. **El aviso lo emite el CoreGraph** tras `enforce_citation_contract`, sobre la evidencia realmente citable, y **no es una instrucción al modelo**: una instrucción se cumple casi siempre, y «casi siempre» no basta para decir si una norma rige. La plantilla marca además cada documento afectado en el bloque de fuentes (`[VIGENCIA NO VALIDADA]`) para que el modelo pueda redactarlo con naturalidad, pero la garantía no depende de que lo haga. **El aviso nombra los documentos afectados**: uno genérico no dice de qué norma se duda, y el usuario necesita saber qué cita confirmar. Si el fallback ya sustituyó la respuesta no se añade nada, porque no hay nada citado de lo que advertir. **Respuesta real** (CoreGraph completo, LLM guionizado citando dos documentos, uno con vigencia validada y otro con `vigent?`): «L'import de la dieta es de 53,34 euros [Reglament d'indemnitzacions...](https://www.uji.es/REG-020) i la justificacio es presenta en 10 dies [Instruccio...](https://www.uji.es/INS-004).» + `---` + «Aviso: la vigencia de la normativa citada no esta validada... Documentos afectados: **Reglament d'indemnitzacions per rao del servei**.» — la instrucción validada no aparece en el aviso. **Dos canónicas para la misma norma = error explícito**: validador `_una_sola_canonica_por_norma` en `CorpusManifest`, que falla **antes de tocar la BD** y nombra las dos rutas. Medido en el informe: 233 fichas en valenciano y 81 en castellano, muchas la misma norma; elegir por orden de aparición indexaría el mismo contenido dos veces sin que nadie lo hubiera decidido. **El validador cazó un dato incidental de ING.0.3**: `test_should_roundtrip_manifest_json` construía dos entradas con la misma `source_url` por reutilizar la fixture; se corrige el dato del test (dos documentos distintos no comparten url oficial), no la regla. **Tests**: 9 en `integration/test_vis_canonica_vigencia.py` + 4 en `public_graphs/test_vis_vigencia.py` + 2 en `test_corpus_manifest.py` = 15 (RED primero: 12 fallos; los 3 que ya pasaban son regresión de lo que VIS.1 dejó hecho). **Cifras**: suite entera **1.301 passed, 1 skipped, 0 failed**; `ruff` limpio en los ficheros del prompt. **Sin `.bat` de pruebas manuales para el bloque**: VIS.1–VIS.3 son backend puro (el único delta de frontend fueron tipos generados por Orval, cubiertos por `tsc` y 214 tests de Vitest), y lo que exigiría juicio humano —una respuesta del asistente sobre el corpus real— está bloqueado por la carga del corpus v1, que sigue pendiente. |
| 2026-07-31 | VIS.2 (RED/GREEN) — Niveles 0/1/2: índice de submaterias, selección escalonada e inyección de subconjunto | **Cursor: VIS.2 ✅ → VIS.3.** **Nivel 0**: el `router_index` (índice de submaterias de `VocabularyService.build_router_index`, ING.0.1) entra por la cascada del `ConfigResolver` —mismo argumento que el `system_prompt` en RAG.2: depende de la organización del chatbot— y lo inyecta la `GenericAnswerTemplateStrategy`, que sigue siendo la única fuente del system prompt. **Se resuelve solo en `MD_AGENT_SELECTOR`**: en RAG serían ~2.300 tokens de prompt que nadie usa, más dos consultas por petición. Acompañado de `RANK_AND_VALIDITY_RULES` (rango superior manda; no presentar como vigente lo que el documento no declara vigente; no citar derogado). Vocabulario vacío → `router_index=None` y el modo degrada al catálogo de documentos, que es el comportamiento anterior: una organización sin vocabulario cargado no es un error de configuración. **Nivel 1**: `list_documents` gana `submateries: list[str]` y devuelve **fichas** —`resum_router`, `rang`, submaterias, tokens y url— nunca `markdown_content`, que es justo lo que el modo selector viene a evitar. **No se añade un tool `list_submateries`**: el índice ya está en el prompt, preguntarlo sería una llamada de más. **Retroceso escalonado** en `AgenticRetrievalStrategy`: submateria → ámbito → catálogo global, con el escalón en `last_index_level`. **La regla que lo hace seguro: el retroceso ensancha el TEMA, nunca el PERMISO** — `nivell_acces`, `us_assistents`, `canonica` y la exclusión de superseded se aplican en los tres escalones, y hay test de que un actor público con una submateria que solo tiene un documento `intern` recibe lista vacía en el escalón global en vez de el documento. **Nivel 2**: `LongContextRetrievalStrategy` **recorta en vez de lanzar**. Lanzaba `ValueError` al pasarse del límite (`long_context_strategy.py:41-46`), y en producción eso no es un aviso sino una caída: el usuario recibe un error en vez de una respuesta parcial y marcada como parcial. Ahora acumula hasta el presupuesto y `RetrievalContext` gana `truncated` + `discarded_documents`, que `MdLongContextPipeline` pasa al debug. Caso límite decidido: si **ni el primer documento cabe** se inyecta igualmente y se marca truncado, porque un contexto vacío no produce ninguna respuesta y uno recortado sí. **Migración `d1m2n3o4p5q6`** aplicada (`alembic current` = head) y **probada en los dos sentidos**: `hub_chatbots.context_token_budget` y `hub_organizaciones.default_context_token_budget`, **ambas nullable**, porque NULL significa «heredar» y el default de plataforma (128.000) vive en el `ConfigResolver`; con un valor no nulo en la tabla, subir el default no llegaría nunca a las filas ya creadas. Expuestas en los tres esquemas de `hub_chatbots_router` y los tres de `hub_organizaciones_router`; `openapi.json` regenerado (109 paths, 175 schemas) y cliente Orval regenerado, `tsc` limpio y **214 tests de frontend verdes**. **Traza real del mecanismo** (BD desechable, corpus de 4 normas, LLM guionizado; la traza con BGE-M3 y modelo real sigue esperando la carga del corpus v1): Nivel 0 → índice de 3 submaterias en 82 tokens, system prompt total 461 tokens; Nivel 1 → el router elige `indemnitzacions-i-dietes` y recibe **2 fichas en 108 tokens**; Nivel 2 → lee **2 documentos de los 4** del chatbot, ambos con `index_fallback_level='submateries'`; y la circular `nivell_acces='intern'` de esa misma submateria **no aparece ni en las fichas ni en la evidencia**. **Tests**: 12 en `tests/public_graphs/test_vis_levels.py` (RED primero: 10 fallos; los 2 que ya pasaban documentan lo que traían ING.0.1 y VIS.1) + `tests/public_graphs/conftest.py` nuevo, con el orden de importación de torch que exige Windows y `db_session` sobre la fixture autorizada `db_url`. **Reapuntados, no borrados**: `test_raises_when_corpus_exceeds_limit` → `test_truncates_when_corpus_exceeds_limit` (el contrato cambió, y el test dice por qué), los dos fakes de `list_index` en `test_agentic_strategy.py`, los dobles `SimpleNamespace` de `tests/api/hub` y la fixture `DEMO_CHATBOT` de `ChatbotsPage.test.tsx`. **Cifras**: suite entera en un solo proceso **1.286 passed, 1 skipped, 0 failed** (1.274 antes + los 12 nuevos); gate de RAG.1 sin regresión. `ruff` limpio en los ficheros del prompt (quedan 2 errores preexistentes ajenos: `inicio` sin usar en `reconciler.py` de ING.0.5 y una lambda `l` en `test_graph_factory.py` de RAG.2). **Desviaciones documentadas**: (1) **el escalón se sella en `EvidenceItem.metadata['index_fallback_level']`**, decidido con el usuario: el prompt pedía «el debug de la evidencia», pero el retroceso ocurre dentro del `AgenticLoop`, cuando el `debug` del `RetrievalResult` del pipeline ya se emitió; la metadata de la evidencia *es* el debug de la evidencia y se mide por respuesta, sin tocar la firma de `AgenticLoop.run` ni el `CoreGraph`. (2) **Conflicto de default con RAG.5, avisado en el código**: RAG.5 pide un default de plataforma de 4.000 para su packer de chunks y reutiliza esta misma columna; bajarlo aquí recortaría `MD_LONG_CONTEXT` a 4k y lo dejaría inservible. El default queda en 128.000 —el Nivel 2 inyecta documentos enteros— con la instrucción escrita en `config_resolver.py` de que RAG.5 aplique su techo **en el packer**, no en el default compartido. (3) **Sin verificación en navegador**: el único delta de frontend son tipos generados por Orval (comprobación de compilación, cubierta por `tsc` + los 214 tests); no se añade campo de UI porque el criterio de done pide «visible en la API admin», y un chat real en modo selector exige clave de LLM y corpus sembrado. |
| 2026-07-31 | VIS.1 (RED/GREEN) — Filtro de metadatos en la capa de recuperación | **Cursor: VIS.1 ✅ → VIS.2.** Nuevo `services/retrieval/metadata_filter.py`: `MetadataFilter` frozen (`ambits`, `submateries`, `max_nivell_acces`, `include_non_canonical`, `include_superseded`) que traduce la clasificación del corpus a condiciones SQL sobre `hub_documents`. **El fail-closed vive en `__post_init__`, no en la factoría**: un nivel de actor desconocido —`None`, cadena vacía o fuera del vocabulario— cae a `'public'` sea cual sea la vía de construcción, así que `MetadataFilter(max_nivell_acces=algo_raro)` no puede abrir el corpus por descuido. `for_actor()` existe por legibilidad, no como única puerta. **Filtro en el WHERE, antes del ORDER BY / LIMIT**: OUTER JOIN de `hub_document_chunks` a `hub_documents` por la FK de ING.0.2, con `document_id IS NULL` pasando siempre (los chunks temporales no tienen metadatos que consultar y ya los acota `owner_id`). **Retirado `_get_superseded_doc_ids`**, que era el precedente que el bloque VIS prohíbe explícitamente: filtraba en memoria tras el `top_k`, de modo que los documentos excluidos consumían plazas del resultado en vez de ser reemplazados; ahora es una subconsulta correlacionada `NOT EXISTS` sobre `hub_crawled_pages` dentro del mismo WHERE. Se eligió subconsulta y no un segundo JOIN para que las tres estrategias unan solo `hub_documents`. **Las otras dos estrategias, que no filtraban nada** (`long_context_strategy`, `agentic_strategy`), reciben el mismo contrato: un documento derogado ya no se inyecta entero en `MD_LONG_CONTEXT` ni lo lista `list_documents`. **Cuarta vía cerrada, no prevista en el plan**: `DocumentIndexProvider` (nació en RAG.2, después de redactarse VIS.1) listaba **todos** los documentos del chatbot sin filtro — un índice sin filtrar es fuga aunque el modelo no llegue a leer el documento, porque el título ya dice que existe. **`include_superseded` desaparece como parámetro** de las tres búsquedas: una sola fuente de verdad en el filtro, cuyo defecto es el cerrado (público, canónico, sin superseded, sin `us_assistents='no'`). **Tests reescritos, no borrados**: los 9 de 9Q.6 en `unit/test_retrieval_quality_filter.py` mockeaban `_get_superseded_doc_ids` sobre una sesión falsa que devolvía los mismos chunks para cualquier SQL — con el filtrado en SQL no probarían nada, así que pasan a `integration/test_retrieval_superseded.py` (5 tests contra BD real) y el fichero de fakes se borra (Caso B). **Tests**: 16 en `integration/test_metadata_filter.py` (RED primero) + 5 de superseded. El que muerde si alguien vuelve a filtrar en Python es `should_apply_filter_in_sql_before_limit`: 6 chunks del ámbito equivocado con similitud máxima y 2 del buscado con similitud nula, `top_k=2` → con el filtro en SQL devuelve los 2 relevantes, con el filtro en Python devolvería lista vacía. **Cifras**: suite entera en un solo proceso —como la ejecuta CI desde TST.1— **1.274 passed, 1 skipped, 0 failed**; por partes, `agents_hub` **659 passed**, `public_graphs`+`test_metadata_filter` **108 passed**, resto **522 passed, 1 skipped**; gate de RAG.1 **31 passed** sin regresión (baseline intacta: recall@5 0,960 · recall@10 0,960 · MRR 0,861 — VIS.1 no cambia el ranking del corpus de fixture, que es público y canónico entero). `ruff` limpio. **Desviaciones documentadas**: (1) los tests van a `integration/` y no a la raíz de `tests/modules/agents_hub/` como decía el plan, porque necesitan el operador `&&` de arrays y el JOIN reales; (2) el criterio de done pedía ejecutar RAG.1 «contra baseline» — se ejecuta el gate de CI sobre el corpus de fixture, que es el único disponible: la vía manual con BGE-M3 sigue esperando la carga del corpus v1; (3) el filtro por ámbito/submateria del **actor** todavía no se cablea desde el grafo: VIS.1 deja el contrato y el defecto cerrado, y VIS.2 es quien lo alimenta con la selección del router. **Hallazgo ajeno, inventariado**: lanzar la suite desde PowerShell da **21 fallos** y desde Git Bash **cero**. 10 son de `tests/infra/test_setup_script.py`, que da **12 passed desde Git Bash** — `subprocess.run(["bash", …])` resuelve a `C:\\Windows\\system32\\bash.exe` (lanzador de WSL, sin distro instalada) en vez de Git Bash. No es código: es de qué shell cuelga pytest. En CI (Linux) es verde. Un test que pasa o falla según quién lo lanza es un gate frágil y merece arreglo propio (localizar Git Bash explícitamente en `_run`), fuera del alcance de VIS.1. **Consecuencia para futuras sesiones: lanza la suite desde Git Bash**, o los 21 rojos del entorno enmascaran los reales — que es justo el asterisco que el Bloque TST vino a quitar. |
| 2026-07-29 | ING.0.5 (RED/GREEN) — Reconciliador de corpus + CLI sobre carpeta local (**cierra el Bloque ING.0**) | **Cursor: ING.0.5 ✅ → Bloque ING.0 COMPLETO.** Tres módulos nuevos en `ingestion/corpus/`: `source.py` (protocolo `CorpusSource` `runtime_checkable` + `LocalDirectorySource`), `reconciler.py` (`CorpusReconciler`) y `load.py` (CLI). **Una sola implementación de la reconciliación, dos fuentes**: SYNC.1 añade `PublicationMcpSource` y no toca el reconciliador. Emparejamiento por `id_publicacio` y, si no viene, por url oficial + idioma —hay test de que cambiar la URL oficial no crea un documento nuevo—. Cinco desenlaces: ingerir / re-ingerir por hash distinto / **actualizar metadatos sin re-trocear** / omitir / omitir por `us_assistents: no` con su motivo. El test del segundo tiene **espía sobre el embedding**: reetiquetar da `llamadas == 0` y el recuento de chunks intacto, que es el invariante de CLAUDE.md §5 verificado y no prometido. **La poda, que es el modo peligroso**: exige que la fuente declare censo (`is_census()`, default `False`), es opt-in, **nunca borra** —marca `us_assistents='no'` + `motiu_exclusio='retirada_de_la_font'`, y hay test de que el recuento de documentos no baja— y lleva **salvaguarda de proporción** (>10 % aborta con `PruneThresholdExceeded` y exige `--force-prune`), que es la red contra un `--dir` mal escrito. **Defecto de diseño encontrado por un test durante el GREEN**: la primera versión calculaba los candidatos a poda por `last_seen_at < inicio`, y en `--dry-run` —que no estampa nada— eso hacía que **ningún** documento pareciera visto y la poda propusiera retirar el corpus entero, abortando por umbral; ahora los emparejados se llevan **en memoria**, que además elimina la dependencia de comparaciones de reloj. Resuelve también `versio_idiomatica_de`, que en el `.md` es una referencia (`id_publicacio`) y en la BD un UUID, en una segunda pasada. Un `HubIngestionJob` por ejecución con los recuentos, porque si el CLI mantiene el corpus durante meses el historial va en la BD. **Desviaciones documentadas**: (1) **el watcher NO se toca** — el plan pedía ampliarlo para refrescar metadatos en el acierto de hash, pero el reconciliador los aplica después de la llamada, así que la tubería compartida con el crawler queda sin acoplar al contrato del corpus, que es mejor que lo planificado; (2) **no se emite `HubContentFinding` al retirar**: el modelo exige `site_id` NOT NULL con FK a `hub_web_sites` y un documento de corpus no tiene sitio; la extensión a hallazgos por chatbot es de RAG.14, así que por ahora la retirada se reporta en el job y en la salida del CLI, y **marcar es la parte que protege el corpus** (el documento deja de alimentar al asistente). **Fallo preexistente RESUELTO por el camino**: el `Windows fatal exception: access violation` que arrastraba `test_full_pipeline.py` y que bloqueaba cualquier test de integración que tocara el chunker. Reducido a dos líneas: `asyncpg connect → import langchain_text_splitters` aborta el proceso; el orden inverso funciona. Es un choque de DLL al cargar torch después de asyncpg en Windows. Arreglado importando `langchain_text_splitters` al principio del conftest de integración, con el diagnóstico escrito ahí. **`tests/.../integration` pasa de crashear a 90 passed**, y `test_full_pipeline.py` corre por primera vez. **Tests**: `test_corpus_source.py` **15** + `test_corpus_reconciler.py` **21** = 36 (RED primero, 21 fallos). Regresión: **584 passed** en `unit`+`integration`. **Sigue pendiente, y con hipótesis nueva**: los 10 de `test_site_model.py` en ejecución conjunta. Descartados `test_hub_document.py` y `test_ingestion_storage.py` como interferencia. La mejor pista es el **`event_loop` de sesión del `conftest.py` raíz**: sobreescribirlo con `scope="session"` está deprecado en pytest-asyncio 1.x y produce exactamente este síntoma —el `INSERT` del sitio y el de la página acaban en transacciones distintas, de ahí el `IntegrityError` de FK—, y el propio conftest de e2e ya documenta que usa scope de función «para evitar conflictos de event loop». Retirarlo es un cambio global de infraestructura de tests y merece prompt propio. |
| 2026-07-30 | RAG.2 (RED/GREEN) — Consolidación de grafos: CoreGraph a producción | **Cursor: RAG.2 ✅ → VIS.1.** `api/v1/hub_chat.py` deja de construir el grafo con `create_agent_graph` y pasa por `GraphFactory` + `CoreGraph`: entran en producción la cascada `ConfigResolver` (Plataforma→Organización→Chatbot), el quality gate con fallback, `LanguagePolicy` y `EvidenceItem` como contrato de evidencia. **Migración aplicada**: `c0l1m2n3o4p5` añade `hub_interactions.fallback_reason` (`'quality_gate' \| 'citation' \| NULL`, indexada, **sin CheckConstraint** — los motivos son vocabulario que crecerá y una restricción obligaría a migrar por cada motivo nuevo; RAG.14 la consume); `alembic current` = `c0l1m2n3o4p5 (head)`. **Cambios en el CoreGraph**: el fallback **emite el mensaje** en vez de dejar `answer=None` —el endpoint lo manda por SSE como respuesta normal, así el usuario ve una explicación y no un `done` vacío—, `enforce_citation_contract` se aplica tras la generación, y el estado gana `fallback_reason` y `sources`. **Loop agéntico portado** a `strategies/agentic_loop.py` (`AgenticLoop`): acumula EvidenceItem y lee por un `DocumentReader` inyectado en vez de importar los tools dentro del bucle, así el modo se prueba sin BD; lo monta `build_agentic_loop_if_needed` en la factory, porque es una decisión de composición y el CoreGraph no debe conocer el `retrieval_mode`. **`md_agent_selector_pipeline` deja de ser stub**: `IndexProvider` inyectable (`DocumentIndexProvider` por defecto) — cumple la enmienda VIS de no cementar el índice, y **corrige un defecto real del stub**: devolvía `markdown_content` de todos los documentos, o sea el corpus entero en el prompt, que es justo lo que el modo selector viene a evitar; ahora el índice lleva título+id y hay un test que falla si el contenido de una entrada pasa de 1.000 caracteres. **Retirada (Caso B)**: borrados `agent/graph.py`, `agent/state.py`, `agent/prompts.py` y `unit/test_graph.py`; escaneo `should_have_no_references_to_retired_graph` a cero sobre `app/` y `tests/`. **Tests**: 12 nuevos de grafo (`tests/public_graphs/test_core_graph_production.py`) + 12 de endpoint (`integration/test_hub_chat_coregraph.py`), los 24 en RED antes de implementar. **Reapuntados, no borrados**: `unit/test_prompts.py` completo (probaba `build_system_prompt`/`format_sources_block`, ahora prueba la `TemplateStrategy` que los absorbió, conservando las 7 aserciones de comportamiento); `TestRetrievalModeDispatch` de `test_hub_chat_sse.py` (comprobaba que el endpoint instanciaba cada `*RetrievalStrategy`; ahora comprueba el seam nuevo: `get_pipeline(mode)` y que el modo del chatbot llega al `cfg` del grafo); el fallback de `test_core_graph.py`; las claves `debug` del selector; y el aviso de traducción de `test_chat_endpoint.py`, que ahora sale de los nodos `detect_language` y `merge`. **Contrato SSE preservado y verificado por snapshot**: mismos tipos de evento (sin ninguno nuevo), `done` con exactamente `{interaction_id, sources, language_fallback, translation_warning}`, `sources[]` con `{document_id, title, url, score}`, `status` con `{node, msg}` y **los tres mensajes de progreso idénticos** pese a que los nombres internos de los nodos cambiaron (`search_or_skip`→`retrieve`, `generate_response`→`generate_answer`). Contrato OpenAPI sin cambios (16 tests verdes) y **frontend sin tocar**. **Cifras**: `agents_hub`+`public_graphs`+`infra` **807 passed, 0 failed**; evaluación de RAG.1 **31 passed** sin regresión. **Desviaciones documentadas**: (1) `services/retrieval/types.Source` **sobrevive una capa por debajo** de los pipelines —`RagVectorPipeline` lo adapta a EvidenceItem, y el prompt sólo pedía retirar el `Source` de `agent/state.py`, que sí desapareció con el fichero—; retirarlo del todo obligaría a reescribir las tres estrategias de `services/retrieval/`, fuera de alcance. `_source_to_dict` y `enforce_citation_contract` aceptan ambos dialectos por eso, documentado en el código. (2) El `system_prompt` del chatbot entra por `PublicGraphConfig` y la cascada, no por un parámetro paralelo, porque *es* configuración efectiva del chatbot. (3) **Verificación en navegador no hecha**: RAG.2 es backend puro (frontend sin cambios) y un chat en vivo exige clave de LLM y corpus sembrado, que está pendiente; el contrato que consume el widget queda cubierto por el snapshot SSE a nivel de endpoint. (4) **Hallazgo ajeno arreglado**: `tests/infra/test_env_generator.py` fallaba de forma intermitente y **en un test distinto cada vez** por `subprocess.TimeoutExpired` a los 30 s (el script genera una clave RSA-2048 y bajo la carga de la suite completa en Windows no llegaba); subido a 120 s con el porqué en el código. No lo causó RAG.2 —no toca esos ficheros— pero un fallo por tiempo estropea cualquier comparación de la suite, que es lo que el Bloque TST acababa de dejar limpio. |
| 2026-07-30 | TST.3 (GREEN) — Cero fallos preexistentes: se acaban los asteriscos (**cierra el Bloque TST**) | **Cursor: TST.3 ✅ → RAG.2. Bloque TST COMPLETO (3 prompts).** Prompt añadido al plan el 2026-07-30 a petición del usuario, tras el cierre de TST.1/TST.2, para llevar a cero los 12 rojos que llevaban meses «inventariados». **Los dos arreglos previstos**: `uv add --dev aiosqlite` (0.22.1; los tests ya presuponían `sqlite+aiosqlite:///:memory:` pero la dependencia no estaba declarada ni en `pyproject.toml` ni en `uv.lock`) y borrado de los 3 tests-smoke de `modules/brain` en `tests/test_imports.py` (**Caso B**: el módulo no existe —solo hay `agents_hub`, `automation`, `redaccion`— y no hay migración asociada; borrado limpio, sin `skip` ni comentarios). **Dos referencias vivas más que el prompt no anticipaba**, encontradas por el barrido previo que el propio prompt exigía: `tests/repro_filter.py` (script de reproducción huérfano en la raíz, importaba `app.modules.brain`, sin ninguna referencia en el proyecto → Caso B, borrado) y `shared/tests/test_security_unification.py`, que hacía `open()` sobre `server/app/modules/brain/extraction_strategies.py` y por tanto habría reventado con `FileNotFoundError` si alguien ejecutara ese subárbol (retirado el tercer bloque de comprobación; los dos primeros, sobre ficheros que sí existen, intactos). Retirado además el mapeo obsoleto `app.modules.brain` del codemod histórico `scripts/refactor_imports.py`. **Guardarraíl nuevo** (RED primero, exactamente las 3 líneas esperadas): `should_not_import_nonexistent_project_modules` cruza los `import server.app.modules.<x>` de `tests/` contra los paquetes en disco, así que la próxima retirada de módulo que deje tests huérfanos falla en el acto. Con esto `test_suite_hygiene.py` tiene **6 tests**. **Cifras**: los tres ficheros afectados pasan de **3 failed + 9 errors** a **17 passed**; la selección `unit`+`api`+`core`+`redaccion`+`infra`+ficheros raíz da **522 passed, 1 skipped, 0 failed, 0 errors** (antes 446 passed + 3 failed + 9 errors; la selección de ahora incluye además `tests/infra`, así que el total no es comparable directamente — lo comparable es el cero). **Desviación documentada**: el criterio pedía `grep -r "modules.brain"` a cero «en todo el proyecto»; queda a cero en **código, tests y configuración**, pero se conservan a propósito las menciones documentales —el docstring del guardarraíl que explica por qué existe, y las entradas de `Plan_TDD_Fase1.md`/`PROJECT_STATE.md` que registran el problema—, porque reescribir el historial contradice la regla de que el pasado se registra, no se borra. **Limpieza adicional pedida por el usuario**: la línea `**Cursor actual: 9B.6**` de la sección `Plan_Contrato_OpenAPI.md` llevaba meses obsoleta (ese plan está completo en CF.1–CF.5 y 9B cerró en 9B.14); un agente que leyera el fichero de arriba abajo la veía antes que el cursor real. Sustituida por «Plan completo. No tiene cursor propio», con nota de la limpieza. |
| 2026-07-30 | TST.2 (RED/GREEN) — Ningún test escribe en la BD de desarrollo | **Cursor: TST.2 ✅ → RAG.2. Bloque TST COMPLETO.** Los infractores eran **tres**, no solo el e2e del prompt: `e2e/conftest.py` (motor + `create_all` sobre `DATABASE_URL`; `setup_chatbot` dejaba organizaciones «E2E Test Client» y chatbots residuales), `test_full_pipeline.py` (motor propio inline, residuos «Pipeline Test Client»; nunca migrado porque crasheaba con el access violation hasta TST.1) y `tests/core/auth/conftest.py` (que al menos limpiaba por email en el teardown, pero creaba tablas en la BD del desarrollador igual). **Cambio estructural**: la fixture `db_url` (BD desechable por test) **sube al conftest raíz** `tests/conftest.py` para que todos los directorios la vean —era la única forma de que `tests/core/auth` pudiera usarla—; el conftest de `agents_hub` conserva el orden de importación de torch y `db_session`. Los tres infractores pasan a construir sobre `db_url` (auth añade las tablas SQLModel sobre la desechable y su teardown de emails sobra: la BD entera se borra; `track()` se conserva como no-op para no tocar los 43 tests). **Guardarraíles** (RED primero: 4 fallos): en `test_suite_hygiene.py`, escaneo contra `metadata.drop_all` en tests (afinado a llamadas reales tras un falso positivo con los docstrings que cuentan la historia) y contra `create_all`+`DATABASE_URL` fuera de la fixture autorizada (sqlite en memoria queda fuera de la regla); y en `e2e/test_db_isolation.py`, dos tests en runtime — el motor e2e apunta a `test_hub_*` y el chatbot de `setup_chatbot` **no existe** en la BD de desarrollo. **Criterio de done medido**: recuento en la BD de desarrollo **idéntico antes y después** de correr e2e+auth+integración: `hub_chatbots=22 → 22`, `hub_organizaciones=22 → 22` (los residuos habían crecido de 12 a 21 con las propias ejecuciones de TST.1, buena ilustración del problema). **Limpieza de residuos, reportada**: 29 chunks + 12 interacciones + **21 organizaciones con 21 chatbots en cascada** (los chunks e interacciones hay que barrerlos aparte: no tienen FK a chatbot); queda exactamente el `Chatbot de Ejemplo` del bootstrap (`chatbots=1, organizaciones=1, chunks=0`). Retiradas además **2 BD `test_hub_*` huérfanas** de procesos matados a media ejecución durante el bisect de TST.1 — el `finally` de la fixture no corre si matas el proceso; es el único vector de huérfanas que queda y es benigno (BD aisladas, borrables en cualquier momento). **Un fallo propio durante el GREEN**: el refactor de `test_full_pipeline` perdió el import de `HubDocumentChunk` (`NameError`); corregido, 5 passed. **Desviación documentada**: `should_leave_no_rows_in_the_dev_database_after_e2e` se implementa como test por-test (el chatbot creado no existe en la BD de desarrollo) más la verificación manual del recuento sobre la suite completa, porque un test individual no puede observar el antes/después de toda la suite. e2e+auth+integración: **135 passed**; matriz final unit+integration+evaluation+infra: **697 passed, 0 failed**, y tras ella la BD de desarrollo sigue con `chatbots=1, organizaciones=1, chunks=0, interacciones=0` (solo el bootstrap) y **cero BD `test_hub_*` huérfanas**. |
| 2026-07-30 | TST.1 (RED/GREEN) — Aislamiento entre tests: los 10 fallos de `test_site_model.py` | **Cursor: TST.1 ✅ → TST.2.** **La hipótesis del prompt era falsa y la causa real es otra.** Se retiró el override de `event_loop` y los 10 fallos persistieron intactos. Diagnóstico por instrumentación en tres capas, cada una eliminando clases de causa: (1) desde dentro del test que falla — misma BD desechable, mismo `pg_backend_pid`, mismo `txid_current`, misma clase y tabla, `repo.session is db_session`, y aun así un `add`+`flush` **manual** persiste y el del repo no; (2) la pista que acota el mecanismo: **`session.refresh()` tenía éxito sobre una fila que «no existía»** — una fila insertada por tu propia transacción no puede ser invisible para ti, así que o hubo `ROLLBACK TO SAVEPOINT` o `create` entero no ejecutó nada; (3) hooks de evento **a nivel de la clase `Engine`** (el `echo` está cableado a `False` en `connection.py:21` y no acepta override — `TypeError: multiple values for 'echo'` —, así que la traza por echo no era viable): la traza muestra que **el `INSERT` de `hub_web_sites` y el `SELECT` del `refresh` no pasan por NINGÚN motor del proceso**. Un método que no ejecuta nada de lo que contiene y devuelve un objeto plausible es un mock. **Causa**: `tests/modules/agents_hub/unit/test_hub_sites_router.py:99` — `WebSiteRepo.create = AsyncMock(return_value=site)` **asignado a la clase y jamás restaurado** (ídem `CorpusSelectionRepo.create` en L225). Desde ese test, `create` devolvía siempre el mismo objeto desanclado sin ejecutar SQL, y todo test posterior que insertara páginas contra ese sitio fantasma reventaba con `ForeignKeyViolationError` — que es también por qué **dos tests distintos fallaban con el mismo `site_id`**, el detalle que se anotó y se descartó en la primera sesión de diagnóstico. Explica el patrón completo: dependiente de la selección (solo si ese fichero corre antes), sin errores intermedios, y **invisible para CI**, que ejecutaba `unit/` e `integration/` en procesos separados. **Arreglos**: los dos puntos pasan a `monkeypatch.setattr` (se restaura solo); nuevo `tests/infra/test_suite_hygiene.py` con **3 guardarraíles** (RED primero: 2 fallos) — escaneo estático contra mocks asignados a atributos de clase, contra overrides de `event_loop` en cualquier conftest, y exigencia de `asyncio_default_fixture_loop_scope = \"function\"` en pyproject; la higiene del `event_loop` **se hace igualmente** (override retirado del conftest raíz + política declarada en config), como limpieza y no como fix; y **`ci.yml` pasa a UNA sola invocación de pytest** para unit+integration+e2e, porque en dos procesos el estado filtrado entre tests no existe para CI. **Verificación**: par mínimo `sites_router`+`site_model` 28 passed; **`unit`+`integration` juntos: 600 passed, 0 failed** (eran 10 failed); matriz combinada unit+integration+evaluation+public_graphs **711 passed**; e2e+infra **68 passed** con la política de loops nueva. Actualizada la memoria de gaps preexistentes: los 10 ya no se filtran. **Método que conviene recordar**: cuando una fila «desaparece» pero `refresh()` no protesta, el método entero es un mock; y para trazar SQL con echo cableado, hooks sobre la clase `Engine`, que ven todos los motores del proceso. |
| 2026-07-30 | RAG.1 (RED/GREEN) — Dataset dorado + métricas de recuperación + gate de CI | **Cursor: RAG.1 ✅ → RAG.2.** Cuatro módulos en `evaluation/`: `retrieval_metrics.py` (`recall_at_k`, `mrr`, funciones **puras sin LLM ni BD**), `golden_dataset.py` (`GoldenQuery`/`GoldenDataset` Pydantic frozen + huella de corpus), `retrieval_eval.py` (harness + `check_gate`) y `run_golden.py` (CLI de la vía manual). **Dos decisiones que conviene entender antes de leer las cifras**: (1) el gate de CI corre sobre un **corpus de fixture de 25 normas breves** con un **embedding determinista** (bolsa de palabras proyectada por hash a 1024 dimensiones, L2-normalizada), así que **mide el MECANISMO de recuperación** —fusión híbrida, filtros, ranking, umbral, que es lo que cambian RAG.3–RAG.8— y **NO mide BGE-M3**, cuya calidad exige el corpus real y va por el CLI; está escrito en el docstring de los dos módulos y en el comentario del workflow, porque confundirlo llevaría a conclusiones falsas. (2) El corpus de fixture empezó con 10 documentos y **se subió a 25 al ver que con 22 fragmentos `recall@5` cubría el 23 % del corpus** y un acierto por azar contaminaba la medida. **Línea base medida y commiteada** (pre-mejoras): **recall@5 = 0,960 · recall@10 = 0,960 · MRR = 0,861** sobre 25 consultas escritas con el vocabulario del usuario y no del texto legal («quant cobro de dieta per anar a Madrid?»), etiquetadas para poder leer el informe por clase de consulta. El gate falla solo si alguna métrica baja **más de 0,02**: una mejora siempre pasa y el ruido de un cambio de fusión no rompe el build; sin baseline no hay gate pero sí informe, para que la primera ejecución no falle por no tener con qué comparar. `documents_fingerprint` independiente del orden detecta el dataset medido sobre otro corpus, que es la comparación que no compara nada. El informe guarda **lo recuperado entero** por consulta: sin eso, un fallo del dorado no se puede depurar sin repetir la consulta a mano. **RAGAS queda fuera de CI**, documentado en el docstring de `rag_metrics.py` con el reparto (gate = métricas puras; RAGAS = evaluación periódica manual), y **corregido el `except Exception: pass` silencioso**: ahora registra el motivo con `logger.warning`, porque un número que parece una métrica de RAGAS y en realidad es solapamiento léxico, sin rastro de por qué, es peor que no tener el número. **Cambio estructural**: el `conftest.py` con la BD desechable **sube de `integration/` a `tests/modules/agents_hub/`**, para que la use también `evaluation/`; efecto colateral bueno, el arreglo del orden de importación de torch pasa a cubrir todo `agents_hub`. Añadido el paso «Retrieval regression gate» a `ci.yml`. **Tests**: `test_retrieval_metrics.py` **27** + `test_golden_gate.py` **4** = 31 (RED primero, 27 fallos). Regresión: **121 passed** en integración+evaluación y **651 passed** en unit+public_graphs+infra; `ruff` limpio. **Desviación documentada**: el criterio de done pedía probar el CLI contra el corpus de ING.0, que no está cargado; el CLI queda escrito y probado en su interfaz, y la ejecución contra corpus real va con la carga. |
| 2026-07-29 | (herramientas del corpus — front-matter, panel de consolidación y anclas en el HTML) | **No mueve el cursor.** Entregado fuera del repo, en el pipeline del corpus. **`genera_frontmatter.py`**: emite el front-matter desde `cataleg_transparencia.csv` (226 ficheros con `url_oficial`, `title`, `language`, `id_publicacio`), con `materia`/`categoria` a `materia_antiga` y **nunca** a ámbito; verificado que el hash del hub es **idéntico con y sin front-matter**, así que reetiquetar no reingiere. Tres subcomandos: `extreu-consolidacio`, `dashboard`, `emet`. **La consolidación se propone, no se deduce**, con tres razones medidas: de 225 líneas con «modificado por» la mayoría son referencias del preámbulo a otras leyes (el patrón exige el participio seguido de «por/per»: 757 candidatos laxos → 691 reales); las notas son **de página y no de documento**, así que el artículo que las contiene es una pista y no un hecho; y las líneas numeradas son indistinguibles de los apartados. **`build_dashboard_consolidacio.py`**: panel con la misma tubería que `dashboard_pendents` (localStorage → JSON de decisiones), **agrupado por acto** —691 notas son solo **12 fechas distintas**, y los 92 candidatos de un reglamento son todos del mismo acuerdo: un clic—. Aplicada la cicatriz de `decisions_pendents_recuperades.json`: exporta **todo lo tocado**, también un comentario sin acción. **Tres defectos encontrados probándolo en el navegador**: «Confirma l'acte» confirmaba filas sin ancla que el emisor descartaba después (pérdida silenciosa); usé `alert()`/`confirm()`, que bloquean la extensión y la propia revisión, sustituidos por avisador en línea y doble pulsación; y al pasar la plantilla a raw string quedaron dobles barras que rompían un regex. **Dos preguntas del usuario resueltas**: la **nota genérica** (acuerdo que modifica la norma sin referirse a ningún artículo) **se añade** como acto sin `canvis` —tercera decisión `acte` en el panel, tecla `a`—, que es lo que alimenta la lista «Modificado por» de la cabecera; y las **fechas que faltaban eran el apóstrofo valenciano** (`24 d'octubre`): corregido el regex, de 159 sin fecha a **683 con fecha ISO y 8 marcadas `ILLEGIBLE`**, que son plantillas sin rellenar en el origen (`de xx de febrer de 2024`) y no fallos de análisis. **`build_pagines.py` parcheado** (copia en `.abans_ancores`) para leer de `md_contracte` y emitir `id` y `class` del bloque de atributos: **216 de 229 páginas** con encabezados con `id`, lo que permite que el panel enlace **al artículo** (`html/norma.html#art-30`) y no al documento; verificado que el destino responde, que el ancla existe y que la página lleva el enlace al PDF oficial. **Fix en el hub, commiteado aparte** (`e0c3b63`): el chunker exigía `}` justo tras el ancla, así que `{#art-14 .modificat}` **perdía el ancla en silencio**; ahora parsea el bloque completo y expone `ancora`, `classes` y `estat`. |
| 2026-07-29 | (decisión de alcance — `CONTRATO_MD_CORPUS.md` v2, quince enmiendas) | **No mueve el cursor (ING.0.5, bloque en pausa).** Reescrito el contrato del `.md` con las **quince enmiendas** de `PROPOSTA_ESMENES_CONTRACTE_MD.md`, medidas sobre la conversión completa de los 229 documentos, más un **reencuadre de alcance** que pidió el usuario y que corrige un error de encuadre mío. **El error**: la v1 trataba la jerarquía y las anclas como requisito, y apuntaba al corpus equivocado. Con la estrategia real —Niveles 0-2 inyectan 1-3 normas **completas**, el 99,7 % de la normativa propia— **el chunker no interviene sobre las ~380 normas UJI**: no hay fronteras de fragmento que proteger ni ruta que reconstruir. Y el corpus que sí se trocea, las ~22 leyes BOE/DOGV, llega de `legistools` como **XML consolidado** con la estructura de artículos ya marcada, así que las anclas salen del XML sin clasificar encabezados a mano. **Nueva regla de prioridad (§0 del contrato)**: (1) las anclas son **oportunistas, no obligatorias** —se emiten donde el patrón es inequívoco, 752 de 1.115 encabezados, y no se fuerzan en el resto, porque su único consumidor es el enlace profundo y un ancla ausente degrada a citar el documento—; (2) la jerarquía por tipo se mantiene porque es barata y resuelve el 78 %; (3) **lo obligatorio es que el contenido esté completo**: los **40 artículos sin cuerpo** y el artículo 19 del Convenio ausente del DOGV castellano fallan en TODOS los modos de recuperación y son el trabajo que paga, por delante de cualquier refinamiento de la taxonomía; (4) para BOE/DOGV no se escribe `.md` a mano. **Cuatro decisiones que pedía la propuesta**: prefijos `res-`/`norma-` aceptados (nomenclatura, no diseño); `div-` aceptado con ancla oportunista; **anclas estructurales suprimidas** (opción a — bajó las duplicadas de 127 a 21, y el hub reconstruye la ruta del texto); cabeceras institucionales y aparato del DOGV **se degradan** en el `.md` que alimenta al asistente (crean frontera de fragmento y ruta que no informa), y lo que necesite la vista de publicación es asunto del generador de HTML. **Enmiendas destacadas por impacto**: la 4 (`Artículo 5.5` produce anclas **falsas**, no ausentes — una cita a `art-5` resolviendo a un apartado equivocado es peor que no citar), la 1 (escribí «un anexo no contiene artículos» y el corpus lo desmiente en 190 anclas; ahora `{#annex-1-art-1}`), la 8 (número y rúbrica en encabezados separados con jerarquía invertida: 784 encabezados, el defecto más numeroso) y el aviso del punto 11 (la regla «artículo sin cuerpo = entrada de índice» es falsa y destruía 39 encabezados legítimos). Añadidos el Anexo A con las cifras fechadas (enmienda 15) y el Anexo B de trazabilidad. Plan actualizado en ING.0.4 para reflejar v2. Sin cambios de código: el chunker ya degradaba sin anclas. |
| 2026-07-28 | ING.0.4 (RED/GREEN) — Chunker jerárquico de 5 niveles + anclas + troceado de tablas | **Cursor: ING.0.4 ✅ → ING.0.5.** `MarkdownChunker` pasa de 3 a **5 niveles** de encabezado, extrae el ancla `{#art-14}` del propio encabezado a `chunk_metadata['ancora']`, **la quita del texto** (ruido para el embedding y para el usuario) y guarda la **ruta de ancestros** en `chunk_metadata['ruta']`, de modo que un artículo recuperado aislado no pierde el capítulo que le da sentido. Nuevo `services/retrieval/citations.py:with_anchor()`, cableado en `vector_strategy`: la cita apunta al **artículo** del que sale la evidencia (el ancla del chunk mejor puntuado), no al documento de 40 páginas; sustituye un fragmento previo en vez de acumularlo. **Troceado consciente de tablas**, que es la parte que descubrimos midiendo el corpus: los bloques `TABLA-TEXT` se detectan, se tratan como **unidad** si caben en `table_chunk_size` (4.000 car. por defecto, que deja enteros 51 de los 54 bloques medidos) y, si no caben, se parten **por filas repitiendo la cabecera**; sin eso, todo fragmento menos el primero quedaba con importes sin nombre de columna, que es peor que no tener el dato porque se recupera igual y sostiene una respuesta segura y falsa sobre una cuantía. La procedencia del marcador (`taula_origen`, `pagina`, `dimensions`, `taula_format`) va a `chunk_metadata`, y el formato se lee del **último campo del marcador**, no se adivina del contenido. **Defecto encontrado contra el corpus real y corregido**: el corpus curado mete la **leyenda dentro** del bloque («**Retribucions del professorat…**»), y tomar la primera línea por cabecera convertía la cabecera real en fila de datos; ahora la cabecera es la línea que **precede al separador** `| --- |` y el prefijo repetido incluye la leyenda. **`section_path` retirada por Caso B** (migración `b9k0l1m2n3o4`): declarada desde el esquema inicial y jamás escrita, se leía en 4 sitios que la pasaban como `None`; la ruta estructural vive ahora donde tiene sentido, en `chunk_metadata['ruta']`. Hay test que falla si reaparece una referencia en `app/`. **Verificación de cierre sobre el corpus real convertido** (`publicacio_transparencia_2026-07/md_contracte/`, 198 ficheros con ancla): **229 ficheros → 12.044 chunks**, 59 fragmentos de tabla (8 de bloques partidos), **0 fragmentos con importes y sin fila de cabecera**, **0 tokens `{#…}` sin limpiar**, y el caso canónico —la tabla de retribuciones del Convenio colectivo— hereda `ancora='art-51'` con su procedencia (`t02.png`, pàg. 21, 2x6). **Tests**: `test_chunker_hierarchy.py` **34** (RED primero, 29 fallos). Regresión: **569 passed** en `unit`+`public_graphs`. **Desviaciones documentadas**: (1) el criterio de done pedía regenerar chunks con `corpus_recalculator`, que no es posible todavía porque el corpus no está cargado —lo hace ING.0.5— y se sustituye por la verificación directa contra los ficheros reales, que es más fuerte; (2) `tests/modules/agents_hub/integration` **crashea con `Windows fatal exception: access violation`** al importar `langchain_text_splitters` desde `test_full_pipeline.py`; comprobado con `git stash` que **ocurre igual en árbol limpio**, así que es preexistente y ajeno a este prompt. |
| 2026-07-28 | ING.0.3 (RED/GREEN) — Front-matter como portador + manifiesto derivado | **Cursor: ING.0.3 ✅ → ING.0.4.** Nuevos `ingestion/corpus/frontmatter.py` y `ingestion/corpus/manifest.py`. **La decisión central, con su test**: `hash_markdown_body()` calcula el SHA-256 **sobre el cuerpo**, con el front-matter fuera, de modo que reetiquetar una norma no cambia el `content_hash` y no dispara re-troceado ni re-embedding; cambiar el texto sí. Es el único punto donde se hashea un `.md` curado —si alguien llama a `hash_content(raw)` directamente el invariante se rompe en silencio, y está dicho en el docstring—. **Dos normalizaciones añadidas durante el GREEN**, cada una con test, porque sin ellas el invariante era falso en la práctica: (1) se descartan las líneas en blanco iniciales del cuerpo, así un `.md` que **gana** front-matter después conserva su hash; (2) CRLF→LF, porque el corpus se produce en Windows y el sync puede entregarlo con LF, y el mismo documento con distinto final de línea no debe reingerirse. El parser exige que el `---` abra en la **primera línea**: un `---` suelto más abajo es un separador horizontal, que aparece a menudo en el corpus convertido, y hay test de que no se confunde. Tolera BOM. Front-matter que no es un mapa o con YAML roto → `FrontmatterError`; sin front-matter → `({}, cuerpo)`, que no es error (histórico y BOE no lo traen). **Contrato**: `CorpusDocumentEntry` (Pydantic frozen, `extra="forbid"`) con las tres reglas que rechazan el paquete antes de tocar la BD —`regulation` exige `revisat_per`+`revisat_el`, `us_assistents: no` exige `motiu_exclusio`, y rutas relativas sin `..`, sin absolutas POSIX ni Windows, sin UNC, solo `.md`/`.markdown`— y `CorpusManifest` que además rechaza rutas repetidas. **Las claves desconocidas del front-matter van a `extra`** (→ `doc_metadata`) en lugar de romper la carga: es lo que permite que los 56 campos del esquema fluyan sin que el contrato los enumere, y tiene test. `merge_frontmatter()` implementa «manda el front-matter» campo a campo, conservando lo que el front-matter no menciona. `assert_vocabulary()` valida los códigos de **todas** las entradas de una vez y el error **enumera todos** los desconocidos con el fichero en que aparecen (quien prepara 380 normas necesita la lista completa, no el primer fallo); no consulta el vocabulario si no hay códigos. `versio_idiomatica_de` es la **referencia** de la canónica (`id_publicacio`), no un UUID: al escribir el `.md` ese id no existe; lo resuelve ING.0.5. **Desviación documentada**: el prompt decía que `normativa_uji_log.json` «se puede transformar a este formato», y no es cierto —es un **log de ejecución** del conversor (recuentos de ok/skipped/errores y rutas de PDF fallidos), sin un solo metadato por documento—. El catálogo real es `cataleg_metadades_amb_resum.csv` (`id;fitxer;titol;tipus;materia;categoria_actual;idioma;organ_emissor;data_aprovacio;estat_vigencia;n_caracters;resum_abstractiu;resum_extractiu_suport`), y el mapeo queda documentado en el módulo y en `docs/CONTRATO_MD_CORPUS.md` con una regla dura: **`materia`/`categoria_actual` NO se mapean a `ambit_principal`/`submateries`**, porque el eje viejo mezcla cuatro ejes y automatizarlo importaría al índice la incoherencia que el informe midió (3 documentos etiquetados «Gestió econòmica» frente a 121 seleccionados por la vicegerencia); se conservan como `materia_antiga` para trazabilidad. **`PyYAML` declarado como dependencia real** en `pyproject.toml` + `uv lock`: estaba disponible como transitiva y la ingesta la importa en producción, que es la misma deuda que el informe señalaba con Docling. **Tests**: `test_corpus_manifest.py` **45** (RED primero, 44 fallos), cubriendo parser, invariante del hash, rutas, revisión obligatoria, enumeraciones, `extra`, precedencia del front-matter, manifiesto JSON/YAML y validación de vocabulario. Sin regresiones: **575 passed** en `unit`+`integration`+`infra`, con los 10 preexistentes de `test_site_model.py` y ninguno nuevo. Backend puro → sin `.bat`. |
| 2026-07-28 | (falsa alarma de rendimiento — la suite NO tarda 4 h) | **No mueve el cursor. Entrada conservada para que nadie repita el diagnóstico equivocado.** Al cerrar ING.0.3, `unit`+`integration`+`infra` reportó **16.995 s (4:43:15)** y se anotó como problema de coste, con hipótesis de contención de Postgres entre las fixtures de BD desechable. **Era falso**: el proceso corría en segundo plano y la máquina estuvo suspendida entre la jornada de mañana y la de tarde, y pytest mide reloj de pared, no CPU. Medido después por partes: `unit`+`integration` **199 s** + `tests/infra` **126 s (61 passed)** = **~5 min 25 s de trabajo real**. Sin problema de coste, sin necesidad de tocar las fixtures por rendimiento, y las de BD desechable valen para CI. **Lo que sigue pendiente y no tiene nada que ver**: los 10 de `test_site_model.py` que fallan en ejecución conjunta y pasan en solitario (`IntegrityError` de FK contra `hub_web_sites`, interferencia desde `unit/`, descartado `test_ingestion_storage.py`). Es un problema de **aislamiento**, no de coste. **Lección de método**: al medir con procesos en segundo plano, el reloj de pared no sirve; contrastar por partes antes de escribir una hipótesis en el registro. |
| 2026-07-28 | ING.0.2 (RED/GREEN) — Modelo de datos del documento normativo | **Cursor: ING.0.2 ✅ → ING.0.3.** 17 columnas nuevas en `hub_documents` bajo la regla que evita las 56: *columna de primer nivel solo si algo la filtra, la ordena o la usa como puerta*; el resto del esquema (`rang`, `aplica_a`, `resum_router`, `preguntes_tipus`, `termes_bilingues`, `deroga`…) va a `doc_metadata` JSONB. **Asimetría deliberada en las restricciones**: `nivell_acces`/`us_assistents`/`content_class` llevan `CheckConstraint` (enumeraciones estables con consumidor: el filtrado fail-closed de VIS.1), mientras `ambit_principal`/`submateries`/`estat_vigencia` **no** lo llevan —los dos primeros porque son vocabulario revisable (CLAUDE.md §5) y el tercero porque el catálogo real trae valores como `vigent?`—; hay test parametrizado que falla si alguien añade un CHECK donde no toca. **Sin columna `origen`**: se reutiliza `source_kind` ampliando sus valores documentados a `publicacio`/`boe`, porque duplicar el eje sería deuda. **`ARRAY` del dialecto PostgreSQL y no el genérico**: lo descubrió un test —`AttributeError: ... has no attribute 'overlap'`—, y es la diferencia entre que VIS.1 pueda filtrar con el operador de solapamiento `&&` o no; con el genérico habría que haberlo reescrito en VIS.1. Índices: btree `(chatbot_id, ambit_principal)` y `(chatbot_id, nivell_acces)`, GIN sobre `submateries`, `submateries_internes` y `doc_metadata`, y btree sobre `id_publicacio` (clave de emparejamiento de SYNC.1). **FK de `hub_document_chunks.document_id`** → `hub_documents.id` ON DELETE CASCADE, que VIS.1 necesita para el JOIN: se eligió JOIN y no denormalizar los metadatos sobre el chunk para que reclasificar sea **un** UPDATE y no haya dos copias que divergir; la migración limpia huérfanos antes del ADD CONSTRAINT (0 en esta BD, pero sin eso falla en cualquier BD con datos) y `document_id` sigue nullable porque los chunks temporales de subida no tienen documento. **Deuda de ING.0.1 pagada**: nuevo `services/corpus_reclassifier.py` (edge) con `reclassify_documents(session, axis, codi_antic, codi_nou, chatbot_id=None)`, que usa `array_replace` en SQL, es idempotente (segunda pasada = 0) y **tiene test de que no toca ni un chunk** — es el invariante que sostiene la promesa de vocabulario revisable: si algún día toca chunks, cada revisión del vocabulario pasa a costar un re-embedding del corpus. **Dos fixes de esquema** hallados al comparar la BD de desarrollo con una instalación limpia: (1) `hub_ingestion_jobs.canonical_url`/`.original_filename`, que el ORM declaraba y `POST /hub/ingestion/upload` escribe pero ninguna migración creaba ⇒ **la subida de documentos fallaba con UndefinedColumn en cualquier despliegue limpio** (misma familia que `hub_ingestion_sources` en 11.2), añadidas con guarda `_column_exists`; (2) `hub_interactions.metadata`, inalcanzable desde el ORM porque SQLAlchemy reserva `metadata` en las clases declarativas, retirada por Caso B tras comprobar con grep que nadie la lee. **Migración** `a8j9k0l1m2n3`, aplicada y **probada en los dos sentidos** (`downgrade` a `z7i8j9k0l1m2` y `upgrade head` de vuelta). **Tests**: 34 en `test_document_metadata_model.py` (metadatos del ORM) + 17 en `test_document_metadata_db.py` (BD real: arrays, JSONB, FK y cascada, `&&`, GIN, reclasificación) + 4 en `tests/infra/test_migrations_fresh_install.py`, tres de ellos nuevos y sobre el esquema resultante de `alembic upgrade head` en BD limpia, que es la única forma de cazar la clase de fallo del punto (1). **55 passed**, RED primero (45 fallos iniciales). Sin regresiones: **469 passed** en `agents_hub` unit+integration. **Desviación documentada**: la nota de la migración deja escrito que rellenar las filas existentes con `nivell_acces='public'` **no es fail-closed** y solo es admisible porque todo el corpus cargado hoy viene del crawler de webs públicas; en cuanto entren circulares internas, el valor tiene que venir del front-matter y nunca del default. Backend puro → sin `.bat`. |
| 2026-07-28 | (fix) La suite de integración destruía la BD de desarrollo | **No mueve el cursor.** Hallado al arrancar ING.0.2: tres ficheros de `tests/modules/agents_hub/integration` creaban las tablas del hub con `create_all` sobre `DATABASE_URL` —la BD de desarrollo— y las **destruían con `drop_all`** al terminar cada test. Dos consecuencias verificadas: (1) **cualquier ejecución de la suite dejaba la BD de desarrollo sin esquema del hub**, sellada en head pero con cero tablas `hub_*`; es el estado en que se encontró la BD al arrancar ING.0.1 y el que reapareció al ejecutar la suite justo después de repararla, así que **la causa no era (solo) el renombrado de la BD, era esto**; (2) los 10 fallos de `test_site_model.py` en ejecución conjunta que pasaban en solitario. Nuevo `conftest.py` del directorio con **BD desechable por test**, siguiendo el patrón de `tests/infra/test_migrations_fresh_install.py`; nadie vuelve a hacer `drop_all` sobre la BD del desarrollador, y los 24 tests de los tres ficheros pasan juntos. **Pendiente inventariado**: los 10 de `test_site_model.py` **siguen fallando en la selección completa** `unit`+`integration` (pasan solos y pasan con los otros dos ficheros de BD). El síntoma cambió: ya no es `drop_all`, es un `IntegrityError` de FK contra `hub_web_sites` que apunta a interferencia entre tests desde `unit/` —descartado `test_ingestion_storage.py`—, probablemente por el `event_loop` de sesión del `conftest.py` raíz o por el motor global de `connection.py:get_engine()`. No bloquea ING.0.2 y queda para un prompt propio. |
| 2026-07-28 | ING.0.1 (RED/GREEN) — Vocabulario controlado como dato versionado | **Cursor: ING.0.1 ✅ → ING.0.2.** Nuevo `HubVocabularyTerm` (`config_models.py`, `HubConfigBase` → configuración cloud que se sincroniza a edge), `services/vocabulary_service.py` (edge) y `vocabulary/load.py` (CLI de carga desde CSV). **La decisión de diseño que gobierna el prompt**: los EJES son estructura (`VocabularyAxis` StrEnum: ambit/submateria/rang/colectiu/tipus, porque añadir un eje exige código que lo consuma) y los TÉRMINOS son dato (filas, porque el vocabulario está pendiente de validar por SG). **La tabla NO lleva `CheckConstraint` sobre `codi` ni `axis`, y hay un test que falla si alguien lo añade** — sería justo lo que impide revisar el vocabulario. Renombrar/fusionar = fila nueva + la vieja con `vigent=False` y `substituit_per_codi`; **la cadena de sustituciones es la traza de auditoría**, sin tabla de historial aparte. `resolve()` la sigue hasta el término vigente con **corte por ciclo** (`VocabularyCycleError`, no bucle infinito) y error explícito si la sustitución apunta a un fantasma. `validate()` devuelve **todos** los códigos malos, no el primero: quien valida un documento los necesita de una vez. `build_router_index()` genera el Nivel 0 agrupado por ámbito, excluyendo lo no vigente y omitiendo los ámbitos sin submaterias vigentes. **Frontera edge/cloud respetada**: el servicio no importa el modelo ORM (hay test que lee el fichero y falla si aparece `config_models` o `HubVocabularyTerm`); el acceso va por `ConfigProvider.list_vocabulary`, método nuevo del protocolo, implementado en `LocalConfigProvider`. El CLI valida los `parent_codi` contra el eje `ambit` **antes de escribir nada** y rechaza el CSV entero si alguno falta —un vocabulario a medias hace fallar la validación de documentos de forma aleatoria—, es idempotente por la clave natural `(organizacion_id, axis, codi)` y preserva el estado de vigencia (lo gobierna `supersede_term`, no el CSV). Parser tolerante a lo que trae el fichero real: BOM de Excel (`utf-8-sig`) y comas sin comillas en `normes_exemple`, que desbordan la última columna (`restkey`); `abast` es la descripción que ve el router en las submaterias y `descripcio_router` en los ámbitos. **Desviación documentada**: el prompt pedía `reclassify()` con UPDATE de los documentos afectados, pero las columnas `submateries`/`ambit_principal` de `hub_documents` **no existen hasta ING.0.2**; se implementa `supersede_term()` (lado vocabulario) y el barrido de documentos se hace en ING.0.2. **Migración** `z7i8j9k0l1m2` (+ índice `(organizacion_id, axis)`, que es la consulta caliente). **Bloqueante encontrado, no causado por este prompt**: la BD de desarrollo `govgenai` está sellada en head pero **no tiene ninguna tabla `hub_*`**, así que la migración no puede aplicarse ahí (falla en la FK a `hub_organizaciones`); se validó sobre **BD desechable con la cadena completa** y `tests/infra/test_migrations_fresh_install.py` verde. **Verificación de cierre real** sobre esa BD desechable: `alembic current = z7i8j9k0l1m2`, cero CHECK constraints, **5 ámbitos y 58 submaterias** cargados desde `vocabulari/*.csv`, **segunda pasada `creados=0 actualizados=0 sin_cambios=5/58`** (idempotencia real, no solo en test), e índice del Nivel 0 de **9.242 caracteres ≈ 2.310 tokens** — el informe de materias había medido 2.307, así que el presupuesto del system prompt se confirma con el código. **Tests**: `test_vocabulary_service.py` **40** (RED primero: 40 fallos), cubriendo validate/resolve/ciclos/índice/alcance por organización/frontera/metadatos del modelo/parseo CSV/idempotencia/padres colgantes/dry-run/supersede. Actualizado el guardarraíl `test_edge_boundary.py`, que lleva lista explícita de tablas de configuración y **debe** fallar al aparecer una nueva: `hub_vocabulary_terms` añadida con el motivo. Sin regresiones: **417 passed** en `agents_hub` unit+integration. **Fallos preexistentes verificados contra árbol limpio** (`git stash`): 10 de `test_site_model.py` (solo en ejecución conjunta; pasan en aislamiento) — clean: 10 failed/378 passed, con cambios: 10 failed/417 passed — y 3 `test_brain_*` de `test_imports.py` (módulo `modules/brain` inexistente, ya inventariado). Backend puro → sin `.bat` de pruebas manuales. |
| 2026-07-28 | (replanificación — corpus normativo: ING.0 reescrito + Bloque VIS + Bloque SYNC) | **No mueve el cursor (sigue en ING.0.1, ahora con contenido distinto).** Origen: valoración cruzada entre los prompts de ingesta del plan y la estrategia de recuperación definida fuera del repo — `openwebui-gerencia\INFORME_ESTRATEGIA_ASISTENTE_GERENCIA.md` y, sobre todo, `Descarregar_pdf\normativa_propia\INFORME_MATERIES_I_METADADES_AGENTS.md`, donde la clasificación está cerrada (5 ámbitos, 58 submaterias, esquema de 56 campos en `vocabulari/`). **Diagnóstico verificado contra el código, no sobre el papel**: el Bloque RAG ya estaba alineado con el Nivel 3 de la estrategia (RAG.3 HNSW, RAG.4 tsvector, RAG.6 es literalmente `bge-reranker-v2-m3`, RAG.7/RAG.8), pero el Bloque ING.0 era **incompatible** por seis motivos: (1) el manifiesto tenía `category: str` único donde hacen falta `ambit_principal` + `ambits_secundaris[]` + `submateries[]` + `submateries_internes[]`; (2) **no había dónde persistirlo** — `HubDocument` (`operational_models.py:142-173`) no tiene columna JSON ni `content_class` ni `reviewer`, así que el propio ING.0.2 antiguo («PRESERVA la procedencia en HubDocument») no compilaba sin una migración que ningún prompt contemplaba; (3) **cero filtrado por metadatos** en la recuperación (`retriever.py:63-71,104-112` filtra solo por `chatbot_id`, `is_temporary`/`owner_id` y lengua; no hay un solo operador JSONB en el repo), que es el Nivel 1 y lo que `CRITERIS` §1.4 exige para el control de acceso; (4) los tres modos (`retrieval_mode`, `config_models.py:101-104`) son **excluyentes por chatbot**, mientras la estrategia los necesita combinados por consulta — y `MD_LONG_CONTEXT` inyecta *todo* el corpus con `ValueError` a 128k (`long_context_strategy.py:41-46`) y `MD_AGENT_SELECTOR` en CoreGraph es un stub que devuelve todos los documentos (`md_agent_selector_pipeline.py:64`); (5) la vigencia solo existe atada a `HubCrawledPage.superseded` y **long-context y agentic no la aplican en absoluto** — un derogado se inyecta entero; (6) bilingüe con modelos opuestos (el hub indexa val y es como documentos distintos; la estrategia indexa solo la canónica). **Cambios**: `Bloque ING.0` pasa de 2 a **5 prompts** (vocabulario como dato / modelo de datos / front-matter+manifiesto / chunker 4 niveles+anclas / CLI de carga); **Bloque VIS nuevo** (3 prompts: filtro de metadatos en SQL con `nivell_acces` fail-closed, Niveles 0/1/2 con el índice de submaterias —2.307 tokens medidos, frente a los ~72k del catálogo de fichas que no cabe—, canónica bilingüe + advertencia de vigencia); **Bloque SYNC nuevo** (2 prompts: sync con la BD de publicación vía el servicio MCP `execute_dataset` de la UJI, y caducidad activa). **Cuatro enmiendas in situ** al Bloque RAG: RAG.2 no cementa el índice de documentos, RAG.4 lleva los pares bilingües al `tsvector`, RAG.5 deja de crear `context_token_budget` (lo crea VIS.2), RAG.7 embebe solo contexto estructural. **Decisiones del usuario que cierran el diseño (2026-07-28)**: (a) el vocabulario es una propuesta pendiente de validar por SG y **debe seguir siendo modificable** ⇒ términos en tabla versionada + prohibición de embeber taxonomía (reclasificar = un `UPDATE`, no un re-embedding); (b) el `.md` autoritativo vivirá **en la BD de publicación** y de ahí saldrán PDF y HTML ⇒ el sync sustituye al crawler, que **queda desactivado para este corpus** (fuente autoritativa única, `CRITERIS` §3.2); (c) **las circulares de Gerencia pasan por el mismo circuito de publicación** con `nivell_acces: intern` ⇒ un solo maestro, el hub es réplica, y **no se construye UI de metadatos** (sin código especulativo); (d) los `.md` llegarán con **5 niveles de encabezado y ancla por unidad citable** (especificación en `docs/CONTRATO_MD_CORPUS.md`), también retroactivamente sobre el histórico ⇒ el parser estructural de la tarea 8 del informe **no se implementa en el hub**; (e) el deploy puede retrasarse, prioridad a los fundamentos. **Desviación documentada**: no se añade columna `origen` — se reutiliza `source_kind` ampliando sus valores a `'publicacio'`/`'boe'`, para no duplicar el eje. **Deuda detectada y encauzada**: `hub_document_chunks.document_id` no tiene FK (la añade ING.0.2, que VIS.1 necesita para el JOIN) y `HubDocument.section_path` está declarada y jamás escrita en todo `server/app` (ING.0.4 la puebla o la retira por Caso B). Total nuevos prompts Fase 1: **+8** (ING.0 de 2 a 5, VIS×3, SYNC×2). Sin código tocado en esta sesión. **Ampliación del mismo día (dos transportes)**: el usuario informa de que el pipeline de publicación **no estará hasta dentro de meses**, así que el CLI no es una carga inicial sino **el mecanismo de mantenimiento del corpus** durante ese tiempo, y después debe convivir con el transporte programático. Para que eso no genere dos pipelines divergentes, la **reconciliación se traslada de SYNC.1 a ING.0.5** detrás de un protocolo `CorpusSource`: `LocalDirectorySource` ahora, `PublicationMcpSource` en SYNC.1 (que baja de Opus a Sonnet y pasa a ser un adaptador; sus tests de reconciliación se parametrizan por fuente para que carpeta y MCP den el mismo resultado). Cuatro decisiones nuevas: (1) **lo incremental no es un modo** — emerge del hash; (2) **el censo sí es un modo y es peligroso** — `--prune` solo si la fuente declara corpus completo, con **salvaguarda de proporción** (aborta por encima del 10 % y exige `--force-prune`), porque un `--dir` mal escrito retiraría el corpus entero; un dataset de deltas devuelve `is_census() == False` y por tanto nunca poda; (3) **`last_seen_at`** en `hub_documents` como mecanismo de la poda (se marca lo que el último censo no vio, no lo que falta en una carga parcial), y **`id_publicacio`** indexado desde el primer día aunque venga vacío, con fallback `url_oficial`+`language`, para que el día del cambio de transporte sea una continuación y no una recarga; (4) **un `HubIngestionJob` por ejecución** con seis recuentos, porque si el CLI va a mantener el corpus durante meses el historial tiene que estar en la BD y en el admin, no en la consola de quien lo ejecutó. Añadida al cierre del bloque la verificación a mano de la idempotencia real (segunda pasada = todo omitido; tercera con un `.md` reetiquetado = metadatos actualizados y cero re-embeddings) y la documentación del procedimiento de actualización por CLI en `docs/`. **`CLAUDE.md`**: las dos reglas duras (vocabulario como dato, taxonomía nunca embebida) añadidas como §5 de las Reglas Maestras de Arquitectura, a petición del usuario. |
| 2026-07-27 | SEC.6 (RED/GREEN) — Validación de subidas: magic bytes + límite de tamaño [M4] | **Cursor: SEC.6 ✅ → ING.0.1.** Nuevo `server/app/core/uploads.py` con `validate_upload(file, kind, max_bytes)` y `UploadKind.PDF` / `UploadKind.TEXT`. **Deja de mirarse `content_type`**: lo fija el cliente en el multipart y es spoofeable; ahora se valida **extensión + magic bytes** (`%PDF-`) → 415. Para los formatos de texto, que no tienen firma, se rechaza el binario disfrazado por **byte NUL** — durante el GREEN se descubrió que decodificar UTF-8 **no basta**, porque `\x00\x01\x02\x03` es UTF-8 perfectamente válido y colaba un binario en un `.md`; el NUL es la señal que usa git para lo mismo. **Corte por tamaño durante la lectura** en trozos de 64 KB sobre un `SpooledTemporaryFile` (RAM hasta 1 MB, disco después) → 413 sin llegar a retener el fichero; hay test que cuenta bytes leídos y falla si el validador se traga más de lo necesario. `MAX_UPLOAD_MB` (default 10) y `MAX_DOCUMENTS_PER_CHATBOT` (0 = sin límite) por entorno, añadidas a `.env.example` y a `scripts/generate_env.sh`. **Cuota de documentos por chatbot** → 429, como función **pura** `assert_within_document_quota(recuento)` que recibe el conteo ya calculado: `core/` es capa compartida y no debe importar modelos operacionales de `agents_hub` (frontera edge/cloud), así que la consulta `func.count()` vive en el router. **Duplicación eliminada**: los dos endpoints de subida (`api/v1/ingestion.py` user-upload y `routers/hub_ingestion_router.py` upload) tenían cada uno su propia comprobación de MIME y su propio límite (10 MB hardcodeado en uno, ninguno en el otro, que además hacía `await file.read()` completo); ahora ambos llaman a la validación compartida. **Reutilizable por el Bloque ING** tal como pedía el prompt: `UploadKind.TEXT` acepta `.md`/`.markdown`/`.txt`. **Desviación documentada**: el prompt mencionaba "cuota por organización/chatbot (nº **o** tamaño acumulado)"; se implementa por **número** de documentos, que no requiere migración —no hay columna de tamaño en `HubIngestionJob`— y satisface el "o" del enunciado. **Tests**: `tests/api/test_upload_validation.py`, **16** (RED primero): pdf válido, ejecutable renombrado a `.pdf` con content_type correcto, extensión no admitida, sin nombre, vacío, exceso de tamaño, corte real de lectura, fichero exactamente en el límite, markdown para ING, binario disfrazado de `.md`, `PDF` rechazando `.md`, default desde settings, y los tres de cuota (0 = ilimitado, límite alcanzado, por debajo). Sin regresiones: **65 passed** en todo lo que toca ingestión/upload y **352 passed** en unitarios de `agents_hub`. Comprobado además que el frontend no ramifica por código de estado en la subida, así que el cambio 422→415/413 no lo rompe. Backend puro → sin `.bat` de pruebas manuales. |
| 2026-07-27 | 11.3 (RED/GREEN) — Script de Inicialización y Semillas (**cierra Fase 11**) | **Cursor: 11.3 ✅ → SEC.6. Bloque Fase 11 COMPLETO.** Dos piezas: **`server/app/scripts/bootstrap.py`** (sembrado idempotente, patrón de `seed_llm_configs.py`) y **`scripts/setup.sh`** (orquestación). El sembrado crea SuperAdmin, proveedor `ollama`, config de LLM por defecto, organización, `Chatbot de Ejemplo` y sus **prompts de bienvenida en es/ca/en con texto distinto por idioma**; cada entidad se busca por su **clave natural** antes de crearse (email, `name` de chatbot —que es UNIQUE—, y la tripleta `chatbot_id+slug+language`), de modo que la segunda pasada no duplica nada. **Decisión**: no se rehashea ni se pisa el SuperAdmin existente — si el admin cambió la contraseña tras instalar, reejecutar `setup.sh` no debe revertirla. El script cubre los 5 pasos del prompt: requisitos (docker, demonio vivo, puertos 80/443/5432 comprobados **todos** antes de abortar, para no descubrirlos de uno en uno), stack, migraciones, sembrado y resumen con URLs. Flags `--non-interactive` (credenciales por entorno), `--dry-run`, `--skip-checks`, `--skip-stack`, `--compose-file`. **Portabilidad Linux/macOS** (criterio del prompt): shebang `/usr/bin/env bash`, `set -euo pipefail`, bash 3.2 —sin arrays asociativos ni expansiones de caja— y sondeo de puertos con la redirección `/dev/tcp` de bash en vez de `nc`/`lsof`/`ss`, que no están garantizados. **Tres defectos encontrados y corregidos durante el GREEN**: (1) `--dry-run` sin `--non-interactive` se quedaba pidiendo credenciales pese a no ir a crear nada; (2) el bucle de `read` giraba **infinito** sin terminal (CI o tubería) —ahora `leer_o_abortar` detecta EOF y sale con instrucciones—; (3) `docker compose exec` **no hereda el entorno del host**, así que en la ruta con stack la contraseña nunca habría llegado al contenedor y el sembrado habría abortado: añadido `-e SUPERADMIN_PASSWORD`. La contraseña viaja siempre por entorno, nunca por línea de comandos (`ps` la expondría) y no se imprime jamás. **Tests** (RED primero, 19 fallos iniciales): `test_setup_script.py` **12** (interfaz, requisitos, plan, no-interactivo, resumen, portabilidad bash 3.2) + `test_bootstrap_seed.py` **8** contra BD desechable con migraciones reales (hash bcrypt, 3 idiomas con textos distintos, idempotencia por inventario de 5 tablas, no-sobrescritura del hash). Verificado además a mano contra la BD de desarrollo: 1ª pasada crea, 2ª informa "ya existía" y los recuentos no se mueven (chatbots=1, prompts=3, orgs=1). Suite `server/tests/infra`: **58 passed, 0 skipped**. |
| 2026-07-27 | 11.2 (GREEN + guardarraíl) — Docker Compose de Producción "One-Click" | **Cursor: 11.2 ✅ → 11.3.** `docker-compose.prod.yml` levanta el stack completo con un comando: **app** (build del `Dockerfile` raíz), **frontend** (build multi-stage Node→Nginx con `frontend/Dockerfile` + `nginx.conf` que proxya `/api` al backend y sirve el fallback SPA), **postgres** (`pgvector/pgvector:pg16`), **minio**, y **ollama opcional** (`profiles: [ollama]`, no arranca por defecto). Volúmenes persistentes `postgres_data_prod` / `minio_data_prod` / `ollama_data`. **Topología de red verificada**: `sandbox-net` es `internal: true` y solo la comparten `script-sandbox` y `app`; `app` está además en `default`, de modo que el sandbox queda sin salida a Internet (requisito SBX) mientras el backend conserva egress hacia los proveedores LLM. Arranque ordenado: `migrate` (one-shot) → `app` (`service_completed_successfully`) → `frontend` (`service_healthy`). `name:` distinto en dev y prod para que `docker compose -f docker-compose.yml up` no recree contenedores de producción. **Fixes de migraciones para instalación limpia** (4 revisiones): `hub_ingestion_sources` no existe en una BD nueva —la crea código fuera de la cadena y 9Q.0 la retira— así que las migraciones que le añadían columnas ahora comprueban `_table_exists` antes de tocarla; sin esto `alembic upgrade head` fallaba en un despliegue desde cero. **Guardarraíl añadido en esta sesión**: `test_every_service_healthy_dependency_has_a_healthcheck` — el servicio `app` NO declara healthcheck en el compose porque lo hereda del `HEALTHCHECK` de su Dockerfile (Docker expone el estado de salud venga de donde venga), un caso sutil que el resto de tests no cubría; el test valida ambas procedencias y se verificó por mutación que falla al retirar el `HEALTHCHECK` del Dockerfile. **Tests**: `server/tests/infra/` **36 passed, 1 skipped** (el skip es `test_migrations_fresh_install.py`, que requiere Postgres levantado). Trabajo de compose implementado en la sesión del 2026-07-15 que quedó sin commitear ni reflejar en el cursor; esta entrada lo cierra. Sin `.bat`: infraestructura declarativa, la verificación de arranque real va en las pruebas manuales del bloque. |
| 2026-07-27 | (planificación — chatbots autenticados, cuotas por usuario y vigencia temporal) | **No mueve el cursor (sigue en 11.2).** Origen: al definir el piloto de gerencia con Open WebUI se detectó la necesidad de (a) chatbots usables **solo autenticados** (gestión interna para personal), (b) **máximo de tokens por usuario**, y (c) **límite de uso temporal** en chatbots públicos. **Revisión del código real, que corrige dos premisas**: (1) el chat **ya es autenticado hoy** — `api/v1/hub_chat.py:96-103` exige `get_current_user` + `require_scopes("chat:test")`, y `require_scopes` no filtra sesiones humanas (`deps.py:85-94`), así que sin SEC.2 cualquier usuario autenticado puede conversar con cualquier chatbot de cualquier organización; lo que falta es el camino **anónimo** (D.1); (2) no existe **ningún** campo de visibilidad en `HubChatbot` (solo `is_active`) **ni contabilidad de tokens** en `HubInteraction` — sin esta última no hay cuota posible, solo conteo de peticiones, y no estaba en ningún prompt del plan. **Verificación de OWUI (fuentes upstream)**: su RBAC de modelos es real (private/public/grupos) pero "public" significa *todos los usuarios autenticados de la plataforma*, no anónimos, y su modelo de permisos es **aditivo sin "deny"** (no puede expresar `restricted` fail-closed); las **cuotas por usuario NO existen de forma nativa** (peticiones abiertas #23323, #1430, #6692, discussion #3607) y los despliegues grandes las resuelven con **LiteLLM sidecar** o el plugin externo `openwebui-token-tracking` — ambas descartadas aquí (LiteLLM duplicaría `model_factory` y sacaría el gasto del perímetro auditado; el plugin es la Opción B ya descartada en `docs/DECISION_OPENWEBUI_CARCASA_CHAT.md` §3). **Conclusión: OWUI no puede ser la frontera** — si el gate vive allí, el backend sigue sirviendo el chatbot a cualquier PAT, API key de widget, frontend React o servidor MCP. **Cambios en el plan** (`Plan_TDD_Fase1.md`): **2 prompts nuevos** — **SEC.2.1 (Opus)** `access_mode` ('public_anon'/'authenticated'/'restricted') + `allowed_roles`/`allowed_saml_groups` (ARRAY, sin tabla de grants) + helper único `assert_chatbot_access(actor, chatbot, via)` consumido por hub_chat/adaptador OpenAI/widget + migración **fail-closed** (existentes a 'authenticated') **y** Parte 2 de identidad delegada: cabecera `X-GovGenAI-Actor` (JWT HS256, `exp`<=300s, `aud`), scope `chat:onbehalf`, `resolve_effective_actor` que **hereda `organizacion_ids` del PAT y nunca de la cabecera** (anti-escalada); **SEC.4.1 (Sonnet)** `valid_from`/`valid_until`/`total_token_budget`/`unavailable_message` con **estado derivado, no persistido** (sin `closed_reason`, sin voltear `is_active`) porque el contador acumulado es dato operacional y `HubChatbot` es config que se sincroniza cloud→edge. **3 ampliaciones**: **SEC.4** pasa de cuota por chatbot/día a **contabilidad real de tokens** (`prompt_tokens`/`completion_tokens`/`cost_estimated` en `HubInteraction`, `usage_source='provider'` \| `'estimated'`) + contador operacional nuevo `HubUsageCounter(subject_type, subject_id, window_key)` con UPSERT atómico + **cuotas en cascada** usuario/día→usuario/mes→chatbot/día→organización/mes→IP/día + `GET /hub/usage/me`; **D.1** el endpoint del widget pasa por las tres guardas en orden (acceso→disponibilidad→cuota) y **la API key sola deja de autorizar** (403 si `access_mode != 'public_anon'`); **OWUI.1/OWUI.3** `/v1/models` filtrado por `assert_chatbot_access`, traducción de 429/403 a errores OpenAI legibles, el Pipe firma la cabecera de actor con `ACTOR_SIGNING_SECRET`, y sección obligatoria "Lo que NO se usa de Open WebUI, y por qué". **Decisión del usuario (a)**: identidad propagada por cabecera firmada, **no** un PAT por usuario (no escala a cientos de personas ni sobrevive a las bajas). Total nuevos prompts Fase 1: **+2** (SEC.2.1, SEC.4.1) → el Bloque SEC pasa de 7 a **9 prompts**. No afecta a la Fase 11 ni al cursor. |
| 2026-07-15 | Planificación Bloque RAG (fase 2: detalle verbatim) | **Cursor: sin cambio (11.2).** Segunda pasada del Bloque RAG en `Plan_TDD_Fase1.md`: los 14 prompts pasan de descripción corta a **detalle verbatim ejecutable** al estilo SEC/ING.0 (secciones de implementación con ficheros reales, migraciones Alembic explícitas, tests `should_*` enumerados RED-primero, criterio de done con cifras de RAG.1 obligatorias en cada mejora del retriever). Decisiones cerradas en el detalle: umbral `min_retrieval_score` sobre similitud coseno pre-fusión (la rama léxica no filtra); `context_token_budget` como campo nuevo en cascada (chatbot/org/plataforma, default 4000); **`reranker_enabled` migra su default heredado True→False** (nunca se consumió; el admin lo activa tras validar con datos); parent-child con columna `parent_content` (sin join); un solo modelo de embedding por despliegue edge (mezcla ⇒ error explícito + CLI re-embed); `fallback_reason` en `HubInteraction` (RAG.2) como señal para RAG.14; query rewriting y bypass gateados (default OFF / scope `chat:debug`). Verificado contra código real (rutas de `agents_hub`, flags en `config_models.py`/`config_resolver.py`, `corpus_recalculator.py` reutilizado para regeneraciones). Bloque listo para ejecutar tras las pruebas manuales post-ING.0. |
| 2026-07-15 | Planificación Bloque RAG (fase 1: relación de prompts) | **Cursor: sin cambio (11.2).** A partir de la comparativa arquitectónica del RAG con LAMB (`docs/COMPARATIVA_RAG_LAMB.md`, creada hoy: diseño, ingesta, embeddings, retrieval, integración LLM, evaluación, + análisis "RAG vs agentes"), se crea el **Bloque RAG** en `Plan_TDD_Fase1.md` con la **relación de 14 prompts** (RAG.1 dataset dorado+CI de retrieval; RAG.2 consolidación de grafos CoreGraph→producción **Opus**; RAG.3 HNSW; RAG.4 tsvector+GIN; RAG.5 umbral+presupuesto tokens; RAG.6 reranker BGE-reranker-v2-m3 **Opus**; RAG.7 contextual retrieval; RAG.8 parent-child+chunking configurable; RAG.9 metadato embeddings+re-embedding; RAG.10 query rewriting conversacional; RAG.11 bypass/debug; RAG.12 progreso granular jobs; RAG.13 test scenarios; RAG.14 feedback→huecos de corpus). Decisiones del usuario: alcance completo, posición tras pruebas manuales/antes de SEC.1-5,7, y **planificación en dos fases** — hoy solo la relación; el **detalle verbatim queda pendiente de 2ª pasada**. `docs/Arquitectura.md` ampliada: nueva §7.5 (estrategia de recuperación y evolución agéntica) y §13.2 (stack RAG: pgvector+HNSW, FTS+RRF, reranker, dataset dorado). Sin cambios de código. |
| 2026-07-13 | 11.1 (RED/GREEN) — Generador de Configuración .env.example | **Cursor: 11.1 ✅ → 11.2.** `scripts/generate_env.sh`: script bash idempotente que genera `.env` (raíz, leído por docker-compose), `server/.env` (copia exacta, leída por `config.py` fuera de Docker) y `frontend/.env` (`VITE_API_URL`), con secretos aleatorios en cada ejecución (JWT_SECRET_KEY, LangFuse NEXTAUTH/SALT/ENCRYPTION, Postgres/MinIO passwords, `AUTOMATIA_SIGNING_KEY` como RSA-2048 PEM válida vía `openssl genrsa`). Flags `--mode local\|cloud` (default `local`: STORAGE_BACKEND=file, sin Ollama key), `--force` (regenera; sin él conserva ficheros existentes), `--output-dir` (testabilidad). **`.env.example` (raíz) reescrito** y verificado 1:1 contra las variables realmente leídas en `config.py`/`model_factory.py`/`mcp_server/config.py` (grep exhaustivo de `os.getenv`/`os.environ.get`); añadida sección "CONECTIVIDAD INSTITUCIONAL (MCP)" ausente hasta ahora; retirada `SHOW_ADMIN_TAB` (muerta, sin lectores en el código — resto de NiceGUI `ui/`). **`.env.production.example` retirado** (Caso B: huérfano, consolidado en el `.env.example` único + el generador con `--mode cloud`); limpiada la excepción correspondiente en `.gitignore`. Nuevo `frontend/.env.example` (documenta `VITE_API_URL`). **Tests** (TDD): `server/tests/infra/test_env_generator.py`, 10 tests que invocan el script vía subprocess sobre directorios temporales (nunca tocan el `.env` real): creación de los 3 ficheros, unicidad de secretos entre ejecuciones, validez de la PEM RSA (verificada con `cryptography.load_pem_private_key`), idempotencia sin `--force` / regeneración con `--force`, modo local por defecto, sección MCP presente, `frontend/.env` con `VITE_API_URL`, rechazo de `--mode` desconocido, y paridad `.env`↔`server/.env`. RED confirmado (10/10 fallando sin script) → GREEN (10/10 verdes). **CI**: se detectó que `server/tests/infra/` (incluido el test SBX.4 preexistente `test_compose_sandbox.py`) nunca se ejecutaba en el pipeline — añadido al step "Run unit tests" de `.github/workflows/ci.yml`. Verificado localmente el conjunto exacto de CI (incluyendo `tests/infra/`): **364 passed**. No requiere `.bat` de pruebas manuales (script CLI puro, sin interacción de navegador). |
| 2026-07-13 | ROL.2 (RED/GREEN) — Renombrado institucional en el frontend | **Cursor: Bloque ROL ✅ COMPLETO → Fase 11 (11.1).** Frontend propagado desde el contrato de ROL.1. **Orval**: `npm run generate:api` regeneró tipos/hooks desde `openapi.json`; nuevo `hub-organizaciones/` + `organizacion{Read,Create,Update}`; borrados los generados obsoletos `hub-clients/` y `client{Read,Create,Update}` (se conserva `edgeClientOut`, concepto edge distinto). **API manual**: `shared/api/clients.ts`→`organizaciones.ts` (endpoints `/api/v1/hub/organizaciones`, funciones `fetch/create/update/deleteOrganizacion`). **Páginas/rutas**: `ClientsPage`→`OrganizacionesPage`, ruta `/hub/clients`→`/hub/organizaciones` (App.tsx + HubLayout sub-nav). **Roles**: literales `'admin'/'partner'`→`'superadmin'/'admin'` en redacción (ReportTemplateBuilder, LLMDraftPreview, AdminScriptReviewQueue, BlockDebugPanel) y `AccessTokensPage.scopesForRole` (superadmin=ALL, admin=ALL−`chatbots:write`); `AuthContext` es role-agnóstico (role: string), sin cambios. **Chatbot**: `client_id`→`organizacion_id` en `ChatbotsPage` y mocks de test. **i18n** es/ca/en: claves `client*`→`organizacion*`, valor `organizacion_admin` "Partner ID"→"Admin ID". **Tests**: `tsc -b` sin errores; Vitest **214/214** verde (+ a11y **10/10**); tests renombrados (`OrganizacionesPage.test`, `admin-organizaciones.a11y.test`) + nuevo `rol2_rename.test` (i18n keys, nav /organizaciones, techo de scopes por rol). **.bat** `pruebas_manuales_promptROL_2.bat` (cp1252, login por rol + navegación). **Desviación documentada**: los tests de "routing por sección de rol" del prompt presuponían infra de rutas gateadas por rol que NO existe (PrivateRoute sólo comprueba auth); por "sin features no pedidas" se hizo el renombrado fiel y se testeó el rol donde vive (techo de scopes `scopesForRole`), sin crear secciones nuevas. |
| 2026-07-13 | ROL.1 (RED/GREEN) — Renombrado institucional en el backend | **Cursor: ROL.1 ✅ → ROL.2.** Backend renombrado y migrado. **Roles** (`core/auth/models.py`): enum `superadmin \| admin \| user` (+`informer` **preservado**: no está en el mapa de renombrado y retirarlo rompería el mapeo SAML; decisión documentada); `is_superadmin`/`is_admin`. **Gates** (`api/deps.py`): `require_superadmin` (solo superadmin) y `require_admin` (superadmin+admin), propagados a todos los routers vivos; PAT scopes (`superadmin`=ALL, `admin`=ALL−`chatbots:write`); precedencia SAML. **Cuentas** (`database/models.py`): `AdminAccount`→`SuperAdminAccount`, `PartnerAccount`→`AdminAccount` (reutilización del nombre; FKs actualizadas). **Auth router**: `/auth/superadmin/login` + `/auth/admin/login` (bug de password del admin se mantiene TAL CUAL para SEC.1). **Hub**: `HubClient`→`HubOrganizacion`, tabla `hub_clients`→`hub_organizaciones`, `client_id`→`organizacion_id` en `hub_chatbots`/`hub_web_sites` + servicios/seeds; router `hub_clients_router`→`hub_organizaciones_router` (`/hub/organizaciones`, `OrganizacionRead/Create/Update`). **Migración** `y6h7i8j9k0l1` aplicada (`alembic upgrade head`, ALTER RENAME preservando datos: 1→1 filas verificado; `alembic current` en head). **Retirada Caso B** (cluster muerto server-side): `ai_brain`, `partner_*` services, `consumption_query_service`, `security_policy_service`, `billing_engine`, `api/v1/automation.py`, `telemetry_router.py` + tests legacy huérfanos. **OpenAPI reexportado** (server+frontend; 109 paths/175 schemas; verificado `/hub/organizaciones`, `OrganizacionRead`, `organizacion_id`, logins nuevos). **Tests**: grep=0 de nombres antiguos en `server/app` (excl. `ui/`); todos los tests afectados por el rename verdes (136 en el re-run). **Deviaciones documentadas**: (1) `ui/` NiceGUI legacy se deja para CAL.1 (excluido del grep); (2) el `client_id`/`partner_id` **legacy AutomatIA** (`AutomationLibrary`, `ClientAccount`, `library_router`) es un concepto distinto, fuera del mapa ROL, no renombrado; (3) fallos PRE-EXISTENTES ajenos al rename: `aiosqlite` no declarado en `pyproject`/`uv.lock` (rompe `test_admin_models`/`test_prompt2_db_api`) y `modules/brain` inexistente (rompe 3 `test_imports`). Pendiente: **ROL.2** (frontend Orval/rutas/i18n, Sonnet). |
| 2026-07-11 | (planificación — ampliación de CAL.1 con Caso B de client_app) | **No mueve el cursor.** `Plan_TDD_Fase1.md`: prompt CAL.1 ampliado con la limpieza **Caso B de `client_app/`** (triaje del replanteamiento de Fase 2, Categoría B): borrar los 5 UI `_legacy` trackeados (`_legacy_webhook_page`, `connections_page_legacy`, `custom_script_page_legacy`, `graphics_page_legacy`, `rpa_page_legacy`) + artefactos autogenerados trackeados en `modules/extraccion/` (`.servicios_generados/custom_extractor*`, `servicios/custom_prueba*`) + añadirlos a `.gitignore` para que el runtime no los re-trackee. **Verificado contra git**: el `.venv/` de client_app NO está trackeado (corrige el dato del inventario; solo ruido de disco, fuera de alcance). **Fuera de alcance explícito** del prompt: los imports rotos a factories inexistentes (`workflow_engine.py:1417/1478`, `clarification_service.py:504`, `graphics_wizard.py:8`) y el `report_factory` duplicado — son Categoría C pendiente del go/no-go de migración (Plan_TDD_Fase2.md §4) y se resuelven con esa decisión (borrado o reescritura), no con parches previos. Tests/cierre ampliados: grep de los 5 legacy = 0, `git ls-files` de extraccion vacío, colección pytest de client_app sin errores. Motivación de fecha: dejar el repo limpio de artefactos internos antes de la apertura AGPLv3 (sept 2026). |
| 2026-07-11 | (docs — alineación de docs/Arquitectura.md y CONTRIBUTING.md) | **No mueve el cursor.** **docs/Arquitectura.md**: reconciliada con las decisiones recientes — auth pasa de "OIDC/SAML" a **SSO SAML 2.0 + PAT** (OIDC diferido) en §4.1/§6.2/§7.1; multi-tenancy ahora explicita el aislamiento por token+consulta; nueva **§7.4 "Estrategia de ingesta y calidad del corpus"** (corpus curado como vía principal + revisión humana obligatoria para `regulation` + scraper para web estructurada + auditoría post-ingesta); §8.2 añade que la migración de AutomatIA es **selectiva, no en bloque** (generación superada/reimplementada; valor duradero = ejecución determinista/desatendida/auditable); §17 añade 4 decisiones adoptadas (auth SAML/PAT, ingesta curada, migración selectiva, aislamiento multi-tenant) y §18 añade 3 decisiones pendientes (alcance migración AutomatIA, institución/expediente piloto F3, acceso APIs UJI/G400/ENI). **CONTRIBUTING.md**: reescrito de la v2.0 NiceGUI antigua (DI por `state` global, `BrainAPIClient`, `translations.json`, `ARCHITECTURE.md`) al monorepo actual (FastAPI+React+Thin Client): fuentes de verdad, uv+PowerShell, TDD+Conventional Commits sin Co-Authored-By, Contract-First/Orval/SDUI/HATEOAS, i18next, frontera Edge/Cloud (DEPLOY_MODE, dos DeclarativeBase, ConfigProvider), seguridad (aislamiento multi-tenant, sandbox microservicio, PII al edge, secretos por env, StorageService), retirada de legacy. Apunta a CLAUDE.md como regla operativa dominante. |
| 2026-07-11 | (planificación — recomendaciones de la Fase 3) | **No mueve el cursor.** Añadida a `Plan_TDD_Fase3.md` la sección "Recomendaciones de la Fase 3", que precede y condiciona a E1–E5/FASE 17/19. **(1) Prerrequisitos reconciliados con el estado real**: no hay OIDC (sí SAML/JWT/PAT del Bloque AUTH); el aislamiento multi-tenant lo aporta SEC.2 (Fase 1) y los endpoints de expedientes deben pasar por `assert_org_access` desde el diseño (datos personales sensibles); nomenclatura nueva (`organizacion_id`, roles superadmin/admin/user, `responsable_rol`); Thin Client 2.A es prereq de NodoScript/NodoRPA (delegan al Edge, invariante 3C.0); MCP Client 2C.0 prereq de 3.C. **(2) Recomendaciones técnicas**: E2 usar `langgraph-checkpoint-postgres` oficial (no tabla propia); E3 auditoría inmutable con defensa en profundidad (rol BD sin UPDATE/DELETE + anclaje periódico del hash de cabeza, no solo ausencia de endpoints); 3B.6 Fairness exige definir metadatos de colectivo + base jurídica RGPD antes de codificar; 3B.5 Snapshots se apoya en `valid_from`/`valid_to` del `CorpusManifest` (ING.0.1). **(3) Reutilización**: NodoScript/NodoValidador reusan `ScriptSecurityAuditor`+`sandbox_client` ya migrados; NodoAnalista reusa retriever `agents_hub` + KB `regulation` ingerida por corpus curado (ING.0); motor NER/export/RunManifest ya existen. **(4) Riesgo dominante**: E5 (ENI/ENS + APIs UJI/G400) es incertidumbre externa, no de código → spike temprano de acceso real al inicio de F3; el `AdaptadorNativo` permite avanzar E1–E4 sin sistemas externos; confirmar institución/caso comprometido antes de invertir ~9-10 semanas. Origen: `docs/VALORACION_PROYECTO.md`. |
| 2026-07-11 | (replanteamiento — utilidad de la migración de servicios NiceGUI de Fase 2) | **No mueve el cursor.** Añadida a `Plan_TDD_Fase2.md` la sección "Recomendaciones y replanteamiento de la Fase 2" con: (1) las 6 recomendaciones de ejecución de la valoración (inventario medible, refactor previo de `AIBrainService`/`automation.py`, Secure Pairing 2A.2 bloqueante, esqueleto 9.16 con reconexión/heartbeat, adelantar MCP Client 2C.0, reutilizar Focus Mode transversal); (2) **inventario real de `client_app/`** (489 ficheros; ~99 UI NiceGUI, ~81 servicios, 238 tests, `.venv` commiteado) con triaje por categorías; (3) **valoración de utilidad go/no-go**. **Hallazgo clave**: gran parte del valor legacy YA está reimplementado en `server/app/modules/redaccion` (auditor AST, anonimización, ETL, charts, export, copiloto) y `agents_hub` (RAG); los factories que la Fase 2 planeaba migrar (`etl_factory`/`graphics_factory`/`deterministic_*`) **ni existen ya en el legacy** (imports rotos). **Recomendación**: NO migrar en bloque; migrar por goteo solo bajo caso de uso nombrado del piloto. El valor que un agente IA general (tipo Claude cowork) NO reemplaza es la **ejecución determinista, desatendida, firmada y auditable** (watchers/triggers/runner/gobernanza RIA-ENS), no la *generación* (ya superada). Efecto sobre el plan: FASE 16 se reduce a wrapper LangGraph (no migración), FASE 15 a unificación, FASE 18 se difiere, 9.12b ya cubierto por 9R.5.9; la Fase 2 se reduce de facto a **Subfase 2.A (Thin Client, infra para Fase 3)** + limpieza Categorías A/B. Decisión servicio-a-servicio de la Categoría C (workflow_engine, flow_registry, triggers, watchers, RPA, `.automatia`) pendiente del usuario. Origen: `docs/VALORACION_PROYECTO.md` + inventario. |
| 2026-07-11 | (planificación — bloques de complección de Fase 1: ROL + SEC + CAL + ING.0) | **No mueve el cursor** (sigue en 11.1). A partir de `docs/VALORACION_PROYECTO.md` (aprobada), añadidos a `Plan_TDD_Fase1.md` **16 prompts TDD atómicos autocontenidos** en 4 bloques nuevos, con orden de ejecución recomendado `ROL → Fase 11 → SEC → CAL → Deploy` (+ ING.0 cuando el corpus esté listo). **Bloque ROL** (renombrado institucional, 2 prompts): ROL.1 (**Opus** — refactor de modelos+migración Alembic con preservación de datos+deps de seguridad; mapa `AdminAccount`→`SuperAdminAccount`, `PartnerAccount`→`AdminAccount`, `HubClient`→`HubOrganizacion`, `client_id`→`organizacion_id`, roles superadmin/admin/user; trampa documentada: `AdminAccount` se reutiliza para otra entidad → renombrar tablas en orden), ROL.2 (Sonnet — Orval+rutas+i18n+checks de rol). **Bloque SEC** (endurecimiento, bloqueante de Deploy, 7 prompts): SEC.1 (login de Admin con verificación de contraseña — cierra A1, el bypass), SEC.2 (**Opus** — aislamiento multi-tenant: claim `organizacion_ids` en JWT/PAT + capa única `assert_org_access`/`scope_query_to_orgs` + **test de aislamiento como gate de CI** — cierra A2), SEC.3 (CORS por entorno — A4), SEC.4 (rate limiting + cuotas de coste LLM — A5), SEC.5 (temas: auth en GET + anti path-traversal — M2), SEC.6 (uploads: magic bytes + límite de tamaño en streaming — M4, validación reutilizable por ING), SEC.7 (docs off en prod + cabeceras de seguridad — M5). Nota: el token público del widget (M1) ya lo cubre D.1 y los secretos→Secret Manager (A3) D.2; SEC no los duplica. **Bloque CAL** (deuda de calidad pre-AGPLv3, 5 prompts): CAL.1 (retirar `server/app/ui/` NiceGUI + tests `test_admin_*_ui` + huérfano `app/modules/extraccion/` + scripts ad-hoc en tests/), CAL.2 (**Opus** — migrar los 5 módulos `shared/api/*.ts` manuales a hooks Orval, cierra el TODO CF.4 y elimina tipos/`localhost` hardcodeados), CAL.3 (descomponer `DocumentsPage.tsx` 1019 líneas), CAL.4 (completar `ca/admin.json` + retirar strings hardcodeados + labels htmlFor), CAL.5 (lazy routes/code-splitting del bundle ~927 KB + retirar shims `init_db`/seeds). **Bloque ING.0** (ingesta de corpus curado, 2 prompts): ING.0.1 (contrato `CorpusManifest`/`CorpusDocumentEntry` con procedencia y revisión obligatoria para `regulation`), ING.0.2 (CLI de carga masiva passthrough que reusa `IngestionWatcher`, preserva `source_url`/`content_hash`/`language`/`content_class` en `HubDocument`, idempotente por content_hash, `--dry-run`). Decisiones de alcance vía AskUserQuestion (2026-07-11): (1) alcance = SEC + calidad + ING.0 (completo); (2) renombrado de roles = incluir prompts ahora (más barato que tras Fase 3). Total Fase 1: 63 → 79 prompts (+16). |
| 2026-07-11 | (replanificación — estrategia de ingesta: corpus curado como vía principal) | **No mueve el cursor** (sigue en 11.1). Decisión tomada tras la experiencia real con el corpus de normativa UJI: la descarga automática de PDF + conversión a `.md` **sin revisión humana no es fiable** — según el origen del PDF, Docling genera ruido (tablas aplanadas, artículos partidos, idioma mal detectado) que hay que corregir antes de indexar. Para un chatbot de administración pública, **una norma con ruido citada con autoridad es peor que no responder** → la revisión previa es obligatoria para contenido normativo, no opcional. **Herramienta externa ya validada**: pipeline `C:\Users\fabra\Documents\Descarregar_pdf\normativa_uji` (scraping→descarga→detección idioma val/es→conversión Docling→renombrado, con log JSON, dry-run, idempotencia, heurística de tablas por nº páginas `TABLE_STRUCTURE_MAX_PAGES=150`, manejo de PDF corrupto/escaneado). Ha convertido todo el corpus UJI, **pendiente de revisión humana**. **Base arquitectónica que abarata el cambio**: el pipeline de la app ya es markdown-céntrico (todo lo posterior a la conversión opera sobre markdown; `DoclingProcessor` es solo el adaptador de entrada) → meter `.md` curado es passthrough, y la dedup por `content_hash` evita duplicados al reimportar. **Decisiones**: (1) **La ingesta de corpus curado externamente pasa a ser la vía principal**; la conversión pesada (Docling/pdfplumber) vive fuera de la ruta de petición (coherente con extraer Docling a microservicio) y la curación iterativa vive en repo git, no en la BD de la app. (2) **Revisión humana obligatoria por clase de contenido**: `regulation`/normativa exige revisión antes de indexar; FAQs/info pública genérica toleran vía ligera. (3) **Formalizar un "paquete de corpus"**: carpeta de `.md` revisados + manifiesto por documento (URL origen, hash del PDF original, versión Docling, idioma, categoría, revisor + fecha de revisión; `valid_from`/`valid_to` cuando llegue F3 snapshots). El `normativa_uji_log.json` ya es ~80% del manifiesto; falta el estado de revisión. La importación (Fase 0) **preserva la procedencia en `HubDocument`** (`source_url`, `content_hash`, `language`) para citas auditables y para los snapshots `as_of_date` (F3.B.3). (4) **El scraper autónomo in-app se degrada a nicho**: el Asistente HITL de selectores (1.A.8 `analyze-html`) y las Spider Skills valen para **fuentes web estructuradas y homogéneas**, NO como vía general para normativa PDF heterogénea. Reevaluar si el `SpiderRegistry` sobrevive o se funde con el Bloque ING (pregunta ya abierta en historial 2026-06-23). (5) **9Q es capa complementaria, no confundir**: 9Q audita la calidad/deriva del contenido *ya ingerido* (contradicciones, obsolescencia, superseded); NO arregla el ruido de conversión, que es previo y se resuelve en el pipeline externo + revisión. (6) **No sobreingeniería en la app**: dentro de la app solo (a) importar con procedencia, (b) preview de chunking + test de recuperación antes de poner "en vivo", (c) auditor de deriva 9Q. La corrección de ruido markdown se hace en editor/git, no en una UI web. **Reordenación del Bloque ING**: Fase 0 (carga masiva de corpus curado) sube a prioridad alta y es la vía de referencia. **Deuda del tool externo si se oficializa**: rompe aislamiento (`config.py:10` inyecta el `.venv` de AI_agents_hub por ruta absoluta en `sys.path` — darle su propio proyecto `uv`); falta la fase "revisado" explícita y una salida canónica única (hoy hay `normativa_propia_md`, `_corregida`, `_v2`… → repo git + carpeta canónica). Origen: valoración en `docs/VALORACION_PROYECTO.md`. |
| 2026-06-23 | (planificación — Bloque ING: ingesta multi-formato) | **Backlog, no programado** (no mueve el cursor). Valoración: hoy la ingesta solo acepta `application/pdf` (validación MIME duplicada en `api/v1/ingestion.py:31` y `routers/hub_ingestion_router.py:199`). **Hallazgo arquitectónico clave**: el pipeline ya es **markdown-céntrico** — en `ingestion/watcher.py` todo lo posterior a la conversión (`hash_content`→`detect_language`→`MarkdownChunker.split`→`embedding_service.embed`→`HubDocumentChunk`) opera sobre una cadena markdown; `DoclingProcessor` (`docling_processor.py`) es solo el adaptador de entrada PDF→md. Por tanto `.md`/`.txt` son **passthrough** y no tocan el pipeline. Idempotencia ya cubierta: `watcher.py:102-108` deduplica por `content_hash`, así que reimportar un `.md` curado no duplica (habilita el flujo "curar fuera del sistema → reimportar"). **4 fases previstas**: **Fase 0** — script/CLI de carga masiva que recorre una carpeta y reusa `IngestionWatcher` con `MarkdownConverter` passthrough (desbloquea el corpus de ~300 docs de normativa UJI sin UI ni ZIP, porque el script ya tiene acceso al sistema de ficheros). **Fase 1** — registro de conversores (`SourceConverter` protocolo: `PdfConverter` Docling existente + `MarkdownConverter`/`TextConverter` passthrough) + validación por extensión+magic bytes compartida entre los dos endpoints (hoy duplicada), TDD. **Fase 2** — soporte ZIP (`ArchiveExpander` con guardas anti zip-slip/zip-bomb, allowlist por extensión, ignora ocultos; crea N jobs hijos con **throttling**/semáforo — 300 `BackgroundTasks` + 300× embeddings BGE-M3 saturarían el threadpool — + job/lote agregador de progreso). **Fase 3** — frontend (ampliar `accept` del dropzone en `DocumentsPage.tsx`, subida carpeta/ZIP, progreso de lote, i18n namespace `admin`). **Decisiones acordadas vía AskUserQuestion (2026-06-23)**: (1) primera fase backend = solo `.md`/`.txt` passthrough (ofimática .docx/.html/.csv fuera del núcleo; Docling ya soporta .docx si se enchufa luego al registro); (2) carga masiva inmediata vía script; (3) ZIP con throttling, **sí**, pero — **revisión de secuencia** — el ZIP headless no tiene consumidor (con acceso a ficheros el script recorre la carpeta directamente; el ZIP solo existe como transporte para el navegador), así que **Fase 2 ZIP se funde con Fase 3 UI** en una sola tanda "subida por navegador". Con Orval la UI es barata (capa de datos regenerada del contrato; dropzone/polling/lista de jobs ya existen en `DocumentsPage.tsx`); lo único nuevo no trivial es el progreso de lote. **Bloqueante de arranque**: el usuario revisará los `.md` y generará la carpeta definitiva curada antes de Fase 0 (en su corpus hay varias versiones — `normativa_propia_md`, `..._corregida`, `..._v2`, `..._v2_corregit` — apuntar a la equivocada ingiere normativa sin curar; pendiente además aplicar correcciones aún no aplicadas). Datos a confirmar al arrancar Fase 0: carpeta canónica, `chatbot_id` destino, y si forzar `language` por carpeta o dejar `detect_language` (corpus mezcla CA/ES). |
| 2026-06-12 | MCP.4 (cierra Bloque MCP) | Tool `test_chat` + documentación `docs/MCP_SERVER.md`. **Tool** (`mcp_server/tools/chat.py`): `test_chat(chatbot_id, message, lang?)` [scope `chat:test`] envuelve `POST /api/v1/hub/chat/{id}` (`hub_chat`, edge), que responde **SSE** — `ApiClient.post` consume el stream completo y `parse_chat_sse` agrega los eventos `token`→`answer` y extrae `sources`/`interaction_id`/`language_fallback`/`translation_warning` del evento `done`; evento `error`→`ApiError` legible. `lang` aceptado pero no reenviado (el endpoint autodetecta). Función core renombrada a `run_test_chat_core` (el prefijo `test_` hacía que pytest la recolectara al importarla). Registrada en `build_server` (`register_chat_tools`). **Backend** (decisión documentada, como en MCP.2): el endpoint de chat solo tenía `Depends(get_current_user)` → el scope `chat:test` era decorativo; añadido `dependencies=[Depends(require_scopes("chat:test"))]` al `chat_stream` (cambio mínimo: las sesiones JWT humanas pasan —pat_scopes None—, solo los PAT se filtran; sin cambio de path/operationId → sin regen de OpenAPI). **docs/MCP_SERVER.md**: propósito+arquitectura (opción A stdio), nota dev/prod (edge vs cloud), env vars + cómo emitir el PAT (`/hub/access-tokens`), mapa scopes→tools, `claude mcp add govgenai --env ... -- uv run --directory mcp_server mcp run server.py`, ejemplos de sesión (autoría de plantilla; configurar+probar chatbot), seguridad (HITL+gating+PAT revocable+sin acceso a server/app), referencia de toolset, verificación de integración manual sin `.bat`. **Tests**: 7 en `test_chat_tool.py` (respx: parser SSE agrega tokens+sources, evento error→ApiError, llamada al endpoint, 403→ScopeError, 404→legible, registro de test_chat, smoke de MCP_SERVER.md). Suite MCP **54/54** verde; chat endpoint backend 19/19 sin regresión; servidor lista **16 tools + 5 resources**; 0 imports de `server.app`. Backend/integración MCP → sin pruebas manuales de navegador. Cursor MCP.4 → 11.1. **Bloque MCP completo (MCP.1-MCP.4).** |
| 2026-06-12 | MCP.3 | Tools MCP de configuración de chatbots (namespace cloud) — **tools-only, sin cambios de backend** (MCP.3 no tiene sección Backend). `mcp_server/tools/chatbots.py` (funciones `_core` testeables + 10 wrappers `@mcp.tool`, registradas en `build_server`). **Lectura** [scope `chatbots:read`]: `list_clients` (GET `/hub/clients`), `list_chatbots(client_id?)` (GET `/hub/chatbots` + **filtro client-side** —el endpoint no filtra por cliente), `get_chatbot(id)` (vía listado + filtro —**no existe GET individual**; 404→ApiError), `get_corpus_stats(id)` (GET `/hub/chatbots/{id}/corpus-stats`, incluye `recommended_mode`), `list_prompt_templates(chatbot_id?)` (GET `/hub/prompt-templates/` con `chatbot_id` query —filtro server-side). **Escritura** [scope `chatbots:write`]: `create_chatbot(payload,confirm)`, `update_chatbot(id,patch,dry_run=True)` (**dry_run por defecto**: GET estado actual→diff campo-a-campo, NO persiste; `dry_run=False`→PATCH; mitiga la mutación in-place que cambia el widget público al instante), `update_prompt_template(id,payload,confirm)`, `assign_child(router,child,confirm)` (POST `/children` {child_chatbot_id}), `unassign_child(router,child,confirm)` (DELETE `/children/{id}`). **Resources** (`register_chatbot_resources`): `govgenai://chatbots/enums` (empaquetado: retrieval_mode/public_graph_profile/kind/language_mode —`public_graph_profile`/`kind`/`language_mode` son `str` libres en el contrato, solo `retrieval_mode` es Literal), `govgenai://chatbots/schema` (ChatbotCreate/ChatbotUpdate extraídos del `/openapi.json` vivo, sin acoplar al backend). **Decisión ApiClient**: el server devuelve **400** (no 422) para reglas de negocio (límite 128K MD_LONG_CONTEXT, violaciones de jerarquía) → mapeo 400 **y** 422 a `ValidationError` (misma familia de validación). **Tests**: 20 en `test_chatbots_tools.py` (respx: lecturas, filtro client-side, diff/dry_run sin PATCH, gating confirm, 400→ValidationError, 403→ScopeError, registro de las 10 tools + 2 resources) + 1 en `test_scaffolding.py` (400→ValidationError) = 21 nuevos. Suite MCP **47/47** verde; 0 imports de `server.app`; servidor lista 15 tools + 5 resources. Backend puro → sin pruebas manuales. Cursor MCP.3 → MCP.4. |
| 2026-06-12 | MCP.2 | Tools MCP de autoría de plantillas + 3 endpoints backend. **Backend** (`hub_redaccion_router`, Deploy: edge): `GET /hub/redaccion/template-schema` (`getTemplateSchema` → `ReportTemplateSpec.model_json_schema()`, alimenta el resource MCP.1; scope `redaccion:templates:read`), `GET /hub/redaccion/template-versions/{version_id}` (`getTemplateVersion` → spec completa; antes solo existía `/ui-contract`; read), `POST /hub/redaccion/templates/{template_id}/versions` (`publishTemplateVersion`, append-only: valida la spec contra `ReportTemplateSpec`→422, calcula `next_version` vía `ReportTemplateVersionRepo.list`, `dry_run=true` valida sin persistir; gated `require_role(admin,partner)` + `require_scopes(redaccion:templates:write)`). **Decisión documentada**: la sección "Backend" del prompt solo nombraba template-schema, pero las tools mandadas de lectura/escritura requieren los otros dos endpoints para funcionar end-to-end; `create_template` mapea a `approve-as-template` (ruta draft con DraftValidator) y `publish` valida vía `ReportTemplateSpec` (no DraftValidator: una spec no es un draft). **Tools** (`mcp_server/tools/templates.py`, funciones `_core` testeables + wrappers `@mcp.tool`, registradas en `build_server`): `list_templates`, `get_template_spec`, `validate_template_draft` (POST `/redaccion/llm-drafts/validate`), `create_template(draft,name,is_global,confirm)` (confirm=False→solo `/validate`; confirm=True→`/approve-as-template`), `publish_template_version(template_id,spec,confirm)` (confirm=False→`?dry_run=true`; confirm=True→publica). Gate HITL por construcción: sin `confirm=True` no persiste. OpenAPI reexportado (106→109 paths, ambas copias). **Tests**: 7 backend (`test_mcp_template_endpoints.py`, sesión `AsyncMock`, spec válida vía `GenericReportProfile.default_spec()`) + 12 MCP (`test_templates_tools.py`, respx: mapeo tool→endpoint, gating sin confirm, dry_run con query param, 422→ValidationError, 403→ScopeError, registro de las 5 tools) = 19 nuevos. Suite MCP 26/26 verde; deploy-mode sin regresión. Backend puro → sin pruebas manuales. Cursor MCP.2 → MCP.3. |
| 2026-06-12 | MCP.1 | Scaffolding del servidor MCP stdio. Paquete uv autocontenido `mcp_server/` (layout flat, `[tool.uv] package=false`, **sin imports de `server/app`** — grep limpio), siguiendo el patrón de `services/script_sandbox/`. `config.py`: `load_config(env)` lee `GOVGENAI_API_BASE_URL`+`GOVGENAI_PAT` (lanza `ConfigError` listando TODAS las que falten) + `GOVGENAI_GRAPH_PROFILES_PATH` opcional (default `<repo>/docs/GRAPH_PROFILES.md`). `errors.py`: `ApiError` base + `AuthError`(401)/`ScopeError`(403)/`ValidationError`(422, con body en `.detail`)/`ServerError`(5xx). `api_client.py`: `ApiClient(httpx.AsyncClient)` inyecta `Authorization: Bearer <PAT>` en cada request, verbos get/post/patch/delete, `_raise_for_status` mapea códigos (otros 4xx→`ApiError` genérico), parse JSON/`None`(204). `resources.py`: `register_resources(mcp, client_provider, config_provider)` con providers **perezosos** (construir el servidor no exige env; el PAT solo se necesita al leer un resource que toca la API) — 3 resources: `govgenai://redaccion/template-schema` (vía `GET /api/v1/hub/redaccion/template-schema`, endpoint garantizado en MCP.2), `govgenai://redaccion/profiles` (empaquetado, espejo documentado de `ReportProfileId`), `govgenai://docs/graph-profiles` (lee el markdown del repo, fuente única). `server.py`: `FastMCP("govgenai")` + `build_server()` + singletons lazy `get_config`/`get_client` + `mcp.run("stdio")`; bootstrap de `sys.path` para `mcp run server.py`. Deps `mcp[cli]`+`httpx`+`pydantic`; dev `respx`+`pytest-asyncio`. `README.md` con tablas de env y resources. **14/14 tests verdes** (`respx`, sin red): header Bearer en cada request, mapeo 401/403/422(con body)/5xx/404/204, config (falta PAT / faltan ambas / OK), `list_resources` lista las 3 URIs, `read_resource` de los 3 resources. `mcp run server.py` arranca en stdio (rc=0 en EOF). Backend puro → sin pruebas manuales. Cursor MCP.1 → MCP.2. |
| 2026-06-11 | AUTH.4 + limpieza colección | **AUTH.4 (frontend, cierra Bloque AUTH)**: namespace i18n `auth` (es/en/ca) registrado en `i18n/index.ts`. `LoginPage`: botón "Entrar con SSO institucional" (visible si `VITE_SAML_ENABLED`, redirige a `/api/v1/auth/saml/login`) + divisor, mantiene fallback email/contraseña. `AuthCallbackPage` (`/auth/callback`, pública): extrae `#token=` del fragment → `login()` → `/hub`. `AccessTokensPage` (`/hub/access-tokens`): tabla de PAT, modal de creación (react-hook-form+zod: nombre, scopes según rol —partner sin `chatbots:write`—, caducidad), token en claro mostrado UNA vez con copiar+aviso, revocar con confirm. OpenAPI reexportado (106 paths) + Orval regenerado (`auth-pat`: useCreatePat/useListPats/useRevokePat + PatView/PatCreateRequest/PatCreatedResponse). `HubLayout` refactorizado a `useTranslation([...])` + entrada nav "Tokens de acceso". `App.tsx`: rutas callback+access-tokens. Tests: 8 Vitest (4 AccessTokensPage incl. a11y + 4 SsoLogin/Callback); 209/209 suite frontend + 10/10 a11y sin regresión; tsc limpio. `pruebas_manuales_promptAUTH_4.bat` (ANSI 1252, bytes @ech). **Limpieza de errores de colección preexistentes**: repuntados 4 tests billing a `automation.billing_engine` (11 verdes) + `test_new_endpoints` a solo `sign_manifest`; borrados huérfanos (`test_brain_billing`, `test_admin_page`, `test_partner_session`, `test_multitenancy_flow`, dirs `tests/ui` y `tests/e2e`); **fix de raíz**: eliminado import de `nicegui` en `automation/cortex.py` y `infrastructure/llm_gateway.py` (→ `asyncio.to_thread`), desacoplando backend↔UI y desbloqueando deploy edge. `pytest --collect-only` = 946 tests, 0 errores. Cursor AUTH.4 → MCP.1. **Bloque AUTH completo.** |
| 2026-06-11 | AUTH.3 | Personal Access Tokens revocables + dependencia auth dual JWT/PAT. ORM `HubPersonalAccessToken` (HubConfigBase: id/owner_id/owner_email/owner_role/name/token_prefix/token_hash sha256/scopes ARRAY/created_at/expires_at/last_used_at/revoked_at, índices owner_id+token_prefix). Migración `x5g6h7i8j9k0_hub_pat_auth3` (down_revision w4f5g6h7i8j9) **aplicada** (`alembic current` = x5g6h7i8j9k0 head). `core/auth/pat/scopes.py`: catálogo (redaccion:templates:read/write, chatbots:read/write, chat:test) + techo por rol (admin=ALL, partner=ALL−chatbots:write —reserva la mutación in-place de chatbots a admin vía token máquina, ver mcp.md val.2; partner sigue configurando por UI), `validate_scopes`/`allowed_scopes_for_role`/`UnknownScopeError`. `core/auth/pat/service.py`: `PatService(session)` con `create` (token plano `pat_<prefix>_<secret>`, guarda sha256+prefix, valida scopes y techo de rol → `PatForbiddenError`; plano devuelto 1 vez), `verify` (lookup por prefix, `hmac.compare_digest`, rechaza revocado/expirado → `PatInvalidError`, actualiza last_used_at; principal capturado ANTES del commit por expire_on_commit), `list_for`, `revoke` (404 si no es del owner). `PatPrincipal` (UserInfo+scopes). `api/deps.py`: `get_current_user` ahora dual — Bearer `pat_…`→PatService.verify (scopes en `request.state.pat_scopes`), resto→JWT (pat_scopes=None); `require_scopes(*needed)` (sesión humana no se filtra; PAT sin scope→403 PAT_SCOPE_MISSING). `routers/pat_router.py` (Deploy: cloud): POST/GET/DELETE /auth/pats (201 con token plano, 403 PAT_FORBIDDEN, 422 UNKNOWN_SCOPE, 204 revoke, 404 ajeno), registrado en `_register_cloud`. Tests: 19 nuevos (13 `test_pat.py` servicio+dependencia con httpx.AsyncClient, 6 `test_pat_router.py`) + `test_auth_context.py` actualizado al nuevo contrato (request con .state). 43/43 verdes en `tests/core/auth` + `tests/unit/test_auth_context`. Sin regresión en hub_themes (auth cloud). Nota: la vía JWT no abre conexión BD (AsyncSession lazy); solo los PAT consultan BD. Cursor AUTH.3 → AUTH.4. |
| 2026-06-11 | AUTH.2 | Provisioning JIT + mapeo identidad SAML → cuenta + rol + JWT. ORM `HubSsoUser` (HubConfigBase, en `config_models.py`: id/email único/display_name/role/external_id/idp_entity_id/is_active/created_at/last_login_at). Migración `w4f5g6h7i8j9_hub_sso_users_auth2` (down_revision v3e4f5g6h7i8) **aplicada** (`alembic current` = w4f5g6h7i8j9 head). `core/config.py`: +`saml_group_role_map`. `core/auth/saml/role_mapping.py`: `resolve_role(attributes)` — (1) atributo de rol explícito válido, (2) mapeo grupo→rol (SAML_GROUP_ROLE_MAP JSON) con precedencia admin>partner>informer>user, (3) default. `core/auth/saml/identity_service.py`: `SamlIdentityService(session).resolve_session(nameid, attributes)` — email obligatorio (`SamlMissingEmailError`), match AdminAccount/PartnerAccount activos por email→rol, si no upsert HubSsoUser (rol vía resolve_role, actualiza last_login_at, no duplica). ACS de `saml_auth_router.py` cableado: resuelve identidad → `create_token` → 302 a SAML_FRONTEND_RETURN_URL con `#token=` en fragment (o JSON con token si no hay return url). Tests: 11 nuevos en `test_saml_identity.py` (4 resolve_role puro + 6 resolve_session BD + 1 ACS completo firma→JWT→redirect) usando fixtures de `conftest.py` (SAML firmado sin red + fixture `db` con engine fresco por test y limpieza por email). AUTH.1 `test_saml_sp.py` refactorizado a las fixtures del conftest (happy-path ACS movido a AUTH.2). 21/21 verdes en `tests/core/auth/`. `.env.example`: +SAML_GROUP_ROLE_MAP. Notas: engine async por test (no reusar server_engine global → evita 'another operation in progress'); override de get_session en el test ACS crea su engine dentro del request (loop del TestClient). Cursor AUTH.2 → AUTH.3. |
| 2026-06-11 | AUTH.1 | SP SAML 2.0 con **python3-saml** (OneLogin) + IdP de pruebas. Dependencias `python3-saml>=1.16.0` (+ xmlsec 1.3.17, isodate; wheel Windows OK). `core/config.py`: 15 settings SAML (`saml_enabled`, sp entity/acs/sls/cert/key, idp metadata url/xml, attr email/name/role/groups, default_role, frontend_return_url). `core/auth/saml/settings.py`: `build_saml_settings(sp_only=False)` (SP desde env + merge de metadata IdP vía `OneLogin_Saml2_IdPMetadataParser`; `security.wantMessagesSigned=True`; `InvalidSamlConfigError` si falta config). `core/auth/saml/request_adapter.py`: `prepare_saml_request(request)` (https/http_host/script_name/get_data/post_data desde el Request FastAPI, `await request.form()`). `routers/saml_auth_router.py` (Deploy: cloud): GET /auth/saml/login (302 con SAMLRequest), POST /acs (process_response → 401 SAML_VALIDATION_FAILED / 200 con name_id+attributes; JWT en AUTH.2), GET /metadata (XML SP, sp_validation_only), GET /logout (501 si no hay SLS). Feature flag: 404 si SAML_ENABLED=false. Registrado en `_register_cloud` (main.py). Tests: 11 verdes en `tests/core/auth/test_saml_sp.py` — fixtures de SAMLResponse firmada generadas en el test con cert/clave de prueba (cryptography) + `OneLogin_Saml2_Utils.add_sign`, sin red; cubren build_settings (+sp_only +missing idp), request adapter, metadata, login redirect, ACS válido, firma con cert ajeno→401, aserción expirada→401, audience incorrecta→401, feature flag off→404. `.env.example`: sección SSO SAML. `CONTRIBUTING.md`: dependencia de sistema libxml2/libxmlsec1. Prompt backend puro → sin pruebas manuales. Cursor AUTH.1 → AUTH.2. |
| 2026-06-11 | (planificación — adelanto AUTH + bloque MCP) | Plan_TDD_Fase1.md: insertados **Bloque AUTH** (4 prompts) y **Bloque MCP** (4 prompts) entre el Bloque 9Q (cerrado) y Fase 11, en la posición actual del cursor. Decisiones vía AskUserQuestion: (1) alcance auth = **SSO SAML + PAT completo** (adelanto de Subfase 1.B.1, antes diferida; en Fase 1 solo se ejecutó la identidad visual/Fase 10); (2) SAML = **Service Provider genérico SAML 2.0** testeable contra IdP de pruebas (fixtures de aserción firmada), metadata real de UJI por env cuando esté; (3) librería **python3-saml** (OneLogin); (4) MCP = **opción A stdio** (wrapper HTTP de la API) con alcance completo: plantillas (mcp.md valoración 1) + chatbots (valoración 2) + test_chat. **AUTH.1** (Opus — SP SAML: settings/request adapter/login/ACS/metadata, fixtures firmadas, feature flag), **AUTH.2** (Sonnet — provisioning JIT HubSsoUser + mapeo atributos/grupos→rol + emisión JWT en el ACS), **AUTH.3** (Opus — HubPersonalAccessToken con sha256+scopes+revocación, dependencia auth dual JWT/PAT, require_scopes, /auth/pats), **AUTH.4** (Sonnet — UI login SSO + gestión de PAT + pruebas manuales). **MCP.1** (Opus — paquete mcp_server/ con SDK mcp/FastMCP stdio + ApiClient httpx con PAT + resources base), **MCP.2** (Sonnet — tools de plantillas con gate de escritura confirm + endpoint template-schema), **MCP.3** (Sonnet — tools de chatbots con diff/dry_run), **MCP.4** (Sonnet — tool test_chat + docs/MCP_SERVER.md + claude mcp add). Distinción clave registrada: SAML autentica humanos; el PAT es lo que consume el servidor MCP headless. Cursor 11.1 → AUTH.1. Total Fase 1: 55 → 63 prompts. |
| 2026-06-06 | 9Q.9 | Frontend UI calidad de contenido web. Orval regenerado (100 paths → `hub-sites` + `hub-content-quality`). Namespace i18n `contentQuality` (es/en/ca, 50+ claves). **SitesPage.tsx**: tabla de sitios, dialog de alta (react-hook-form+zod), botón "Rastrear ahora" (useTriggerSiteCrawl), selección de sitio → SiteMappingPanel inline. **SiteMappingPanel.tsx**: selector de chatbot, CRUD de selecciones (path_prefix), tabla de candidatas con botón "Ingerir" (useIngestPage). **ContentQualityPage.tsx**: selector de sitio, "Analizar ahora" (useAnalyzeSite), tabla de hallazgos con badge de severidad (rojo/amarillo/azul), filtros status/type, Confirmar/Descartar/Resolver (useTransitionFinding), acceso al informe. **WebQualityReportViewer.tsx**: useGetSiteQualityReport → totales + secciones con recomendación + links DOCX/PDF. `HubLayout.tsx`: +2 entradas nav (sites/content-quality). `App.tsx`: +2 rutas. 10/10 tests Vitest verdes. tsc --noEmit limpio. `pruebas_manuales_prompt9Q_9.bat` (ANSI 1252, primeros bytes @ech). **Bloque 9Q cerrado** (9Q.0→9Q.9). |
| 2026-06-06 | 9Q.8 | Informe de auditoría web. `report_contracts.py`: `ContentFindingView / FindingTypeSection / WebQualityReport`. `report_builder.py`: `WebQualityReportBuilder(findings_repo, site_repo).build(site_id, *, status_filter=("new","confirmed"))` — agrupa findings por tipo, genera recomendaciones con plantillas i18n-izables, calcula totals_by_type + totals_by_severity. `report_exporter.py`: `WebQualityReportExporter.to_docx(report) -> bytes` (python-docx); `to_pdf(report)` degradación — intenta LibreOffice, si no devuelve DOCX. `routers/hub_content_quality_router.py` (Deploy: edge): 5 endpoints — GET findings (cola revisión), PATCH transición (422 si inválida), GET report (JSON), GET report/export (DOCX/PDF binario), POST analyze (202). Registrado en `_register_edge`. 14/14 tests nuevos verdes; 336/336 suite completa sin regresiones. |
| 2026-06-06 | 9Q.7 | Capa de selección: gestión de sitios + mapeo sitio→chatbots + candidatas. `selection_contracts.py`: `SiteView/SiteCreate/SitePatch/PageView/SelectionView/SelectionCreate/CandidatePageView`. `selection_service.py`: `CorpusSelectionService(session, page_repo, selection_repo, watcher)` — `candidates(site_id, chatbot_id)` (páginas activas no ingeridas con is_new/matched_rule), `ingest_page(chatbot_id, page_id)` (delega en watcher con crawled_page_id), `retire_page(chatbot_id, page_id)` (borra chunks + docs, devuelve count). `routers/hub_sites_router.py` (Deploy: edge): 12 endpoints (CRUD sitios/selecciones/páginas + crawl trigger + candidatas + ingest/retire), `get_quality_job()` injectable, `get_selection_service()` dependency. Registrado en `_register_edge`. `test_deploy_mode.py` corregido: `startswith` → `==` para diferenciar cloud (`/hub/chatbots` collection) de edge (`/hub/chatbots/{id}/selections`). 21/21 nuevos tests verdes; 322/322 suite completa sin regresiones. |
| 2026-06-06 | 9Q.6 | RAG consciente de calidad. `HybridRetriever._get_superseded_doc_ids(chatbot_id) -> set[str]`: JOIN hub_documents → hub_crawled_pages WHERE superseded=True AND crawled_page_id IS NOT NULL. `vector_search` / `keyword_search` / `hybrid_search`: nuevo param `include_superseded: bool = False`; post-filtrado Python sobre SearchResult.metadata["document_id"]. Chunks sin document_id (PDFs/legado) nunca excluidos. `VectorRetrievalStrategy.get_context` propaga `include_superseded=False` explícito. 9/9 tests verdes; 301/301 unit tests sin regresión. |
| 2026-06-06 | 9Q.5 | Job asíncrono de calidad por sitio. `ingestion/quality/quality_job.py`: `SiteQualityAnalysisJob(session_factory, site_crawler, detectors, watcher, selection_repo, *, run_semantic=True)` + `SiteQualitySummary`. Pasos: (1) crawl → (2) detección (det. + sem. por separado; fallo aislado → summary.errors); (3) consolidación: `HubCrawledPage.superseded=True + superseded_by_page_id` + `quality_score` = 1.0 - penalizaciones (0.5/critico, 0.2/warning, 0.1/info), clampado a 0.0; (4) auto-ingesta: páginas nuevas del diff × selecciones `auto_ingest_new=True` que casan → watcher con `crawled_page_id`. `run_semantic=False` salta detectores `_is_semantic=True`. `ingestion/quality/quality_scheduler.py`: `_get_due_sites(session, now)` expuesta para tests + `create_quality_scheduler(session_factory, job, *, interval_hours, enabled)` con APScheduler. `site_crawler.py`: `SiteCrawlSummary` extendida con `new_page_ids`, `changed_page_ids`, `gone_page_ids` (backward-compatible). `watcher.py`: `process_source` acepta `crawled_page_id=None` y lo asigna al `HubDocument`. `config.py`: `CONTENT_QUALITY_ENABLED`, `CONTENT_QUALITY_INTERVAL_HOURS`, `CONTENT_QUALITY_SEMANTIC_ENABLED`. `main.py`: `_start_quality_scheduler()` en lifespan con `_NullCrawler`/`_NullSelectionRepo` stubs para arranque sin deps completas. 13/13 tests verdes; 292/292 unit tests en verde (sin regresiones). |
| 2026-06-02 | 9Q.4 | Detector semántico site-scoped. Migración `v3e4f5g6h7i8` (head): `page_embedding Vector(1024)` en `hub_crawled_pages` (misma dimensión que HubDocumentChunk.embedding para que cruce coseno). ORM `HubCrawledPage.page_embedding` añadido. `ingestion/quality/semantic_detector.py`: `SemanticContradictionDetector(session, llm, embedding_service, *, similarity_threshold=0.92, max_pairs_per_run=200, anonymizer=None)` — **sin finding_repo** (la firma del plan devuelve `list[ContentFinding]`; la persistencia es del job 9Q.5). `analyze(site_id)`: (0) cobertura por `audit_semantic_scope` — `ingested` reusa embedding del chunk representativo (mayor len(content)) del primer HubDocument por página, **0 embeddings**; `full` embebe markdown_content de páginas activas sin page_embedding y lo persiste (idempotente: 2º run no re-embebe). (1) clustering coseno en Python (`_cosine`, decoplado de pgvector SQL → testeable con fakes deterministas). (2) control de coste: descarta pares de misma canonical (eso es 9Q.3 superseded), ordena por similitud desc, limita a max_pairs_per_run; **0 pares → return [] sin LLM**. (3) anonimizador opcional `Callable[[str],str]` aplicado a ambos textos pre-LLM. (4) juez LLM (`generate(prompt,context)`, JSON estricto, robusto a fences vía `_extract_json` reutilizado de 9R.4.1): duplicate→warning, contradiction→critical (confidence propagada), unrelated→nada; signal con similarity+explanation+textos truncados. Migración aplicada (DB local en estado divergente: tablas reconstruidas vía create_all + `alembic stamp v3e4f5g6h7i8`). 15 tests RED→GREEN (fakes deterministas, sin red ni BD real). Regresión focalizada (detector det. + signals + crawler + edge_boundary + site_model): 69/69 verdes. |
| 2026-06-02 | 9Q.3 | Detector determinista site-scoped. `ingestion/quality/deterministic_detector.py`: `DeterministicQualityDetector(session, finding_repo, thin_token_threshold, stale_days, now_fn)`. Reglas: **empty** (markdown_content None/blank/token_count==0 → critical), **thin** (token_count < umbral y > 0 → warning), **stale** (max(sitemap_lastmod, http_last_modified, content_year-as-date) anterior a now-stale_days → info; las tres señales None → no evalúa; signal incluye date+age_days), **crawl_error** (page.status=="error" → critical), **orphan_page** (page gone + HubDocument.crawled_page_id apuntándola → warning; query explícito sin relationship), **superseded** determinista (agrupar por `_process_key` = path sin segmentos de año; URLs sin año reciben clave única `__no_year__/path` para no agrupar con versiones datadas; dentro del grupo ordenar por effective_date = max(sitemap_lastmod, http_last_modified, content_year-as-date, first_seen_at); más reciente = vigente, resto → finding superseded con related_page_id; NOT muta HubCrawledPage.superseded). Idempotente por `uq_finding_dedup` del repo. 23 tests RED→GREEN (fakes en memoria, without BD). Suite unit agents_hub: pendiente de verificar. |
| 2026-06-02 | 9Q.2 | Crawl de sitio + señales + diff de sitemap. `ingestion/quality/signal_extractor.py`: `CrawlSignalExtractor` (puro, sin BD) — `extract_from_headers` (Last-Modified RFC 7231 vía `parsedate_to_datetime`, ETag, case-insensitive, tolera malformado→None), `extract_canonical` (regex `<link rel=canonical>`, primero gana), `extract_content_year` (año en path `/2024/` > año más reciente plausible en texto [1990, año+1]; descarta absurdos), `fetch_sitemap` (descarga 1×, parsea `<url><loc><lastmod>` con ElementTree namespace-agnostic, soporta `<sitemapindex>` recursivo profundidad≤2, deriva `/sitemap.xml` desde base, falla en silencio→{}). `ingestion/quality/site_crawler.py`: `SiteCrawler(session, spider, signal_extractor, page_repo)` + `SiteCrawlSummary` (pages_new/changed/gone/error/total/errors). `crawl_site`: unión(spider.crawl URLs, sitemap), upsert por página con markdown_content+content_hash+title+token_count+language+5 señales, diff (nueva→pages_new, content_hash distinto→pages_changed con hash previo capturado ANTES del upsert por identity-map, ausente→mark_gone bulk), fallo de fetch por URL aísla la página (status=error) sin abortar, fallo global→site.status=error. Refactor spider: `GenericSpider._fetch` devuelve `(body, headers)` (httpx `dict(resp.headers)`); `crawl` desempaqueta; tests top-level adaptados a la nueva firma. Sin migración (columnas de señales ya en 9Q.0). 26 tests nuevos verdes (15 señales + 11 crawler, fakes en memoria sin BD). Suite `tests/modules/agents_hub/`: 297/297 verdes (3 fallos + 8 errores observados en una corrida fueron flakiness de tests pesados torch/sentence-transformers en Windows; re-corrida limpia). |
| 2026-06-01 | 9Q.1 | Contratos Pydantic `ContentFinding` (frozen, confidence 0–1, 8 tipos, 4 estados), `_VALID_FINDING_TRANSITIONS`, `InvalidFindingTransitionError`. ORM `HubContentFinding` (HubOperationalBase, FK site_id→hub_web_sites CASCADE, FK page_id→hub_crawled_pages SET NULL, `uq_finding_dedup`, status indexado, `__init__` para default Python). `ContentFindingRepo`: `upsert` (respeta uq_finding_dedup), `list_by_site` (filtros status/finding_type), `transition` (valida transición, sets reviewed_at/reviewed_by), `get`. Migración `u2d3e4f5g6h7` aplicada (head). `test_edge_boundary.py` actualizado (hub_content_findings añadida). Bugfix en 9Q.0: migración `t1c2d3e4f5g6` guardada para evitar fallo en DB sin `hub_documents` (add_column ahora condicional). 25/25 tests verdes en `test_content_findings.py`; 5/5 tests verdes en `test_edge_boundary.py`. |
| 2026-06-01 | 9Q.0 | Modelo sitio/página/selección + retirada de `HubIngestionSource`. ORM `HubWebSite`+`HubCrawledPage`+`HubCorpusSelection` (HubOperationalBase, sin `relationship()` cross-base). `HubDocument.crawled_page_id` (FK→hub_crawled_pages, ON DELETE SET NULL). Repos `ingestion/quality/site_repo.py`: `WebSiteRepo`, `CrawledPageRepo` (upsert respeta `uq_page_site_url`, `mark_gone` bulk, `get_by_canonical`), `CorpusSelectionRepo` (matches `path_prefix` URL.path-prefix; `manual` y `sitemap_section` siempre False). Migración `t1c2d3e4f5g6` (head): crea las 3 tablas + crawled_page_id, DATA MIGRATION `HubIngestionSource→(site+selection rule_type='manual')` con derivación `client_id` desde chatbot (fallback nullable documentado), DROP `hub_ingestion_sources`. Retirada legacy: `source_scheduler.py` borrado + arranque en `main.py/lifespan` retirado (scheduler nuevo en 9Q.5), endpoints "Fuentes web" del `hub_ingestion_router.py` eliminados (`list_sources`/`create_source`/`update_source`/`delete_source`/`trigger_source_check`), docstring `spider_factory.py` actualizado, tests legacy `test_ingestion_sources.py` y `test_source_scheduler.py` borrados, imports de `HubIngestionSource` retirados de test_database_models / test_retriever / test_full_pipeline / test_ingestion_language, `test_edge_boundary.py` actualizado (hub_ingestion_sources retirado del set, hub_web_sites/hub_crawled_pages/hub_corpus_selections añadidos). `grep -r HubIngestionSource` en `server/app/` y `server/tests/` (excluyendo el test que verifica su ausencia y la migración) = 0. DB local en estado divergente desde 1C.1 (alembic apuntaba a q8 sin tablas creadas): tablas reconstruidas vía `create_all` + `alembic stamp t1c2d3e4f5g6`. 15/15 tests nuevos verdes en `test_site_model.py`; 246/246 tests verdes en `tests/modules/agents_hub/`. |
| 2026-05-27 | (replanificación — rebase bloque 9Q a split sitio/corpus) | Plan_TDD_Fase1.md: **rebase del bloque 9Q** tras detectar que el modelo per-chatbot fundía crawl e ingesta y no soportaba (a) auditar todo un sitio ingiriendo solo partes, (b) una página alimentando varios chatbots, (c) un único crawl por sitio. Decisión 2026-05-27: **split sitio/corpus** con 3 entidades nuevas — `HubWebSite` (unidad de crawl+auditoría, propiedad del client no del chatbot), `HubCrawledPage` (página rastreada con señales y flags de calidad), `HubCorpusSelection` (mapeo N:M página→chatbot por regla path_prefix/sitemap_section/manual) + `HubDocument.crawled_page_id`. **`HubIngestionSource` (monitor de URL suelta) se reemplaza** y se elimina; la ingesta de PDF subido es independiente y se mantiene. Auditoría semántica configurable por sitio (`audit_semantic_scope`: ingested/full). Actualización periódica vía **diff de sitemap** (nuevas/cambiadas/desaparecidas) + auto-ingesta de candidatas. 8 prompts → 10: 9Q.0 (**Opus** — modelo+migración+retirada HubIngestionSource), 9Q.1 (Sonnet — findings keyed a sitio/página), 9Q.2 (**Opus** — crawl de sitio + señales + diff sitemap), 9Q.3 (Sonnet — determinista site-scoped), 9Q.4 (**Opus** — semántico + audit_semantic_scope), 9Q.5 (Sonnet — SiteQualityJob + scheduler + consolidación + auto-ingesta), 9Q.6 (Sonnet — retriever excluye páginas superseded), 9Q.7 (Sonnet — capa de selección + gestión de sitios + candidatas), 9Q.8 (Sonnet — informe por sitio + export + endpoints), 9Q.9 (Sonnet — frontend sitios+mapeo+candidatas+auditoría + pruebas manuales). Cursor 9Q.1 → 9Q.0. Total Fase 1: 53 → 55 prompts. |
| 2026-05-27 | (planificación — bloque 9Q calidad de contenido web) | Plan_TDD_Fase1.md: nuevo **Bloque 9Q (Calidad de Contenido Web Ingestado)** con 8 prompts TDD atómicos, insertado entre el bloque SBX y Fase 11. Decisión arquitectónica (2026-05-27): **motor único de detección con dos canales de salida** frente a app/crawler independiente — razón principal: no rastrear dos veces webs de administración pública (rate limits, cortesía); el informe es otro consumidor, no otro motor. Salidas: (1) higiene RAG (`superseded`/`quality_score` en HubDocument, retriever que los excluye), (2) informe de auditoría web (JSON + visor admin + export DOCX/PDF). Taxonomía de hallazgos: superseded/duplicate/contradiction/empty/thin/stale/crawl_error/orphan_chunk. Prompts: 9Q.1 (Sonnet — HubContentFinding + contratos + repo + flags + migración), 9Q.2 (Sonnet — señales baratas en crawl: Last-Modified/ETag/sitemap lastmod/canonical/año), 9Q.3 (Sonnet — detector determinista: vacías/finas/stale/error/supersesión por URL+fecha), 9Q.4 (**Opus** — detector semántico: clustering pgvector + juez LLM duplicado/contradicción + control de coste), 9Q.5 (Sonnet — job asíncrono no bloqueante + scheduler propio + consolidación de flags), 9Q.6 (Sonnet — retriever excluye superseded), 9Q.7 (Sonnet — report builder + export DOCX/PDF + endpoints edge), 9Q.8 (Sonnet — UI cola de revisión + visor + descarga + pruebas manuales). Todo edge. **Sin accesibilidad del sitio rastreado** (backlog v2; accesibilidad del frontend ya en Fase 20). Cursor movido 11.1 → 9Q.1. Total Fase 1: 45 + 8 = 53 prompts. |
| 2026-05-05 | CF.1 completo | Infraestructura contractual: export_openapi.py + openapi.json |
| 2026-05-05 | CF.2 completo | Orval instalado, orval.config.ts, carpeta generated/ |
| 2026-05-05 | CF.3 completo | Compilación TypeScript limpia con tipos generados |
| 2026-05-10 | — | Configuración de permisos Claude Code + regla de estado |
| 2026-05-10 | CF.4.2 | Refactor ChatbotsPage: Orval hooks + zodResolver + mapApiErrorsToFormErrors |
| 2026-05-10 | CF.4.3 | Extraer mapApiErrorsToFormErrors a shared/utils/formErrors.ts + tests + test (e) verde |
| 2026-05-10 | CF.4.4 | Eliminar chatbots.ts: migrar todas las llamadas fetch de 4 páginas a hooks Orval; 96/97 tests verdes |
| 2026-05-11 | CF.5.1 | Añadir job "contract" al CI: exportar OpenAPI → Orval → tsc --noEmit → vitest run |
| 2026-05-11 | CF.5.2 | Prueba de fuego del contrato: name→display_name genera 17 errores TS2339; corregidos 5 errores preexistentes (customInstance firma, _ContractCheck, kind cast, comparación 'vector', variable sin usar) |
| 2026-05-11 | 9R (planificación) | Incorporado el bloque 9R Redacción Contract-First en Plan_TDD_Fase1.md (29 prompts atómicos TDD: contratos, GENERIC_REPORT, bloques, LLMSpecService, ExtractionPipelineFactory, DraftingCoreGraph, UI por contrato, HITL, RunManifest, vertical slice MVP). 9.11a–9.11d marcados superseded. |
| 2026-05-11 | 9B.4 | Contrato de evidencias: EvidenceItem + RetrievalResult + RetrievalPipeline Protocol + GraphDeps. 12 tests verdes en tests/public_graphs/test_retrieval_contract.py. |
| 2026-05-11 | 9B.5 | RetrievalPipelineFactory + 3 pipelines (RagVectorPipeline, MdLongContextPipeline, MdAgentSelectorPipeline stub). 29 tests verdes en tests/public_graphs/. |
| 2026-05-11 | 9B.6 | protocols.py: RetrievalOutput + RetrievalStrategy Protocol + PipelineRetrievalStrategy. 5 tests verdes en test_retrieval_strategy_protocols.py. |
| 2026-05-11 | 9B.7 | MergeStrategy/TemplateStrategy/LanguagePolicy en protocols.py + core/core_graph.py (CoreGraph + CoreGraphState). 3 tests verdes en test_core_graph.py. |
| 2026-05-11 | 9B.8 | profiles/public_kb_rich.py: SingleSourceRetrievalStrategy + PassthroughMergeStrategy + GenericAnswerTemplateStrategy + DefaultLanguagePolicy. 3 tests verdes en test_public_kb_rich.py. |
| 2026-05-11 | 9B.9 | profiles/public_portal_router.py: PortalRouterRetrievalStrategy (selección stub → primer hijo, carga config efectiva, usa retrieval_mode del hijo). 2 tests verdes en test_public_portal_router.py. |
| 2026-05-11 | 9B.10 | 7 tests RED en test_uji_aggregator.py + stub public_portal_aggregator.py. Fallan por comportamiento (stub vacío), no por imports. Contrato: 2 buckets por fuente, merge condicional, secciones template, warning traducción. |
| 2026-05-11 | 9B.11 | UjiDualSourceRetrievalStrategy + UjiMergeStrategy + UjiAnswerTemplateStrategy implementados. 7 tests verdes en test_uji_aggregator.py. |
| 2026-05-11 | 9B.12 | PreferLanguagePolicy + StrictLanguagePolicy + NeutralLanguagePolicy en protocols.py. CoreGraph extiende estado con translation_warning + llama filter_items/should_warn_translation en merge_node. DefaultLanguagePolicy hereda PreferLanguagePolicy. 59 tests verdes (10 nuevos en test_language_policy.py). |
| 2026-05-11 | 9B.13 | GraphFactory en core/graph_factory.py: resuelve config → selecciona perfil en registry → crea CoreGraph. Registro de los 3 perfiles (PUBLIC_KB_RICH, PUBLIC_PORTAL_AGGREGATOR, PUBLIC_PORTAL_ROUTER) con stubs. 62 tests verdes (3 nuevos en test_graph_factory.py). |
| 2026-05-11 | 9B.14 | docs/GRAPH_PROFILES.md (guía CoreGraph, perfiles, retrieval_mode, cómo extender). test_profile_contract.py: 12 tests parametrizados (3 perfiles × compile+non-null + 3 perfiles × 3 modos smoke). test_pipeline_contract_suite.py: 6 tests parametrizados (3 pipelines × 2 contratos). 80 tests verdes en total. Subfase 9B completada. |
| 2026-05-11 | (planificación Fase 2) | Plan_TDD_Fase2.md enriquecido con análisis de migración PDF: nuevo prompt 9.12b.0 (auditoría funcional legacy + spec), Guía 9C.1 (tabla de 12 pasos atómicos con artefactos verificables), riesgos explícitos en 9.12b, firmas de `prompts.py`, `ExtractionPhase` state machine en 9.13, tabla de niveles de automatización, tests de `useExtractionRun` ampliados de 3 a 8 + 8 tests nuevos de componentes wizard. |
| 2026-05-11 | (config) | CLAUDE.md: regla de retirada legacy actualizada a dos pasos — Caso A (NiceGUI en migración activa) → mover a `_legacy_nicegui/` al cerrar el prompt GREEN, borrar al cerrar el prompt de verificación de subfase; Caso B (código huérfano) → borrar directamente. |
| 2026-05-12 | (planificación 1.C) | Plan_TDD_Fase1.md: añadidos 3 prompts en Subfase 1.C — 1C.1 (Autosave con concurrencia optimista), 1C.2 (Editor accesible: shortcuts + focus trap + WCAG 2.2 AA), 1C.3 (Preview imprimible + anexo de auditoría compartido con ExportService). PROJECT_STATE.md: corregido nombre de Subfase 1.C ("Privacidad y Exportación" → "Infraestructura de Diseño y Exportación Avanzada"). |
| 2026-05-12 | (planificación 9R + 1.B) | Plan_TDD_Fase1.md: añadidos 3 prompts en bloque 9R — 9R.3.3 (BlockReference + projection + orden topológico + detección de ciclos), 9R.4.4 (versionado de plantillas: política de anclaje y endpoints de migración de workspaces), 9R.6.6 (BlockState=failed + BlockExecutor con retry + propagación de fallos por topología). 9R.6.5 ampliado con instrumentación Langfuse (span raíz + spans por nodo + atributos de modelo/tokens). PROJECT_STATE.md: restaurada Fase 10 (Plantillas y Temas) dentro de Subfase 1.B. |
| 2026-05-12 | 9R.0 | docs/REDACCION_CONTRACT_FIRST.md: propósito del bloque, 8 decisiones de diseño, diagrama DraftingCoreGraph, mapa de ejecución 9R.0→9R.10, reglas duras, glosario. CLAUDE.md: párrafo modules/redaccion/ como edge. Esqueleto de carpetas server/app/modules/redaccion/{contracts,profiles,pipelines,graph,services}/ + frontend/src/redaccion/. |
| 2026-05-12 | 9R.1.1 | ReportTemplateContract + ReportTemplateVersion + ReportTemplateSpec + SectionContract + stubs BlockContract/InputContract/ReportUIContract. AIBlockPolicy/ReviewPolicy/ExportPolicy enums. frozen=True en Version. Validador global→platform. 10 tests verdes en tests/redaccion/test_report_template_contract.py. |
| 2026-05-12 | 9R.1.2 | blocks.py: BlockContract discriminated union (10 tipos, discriminator=kind). inputs.py: InputSlot + InputContract. ui.py: UISection + UIDropzoneDescriptor + UIFieldDescriptor + ReportUIContract. template.py refactorizado para importar de los nuevos módulos. 31 tests verdes en total (21 nuevos). |
| 2026-05-12 | 9R.1.3 | runtime.py: BlockStatus/WorkspaceStatus Literal + _VALID_TRANSITIONS + InvalidBlockTransitionError + Citation + ApprovalRecord + InputArtifact + ExtractionWarning + BlockState.validate_transition + WorkspaceState. drafts.py: ReportTemplateDraft + DraftValidationError + ReportTemplateDraftValidationResult. 44 tests verdes en total (13 nuevos). |
| 2026-05-12 | 9R.1.4 | ORM models: HubReportTemplate + HubReportTemplateVersion + HubWorkspace + HubWorkspaceBlock (UNIQUE workspace_id+block_id) + HubRunManifest. Repos: ReportTemplateRepo + ReportTemplateVersionRepo (append-only) + WorkspaceRepo + WorkspaceBlockRepo + RunManifestRepo. Migración m4b5c6d7e8f9 aplicada (BD local). 53 tests verdes en total (9 nuevos). |
| 2026-05-12 | 9R.2.1+2.2 | profiles/registry.py: ReportProfile Protocol + ReportProfileRegistry (register/get/list). profiles/generic_report.py: GenericReportProfile con default_spec() de 9 secciones, 15 bloques, InputContract Excel/PDF/text, AIBlockPolicy.REQUIRED_REVIEW. profiles/__init__.py: auto-register GENERIC_REPORT. ReportProfileId actualizado a Literal de 5 valores. 68 tests verdes en total (15 nuevos). |
| 2026-05-12 | 9R.3.1+3.2 | blocks/handlers.py: 10 handlers (StaticText, UserInput, DeterministicData, Table, Chart, AIAssistedText, AISummary, AIRewrite, Citation, ReviewGate) con uses_ai/requires_approval/validate/execute/to_manifest. services/block_state_machine.py: BlockTransitionEvent enum + BlockTransitionAuditEvent + BlockStateMachine.transition() + check_assembly_readiness(). 82 tests verdes en total (14 nuevos). |
| 2026-05-12 | 9R.3.3 | contracts/block_io.py: BlockReference (block_id, projection, field_path). blocks.py: depends_on → list[BlockReference]. runtime.py: block_outputs añadido a WorkspaceState. services/block_topology.py: BlockTopology.sort() + detect_cycles() + resolve_inputs() + check_dependencies_ready() + safe_field_resolver con pre-validación. generic_report.py + handlers.py actualizados. 98 tests verdes en total (16 nuevos). |
| 2026-05-12 | 9R.4.1 | services/llm_spec_service.py: LLMSpecService(llm, model_name) + propose_template(prompt_nl, owner_kind) → ReportTemplateDraft. _build_system_prompt diferenciado por owner_kind (admin incluye is_global). _extract_json con soporte markdown fences. TypeAdapter para BlockContract. Sin acceso a BD. 102 tests verdes en total (4 nuevos). |
| 2026-05-12 | 9R.4.2 | services/draft_validator.py: DraftValidator.validate() — 4 checks (profile, depends_on, CHART/TABLE→DETERMINISTIC_DATA, AI+REVIEW_GATE) + _normalize() (IDs únicos, orden por sección). drafts.py: proposed_profile cambiado a str para aceptar output crudo LLM. 108 tests verdes en total (6 nuevos). |
| 2026-05-12 | 9R.4.3 | routers/redaccion/llm_drafts_router.py: 4 endpoints (propose, validate, approve-as-template, approve-as-workspace). get_llm_spec_service como dep inyectable. approve endpoints invocan DraftValidator; 403 si partner intenta is_global; 422 si draft inválido. Registrado en main.py bajo _register_edge. 114 tests verdes (6 nuevos). |
| 2026-05-12 | 9R.4.4 | TemplateMigrationService (detect_new_version + migrate_workspace) + NewVersionNotice + CompatibilityConflictError. ORM: 2 columnas nuevas en hub_workspaces (parent_workspace_id, archived_reason). Migración Alembic n5c6d7e8f9a0 aplicada. hub_redaccion_router.py: GET template-update-notice + POST migrate. Registrado en _register_edge. docs/REDACCION_CONTRACT_FIRST.md: sección "Versionado de plantillas". 163 tests verdes (10 nuevos). |
| 2026-05-12 | 9R.5.1 | pipelines/contracts.py: StorageRef + ExtractionInput + ExtractedTable + ExtractedMetric + ExtractionWarning + ExtractionProvenance + ExtractionResult + ExtractionPipeline (Protocol, runtime_checkable). tests/modules/redaccion/test_extraction_contracts.py: 14 tests verdes (nuevo directorio). 94 tests verdes en total. |
| 2026-05-13 | 9R.5.2 | excel_pipeline.py: ExcelExtractionPipeline (pandas+openpyxl, sheet/header_row/required_columns, provenance con _sheet/_header_row). pdf_text_pipeline.py: PDFTextExtractionPipeline (pdfplumber, detección NON_EXTRACTABLE_PDF). conftest.py con fixtures dinámicas (openpyxl + reportlab). 6 nuevos tests verdes. pandas + pdfplumber añadidos a pyproject.toml. 20 tests verdes en tests/modules/redaccion/. |
| 2026-05-13 | 9R.5.3 | pdf_table_pipeline.py: PDFTableExtractionPipeline (camelot lattice→stream, filtro df.shape[1]<2 para falsos positivos de texto plano, NO_TABLES_FOUND warning). manual_pipeline.py: ManualInputPipeline (parse_number en/es, parse_date ISO/EU, validación contra slot dict). conftest.py: fixture pdf_with_tables (reportlab Table+GRID, 3 cols). camelot-py + pdfplumber añadidos a pyproject.toml. 25 tests verdes en tests/modules/redaccion/. |
| 2026-05-13 | 9R.5.4 | factory.py: ExtractionPipelineFactory (register/get/list, UnknownSourceKindError, build_default_factory con imports tardíos para no cargar Docling en import). admin_script_pipeline.py: AdminScriptExtractionPipeline (aprobación HITL, re-audit AST, sandbox subprocess con repr() para serializaroptions como literales Python). services/script_auditor.py: ScriptSecurityAuditor migrado de supervisar_codigo() con AST real (ast.walk). 33 tests verdes en tests/modules/redaccion/. |
| 2026-05-13 | 9R.6.1 | contracts/runtime.py: spec + artifacts_normalized añadidos a WorkspaceState. graph/state.py: re-export. graph/nodes/load_template.py: LoadTemplateNode + TemplateVersionNotFoundError. graph/nodes/validate_inputs.py: ValidateInputContractNode (emite ExtractionWarning missing_input). graph/nodes/file_normalization.py: FileNormalizationNode. graph/core_graph.py: build_core_graph() → LangGraph compilado. 5 tests verdes en test_core_graph_nodes_load.py. |
| 2026-05-13 | 9R.6.2 | graph/nodes/deterministic_extraction.py: DeterministicExtractionNode (itera DETERMINISTIC_DATA blocks, mapea slot→artifact por kind, escribe content+status=extracted). graph/nodes/data_quality_check.py: DataQualityCheckNode (detecta critical kinds, status=in_review) + data_quality_router. graph/nodes/missing_data_question.py: MissingDataQuestionNode (hitl_question warnings accionables). core_graph.py actualizado con nodos nuevos y arista condicional. 6 tests verdes en test_core_graph_nodes_extraction.py. |
| 2026-05-13 | 9R.6.3 | graph/nodes/ai_assist_draft.py: AIAssistDraftNode (LLMService Protocol, contexto solo de extracted/approved, content={text,model_used,prompt_version}, salta approved/locked, status=error en fallo sin contaminar blocks). graph/nodes/citation_traceability.py: CitationAndTraceabilityNode (Citation desde depends_on, proyecciones raw/summary/field). core_graph.py: data_quality_router redirige ok→ai_assist_draft; missing_data→__end__; citations→finish. 5 tests verdes en test_core_graph_nodes_ai.py. |
| 2026-05-13 | 9R.6.4 | contracts/runtime.py: BlockState+original_ai_content, WorkspaceState+user_edits+final_document+final_document_hash. graph/nodes/review_gate.py: UserReviewGateNode (ai_generated→needs_review, status=in_review si pending) + review_gate_router. graph/nodes/apply_user_edits.py: ApplyUserEditsNode (override contenido, preserva original_ai_content). graph/nodes/final_assembler.py: FinalAssemblerNode (solo approved/locked, SHA-256, status=assembled). core_graph.py: flujo completo con arista condicional review_gate. 6 tests verdes en test_core_graph_nodes_assembly.py. |
| 2026-05-13 | 9R.6.5 | contracts/manifest.py: DraftingRunManifest (id, workspace_id, template_version_id, report_profile, warnings, user_approvals, final_document_hash, status_at_close). graph/tracing.py: SpanHandle+TracingService Protocol, NoOpTracingService, traced_node wrapper (node_span, AI attrs, error event). graph/nodes/audit_log.py: AuditLogNode (open_trace con workspace_id, HubRunManifest ORM, run_manifest_id en span). core_graph.py: todos los nodos envueltos con traced_node, audit_log como nodo final. 10 tests verdes en test_core_graph_audit_tracing.py. |
| 2026-05-13 | (planificación orden F1) | Reordenación de subfases 1.B/1.C/Deploy: Fase 10 → 1C.0+1C.1 → Fase 13 NER (solo redacción) → 1C.2-1C.4 → Fase 20 reducida (sin consola conversacional) → Deploy GCP. Admin conversacional diferido a Fase 2; integración NER+expedientes a Fase 3. Plan_TDD_Fase1.md alcance y "Qué NO" actualizados. |
| 2026-05-13 | (planificación F1) | Fase 11 (Autoinstalación y Distribución Software Libre, prompts 11.1–11.3) recuperada en el orden de ejecución: antes de Deploy GCP. Estaba en Plan_TDD_Fase1.md pero omitida del orden acordado en PROJECT_STATE.md. |
| 2026-05-13 | (planificación — Fase 13 + Fase 20 redactadas) | Plan_TDD_Fase1.md: añadidos 4 prompts TDD atómicos. **Fase 13 NER reversible** (entre 1C.1 y 1C.2 en ejecución): 13.1 (Opus — `RunAnonymizationContext` con forward/reverse maps + `InitAnonymizationNode` en el grafo + hooks pre/post-LLM + 4 modos OFF/DETECT_ONLY/REPLACE/REPLACE_WITH_DISPOSITION_7 + citation preservation + 16 tests; reutiliza PiiDetector y FakerGenerator de 9R.5.5), 13.2 (Sonnet — endpoints GET summary / PATCH mode / POST re-analyze + UI panel en BlockDebugPanel + 10 tests). **Fase 20 reducida** (entre 1C.4 y Fase 11 en ejecución): 20.1 (Sonnet — jest-axe + axe-core + 9 tests baseline + CI gate + helper reutilizable + A11Y_GUIDELINES.md), 20.2 (Sonnet — sweep de remediación por 7 categorías: labels/contrast/headings/focus/aria/images/forms + verde de los 9 baselines + A11Y_CHECKLIST.md manual). Total Fase 1 ahora: 41 prompts, distribución 5 Opus (9R.5.5, 9R.5.8, 9R.10.2, 1C.0.bis, 13.1) + 36 Sonnet. |
| 2026-05-13 | (config — modelo por prompt: cobertura completa Fase 1) | Plan_TDD_Fase1.md: completada la anotación `**Modelo sugerido**` en los 23 prompts restantes (1C.1-1C.4, 10.1-10.11, 11.1-11.3, D.1-D.5). Todos **Sonnet** — son prompts con alcance cerrado, patrones React/FastAPI/Docker conocidos. PROJECT_STATE.md: tabla de bloques actualizada con anotación específica por bloque (Fase 13 NER queda "Sonnet probable, Opus si reversibilidad se complica" pero sus prompts aún no están redactados; idem Fase 20 WCAG). Total Fase 1: 37 prompts anotados, distribución final 4 Opus (9R.5.5, 9R.5.8, 9R.10.2, 1C.0.bis) + 33 Sonnet. |
| 2026-05-13 | (config — modelo por prompt) | CLAUDE.md: nueva sección "Modelo por prompt" con heurística (Opus para decisiones de diseño embebidas + migración >800 LOC + debugging multi-módulo; Sonnet para alcance acotado). Plan_TDD_Fase1.md: 14 prompts anotados con `**Modelo sugerido**` bajo el título (4 Opus: 9R.5.5/9R.5.8/9R.10.2/1C.0.bis; 10 Sonnet: el resto). PROJECT_STATE.md: columna "Modelo sugerido siguiente" en tabla de bloques + línea "Modelo sugerido para el próximo prompt" junto al cursor. Convención: el agente menciona discrepancia si el modelo de sesión no coincide con el sugerido, pero no auto-cambia. |
| 2026-05-13 | (planificación 9R.5.9 — adelanto Docling rico) | Plan_TDD_Fase1.md: insertado **9R.5.9** que sube el `PDFTextExtractionPipeline` actual (que ya usa Docling pero con `do_table_structure=False` y solo `export_to_text()`) a su capacidad completa: produce `ExtractedDocument` con markdown global + markdown por página + tablas estructuradas con bbox + páginas indexadas + heurística text_linear/complex_tables. Retro-compatible (campo `document: ExtractedDocument \| None` añadido a `ExtractionResult`). Micro-ajustes en AIAssistDraftNode (9R.6.3) y CitationAndTraceabilityNode para preferir markdown + tables_json. **No incluye** el wizard interactivo de fases (analyze/extract/refine/generate_script) — eso sigue en Fase 2 prompt 9.13. **Plan_TDD_Fase2.md**: prompt 9.12b marcado como "deuda residual" (lo grueso ya está en 9R.5.9, queda solo limpieza de imports legacy). Decisión: opción B — adelantar solo el motor, no el wizard, porque el wizard tiene mejor encaje con RPA/automatización. |
| 2026-05-13 | (planificación 9R + 1C: rescate legacy charts/ETL/copilot) | Plan_TDD_Fase1.md: insertados **9R.5.7** (block kind CHART, migración de graphics_factory + deterministic_graphics_service legacy con patrón dual determinista+IA) y **9R.5.8** (block kind DATA_TRANSFORM, migración de etl_service + deterministic_etl + etl_factory legacy, operaciones filter/aggregate/join/pivot/groupby + modo IA NL→operations con refinamiento iterativo). Añadido **1C.0.bis** (Copilot Drawer: RAG sobre docs + NL→config estructurada para chart/etl/script, migra conceptualmente copilot_chat.py y focus_manager.py legacy). Decisión: opción A (todo en MVP) para mantener "determinista first" coherente — sin CHART y DATA_TRANSFORM, cualquier cálculo no trivial caería en IA, contradiciendo la premisa. Migración legacy a `_legacy_nicegui/` en cada prompt GREEN. |
| 2026-05-13 | (config) | CLAUDE.md: regla de retirada legacy revisada — Caso A ahora es **un solo paso automático** (mover a `_legacy_nicegui/` al cerrar el prompt GREEN). El borrado definitivo de `_legacy_nicegui/` se realiza **manualmente por el usuario** al cierre de Fase 1 completa, NO al cierre de cada subfase. `_legacy_nicegui/` pasa a ser cuarentena de larga duración (referencia viva durante toda Fase 1). Plan_TDD_Fase1.md prompt 9R.5.5 actualizado para reflejar esta regla. |
| 2026-05-13 | (planificación 9R.5.5 refinado) | Plan_TDD_Fase1.md: ampliado **9R.5.5** para migrar el módulo de anonimización legacy de `client_app/app/modules/privacy/` (1011 líneas + 14 tests). Incluye spaCy NER + Faker contextual + regex + anclajes de formulario + Disposición 7ª LOPDGDD. Reemplaza la propuesta inicial de "Faker manual sin NER" (insuficiente para PDFs y propensa a errores en Excel). Adapta a `StorageRef`, async I/O, sin EncryptionService (post-MVP). Para PDFs: opción 1 — Docling → markdown → anonimizar texto → devolver .md sintético (sin regenerar PDF). Fase 13 se reduce a hooks pre/post-LLM reutilizando PiiDetector. Mínimo 19 tests verdes. |
| 2026-05-13 | (planificación 9R scripts + DOCX-only) | Plan_TDD_Fase1.md: insertados 4 prompts nuevos en bloque 9R — **9R.5.5** (ScriptProposalService + TestDataAnonymizerService + endpoints propose/anonymize/test/validate-test-result), **9R.5.6** (workflow aprobación scripts: save-to-private-template self-service + submit-for-review + cola admin + admin-retest con hash match + approve/reject), **9R.7.5** (UI wizard 7 pasos para usuario + AdminScriptReviewQueuePage), **9R.7.6** (statusLabels.ts + StatusBadge + BlockDebugPanel admin-only). Modificado **9R.9.2** a DOCX-only (ODT a backlog post-MVP, mantener abstracción Exporter). Sandbox test obligatorio antes de persistir (test_validated_by_proposer_at). Anonimización obligatoria si target_owner_kind='platform'. |
| 2026-05-13 | 9R.8.1 | workspaces_router.py (Deploy: edge): 5 endpoints PATCH approve/reject/regenerate/edit + POST resume. HubWorkspaceAuditEvent ORM model + WorkspaceAuditEventRepo. Migración 6dc2c440f15e aplicada. 6/6 tests verdes en test_block_transitions.py. |
| 2026-05-13 | 9R.7.4 | llm_drafts_router: operation_id añadidos (proposeLlmDraft/validateLlmDraft/approveAsTemplate/approveAsWorkspace). Orval regenerado. LLMDraftPreviewPage: prompt textarea → propose → draft preview editable → auto-validate (useEffect) → modo template/workspace (admin) → approve. Ruta /redaccion/draft registrada. 5/5 tests verdes. TypeScript limpio. |
| 2026-05-13 | 9R.7.3 | hub_redaccion_router: DTOs TemplateOut/TemplateCreateIn/WorkspaceCreateIn/WorkspaceCreatedOut + endpoints listTemplates/createTemplate/createWorkspace (operation_id explícito). Orval: 54 paths, 78 schemas; hooks useListTemplates/useCreateTemplate/useCreateWorkspace. frontend: ReportTemplateBuilderPage (admin, access-denied para no-admin) + GenericReportWizard (user). Rutas /redaccion/builder y /redaccion/wizard registradas en App.tsx. 4/4 tests verdes. TypeScript limpio. |
| 2026-05-13 | 9R.7.2 | hub_redaccion_router: DTOs WorkspaceOut/BlockStateOut/BlockPatchRequest/ExtractionWarningOut + 3 endpoints (getWorkspaceById, patchWorkspaceBlock, getWorkspaceWarnings). Orval regenerado (52 paths, 74 schemas). frontend/src/redaccion/components/: BlockEditor + AIBlockReviewPanel + DataQualityPanel + WorkspaceStatusBar. Casts `as unknown as WorkspaceOut` por patrón customInstance que devuelve r.data directo. 5/5 tests verdes (WorkspacePanels.test.tsx). TypeScript limpio. |
| 2026-05-13 | 9R.7.1 | hub_redaccion_router: GET /template-versions/{id}/ui-contract → ReportUIContract (expone UIDropzoneDescriptor, UIFieldDescriptor, UISection en OpenAPI). OpenAPI regenerado (49 paths, 70 schemas). Orval: nuevos tipos ReportUIContract, UIDropzoneDescriptor, UIFieldDescriptor, UISection, UIDropzoneDescriptorLabel, UIFieldDescriptorLabel. frontend/src/redaccion/components/: ReportUIContractRenderer.tsx + DynamicUploadSlots.tsx + DynamicFieldRenderer.tsx. i18n common: +continue (es/en/ca). 5/5 tests verdes (test_ReportUIContractRenderer). TypeScript limpio. |
| 2026-05-13 | 9R.6.6 | runtime.py: FailureKind Literal + WorkspaceBlockedByFailedBlocksError + BlockState(failure_kind, last_error_message, retry_attempts) + WorkspaceState(regenerate_blocks, skip_blocks). services/block_executor.py: LLMTimeoutError/ScriptRuntimeError/ASTValidationError + BlockExecutor(retry 1×, span events, failure_kind map) + NodeResult + propagate_dependency_failures(cascada). ai_assist_draft.py: per-block failed(ai_failed), continúa. review_gate.py: REGENERATE (reset por failure_kind) + SKIP (solo no-required). final_assembler.py: lanza WorkspaceBlockedByFailedBlocksError para required fallidos fuera de skip_blocks. manifest.py: FailedBlockInfo + failed_blocks. audit_log.py: recoge failed_blocks. ORM: +failure_kind+last_error_message+retry_attempts en hub_workspace_blocks. Migración o6d7e8f9a0b1 (pendiente de BD). 79 tests verdes (14 nuevos en test_core_graph_failed_blocks.py). |
| 2026-05-15 | 9R.5.5 | modules/redaccion/services/anonymization/ (AnonymizationContext + FakerGenerator + PiiDetector + AnonymizerService + policies) — migración fiel del legacy de 1011 LOC con StorageRef, async I/O, sin EncryptionService, sin enterprise_audit, fallback explícito sin spaCy, Disposición 7ª LOPDGDD. services/test_data_anonymizer.py: describe_columns/anonymize_tabular/anonymize_pdf_to_text/preview_pdf_spans (PDF→markdown sintético sin regenerar PDF). services/script_proposal_service.py: propose() con few-shot + audit_result via ScriptSecurityAuditor. ORM HubScriptProposal + ScriptProposalRepo + migración p7e8f9a0b1c2 aplicada. routers/redaccion/scripts_router.py con 6 endpoints (propose/describe-test-data/preview-pdf-spans/anonymize-test-data/test/validate-test-result) registrado en _register_edge. docs/REDACCION_CONTRACT_FIRST.md: nueva sección "Módulo de anonimización". Legacy movido a _legacy_nicegui/ (privacy/, anonymization_service.py, pii_detector.py). 19 tests RED→GREEN (11 anonimización + 8 workflow). Suite redacción completa: 104/104 verdes. |
| 2026-05-15 | 9R.5.6 | routers/redaccion/scripts_router.py: 6 endpoints nuevos (save-to-private-template, submit-for-review, GET /pending, admin-retest, approve, reject) con gates save_to_private_template (AUDIT_FAILED / TEST_NOT_VALIDATED), submit-for-review (NOT_ANONYMIZED), approve (HASH_MISMATCH con ventana 10 min), reject (blocks_further_changes). HubScriptProposal.admin_retest_json añadido + migración q8f9a0b1c2d3 aplicada. _embed_script_block helper incrusta DETERMINISTIC_DATA en nueva HubReportTemplateVersion (append-only). docs/REDACCION_CONTRACT_FIRST.md: sección "Workflow de scripts metaprogramados". 11 tests RED→GREEN. Suite redacción completa: 115/115 verdes. |
| 2026-05-15 | 9R.5.7 | Block kind CHART: DeterministicChartService (5 tipos bar/line/pie/scatter/histogram + agregación, matplotlib Agg in-process), ChartFactory (LLM→matplotlib script + ScriptSecurityAuditor), chart_renderer (sandbox subprocess: audit AST→CSV tmp→wrapper script→savefig), ChartBlockConfig (mode deterministic/ai + x_axis/y_axis/color_by/nl_prompt). ChartHandler async (execute() rutas determinista e IA, to_manifest con chart_type+mode). charts_router (POST /preview/deterministic + /preview/script). WHITELIST_MODULES extendida (matplotlib/seaborn/numpy/base64). test_edge_boundary.py corregido (hub_script_proposals + hub_workspace_audit_events). matplotlib+seaborn añadidos a pyproject.toml. Legacy movido a _legacy_nicegui/ (graphics_factory, graphics_service, deterministic_graphics_service, graphics_page, chart_configuration, tests asociados). 16 tests RED→GREEN. Suite completa: 375/375 verdes. |
| 2026-05-16 | 9R.5.8 | Block kind DATA_TRANSFORM: operations.py (discriminated union FilterOp/AggregateOp/JoinOp/PivotOp/NormalizeOp/GroupByOp + parse_operations), DeterministicETLService.execute(df, ops, joinable_resolver?), ETLFactory (NL→list[Operation] con refinamiento iterativo max=3 + fallback a script Python auditado vía ScriptSecurityAuditor), ETLService.run(mode=deterministic/ai). DataTransformBlockConfig (mode/source_block_ref/operations/nl_instruction + validador). DataTransformHandler async (execute rutas determinista e IA via ETLService, to_manifest con operations_applied). DataTransformationNode en grafo (entre deterministic_extraction y data_quality_check, failure_kind=script_failed aísla fallos por bloque). core_graph.py actualizado (etl_llm/etl_model_name). docs/REDACCION_CONTRACT_FIRST.md: sección "Bloques de transformación" + DataTransformBlock añadido a la enumeración de tipos. Legacy movido a _legacy_nicegui/ (etl_service, deterministic_etl_service, etl_factory, etl_page, transform_operations, 7 tests asociados). 32 tests RED→GREEN. Suite redacción completa: 163/163 verdes. |
| 2026-05-16 | 9R.5.9 | PDFTextExtractionPipeline actualizado a extracción Docling rica: do_table_structure=True, produce ExtractedDocument (markdown completo, páginas con ExtractedPage, tablas con ExtractedTableRich + bbox por tabla y celda, heurística extraction_strategy text_linear/complex_tables). ExtractionResult.document: ExtractedDocument\|None añadido a contracts.py. DeterministicExtractionNode: asyncio.to_thread + serializa document en block.content. AIAssistDraftNode: prefiere document.markdown[:30000] sobre free_text; si complex_tables, añade tables_json al contexto. CitationAndTraceabilityNode: rellena Citation.page desde pages[0].page_num del documento rico. 16 tests RED→GREEN en test_docling_rich_extraction.py. Suite completa: 423/423 verdes. |
| 2026-05-17 | 9R.7.6 | statusLabels.ts (mapBlockStatusToUserLabel/mapWorkspaceStatusToUserLabel/mapFailureKindToKey), StatusBadge.tsx (shared), BlockDebugPanel.tsx (admin/partner only, colapsable, muestra last_error_message). i18n namespace `redaccion` (es/en/ca). BlockEditor + WorkspaceStatusBar + AIBlockReviewPanel refactorizados: ningún `{block.status}` crudo en JSX (grep limpio). Mensajes de fallo traducidos por failure_kind. 6 tests verdes en StatusLabels.test.tsx. Suite completa: 34/34 verdes. TypeScript limpio. |
| 2026-05-17 | 9R.8.2 | 5 E2E tests HITL block transitions (approve, reject→regenerate, edit, missing-input resume, admin lock) en tests/modules/redaccion/e2e/test_block_transitions_e2e.py. AsyncMock + MagicMock stateful: mutaciones de block.status persisten entre requests. test_e2e_admin_locks_block_to_prevent_further_changes: 422 para reject/regenerate desde approved. 5/5 verdes. |
| 2026-05-17 | 9R.9.1 | DraftingRunManifest expandido: UploadedDocumentInfo + InputContractValidationResult + ExtractedBlockSummary + AIBlockSummary + campos user_approvals/warnings/failed_blocks/final_document_hash/status_at_close. template_id: UUID\|None (retrocompatible con AuditLogNode). manifests_router.py: GET /workspaces/{id}/manifest + GET /manifests/{id}. Registrado en _register_edge. 7/7 tests verdes. |
| 2026-05-17 | 9R.9.2 | ExportService DOCX (export_service.py): ExportNotReadyError si hash=None, anexo de auditoría con uploaded_docs/ai_blocks/approvals/warnings en tablas. python-docx>=1.1.0 añadido a pyproject.toml. docs/REDACCION_CONTRACT_FIRST.md: sección "Exportación DOCX" con justificación ODT-backlog. 6/6 tests verdes + 1 skip condicional LibreOffice. Suite: 197/197 verdes. |
| 2026-05-17 | 9R.10.1 | 7 tests RED de aceptación del slice GENERIC_REPORT MVP en test_generic_report_slice_mvp.py. Fallos previstos: AssertionError (status='draft' sin graph), 404 (upload/run no implementados), ModuleNotFoundError (WorkspaceRunService inexistente). slice_mvp_status.md: checklist de wire-up para GREEN. 7/7 fallan como RED. |
| 2026-05-17 | 9R.10.2 | WorkspaceRunService (validate_inputs + run_sync dry-run + start_run async). 2 endpoints nuevos en workspaces_router.py: POST /{id}/inputs/{slot_id} (upload multipart → StorageService + inputs_json) y POST /{id}/run (RunStartedOut, status 202, transición ingesting→drafting). approve-as-workspace ahora devuelve status="ingesting" en lugar de "draft". InputUploadOut + RunStartedOut DTOs. OpenAPI 77 paths/130 schemas. 7/7 tests slice MVP verdes. Suite redacción: 204 passed, 1 skipped. **Bloque 9R completado.** |
| 2026-05-17 | 9R.9.1 | DraftingRunManifest expandido (frozen, +UploadedDocumentInfo/ExtractedBlockSummary/AIBlockSummary/InputContractValidationResult/template_id/model_used/citations/prompt_versions). manifests_router.py: GET /workspaces/{id}/manifest + GET /manifests/{id}. Registrado en _register_edge. OpenAPI regenerado (75 paths, 127 schemas). Orval + TypeScript limpios. 7/7 tests verdes. |
| 2026-05-17 | 9R.8.2 | 5 tests E2E de transiciones de bloque en server/tests/modules/redaccion/e2e/test_block_transitions_e2e.py: approve→assemble, reject→regenerate→approve, edit+original preserved, missing_input→upload→resume, admin locks (422 en reject/regenerate desde approved). AsyncMock+TestClient stateful (mismo mock object entre requests). 5/5 verdes. |
| 2026-05-17 | 9R.7.5 | ScriptProposalWizardPage (7 pasos: describe→código→test data→anon→PDF preview→test→validate+save) + AdminScriptReviewQueuePage (cola admin con redirect no-admin a /hub, ProposalCard con useAdminRetestScript + hash-match-indicator + btn-approve-proposal). i18n namespace `scripts` (es/en/ca). Rutas /redaccion/scripts/wizard y /redaccion/scripts/review en App.tsx. Orval ya generado (73 paths, 118 schemas). 9 tests verdes. TypeScript limpio. |
| 2026-05-17 | (planificación — bloque SBX seguridad sandbox) | Plan_TDD_Fase1.md: nuevo bloque **SBX (Sandbox aislado de ejecución de scripts)** con 4 prompts atómicos TDD, insertado entre Fase 20 reducida y Fase 11. Decisión arquitectónica: "Microservicio Evaluador" (contenedor estático `script-sandbox` con red dedicada aislada de postgres/minio/internet) frente a Docker-out-of-Docker (descartado por requerir socket de Docker, no compatible con Cloud Run). SBX.1 (Opus — FastAPI sandbox + runner subprocess efímero + re-auditoría AST defensiva + Dockerfile, 13 tests), SBX.2 (Sonnet — SandboxClient httpx + LocalSandboxClient + settings, 12 tests), SBX.3 (Sonnet — refactor admin_script_pipeline + chart_renderer + etl_service + scripts_router a SandboxClient async, 0 ocurrencias de subprocess/exec en redaccion/), SBX.4 (Sonnet — docker-compose dev+prod con red sandbox-net internal, cap_drop ALL, read_only, tmpfs, user 65534 + docs/SANDBOX_SECURITY.md + verificación manual de aislamiento). Capas de defensa: AST audit → red aislada → contenedor sin privilegios → subproceso efímero → rlimits Docker → gVisor en GCP. Total Fase 1: 41 + 4 = 45 prompts, distribución 6 Opus + 39 Sonnet. |
| 2026-05-18 | 10.1 | Fase 10 TDD RED: frontend/src/themes/__tests__/theme.test.ts — 14 tests que importan ThemeConfig/validateTheme/DEFAULT_THEME/mergeThemes/ThemeColors/ThemeTypography/ThemeSpacing/ThemeBorderRadius/ThemeComponents desde ../types (módulo aún inexistente). Fallo confirmado: "Failed to resolve import ../types". |
| 2026-05-18 | 10.2 | Fase 10 TDD GREEN: frontend/src/themes/types.ts — ThemeColors/ThemeTypography/ThemeSpacing/ThemeBorderRadius/ThemeShadows/ThemeAnimations/ThemeComponents/ThemeConfig interfaces + DEFAULT_THEME (azul universitario) + validateTheme (regex hex/rgb/hsl, required colors) + mergeThemes (deepMerge recursivo). 15/15 tests verdes. Suite completa: 146/146. |
| 2026-05-18 | 10.3 | Fase 10 TDD RED: frontend/src/themes/__tests__/ThemeProvider.test.tsx — 16 tests (useTheme hook, theme loading, CSS vars injection, persistence localStorage, predefined themes). Fallo confirmado: "Cannot resolve import ../ThemeProvider". |
| 2026-05-18 | 10.4 | Fase 10 TDD GREEN: ThemeProvider.tsx (React Context + CSS vars injection via style element + localStorage + loadTheme/switchTheme/resetTheme) + presets.ts (DARK_THEME #1a1a2e, UNIVERSITY_THEME #003366, HIGH_CONTRAST_THEME #0000ff, THEME_PRESETS). loadTheme separado de setTheme: no persiste en localStorage (evita contaminación entre tests), resetea a DEFAULT_THEME en error. 14/14 tests verdes. Suite: 160/160. |
| 2026-05-18 | 10.5 | Fase 10 Presets: presets.ts ya creado como dependencia de 10.4. Contenido verificado contra plan (DARK/UNIVERSITY/HIGH_CONTRAST + THEME_PRESETS + ThemePresetName). Sin tests propios — cubierto por los 14 tests del 10.4. Suite: 160/160. |
| 2026-05-18 | 10.6 | Fase 10 CSS Base: frontend/src/themes/base.css — reset + fallback vars CSS (:root con colores/tipografía/espaciado/radios/sombras/animaciones/componentes) + clases .chatbot-widget/.chatbot-header/.chat-bubble/.chatbot-input/.chatbot-btn/.chatbot-typing + @keyframes fadeIn/slideIn/typing + .sr-only + responsive 480px. Sin tests propios. Suite: 160/160. |
| 2026-05-18 | 10.7 | Fase 10 ThemeEditor: ThemeEditor.tsx (tabs colores/tipografía/preview, ColorInput, applyPreset, handleExport/Import JSON, validationErrors, handleSave/Preview/Reset) + ThemeEditor.module.css (grid colores 2 cols, tabs, actions bar). TypeScript limpio. Sin tests propios. Suite: 160/160. |
| 2026-05-18 | 10.8 | Fase 10 API temas: server/app/routers/hub_themes_router.py (Deploy: cloud) — ThemeColors/ThemeTypography/ThemeSpacing/ThemeConfig/ThemeCreate/ThemeUpdate/ThemeResponse + 6 endpoints: GET /presets (no auth), GET /hub/themes (partner), GET\|POST /hub/themes/{id} (partner/admin), PUT\|DELETE /hub/themes/{id} (admin), POST /{id}/apply/{chatbot_id}. Almacenamiento simplificado en data/themes/. Registrado en _register_cloud. Suite redacción: 204/204. |
| 2026-05-18 | 10.9 | Fase 10 Tests: server/tests/test_hub_themes.py — TestThemeValidation (3 tests: valid/invalid name/extra fields), TestThemeAPI (3 tests: GET presets no-auth, POST 401 sin auth, POST 201 con dependency_overrides+tmp_path patch THEMES_DIR), TestThemeStorage (1 test: _save_theme/_load_theme round-trip con patch THEMES_DIR→tmp_path). ASGITransport minimal test app sin lifespan. 7/7 verdes. |
| 2026-05-18 | 10.10 | Fase 10 App.tsx: ThemeProvider importado y envuelto alrededor de BrowserRouter (dentro de QueryClientProvider). base.css importado. QueryClient con defaultOptions (retry 2, staleTime 5min). getThemeUrl() lee ?theme= de URL params y lo pasa como themeUrl al Provider. TypeScript limpio. Suite frontend: 160/160. |
| 2026-05-18 | 10.11 | Fase 10 AIBrainPage: AIBrainPage.tsx (chatbot select + prompt editor con HighlightedVars + version display + model select con is_default + sandbox test latency) + i18n es/en/ca (14 claves brain_*) + ruta /hub/brain en App.tsx + nav en HubLayout. Tests: test_prompt_editor_saves_new_version (versión v3→v4 mostrada tras save) + test_model_selector_updates_chatbot (PATCH is_default:true verificado). 2/2 verdes. Suite: 162/162. Fase 10 completa. |
| 2026-05-18 | 1C.0 | Subfase 1.C FocusMode: npm install zustand + useFocusStore.ts (Zustand, viewMode/drawerVisible/activeTab/context + setViewMode/toggleDrawer/setActiveTab/setContext/reset) + DrawerHub.tsx (tabs dinámicas: informe→Bloques/Datos/IA, flujo→Configuración/Data Pills/Copilot; role=tab) + FocusLayout.tsx (HOC setContext+setViewMode en mount, reset en unmount, sidebar-collapsed). 5/5 tests verdes. Suite: 167/167. |
| 2026-05-18 | 1C.0.bis | Subfase 1.C Copilot Drawer: backend services/copilot/ (DocsRetriever con embeddings inyectables + chunking Markdown + filtro módulo; CopilotService.answer→RAG con citas, .translate_nl_to_config→dispatch a ChartFactory/ETLFactory/ScriptProposalService) + copilot_router.py /api/v1/redaccion/copilot/{ask,translate} (Deploy: edge). useFocusStore extendido (pendingAction + dispatchCopilotAction + clearPendingAction); hook useCopilotAction(kind, onApply); CopilotPanel.tsx (pills contextuales + modos ask/chart/etl/script + apply→dispatch); copilotApi.ts (fetch helper); DrawerHub renderiza CopilotPanel cuando activeTab='copilot' (tab añadido en informe). i18n redaccion.copilot.* ES/EN/CA. Tests: 6 pytest (5 plan + 1 defensa) + 2 Vitest. Backend 210 (+6), Frontend 169 (+2). |
| 2026-05-18 | 1C.1 | Subfase 1.C Autosave: migración r9a0b1c2d3e4 añade version int(default 1) a hub_workspaces y hub_workspace_blocks. contracts/state_update.py (BlockUpdate/WorkspaceStatePatch/WorkspaceStatePatchResponse/WorkspaceStateConflictResponse). services/autosave_service.py (WorkspaceAutosaveService.apply_patch con SELECT FOR UPDATE, ConflictError, InvalidTransitionError, WorkspaceNotFoundError, validate transitions ANTES de mutar, rollback en cualquier fallo). Endpoint PATCH /api/v1/redaccion/workspaces/{id}/state (200/404/409/422). Frontend hooks/useAutosave.ts (debounce 1500ms, retry x3 con backoff exponencial, on 409→onConflict + status='conflict', tras retries→'offline'). components/ConflictModal.tsx (reload-only). WorkspaceStatusBar admite autosaveStatus prop con badge tonal. i18n redaccion.autosave.* ES/EN/CA. Tests: 7 pytest (correct/stale ws/stale block/invalid transition/increments/atomic/ownership) + 5 Vitest (debounce/conflict/retry/offline/version). Backend 217 (+7), Frontend 174 (+5). Migración no aplicada en dev (DB en estado divergente: alembic at q8f9a0b1c2d3 pero tablas no creadas vía create_all aún). |
| 2026-05-18 | 13.1 | Fase 13 NER reversible: services/anonymization/run_context.py (RunAnonymizationContext con substitute por longitud desc + reverse exact-match; AnonymizationMode OFF/DETECT_ONLY/REPLACE/REPLACE_WITH_DISPOSITION_7; PiiSpan; AnonymizationSummary sólo metadata; disposition_7_mask para DNI/NIE/Passport). services/anonymization/hooks.py (apply_pre_llm/apply_post_llm no-ops en OFF/DETECT_ONLY). graph/nodes/init_anonymization.py (InitAnonymizationNode con detector+faker inyectables; gather de artifacts_normalized + blocks.content; LOPDGDD máscara irreversible). Hooks aplicados en AIAssistDraftNode (pre context, post text), ChartFactory.generate_script y ETLFactory.generate_operations_from_nl (param opcional anonymization_context). core_graph.py inserta init_anonymization entre data_quality_check(ok) y ai_assist_draft. WorkspaceState.anonymization_mode+anonymization_context. DraftingRunManifest.anonymization_summary. Migración s0b1c2d3e4f5 añade anonymization_mode String(40) default 'replace' a hub_workspaces. Docs en REDACCION_CONTRACT_FIRST.md (§"NER reversible (Fase 13)"). Tests: 20 pytest (8 context + 8 init+hooks + 4 citations+manifest), ≥16 requeridos. Backend 237 (+20). Frontend 174 (sin cambios). |
| 2026-05-18 | 13.2 | Fase 13 UI auditoría NER: anonymization_router.py (Deploy: edge) con 3 endpoints — GET /{id}/anonymization-summary (loads last HubRunManifest.payload_json.anonymization_summary; 404 sin run; 403 no-owner-no-admin), PATCH /{id}/anonymization-mode (422 MODE_LOCKED_DURING_EXECUTION si status in drafting/in_review/assembled/exported; audit event anonymization_mode_changed), POST /{id}/re-analyze (202 queued, audit event re_analyze_started). WorkspaceAnonymizationPanel.tsx (modo radio buttons deshabilitados si locked, badge LOPDGDD para disposition_7, tabla counts_by_type, botón re-analizar). useAnonymizationApi.ts hooks (useGetAnonymizationSummary/usePatchAnonymizationMode/useReAnalyzeAnonymization usando React Query). BlockDebugPanel refactorizado con tabs "Detalles técnicos" / "Anonimización" (tab visible para admin/partner cuando workspace no en ejecución). i18n redaccion.anonymization.* ES/EN/CA. Registrado en _register_edge main.py. Tests: 6 pytest + 4 Vitest (10/10 verdes). Backend 243 (+6), Frontend 178 (+4). Fase 13 completa. |
| 2026-05-20 | 1C.2 | Accesibilidad WCAG 2.2 AA en editor: useEditorShortcuts hook (Escape/Cmd+S/Cmd+Enter/Cmd+Z con handlers inyectados), BlockStateAnnouncer (role=status + aria-live=polite), WorkspaceEditor (prop workspace, aria-describedby por bloque, transiciones anunciadas). FocusLock (react-focus-lock) + role=dialog en DrawerHub. ARIA en BlockEditor (role=region + aria-label + aria-describedby hidden span). test-utils/axe.ts (expectNoAxeViolations con axe-core) + test-utils/fixtures.ts. i18n redaccion.announcer.* + editor.block_region ES/EN/CA. 10/10 tests verdes. Frontend 188/188. |
| 2026-05-20 | 1C.3 | Vista previa imprimible y anexo de auditoría: contracts/preview.py (PreviewPayload/PreviewSection/PreviewBlock/PreviewAuditEntry/TocEntry), services/preview_builder.py (PreviewBuilderService + PendingBlocksError), ExportService.__init__(builder=None)+export()+last_payload_used, endpoint GET /{id}/preview (409 si pending). Frontend: PreviewRenderer.tsx (DOMPurify sanitize, citas superíndice linkadas a #audit-{chunk_id}, data-preview-section attrs), WorkspacePreview.tsx (ruta /redaccion/workspaces/:id/preview, CSS @media print/@page A4), fixtures.ts + samplePreviewPayload. OpenAPI 88 paths/157 schemas. Orval: useGetWorkspacePreview. 5 pytest + 3 Vitest = 8/8 verdes. |
| 2026-05-20 | 1C.4 | Exportación Avanzada DOCX/ODT: modules/agents_hub/services/export_service.py (ExportService(storage) + _build_docx/odt con python-docx/odfpy, regex Markdown parser, Referencias desde retrieved_chunks, font via ThemeConfig). routers/hub_agents_router.py (Deploy: edge, POST /api/v1/hub/agents/workspaces/{id}/export → ExportOut). Registrado en _register_edge. OpenAPI 89 paths/159 schemas. Orval: hub-agents/hub-agents.ts con useExportAgentWorkspace. 6/6 pytest verdes. Subfase 1.C completa. |
| 2026-05-20 | 20.1 | Infraestructura axe-core WCAG: src/test/a11y.ts (helper expectNoA11yViolations con 27 reglas WCAG 2.2 AA). 9 tests baseline en src/__tests__/a11y/ (widget, workspace, llm-draft-preview, script-wizard, chatbots, clients, llm-configs, prompts, login). script test:a11y en package.json. Job a11y en ci.yml (continue-on-error: true). docs/A11Y_GUIDELINES.md + docs/a11y-baseline.md (1 violación detectada: select-name en PromptsPage). 8/9 tests verdes. |
| 2026-05-20 | 20.2 | Sweep WCAG remediación: PromptsPage (aria-label en 2 selects + input búsqueda + aria-labelledby en modal). AppLayout nav aria-label. HubLayout nav aria-label. i18n (es/en/ca): nav.main + hub.subnav_aria + filter_usage/chatbot/search_prompts _aria. CI gate activado (sin continue-on-error). a11y-baseline.md eliminado. docs/A11Y_CHECKLIST.md creado (checklist manual 7 categorías WCAG). 9/9 tests a11y verdes. 191 tests suite principal verdes. Fase 20 completa. |
| 2026-05-21 | SBX.4 | docker-compose.yml (dev): servicio script-sandbox con cap_drop ALL, read_only, security_opt no-new-privileges, user 65534, sandbox-net (internal: false en dev, puerto 5001 en host). docker-compose.prod.yml: mismo servicio sin ports + app conectado a sandbox-net + SANDBOX_BASE_URL inyectado + sandbox-net internal: true. docs/SANDBOX_SECURITY.md: 6 secciones (modelo amenazas, 8 capas defensa, limitaciones, auditoría, gVisor on-premise, dev vs prod). .env.example: SANDBOX_BASE_URL + SANDBOX_TIMEOUT_SECONDS_DEFAULT activos. pruebas_manuales_promptSBX_4.bat: 5 checks (4 aislamiento de red + 1 inspect capacidades) + smoke E2E. 5/5 tests verdes en tests/infra/test_compose_sandbox.py. Bloque SBX completo. |
| 2026-05-21 | SBX.3 | Refactor de pipelines a SandboxClient: admin_script_pipeline (extract→extract_async, inyección client), chart_renderer (async + sandbox_client param), etl_service (sandbox_client inyectado, _execute_fallback_script async), scripts_router (test_proposal + admin_retest usan Depends(get_sandbox_client), eliminado _SANDBOX singleton). Corrección: circular import sandbox_client↔chart_renderer resuelto con lazy import de ChartRenderError. Corrección: textwrap.dedent con f-string multilinea producía IndentationError; reescrito con concatenación explícita. Tests adaptados: test_factory_and_admin_script (extract→extract_async async), test_chart_services (render_chart_from_script async), fixtures test_app+approval_app con Depends(get_sandbox_client)→LocalSandboxClient. 5 tests nuevos en test_sbx3_wiring.py. 0 ocurrencias subprocess.run/Popen/exec( en modules/redaccion/. 253 tests verdes, 1 skipped. SBX.3 completo. |
| 2026-05-21 | SBX.2 | SandboxClient en server/app/core/sandbox_client.py: Protocol SandboxClient + SandboxUnavailableError + HttpSandboxClient (httpx async, retry ConnectError, httpx.Timeout(read_timeout, connect=x)) + LocalSandboxClient (subprocess inline, solo dev/tests) + get_sandbox_client() dep (SANDBOX_MODE env; local automático si TESTING=1). config.py: 5 campos SANDBOX (sandbox_base_url/timeout_seconds_default/connect_timeout_seconds/max_retries_on_connect_error/sandbox_mode). respx añadido a dev deps. .env.example: sección Sandbox documentada. 12/12 tests verdes (server/tests/core/test_sandbox_client.py). Suite redacción: 248 passed, 1 skipped — sin regresiones. |
| 2026-05-21 | SBX.1 | Microservicio aislado `services/script_sandbox/` (FastAPI autocontenido, sin importar nada de `server/app`). sandbox/auditor.py (copia defensiva del `ScriptSecurityAuditor` con su propia WHITELIST_MODULES), sandbox/contracts.py (Pydantic: ExecuteExtractionRequest/Response, ExecuteChartRequest, ExecuteEtlRequest), sandbox/runner.py (`run_subprocess` con Popen+kill explícito+drenado en `finally`, `SubprocessTimeoutError` carga el `Popen` ya reapado), sandbox/wrappers.py (build_extraction/chart/etl_wrapper con `repr()` para literales True/False/None; sentinel `sys.exit(7)` cuando ETL no define `transform`), sandbox/main.py (4 endpoints: GET /health, POST /execute-extraction → JSON, POST /execute-chart → bytes image/png, POST /execute-etl → text/csv). Códigos de error: 422 SCRIPT_AUDIT_FAILED/SCRIPT_EMPTY/ETL_NO_TRANSFORM, 504 SCRIPT_TIMEOUT, 500 SCRIPT_EXECUTION_ERROR (stderr capado 500 chars). pyproject.toml (deps mínimas: fastapi, pydantic, pandas, matplotlib, seaborn, numpy, openpyxl, pdfplumber, uvicorn) + Dockerfile (python:3.12-slim, USER 65534:65534, sin pip-cache). 13/13 tests verdes (test_health, test_execute_extraction ×5, test_execute_chart ×2, test_execute_etl ×2, test_auditor_defense ×1, test_runner_kills_runaway ×2). |
